# back-end-note.md — Tool D (Smart DCA)

> **Đây là LỚP MỎNG.** Nguồn sự thật kỹ thuật là `tool-d-smart-dca.md` (v8, 5.419 dòng, ở gốc repo).
> File này chỉ giữ những thứ spec không có chỗ chứa: phạm vi Giai đoạn 1, quyết định nền tảng,
> Open Questions, Mâu thuẫn, và Lịch sử thay đổi. **Không chép nội dung spec vào đây** —
> chép thành hai bản là nguồn sinh mâu thuẫn số một (Nguyên tắc 10 của quy trình).
> Cập nhật theo Phụ lục B.5: nối thêm có ngày tháng, không ghi đè.

---

## 0. Phạm vi — Giai đoạn 1 [v1.0]

*Nội dung của `template/form-xac-dinh-pham-vi.md`, điền ngày 06/09/2026.*

| Trường | Nội dung |
|---|---|
| Tên project | Tool D — Smart DCA ngắn hạn (Zone Absorption) |
| Ngày xác định phạm vi | 06/09/2026 |
| Vấn đề cần giải quyết | Kiểm chứng có kỷ luật xem chiến lược "hấp thụ tại vùng giá" (Zone Absorption) triển khai theo tranche neo zone trên Binance USDⓈ-M Futures có kỳ vọng dương thật hay không. **Nếu không giải quyết:** rơi vào cái bẫy phổ biến nhất của giao dịch định lượng — thử đủ nhiều cấu hình thì kiểu gì cũng ra một bản backtest đẹp, rồi mất tiền thật vì con số đó chỉ là may mắn được tuyển chọn. |
| Đối tượng người dùng | Một người dùng duy nhất (chủ dự án). Không có nhiều vai trò. |
| Quy mô dự kiến | 1 người. ~100 cặp giao dịch trong pool. Ước lượng ≥150 lệnh/năm (SÀN — dưới ngưỡng này là hỏng từ thiết kế, xem §10.2). |

### 0.1. Danh sách chức năng theo ưu tiên

| Chức năng | Ưu tiên | Ghi chú |
|---|---|---|
| Hạ tầng toàn vẹn phép đo (PHẦN 0d) | **Phải có — TRƯỚC MỌI THỨ** | Không có nó thì mọi con số sinh ra sau đều vô nghĩa (spec dòng 521) |
| Sổ quản trị phép thử (DR-010/011/014) | **Phải có** | Ngân sách N=114, lockbox chạm đúng một lần |
| Cấu hình + tương thích Freqtrade (§0c) | **Phải có** | |
| Bộ test khoá L-Zxx | **Phải có** | Là cách duy nhất biết các ràng buộc còn sống |
| Zone Detection Engine (PHẦN 1) | Nên có — **sau cổng D0-PRE** | |
| Kiến trúc tranche + gate DG1–DG8 | Nên có — sau D0-PRE | |
| Risk Engine độc lập (PHẦN 6) | Nên có — sau D0-PRE | |
| Ablation D0.9 (PHẦN 10) | Nên có — D4, sau cổng D3.5 | |
| Live vốn nhỏ | Chưa cần — D12 | |

### 0.2. Quyết định nền tảng [v1.0]

| Hạng mục | Lựa chọn | Lý do |
|---|---|---|
| Ngôn ngữ/Framework | Python + **Freqtrade** | Spec §0c chỉ định. Dùng `adjust_trade_position` (cốt lõi tranche 2/3) và `custom_stoploss` |
| Môi trường chạy | **Docker Desktop** | Python host là 3.14, Freqtrade cần 3.11–3.13 và không hỗ trợ Windows native. Cũng thoả nguyên tắc 7 (parity) |
| Lưu trữ | **JSONL append-only** (`trial_registry`, `idea_queue`) + SQLite của Freqtrade cho lệnh | Sổ nghiên cứu cần append-only kiểm toán được, không cần DB quan hệ. **Không có ERD / từ điển dữ liệu ở D0-PRE** — chưa có bảng nào |
| Sàn | Binance USDⓈ-M Futures, isolated margin | §0.3 |
| Mã nguồn | Git local + GitHub **private** | Ràng buộc 0d.5 đòi `git_sha` trong mọi bản ghi kết quả |
| Hosting | **Không có.** Chạy local; live (D12) sẽ quyết sau | Bot giữ API key sàn, không hợp với PaaS công cộng |

### 0.3. Gate kiểm tra Giai đoạn 1

- [x] **Nhiều loại người dùng/vai trò?** → **KHÔNG** (1 người). Không dùng `form-phan-quyen.md`.
- [x] **Pipeline đa-agent + tối ưu KPI (Phụ lục A)?** → **KHÔNG.** Ba điều kiện kích hoạt đều chưa đúng. Không dùng `form-sow-kpi.md`.
- [x] **Gate G1–G4 của `api-integration-rules.md`?** → 🔴 **CÓ** (G1, G2, G4 đều CÓ: gọi REST/WebSocket Binance; dùng SDK ccxt qua Freqtrade; poll dữ liệu OHLCV/OI). → ✅ **Đã hoàn thành** (TD-0079, 06/09/2026): `api-integration-rules.md` Mục 4.1–4.4 điền đủ 4 bảng (dịch vụ, endpoint, mã lỗi, ngưỡng) cho cả nhóm đọc dữ liệu (D0-PRE) lẫn nhóm đặt lệnh (D3.5+); `provider-map.md` điền cho Binance USDⓈ-M Futures (không có provider dự phòng — lý do ghi trong file, spec gắn chặt với hành vi riêng của Binance). Bảng nghiệm thu R1-R12 (Mục 5) chưa chạy — chờ tới khi có `api_client` thật gọi mạng lần đầu (Khối 8).

---

## 1–5. UI Behavior / Data Shape / Draft API / Validation / Edge Cases

**N/A.** Tool D không có giao diện người dùng. Giao diện duy nhất là dòng lệnh (8 entrypoint E1–E8)
và Telegram/WebUI dựng sẵn của Freqtrade ở chế độ **chỉ theo dõi** (§0c.1).
Hợp đồng dữ liệu tương ứng nằm ở §8 của spec (Decision Log) và §9c.2 (schema registry) — không chép lại ở đây.

---

## 6. Open Questions [v1.0]

*Điều CHƯA quyết định. Khác với Mâu thuẫn (mục 7) — là điều ĐÃ quyết nhưng xung đột nhau.*

| # | Câu hỏi mở | Ảnh hưởng nếu chưa trả lời | Mức độ |
|---|---|---|---|
| OQ-01 | ✅ **Đã giải** (TD-0041, 06/09/2026) — Ngưỡng **DSR-adjusted expectancy** Nhánh 1 §10.2 | `docs/decisions/DR-D0PRE-03-nguong-dsr-expectancy.md`: công thức `mean(R) − √(2·ln N)·std(R)/√n`, ngưỡng **0,10 R** (suy từ chi phí backtest không thấy × hệ số an toàn). **Blocker B6 gỡ.** L-Z35 sang biến thể best-known (−inf, chưa đo) vẫn FAIL | — |
| OQ-02 | ✅ **Đã giải** (TD-0043, 06/09/2026) — `E_D`, `L_exchange`, `rho`, % lỗ/ngày | `docs/decisions/DR-D0PRE-06-von-va-don-bay.md`: **500 USDT / 3x / 0,375% / 8%**; `tradable_balance_ratio` 0.99→0.5. Chủ dự án chọn 500 dù khuyến nghị ≥ 1.000 — chấp nhận ~20% pool bị L-Z20 từ chối ở zone rộng (`docs/min-notional-check.md`, TD-0082) | — |
| OQ-03 | ✅ **Đã giải** (TD-0042, 06/09/2026) — Thang drawdown | `docs/decisions/DR-D0PRE-04-thang-drawdown.md`: giữ **5/8/20%**, Cấp C Hạng 0. Test khoá `halt == daily_loss_budget_pct` | — |
| OQ-04 | ✅ **Đã giải** (TD-0083, 06/09/2026) — Ngưỡng lọc pool §0.3 (i)–(iv) | `docs/decisions/DR-D0PRE-05-pool-criteria.md`: volume 24h ≥15tr USDT (i), tuổi niêm yết ≥180 ngày (ii), (iii)/(iv) không cần ngưỡng riêng — thoả gián tiếp/kiểm tại thời điểm vào lệnh. Trên dữ liệu thật 06/09/2026 (528 hợp đồng): **102 mã** vào pool giao dịch, 426 vào EXPLORE (gồm BTC/ETH). `config/pool.yaml` đã ghi, tiêu 4 trial B0 (D-0001→D-0004) | — |
| OQ-05 | ✅ **Đã giải** (TD-0084, 06/09/2026) — Mốc chia T0/T1/T2/T3 | `docs/decisions/DR-D0PRE-07-t0-t1-t2-t3.md`: T0=09/04/2024, T1=12/06/2025, T2=29/01/2026, T3=06/09/2026 (hôm nay). T2 neo vào chế độ thị trường THẬT — BTC sập liên tục >30% từ 29/01/2026 (đỉnh sập -53%), khác hẳn CALIB/WFO (dd trung bình -11% đến -14%). `lockbox_seal_1.json` đã niêm phong (510 file OHLCV thật, 102 mã). Phát hiện kèm: sửa bug thật `verify_all_seals()` trỏ sai thư mục (LOCKBOX_DATA_DIR thay vì LOCKBOX_FUTURES_DIR) tồn tại từ TD-0071/72, không bị bắt vì trước đó chưa có seal thật | — |
| OQ-06 | `v_min` (§3.3b) — hiện `null` | Không được điền ở D0-PRE (phải calibrate bằng dữ liệu ở B1), nhưng L-Z15 đòi nó **có trạng thái**, không được "im lặng" → ghi `TUNED_PENDING`. 🔴 **08/09/2026 — đọc kèm MT-15:** câu hỏi này chỉ là *giá trị còn trống*; MT-15 là *công thức sai và điều kiện (c) có 0 dòng code*. Điền một con số vào đây **KHÔNG** đủ để §3.3b chạy đúng.<br>✅ **Đã giải 08/09/2026 (DR-D4-03, `8046a27`)** — `v_min = 1.0`, trạng thái **`FROZEN`** + `frozen_rationale`, rời hẳn `TUNED_PENDING` (spec dòng 3882 chỉ cho TUNED hoặc FROZEN; `TUNED_PENDING` nay chỉ còn hợp lệ khi khoá còn `null`, có test canh). **0 trial.** 🔑 **ĐÓNG BĂNG TẠI CHỖ — giữ trong `tier_b`, KHÔNG chuyển sang `tier_frozen`:** `dof.py:91` tính `N` từ số khoá THẬT trong `tier_b`, nên chuyển ra sẽ hạ `\|tier_b\|` 12→11 ⇒ **N 114→108** ⇒ rào DSR **3,0777→3,0601**, tức **nới chuẩn đánh giá của chính mình**. Giữ ở đây = vẫn bị tính phí cho một bậc tự do mà ta không dùng. **Khác DG5 (DR-D4-02):** DG5 chưa từng được đếm nên đóng băng nó không trừ gì; `v_min` đã được đếm nên đóng băng theo cách đó **thật sự** rút một bậc khỏi mẫu số — cùng động tác, hai hậu quả trái ngược, phân biệt bằng đúng câu hỏi *"thứ này đã nằm trong 12 chưa?"*. 🔴 `1.0` là **CHỖ GIỮ CHƯA CALIBRATE** (mốc trung tính, chọn bằng định nghĩa chứ không bằng kết quả) — đọc kèm MỌI kết quả có `Z0` tham gia. Ba điều kiện mở lại (là VÀ): DR-D4-03 §5 | ✅ |
| OQ-07 | ✅ **Đã giải** (TD-0120, 07/09/2026) — Tiêu chí chọn ý tưởng của quý (§9c.7.4) | `docs/decisions/DR-Q3-2026-tieu-chi-chon-y-tuong.md`, **commit RIÊNG và TRƯỚC** mọi dòng `idea_queue.jsonl` (spec dòng 4935 cấm mở queue trước khi commit tiêu chí). **Ba phần.** (1) **HẠN NGẠCH CHỌN quý 3/2026 = 0**, suy từ ba ràng buộc cứng chứ không phải khẩu vị: Tool D chưa có lệnh live nào (đang ở D2/D12); `mult_edge` (§6.2 hệ số 5) tính trên **50 lệnh live gần nhất** nên trước đó bằng 1.0 **theo định nghĩa** — tín hiệu “edge chết” chưa thể xuất hiện; lockbox có ĐÚNG MỘT và Zone Absorption **chưa chạm**, mà ứng viên mới cần lockbox trên dữ liệu chưa dùng (DR-011, DR-012 Hạng 2, §9c.7.5 — chính là câu hỏi mở #10 của spec, *“trần cứng cho nhịp thay thế chiến lược”*). Khai 0 **không mất gì**: §9c.7.4 cho “tối đa 1/quý” và cấm dồn slot / chọn bù. (2) **Ba điều kiện MỞ LẠI cửa chọn, viết TRƯỚC**, cần một trong ba: `mult_edge = 0.5`; hoặc phán quyết **L2/L3** (§11b.1 — cả hai theo DR-012 đều đòi suất A + lockbox MỚI); hoặc **B3 cạn** mà vẫn cần đổi (DR-012 Hạng 2). Không cái nào xảy ra → quý sau **mặc định lại là 0**, không bàn lại. Đây là điểm cốt lõi: biến *“khi nào thay chiến lược”* từ một quyết định lấy **lúc đang thua** thành ba điều kiện đọc được từ số — đúng mục đích §9c.7.2 (*“thay vì phải nghĩ vội lúc đang thua”*). (3) **Bốn tiêu chí xếp hạng theo THỨ TỰ TỪ ĐIỂN** `TC-Q3-2026-01…04`: cơ chế độc lập với thanh khoản zone → sức mạnh câu “AI TRẢ TIỀN” → bác bỏ được bằng ít dữ liệu → dùng lại được hạ tầng đo (xếp cuối **cố ý**: rẻ không được thắng đúng). **Không dùng điểm có trọng số** vì trọng số là một bộ tham số — sau khi đã nhìn thấy các đơn, người chấm sẽ vặn nó cho ra kết quả mình vốn thích và **không ai đo được lần vặn đó** (§9c.7.4: *“bước CHỌN bị nhiễm, và nó VÔ HÌNH”*). Thứ tự từ điển không có núm để vặn. **Máy kiểm (`TD-0120`)** — nửa thực thi được của ràng buộc mà spec tự nhận là không máy nào kiểm nổi, cùng thủ thuật §12d (*“mọi câu phải trích một con số”*): `selection_reason` phải **trích ít nhất một mã TC có thật của đúng quý**; bốn ca sổ bẩn = không trích mã / trích mã không có thật / quý chưa niêm phong tiêu chí mà đã chọn / quý khai hạn ngạch 0 mà có SELECTED. Ba ca sau đều **fail-closed**. Còn lại: **cửa NỘP mở** (trần 10 đơn/quý), phiên sinh ý tưởng phải **bịt mắt** (không xem kết quả Tool D — DR-009) | — |
| OQ-08 | ✅ **Đã giải** (TD-0085, 06/09/2026) — Backup lockbox ra ngoài git | `docs/decisions/DR-D0PRE-08-lockbox-backup.md`: chủ dự án chọn thư mục khác trên cùng máy (`E:\lockbox-backup-tool-d\`) để bắt đầu — chưa chống mất máy vật lý, nâng cấp cloud để sau. Test khôi phục THẬT đã chạy 1 lần: backup 510 file → verify PASS → khôi phục vào thư mục tạm mới → verify PASS độc lập | — |
| OQ-09 | ✅ **Đã giải** (TD-0196, 09/09/2026) — Ngưỡng Circuit Breaker cho Risk Supervisor (§6.6) | Chốt đúng đề xuất ban đầu, không đổi số: **5 lỗi liên tiếp**, backoff **1s→2s→4s→…→60s** — ghi ở `api-integration-rules.md` v1.1 (dòng 67, 101), implement ở `src/tool_d/risk_supervisor.py` (43 test khoá, TD-0196). Đọc qua ENV `TOOLD_BREAKER_THRESHOLD` (tham số VẬN HÀNH, không phải tín hiệu — không tính DOF). 🔴 **Đính chính 13/09/2026:** dòng này lẽ ra phải đổi sang ✅ ngay từ 09/09/2026 nhưng bị bỏ sót — bảng vẫn ghi 🟡 hơn 4 ngày sau khi đã chốt (đúng bẫy N12 mục 3: một dòng trạng thái cũ trông giống sự thật hơn là bằng chứng). Phát hiện lại khi rà `TD-0209` (heartbeat/cảnh báo tiến trình `stopped`/idle) đang khai phụ thuộc **đúng OQ-09 này** dù chính ghi chú của TD-0209 tự nhận không cùng phạm vi §6.6 (giám sát margin/thanh lý, không giám sát "tiến trình có đang chạy") — phụ thuộc đó là ghi nhầm lúc tạo việc, đã gỡ ở `TASKS.md` (xem TD-0209). Ngưỡng/kênh cảnh báo thật sự TD-0209 cần là một quyết định RIÊNG, mới — chốt 13/09/2026 trong chat: kênh **Telegram thật**, ngưỡng **5 phút** không nhịp tim, dùng chung tiến trình giám sát với TD-0241 | — |
| OQ-10 | Số lệnh/năm thật (sàn 150, §10.2) — ước lượng tay (`docs/estimate-trades-per-year.md`, TD-0081) cho khoảng **129–1.652 lệnh/năm** (long-only, 102 mã), vắt ngang sàn 150, không kết luận nhị phân được bằng suy luận | Không chặn D0-PRE. Chặn D4 nếu số đo thật ở D1 (H1-D + zone detection trên CALIB) < 150 | 🟡 Đo lại thật ở D1, không coi ước lượng tay là kết luận cuối |
| OQ-11 | ✅ **Đã giải** (07/09/2026) — Dashboard xác định tiến độ **D0→D12** bằng cách nào? (phía front-end nêu là `OQ-FE-04`) | **CHỐT PA3 (lai).** Phát hiện then chốt: **12 chặng KHÔNG cùng một loại** — `D0` là điều kiện về **Tool A** (bot khác, repo khác → không bao giờ kiểm được từ đây); `D0-PRE`/`D3.5`/`D4`/`D9.5` là **cổng**, có tiêu chí đạt bằng số chốt trước và đẻ ra hiện vật không giả được; `D1`/`D2`/`D3`/`D5–D9`/`D10`/`D11`/`D12` là **pha phát triển**, "xong" là phán xét của người. Ép một cơ chế đồng phục lên cả 12 sẽ buộc 7 pha phải có "khoá" mà **không có tiêu chí máy nào gác** — biến lời khai của người thành một trường JSON trông như bằng chứng máy, đúng thứ §0d.6 (*tách "đã audit" khỏi "đã đạt"*) và L-Z51 sinh ra để chặn. **Ba phần:** (1) **KHÔNG có trường `current_stage`** — vị trí suy ra từ cổng đã chứng minh, không khai trực tiếp. (2) Bốn cổng có **khoá write-once** trong `runtime_state.json`, lệnh đóng cổng **tự kiểm bằng máy rồi mới ghi** (như `close_d0_pre_gate()` từ chối khi sổ bẩn): `d0_pre_complete` ✅ đã có (TD-0086) · `d3_5_complete` ← bản ghi Δ_R hợp lệ đã commit · `d4_complete` ← đếm được **đúng 18 trial `B2` CONSUMED** · `d9_5_complete` ← `lockbox_access.log` có bản ghi hợp lệ (L-Z13 pass). Khoá chưa tồn tại → **CHƯA QUA** (fail-closed), không bao giờ mặc định là đã qua. (3) Pha phát triển và `D0` **không đánh dấu** — hiện trạng thái thứ ba *"chưa có mốc máy đọc được"*; câu trả lời chính của trang Tiền thật đổi sang **"cổng gần nhất chưa qua"**. **Đã loại** hướng suy tiến độ từ dấu ✅ trong `TASKS.md`: chính N12 ghi việc này *"đã gây ít nhất 3 lần một task bị đánh dấu ✅ giả dù chưa có code thật"*. Front-end đã thi hành phần của mình (FE-0046, 07/09/2026); backend còn ba lệnh đóng cổng (TD-0088). 🔴 **ĐÍNH CHÍNH 14/09/2026 — tiêu chí `d4_complete` ở trên ĐÃ BỊ THAY, giữ nguyên chữ cũ làm lịch sử:** câu *"đếm được **đúng 18** trial `B2` CONSUMED"* nay không còn hiệu lực. `DR-D4-11` (`79db3a8`, thi hành `TD-0236`/`5910f74`) thay nó bằng **tiêu chí HIỆN VẬT**. Lý do KHÔNG phải con số sai (`DR-D4-01:15` cấp 9 chứ không 18) — mà là `DR-D4-10:211-213` đã hạ **7/9 suất xuống thống kê MÔ TẢ**, nên MỌI cổng đếm trial đều **PASS kể cả khi toàn bộ số suất đó đều là loại mô tả và `Z0-T1` — cấu hình DUY NHẤT D4 còn phán quyết được — chưa từng chạy**. Tức PASS RỖNG nằm trong chính ĐỊNH NGHĨA cổng; hạ 18 xuống 9 không sửa được nó. Giữ nguyên chữ cũ vì nó ghi lại thứ chủ dự án đã NHÌN THẤY khi quyết PA3 ngày 07/09 | — |
| OQ-12 | **TD-0114 (L-Z49 CRITICAL, D7) bị chặn — thiếu ba phụ thuộc chưa có code**: (1) `trend_dir` (Phần 2, §2.2) — cần cho DG2 và DG6 điều kiện A/C, Phần 2 **chưa được viết** ở D1 (D1 mới có Phần 1 — zone detection/ZSS); (2) dữ liệu funding rate tích luỹ theo trade — cần cho DG7 (§4c); (3) xác nhận price-action §3.3b (rejection wick hoặc RSI(14,1H) divergence dùng `touch_count` đã có) — điều kiện bắt buộc trước khi mở tranche 1 "thật", cũng chưa có code | Không chặn D1 (đã đóng, tag `d1-complete`). **Chặn D4** — TD-0114 phải PASS trước khi gỡ blocker B2 sang D4, và TD-0115/TD-0117 (Khối 13) đều phụ thuộc TD-0114 | 🟡 Chốt thứ tự xây trước khi tiếp tục Khối 13 |
| OQ-13 | **Kênh 2 — đề xuất đổi tham số khi hệ thống ĐÃ CHẠY: luật có đủ, máy canh chưa có gì.** §12c.3 + §12d đã quy định rất chặt (chỉ đọc đầu ra `periodic_report.py`, cấm đọc DB lệnh thô / tự tính chỉ số mới; **mọi câu phải trích một con số cụ thể**, đề xuất không có số → loại toàn bộ; tự khai hạng L1/L2/L3; nguồn kích hoạt ∈ {HEALTH, GATE_FAIL, CONDITIONAL_UNFREEZE}; tham số Cấp C **không có ô nhập**; Cấp B đòi HAI điểm quyết định liên tiếp; tốn 1 trial từ B3). Nhưng **`L-Z26` 🔴 CRITICAL chưa có một dòng code nào** (`grep` toàn repo: 0 kết quả) và **chưa có sổ ghi đề xuất**. | Spec §12d.4 (L-Z26) vs code hiện có | 🟡 Không chặn D2 — bây giờ chưa có báo cáo định kỳ thật nào để đọc, nên chưa có gì để đề xuất. **Phải xong trước D11 (dry-run)**, là lúc kênh này bắt đầu được dùng thật \| **Chưa chốt — phác thảo thiết kế đã bàn 07/09/2026, chờ duyệt khi tới lượt:** sổ thứ ba `registry/param_change_proposals.jsonl`, append-only như hai sổ kia. Điểm thiết kế then chốt: **biến ràng buộc chống chém gió thành cấu trúc dữ liệu** — thay vì một ô lý do viết văn xuôi rồi *hứa* sẽ trích số, đề xuất là một **danh sách luận điểm**, mỗi luận điểm bắt buộc `{chi_so, gia_tri, dai_ky_vong, tham_so_tro_toi}`; câu không trích được số thì **không tồn tại được về mặt cấu trúc**, không cần ai nhớ luật để gạch bỏ nó. Kèm `bao_cao_hash` (buộc gắn vào **đúng một** bản báo cáo — chặn việc gom số nhiều kỳ để kể chuyện), `nguon_kich_hoat`, `cap_tham_so`, `tu_khai_hang`, `trial_id` trừ từ B3. Máy **từ chối ghi** khi: Cấp C, thiếu hạng, hoặc danh sách luận điểm rỗng. **Cạm bẫy phải chặn cùng lúc:** để LLM đề xuất một **bộ** tham số tối ưu là `hyperopt` viết bằng tiếng Việt — cùng hành vi tìm kiếm, mất phần đếm được số lần thử. Spec chỉ cho **một tham số, một điểm quyết định**. Xem thêm **MT-12** (cùng nguyên tắc “không gian tìm kiếm phải bị chặn trước”). ✅ **Đã giải** (TD-0125, 07/09/2026) — phác thảo trên được thi hành **nguyên vẹn**, không đổi thiết kế; chi tiết máy kiểm ở **MT-13** |

---

## 7. Mâu thuẫn & Cần làm rõ [v1.0]

| # | Mâu thuẫn phát hiện | Giữa cái gì với cái gì | Mức độ | Cách giải quyết | Ngày | Người quyết |
|---|---|---|---|---|---|---|
| MT-01 | Sổ trial vừa phải **append-only tuyệt đối** (dòng 3762: *"phát hiện sửa dòng cũ = toàn bộ registry mất hiệu lực"*) vừa phải **chuyển trạng thái** RESERVED→CONSUMED/REFUNDED tại chỗ (dòng 3738, DR-014). Không thể làm cả hai | Nội tại spec: §9c.2 quy tắc 2 vs DR-014 | 🔴 Chặn TD-0050 | **ĐÃ CHỐT:** `trial_registry.jsonl` là **sổ nhật ký sự kiện** — mỗi dòng một sự kiện (RESERVE / SEAL / CONSUME / REFUND / CONTAMINATE) mang cùng `trial_id`. Trạng thái là **bản chiếu** tính lại khi đọc, không phải trường trên đĩa. Giữ nguyên append-only tuyệt đối, và được thêm dấu vết ai-làm-gì-lúc-nào | 06/09/2026 | Chủ dự án |
| MT-02 | *"KHÔNG ĐƯỢC CHẠM DỮ LIỆU TRƯỚC KHI D0-PRE XONG"* (dòng 4464) vs chính D0-PRE lại chứa việc "chốt pool ~100 mã" (dòng 4447) và "verify độ dài lịch sử OI" (dòng 4457) — hai việc bắt buộc gọi API Binance | Nội tại spec: §12 với chính nó | 🔴 Chặn TD-0080 | **ĐÃ CHỐT:** "chạm" = **đánh giá cấu hình** trên CALIB / WFO / LOCKBOX (theo DR-014 mục 2, dòng 3490–3494). Đo thông tin mô tả không sinh chỉ số chiến lược nên không phải "chạm", nhưng vẫn ghi sổ dòng `CTRL` (0 trial), bắt buộc có assert khoảng thời gian, và **bị đẩy xuống Khối 8** — sau khi tầng chống nhiễm đã sống | 06/09/2026 | Chủ dự án |
| MT-03 | Hai nguồn sự thật cho ngân sách B3: `tool_d_config.yaml` có `_budget_remaining_B3: 20` *"hệ thống giảm, không sửa tay"* (dòng 2338) vs DR-014 tính Khả dụng từ registry (dòng 3484–3485) | §6.9.5 vs DR-014 | 🟡 Phải giải trước TD-0051 | **ĐÃ CHỐT:** **registry là nguồn sự thật**. Khoá YAML đổi thành `_budget_remaining_B3: null  # derived — xem registry`, loader **raise** nếu nó khác `null`. Chính spec cảnh báo đây là cơ chế đã gây đợt lệch số của v5 (dòng 3315–3317) | 06/09/2026 | Chủ dự án |
| MT-04 | `zone_width` xuất hiện hai chỗ với hai số phận: comment `"13?"` trong `tier_b` (dòng 2351–2353) vs `0 (hoặc 1)` trong bảng DOF (dòng 3210) | §6.9.5 vs DR-010 | ✅ Đã giải (TD-0030) | **CHẾT** — `docs/decisions/DR-D0PRE-01-zone-width-verify.md`: `zone_width_pct` LUÔN BẰNG chính xác `0.6×ATR/price` theo cấu trúc §1.1, ngưỡng lọc là `0.5×ATR/price` — với cùng mốc ATR/price, `0.6≥0.5` là hằng đẳng thức, không phải điều kiện. Đã xoá hẳn khỏi `tool_d_config.yaml`, không để lại comment. `N_ĐĂNG_KÝ = 114` | 06/09/2026 | Claude (phân tích cấu trúc, không cần dữ liệu) |
| MT-05 | Quy trình vibe-code Giai đoạn 4 viết cho web app trên Render (Image Preview, health check `/health`, DB staging) — không áp thẳng được cho bot trading local | `QUY-TRINH-VIBE-CODE.md` GĐ4 vs bản chất project | 🟡 | **ĐÃ CHỐT:** ánh xạ ghi ở `CLAUDE.md` mục N11. Giữ "cùng 1 Dockerfile"; thay Image Preview bằng image digest local; "URL preview" → Binance testnet (D3.5 / D10) → dry-run (D11); production → D12 vốn nhỏ. **GĐ4 không áp cho D0-PRE** | 06/09/2026 | Chủ dự án |
| MT-06 | L-Z25 đòi grep cả *"lịch sử lệnh"* (dòng 510) để chứng minh chưa từng chạy hyperopt — nhưng lịch sử shell nằm ngoài repo và shell trong container là ephemeral | Yêu cầu test vs khả năng kỹ thuật | 🟡 | Kiểm được: `git grep` worktree + `git log -S hyperopt --all` (bắt cả file đã xoá) + `runs/cmd_audit.log` do wrapper tự ghi. **Không** kiểm được: người gọi `docker run … freqtrade hyperopt` thủ công. Rủi ro tồn dư — ghi `docs/research-log.md`, cùng loại thừa nhận với dòng 3317–3320 | 06/09/2026 | Chủ dự án |
| MT-07 | Khối xuất xứ spec quy định **đúng 7 khoá** (dòng 597–616), nhưng chạy trong Docker thì phiên bản Freqtrade nằm trong image chứ không nằm trong git → không truy được về sau | §0d.5 vs quyết định dùng Docker | 🟡 | **ĐÃ CHỐT:** thêm khoá thứ 8 `runtime_image_digest`. Không phá L-Z40 (test đòi *đủ* 7 khoá, không cấm khoá thứ 8) | 06/09/2026 | Chủ dự án |
| MT-08 | `registry.py` **chưa thi hành** phần kế toán `CTRL` đã chốt ở MT-02, ba lỗi: (1) `TrialProjection` không lưu `budget_line` nên `n_used()`/`n_reserved()` cộng cả dòng CTRL; (2) `reserve()` chặn `contribution < 1` nên dòng *"CTRL 0 trial"* mà MT-02 yêu cầu **không ghi được**; (3) `reserve()` kiểm ngân sách cho mọi dòng nên khi N cạn thì **không ghi được điểm kiểm soát** — đúng lúc sắp go-live là lúc cần kiểm tra tái lập nhất | Code `src/tool_d/ledger/registry.py` vs §0d.4 dòng 594 + DR-014 §2 dòng 3490 + dòng 3608 + dòng 3715 + quyết định **MT-02** | ✅ **ĐÃ THI HÀNH 07/09/2026** (TD-0130, commit `5d5ae82` + `e6a67f1`). Trước đó: 🟡 chưa gây thiệt hại (sổ 4 trial đều B0, chưa có dòng CTRL nào) nhưng phải sửa trước dòng CTRL đầu tiên, tức trước D3.5 — đã kịp | **ĐÃ CHỐT — phương án A + cơ chế xác thực:** (1) `TrialProjection` mang `budget_line`; `n_used()`, `n_reserved()` và phép kiểm ngân sách trong `reserve()` **loại CTRL ra**; giữ nguyên bất biến `contribution >= 1` (không mở đường mức 0). (2) CTRL phải khai thuộc **một trong hai dạng, máy kiểm chứ không nhận lời khai** (cùng triết lý DR-014 §3): *tái lập* (§0d.4) — có `reproduces_trial_id` và `config_hash`/`params_frozen_hash` **bằng đúng** bản ghi trial đó; *đo thước* (D3.5 Bước 1, MT-02) — đầu ra giới hạn cứng vào danh sách trắng không chứa chỉ số hiệu năng nào (spec dòng 3605–3607). Không thoả dạng nào → **từ chối ghi CTRL** (fail-closed), tính như trial thường. (3) Test khoá mới **L-Z56**: 1 trial B1 + 1 trial CTRL đều CONSUMED → `n_used() == 1`; và `reserve()` dòng CTRL vẫn thành công khi ngân sách đã cạn. **Vì sao cần (2):** L-Z55 một mình KHÔNG đủ cho CTRL — một điểm kiểm soát hợp lệ *có* chạm CALIB nên không vi phạm timerange; chỗ phân biệt phải là **ĐẦU RA**, không phải dữ liệu chạm. **Quy mô sai số nếu bỏ qua** (suy từ hằng số spec, không cần dữ liệu): điểm kiểm soát chạy sau mỗi lần một tham số đổi trạng thái, Tầng B có 12 tham số, trần B3 = 20 → cận trên ~20–25 trial trên 114 (**~18–22% ngân sách**); nguy hơn con số là **động cơ chạy ngược** — càng kỷ luật càng bị phạt, dẫn tới bỏ điểm kiểm soát. Méo mó DSR không đáng kể (N 114→134 nâng ngưỡng √(2·ln N) +1,7%)  **THI HÀNH (TD-0130, 07/09/2026):** cả ba mục trên đã có code + test khoá (`tests/lock/test_td0130_ctrl_khong_tinh_vao_n.py`, **18 test**). Hai chỗ làm **khác/chặt hơn** phương án đã duyệt, ghi rõ để không ai tưởng là làm sai: (a) `CTRL_OUTPUT_ALLOWED` là danh sách **CHO PHÉP** (`price_delta`/`tranche_index`/`direction`) chứ không phải danh sách **CẤM** các chỉ số hiệu năng như phương án viết — đúng chữ spec dòng 3605-3607 *"bộ chạy cưỡng chế danh sách này"*; lý do: danh sách cấm chỉ chặn được những tên đã nghĩ ra trước, khoá nào chưa nghĩ tới sẽ lọt, tức nó không fail-closed. (b) Thêm ca phương án chưa nêu: **khai CẢ HAI dạng → từ chối** — khai chồng là chừa đường lách sang dạng dễ kiểm hơn. Lời khai CTRL được ghi vào chính sự kiện RESERVE để audit về sau tự đối chiếu lại được, không phải tin rằng cửa ghi đã kiểm. **KHÔNG đụng `audit_checks.py`** — `check_lz11` gọi `ledger.n_used()` nên tự đúng theo. 🐛 **Bằng chứng cho chính bài học MT-03 (hai nguồn sự thật), lần này do phiên viết code gây ra:** commit đầu (`5d5ae82`) thêm hai khoá `reproduces_trial_id`/`ctrl_output_whitelist` vào sự kiện RESERVE nhưng **quên `trial_event.schema.json`** — schema có `additionalProperties: false`, nên dòng CTRL **đầu tiên** ghi vào sổ thật sẽ vi phạm chính schema mà audit dùng. **735 test xanh vẫn không bắt được**, vì `test_registry_schemas.py` chỉ đối chiếu FIXTURE GÕ TAY, không dòng nào đối chiếu đầu ra THẬT của `reserve()`; còn sổ thật thì chưa có dòng CTRL nào để nổ. Vá ở `e6a67f1`: schema biết hai khoá mới, **và** thêm 3 test đối chiếu đầu ra thật của `reserve()` với chính schema audit dùng — đóng cả LỚP lỗi, không chỉ ca vừa gặp. Ràng buộc liên-dòng (hash phải khớp bản ghi gốc) **cố ý KHÔNG nhân đôi** vào schema: schema không diễn đạt được, và nhân đôi luật là tự tạo nguồn sự thật thứ hai. **Bằng chứng:** Docker 744 (baseline đo lại ngay lúc so) + 18 = **762 passed, 0 failed**; E6 sổ thật exit 0, `L-Z11 N_ĐÃ_DÙNG=4` không đổi, sổ vẫn 12 dòng; hộp cát trên **bản sao sổ thật**: khai suông/`pnl_abs`/hash lệch → TỪ CHỐI và sổ không dài thêm, đo thước sạch → ghi được với `n_used` vẫn 4 và `available` vẫn 110, ngân sách cạn → B1 raise `BudgetExhaustedError` còn CTRL **vẫn ghi được**. | 07/09/2026 | Chủ dự án |
| MT-09 | **Trùng tên test khoá `L-Z56`.** Spec đã gán `L-Z56` cho test *"bộ chạy ablation TỪ CHỐI khởi động khi chưa có kết quả cổng D3.5"* (dòng 278 và 3692: *"Test khoá cứng — xem L-Z56 → L-Z58"*). Quyết định **MT-08** (07/09/2026) lại gán `L-Z56` cho test kế toán CTRL (`n_used()` loại CTRL + `reserve()` CTRL vẫn chạy khi ngân sách cạn) — hai test khác hẳn nhau, cùng một mã | Spec dòng 278/3692 vs MT-08 mục (3) | 🟡 Chưa gây thiệt hại (chưa test nào được viết) nhưng phải sửa **trước khi viết test đầu tiên mang mã đó**, nếu không sổ test có hai `L-Z56` và mọi câu "L-Z56 PASS" thành mơ hồ | **ĐÃ CHỐT (07/09/2026) — phương án (c): không cấp phát mã `L-Z` nào, đặt tên theo việc TD sinh ra test — `test_td0087_ctrl_khong_tinh_vao_n.py`.** Rà lại phát hiện: **21/21 test mang mã `L-Z` trong `tests/lock/` đều truy được về đúng mã spec đã gán**; ba test khoá do triển khai tự nghĩ ra (`test_td0057_run_audit_wired.py`, `test_td0072_verify_seal_wired.py`, `test_td0090_d0_pre_gate_wired.py`) đều đã mang mã `TD-xxxx`, không mã `L-Z` nào. Tức repo **đã có sẵn quy tắc bất thành văn**: `L-Z` là mã do spec cấp, test tự nghĩ mang mã `TD-xxxx` — chưa từng bị phá cho tới đề xuất `L-Z56` trong MT-08. Bác bỏ (a) `L-Z59`: đó là số kế tiếp mà spec (đã cấp liền `L-Z52`→`L-Z58` ở v8) nhiều khả năng sẽ dùng tiếp ở v9 — chọn nó là tự gài lại đúng va chạm vừa xảy ra, chỉ dời sang lúc đắt hơn (test đã viết, đã vào CI, đã được nhắc ở nhiều file). Bác bỏ (b): phải sửa spec để nhường chỗ cho một test không phải spec sinh ra — sai hướng, spec là nguồn sự thật cao nhất (N1). Việc đổi tên: `TD-0087` | 07/09/2026 | Chủ dự án |
| MT-10 | Khối `evidence` trong `registry/runtime_state.json` (TD-0086) **trộn chữ gõ tay với số máy đo**: `lock_tests` và `periodic_report` là chuỗi hằng viết thẳng trong `close_d0_pre_gate()` (kể cả con số *"215 passed"* và ngày tháng), chỉ `trial_ledger_audit` do code sinh (`audit_text.splitlines()[0]`). Docstring của E6 có nói rõ giới hạn này, nhưng **bản thân file JSON thì không** — người/máy đọc lại không phân biệt được "đo được" với "người khai" | `close_d0_pre_gate()` vs §0d.6 (*tách "đã audit" khỏi "đã đạt"*) + L-Z51 (*"đạt chỉ khi có bằng chứng trên đĩa từ lần chạy THẬT"*) | 🟡 Không sai số nào ở D0-PRE (cả ba điều kiện đều đã chạy thật, có ghi `docs/research-log.md`), nhưng dashboard đọc `evidence` dưới luật **FE-L03** *"chỉ xanh khi có bằng chứng máy"* → đang trình bày chữ gõ tay như bằng chứng máy. Phải sửa **trước khi cổng thứ hai dùng lại khuôn này** (ba cổng của OQ-11) | **ĐÃ CHỐT — gắn nhãn nguồn cho từng mục bằng chứng:** mỗi mục trong `evidence` đổi thành `{"nguon": ..., "noi_dung": ...}` với `nguon` nhận đúng một trong hai giá trị `do-duoc` hoặc `nguoi-khai`. `do-duoc` **chỉ** dành cho chuỗi do code sinh trong **chính lần chạy đó**; mọi thứ khác là `nguoi-khai`. Dashboard hiện hai màu khác nhau và **không** tính `nguoi-khai` là bằng chứng máy cho FE-L03. Áp cho cả ba cổng mới của OQ-11. **Không hồi tố** bản ghi `d0_pre_complete` đã ghi (bất biến, không ghi lại) — bản đọc coi **mục thiếu nhãn `nguon` là `nguoi-khai`** (fail-closed), nên không cần khoá phiên bản | 07/09/2026 | Chủ dự án |
| MT-11 | **Trần chọn ý tưởng ra khỏi Idea Queue ghi hai số khác nhau**: §9c.7.4 ràng buộc (2) (dòng 4108) *“Chọn tối đa **1 ý tưởng/quý** ra khỏi queue”* vs L-Z17 / §9.3 / DR-010 (dòng 3888) *“Số slot NGÂN SÁCH A đã tiêu **≤ 5/quý**”*, ghi rõ là dùng chung Tool A + Tool D. Code thi hành **5** (`BUDGET_A_SLOTS_PER_QUARTER_MAX`, `src/tool_d/ledger/audit_checks.py`). Kèm theo: L-Z17 vượt trần đang **chặn chạy backtest** theo H16 (dòng 4347 *“Fail → chặn chạy, không cảnh báo suông”*) — tức chuyện CHỌN ý tưởng lại khoá chuyện ĐO | Nội tại spec: §9c.7.4 vs §9.3/DR-010; và H16 vs bản chất con số 5 | 🟡 Chưa gây thiệt hại (`registry/idea_queue.jsonl` **0 dòng**, chưa có bản ghi `SELECTED` nào) nhưng phải chốt **trước khi mở queue**, gắn với OQ-07 | **ĐÃ CHỐT (07/09/2026) — hai phần.** (1) **Con số: 5/quý, dùng chung Tool A + Tool D**; dòng “1/quý” hiểu là **nhịp khuyến nghị**, không phải trần cứng. Giữ nguyên `BUDGET_A_SLOTS_PER_QUARTER_MAX = 5`, không sửa code con số. (2) **Thi hành: chỉ đếm + cảnh báo, KHÔNG chặn cứng** — `WARN_ONLY_CODES = {“L-Z17”}` trong `audit_checks.py`, `run_audit()` loại nó khỏi phép tính exit code (vẫn đếm vào `fail` để giữ bất biến `ok+fail+unmeasured == total` của `audit_line()`, và vẫn in nhãn **⚠️ VƯỢT TRẦN**). **Đây là sửa có ý thức dòng H16 (4347) cho RIÊNG L-Z17**, không phải cho cả bảng: số 5 là kỷ luật CON NGƯỜI (chọn ý tưởng nào đáng thử), **không chảy vào N/DSR** — vượt trần không làm sai một con số nào, chỉ nghĩa là đã thử nhiều giả thuyết hơn dự tính; và đòn bẩy cũ **phạt sai chỗ**. 🔴 **`L-Z16` GIỮ NGUYÊN chặn cứng** và không bao giờ được đưa vào `WARN_ONLY_CODES` — đó là **chống nhiễm dữ liệu** (ý tưởng nghĩ ra sau khi xem kết quả Tool D), không phải kỷ luật cá nhân. **Rủi ro đã chấp nhận:** §9c.7.4 nói thẳng bước CHỌN là nơi việc tự lừa mình xảy ra và **nó vô hình**; đặt cảnh báo mềm lên đúng cái van canh chỗ đó thì cảnh báo dễ bị bỏ qua đều đặn — chủ dự án chọn đánh đổi này có ý thức. **Hệ quả phải nói rõ, không để lặng lẽ:** `close_d0_pre_gate()`/`close_d1_gate()` từ chối đóng cổng khi `audit_exit != 0`, nên từ nay **vượt trần 5 cũng không chặn đóng cổng** nữa (đã ghi vào docstring hai hàm đó). **Phát hiện phụ ghi kèm:** trần **nhập** queue 10 ý tưởng/quý (dòng 4095) **hiện chưa có test khoá nào thi hành** — mới là văn bản; chưa code, ghi nhận ở đây để không tưởng nhầm là đã có máy canh. Thi hành: **TD-0118**, test khoá `tests/lock/test_td0118_lz17_canh_bao_khong_chan.py` (5 test — gồm một test canh riêng để `L-Z16` không bị nới lây); Docker 492 → 497 passed. 🆕 **Cập nhật 07/09/2026 (TD-0124):** phần ghi nhận *“trần **nhập** queue 10 ý tưởng/quý hiện chưa có test khoá nào thi hành — mới là văn bản; chưa code”* **đã được lấp**: `check_td0124_tran_nhap_don_moi_quy()` + cửa ghi `submit_idea()`, **chặn CỨNG** (KHÔNG vào `WARN_ONLY_CODES`). Lý do chặn cứng trong khi L-Z17 chỉ cảnh báo: trần **CHỌN** là kỷ luật con người và không chảy vào N/DSR; trần **NHẬP** thì có — chi phí sinh ý tưởng bằng LLM ≈ 0, nên tỉ lệ chọn thấp biến bước CHỌN thành nơi khai thác dữ liệu quy mô lớn, và bước chọn thì không ai ghi sổ cho (§9c.7.4). Đây là chống nhiễu, cùng loại L-Z16 | 07/09/2026 | Chủ dự án |
| MT-12 | **Tờ đơn ý tưởng (§9c.7.3) chỉ có ba câu văn xuôi kinh tế** (mechanism / who_pays / durability) — đủ để LỌC RÁC, **không đủ để CHẠY**. Khoảng trống từ *“Zone Absorption có edge”* tới một phép thử chạy được đang lấp bằng trí nhớ — đúng loại chỗ sinh ra số sai mà PHẦN 0d tồn tại để chặn | Spec §9c.7.3 (đủ cho §0.1) vs yêu cầu vận hành thật: một ý tưởng được CHỌN phải trở thành cấu hình chạy được, có ngưỡng bác bỏ, và trừ đúng số trial đã khai vào N=114 | 🟡 Chưa gây thiệt hại (`idea_queue.jsonl` **0 dòng**), phải xong **trước ý tưởng đầu tiên được CHỌN** | **ĐÃ CHỐT (07/09/2026) — SỬA ĐẶC TẢ §9c.7.3, mô hình HAI CỬA.** Ghi ở đây theo đúng tiền lệ **MT-07** (thêm khoá thứ 8 vào khối provenance): quyết định sống trong `back-end-note.md`, **không chép nội dung spec sang file khác** (N1). **Cửa NỘP** (rẻ, trần 10 đơn/quý) — thêm **một** trường `phep_thu_du_kien`: *“đo cái gì, so với cái gì, thì biết ý tưởng này SAI?”*, mức phác thảo, chưa cần số. Mục đích duy nhất: loại ý tưởng **không thể bác bỏ được**, ở cửa rẻ nhất. **Cửa CHỌN** (1 lần/quý, đúng lúc tiêu 1 suất Ngân sách A) — bắt buộc bốn trường `tin_hieu` (công thức tính được + khung thời gian), `quy_tac` (nối vào chỗ nào của chiến lược), `nguong_bac_bo` (con số nói ý tưởng SAI, viết TRƯỚC khi chạy), `so_bien_the` (khai trước số cấu hình sẽ thử). Thiếu bất kỳ trường nào → **sổ bẩn** (`TD-0119a`), fail-closed, dùng đúng khuôn `selection_reason` đã có sẵn ở §9c.7.4. **Vì sao ĐẶT Ở CỬA CHỌN, không phải cửa NỘP** (đã bác bỏ phương án “tất cả ở cửa nộp”): (a) trần nộp 10 / trần chọn 5 → bắt viết đủ phép toán cho cả 10 là **làm 10 lần công cho 1 lần dùng**, và bóp chính nguồn cung mà hàng chờ sinh ra để nuôi; (b) ý tưởng có thể nằm chờ vài quý → số viết lúc nộp sẽ lạc hậu, viết lại đúng lúc sắp chạy thì mất hết tác dụng “viết trước”; (c) điều kiện của tiền-đăng-ký là **“viết trước khi thấy kết quả của CHÍNH phép thử này”**, không phải “trước khi vào hàng chờ” — nên cửa CHỌN vẫn thoả. **Quy ước nối hai sổ (phần quan trọng hơn cả bốn trường):** trial sinh ra từ một ý tưởng trong hàng chờ ghi `hypothesis_slot = <idea_id>` (dạng `IQ-xxxx`); audit đếm số trial **CONSUMED** mang slot đó, phải **≤ `so_bien_the`** đã khai (`TD-0119b`). Không có phép đối chiếu này thì `so_bien_the` chỉ là **lời khai trông như bằng chứng máy** — đúng loại sai lầm **MT-10** vừa phải sửa; và nó đánh trúng nút thắt thật: N=114, hết là hết. **Mã test:** `TD-0119a`/`TD-0119b`, **không** cấp mã `L-Z` mới — theo quy tắc đã chốt ở **MT-09** (mã `L-Z` chỉ do spec cấp). **Ghi kèm để không hiểu nhầm về sau** (§12d.2, khi LLM tham gia sinh ý tưởng): ranh giới hợp lệ **không phải “ai bấm nút”** mà là ***“không gian tìm kiếm có bị chặn TRƯỚC không”*** — người duyệt được *nội dung* đề xuất, **không** duyệt được *không gian tìm kiếm* đã sinh ra nó. Vì vậy “LLM đề xuất, người duyệt” **một mình KHÔNG đủ**; cái làm nó hợp lệ là LLM chỉ nhìn một tập đầu vào đã cố định trước. Thi hành: **TD-0119** (`f1a0409`) — schema + 2 phép kiểm + 10 test khoá; Docker **615 passed, 0 failed** | 07/09/2026 | Chủ dự án |
| MT-13 | **Kênh 2 (§12c.3/§12d) có luật rất chặt nhưng KHÔNG có sổ và KHÔNG có máy canh — `L-Z26` 🔴 CRITICAL có 0 dòng code.** Không phải mâu thuẫn chính sách: luật đã đúng và đủ. Cái sai là **chưa có gì thi hành nó**, y hệt hình dạng của MT-02 (chính sách đã chốt, code chưa theo) | Spec §12d.4 (L-Z26) vs code hiện có | 🟡 Không chặn D2 (chưa có báo cáo định kỳ thật nào để đọc), **phải xong trước D11** | **Giải (TD-0125, 07/09/2026 — chủ dự án duyệt nguyên phác thảo ở OQ-13):** sổ THỨ BA `registry/param_change_proposals.jsonl` + `registry/schemas/param_change_proposal.schema.json`, append-only như hai sổ kia. 🔑 **Điểm thiết kế then chốt — biến ràng buộc thành cấu trúc dữ liệu:** §12d.2 BƯỚC 3 đòi *“MỌI câu phải TRÍCH một con số cụ thể”*. Một ô lý do văn xuôi kèm lời hứa sẽ trích số là thứ cần **người nhớ luật** mới thi hành được — người thì quên, nhất là lúc đang thua. Vì vậy `luan_diem` là **mảng có cấu trúc**, mỗi phần tử bắt buộc `{chi_so, gia_tri (kiểu SỐ), dai_ky_vong, tham_so_tro_toi}`: câu không có số **không biểu diễn được**. Cùng thủ thuật TD-0120 dùng cho `selection_reason`. **Tám ca TỪ CHỐI ghi** (kiểm TRƯỚC khi mở file — sổ append-only không có đường lùi): (1) sai schema; (2) `cap_tham_so = C` **hoặc** tham số thuộc `tier_c` — §12c.3 *“không có quy trình, không có ô nhập”*, khai cấp khác cũng không lọt vì máy đọc config thật; (3) `luan_diem` rỗng; (4) luận điểm không trích được số; (5) `tham_so_tro_toi` không phải khoá thật (`loader.resolve`); (6) 🔴 **các luận điểm trỏ >1 tham số** — đề xuất một BỘ tham số là tìm kiếm hàng loạt viết bằng tiếng Việt (§0c.2 CẤM) và là dấu hiệu **L2 đội lốt L1** (dòng 4832); (7) `bao_cao_path` không tồn tại hoặc sha256 ≠ `bao_cao_hash`; (8) `status = APPLIED` mà `trial_id` rỗng. Audit thêm một ca chỉ nó làm được: APPLIED phải trỏ trial **CÓ THẬT và CONSUMED** trong sổ trial — không nhận lời khai, cùng khuôn TD-0119b. 🔑 **`bao_cao_hash` do MÁY tự băm** từ file báo cáo E5 đã lưu (người không có đường nhập liệu, DR-014 §3) — và **KHÔNG sửa `periodic_report.py`**, tránh hẳn *“ĐỔI NỘI DUNG BÁO CÁO = TIÊU 1 TRIAL”* (dòng 4808) + `FROZEN_CONTENT_HASH` (TD-0062). 🔑 **`kiem_de_xuat()` dùng CHUNG** cho cửa ghi và `check_lz26_*`: cửa ghi bịt lỗ “nộp sai”, audit bịt lỗ “sửa sổ bằng tay” — hai lớp có hai bản luật riêng thì lớp nào cũng có thể là lớp sai. **Không thêm entrypoint thứ 9** (cờ `--nop-de-xuat` trên E6). ⚠️ **Phạm vi KHÔNG phủ, ghi rõ:** §12c.3 đòi Cấp B có **HAI** điểm quyết định LIÊN TIẾP — chưa kiểm được vì chưa có đánh số điểm quyết định (chưa có lệnh live) → **TD-0127**, cùng `L-Z27`/`L-Z28`. Bằng chứng: 30 test khoá `tests/lock/test_lz26_de_xuat_doi_tham_so.py`; Docker **687 passed, 0 failed** | 07/09/2026 | Chủ dự án |
| MT-14 | **Bước 2 của DR-015 đã đo KHÔNG trên testnet** — lệch chữ của spec. Spec ghi hai lần *"Bước 2 thăm dò mức-lệnh trên **testnet**"* (dòng 39 và 2975). Thực tế TD-0162 suy tỷ lệ không-khớp từ **dữ liệu khớp lệnh THẬT** (dump `aggTrades` của `data.binance.vision`), không đặt một lệnh nào | Spec DR-015 §2 vs `DR-D35-01` + `src/tool_d/fill_probe.py` | 🟡 Không chặn D4 — nhưng phải nói rõ trước D10 | **ĐÃ CHỐT trước khi đo, không phải biện minh sau:** `docs/decisions/DR-D35-01-moi-truong-do-buoc-2.md` (commit `4c55b78`), chủ dự án chọn *"suy từ dữ liệu khớp lệnh THẬT"*. Lý do loại testnet: đại lượng cần đo là hàm của **độ sâu sổ lệnh và dòng lệnh thật**; futures testnet có sổ lệnh riêng, mỏng, phần lớn thanh khoản từ bot thử nghiệm ⇒ con số đo ở đó là con số **về một thị trường khác**. 🔴 **Giá phải trả, đã ghi trong `d3_5_han_che`:** phép đo là **GIÁN TIẾP** — post-only bị từ chối, khớp một phần, và vị trí hàng đợi đều KHÔNG quan sát được; cả ba đều tính về phía bất lợi. Vì thế `p_nf = 0` **không** được đọc thành "không bao giờ trượt khớp". D10 vẫn phải xác nhận trên lệnh thật | 08/09/2026 | Chủ dự án |
| MT-15 | **§3.3b thi hành SAI công thức, và điều kiện (c) có 0 dòng code.** `src/tool_d/entry_confirmation.py` + test khoá `tests/lock/test_lz6_xac_nhan_entry_confirmation.py` thi hành **`(a) HOẶC (b)`**. Spec PHẦN 3b (dòng 1289–1317) ghi **`(a VÀ c) HOẶC (b)`** kèm câu *"không phải (a OR b OR c) — (c) là BỔ NGỮ cho (a), không phải tín hiệu độc lập"*; dòng 1115 gọi (c) là *"BỔ NGỮ **BẮT BUỘC** cho (a)"* | Code + test khoá L-Z6 (TD-0129) vs spec PHẦN 3b | 🔴 **Chặn TD-0182 (phần nối) và TD-0183 (arm Z0-V1)** | **CHƯA CHỐT — chờ chủ dự án.** 🔴 Hệ quả nặng nhất KHÔNG phải "thiếu một bộ lọc": §10.1b định nghĩa arm **`Z0-V1` đúng bằng "tắt (c)", tức `(a) HOẶC (b)`** — chính là thứ code đang có. Nối như hiện tại thì **`Z0` TRÙNG KHỚP `Z0-V1`**, và một suất trial trong `N=114` bị tiêu để đo một khác biệt **bằng không**, trong khi bảng kết quả trông hoàn toàn bình thường — chỉ là hai cột giống hệt nhau. Đây là bẫy **PASS RỖNG** ở dạng mới: không làm hỏng phép đo, mà **đốt ngân sách phép thử**; không phép kiểm nào hiện có báo đỏ vì nó. Hai việc cần quyết: (i) sửa công thức + viết (c) — sẽ phải **đổi một test trong `tests/lock/`**, nên cần lệnh tường minh; (ii) `v_min` (xem OQ-06) cần một con số hoặc chốt thành việc calibrate riêng trước D4. **Không tự chọn một bên** (quy tắc 11).<br>✅ **ĐÃ THI HÀNH 08/09/2026** — `docs/decisions/DR-D4-03-vmin-dong-bang-tai-cho.md` (`8046a27`, commit RIÊNG và TRƯỚC) + vá `942efe7`. Chủ dự án chốt **phương án C** sau khi xem bảng chi phí 4 phương án. Công thức về đúng `(a VÀ c) HOẶC (b)`; `bat_dieu_kien_c` là tham số **BẮT BUỘC, không mặc định**. 🔑 **Lo ngại "phải phá test khoá" hoá ra SAI:** 5 lời gọi cũ giữ NGUYÊN mọi khẳng định, chỉ thêm `bat_dieu_kien_c=False`, và **trở thành test khoá cho chính arm `Z0-V1`** — vì `(a) HOẶC (b)` đúng là định nghĩa `Z0-V1` (§10.1b). **+17 ca, −0 ca.** Bằng chứng Docker: **1217 passed, 0 failed**; phá thật trên **file sản xuất** (xoá `frozen_rationale`) → **đúng 1 ca đỏ**. 🔴 **Bẫy PASS RỖNG thứ NĂM, do CHÍNH hành động sửa tạo ra:** `check_lz15` cũ chỉ soi tham số `tier_b` đang `null`, nên khoảnh khắc ghi `1.0` vào là nó **thôi canh** `v_min` — đã siết cùng lượt (DR-D4-03 §6) 🔄 **ĐÍNH CHÍNH 11/09/2026 — ô này ghi *"CHƯA CHỐT"* là LỖI THỜI, giữ nguyên chữ cũ làm lịch sử.** (a) Đã chốt từ 08/09/2026 bằng `DR-D4-03` (phương án C: `v_min = 1.0` FROZEN tại chỗ, giữ trong `tier_b` nên `N` vẫn 114). (b) 🔑 **Nỗi lo trung tâm — *"`Z0` TRÙNG KHỚP `Z0-V1`, tiêu 1 suất trial đo một khác biệt bằng không"* — nay CHẾT BẰNG PHÉP ĐO, không bằng lập luận:** `TD-0214` (`td0214-ba-arm-cuoi-sau-va.json`, 88 mã, WFO, sau vá `TD-0207`, 0 trial) đo `Z0-V1` = **31 lệnh** vs `Z0` = **22 lệnh**, `n` = 40 vs 28. Hai arm KHÁC NHAU. Bẫy PASS RỖNG đã tan sau khi `TD-0193` nối §3.3b. ⚠️ Nhưng nó tan theo hướng làm bài toán **KHÓ HƠN**: `Z0-V1` không phải arm thừa, nó là arm thật với cỡ mẫu riêng — và vẫn INCONCLUSIVE (`DR-D4-10 §1.2`, cần `n ≥ 319`).| 08/09/2026 | Chủ dự án |
| MT-16 | 🔴 **KHÔNG CÓ TẦNG ĐỊNH CỠ LỆNH.** `custom_stake_amount` (§6.8e) có **0 dòng code** trong toàn repo — chỉ xuất hiện bên trong chuỗi chú thích `config/freqtrade/config.json:11` tự nói nó *"chưa viết"*. Năm trên sáu hệ số §6.2 (`mult_regime`/`mult_corr`/`mult_dd`/`mult_edge`/`mult_deploy`) **0 dòng**; `mult_zss` chỉ có trong docstring và một cột-để-xem của `ZoneDetectionProbe`; `rho_eff` chỉ là một dòng chú thích (`notional.py:16`).<br>📛 **Đặt tên theo GỐC, không theo triệu chứng** (chủ dự án chốt): triệu chứng đầu tiên nhìn thấy là *"thiếu `mult_zss`"*, nhưng đặt tên như vậy thì sáu tháng nữa ai đó vá `mult_zss` rồi tưởng đã xong | Spec §6.2 + §6.8e vs code thật + dữ liệu ĐÃ NIÊM PHONG | 🔴 **Chặn cổng D4** và chặn việc **NỐI** bất kỳ arm nào vào chiến lược (hàm THUẦN vẫn dựng được — TD-0181/0183 đã làm đúng thế) | ✅ **ĐÃ CHỐT — `DR-D4-04` (`e5138ec`), ĐANG THI HÀNH.** Chủ dự án **bác** cách đóng khung ban đầu (*"có đưa tầng định cỡ vào phạm vi không"* — trình như một LỰA CHỌN kèm chữ "nghiêng"): không định cỡ theo rủi ro cố định thì *"1 R"* trôi theo từng lệnh, và §10.2 phán quyết bằng expectancy **theo R** ⇒ con số phán quyết **không có đơn vị**. **TD-0187 ✅ xong** (`c0ba7a0` + `bfe01ed`, full suite **1288 passed, 0 failed**): đo trên FILL backtest THẬT ra tỉ trọng **1:1:1** (MT-16 là 1:1:2), `leverage = 3`, `stake × L == cost`, cỡ lệnh biến theo `1/R_eff`; phá thật (trả `trade.stake_amount` về) → **đúng 3 ca đỏ**. Còn **TD-0188** (kết nạp §6.8f B2) và **TD-0189** (TP, = MT-17).<br>**Bằng chứng, đo trên artifact D3.5 đã niêm phong (91 lượt khớp thật):** cỡ lệnh **39,98 USDT hằng số** — thiết kế là `rho×E_D/R_eff` = **62–208 USDT, BIẾN theo `R_eff`**; tỉ trọng tranche **[0,2501 · 0,2499 · 0,4999]** thay vì `[⅓,⅓,⅓]` mà `trade_plan.py:21` ghi *"ĐÓNG BĂNG"* và `tier_frozen.w_tranche` khai.<br>**Cơ chế:** `ZoneAbsorptionMinimal.py:247/249` trả `trade.stake_amount` = **TỔNG stake hiện tại**, không phải phần của tranche 1 ⇒ 10 → +10 → +20.<br>🔑 **Bằng chứng mạnh nhất là một dòng chú thích TỰ CHỨNG MINH MÌNH SAI:** `config.json:11` viết *"giá trị 10 ở đây không bao giờ thật sự được dùng"* — vì `custom_stake_amount()` chưa bao giờ được viết, câu đó **sai**, và **10 USDT chính là cỡ của mọi lệnh trong mọi arm**.<br>⏱️ **Mốc thời gian, ghi để không ai đọc nhầm thành phát hiện SAI:** mọi câu "0 dòng code" dưới đây đúng **tại 08/09/2026 sáng, trước `c0ba7a0`**. Từ `bfe01ed` (chiều 08/09) `mult_zss` là hệ số THẬT trong `rho_eff`, đo được **≈ 0,62** trên fill backtest.<br>**BẢY hệ quả:** (i) **`Z0-S1` vô nghĩa** — baseline `Z0` **cũng đang** là notional cố định ⇒ hình dạng MT-15 lần HAI, một suất trial đo khác biệt bằng không; (ii) §10.2 phán quyết bằng **expectancy trên `pnl_abs`**, tỉ lệ thuận cỡ lệnh ⇒ **mọi ngưỡng tuyệt đối của D4 sẽ nói về một hệ thống 39,98 USDT/lệnh**; (iii) `w_tranche` khai **FROZEN mà KHÔNG máy nào thi hành** — cùng lớp lỗ hổng `L-Z15` ở MT-15: một ràng buộc đã khai không có ai canh; (iv) `trade_plan.py:58` dùng `p_avg=(p1+p2+p3)/3` nên **`r_eff_plan` khai CAO hơn thực tế 10,6%** (trung vị, 24 lệnh đủ 3 tranche; biên −11,0%…−9,8%), chảy thẳng vào **DG7** ⇒ dừng-vì-funding **kích hoạt muộn hơn thiết kế**.<br>🔴 **(v) KHÔNG file nào cài `leverage()`** ⇒ Freqtrade mặc định **1x** (*"If not implemented, leverage defaults to 1x"*) ⇒ **mọi backtest tới 08/09 chạy 1x, không phải `L_exchange = 3`** (DR-D0PRE-06). Kéo theo: **`L-Z3 ≥ 8` (đệm thanh lý) CHƯA TỪNG bị thử thách** — ở 1x không có thanh lý; một cổng an toàn chưa bao giờ có cơ hội đỏ.<br>🔴 **(vi) SL bị NHÂN đòn bẩy — bug sống từ D1, vô hình ĐÚNG VÌ tầng đòn bẩy chưa tồn tại.** `ZoneAbsorptionMinimal:256` trả `(kh.sl / current_rate) − 1`; Freqtrade hiểu giá trị đó là *"rủi ro của lệnh"* trên vốn **ĐÃ NHÂN** đòn bẩy (*"10% stoploss at 10x triggers on a 1% move"*). Ở 3x, SL thật nằm gần **gấp BA** kế hoạch. Đo trên lần chạy đầu của `ZoneAbsorption`: **38/38 lệnh nổ stop trong vài phút, ở giá CAO HƠN `sl`**. Sửa bằng `stoploss_from_absolute(..., leverage=trade.leverage)`. **KHÔNG sửa Minimal** (fixture `L-Z49`) — nhưng phải biết nó mang bug này.<br>🔴 **(vii) `TD-0082` (min-notional 102/102 mã) KHÔNG phủ cấu hình thật, và Freqtrade NUỐT lỗi.** TD-0082 kiểm bằng `tranche1_notional()` thuần — **chưa nhân `mult_*`** (hạ cỡ tới 0,5×) và **chưa chia đòn bẩy**. Đo thẳng từ Freqtrade: `BTC/USDT:USDT` có **`cost.min = 50 USDT`/lệnh** (LTC 20). Nặng hơn: `strategy_safe_wrapper` hạ mọi exception của callback thành **WARNING rồi đi tiếp**, `rc = 0` — nên `SizingError` fail-closed bị **nuốt 12 lần** và lệnh mẫu biến mất trong im lặng. **Một chốt fail-closed bị nuốt là một chốt KHÔNG TỒN TẠI** — PASS RỖNG ở tầng framework, không phép kiểm nào của dự án nhìn thấy. Đã bịt: test khoá TD-0187 **CẤM** mọi dòng *"Strategy caused the following exception"* trong log backtest. ⚠️ **TD-0082 cần mở lại trước khi chạy arm** (không chặn D4 vì BTC/ETH ngoài pool — DR-D0PRE-05).<br>⚠️ **Δ_R KHÔNG sai số học** — `_planned_risk_usdt()` (`buoc1_lech_tranche.py:141`) tính từ `cost` QUAN SÁT ĐƯỢC, không từ `r_eff_plan`. **Nhưng nó mô tả hệ thống 39,98 USDT / ¼-¼-½.** ~~Dựng tầng định cỡ ⇒ `planned_risk` đổi ⇒ phải ĐO LẠI Δ_R, tức mở lại artifact đã niêm phong của cổng D3.5.~~ 🔴 **RÚT LẠI 08/09/2026 — câu gạch trên SAI, và nó đã lan qua HAI phiên trước khi bị đo.** Δ_R **bất biến tuyệt đối với cỡ lệnh**: ×1 / ×3 / ×20 cho **cùng một số tới bit cuối** (`0.16120552895757337`, lệch `0.000e+00`; hai phiên kiểm độc lập), và đổi tỉ trọng ¼¼½ → ⅓⅓⅓ chỉ lệch **0,12%**. Lý do: `lệch_R = qty × Δgiá / planned_risk` có cỡ lệnh ở **CẢ tử lẫn mẫu**. **D3.5 KHÔNG phải mở lại.** 🔑 **Câu chặn rút ra, dùng từ nay:** *"đại lượng này có NHÌN THẤY thứ tôi sắp đổi không?"* — hỏi trước mọi kết luận "phải đo lại". Cả hai phiên đã **quan sát đúng** (Δ_R tính từ `cost` quan sát ⇒ gắn với hệ thống đã chạy) rồi **suy ra sai** (⇒ đổi hệ thống thì phải đo lại).<br>🔒 **Không phiên nào tự vá**, kể cả dòng 247/249 dù chỉ một dòng — sửa nó đổi hành vi của chiến lược mang test khoá **`L-Z49` CRITICAL** có bằng chứng backtest thật. ✅ Đã giải bằng cách **dựng file MỚI** `ZoneAbsorption.py`, Minimal không sửa một dòng | 08/09/2026 | Chủ dự án |
| MT-17 | 🔴 **KHÔNG CÓ TẦNG CHỐT LỜI (PHẦN 5).** `ZoneAbsorptionMinimal` đặt `minimal_roi = {"0": 10}` (tắt), `src/tool_d/` không có module TP nào, `TASKS.md` trước 08/09 không có dòng nào về TP. Mọi lệnh chỉ thoát bằng SL / DG6 / DG7 / DG8 (hết giờ) | Spec PHẦN 5 (dòng 1649–1692) + câu hỏi mở #3 và #11 (dòng 5045, 5052 — spec **mặc định D0.9 chạy CÓ TP**, và hỏi *"TIME_STOP có cắt nhiều lệnh lãi không"*, một câu vô nghĩa nếu không có TP) vs code | 🔴 **Chặn cổng D4** | ✅ **ĐÃ CHỐT — `DR-D4-04` §4, việc TD-0189** (phụ thuộc TD-0187 ✅).<br>🔑 **Về chữ "R:R" chủ dự án nêu:** §5.1 **cố ý KHÔNG dùng R:R cố định** — risk = `rho × E_D` cố định (D0.1), reward = **khoảng cách tới zone đối diện**. R:R ở Tool D là **một ĐẦU RA đo được trên từng lệnh**, và chỉ đo được khi **CẢ HAI** tầng tồn tại. Trước 08/09 **không tầng nào** tồn tại: risk là 40 USDT hằng số (MT-16), reward không có code. **Expectancy của một hệ thống KHÔNG BAO GIỜ chốt lời không nói gì về Smart DCA** — mà §10.2 phán quyết bằng đúng đại lượng đó.<br>⚠️ **Chặn bởi một con số:** `tp1_haircut_pct = 20` (tunable #9, spec dòng 1655 `[CẦN CALIBRATE]`) có giá trị nhưng **KHÔNG có trạng thái** trong `param_status.yaml` — đúng lớp `v_min` trước DR-D4-03, và bản siết L-Z15 của DR-D4-03 **không bắt được** vì nó chỉ soi mục đã khai. Chủ dự án quyết TUNED/FROZEN **trước khi TP chạy**.<br>**Vì sao sót:** Khối 16 khi mở đối chiếu *chín arm* với code nhưng **không đối chiếu *chiến lược* với danh mục PHẦN của spec** — lỗi của phiên mở khối, cùng gốc với MT-16 | 08/09/2026 | Chủ dự án |
| MT-18 | 🔴 **`L-Z29` canh SỐ ĐẾM của `tier_b`, không canh DANH TÍNH.** Vế (b)/(c) của `L-Z29` (`dof.py:89`, spec dòng 3918-3925) chỉ so `\|tier_b\|` với tổng `dof_v6` trong `dof_inventory.yaml` — một phép so **con số**. Phát hiện khi làm TD-0189: đưa `tp_fallback_dist_r` (khoá NẠNG ĐÓNG BĂNG, spec dòng 1674 *"0 trial"*) vào `tier_b`, đồng thời bỏ `dg7_funding_frac` ra và sửa `param_status.yaml` cho khớp — `\|tier_b\|` vẫn 12, `L-Z29`/`L-Z15`/TD-0169/TD-0190 **đều xanh** (`1 failed, 67 passed` — chỉ test hẹp của TD-0189 bắt được). `N = 114` và rào DSR `3,0777` **đứng im**, trong khi thứ đang được tune đã đổi hẳn — hoán một tham số ĐÓNG BĂNG lấy một tham số ĐANG TUNE thì mọi bất biến đắt tiền của dự án đi qua trót lọt. 🔑 **Họ hàng với TD-0190 nhưng khác hình dạng:** TD-0190 là *người bị canh tự chọn phạm vi bị canh* (danh sách tự khai bỏ trống vẫn hợp lệ); MT-18 là *bất biến chỉ đo TỔNG, tổng đứng im trong khi thành phần đổi hết* | `L-Z29`/`dof_inventory.yaml` (DR-010) vs cách `N`/rào DSR thực sự phụ thuộc — không chỉ **bao nhiêu** khoá trong `tier_b`, mà **khoá nào** | 🟡 **Không chặn D4** — TD-0189 đã tự đóng lỗ hổng ở phạm vi hẹp của chính nó (`test_hai_khoa_nang_KHONG_duoc_nam_trong_tier_b`, ghim đúng hai khoá nạng không được vào `tier_b`). Nhưng đó là vá TỪNG CA, không phải sửa cơ chế — 10 khoá `tier_b` còn lại và mọi khoá `tier_frozen` khác vẫn hoán được cho nhau mà `L-Z29` không biết | ✅ **ĐÃ CHỐT 09/09/2026 — phương án (a).** Vá TỪNG CA như TD-0189 đã làm: mỗi khi phát hiện một cặp khoá nguy hiểm thì thêm một test hẹp ghim đúng cặp đó, không đổi kiến trúc `dof_inventory.yaml`/`L-Z29`. Phương án (b) — `dof_inventory.yaml` mang danh sách TÊN khoá, `L-Z29` so TẬP HỢP thay vì số đếm — bị hoãn có ý thức: đóng lỗ hổng gốc nhưng đổi hình dạng một file kiểm kê đang dùng, là quyết định kiến trúc không có gì đang chảy máu ép phải làm ngay | 09/09/2026 | Chủ dự án |
| MT-19 | 🟡 **KHÔNG CÓ ĐƯỜNG HỢP LỆ ĐỂ GHI SỔ MỘT PHÉP ĐO MÔ TẢ TRÊN DỮ LIỆU POOL.** N2/MT-02 đã chốt: *đo thông tin mô tả* trên CALIB **không** tính là "chạm", **nhưng vẫn phải ghi một dòng `CTRL`**. Mà dòng `CTRL` dạng *đo thước* bị máy cưỡng chế qua `CTRL_OUTPUT_ALLOWED` (`src/tool_d/ledger/registry.py:33`) = **đúng ba trường** `price_delta` / `tranche_index` / `direction` — ba trường sinh ra cho DR-015 Bước 1, đúng chữ spec dòng 3605-3607. Phép đo mô tả nào có đầu ra khác ba trường đó thì **cửa ghi từ chối**, tức luật cho phép một việc mà cơ chế không có chỗ ghi nhận nó | MT-02 (đo mô tả được phép, phải ghi CTRL) vs `CTRL_OUTPUT_ALLOWED` (danh sách CHO PHÉP đóng, 3 trường) | 🟡 Không chặn TD-0182 | **CHƯA GIẢI — ghi nhận theo quy tắc 11, không tự chọn bên.** Ca vừa gặp đã đi vòng hợp lệ: đo trên **tập EXPLORE** (BTC/ETH + alt), nơi `DR-D0PRE-05` §4 ghi rõ *"dùng để SINH giả thuyết, **0 trial**"* và nằm ngoài phạm vi DR-014 — nên không cần dòng `CTRL` nào. Đường vòng đó **chỉ dùng được khi câu hỏi không đòi chính dữ liệu pool**. Ca đo mô tả tiếp theo mà bắt buộc dùng 102 mã pool sẽ **chặn cứng**, và khi đó phải chọn giữa: (a) mở rộng `CTRL_OUTPUT_ALLOWED` bằng một DR — đắt, vì nó nới đúng chốt fail-closed đang bảo vệ ranh giới *"CTRL không được sinh chỉ số hiệu năng"*; hay (b) tiêu 1 suất trial trong 114. 🔴 **KHÔNG tự nới `CTRL_OUTPUT_ALLOWED`** — chốt đang chặn thì không gỡ vì nó đang chặn; và nó là danh sách CHO PHÉP chứ không phải danh sách CẤM, nới một lần là mất luôn tính chất fail-closed đó | 09/09/2026 | Ghi nhận (phiên `-f4`), chờ chủ dự án ✅ **ĐÃ CHỐT 14/09/2026 — chủ dự án chọn đường (c): thêm DẠNG CTRL THỨ BA (*đo mô tả*), KHÔNG nới danh sách của D3.5.** Trường khai riêng (`ctrl_mo_ta_whitelist`) + allowlist **liệt kê đích danh**, mỗi tên kèm một câu vì-sao-không-phải-chỉ-số-hiệu-năng; danh sách ba trường của D3.5 Bước 1 **không đụng một chữ**. 🔴 **VÀ MỤC NÀY TỪNG ĐÓNG KHUNG SAI VẤN ĐỀ — giữ nguyên chữ trên làm lịch sử:** nó viết như thể *“`CTRL_OUTPUT_ALLOWED` chỉ nhận 3 trường của DR-015”* là một **chốt spec** va với `MT-02`. Đọc **cả ba** chỗ spec nhắc danh sách đó thì không phải: `:3604-3607` buộc nó vào ca **escalation** (*“CHỈ khi CALIB < 30 sự kiện kích hoạt tranche 2+ mới được mở rộng sang WFO, VÀ KHI ĐÓ đầu ra bị GIỚI HẠN CỨNG”*), `:71-72` buộc vào *“cổng thăm dò **D3.5**”*, `:4474` buộc vào *“**Bước 1**”*. Trong khi `registry.py:389-401` áp cho **MỌI** lời khai dạng đo-thước ⇒ đây là **MÃ CHẶT HƠN SPEC**, cùng họ `MT-28` nhưng **ngược chiều** (MT-28: spec để ô trống, mã đã có số; đây: spec buộc hẹp một phạm vi, mã áp rộng ra toàn bộ). 🔑 **Giả thuyết rẻ nhất cho vì sao, và nó khớp bằng chứng hơn “tưởng spec đòi thế”: KHÔNG THỂ ĐẶC BIỆT HOÁ.** Sổ **không có trường phân biệt pha** — thử 8 tên (`pha` `buoc` `phase` `step` `stage` `giai_doan` `cong` `gate`) cho **0/8** ở cả `registry.py` lẫn `trial_event.schema.json`; và docstring `_kiem_ctrl_do_thuoc` (`:389-391`) **tự khai** *“Dạng đo thước (**D3.5 Bước 1**)”*, tức mã BIẾT phạm vi mà chỗ duy nhất ghi nó là một chú thích — chú thích không cưỡng chế được gì. ⇒ Phương án *“thu hẹp về đúng phạm vi spec”* **KHÔNG BIỂU DIỄN ĐƯỢC** hôm nay: thu hẹp mà không thêm trường phân biệt thì **không phải thu hẹp, mà là bỏ lớp canh cho mọi dòng CTRL**. 🔑 Nhưng đường (c) **biểu diễn được NGAY, không cần trường pha** — `registry.py:334-357` đã phân biệt hai dạng **bằng TRƯỜNG NÀO CÓ MẶT** (`reproduces_trial_id` vs `ctrl_output_whitelist`), nên dạng thứ ba dùng đúng cơ chế đó. 🔴 **RÀNG BUỘC THI HÀNH, sai là mở cửa lách:** ba phép kiểm ở `:337-347` đều **NHỊ PHÂN** (`A and B` → từ chối · `not A and not B` → từ chối · `if A … else`), nên thêm trường mà **không** nới chúng thì khai một mình trường mới bị từ chối **kèm thông báo SAI**, còn khai chồng với `ctrl_output_whitelist` thì **QUA ĐƯỢC** và danh sách mô tả **bị bỏ qua IM LẶNG** — đúng cửa lách mà chú thích `:340` sinh ra để chặn, và dạng thứ ba theo định nghĩa **là** dạng dễ kiểm hơn. ⇒ Đổi *“một trong HAI”* thành *“một trong BA”* + ba nhánh tường minh, **trong CÙNG một commit** với việc thêm trường. Chưa có mã việc |
| MT-20 | 🔴 **§1.3 (zone chết sau 40 nến 4H) và §5.1 (zone đối diện là LUẬT, `TP_fallback` là NGOẠI LỆ) KHÔNG THỂ CÙNG ĐÚNG trên dữ liệu thật.** Định lượng bởi phiên `-94` trên tập EXPLORE (100 mã 4H, 213.839 nến, **0 trial**): zone đỉnh gần nhất **quá hạn 40 nến ở 73–75%** số ca (tuổi trung vị **169 nến ≈ 28 ngày**). Áp hạn dùng §1.3 cho zone đối diện thì tỉ lệ nạng lên **79–87%**, mà spec dòng 1684 chốt *"nạng > 40% ⇒ phân loại L2"* — tức **L2 thành kết luận biết trước**, không còn là phép đo. Và nạng chính là `p_avg + 1,5 × R_eff` — **một bội số R cố định**, đúng thứ §5.1 dòng 1653 mang tiêu đề *"Vì sao KHÔNG dùng TP theo R-multiple"* để bác bỏ | §1.3 vs §5.1, phát hiện khi dựng vòng quét zone đỉnh cho TP1 (TD-0189, chặng 2a) | 🔴 Chặn kết luận đúng của D4 nếu đọc kết quả TP mà không biết quyết định này | ✅ **ĐÃ CHỐT — `DR-D4-06`** (`0d56d01` + `5392ccf` + `bc1d487`, commit RIÊNG và TRƯỚC mọi dòng mã thi hành; thi hành ở `773251f`). §1.3 **KHÔNG áp** cho zone đối diện — quyết định được KHAI, không phải chốt rỗng (phân biệt: *"một luật không áp dụng vì có người QUYẾT ĐỊNH thế thì không phải chốt rỗng; chốt rỗng là luật trông như đang áp mà cấu trúc không cho nó đỏ"*). H-4 tách làm **HAI số** — căn cứ nằm trong chính câu spec viết ra H-4 (dòng 1684): tiền đề là về **KHOẢNG CÁCH**, không về **tuổi**, nên `ty_le_khong_co_zone` chịu ngưỡng 40% còn `ty_le_zone_qua_han` là **PHẢN THỰC** (đo giá của phương án cũ trên chính dữ liệu D4, 0 trial — nửa còn lại của phép tự bác bỏ). Tuổi zone ghi vào Decision Log mọi lệnh TP-theo-zone; zone đóng băng tại lúc vào lệnh (TP1 nằm trong `(0…3,2R]`, nạng là đúng `1,5R`, tính lại giữa chừng làm mục tiêu nhảy cả hai chiều). **Ba điều kiện xét lại viết TRƯỚC** (DR §5): `ty_le_khong_co_zone` > 40% trên D4 thật / D4 cho thấy TP-trên-zone-cũ tệ hơn nạng / đo lại theo LỆNH trên POOL ra bậc độ lớn khác. 🔑 **Sự cố phụ đáng ghi:** hai phiên (`-2f`, `-94`) độc lập viết **hai file `DR-D4-06`** cho cùng quyết định, cùng lúc — cả hai commit đều sạch theo N12 mục 1-5, nhưng vẫn ra **hai nguồn sự thật**. Giải bằng tiêu chí đã dùng ở va chạm TD-0119/TD-0120 (định danh máy đọc thắng); xem N12 mục 6 mới | 09/09/2026 | Chủ dự án |
| MT-21 | 🔴 **`TD-0182` đánh dấu ✅ nhưng chỉ hoàn thành NỬA phạm vi đã khai trong chính tên task.** Tiêu đề: *"Nối Phần 2 (trend filter) **+ §3.3b (xác nhận entry)** vào TÍN HIỆU VÀO LỆNH"* — hai việc. Bằng chứng khi đóng (`7c06a8d`) chỉ nói về trend filter (`nối qua du_dieu_kien_trend_theo_tang()`); §3.3b không xuất hiện một chữ nào. Đo lại (`ebc5089`): `entry_confirmation`/`tim_xac_nhan_entry`/`bat_dieu_kien_c` — **0 lần** trong `ZoneAbsorption.py`. Cùng phiên phát hiện thêm một ca y hệt ở `Z1`: `sl_neo_atr`/`ke_hoach_theo_arm`/`CHE_DO_SL_THEO_ARM` cũng **0 lần** — SL cố định của Z1 (đặc trưng phân biệt DUY NHẤT của arm đó) chưa từng được đọc. Hệ quả đo được: **`Z1` và `Z0-V1` chạy giống hệt `Z0`, trùng khít 83/83 lệnh** trên tập EXPLORE (phiên `-f4`, `docs/du-lieu-do/dg2-explore-quet-arm.json`) — tái diễn MT-15 ở một lớp khác (công thức đã sửa đúng, nhưng chưa bao giờ được gọi trên đường sản xuất). Không có test nào ở tầng chiến lược/backtest hỏi *"công tắc này có được GỌI không"* — chỉ có test đơn vị của module cô lập (`test_td0183_cong_tac_arm.py`), đúng hình dạng "canh đúng chỗ nhưng đường chạy không bao giờ qua" mà TD-0188 đặt tên | TASKS.md ghi TD-0182 là ✅ vs mã nguồn thực tế thiếu nửa phạm vi đã khai trong chính task đó | 🔴 Chặn `TD-0184` — chạy ablation bây giờ tiêu 2/9 suất trial (18% ngân sách B2 Long) mua thông tin bằng 0 | **CHỐT theo chỉ đạo chủ dự án (09/09/2026): chặn `TD-0184` cho tới khi cả hai được nối.** Mở `TD-0192` (nối Z1 — đổi công thức SL một dòng, rủi ro thấp) và `TD-0193` (nối §3.3b — đổi hành vi vào lệnh của TOÀN hệ thống, cần thiết kế trạng thái "chờ xác nhận tối đa 3 nến", vùng có tiền sử lookahead thật ở `TD-0170`). Không phiên nào tự sửa khi phát hiện — cả hai đổi hành vi hệ thống, không phải chi tiết triển khai nội bộ (quy tắc 2) 🔄 **ĐÍNH CHÍNH 11/09/2026 — giữ nguyên chữ cũ làm lịch sử.** (a) Điều kiện chặn của RIÊNG mục này **đã thoả**: `TD-0192` ✅ và `TD-0193` ✅, cả hai đã nối. 🔴 **Nhưng KHÔNG được đọc thành *"TD-0184 hết bị chặn"*** — `MT-24` và `MT-25` vẫn đang chặn, và `DR-D4-10` (11/09/2026) đã định lại phạm vi D4. (b) 🔑 **Phát hiện *"`Z1` ≡ `Z0`"* nay CHẾT BẰNG PHÉP ĐO:** `TD-0214` đo `Z1` = **6 lệnh** vs `Z0` = **22**; `TD-0192` có tác dụng thật, mạnh tới mức cắt 22 → 6. (c) ⚠️ **Nhưng mâu thuẫn cũ chết đi thì một mâu thuẫn MỚI sinh ra đúng chỗ đó:** `TD-0215` đo `n_giao = 6`, `chi_co_o_B = 0` ⇒ `Z1` là **TẬP CON NGHIÊM NGẶT** của `Z0` — 72,7% lệnh mất ở cổng kết nạp §6.8f vì đổi SL kéo theo đổi cỡ lệnh, nên phép so `Z1` vs `Z0` không còn so hai kiểu SL. Xem **`MT-31`** và `DR-D4-10 §2.5` hạng (3).| 09/09/2026 | Chủ dự án |
| MT-22 | 🔴 **Spec §3.3b tự mâu thuẫn: cửa sổ chờ xác nhận mở ở "LẦN ĐẦU" giá chạm zone (dòng 1075: "Khi giá chạm zone lần đầu (sau `confirmed_at_bar`)... CHỜ..."), nhưng điều kiện (b) đòi so với "lần chạm zone gần nhất TRƯỚC ĐÓ" (dòng 1081-1082).** Nếu cửa sổ chỉ mở ở lần chạm ĐẦU TIÊN thì tại đúng lần đó KHÔNG TỒN TẠI "lần chạm trước đó" để (b) so sánh ⇒ (b) là **code chết**, điều kiện thực tế rút về `(a VÀ c)`, mất hẳn một nhánh spec cố ý thiết kế. Phát hiện khi thiết kế `TD-0193` (nối §3.3b vào tín hiệu vào lệnh), đối chiếu chéo độc lập bởi hai phiên `-94` và `-f4` — cả hai cùng đọc ra mâu thuẫn, cùng nghiêng về hướng "mở cửa sổ ở MỖI lần chạm" (giữ (b) sống), nhưng đó là **diễn giải trái chữ spec**, không phải đọc thẳng. 🔴 **ĐÍNH CHÍNH (09/09/2026, cùng ngày):** câu *"(b) là code chết"* ở trên **SAI** — `zone_hop_le()` đòi `so_touch >= 1` làm điều kiện công nhận zone, đếm trên cửa sổ TRƯỚC nến xác nhận, nên mọi zone hợp lệ đã có sẵn ≥1 lần chạm để (b) so sánh (đo trên 4.944 zone thật, EXPLORE, `docs/du-lieu-do/do_mt22_a_vs_b.py`: nhánh (b) gánh 51,3% số lệnh của phương án "chỉ lần chạm đầu"). Cả `-94` lẫn `-f4` đọc sót điều kiện này. Lý do ban đầu nghiêng B ("cứu (b)") vì thế **bị chính phép đo bác** — nhưng mâu thuẫn không biến mất, nó đổi hình: spec dòng 1075 ("lần đầu") vẫn nghiêng A, còn chữ "lượt chạm **NÀY**" ở dòng 1085 (hàm ý còn lượt khác) lại nghiêng B — cả hai đọc sót chữ đó ở lượt đầu | Spec dòng 1075 ("lần đầu") vs dòng 1081-1082 ("lần chạm gần nhất trước đó") vs dòng 1085 ("lượt chạm NÀY") | 🔴 Chặn `TD-0193` phần lõi (quét từng lần chạm) — chọn sai chiều thì hoặc bỏ lỡ vĩnh viễn phần lớn zone (A), hoặc dò cấu hình trốn sổ trial bằng cách thử nhiều lần tới khi "thành công" (B, đúng cơ chế spec cảnh báo chống overfitting) | ✅ **CHỐT (09/09/2026) — Phương án A + ghi phản thực cho B.** Đọc đúng chữ "lần đầu": cửa sổ chờ xác nhận mở ĐÚNG MỘT LẦN mỗi zone. Lý do chọn: hướng sai của A (bỏ lỡ) an toàn hơn hướng sai của B (vào lệnh trên xác nhận chỉ xuất hiện vì đã thử nhiều lần) — chọn bảo thủ vì D4 tiêu 9 suất trial đo hệ thống này. Đo trên EXPLORE: A cho 43,1% zone vào lệnh, B cho 81,2% (chỉ dùng được TỈ LỆ, không dùng số tuyệt đối — chưa áp bộ lọc trend/kết nạp danh mục). **Kèm cơ chế ghi phản thực, ĐIỀU KIỆN MỞ LẠI BẤT ĐỐI XỨNG** (do `-f4` sửa sau khi bác bản đầu của `-94` là "0 trial cho cả hai chiều"): trong lúc quét A, tiếp tục ghi (không phát tín hiệu) lần chạm nào SẼ xác nhận nếu chạy B. *Loại B ra* — làm được, 0 trial, chỉ cần ĐẾM (ví dụ: phần B thêm quá ít, hoặc dồn vào lần chạm muộn khi zone gần hết hạn). *Nhận B vào* — KHÔNG có đường tắt, phải trả một suất trial B1 đăng ký trước; con số phản thực là CẬN TRÊN của B (bỏ qua cổng kết nạp danh mục §6.8f và ảnh hưởng lên `mult_corr`/`mult_deploy` của lệnh sau), không phải dự báo expectancy. Ghi phản thực không được đụng state của đường A (test bắt buộc: bật/tắt ghi phản thực ⇒ tập lệnh thật không đổi một lệnh nào). **Câu hỏi mở còn treo, cần diễn giải khi code:** mốc (giá, RSI) cho (b) ở lần chạm đầu là một cụm chạm khung 4H, phải quy về một nến 1H cụ thể — chưa ai định nghĩa ánh xạ đó | 09/09/2026 | Chủ dự án |
| MT-23 | 🔴 **`tier_b` khai 12 tham số tunable, nhưng 7 trong số đó KHÔNG có đường đọc nào từ `config/tool_d_config.yaml` vào mã chạy — giá trị thật đến từ HẰNG SỐ CỨNG trong `.py`.** Cùng hình dạng **MT-08** (*code chưa thi hành điều đã chốt*), không phải mâu thuẫn giữa hai quyết định: N4 đã chốt *"mọi tham số đọc từ `tool_d_config.yaml`, không nơi nào khác"*; cái sai là code chưa làm thế. Đo tĩnh bằng AST, 0 trial (`docs/du-lieu-do/do_duong_doc_tham_so_tier_b.py` → `td0195-duong-doc-tham-so-tier-b.json`): `zss_threshold`←`zone_strength.py:20 NGUONG_ZSS` (hàm `zone_hop_le`) · `buf_sl_atr`←`trade_plan.py:20 BUF_SL_HE_SO` (`tinh_ke_hoach`) · `dg6a_atr_ratio`←`dg6_early_invalidation.py:22` (`dieu_kien_a`) · `dg6d_retrace_frac`←`:26` · `funding_rate_pct`←`:25` (cả hai ở `dieu_kien_d`) · `wick_close_upper_frac`← số ma `0.5` (`entry_confirmation.py:134,136`, không cả một hằng số có tên) · `v_min`← chưa ai truyền (MT-21). 🔴 **Ba cái đầu NẰM TRÊN đường chạy sản xuất** `ZoneAbsorption.py` (`:314`/`:400`, `:322` qua `ke_hoach_theo_arm`, `:808`) | Code (`zone_strength.py`, `trade_plan.py`, `dg6_early_invalidation.py`, `entry_confirmation.py`) vs **N4** (`CLAUDE.md`) + §6.9.5 + `config/dof_inventory.yaml` (khai cả 7 là tunable, tính vào **N = 114**) | ✅ **ĐÃ THI HÀNH PHẦN CÓ HẬU QUẢ 09/09/2026** (TD-0195, `0b5112d`). Trước đó 🟡 — chưa gây thiệt hại vì chưa trial B3 nào tiêu cho 7 khoá này, nhưng **phải sửa TRƯỚC trial B3 đầu tiên** | 🔑 **Vì sao đây là hạng nghiêm trọng chứ không phải chuyện gọn gàng mã nguồn — đo được, không suy luận.** Đổi `zss_threshold` **0,5 → 0,99** trong YAML (ngưỡng gần như không zone nào đạt) rồi chạy backtest THẬT (`test_td0187`) ở hai commit trong lab `git clone --local`: TRƯỚC bản vá (`0152158`) → **17 passed**, MỌI THỨ VẪN XANH; SAU bản vá (`0b5112d`) → **1 failed + 16 errors** (số zone hợp lệ về 0). ⇒ Cho tới sáng 09/09, **một trial B3 chi cho việc calibrate khoá đó mua về THÔNG TIN BẰNG KHÔNG**, và bảng kết quả trông hoàn toàn bình thường vì hai cấu hình cho ra cùng một tập lệnh. **Cùng họ MT-15** (bẫy PASS RỖNG đốt ngân sách) nhưng **im lặng hơn một bậc**: MT-15 còn để lại hai cột arm giống hệt nhau; ở đây **sổ trial ghi CẤU HÌNH, không ghi TẬP LỆNH**, nên không có chỗ nào để hai thứ trùng nhau đứng cạnh nhau mà lộ ra. 🔑 **Vì sao `L-Z15` không phủ:** nó soi `param_status.yaml` — *"đã khai TUNED/FROZEN chưa"* — tức nói về **tư cách** của tham số; MT-23 nói về **đường đi** của nó. Kiểm kê DOF và `param_status.yaml` cũng vậy. **THI HÀNH:** ba khoá trên đường chạy nay đọc `resolve(cfg, "tier_b.<khoá>")` ở `__init__` chiến lược rồi truyền xuống; ba hằng số **XOÁ HẲN, không giữ làm mặc định** — mặc định là chỗ để một đường gọi quên khai mà vẫn lặng lẽ chạy (cùng lý do `bat_dieu_kien_c` không có mặc định). Fixture `L-Z49` (`ZoneAbsorptionMinimal`) và `ZoneDetectionProbe` cũng đọc YAML: fixture giữ bản sao riêng thì thứ nó chứng minh chạy được là **một cấu hình không ai chạy**. Lớp canh mới `tests/lock/test_td0195_tham_so_tier_b_co_duong_doc.py` (AST, không grep — chuỗi trong docstring trông y hệt đối số thật, tiền lệ `L-Z25` của phiên `-94`). **Hành vi backtest BẤT BIẾN** (mọi giá trị YAML khớp đúng hằng số cũ) ⇒ **0 trial, N giữ 114**, rào DSR không đổi, không đụng `param_status.yaml`/`dof_inventory.yaml`. Bằng chứng Docker: **1481 passed, 0 failed**. Kiểm có răng: trả `NGUONG_ZSS` về hằng cứng ⇒ **đúng 1 ca đỏ**. ⚠️ **Giới hạn của lớp canh, do CHÍNH phép phá phơi ra:** ca *"có đường đọc"* **VẪN XANH** khi hàm nhận bỏ qua giá trị — AST chứng minh được chiều **PHỦ ĐỊNH** (không literal ⇒ chắc chắn không đọc), chiều **KHẲNG ĐỊNH** do phép đối chứng đổi-YAML gánh; đó là lý do phải làm phép đối chứng chứ không dừng ở AST. ⏳ **CÒN NỢ 4 khoá, hoãn CÓ LÝ DO** (`v_min`, `wick_close_upper_frac` — chờ TD-0193 nối §3.3b, MT-21; `funding_rate_pct`, `dg6d_retrace_frac` — `dieu_kien_d()` chưa có người gọi, `ZoneAbsorption` truyền `d=False` cứng): vá một đường không ai đi thì lớp canh sẽ canh một thứ chưa chạy bao giờ. Ghi trong `MIEN_TRU` kèm lý do + điều kiện gỡ, **và có ca đỏ khi miễn trừ hết hạn** — danh sách miễn trừ không tự dọn sẽ phình ra cho tới lúc che mất chính thứ nó theo dõi. 🔴 **Câu phải chốt TRƯỚC khi nối `dieu_kien_d`: ĐƠN VỊ.** YAML ghi `funding_rate_pct: -0.05` (PHẦN TRĂM), hằng số cũ là `-0.0005` (TỈ LỆ) — cùng một số vật lý, khác đơn vị; nối mà quên ÷100 thì ngưỡng sai **100 lần**, đúng lớp lỗi `L-Z48c` sinh ra để chặn. | 09/09/2026 | Chủ dự án |
| MT-24 | 🔴 **TRÍCH DẪN BỊA trong mã: `docs/du-lieu-do/td0184-do-nhay-rao-dsr.py:12` viết, CÓ DẤU NGOẶC KÉP như trích nguyên văn, rằng `DR-D0PRE-05` §4 *"cấm THẲNG — KHÔNG expectancy, KHÔNG PnL theo arm"*. Điều khoản đó (dòng 66-74) KHÔNG chứa câu này.** Truy được chuỗi lan truyền: `DR-D4-08` §6 và `DR-D4-09:158` viết **paraphrase KHÔNG đánh dấu** (*"chỉ đếm, không PnL, đúng ranh giới DR-D0PRE-05 §4"*) → file viết sau tin đó là chữ gốc rồi **đóng khung thành trích dẫn**. Chỗ thứ tư: khoá `ranh_gioi` trong `td0193-lenh-nam-explore.json` (artifact — KHÔNG sửa tay, sửa là hỏng xuất xứ). Ba ràng buộc CÓ THẬT, hai phiên độc lập xác nhận: (a) EXPLORE không bao giờ dùng để **validate** (§9c.4b dòng 3830); (b) mã EXPLORE không bao giờ sang pool; (c) `DR-014` §2 (spec dòng 3490) — mọi lần chạy EXPLORE **ngoài sổ ngân sách**, đây là quy tắc **KẾ TOÁN** (0 trial bất kể đo gì), KHÔNG phải quy tắc **NỘI DUNG**. Không câu nào cấm PnL | `td0184-do-nhay-rao-dsr.py:12` + `DR-D4-08` §6 + `DR-D4-09:158` vs `DR-D0PRE-05` §4 + spec §9c.4b (dòng 3807-3850) | 🔴 Chặn TD-0184 | **CHƯA GIẢI — ghi nhận theo quy tắc 11, không tự chọn bên.** 🔴 **KHÔNG tự nới ranh giới EXPLORE bằng phát hiện này** — nó nói ràng buộc *chưa từng được quyết*, KHÔNG nói ràng buộc *sai*. Cách đọc chặt vẫn có lý: đo `mean_R` trên EXPLORE rồi lấy làm tin tưởng thì đúng là chạm ràng buộc (a). Lập luận phân biệt: `std` đo **độ phân tán của thước** (trả lời *"cần bao nhiêu mẫu"* — phân tích lực thống kê), `mean` đo **hệ thống** (trả lời *"có lãi không"* — VALIDATE); cùng loại với `DR-015` Bước 1 mà dự án đã nhận là *đo thước*, 0 trial. Đó là **DIỄN GIẢI**, phải qua một DR viết TRƯỚC, và cơ chế chặn phải là danh sách CHO PHÉP fail-closed (khuôn `CTRL_OUTPUT_ALLOWED`, `registry.py:33`), không phải lời hứa không nhìn. Hệ quả nếu để nguyên: đóng con đường **0 trial** duy nhất tới `std_R` | 10/09/2026 | Ghi nhận, chờ chủ dự án ✅ **ĐÃ GIẢI 16/09/2026 — `DR-D4-13` §1 (`daaaba9`), chủ dự án chốt: EXPLORE theo ĐÚNG CHỮ spec §9c.4b.** Spec ghi cho EXPLORE *"Phân tích KHÔNG giới hạn, 0 trial"* ⇒ được tính mọi đại lượng, **kể cả** `std_R`/`mean_R`/PnL theo arm. Cấm cứng (diễn giải ràng buộc (a) thành hành vi, `DR-D4-13` §1.2): dùng số EXPLORE làm căn cứ **phán quyết cổng**, làm lý do **đổi arm/ngưỡng/cách đọc đã viết TRƯỚC**, hay làm lý do **chứng minh cấu hình đáng tiêu suất** — giả thuyết đi qua Idea Queue (`TD-0126`) và trả trial đầy đủ. 🔄 **ĐÍNH CHÍNH ô bên trái, giữ chữ cũ:** câu *"đóng con đường 0 trial duy nhất tới `std_R`"* hết đúng — con đường đó **mở sẵn theo spec**, không cần DR mở; và cơ chế *"danh sách CHO PHÉP fail-closed chỉ xuất `std_R`"* **bị loại** (`DR-D4-13` §1.5: tính `std` buộc tính `mean` trước, rào không chặn được thứ nó định chặn). **Chưa có máy chặn** cho §1.2 — điều kiện dựng viết trước ở `DR-D4-13` §1.4. 🔑 **Một lần trượt cùng họ với chính lỗi này:** khuyến nghị đầu của phiên soạn DR là *"giữ chỉ đếm"*, lập luận mà **chưa mở §9c.4b** — chủ dự án chọn theo, rồi đổi khi được hỏi lại kèm dữ kiện (`DR-D4-13` §0). Đính chính tại chỗ: `DR-D4-08`/`DR-D4-09` (`cd7ca8e`), docstring `td0184-do-nhay-rao-dsr.py` (`b6217c0`, Docker `tests/lock` **1218 passed**); artifact `td0193-lenh-nam-explore.json` **không sửa**. ⚠️ Mục này **KHÔNG còn chặn TD-0184** — thứ chặn duy nhất còn hiệu lực là `TD-0247` |
| MT-25 | 🔴 **Đơn vị `R_realized` của Z0 bị CO, và vì thế `DR-D4-09` §7 điều 1 (*"chỉ lật nếu `std_R < 0,5`"*) không dùng được như đang viết.** Hai vế phải đọc CÙNG NHAU, tách ra là hiểu sai: **(i) CƠ CHẾ, đo được:** `sizing.py:340` đặt `planned_risk_usdt = n_full × r_eff` với `n_full` = thang **ĐẦY ĐỦ ba tranche**, trong khi `td0193-lenh-nam-explore.json` cho thấy Z0 khớp **100% chỉ tranche 1** (`phan_bo_tranche {"1": 62}`; `Z0 ∈ arm_switches.ARM_DON_TRANCHE`) ⇒ mẫu số lớn hơn rủi ro thật bỏ ra ⇒ đơn vị R co lại theo hệ số λ < 1. **(ii) SUY RA từ (i):** khi R co vì đổi đơn vị thì `mean_R` và `std_R` co theo **CÙNG λ**, nên `DSR_adj → λ·DSR_adj` trong khi ngưỡng `0,10 R` là **tuyệt đối, không nhân λ** ⇒ ngưỡng hiệu dụng thành `0,10/λ`; đại lượng bất biến là Sharpe mỗi lệnh `S = mean_R/std_R`, và `S ≥ 0,10/std_R + h/√n` **TĂNG khi `std_R` giảm** (std 1,25→0,40 đẩy `S` cần ở n=40 từ 0,567 lên 0,737) | `DR-D4-09` §7 điều 1 vs `sizing.py:340` + `arm_switches.ARM_DON_TRANCHE` + `td0193-lenh-nam-explore.json` | 🔴 Chặn TD-0184 | **CHƯA GIẢI.** 🔑 **Phát biểu chính xác, đừng rút gọn quá tay** (điểm do phiên `be` nêu, và nó đúng): §7 điều 1 giữ `mean_R` cố định khi cho `std_R` chạy — **giả định đó SAI đúng trong trường hợp `std_R` nhỏ đi VÌ đơn vị R bị co (vế i)**, vì khi đó `mean_R` co theo cùng hệ số và Sharpe mới là bất biến. Nếu `std_R` nhỏ vì phân tán **thật sự chặt** (không đổi thang) thì §7 điều 1 **đúng**. Nói trần *"std nhỏ làm cổng khó hơn"* mà không kèm vế (i) là lật một sự thật toán học phổ quát — không phải điều đang xảy ra. Hệ quả thi hành: mọi phép đo `std_R` **phải phát ra kèm `ty_le_rui_ro_da_trien_khai`** (Σ rủi ro tranche ĐÃ khớp / `planned_risk_usdt`) — thuần chênh-lệch-giá + chỉ số tranche, KHÔNG có PnL, nằm gọn trong tinh thần `CTRL_OUTPUT_ALLOWED`. Đo `std_R` một mình là mua một con số **không biết đọc theo chiều nào** | 10/09/2026 | Ghi nhận, chờ chủ dự án ✅ **PHẦN ĐƠN VỊ ĐÃ GIẢI 14/09/2026 — `DR-D4-12` §1 (`63769f8`), chủ dự án chốt: Nhánh 1 phán quyết trên `R` theo RỦI RO ĐÃ TRIỂN KHAI.** `sizing.py` **không đổi một dòng** (đây là đại lượng ĐỌC ở tầng đo, không phải đại lượng đặt lệnh), nên hai bất biến `planned_risk_usdt` (`:28`) và `Σ w_tranche = 1` (`:277`) giữ nguyên — chúng đang gánh việc bác lý lẽ *“tranche 2/3 bơm thêm rủi ro”* ở `DR-D4-10` §2.4. Bản ghi arm mang **CẢ HAI** vế; `ty_le_rui_ro_da_trien_khai` đã `required` sẵn trong `CHI_SO_BAT_BUOC` (`arm_record.py:105`). 🔴 **ĐÍNH CHÍNH KÈM, và nó bác một con số của chính mục này: `λ` KHÔNG phải hằng số.** `trade_plan.py:73-74` cho `p_avg = (p1 + p2 + p3) / 3` rồi `r_eff_plan = (p_avg − sl) / p_avg`, và `:68-70` cho `p3 < p2 < p1` ⇒ `λ = w₁ · [(p1−sl)/p1] ÷ [(p_avg−sl)/p_avg]` **phụ thuộc hình học zone của TỪNG LỆNH**, chỉ chắc `λ > w₁ = 1/3`. Nên con số `λ ≈ 0,53` mà `DR-D4-10` §1.2 dùng là **XẤP XỈ, không phải phép đo**, và câu *“đổi mẫu số làm rào nhẹ đi đúng hai lần”* chỉ đúng **dưới giả định λ hằng số** — `std_R` không co đúng `1/λ` khi λ đổi theo lệnh. `DR-D4-12` vì thế **KHÔNG ghim một con số λ nào** (ghim QUAN HỆ, khuôn `TD-0171`); con số do `TD-0184` đo và báo. 🔴 Và `DR-D4-12` §1.7 thêm **một bất biến `TD-0184` phải ĐO, không được giả định**: ở lệnh khớp đủ ba tranche, `Σ rui_ro_da_trien_khai` có bằng `planned_risk_usdt` không — *“công thức nói có”* và *“fill thật cho bằng”* là hai phát biểu khác nhau; lệch ⇒ **DỪNG**, không tự chỉnh hệ số cho khớp. ⚠️ Phần **ngoài** đơn vị của mục này (cách đọc `DR-D4-09` §7 điều 1) **chưa giải** ✅ **PHẦN CÒN LẠI ĐÃ GIẢI 16/09/2026 — `DR-D4-13` §2 (`daaaba9`), chủ dự án chốt:** `std_R`/`mean_R` trong `DR-D4-09` §7 điều 1 là của **`R_trien_khai`** (`DR-D4-12` §1.4); `1,25` và `0,5` **giữ nguyên, áp trực tiếp**. Căn cứ (DIỄN GIẢI): trên `R_trien_khai` mẫu số là rủi ro thật đã bỏ ra, nên cơ chế co của vế (i) không nằm trên con số được đọc ⇒ `std_R` nhỏ là phân tán chặt thật — đúng trường hợp mà ô bên trái tự khai §7 điều 1 **đúng**. 🔴 **CẤM quy đổi ngưỡng bằng `λ`** (`0,5×λ`, `0,5/λ`) khi chỉ có `std(R_ngan_sach)` — `λ` đổi theo từng lệnh (đính chính ngay trên); phải tính lại trên `R_trien_khai` từ bản ghi arm. Mất căn cứ cùng lúc nếu bất biến `DR-D4-12` §1.7 lệch. Loại phương án viết lại thành ngưỡng Sharpe (đổi một điều kiện mở lại đã viết TRƯỚC, không mua thêm gì khi đơn vị đã chốt). Đính chính tại chỗ ở `DR-D4-09` §7 (`cd7ca8e`). ⚠️ Mục này **KHÔNG còn chặn TD-0184** — thứ chặn duy nhất còn hiệu lực là `TD-0247` |
| MT-26 | 🟡 **`DSR_adj` KHÔNG so sánh được giữa các arm khác cỡ mẫu, mà Nhánh 1 §10.2 lại áp cho *"CẤU HÌNH TỐT NHẤT"*.** SUY RA với cùng `mean_R`, std 1,25: Z0-T0 (n≈807) được cộng không **+0,473 R = 4,7 lần** chính ngưỡng 0,10 R so với Z0 (n≈40); Z0-T1 (n≈211) **+0,343 R = 3,4 lần**. Nếu *"tốt nhất"* xếp theo `DSR_adj` thì **arm BỎ bộ lọc trend thắng gần như tự động** — không phải vì nó tốt hơn mà vì nó vào lệnh nhiều hơn, và đó đúng là arm spec dòng 962 cảnh báo sẽ biến Zone Absorption thành mean-reversion. `DR-D4-09` §2.1 mới bắt Z0-T0/Z0-T1 phải so **KHÔNG paired**, nhưng không có dòng nào xử lý thiên lệch theo `n` bên trong chính phép so không-paired đó | §10.2 Nhánh 1 (*"bất kể nó là Z0, Z3 hay Z3b"*, dòng 4253) vs `DR-D4-09` §2.1 + bảng cỡ mẫu §1.1 | 🟡 Không chặn hôm nay (chưa arm nào chạy), **phải giải trước khi đọc kết quả D4** | **CHƯA GIẢI.** Hai câu cần chủ dự án làm rõ: (1) *"cấu hình tốt nhất"* của Nhánh 1 có bao gồm Z0-T0/Z0-T1 không, hay chúng chỉ là arm CHẨN ĐOÁN của §10.1b; (2) nếu có, xếp hạng phải dùng đại lượng bất biến theo cỡ mẫu thay vì `DSR_adj` thô. **Không tự chọn** — đây là đổi cách phán quyết của cổng ✅ **ĐÃ CHỐT 12/09/2026 — CÂU 1, phương án (C): phân đôi hai arm.** `Z0-T1` là **CẤU HÌNH ỨNG VIÊN**; `Z0-T0` chỉ là **arm CHẨN ĐOÁN**, không dự Nhánh 1. 🔑 Căn cứ nằm trong chính spec, không phải cân nhắc tiện lợi: §10.1b viết *"`Z0-T1` ≥ `Z0-T2` → **giảm 3 DOF** → hạ `N`, hạ rào DSR"* — chỉ giảm DOF khi **bỏ tầng 1D khỏi hệ thống SẢN XUẤT**, tức spec **có** tính tới việc NHẬN `Z0-T1`; ngược lại spec dòng 962 cảnh báo `Z0-T0` thoái hoá thành mean-reversion ⇒ đọc như chẩn đoán. ⇒ **D4 có ĐÚNG MỘT cấu hình ứng viên vượt sàn 150: `Z0-T1`** (325,6 lệnh/năm, `n = 206`, ngưỡng hiệu dụng `mean_R` 0,22–0,42 R — cao nhưng **không bất khả** như nhóm C 0,44–0,97 R). Đã thi hành ở `DR-D4-10 §2.1`/`§2.3` (`24b1d27`). 🔴 **(C) CHỈ trả lời CÂU 1. CÂU 2 VẪN MỞ** — *xếp hạng phải dùng đại lượng bất biến theo cỡ mẫu thay vì `DSR_adj` thô* — và **phải giải TRƯỚC khi đọc kết quả D4**. (C) định nghĩa **ai được dự**, không định nghĩa **so bằng gì**; nguyên nhân gốc mà mục này nêu (thiên vị +0,473 R cho arm đông lệnh) **chưa được gỡ**.| 10/09/2026 | Ghi nhận, chờ chủ dự án 🔄 **CẬP NHẬT 14/09/2026 — câu 2 VẪN MỞ, và `DR-D4-12` §8 chỉ ĐỀ XUẤT cách đọc, KHÔNG phán quyết.** Đề xuất, ghi ra để cãi lại được chứ không im lặng bỏ qua: với đúng **MỘT** arm mang cờ `pham_vi_phan_quyet = phan_quyet` (`ARM_MUA_PHAN_QUYET = {"Z0-T1"}`, `arm_record.py:83`), Nhánh 1 là câu hỏi **TUYỆT ĐỐI** ⇒ *“cấu hình tốt nhất”* = `Z0-T1` không đi qua một phép xếp hạng nào ⇒ câu 2 **không kích hoạt ở vòng này**. 🔴 Nhưng đó là **cách đọc**, không phải một quyết định; `TASKS.md:468` vẫn xếp `MT-26` vào bảy mâu thuẫn **phải đóng trước Giai đoạn 5** |
| MT-27 | 🟡 **Không có đường nào đo expectancy baseline mà không tiêu trial — nên vòng lặp *"đo thô → sửa logic → đo lại"* mà mọi quy trình chống overfitting coi là bước ĐẦU TIÊN thì Tool D không mua được.** Checklist ngoài (Giai đoạn 1) đòi *expectancy dương ở backtest thô, chưa tối ưu gì*, và coi việc quay lại sửa logic rồi đo lại là chuyện bình thường. Nhưng `DR-014 §2` tính **mọi lần đánh giá cấu hình** trên CALIB/WFO/LOCKBOX = **1 trial**; N6 cấm điền `0.0` thay cho *chưa đo*; và `CTRL_OUTPUT_ALLOWED` **cố ý không chứa một chỉ số hiệu năng nào** (spec dòng 3605-3607: *"TUYỆT ĐỐI không PnL, không win/loss, không metric theo arm"*). ⇒ Mỗi vòng lặp tốn ít nhất 1 suất trong 114, và trạng thái hiện tại xác nhận hậu quả: **0 con số expectancy/Sharpe/DSR trên toàn repo** (`runs/` 0 thư mục trial, 0 file `.seal`, 4/114 trial đã tiêu đều là B0 với `outcome.expectancy: null`) | Checklist anti-overfitting ngoài (chủ dự án đưa 10/09/2026, `docs/doi-chieu-checklist-antioverfitting.md` §2 mục GĐ1.4) vs `DR-014 §2` + N6 + spec dòng 3605-3607 | 🟡 | **Ghi nhận theo quy tắc 11, KHÔNG tự chọn bên.** Phân loại 🟡 vì D4 hiện đã dừng vì lý do khác (cỡ mẫu, `DR-D4-08 §6`) nên chưa ai cần con số này ngay; **thành 🔴 ngay khoảnh khắc có người cần một expectancy baseline**. 🔑 Họ hàng gần **MT-19** nhưng **khác đối tượng**: MT-19 về **đại lượng mô tả** (đếm, phân bố), MT-27 về **chỉ số hiệu năng** — và chính vì `CTRL_OUTPUT_ALLOWED` loại bỏ chỉ số hiệu năng một cách có chủ đích nên đường vòng EXPLORE/CTRL đã dùng cho MT-19 **không áp được** ở đây. Ba đường có thể có, chưa chọn: (a) tiêu 1 suất B3 cho một phép đo baseline và khai trước; (b) mở rộng `CTRL_OUTPUT_ALLOWED` bằng một DR — 🔴 nhưng đó là danh sách CHO PHÉP, nới một lần là mất tính fail-closed; (c) chấp nhận không bao giờ có baseline thô và đọc D4 thuần bằng phép so giữa các arm | 10/09/2026 | ⏳ chờ chủ dự án |
| MT-28 | 🟡 **Spec để TRỐNG ô ngưỡng DSR trong khi code đã có số — code đi TRƯỚC spec.** `tool-d-smart-dca.md:4260` vẫn là `DSR-adjusted expectancy ≥ ......   🔴 PHẢI ĐIỀN SỐ Ở D0-PRE`, còn `src/tool_d/gates/thresholds.py:24` đã có `DSR_ADJ_EXPECTANCY_MIN = 0.10` theo `DR-D0PRE-03` (TD-0041, blocker B6 đã gỡ từ 06/09/2026) | spec dòng 4260 vs `src/tool_d/gates/thresholds.py:24` (`DR-D0PRE-03`) | 🟡 | **Ghi nhận, không tự sửa spec (quy tắc 5).** 🔑 N1 nói spec thắng khi mâu thuẫn — nhưng ở đây spec là một **Ô TRỐNG**, không phải một giá trị khác, nên đây là **trôi lệch tài liệu**, không phải xung đột giá trị: không có hai con số để chọn giữa. Rủi ro thật là người đọc spec kết luận *"ngưỡng chưa điền ⇒ blocker B6 còn"* — đúng cái bẫy đã lừa hai phiên hồi 08/09/2026 (xem đính chính trong `CLAUDE.md`). Đề xuất: điền số vào spec kèm trỏ `DR-D0PRE-03`, hoặc ghi một dòng đính chính tại chỗ theo tiền lệ đã dùng. ✅ Ghi nhận kèm: `thresholds.py:30` giữ `BEST_KNOWN_DSR_ADJ_EXPECTANCY = -inf` là **ĐÚNG** N6 — chưa có lần đánh giá nào, không phải số bịa | 10/09/2026 | ⏳ chờ chủ dự án |
| MT-29 | 🔴 **§10.1 bắt buộc chạy TÁCH RIÊNG theo hướng, nhưng §10.2 phát biểu tiêu chí cổng KHÔNG theo hướng — spec không nói tiêu chí là mỗi hướng hay tổng hệ thống.** §10.1 dòng 4187: *"phải chạy **hai lần độc lập** — một cho Long, một cho Short — không giả định kết quả Long tự động áp dụng cho Short"*; checklist bất biến dòng 4973-4974 lặp lại: *"chạy TÁCH RIÊNG cho Long và Short, **không dùng chung kết quả**"*. Nhưng trong toàn bộ §10.2 (dòng 4251-4326) chỉ có **hai** chỗ nhắc hướng: PBO *"tính riêng mỗi hướng"* (4287) và tiền đề Δ_R Long/Short riêng (4297). Hai tiêu chí quyết định — *"số lệnh/năm ≥ 150"* (4274) và *"DSR-adjusted expectancy"* (4260) — **KHÔNG nói** là mỗi hướng hay tổng | §10.1/§10.1b (dòng 4187, 4234, 4973-4974) + §3.3d (1162) vs §10.2 (4260, 4274) | 🔴 Chặn cách ĐỌC kết quả D4 | ✅ **ĐÃ CHỐT 12/09/2026 — phương án (A): sàn 150 lệnh/năm áp cho MỖI HƯỚNG.** 🔑 **Lý do quyết định là TÍNH NHẤT QUÁN, không phải sự khắt khe:** Nhánh 1 có HAI rào cùng phục vụ một mục đích *"đủ mẫu để kết luận"* — sàn 150 (thô) và rào DSR (tinh). Rào DSR tính **trên từng arm, từng hướng**, vì §10.1 dòng 4187 cấm gộp kết quả hai hướng. Chọn (B) sẽ đo hai tiêu chí của CÙNG một cổng trên HAI đơn vị khác nhau — sàn tính trên hệ thống gộp, rào DSR tính trên từng arm — khiến sàn **thôi canh thứ nó sinh ra để canh**: hệ thống gộp qua sàn trong khi từng arm vẫn thiếu mẫu cho phán quyết. Đó đúng lớp chốt rỗng mà `MT-26` mô tả, ở một chỗ khác. (A) giữ hai rào trên **cùng đơn vị**, và khi đó chúng nói cùng một điều — đo được: cả hai đều bảo *"chưa đủ mẫu"*. 💰 **Giá phải trả, đã biết và chấp nhận có ý thức:** ở tốc độ **44,8 lệnh/năm** (TD-0205/TD-0212, đo trên WFO), Long-only **KHÔNG BAO GIỜ** qua được sàn. 🔴 **Hệ quả thi hành ngay:** điều kiện dừng của `DR-D4-08` §6 chuyển từ *"chờ chủ dự án quyết"* sang **"đã quyết và VẪN DỪNG"** — D4 Long-only **không chạy, 0 suất trial tiêu**. Nhất quán với `DR-D4-10` §2.4 (mặc định `Z0` single-entry), hai quyết định cùng chiều. ⚠️ **KHÔNG gỡ** `MT-24`/`MT-25` — hai cái đó vẫn 🔴 chặn `TD-0184` vì lý do khác. 🔓 **Điều kiện xét lại, viết TRƯỚC (khuôn OQ-07):** chỉ mở lại khi tốc độ lệnh **CỦA MỘT HƯỚNG** đạt ≥ 150/năm **đo được** — KHÔNG phải khi tổng hai hướng đạt, và KHÔNG phải bằng cách nới sàn. Đường hợp lệ để đạt: mở Short (một bộ arm riêng, có sàn riêng), mở rộng pool, hoặc kéo dài cửa sổ — mọi đường đều phải qua DR riêng 🔄 **ĐÍNH CHÍNH 12/09/2026 — giữ nguyên chữ cũ làm lịch sử.** Kết luận *"Long-only KHÔNG BAO GIỜ qua được sàn ⇒ D4 Long-only không chạy, 0 suất trial tiêu"* lấy căn cứ từ **44,8 lệnh/năm** — mà 44,8 là con số của arm **`Z0`**. Đo trên cùng `td0212-ba-arm-sau-va.json`: **`Z0-T1` = 325,6/năm** và **`Z0-T0` = 1.396,0/năm**, **cả hai VƯỢT sàn 150**. ⇒ Câu đó đúng với **7/9 arm**, **không đúng với hai arm còn lại**, và kết luận *"D4 không chạy"* **ngầm giả định** `Z0-T0`/`Z0-T1` không phải cấu hình — tức nó **ngầm trả lời câu 1 của `MT-26`** trong khi `MT-26` lúc đó đang ghi CHƯA GIẢI. ✅ `MT-26` (C) chốt cùng ngày đã trả lời: dưới (C) thì **D4 CÓ một nhánh chạy được** (`Z0-T1`). 🔑 Phần **cốt lõi** của mục này **KHÔNG lung lay**: chốt *"sàn 150 áp cho MỖI HƯỚNG"* và lý do tính-nhất-quán-đơn-vị vẫn đứng nguyên; chỉ **phạm vi** của câu suy ra bị thu hẹp. Đây là ca *"mọi mảnh đều đúng, chỉ PHẠM VI kết luận sai"* — `research-log` 12/09.| 12/09/2026 | Chủ dự án |
| MT-30 | 🟡 **`_zone_dinh_tren` lọc `> p_avg` bằng một `p_avg` ĐÃ CŨ ⇒ zone đối diện hợp lệ bị loại vĩnh viễn, im lặng.** Danh sách ứng viên TP1 đóng băng ở tranche 1 (`ZoneAbsorption.py:784`) với `p_avg = trade.open_rate` **lúc đó** = `p1`; nhưng đây là DCA-XUỐNG nên `p_avg` tụt dần khi tranche 2/3 khớp, và `chon_muc_tp1` về sau lọc lại với `p_avg` mới. Zone đỉnh nằm trong khoảng **(`p_avg` cuối · `p1`]** đã bị loại ở bước đóng băng và **không bao giờ được xét lại**. Số đo được trên fixture TD-0187: `p1 = 95,339` vs `open_rate` cuối `= 94,972` — một cửa sổ rộng **0,367** (≈ 0,46 `R_eff`) bị mù. 🔑 Đáng chú ý: `chon_muc_tp1` (`take_profit.py:290`) **đã tự lọc** `z > p_avg` bằng `p_avg` ĐÚNG, nên phép lọc ở `_zone_dinh_tren` vừa **THỪA** vừa **CÓ HẠI** | `ZoneAbsorption.py:784` + `_zone_dinh_tren` vs `take_profit.chon_muc_tp1:290` và `DR-D4-06` §3 | 🟡 Vô hại trên fixture hiện tại (không có zone nào rơi vào khoảng đó); trên dữ liệu thật là zone hợp lệ mất im lặng | **CHƯA GIẢI — không tự sửa.** Bỏ phép lọc `> p_avg` khỏi `_zone_dinh_tren` là cách sửa hiển nhiên, nhưng nó chạm `DR-D4-06` §3 — quyết định *"đóng băng danh sách ứng viên ở tranche 1"* mà chủ dự án đã chốt 09/09/2026 để mục tiêu TP1 không nhảy hai chiều. Phải phân biệt được **đóng băng DANH SÁCH** (quyết định đã chốt, giữ) với **lọc bằng `p_avg` sai thời điểm** (khả năng là sơ suất) — hai thứ nằm trong cùng một dòng mã, và tách chúng là một quyết định về hành vi TP, không phải một bản vá kỹ thuật. ⚠️ Sửa xong sẽ **đổi tập ứng viên TP1** ⇒ đổi mọi con số D4 đã đo (như TD-0207 đã phải ghi) | 10/09/2026 | Ghi nhận (phiên TD-0210), chờ chủ dự án |
| MT-31 | 🔴 **Ma trận ablation §10.1 thiết kế mỗi arm đổi ĐÚNG MỘT biến, nhưng `Z1` đổi hai thứ cùng lúc — và bảng kết quả KHÔNG có cách nào lộ ra điều đó.** `Z1` thay SL neo-zone bằng `2,2×ATR` ⇒ `r_eff_plan` đổi ⇒ cỡ lệnh đổi (`sizing` chia `R_eff`) ⇒ **cổng kết nạp §6.8f chặn khác**. Đo được (TD-0215, `td0215-z1-vs-z0-tap-lenh.json`, 88 mã EXPLORE, WFO, 0 trial, `Z0`+`Z1` chạy CÙNG một lượt): `n_A = 22 · n_B = 6 · n_giao = 6 · chi_co_o_A = 16 · **chi_co_o_B = 0**` ⇒ `Z1` là **tập con NGHIÊM NGẶT** của `Z0`, **72,7%** số lệnh biến mất — và biến mất ở tầng kết nạp, **không phải vì SL tốt hay xấu**. Mà `Z1` chính là arm §10.1 cấp cho câu *"SL neo zone có tốt hơn SL theo ATR không"* | §10.1 (*"mỗi arm đổi một biến"*) vs `arm_switches.sl_neo_atr` + `sizing` + cổng kết nạp §6.8f | 🔴 Chặn cách đọc `Z1` trong D4 | **CHƯA GIẢI — ghi theo quy tắc 11, không tự chọn bên.** 🔑 **Phải tách HAI vế, gộp lại là xử sai:** (i) **nội tại KHÔNG hỏng** — trên 6 lệnh giao, `Z1` vs `Z0` khác đúng MỘT biến, phép so paired ở đó **hợp lệ về cấu trúc**; nó **HẸP**, không **VÔ HIỆU**. (ii) **ngoại suy HỎNG** — 6 lệnh đó là tập được CHỌN bởi chính cơ chế tương quan với biến đang xét (cỡ lệnh là hàm của SL), nên không suy rộng ra 22 lệnh. Thiếu phân biệt này thì người sau hoặc **vứt phép so đi** (mất phần đúng) hoặc **dùng nó phán quyết** (dùng phần sai). Cỡ mẫu còn cắt thêm: `n_giao = 6` ⇒ thuế nhiễu paired **0,497 → 1,571 R** tuỳ `ρ` = **5,0 → 15,7 lần** ngưỡng 0,10 R. 🔴 **Vì sao hạng này NGUY HƠN hai hạng đã ghi** (điểm phiên `-c0` nhấn, và nó đúng hơn cách tôi đặt vấn đề ban đầu): `MT-21`/`MT-15` (*"biến không đổi gì"*) và `DR-D4-10` §2.5 (*"biến gần như không tồn tại"*) **TỰ LỘ RA** — bảng kết quả trống hoặc trùng khít, nên có cơ hội bị bắt. Hạng này thì **con số vẫn ra, vẫn hợp lệ, vẫn nằm đúng ô của nó trong bảng ablation — không có gì để nghi.** Nó chỉ trả lời **một câu khác câu in trên đầu cột**. ⚠️ Chưa kiểm ba arm còn lại (`Z0-S1` đổi notional, `Z0-V1` đổi điều kiện (c)) có cùng hình dạng không — cả hai cũng lệch `n` so với `Z0` (31 và 40), nên **không được giả định là sạch**. Quan sát thật thu được dù hẹp: ngay trên 6 lệnh chung, `Z1` có `TIME_STOP` **2/6 (33%)** trong khi `Z0` **0/22** | 11/09/2026 | Ghi nhận (phiên TD-0215), chờ chủ dự án |
| MT-33 | 🔴 **Các arm Phần 2 KHÔNG còn lồng nhau sau bản vá `TD-0207` — *"nới bộ lọc chỉ THÊM lệnh"* là SAI.** Đối chiếu `so_tap_lenh` trước/sau vá: `Z0 ⊆ Z0-T0` từ **đúng tuyệt đối** (`chi_co_o_B = 0`, `td0205`) thành **1 lệnh mồ côi** (`td0212`); `Z0-T1 \| Z0-T0` lệch từ **1 → 20 lệnh = 12,5%** của `Z0-T1`. 🔑 **Đây là điều vô lý cần giải thích, không phải một con số phụ:** `Z0-T0` **tắt hết** bộ lọc trend, nên mọi lệnh qua được bộ lọc **đầy đủ** thì đương nhiên phải qua được **không lọc**. Cơ chế duy nhất giải thích được là **TRANH CHỖ MỞ** (một lệnh mở mỗi cặp): ở `Z0-T0` có 686 lệnh, một tín hiệu **chưa lọc** đến sớm hơn chiếm mất chỗ mà lệnh **đã lọc** sẽ dùng — cùng cơ chế đã làm `Z0-T0` mất 8% lệnh sau vá | §10.1b (ma trận ablation giả định mỗi arm đổi ĐÚNG MỘT biến) vs `td0205`/`td0212` `so_tap_lenh` (đo được) | 🟡 Không chặn hôm nay (D4 chưa chạy), **phải giải trước khi đọc kết quả D4** | **Ghi nhận theo quy tắc 11, chưa chọn cách xử.** Đây là **hạng (3)** của `DR-D4-10 §2.5` (*"biến đổi KÈM thứ khác đổi theo"*, tiền lệ `MT-31`/`Z1`) áp vào chính hai arm mà `MT-26` (C) vừa xếp là ứng viên/chẩn đoán. Hệ quả: phép so `Z0-T1` vs `Z0` **không phải phép so trên cùng một tập nền** ⇒ (i) theo `DR-D4-09 §2.1` phải so **paired trên tập giao** và khai riêng phần không giao; (ii) theo `DR-D4-09 §2.4` (`206/28 = 7,4 ≥ 2`) **CẤM** xếp hạng bằng `DSR_adj` thô — trùng đúng **câu 2 của `MT-26` đang mở**. Đã thi hành phần mô tả ở `DR-D4-10 §2.5` (`24b1d27`). ⚠️ **Ghi nợ:** chưa ai đo quan hệ tập hợp giữa nhóm `Z0`/`Z0-T1`/`Z0-T0` và nhóm `Z3`/`Z3b`/`Z2` — `Z0` và `Z3` **trùng CỠ** (22) nhưng **chưa có phép đo nào chứng minh trùng TẬP** ⇒ `TD-0228` (🔓, `17bb4c2`) | 12/09/2026 | Ghi nhận, chờ chủ dự án |
| MT-34 | 🔴 **POOL MÀ D4 SẼ BACKTEST LÀ ẢNH CHỤP NGÀY HÔM NAY — `DR-D1-01` §3 đã chốt KHÔNG chấp nhận lệch sống sót, nhưng phần thi hành chưa bao giờ tồn tại.** Tự kiểm 12/09/2026: `grep -rn "delisted_at" --include=*.py` cho **đúng 5 kết quả** — `src/tool_d/pool.py:32` (khai báo), `:115` (dùng), **3 chỗ còn lại đều trong `tests/unit/test_pool.py`** ⇒ **không mã sản xuất nào gán nó**. Bộ sinh `SymbolStat` DUY NHẤT (`pool.py:41 build_symbol_stats`) lọc `status == "TRADING"` + `quote_volume_24h` từ ticker **24h hiện tại** ⇒ `pairlist_point_in_time()` **không có người gọi nào có thể đúng**. `config/pool.yaml` (102 mã) chốt bằng `compute_pool()` (`entrypoints/build_pool.py:226`); docstring `:3-7` của chính file đó tự khai *"H1-D ĐẦY ĐỦ … là việc RIÊNG của D1 — **CHƯA viết ở đây**"*, mà Khối 10 `TASKS.md:146-156` có **đúng ba dòng** TD-0095/0096/0097, **cả ba ✅**, và cả ba chỉ là **hàm thuần + test dựng tay**. 🔑 **Lần này ✅ KHÔNG sai** — TD-0096 thật sự đã viết hàm và thật sự có test; thứ sai là **PHẠM VI** của cái ✅: nó chứng nhận một **HÀM**, và bị đọc thành chứng nhận một **NĂNG LỰC**. **ĐÃ ĐO — `TD-0230`, 0 trial, `docs/du-lieu-do/td0230-lech-song-sot-pool.json`** (liệt kê kho tĩnh `data.binance.vision`, metadata SÀN, không đọc một nến pool nào): **(a) kênh *"mã chưa sinh vẫn nằm trong rổ"* — 44/102 mã pool (43,1%) KHÔNG tồn tại tại `T0`, 24/102 (23,5%) không tồn tại tại `T1`** (thêm 2 mã cùng tháng `T0` không phân giải được); **(b) kênh *"mã đã chết bị loại khỏi rổ"* — 31 mã đã ngừng sinh nến, trong đó 15 ngừng TRONG CALIB `[T0,T1]` và 0 ngừng trong WFO `[T1,T2]`** | `DR-D1-01` §3 (*"Không mở DR chấp nhận survivorship bias"*) + spec `:4338` (*"mỗi lần đổi pool = MỌI số cũ không so sánh được"*) vs `config/pool.yaml` + `build_pool.py:226` (`compute_pool()`, tiêu chí đo tại 09/2026) | 🔴 **Chặn TD-0184** — lý do thứ ba, và nó về **NỀN DỮ LIỆU** chứ không về cỡ mẫu như hai lý do cũ | **CHƯA GIẢI — ghi nhận theo quy tắc 11, không tự chọn bên.** 🔴 **Chiều lệch là chiều PASS cho `expectancy`** (mã sập → rời sàn → không vào mẫu ⇒ mẫu còn lại đẹp hơn trung bình thật) — loại lệch mà **không cổng nào của dự án tự bắt được**. ⚠️ **KHÔNG khai chiều cho `tần suất lệnh`**: chưa ai đo, và có thể NGƯỢC (mã sập biến động mạnh hơn, mà hệ này vào lệnh theo zone + xác nhận ⇒ có thể sinh NHIỀU tín hiệu hơn ⇒ loại chúng làm tần suất THẤP đi, tức chiều FAIL). Khai một chiều chưa đo — kể cả chiều *"nghiêm hơn"* — vẫn là khai; đó là *"ảnh trong gương của PASS RỖNG"*, hậu quả là **một phép đo không bao giờ chạy** và không để lại dấu vết nào để nghi. ⚠️ **Con số 385** (*mã sống tại `T1` mà pool không có*) trong artifact **KHÔNG phải số về lệch sống sót** — nó bị chi phối bởi mã ĐANG niêm yết nhưng TRƯỢT ngưỡng volume 15tr USDT, vì phép đo chưa áp tiêu chí volume tại `t` (cần nến thật ⇒ với mã pool thì **chặn cứng ở MT-19**). Đã khai trước trong `ranh_gioi`; **không được dẫn con số đó ra ngoài**. ⚠️ **Và con số 15 của kênh (b) chưa phân biệt được ĐỔI TÊN với SẬP:** danh sách gồm `MATICUSDT` (→ POL), `RNDRUSDT` (→ RENDER), `EOSUSDT` (→ A/Vaulta) — **đổi ticker thì tài sản vẫn sống**, nên đó KHÔNG phải lệch sống sót; lẫn hai thứ là đọc 15 thành *"15 coin sập trong CALIB"*. Phân biệt được cần nguồn về đổi tên, phép đo này không có. 🔴 **KHÔNG tự đổi `config/pool.yaml`** — `build_pool.py` **từ chối ghi đè** có chủ đích (spec `:350-352`: không được chọn lại pool sau khi đã thấy kết quả), và spec `:4338` nói đổi pool ⇒ backfill lại toàn bộ + mọi số cũ mất hiệu lực. **Ba đường, chưa chọn:** (a) dựng H1-D đầy đủ TRƯỚC D4 — đắt nhất, nhưng là thứ `DR-D1-01` §3 đã chốt; (b) chạy D4 trên pool hiện tại **kèm hạn chế khai vào `d4_han_che`** — rẻ, và có một căn cứ THẬT đỡ: kênh (b) **bằng 0 trên đúng cửa sổ WFO**, nên phần *"mã đã chết"* không chạm phán quyết `Z0-T1`; nhưng kênh (a) thì **không** đỡ được — 23,5% rổ không tồn tại tại `T1`; (c) thu cửa sổ D4 về đoạn rổ ổn định. ⚠️ **Phạm vi rộng hơn D4:** EXPLORE **cũng** là ảnh chụp hôm nay, nên **toàn bộ bảng chín arm** `td0205→td0215` thừa hưởng cùng kênh lệch — không riêng `DR-D4-10`. 📌 **Phân biệt phải giữ:** đây là lệch **THÀNH PHẦN MẪU** (ai có mặt), **không** phải lệch **ĐỘ DÀI** (có bao nhiêu dữ liệu) — `td0212` cho `ma_nam = 50,12` trên 88 mã × 0,632 năm = **0,569 năm/mã < 0,632**, tức kịch bản **đã** trừ phần dữ liệu thiếu của mã lên sàn muộn. Gộp hai thứ sẽ dẫn tới vá một chỗ vốn đã đúng. 🔴 **Khoảng hở kèm theo, chưa có mã nào nên chưa hỏng:** bộ dựng pool point-in-time nào lấy ứng viên từ kho **bằng lọc TÊN** sẽ hút **191 mã `TRADIFI_PERPETUAL`** (cổ phiếu token hoá — `AAPLUSDT`/`AMZNUSDT`/`ANTHROPICUSDT`, đo được `status=TRADING`) vào pool crypto. `pool.py:41` đang lọc `contractType == "PERPETUAL"` **đúng** nên mã hiện tại an toàn; hở nằm ở **phía kho**. 📌 **Đề xuất kèm, KHÔNG tự làm (quy tắc 5):** con số *"219 mã đã huỷ niêm yết"* của `DR-D1-01` §1 **đếm quá tay** — phép trừ `874 − 658` trừ một tập lọc theo `contractType` (phía sàn) khỏi một tập lọc theo **TÊN** (phía kho); đo được `kho − perp = 210` mà **179 trong số đó VẪN sinh nến hôm nay**, số ngừng thật là **31**. Đề nghị **gắn đính chính tại chỗ, giữ nguyên chữ cũ** (tiền lệ `DR-D0PRE-06`/`MT-29`) ở `DR-D1-01` §1 và dòng `TD-0095` của `TASKS.md`. **Kết luận của `DR-D1-01` KHÔNG lung lay** — nguồn có thật và cho mốc ngày thật: xác nhận độc lập chính ca §2 đã kiểm tay, `BTTUSDT` = **2021-04 → 2022-01**, khớp *"06/04/2021 đến 26/01/2022"* | 12/09/2026 | Ghi nhận (phiên `[3f7d14]`), chờ chủ dự án 🔄 **ĐÃ ĐO 13/09/2026 — `TD-0231`, `4f2beed`, 0 trial. Giữ nguyên chữ trên làm lịch sử.** Mục này ban đầu gộp mọi lệch vào một khối; đo xong tách được **BA KÊNH**, và chỉ một kênh gây **thiên lệch** trên đường phán quyết: **(1) mã đã chết bị loại khỏi rổ** = **0 trong WFO** (15 trong CALIB) — đã đo ở `TD-0230`; **(2) mã chưa sinh vẫn trong rổ** = **cỡ mẫu, KHÔNG phải thiên lệch** (độ phủ WFO **624/714 = 87,4%**), vì mã chưa tồn tại **không sinh lệnh nào** nên không làm các lệnh còn lại trông đẹp hơn, và `td0212` đã chuẩn hoá theo `ma_nam` thật (0,569 năm/mã < 0,632); **(3) tiêu chí volume đo tại 09/2026** — kênh thật, nay đã đo. 🔴 **Kết quả tại `T1` (mốc DUY NHẤT chạy vào phán quyết D4): `pool.yaml` = 102 mã, pool ĐÚNG tại `T1` = 116 mã, CHUNG chỉ 55. `K = 61` (52,6% pool đúng vắng khỏi `pool.yaml`), `M = 47`.** Tại `T0`: `K = 122` (74,4%); tại `T2`: `K = 38` (40,4%). **Cách đọc viết TRƯỚC khi chạy** (trong chính kịch bản, commit `ab290eb` **trước** lượt chạy — kiểm bằng thứ tự commit) đặt ngưỡng `K/|pool_T1|` ≲ 10% ⇒ **52,6% là gấp hơn NĂM LẦN**, tức kết cục *"K lớn"*: **WFO đang đo SAI RỔ theo chiều PASS**. **Phần KHÔNG nhiễu, tách ra để kết luận không bị bác bằng *"nhiễu ngưỡng volume"*:** trong `M = 47` có **24 mã KHÔNG TỒN TẠI tại `T1`** ⇒ **không thể** thuộc pool đúng, bất kể volume; 23 mã còn lại tồn tại nhưng trượt tiêu chí — phần này **có** thể nhiễu. 🔑 **Và `TD-0231` là người gọi mà `pool.py:112` đòi suốt từ TD-0096 và chưa từng tồn tại** — nó cấp `quote_volume_24h` **tại `t`** rồi gọi `pairlist_point_in_time()` đã có sẵn, **không sửa `pool.py`**. ⚠️ **BA HẠN CHẾ TỰ KHAI:** (a) nhiễu ngưỡng volume **một ngày** chưa định lượng được, **và đó là lỗi của artifact**: **không lưu volume từng mã** nên không ai kiểm được bao nhiêu mã nằm sát ngưỡng 15tr — kết luận không phụ thuộc nó (52,6% vs 10%) nhưng **tính tái lập thì có**; (b) **428/475** mã ở `T1` dùng mốc onboard **THÁNG** (xấp xỉ), 47 mã dùng mốc **NGÀY** chính xác (chỉ tải thêm khi `|tuổi − 180| ≤ 31`) — artifact ghi riêng, không gộp; (c) 🔴 **phép chạy `pairlist_over_time()` trong artifact KHÔNG chứng minh gì cả** — nạp cho mỗi mốc đúng tập pool đúng của mốc đó với volume giả `2×` sàn nên dĩ nhiên `explore = 0`; **ràng buộc §9c.4b KHÔNG được thi hành**. Đây là lỗi thiết kế bước verify, khai ra thay vì để nó trông như một phép kiểm đã chạy. 🔴 **Vẫn KHÔNG tự đổi `config/pool.yaml`** (chưa đổi từ `3f20c66`) — ba đường của mục này giữ nguyên, nhưng nay chọn giữa chúng **bằng một con số** thay vì bằng một lập luận. ⚠️ Và lệch CALIB **không chạm** phán quyết D4: mọi bằng chứng D4 mang `timerange: 20250612-20260129`, `DR-D4-09:23` ghi *"không phải toàn bộ [T0,T2]"*, dữ liệu 5m chỉ có cho `[T1,T2]` (`TD-0200`) ⇒ con số 44/102-tại-`T0` là hạn chế của *tương lai nếu ai chạy CALIB*. Bản phân tích đầu của phiên `[3f7d14]` đặt nó thành tiêu đề — **sai phạm vi**, đã đính chính. 🔴 **CHỦ DỰ ÁN CHỐT 13/09/2026: đi ĐƯỜNG A-ĐẦY-ĐỦ** (dựng lại rổ point-in-time có xuất xứ TRƯỚC D4), sau khi thấy đề xuất **A-tối-thiểu** của phiên `[3f7d14]` và **chọn khác** — ghi nhận để không ai đọc ngược. ⚠️ **NHƯNG tính tới 14/09/2026 nó VẪN CHƯA CÓ MÃ VIỆC NÀO**, và đây là thứ đáng lo nhất còn lại của mục: mã `DR-D4-11` từng dự kiến cho nó đã được **nhường** cho `TD-0236` (tiêu chí đóng cổng D4) — phía đó sắp commit, phía này chưa mở, và cả hai đều **không có định danh máy đọc được** trên đĩa lúc phân xử (tiêu chí `TD-0119`/`TD-0120`) ⇒ DR cho rổ point-in-time nay **chưa có cả tên**. 🔴 Hệ quả nói thẳng: đây là hạng mục **nhiều tuần** (spec `:4338` xếp H1-D là *"đường găng, tốn nhiều tuần nhất ở Tool A"*), nó **chặn `TD-0184`** tức chặn D4 tức chặn mọi thứ sau đó, và nó là việc duy nhất còn lại mà **mỗi ngày không mở là một ngày mất thẳng** — trong khi mọi việc nhỏ hơn quanh nó đang được đóng rất nhanh. 📌 Ba điều kiện đã biết trước, ghi ra để lúc mở khỏi dựng lại: **(a)** `build_pool.py:210-216` từ chối ghi đè và tự khai đường hợp lệ duy nhất là *"xoá thủ công + ghi DR mới"* (exit 94) ⇒ **DR phải commit TRƯỚC mọi dòng mã**; **(b)** spec `:4338` — *"mỗi lần đổi pool = backfill lại toàn bộ dữ liệu phụ trợ, và MỌI số cũ không so sánh được"*, phạm vi chính xác là các số **quy đổi sang pool** (`325,6` · `n = 206` · `44,8` · phép so sàn 150), còn phép đếm **EXPLORE thô vẫn đúng** như phép đo EXPLORE; **(c)** rổ backtest hiện **KHÔNG CÓ XUẤT XỨ** — `runs/pool_pairs.json` (102 mã, thứ `download-data` thật sự đã dùng) **không được git theo dõi** (`git ls-files runs/` chỉ ra `.gitkeep`) và **không có bộ sinh nào trong repo**, nên A-đầy-đủ **phải** sinh rổ bằng mã có xuất xứ — nếu không thì đổi pool xong vẫn không ai biết backtest chạy trên rổ nào |
| MT-35 | 🔴 **KHOÁ `tier_c.arm_ablation.arm` LÀ MỘT CHUỖI NHƯNG GỘP HAI TRỤC — nên arm SẢN XUẤT và arm ĐƯỢC PHÁN QUYẾT không phải một, và chưa chốt nào nói về trục thứ hai.** `arms.py:TANG_THEO_ARM` ánh xạ `"Z0" → DAY_DU` (đủ bốn điều kiện Phần 2). Đo trên `td0212-ba-arm-sau-va.json` (EXPLORE, WFO, 0 trial), cả ba arm đều **single-entry** (`phan_bo_tranche` 100% tranche 1) nên **trục DCA không phân biệt chúng**: `Z0` (`DAY_DU`) = 22 lệnh = **44,8 lệnh/năm quy đổi pool — DƯỚI sàn 150**; `Z0-T1` (`CHI_4H`) = 160 lệnh = **325,6/năm**; `Z0-T0` (`KHONG`) = 686 = **1.396,0/năm**. `DR-D4-10` §2.1 (`:174`) chốt **`Z0-T1` là cấu hình ứng viên DUY NHẤT vượt sàn**, `MT-26` (C) chốt `Z0-T0` chỉ là arm **chẩn đoán**. §2.4 chốt trục **DCA** (`Z0` thay `Z3`) và §8 chặng (h) giao việc đổi giá trị cho một việc riêng (`TD-0227`) — **nhưng không chốt nào nói trục TREND của arm sản xuất** | `DR-D4-10` §2.4 + §8 chặng (h) (mặc định `Z0`) vs `DR-D4-10` §2.1 `:174` + `MT-26` (C) + `MT-29` (sàn 150 áp **MỖI HƯỚNG**) | 🔴 **Chặn TD-0227** — chủ dự án chốt **TREO** 12/09/2026 | **CHƯA GIẢI — ghi nhận theo quy tắc 11, không tự chọn bên.** ⇒ Nếu D4 phán quyết `Z0-T1` **PASS** mà production chạy `arm: "Z0"`, hệ thống chạy **TIỀN THẬT** trên một cấu hình **chưa được phán quyết và KHÔNG THỂ được phán quyết** ở tốc độ 44,8 lệnh/năm — đúng câu `MT-29` đã chốt và đã đính chính. 🔑 **Đây là *THIẾU MỘT CHỐT*, KHÔNG phải *hai chốt va nhau*** — và phân biệt đó đổi cách xử: `Z0 ⊆ Z0-T1` **tuyệt đối** (`chi_co_o_B = 0`, `n_giao = 22`), tức `Z0` = `Z0-T1` **cộng một bộ lọc loại bỏ 138/160 lệnh (86%)**. Hai arm không xung đột về định nghĩa; chỉ là chưa ai quyết production chạy vế nào. 🔴 **KHÔNG tự đổi `config/tool_d_config.yaml:139`** — `TD-0227` vốn đã phụ thuộc cổng D4 (`TD-0186`), và đổi arm sản xuất lúc ablation chưa chạy xong là tự đổi thứ đang được đo. **Hai đường, chưa chọn:** (a) `TD-0227` đổi đích sang `"Z0-T1"` để arm chạy thật TRÙNG arm được phán quyết; (b) giữ `"Z0"` như một quyết định CÓ Ý THỨC và khai hạn chế *"cấu hình sản xuất không qua sàn mẫu của Nhánh 1"* vào `d4_han_che`. ⚠️ Đường (b) phải trả lời được một câu: `L-Z57` (TD-0185) đòi GATE D0.9 có đường dẫn tới **DỪNG DỰ ÁN** — một cấu hình sản xuất **không thể phán quyết được** thì cổng lấy gì phán quyết nó | 12/09/2026 | Ghi nhận (phiên `[3f7d14]`), chờ chủ dự án ✅ **ĐÃ CHỐT 13/09/2026 — chủ dự án chọn: CHỐT LUẬT, KHÔNG CHỐT ARM. Giữ nguyên chữ trên làm lịch sử.** Luật, ghi **TRƯỚC** khi thấy kết quả D4: **arm sản xuất = arm PASS Nhánh 1**. `Z0-T1` PASS ⇒ production = `Z0-T1`. `Z0-T1` **INCONCLUSIVE hoặc FAIL** ⇒ **KHÔNG có arm sản xuất**, D5 không mở. 🔴 **`Z0` KHÔNG phải phương án dự phòng.** Căn cứ: spec `:4295` *"NHÁNH 2 … **CHỈ chạy nếu Nhánh 1 đã PASS**"*, và `DR-D4-10` §2.4 **tự khai** rằng spec **không có** mặc định cho trạng thái *"Nhánh 1 INCONCLUSIVE"* ⇒ rơi về `Z0` khi kết quả không như ý là **tự phát minh một mặc định ở đúng chỗ spec để trống, SAU KHI đã thấy kết quả**. **Hai dữ kiện đo được đẩy quyết định, ghi để cãi lại được:** (i) cả `Z0` và `Z0-T1` đều thuộc `ARM_DON_TRANCHE` (`arm_switches.py:73-75`, **khai tường minh** chứ không suy) ⇒ chọn vế nào cũng **tôn trọng** §2.4 (trục DCA); (ii) `mult_edge` cần **50 lệnh live** ⇒ ở `Z0` (44,8/năm) là **13,4 tháng**, ở `Z0-T1` (325,6/năm) là **1,8 tháng** — và điều kiện mở lại **(a)** của `DR-Q3-2026` đòi **chính 50 lệnh đó**, nên chọn `Z0` **đóng băng toàn bộ Idea Queue hơn một năm**. 🔑 *"Chọn `Z0` cho an toàn"* là **ảo giác**: `Z0` chặt hơn ở *chọn lệnh* nhưng đổi lấy **mất khả năng quan sát chính mình** — cổng D0.9 không phán quyết được nó, và `L-Z57` (TD-0185) đòi cổng có đường tới **DỪNG DỰ ÁN** mà cổng thì **không có gì để dừng**. Một cấu hình không đo được không phải lưới an toàn. **Thi hành:** `TD-0227` **đổi ĐÍCH** `"Z0"` → `"Z0-T1"`, giữ nguyên mọi ràng buộc đã ghi (test khoá nêu đích danh DR, kiểm-có-răng, rà DOF, **không làm trước cổng D4**). 🔴 **HỆ QUẢ KÉO THEO, phải mở lại tiền đề của `DR-D4-10` §2.4 (quy tắc 11):** §2.4 hoãn câu DCA bằng *"`n = 319` ⇒ **7,1 năm** ⇒ không bao giờ trả lời được bằng dữ liệu ở tần suất này"* — **7,1 năm tính ở 44,8 lệnh/năm, tần suất của `Z0`**. Ở `Z0-T1` (325,6/năm) `n = 319` cần **0,98 năm**; sau hiệu chỉnh độ phủ 87,4% (284,6/năm) là **1,12 năm**. ⇒ Nếu trục trend giải về `Z0-T1` thì tiền đề *"hoãn không có đường ra"* **TAN**, và điều kiện mở lại `n ≥ 319` của `TD-0226` đọc từ *"không bao giờ"* thành **"khoảng một năm"**. ⚠️ **Đây là SUY, chưa phải ĐO:** `Z3`/`Z3b` **chưa từng được đo dưới `CHI_4H`** (`arms.py:TANG_THEO_ARM` gán cả hai `DAY_DU`), và `Z3` giữ chỗ mở **lâu hơn** nên **tranh chỗ** có thể cho kết quả khác — đúng cơ chế `MT-33`. **KHÔNG dẫn 0,98 năm như một dữ kiện.** Đề xuất: gộp phép đo đó vào `TD-0228` (🔓, vốn đã phải chạy `Z0`+`Z3` cùng lượt), nhãn **"ĐẾM, không phán quyết"** |
| MT-36 | 🔴 **PHÁN QUYẾT NHÁNH 1 KHÔNG CÓ ĐOẠN — `n = 206` tính trên TOÀN cửa sổ, nhưng ba cửa sổ TEST của sơ đồ fold chỉ phủ 147/231 ngày.** Tự kiểm trên đĩa: `n = 206` của `DR-D4-10` §2.1 (`:174`) **không phải một số lệnh quan sát được** — nó là **160 lệnh trên 88 mã EXPLORE** rồi quy đổi 88→102 và annualize trên toàn `[T1,T2]` (tái lập đúng: `325,6 × 231/365,25 = 205,9`; cùng công thức cho `Z0` = 28 và `Z0-T0` = 883). Mà `folds.py:174-185` (neo gốc, `DR-D3-01`) cho ba cửa sổ test = `2025-09-04→10-23`, `10-23→12-11`, `12-11→2026-01-29` — **đoạn 84 ngày đầu (`2025-06-12 → 2025-09-04`) là TRAIN-ONLY ở CẢ BA fold**. Hai cách đọc: `n` = **206** (toàn cửa sổ) vs **~131** (chỉ test) ⇒ thuế nhiễu `h/√n` **0,2144 → 0,2689 (+25%)**, rào `mean_R` (std 1,25) **0,368 → 0,436 (+18%)**. 🔴 **Và không có gì buộc chọn vế nào:** `dsr.py:52 dsr_adjusted_expectancy(mean_r, std_r, n_trades, …)` nhận `n_trades: int` với ràng buộc **duy nhất** `>= 2` — **không có ngữ nghĩa cửa sổ nào**; `chay_wfo`/`sinh_folds` **không có người gọi nào** từ `run_ablation.py` (E3 dừng ở `NotImplementedError` `:76-78`) ⇒ **`TD-0184` sẽ quyết câu này BẰNG CÁCH VÔ TÌNH**. Hai mâu thuẫn kèm theo: `DR-D3-01:29-31` phân vai *"train dùng để **XẾP HẠNG** các arm ablation, còn test kiểm **thứ hạng đó có giữ được** sang đoạn sau"* — khác hẳn *"toàn cửa sổ phán quyết"*; và `DR-D3-01` §5.2 nói sàn 30 lệnh áp *"trong cửa sổ test"* trong khi `orchestrator.py:203-216` dùng `len(equity.pnl_abs)` **không lọc cửa sổ** | `DR-D4-09` §1.1 + `DR-D4-10` §2.1 (`n` = toàn cửa sổ `[T1,T2]`) vs `DR-D3-01` §4/§5.2 + `folds.py:174-185` (test = 147/231 ngày, sàn áp *trong cửa sổ test*); và cả hai vs `dsr.py:52` (`n_trades` không có ngữ nghĩa cửa sổ) | 🔴 **Chặn cách ĐỌC kết quả D4** — phải xong **TRƯỚC** `TD-0184`, không phải trước khi đọc | ✅ **ĐÃ CHỐT 13/09/2026 — chủ dự án chọn: BÁO CẢ HAI, PHÁN QUYẾT TRÊN TEST-ONLY.** `n_phan_quyet` = số lệnh trong **ba cửa sổ test**, là con số Nhánh 1 dùng và là `n_trades` truyền vào `dsr_adjusted_expectancy()`. `n_mo_ta` = số lệnh **toàn** `[T1,T2]`, báo kèm, dán nhãn **MÔ TẢ**, **CẤM** dùng phán quyết. 🔑 **Cùng một lượt chạy, 0 trial thêm** — và nó làm khoảng cách giữa hai số **NHÌN THẤY ĐƯỢC** thay vì bị chọn. ✅ **ĐÃ CÓ MÁY, không để thành `MT-08` lần thứ ba** — `TD-0232` (`80efced`): `registry/schemas/arm_result.schema.json` đặt **cả hai** trường là `required` ⇒ bản ghi chỉ mang một con số `n` là **KHÔNG DỰNG NỔI**; `gates/arm_record.py` canh ba bất biến JSON Schema không so được (`n_phan_quyet <= n_mo_ta`; cờ `pham_vi_phan_quyet` khớp bảng `DR-D4-10` §2.1 với `ARM_MUA_PHAN_QUYET` = tập CHO PHÉP đúng một phần tử `{Z0-T1}`; arm `mo_ta` không được mang `ket_cuc = PASS`); `build_arm_record()` **không nhận** cờ phạm vi làm tham số nên khai sai cờ là **không biểu diễn được** (khuôn `TD-0127`). 28 ca, 3 phép phá thật. 📌 **ĐÍNH CHÍNH KÈM, không tự sửa `DR-D4-10` (quy tắc 5):** ngay ở cách đọc toàn-cửa-sổ, `n = 206` cũng **chưa trừ độ phủ pool** (87,4% trên WFO, `TD-0230`) ⇒ nó nên là **~180**, và thuế nhiễu cao hơn 7%. ⚠️ **Hai thứ mục này KHÔNG giải:** sàn 30 lệnh/fold (`n/fold ≈ 44`, ≈38 sau hiệu chỉnh độ phủ) so với `orchestrator.py:219-231` là **tất-cả-hoặc-không** — một fold dưới sàn ⇒ **cả chuỗi `unreadable`**; và mâu thuẫn `DR-D3-01:29-31` về vai train/test vẫn nguyên. Cả hai để chủ dự án quyết khi dựng `TD-0184` | 13/09/2026 | Chủ dự án |
| MT-37 | 🟡 **`Δ_R(LONG) = 0,1612` ĐO TRÊN ĐÚNG HAI CẶP, và `d3_5_han_che` KHÔNG KHAI điều đó.** Đọc artifact đã commit (đọc, **không sửa** — kỷ luật niêm phong cấm *sửa* và cấm *chạy lại*, không cấm *đọc*): `docs/du-lieu-do/dr015-luot-khop-tranche.json` → `_nguon.cap` = `['1000BONK/USDT:USDT', '1000PEPE/USDT:USDT']`, **91 lượt khớp, 2 cặp phân biệt**, `tap_du_lieu = CALIB`, chiến lược `ZoneAbsorptionMinimal`. Còn `registry/runtime_state.json` → `d3_5_han_che` là dict một khoá `noi_dung`, **1074 ký tự, bốn mục** — (1) đo gián tiếp, (2) chỉ LONG, (3) Bước 3 tương đương, (4) `p_nf = 0` trên tập đã cấp khớp — và **KHÔNG mục nào nói phạm vi MÃ**: thử tám chuỗi `2 cặp`/`hai cặp`/`hai mã`/`2 mã`/`1000BONK`/`1000PEPE`/`so_cap`/`pair`, **cả tám đều vắng**. Đường đi của con số truy được: `_nguon.cap` (2 cặp) → `d3_5_delta_r_niem_phong.LONG = 0,1612` (`trial_ledger_audit.py:852`) → `hieu_chinh_hai_chieu(delta_r=…)` (`buoc4_hieu_chinh_hai_chieu.py:143`) → §4 áp `Δ_R` lên **`ARM_UNG_VIEN = ("Z3","Z3b")`** và chỉ hai arm đó (`:62`, `:116-121` raise nếu thiếu). Chốt fail-closed ở `:160` canh *"đã đo chưa"*, **không** canh *"đo trên bao nhiêu mã"* | `dr015-luot-khop-tranche.json` `_nguon.cap` (2 cặp) vs `d3_5_han_che` (bốn mục, không mục nào khai phạm vi mã) — nơi duy nhất cổng D3.5 tự khai hạn chế | 🟡 **CHƯA KÍCH HOẠT, KHÔNG PHẢI ẢNH HƯỞNG NHỎ** — phân biệt này quyết định hành động: *ảnh hưởng nhỏ* thì có thể **không bao giờ** cần làm gì; *chưa kích hoạt* thì **BẮT BUỘC** phải có mặt trước khi điều kiện nổ | **CHƯA GIẢI — ghi nhận theo quy tắc 11.** 🔑 **Phát biểu đúng mức: KHÔNG phải *"Δ_R sai"*** — nó đo **đúng thứ nó đo**. Thứ thiếu là **PHẠM VI không được khai ở chỗ người ta sẽ đọc**. Cùng họ `MT-29`: mọi mảnh đúng, chỉ **phạm vi** không được nói ra. ✅ **Hôm nay nó KHÔNG chạm đường phán quyết đang sống** (phiên `[f5177d]` nêu, tôi kiểm bằng khai báo tường minh): `Z0-T1` thuộc `ARM_DON_TRANCHE` (`arm_switches.py:73-75`) ⇒ **entry đơn theo thiết kế**, và §4 áp `Δ_R` **chỉ** lên `Z3`/`Z3b` ⇒ ứng viên duy nhất còn phán quyết được **không nằm trong tầm** `Δ_R`. 🔴 **Nhưng điều đó KHÔNG làm nó vô hại — nó đổi LOẠI:** từ *"thiếu sót đang ảnh hưởng kết quả"* thành *"thiếu sót được kích hoạt bởi một sự kiện tương lai **đã có tên**"* (`DR-D4-10` §2.4, điều kiện `n ≥ 319` mở lại câu DCA). Loại thứ hai nguy hơn ở một chiều: **không có triệu chứng nào hôm nay để ai nghi** — đúng họ *"ảnh trong gương của PASS RỖNG"*. ⇒ Phải khai **BÂY GIỜ, khi chưa cần tới nó**; lúc cần thì người khai đã không còn ở đây. 🔑 **Công bằng với cổng D3.5:** `d3_5_han_che` **không** phải bản khai gian — **ba trong bốn mục tự nhận bất lợi cho chính kết luận của nó**. Nó khai **thiếu đúng một chiều**, và chiều đó là chiều **duy nhất không ai nghĩ tới lúc viết**, vì khi ấy cả hệ thống chỉ chạy nổi trên hai mã. ⇒ Vì thế 🟡 *bổ sung một chiều khai*, **KHÔNG** phải 🔴 *cổng khai sai*, và **KHÔNG đề xuất mở lại cổng D3.5**. 📌 **ĐỀ XUẤT CÁCH ĐÓNG, không thi hành: cưỡng chế phạm vi `Δ_R` ở BỘ CHẠY ABLATION (`TD-0184`/`TD-0185`), KHÔNG ở `§4`.** Ba cái lợi: không đụng dấu niêm phong nên không phải trả lời câu *"sửa một cổng đã đóng thế nào"*; `run_ablation.py` còn ở `NotImplementedError` nên **chưa có test khoá nào phải sửa**; và viết fail-closed ngay từ đầu thì đó là *"chốt dựng lúc chưa có số"*, không phải *"chốt dựng lúc đang vướng"*. 🔴 **PHƯƠNG ÁN ĐÃ XÉT VÀ LOẠI — ghi ra để người sau khỏi đề xuất lại** (cùng lý do ghi hai phép kiểm thất bại vào `DR-FAI-01` §4b): *"thêm một vế fail-closed vào chính `§4`"* bị loại vì **hai** lý do độc lập — (a) người gọi `hieu_chinh_hai_chieu()` ngoài module là **đúng một file và nó là TEST KHOÁ** (`tests/lock/test_lz58_…py:39`, helper `_chay()`), nên tham số **bắt buộc** ⇒ phải sửa `L-Z58` ⇒ phải qua ba điều kiện `§7.1` (DR commit **TRƯỚC**, kiểm bằng `git merge-base --is-ancestor`), còn tham số **có mặc định** ⇒ **fail-OPEN**, tức một PASS RỖNG dựng sẵn từ lúc sinh; (b) module đó là **tầng THUẦN** (`grep` cho `open(`/`read_text`/`Path(`/`json.load` ra **0**) và docstring `:6-8` khai thẳng *"§4 cố ý KHÔNG đặt ngưỡng phần trăm — **ngưỡng tuỳ tiện là chỗ uốn kết luận sau khi thấy số**. Nên module này **không có** hằng số nào kiểu `LECH_TOI_DA`"* ⇒ thêm một sàn số mã vào đó là thêm **đúng một ngưỡng tuỳ tiện** vào **đúng module viết ra để không có ngưỡng nào**, và vì **đúng lý do** câu đó nêu (sàn chọn sau khi đã biết `Δ_R` đo trên 2 mã là con số **chọn khi đã thấy số**). ⇒ **Đề xuất sai TẦNG, không sai ý.** ⚠️ **Câu còn mở, không tự chọn đường:** sàn số mã tối thiểu là **quyết định của chủ dự án**, không phải chi tiết triển khai — đặt sai thì nó chặn một phép đo hợp lệ | 13/09/2026 | Ghi nhận (phiên `[3f7d14]`), chờ chủ dự án ✅ **ĐÃ GIẢI 13/09/2026 — `TD-0234` (`237784e`).** Chủ dự án chốt đúng vế mục này đề xuất: **KHÔNG đặt sàn số mã tối thiểu, BẮT KHAI phạm vi**. Thi hành thành **MÁY** chứ không thành một câu trong DR: `arm_result.schema.json` + `arm_record.py` nay bắt buộc `delta_r_pham_vi {so_ma, so_fill, dataset, chien_luoc}` và `so_ma_da_chay` — hai con số đứng cạnh nhau để khoảng cách giữa *thước đo trên mấy mã* và *kết quả đo trên mấy mã* **nhìn thấy được**. 🔑 **Kiểm-có-răng vế quyết định:** THÊM một sàn giả `so_ma >= 10` ⇒ **40 ca đỏ, gồm đúng ca ghim "KHÔNG có sàn số mã tối thiểu"* — tức nếu sau này ai thêm một ngưỡng vào đây thì không cách nào lọt im lặng. Docker 48 passed (28 → 48, **0 ca bị xoá**) |

| MT-38 | 🔴 **CỔNG D4 KHÔNG BAO GIỜ ĐÓNG ĐƯỢC — và vì D5 chỉ mở sau khi nó đóng, một điều kiện không thoả được đang chặn TOÀN BỘ đường tới D11.** Khoá `d4_complete` được định nghĩa là *"đếm được **đúng 18** trial `B2` CONSUMED"* (mục 6, `OQ-11`, chốt phương án PA3 ngày 07/09/2026, commit `6342113`). Nhưng `DR-D4-01` chốt ngày 08/09/2026 rằng D4 chạy **Long trước, 9 trial** (`:15`), và `:113` nói thẳng *"nửa còn lại **KHÔNG tự động** thuộc về Short. Nếu §2b không đạt, 9 suất đó ở lại quỹ chung, không để dành"*. ⇒ Chạy đúng `DR-D4-01` thì sổ có **9** dòng CONSUMED, cổng đòi **18**, và không có đường nào để nó đóng. **Chưa gây thiệt hại vì `close_d4_gate()` chưa tồn tại** — `grep -rn "close_d4_gate" --include=*.py` cho **0 kết quả**; đây là lúc rẻ nhất để sửa, đúng lập luận đã dùng ở `MT-08` | Mục 6 `OQ-11` PA3 (`back-end-note.md:80`, 07/09/2026) ↔ `docs/decisions/DR-D4-01-pham-vi-va-ke-toan.md:15` và `:113` (08/09/2026). 🔑 **Chẩn đoán là *hai quyết định VA NHAU*, không phải *một cái đã thay cái kia*, và đây là dữ kiện chứ không phải suy luận:** `grep -n "d4_complete"` và `"OQ-11"` và `"runtime_state"` trong `DR-D4-01` cho **0 kết quả** ⇒ quyết định 9 **không biết** khoá cổng đòi 18. Con số 18 duy nhất trong DR đó (`:15`) là **B2 = 18 của §10.1b** (9 cấu hình × 2 hướng) trong câu hỏi *"chạy Long trước hay chờ đủ 18 một lượt"*, không phải khoá cổng. Thứ tự commit khớp: `6342113` (07/09) → `df011da` (08/09). Hai chẩn đoán cho hai bản vá khác nhau, nên phải phân biệt trước khi sửa | 🔴 **Chặn `TD-0236`, và chặn việc VIẾT `close_d4_gate()`.** Không chặn D4 chạy — nó chặn D4 **đóng**, tức chặn D5 mở | **CHƯA GIẢI — ghi nhận theo quy tắc 11, không tự chọn bên.** Hai đường đã nhìn thấy: **(a)** khoá đếm **9**, kèm một trường `d4_huong: LONG` để *"đã đóng cho Long"* không bị đọc thành *"đã đóng cho cả hai hướng"*; **(b)** tách hẳn `d4_long_complete` và `d4_short_complete`. 🔴 **Câu thật có thể lớn hơn con số, và người sau đọc mục này rồi đi sửa 18 thành 9 là CHƯA ĐỦ:** `DR-D4-10` đã tái định **7 trong 9 suất thành thống kê MÔ TẢ** (§2.3, và `:427` khai rõ *"DR này KHÔNG tự sửa kế toán 9 suất của `DR-D4-01`"*) ⇒ câu đáng đặt là ***"cổng D4 đóng bằng cái gì, khi phần lớn suất của nó không mua một phán quyết nào?"*** — một câu về **định nghĩa cổng**, không về con số. ⚠️ **CÁCH ĐỌC BỊ BÁC, ghi ra để không có lần thứ ba:** *"mâu thuẫn là 0 khác 18, vì `MT-29` chốt sàn 150 lệnh/năm mà Long chỉ đo được 44,8 nên D4 Long-only không chạy và tiêu 0 suất"*. **SAI.** `DR-D4-10:178-179` đã bác đích danh câu đó bằng chữ từ 12/09: *"44,8 là của arm `Z0`; `Z0-T1` có 325,6 và `Z0-T0` có 1.396,0 — cả hai **vượt sàn**"*, và `:137-138` dặn *"con số phải đọc theo TỪNG ARM, không phải một con số chung"*. Chỗ trượt: `MT-29` chốt sàn áp cho mỗi **HƯỚNG** — đó là trả lời câu *"mỗi hướng hay tổng hệ thống"* — chứ không nói một hướng fail vì **một arm** fail; ghép *"mỗi hướng"* với con số của **một arm** ra một kết luận về cả hướng. Cách đọc này đã xuất hiện **ĐỘC LẬP HAI LẦN** (lần đầu buộc `DR-D4-10` §2.1 phải viết đoạn phản bác tại chỗ; lần hai là một phiên song song, **dù đoạn phản bác đã nằm sẵn trên đĩa**) ⇒ ghi cả cách đọc sai vào đây, cùng lý do `DR-FAI-01` §4b ghi hai phép kiểm **thất bại** | 13/09/2026 → **14/09/2026** | ✅ **ĐÃ GIẢI — `DR-D4-11` (`79db3a8`), thi hành `TD-0236` (`5910f74`). Chủ dự án chốt: cổng D4 đóng bằng HIỆN VẬT, KHÔNG đếm trial.** Cả hai đường nêu trong ô bên trái đều bị loại, và lý do loại mạnh hơn lý do ban đầu ghi ở đây: câu thật không phải *"18 hay 9"* mà là **một cổng đếm trial sẽ PASS kể cả khi `Z0-T1` chưa từng chạy** — PASS RỖNG nằm trong ĐỊNH NGHĨA cổng, nên sửa con số không chạm tới nó. Tiêu chí mới: bản ghi `Z0-T1` mang `pham_vi_phan_quyet='phan_quyet'` + `ket_cuc` đọc được · không bản ghi `mo_ta` nào mang `PASS` · `d4_huong` khai tường minh · `d4_han_che` nhắc câu DCA · số `B2` CONSUMED **khớp số arm đã chạy** (QUAN HỆ, không hằng số — `18` đã lỗi thời sau 24 giờ). `src/tool_d/gates/d4_gate.py` **không chứa số 18 lẫn số 9**, có ca AST ghim. Kiểm-có-răng phá thật trong Docker: bỏ kiểm arm phán quyết ⇒ 2 đỏ · ghim cứng `18` ⇒ 6 đỏ · bỏ kiểm kế toán ⇒ 1 đỏ. **0 trial.** ⚠️ CHƯA xong: `close_d4_gate()` (việc `TD-0186`) và mục 6 `OQ-11` ở trên (đã gắn đính chính, chưa viết lại)** |
| MT-39 | 🔴 **SL KHÔNG SỐNG TRÊN SÀN — và cả chuỗi giả định chặn cổng vào D11 chỉ có nghĩa nếu nó sống trên sàn.** Spec §6.6(1) (`tool-d-smart-dca.md:1955-1957`) viết: *"**SL SỐNG TRÊN SÀN. KHÔNG BAO GIỜ** viết kiểu bot theo dõi giá rồi gửi lệnh đóng khi chạm SL — **cơ chế đó chết cùng tiến trình**"*. Nhưng `config/freqtrade/config.json:49` đặt `stoploss_on_exchange: false`, và chú thích ngay đó tự khai lý do: *"vì `custom_stoploss` (§0c.1) xử lý nội bộ, không đẩy SL lên sàn"*. `ZoneAbsorption.py:972` thi hành đúng cách nội bộ ấy. ⇒ Tool D hôm nay đang chạy **chính xác cái cơ chế §6.6(1) cấm**. 🔴 **Hệ quả nặng hơn bản thân mâu thuẫn:** giả định **D2a** (`spec:2920`) mô tả Freqtrade *"HUỶ + ĐẶT LẠI `STOP_MARKET` mỗi khi giá trị/khối lượng SL đổi"* — đó là hành vi của một stop **trên sàn**; **D2c** đo `gap_ms` của khoảng trống giữa huỷ và đặt lại; §8.3 đòi *"mỗi lần đổi KHỐI LƯỢNG SL = một bản ghi có `gap_ms`"*. Với SL nội bộ thì **không có lệnh nào trên sàn để huỷ**, nên `gap_ms` không có vật lý để đo. Mà `spec:2954` đặt D2b/D2c làm **điều kiện CỨNG để vào D11** | `tool-d-smart-dca.md:1955-1957` (§6.6 ràng buộc 1) ↔ `config/freqtrade/config.json:49` + chú thích `_comment_order_types` + `ZoneAbsorption.py:972` (`custom_stoploss` nội bộ, đã thi hành từ TD-0187). Và ↔ `spec:2920` (D2a/D2b/D2c) + `spec:2966` (phân bố `gap_ms` qua ≥ 30 lần đổi khối lượng SL) + §8.3 | 🔴 **Nổ ở D12, KHÔNG nổ ở D11 hay backtest** — phải nói rõ phạm vi để không ai đọc quá tay: backtest và dry-run **mô phỏng khớp lệnh cục bộ** (`spec:2904-2907`), nên một SL nội bộ hoạt động bình thường ở cả hai và **không làm sai một con số backtest nào đã đo**. Nó nổ khi tiến trình chết lúc đang có vị thế trên tiền thật. 🔴 **Nhưng nó CHẶN cổng vào D11 theo đường khác:** không giải nó thì `TD-0244` (bộ sinh `gap_ms`) không biết phải đo cái gì, và D2c không verify được | **CHƯA GIẢI — ghi nhận theo quy tắc 11, không tự chọn bên.** Ba đường, mỗi đường một cái giá: **(a)** bật `stoploss_on_exchange: true` — khớp §6.6(1) và làm D2a/D2c đo được, nhưng SL của Tool D là **SL neo zone tính động** (`kh.sl`, D0.2), nên phải kiểm Freqtrade có đẩy được một giá trị `custom_stoploss` đổi theo tranche lên sàn không, và đó lại **chính là** giả định D2b chưa verify ⇒ vòng lặp; **(b)** giữ SL nội bộ và **KHAI rủi ro tồn dư** vào một DR (đúng khuôn `DR-D35-01` đã dùng cho testnet) — nhưng khi đó §6.6(1) bị vi phạm bằng văn bản, và ~~`spec:4506` xếp §6.6 vào nhóm *"KHÔNG BAO GIỜ CẮT"*~~ 🔴 **ĐÍNH CHÍNH 14/09/2026 — CÂU GẠCH TRÊN SAI, do chính phiên `[3f7d14]` viết 13/09, phiên `-3f` bắt được.** Đọc nguyên văn `spec:4506-4508`, danh sách là `D0-PRE` · `D9.5` · `H16` · `H17` · `DG8 (§4b)` · `arm Z0-T1 (§10.1b)` · `cổng D3.5 (DR-015)` · `con dấu đo lường + sổ hai trạng thái (DR-014)` — **KHÔNG có §6.6**. ⚠️ **Đính chính phải dừng đúng ở đây, không đi quá:** dòng `:4506` mở đầu bằng *"(giữ nguyên danh sách cũ) + …"*, mà danh sách cũ đó **không được liệt kê ở đâu trong spec**. Nên phát biểu đúng là ***claim KHÔNG được câu được trích chống đỡ***, **không** phải *"§6.6 chắc chắn cắt được"* — câu thứ hai cũng vượt quá bằng chứng, đúng lỗi vừa mắc. 🔑 **Phạm vi thiệt hại, khai ra để không ai phải tự tra:** claim sai nằm ở vế cân nhắc **CHỐNG** phương án (b), mà chủ dự án chọn (a) ⇒ **nó không lật quyết định nào**. Nhưng nó nằm trong file đặc tả và đã **lừa được một người đọc thật** (phiên `-3f` khai bị bảng MT lệch lừa hai lần trong một phiên) ⇒ vẫn phải sửa. **Giữ nguyên chữ sai làm lịch sử**: xoá đi thì mất luôn bằng chứng rằng nó từng lừa được ai, và người sau lại tin một bảng trông sạch; **(c)** SL **hai lớp** — một stop rộng trên sàn làm lưới cuối chống-chết-tiến-trình, cộng `custom_stoploss` nội bộ neo zone làm SL thật; giá phải trả là hai nguồn sự thật cho một mức SL, đúng thứ `LD-09` cấm, và phải chứng minh lớp rộng **không bao giờ** khớp trước lớp hẹp. 🔴 **KHÔNG tự chọn (b) chỉ vì nó rẻ nhất** — nó là đường duy nhất không cần viết dòng mã nào, nên nó là đường dễ bị chọn vì tiện chứ không vì đúng | 13/09/2026 | **Ghi nhận (phiên `[3f7d14]`), chờ chủ dự án** ✅ **ĐÃ CHỐT 13/09/2026 — chủ dự án chọn phương án (a): bật `stoploss_on_exchange: true`.** Xem `docs/decisions/DR-D11-02-sl-tren-san.md` (`62a396b`, đính chính `bcc4bf1`). 🔑 Vòng lặp lo ngại ở (a) **tự gỡ, không xảy ra**: D2b (`closePosition`) đã loại từ trước bằng đọc mã nguồn (`TD-0028`); đọc lại `interface.py`/`freqtradebot.py` (Freqtrade 2026.8) xác nhận `custom_stoploss()` vẫn đẩy được giá trị lên sàn bình thường dù bật cờ — chú thích cũ ở `config.json:49` (*"không đẩy SL lên sàn"*) là **sai kỹ thuật**, nên đây gần với sửa một dòng cấu hình viết trên tiền đề sai hơn là một đánh đổi thiết kế mới. Một giả thuyết phụ (thiếu tham số `after_fill` có thể làm SL hạ về lưới cuối giữa các tranche) đã tự kiểm và tự bác bằng mã nguồn (`trade_model.py:854`) — không phải lỗi. `gap_ms` (`TD-0244`) đo bằng mẫu **CƯỠNG BỨC sự kiện** (không chờ tích luỹ tự nhiên) trên môi trường **live tối thiểu** đã chốt ở `DR-D11-01`/`TD-0243` (KHÔNG phải testnet — bản đầu của `DR-D11-02` viết nhầm testnet, đã đính chính ở `bcc4bf1`), khai là đo cơ chế hệ thống, **0 trial** cùng logic đo-thước `MT-08`. 🔴 **Hạn chế tồn dư PHẢI đọc kèm mọi `p99(gap_ms)`:** mẫu cưỡng bức chỉ đo ĐỘ DÀI khoảng trống cancel→recreate, không đo XÁC SUẤT khoảng trống trùng lúc giá chạy ngược — chi tiết `DR-D11-02` §3.2. Phát hiện phụ ghi kèm trong DR đó: lưới cuối hiệu dụng là `-0,99` (không phải `-0,30` như chú thích cũ trong `ZoneAbsorption.py:233`); LD-21 (`sl_on_exchange_confirmed`) tự thoả khi chọn (a). **Chưa code** (chờ "bắt đầu code"), **chưa xong** — vẫn phụ thuộc `TD-0243` hoàn tất 🔄 **ĐÍNH CHÍNH Ô TRẠNG THÁI 14/09/2026 — chữ *“Chưa code, chưa xong”* ở trên đã LỖI THỜI; giữ nguyên làm lịch sử.** Đo trên đĩa: `config/freqtrade/config.json:49` **đã là `stoploss_on_exchange: true`** (kèm `:48` `stoploss: "market"`, `:50` `interval: 60`), chú thích tại chỗ trỏ đúng `DR-D11-02`/`MT-39`/`TD-0244`; và `TD-0243`/`TD-0244` đều ✅. ⇒ Phương án (a) **đã thi hành**, không còn *“chưa code”*. 🔑 Đây là một trong **SÁU** chỗ tầng trạng thái trôi khỏi đĩa mà `TD-0246` bắt được cùng lúc (xem `MT-46`) — và điều đáng giữ là **cả sáu lệch CÙNG MỘT CHIỀU: văn bản đứng sau hiện thực** |
| MT-40 | 🔴 **THANG DRAWDOWN TỰ VỀ 0 SAU MỖI LẦN KHỞI ĐỘNG LẠI — một trong BA tầng chặn vòng lặp thua lỗ lặng lẽ mất hiệu lực.** `ZoneAbsorption.py:282` khởi tạo `self._dinh_equity: float | None = None` trong `__init__`, và `:1317-1325` (`_dd_pct()`) chỉ nâng đỉnh khi số dư hiện tại cao hơn đỉnh **đang giữ trong bộ nhớ tiến trình**. ⇒ Mỗi lần bot chạy lại, đỉnh reset về số dư **lúc đó**, nên `_dd_pct()` trả **0%**, nên `mult_dd` không bao giờ tới ngưỡng `soft` (5%) hay `halt` (8%). §12b.2 (`spec:4543-4558`) khai `mult_dd` là **một trong BA tầng** dựng ra để *"vòng lặp thua lỗ KHÔNG tạo được"*, và khai luôn rằng ba tầng đó thuộc **Cấp C — *không bao giờ được đổi***. Một tầng Cấp C tự vô hiệu sau mỗi lần restart là một tầng **không tồn tại**, và nó không để lại dấu vết nào để ai nghi ngờ | `spec:4543-4558` (§12b.2 — ba tầng chặn, `mult_dd` HALT ở 8%, Cấp C không bao giờ đổi) + `DR-D0PRE-04` (thang 5/8/20 là Hạng 0, không sửa được nữa) ↔ `ZoneAbsorption.py:282` và `:1317-1325` (đỉnh equity chỉ nằm trong bộ nhớ tiến trình) | 🟡 hôm nay → 🔴 **trước D11.** Vô hại ở backtest (một tiến trình, không restart) ⇒ **không làm sai một con số nào đã đo**. Thành 🔴 đúng lúc có một tiến trình chạy dài nhiều tuần, tức D11 | **CHƯA GIẢI — ghi nhận, không tự sửa (quy tắc 5 và 11).** Đề xuất: đỉnh equity đọc lại từ **nguồn bền vững có xuất xứ** thay vì bộ nhớ tiến trình. Hai nguồn khả dĩ, mỗi nguồn một câu phải trả lời trước: **(a)** tính lại từ DB Freqtrade (`user_data/tradesv3_dryrun.sqlite`) — cần chốt *"đỉnh tính trên equity đóng-lệnh hay equity gồm cả lãi/lỗ chưa thực hiện?"*, vì hai cách cho hai con số drawdown khác nhau; **(b)** một sổ riêng append-only — thêm một nguồn sự thật thứ hai cho một đại lượng DB đã có. 🔑 **Và một câu phải chốt trước cả hai:** đỉnh equity nên được reset **bao giờ**? Không bao giờ reset nghĩa là một lần sụt 8% khoá HALT vĩnh viễn; reset theo kỳ thì *"theo kỳ nào"* là một **tham số mới** chưa ai đếm vào kiểm kê DOF — đúng hình `DR-D4-02` (ngưỡng 30% của DG5 là bậc tự do không ai đếm). Nên bản vá này **không phải một dòng mã**, nó là một quyết định | 13/09/2026 | **Ghi nhận (phiên `[3f7d14]`), chờ chủ dự án** 🔄 **ĐÍNH CHÍNH Ô TRẠNG THÁI 14/09/2026 — chữ *“chờ chủ dự án”* ở trên đã LỖI THỜI; giữ nguyên làm lịch sử.** `TD-0238` **✅ đã xong và đã nối**: `src/tool_d/equity_peak.py` (ghi nguyên tử `.dang-ghi` + rename, state ở `runs/zone_absorption/equity_peak_state.json`) được gọi ở `ZoneAbsorption.py:324-332` (đọc lúc bot start) và `:1483-1503` (`_dd_pct()`, docstring ghi rõ *“TD-0238 (MT-40)”*) ⇒ **2 người gọi sản xuất**, không còn 0. ⚠️ Câu *“đỉnh equity nên reset bao giờ”* mà mục này nêu là một **tham số mới chưa ai đếm vào kiểm kê DOF** — phải kiểm xem `TD-0238` đã giải nó hay chỉ giải phần bền-vững-qua-restart, vì hai thứ đó khác nhau. 🔑 Một trong sáu chỗ lệch của `MT-46` |
| MT-41 | 🔴 **RESTART LÚC CÓ LỆNH CHỜ KHỚP LÀM VỊ THẾ CHẠY VỚI `stoploss = −0,99`, VÀ KHÔNG PHÉP KIỂM NÀO THẤY.** Chuỗi đầy đủ, mỗi khâu đều đọc được trên đĩa: `custom_stake_amount()` (`ZoneAbsorption.py:643`) tính kế hoạch rồi lưu vào `self._cho[pair]` — **bộ nhớ tiến trình**; `order_filled()` (`:758-763`) mới là chỗ ghi nó vào `trade.custom_data`, và nếu `self._cho` không có pair thì nó `raise SizingError("thứ tự callback đã hỏng")`. Giữa hai mốc đó là một lệnh **post-only** có tuổi thọ tới `unfilledtimeout.entry = 180 phút` (`config.json:37`) ⇒ *"restart trong lúc đang có lệnh chờ"* là ca **THƯỜNG** trên một lượt chạy nhiều tuần, không phải ca hiếm. Và `strategy_safe_wrapper` của Freqtrade **NUỐT** exception của callback, hạ xuống WARNING rồi đi tiếp (`MT-16` triệu chứng vii, dự án đã đo: 12 lần `SizingError` bị nuốt, lệnh mẫu biến mất trong im lặng) ⇒ vị thế mở **không có `co_lenh`/`ke_hoach`** trong `custom_data`; rồi `_doc_ke_hoach()` (`:860-865`) raise ở **mọi** callback sau đó, gồm `custom_stoploss` ⇒ Freqtrade không nhận được SL nào và dùng **lưới cuối `stoploss: -0.99`** của config. 🔴 Tức: không có SL, không có TP, không có DCA, **và log chỉ có một dòng WARNING** | `config/freqtrade/config.json:37` (`unfilledtimeout.entry = 180`) + `ZoneAbsorption.py:643` · `:758-763` · `:860-865` · `:972` ↔ `MT-16` triệu chứng (vii) (*"một chốt fail-closed bị nuốt là một chốt KHÔNG TỒN TẠI"*) ↔ `config.json:78` (`stoploss: -0.99` là *"lưới cuối rất rộng, không phải SL thật"*). ⚠️ Lớp canh hiện có **không phủ**: `test_td0187:565-572` cấm dòng *"Strategy caused the following exception"* trong log của **một lượt backtest bằng chứng** — đó là phép kiểm trên **artifact**, không phải chốt chạy lúc vận hành, và backtest không bao giờ restart | 🔴 **trước D11.** Vô hại ở backtest (một tiến trình) ⇒ **không làm sai một con số nào đã đo**. 🔴 Nhưng ở D11/D12 nó là **mất SL trên một vị thế đang mở**, tức hạng nặng nhất trong mọi mục của mục 7 này | **CHƯA GIẢI — ghi nhận, không tự sửa.** Đề xuất `TD-0237`: ghi kế hoạch vào `trade.custom_data` **ngay tại `confirm_trade_entry`** (nơi đã có `trade` và đã trả `False` được nếu từ chối) thay vì chờ `order_filled`, để trạng thái không bao giờ chỉ nằm trong bộ nhớ tiến trình. 🔴 **Và một vế thứ hai, độc lập, quan trọng không kém: KHÔNG được nới `SizingError` thành cảnh báo.** Docstring `_doc_ke_hoach` đã viết đúng lý do — *"thiếu là lỗi lắp ráp, không phải chưa có"* — và `TD-0114` từng bị cấm tái tạo kế hoạch từ dataframe. Đường đúng là **đừng để trạng thái mất**, không phải **đoán lại khi đã mất**. ⚠️ Sửa xong thì mọi số backtest trước đó **không so sánh trực tiếp được** (`DR-D4-08` §8) — nên phải cân với việc D4 chưa chạy, và đó là một phần của quyết định | 13/09/2026 | **Ghi nhận (phiên `[3f7d14]`), chờ chủ dự án** ✅ **ĐÃ CHỐT VÀ THI HÀNH 14/09/2026 — `TD-0237` (`07326c2`), và phương án được chọn KHÁC đề xuất ghi ở chính mục này.** Mục này đề xuất *"ghi kế hoạch vào `trade.custom_data` ngay tại `confirm_trade_entry`"* — tức **đừng để trạng thái mất**. Phương án chốt đi đường khác: **phục hồi sau restart từ HAI nguồn đã bền vững của Freqtrade** — `entry_tag` (giải mã lại, đúng bước `custom_stake_amount()` đã làm) + `trade.stake_amount` THẬT ngay sau khi tranche 1 khớp; `w_tranche`/`L_exchange` là hằng cấu hình (N4) nên `n_full = stake1_thật × L_exchange / w[0]` suy ngược CHÍNH XÁC. Hàm thuần `phuc_hoi_ke_hoach_sau_restart()` ở `sizing.py`. 🔑 **Vì sao đây KHÔNG phải ca `TD-0114` đã bị cấm — phân biệt này là phần đáng giữ nhất:** `TD-0114` cấm tái tạo kế hoạch **từ dataframe**, tức từ **dữ liệu thị trường** có thể đã đổi ⇒ ra một kết quả *khác* bản gốc mà không ai biết. Ở đây nguồn là **trạng thái giao dịch của chính Freqtrade** (`entry_tag` do ta tự ghi lúc vào lệnh, `stake_amount` là số tiền THẬT đã khớp) ⇒ **tất định**, suy ngược ra đúng một giá trị. Ranh giới không phải *"có tái tạo hay không"* mà là ***"tái tạo từ nguồn TẤT ĐỊNH hay từ nguồn có thể đã trôi"***. ✅ Vế fail-closed giữ nguyên: vẫn `raise SizingError` khi `enter_tag` không giải mã được hoặc `stake ≤ 0`, **không nới thành cảnh báo**. ✅ `mult` (6 hệ số) và `rho_eff_pct` **không** suy ngược được (nhiều tổ hợp cho cùng notional) ⇒ đánh dấu `NaN` theo N6 (**không bịa số**), và khai rõ chúng không chảy vào risk management hạ lưu. Kiểm-có-răng: mutation ⇒ đúng **1 ca đỏ** (đúng ca ghim kịch bản restart), 3 ca kia vẫn xanh. Docker full suite **1917 passed, 0 failed** |
| MT-42 | 🔴 **CHẠY DRY-RUN HAI TUẦN VẪN SINH RA 0 CHỈ SỐ ĐỌC ĐƯỢC — báo cáo E5 để 22/22 ở `pending` bằng một HẰNG SỐ, và Decision Log có 0 NGƯỜI GỌI SẢN XUẤT.** Hai khâu, cả hai đọc được trên đĩa: **(1)** `src/tool_d/reporting/report_model.py:26` định nghĩa `_VAN_HANH` là một **hằng số chuỗi** (*"chưa có lệnh đóng thật — D0-PRE chưa có strategy code (Khối 1-8)"*), và `build_metrics()` (`:78-85`) trả `Measured.pending(why)` cho **mọi** chỉ số trong `_METRIC_DEFS` — **không có đường nào** để một chỉ số chuyển sang `ok`. Câu lý do ấy nay còn **lỗi thời**: chiến lược đã có từ TD-0187. **(2)** `src/tool_d/ledger/decision_log.py` có cửa ghi append-only đầy đủ (`dedup_key` theo `order_id` **từng tranche**, `flock`, `ghi_neu_chua_co`, test `L-Z45` + `TD-0201`) nhưng `grep -rn "decision_log"` toàn repo cho **0 người gọi sản xuất** — chỉ test. ⇒ Bốn chỉ số **H-1…H-4** mà §11b.2 đòi *"ghi hàng tuần khi live"*, tỉ lệ khớp post-only, `gap_ms`, `hold_duration`, số mẫu hiệu dụng — **không đại lượng nào có nguồn dữ liệu** | `spec:4991` (§11b.2 — *"H-1/H-2/H-3/H-4 ghi hàng tuần khi live"*) + `spec:4283-4285` (Nhánh 1 đòi báo cáo tỉ lệ TP_fallback, ngưỡng 40%) + `spec:2968-2970` (D10 đòi tỉ lệ khớp post-only) ↔ `report_model.py:26` và `:40-85` (22 chỉ số `pending` bằng hằng số, không bộ tính) ↔ `decision_log.py` (cửa ghi tồn tại, **0 người gọi sản xuất**) | 🔴 **trước D11** — đây là mục làm D11 **vô nghĩa** chứ không phải sai. Và nó là *"thứ được canh không nằm trên đường chạy"* (`TD-0168`) ở dạng nặng nhất: sổ có, luật có, test khoá có, **không ai ghi vào** | **CHƯA GIẢI — ghi nhận, không tự sửa.** Thứ tự bắt buộc: `TD-0239` (nối Decision Log vào đường sản xuất, §8.3) **trước**, rồi `TD-0240` (bộ tính 22 chỉ số) — đảo thứ tự là viết một bộ tính không có đầu vào. 🔴 **Một chốt phải nêu TRƯỚC khi ai chạm `report_model.py`:** `content_fingerprint()` (`:88-101`) băm đúng `code` cộng `label` của từng chỉ số, và `spec:4808` khai *"đổi nội dung báo cáo = **tiêu 1 trial**"* (TD-0062 đóng băng bằng test hồi quy hash) ⇒ **chỉ được đổi bộ TÍNH, tuyệt đối không đổi `code`/`label`**, kể cả câu lý do lỗi thời ở `_VAN_HANH` (may là hàm băm cố ý **không** băm lý do pending — docstring `:95-98` đã khai chủ đích ấy, nên sửa câu đó **không** tiêu trial). ⚠️ Và một câu phải trả lời trước khi tính H-2: nó là *"tương quan giữa ZSS lúc entry và kết quả lệnh"*, tức cần `zss` **đóng băng tại lúc vào lệnh** — `custom_data["tag"]` có mang nó, nhưng chưa ai kiểm nó còn nguyên sau `MT-41` (restart làm mất `custom_data`) ⇒ hai mục này **phải đọc cùng nhau** | 13/09/2026 | **Ghi nhận (phiên `[3f7d14]`), chờ chủ dự án** 🔄 **ĐÍNH CHÍNH Ô TRẠNG THÁI 14/09/2026 — vế (2) đã LỖI THỜI; giữ nguyên chữ trên làm lịch sử.** Câu *“Decision Log có **0 NGƯỜI GỌI SẢN XUẤT**”* nay **SAI**: `TD-0239` ✅ (`05bff6a`) nối `ghi_neu_chua_co` vào `ZoneAbsorption.py:872` (`VAO_RA_LENH`) và `:1149` (`DOI_SL`, `TD-0244`) ⇒ **2 người gọi sản xuất**. Thứ tự bắt buộc *“TD-0239 trước, rồi TD-0240”* vì thế **đã thoả**, và `TD-0240` nay 🔒 có người nhận. ⚠️ **Vế (1) VẪN NGUYÊN:** `report_model.py:78-85` `build_metrics()` là **một list-comprehension duy nhất** trả `Measured.pending(why)` cho cả 22 mục — **không có nhánh nào** dựng `Measured.ok`, nên dry-run vẫn sinh **0 chỉ số đọc được**. 🔴 **Ràng buộc mới cho `TD-0240`, từ `TD-0246`:** nếu chỉ số nào đo một trong **DG1–DG5** thì trên arm sản xuất nó **LUÔN** ra 0, và 0 đó phải đọc là **`BAT_KHA` (cấu trúc không thể xảy ra)** — KHÔNG phải *“đo được và bằng 0”*, cũng KHÔNG phải `pending`; xem `MT-44`. 🔑 Một trong sáu chỗ lệch của `MT-46` |
| MT-43 | 🔴 **`DR-D4-10` §2.4 (arm sản xuất chốt sang single-entry, 12/09) và `DR-D11-01`/`DR-D11-02` (ngưỡng/cơ chế đo `gap_ms` cho D2c, chốt 13/09) va nhau: mọi arm thuộc `arm_switches.ARM_DON_TRANCHE` — không bao giờ thêm tranche — khiến khối lượng SL trên sàn KHÔNG BAO GIỜ đổi, và D2c (§8.3, đo `gap_ms` qua ≥30 lần đổi khối lượng SL) không có gì để đo trên cấu hình sẽ chạy thật.** Phát hiện khi nối `TD-0244` (bộ sinh `gap_ms`) vào `ZoneAbsorption.custom_stoploss()`: hàm luôn trả `[]` một khi arm sản xuất rơi vào tập đó. Đã xét và loại một đường tưởng cứu được: TP1 (chốt 50%) cũng đổi khối lượng SL về lý thuyết, nhưng đọc mã nguồn Freqtrade xác nhận **không** cứu — `freqtradebot.py` gọi `cancel_stoploss_on_exchange(trade, allow_nonblocking=True)` cho MỌI lệnh thoát (kể cả thoát một phần), và hàm đó tự thoát sớm không huỷ gì khi `exchange.get_option("stoploss_blocks_assets") == False` — đúng giá trị Binance Futures khai ở `_ft_has_futures` (`binance.py:58`, khác `_ft_has` spot ở `:40` = `True`). Đường huỷ SL sinh `gap_ms` thật chỉ còn ở LỆNH VÀO khớp (`freqtradebot.py:2406`, không truyền `allow_nonblocking` nên không được miễn) — tức DUY NHẤT do tranche mới, không do TP1. ⚠️ **`MT-35` (chưa giải) làm arm sản xuất CUỐI CÙNG chưa chắc chắn** — `TD-0227` (thi hành `DR-D4-10` §2.4) đang TREO vì khoá `arm` gộp hai trục (DCA + trend); ứng viên có thể là `Z0` (44,8 lệnh/năm, dưới sàn 150) hoặc `Z0-T1` (325,6/năm, ứng viên duy nhất D4 phán quyết được). **Cả hai đều nằm trong `ARM_DON_TRANCHE`** — kết luận của mục này KHÔNG phụ thuộc `MT-35` ngã về phía nào, nên viết theo ĐIỀU KIỆN (`arm ∈ ARM_DON_TRANCHE`), không viết theo tên arm cụ thể | `docs/decisions/DR-D4-10-tai-dinh-pham-vi-d4-theo-co-mau.md` §2.4 (12/09) ↔ `docs/decisions/DR-D11-01-testnet-va-nguong-D10.md` §5.2 + `docs/decisions/DR-D11-02-sl-tren-san.md` §3.2 (13/09, ngưỡng/cơ chế `gap_ms` giả định có sự kiện để đo) ↔ `src/tool_d/arm_switches.py:73-75` (`ARM_DON_TRANCHE = {Z0, Z1, Z0-T0, Z0-T1, Z0-V1, Z0-S1}`) + mã nguồn Freqtrade (`freqtradebot.py` các hàm `cancel_stoploss_on_exchange`/`execute_trade_exit`, `exchange/binance.py:40,58`). Liên quan `MT-35` (arm cuối chưa chốt) | 🔴 **Chặn cách ĐỌC ngưỡng D2c của `DR-D11-01`, KHÔNG chặn D4/D10 chạy** — `TD-0227` còn TREO (`MT-35`) và phụ thuộc cổng D4 chưa đóng, nên hôm nay (`tool_d_config.yaml:139` = `"Z3"`, **không** thuộc `ARM_DON_TRANCHE`, có DCA) D2c vẫn là điều kiện SỐNG; mâu thuẫn chỉ nổ SAU KHI arm sản xuất chuyển vào tập đó | ✅ **ĐÃ CHỐT 13/09/2026 — chủ dự án chọn: `d2c_na` (D2c là N/A) hợp lệ KHI VÀ CHỈ KHI `tier_c.arm_ablation.arm ∈ ARM_DON_TRANCHE`.** Đây là một BẤT BIẾN hai chiều, không phải một sự kiện một lần: đúng lúc arm ĐI VÀO tập đó (N/A bắt đầu áp dụng) VÀ đúng lúc arm ĐI RA (ví dụ nếu `TD-0226`/Idea Queue sau này đưa DCA trở lại production — DCA **không** bị bác bỏ, chỉ chuyển hàng chờ) N/A phải MẤT hiệu lực. Không cưỡng bức sự kiện trên một cấu hình khác chỉ để đo (loại phương án (b)), không mở lại `DR-D4-10` §2.4 để giữ `Z3` (loại phương án (c)). 🔴 **"N/A" được buộc vào ĐÚNG MỘT biến có tên, không phải một câu văn xuôi nhờ người sau nhớ** — đúng bài học `MT-08`: `tests/lock/test_td0244_stoploss_on_exchange.py::test_arm_san_xuat_doi_thi_phai_doc_lai_mt43` chốt CỨNG giá trị arm quan sát được hôm nay (`"Z3"`) làm hằng số so sánh, nên nó đỏ khi arm đổi sang **BẤT KỲ** giá trị khác — dù đi vào hay đi ra khỏi `ARM_DON_TRANCHE` — buộc người sửa phải đọc lại mục này và tự tay xác nhận `d2c_na` mới đúng hay sai, thay vì suy diễn một chiều rồi lặng lẽ đúng-cho-tới-khi-sai (rủi ro `-3f` chỉ ra: một tripwire một chiều sẽ xanh trở lại đúng lúc DCA quay lại — chiều nguy hiểm nhất). Cơ chế đo (`src/tool_d/gap_ms.py` + hook `_ghi_gap_ms()` trong `ZoneAbsorption.custom_stoploss()`, `TD-0244`) **giữ nguyên — không phải code chết**: đang sinh dữ liệu thật ngay hôm nay dưới arm `Z3`, và tự động sống lại nếu DCA quay về production. Còn treo, KHÔNG thuộc `TD-0244`: `DR-D11-01` cần ghi rõ ngưỡng D2c đọc **tại thời điểm D10 chạy thật** theo điều kiện `arm ∈ ARM_DON_TRANCHE`, không giả định N/A cố định trước; `TD-0240` (bộ tính 22 chỉ số E5) khi tới lượt phải TÍNH `d2c_na` ĐỘNG từ chính điều kiện này mỗi lần chạy báo cáo, không phải hằng số tĩnh ghi một lần — đây là cách duy nhất đóng hẳn rủi ro chiều-quay-lại mà một test tripwire tĩnh không đóng được | 13/09/2026 | **Ghi nhận + tự chốt cùng ngày (phiên `[82acb4]`, rà soát chéo `[564e4e]` + `[3f7d14]` — bao gồm một claim của `[3f7d14]` tự rút lại sau khi đọc kỹ hơn)** |
| MT-44 | 🔴 **DG1–DG5 KHÔNG BAO GIỜ ĐƯỢC XÉT trên 6/9 arm — trong đó có CẢ arm ứng viên sản xuất `Z0-T1` và CẢ mốc so `Z0`. Năm cổng, tức phần *“Smart”* của Smart DCA, BẤT ĐỘNG trên cấu hình sẽ lên tiền thật.** `arm_switches.py:73-75` khai `ARM_DON_TRANCHE = {Z0, Z1, Z0-T0, Z0-T1, Z0-V1, Z0-S1}`; `cong_ap_dung()` trả tuple rỗng cho cả sáu (`:255-256`); và `ZoneAbsorption.py:1006-1007` `if cong_ap_dung(self._arm) == (): return None` đứng **TRƯỚC** lời gọi `danh_gia_tat_ca(...)` ở `:1031`. Trong 4 arm `DR-D4-12` §4 chốt chạy (`Z0-T1` · `Z0` · `Z0-T0` · `Z3`), **chỉ `Z3`** còn xét DG1–DG5. 🔑 **KHÔNG phải một lỗi mã** — mã tự khai đúng ý định (`arm_switches.py:71-72`: *“DG1–DG5 chỉ có nghĩa với arm CÓ tranche 2/3”*). Nó là **HỆ QUẢ CHƯA AI VIẾT RA** của `DR-D4-10` §2.4 (mặc định single-entry): chọn single-entry **đồng thời xoá năm cổng và các tham số của chúng khỏi phạm vi đo**. `TD-0228` đã đo vế *“DCA không tạo thêm một cơ hội vào lệnh nào”*; đây là **vế còn lại của cùng một chốt**. 📊 Hệ quả cụ thể, đo ở `TD-0246`: `dg4_bars_1h` có đường đọc (`ZoneAbsorption.py:1040`) nhưng **không bao giờ tới** trên arm ứng viên ⇒ hạng `BAT_KHA_TREN_ARM_NAY`, không phải `pending` và không phải *“đo được và bằng 0”* | `DR-D4-10` §2.4 (single-entry) + `arm_switches.py:73-75,255-256` + `ZoneAbsorption.py:1006-1007` vs spec §10.1 (`Z3` = *“phần Smart của DCA có giá trị?”*) + §4 (DG1–DG5) | 🔴 | **Ghi nhận theo quy tắc 11, không tự chọn bên.** Không đề xuất sửa mã — hành vi đúng ý định. Ba việc phải làm để hệ quả không biến mất: **(1)** `d4_han_che` khai thẳng *“DG1–DG5 bất động trên arm được phán quyết; D4 không đo năm cổng”*; **(2)** `TD-0240` phân loại mọi chỉ số E5 đo một trong năm cổng là **`BAT_KHA`** trên arm sản xuất; **(3)** câu *“năm cổng có đáng không”* chuyển cùng DCA vào Idea Queue (`IQ-0001`), vì đo được nó đòi một arm có tranche 2/3 chạy đủ lâu — tức cùng ràng buộc tần suất `n ≥ 319` | 16/09/2026 | Ghi nhận (`TD-0246`, `b3827ff`), chờ chủ dự án |
| MT-45 | 🔴 **`DR-D4-12` §3.3 (commit `63769f8`, 14/09) xếp *“(3a) Dựng pool point-in-time đầy đủ trước D4”* vào bảng *“Phương án đã LOẠI”* — trong khi `MT-34` ghi chủ dự án ĐÃ CHỐT đúng đường đó từ 13/09.** Nguyên văn `MT-34`: *“CHỦ DỰ ÁN CHỐT 13/09/2026: đi ĐƯỜNG A-ĐẦY-ĐỦ (dựng lại rổ point-in-time có xuất xứ TRƯỚC D4), sau khi thấy đề xuất A-tối-thiểu … và chọn khác — ghi nhận để không ai đọc ngược”*. 🔑 **Cơ chế, khai thẳng vì nó là lỗi của phiên ghi mục này:** phiên đó đọc `MT-34` bằng một lệnh **cắt ~1.500 ký tự đầu**, thấy *“Ba đường, chưa chọn”* rồi dừng; quyết định nằm ở khoảng **ký tự 9.500+ của cùng một dòng**. Nên câu rổ pool được trình lại cho chủ dự án như **chưa chốt**, chủ dự án chọn **(3b″)** — yếu hơn A-đầy-đủ và gần với đúng thứ đã bị bác hôm 13/09 — rồi `DR-D4-12` ghi lựa chọn đó. Chính dòng `MT-34` viết sẵn *“ghi nhận để không ai đọc ngược”*, và nó **vẫn bị đọc ngược**. 📌 Bắt được khi thi hành lệnh *“chuẩn hoá và lưu”*: N9 + quy tắc 3 buộc đọc lại file cũ trước khi ghi, và lần đọc đó đi tới **cuối** dòng thay vì cắt đầu. Bài học phải giữ: **một dòng `MT` dài hàng nghìn ký tự có TRẠNG THÁI ở CUỐI, nên mọi phép đọc cắt đầu đều đọc được VẤN ĐỀ mà KHÔNG đọc được QUYẾT ĐỊNH** — đúng hình dạng *“người bị canh tự chọn phạm vi bị canh”*, chỉ là lần này phạm vi bị một lệnh `cut` chọn | `DR-D4-12` §3.2–§3.3 (`63769f8`) vs `MT-34` (chốt 13/09/2026) | 🔴 | ✅ **ĐÃ GIẢI 16/09/2026 — chủ dự án chốt: A-ĐẦY-ĐỦ VẪN ĐỨNG.** **(3b″) VÔ HIỆU** — nó sinh ra từ việc đọc thiếu, không từ thông tin mới. Thi hành: gắn **đính chính tại chỗ** vào `DR-D4-12` §3 (giữ nguyên chữ cũ, khuôn dự án), rút việc xếp A-đầy-đủ vào *“đã LOẠI”* và ghi rõ nó là quyết định đang hiệu lực. 🔴 **HỆ QUẢ NÓI THẲNG: D4 BỊ CHẶN cho tới khi rổ point-in-time đúng xong** — `TD-0184` không được tiêu một suất `B2` nào trước đó. Và `MT-34` tự khai đây là *“hạng mục nhiều tuần”* (spec `:4338`) và *“VẪN CHƯA CÓ MÃ VIỆC NÀO”*. Con số đo được lúc này làm rõ giá: **52/61** mã của `K` **không có dữ liệu ở bất kỳ đâu** (`binance/` 0/61, `explore/` 9/61) ⇒ A-đầy-đủ kéo theo tải dữ liệu cho ~52 mã + một thư mục dữ liệu thứ ba + backfill lại, và spec `:4338` làm **mọi số quy đổi sang pool** (`325,6` · `n = 206` · `44,8`) mất hiệu lực | 16/09/2026 | Chủ dự án |
| MT-46 | 🟡 **TẦNG TRẠNG THÁI ĐÃ TRÔI KHỎI ĐĨA Ở SÁU CHỖ, TẤT CẢ CÙNG MỘT CHIỀU — văn bản đứng sau hiện thực.** (1) `param_status.yaml:198-201` đóng băng `dg7_funding_frac` với lý do *“DG7 chỉ áp cho SHORT nên hiện chưa có đường chạy nào chạm tới; cùng lý do với `dg6d_retrace_frac`”* — nhưng `funding_stop.py:7-8` tự khai *“ÁP DỤNG KHÔNG ĐIỀU KIỆN”*, `:11-14` *“đúng cho CẢ Long lẫn Short”*, `ZoneAbsorption.py:1156-1162` gọi vô điều kiện, và đo được nó **kết thúc 19,4% lệnh của `Z0-T1`** (31/160), 27,3% của `Z0`. (2) `TIME_STOP_RATIO_BAND` (`thresholds.py:37`) xuất hiện **đúng một lần toàn repo — tại dòng định nghĩa**, KHÔNG có trong `evaluate_branch1()` cùng file dù bảy hằng số còn lại đều có; và đo được TIME_STOP = **0%** ở cả `Z0` lẫn `Z0-T1`, dưới sàn 5% của spec §10.2. (3)(4)(5) ô trạng thái `MT-39`·`MT-40`·`MT-42` lỗi thời so với đĩa (đính chính tại chỗ cùng đợt này). (6) ô `TD-0184` ở `TASKS.md` còn ghi chặn *“63,6 lệnh/năm < sàn 150”* dù `DR-D4-10` §2.1 + `MT-26`(C) + `MT-29` đã xử. 🔑 **TỈ LỆ, để có mẫu số:** kiểm được **4** lời khai `frozen_rationale`, **1 sai** — `dg7`. Ba cái kia trung thực: `max_hold_bars_4h` (`:161-164`, tự khai *“ỨNG VIÊN CHỐT, số tạm”*), `dg6d_retrace_frac` (short-only thật, `dg6_early_invalidation.py:98-99`, và 0 đường đọc sản xuất), `funding_rate_pct` (khai một mâu thuẫn nội tại của spec). 🔴 **Đính chính kèm:** phiên phát hiện ban đầu báo *“2/2 lời khai đều SAI”*, gộp `dg7` với `max_hold_bars_4h` — **SAI**; thứ lệch ở `max_hold` là HÀM Ý của spec §10.2, không phải lời khai, và câu sai đó **đã lọt vào commit `16db105`** trước khi bị bắt. 🔑 **CƠ CHẾ TRÔI của `dg7`:** lý lẽ được **kế thừa BẰNG THAM CHIẾU** — *“cùng lý do với `dg6d_retrace_frac`”* — mà `dg6d` khai **ĐÚNG**. Người viết không bịa; họ trỏ tới một lý lẽ THẬT, chỉ trỏ nhầm chỗ | `param_status.yaml` (`frozen_rationale`) + `thresholds.py:37` + ô trạng thái mục 7 + ô `TASKS.md` vs mã và đĩa | 🟡 | **Ghi nhận theo quy tắc 5 + 11 — không tự sửa `param_status.yaml`.** ⚠️ **`L-Z15` báo ✅ không mâu thuẫn với mục này** — `ledger/audit_checks.py:256-262` **tự khai** trước rằng *“‘Đạt’ chỉ trả lời ‘cả 12 đã KHAI trạng thái chưa?’ — nhưng người đọc sẽ nghe thành ‘cả 12 đã được QUYẾT đúng chưa?’”*; phần mới ở đây là một **thực thể** của khoảng cách đó và **cơ chế** sinh ra nó. Hai việc đề xuất, **chưa mở mã**: **(a)** quét mọi `frozen_rationale` biện minh **bằng tham chiếu** (*“cùng lý do với”*, *“như”*, *“tương tự”*) — rẻ và trúng đích hơn đo từng tham số; **(b)** bắt mọi lý lẽ dạng *“chưa có đường chạy nào chạm tới”* phải trỏ tới một **bằng chứng kiểm được** (`file:line`), cùng thuốc `tu-dien-du-lieu.md` (TD-0245) dùng cho cột database. ⚠️ `TIME_STOP_RATIO_BAND` chưa có người đọc là thứ `TD-0185` phải quyết khi dựng gate: nối vào ⇒ FAIL thêm một tiêu chí (0% < 5%); không nối ⇒ gate âm thầm bỏ một tiêu chí spec | 16/09/2026 | Ghi nhận (`TD-0246`), chờ chủ dự án |

> 🔴 Chặn tiến độ: dừng code ngay, xử lý trước khi tiếp tục.
> 🟡 Có thể chốt sau: ghi nhận, tiếp tục phần khác, **bắt buộc** giải quyết trước Giai đoạn 5 — không mang mâu thuẫn 🟡 vượt qua go-live.

---

## 8. Lịch sử thay đổi

| Mục thay đổi | Loại | Nội dung cũ | Nội dung mới | Lý do | Ngày |
|---|---|---|---|---|---|
| Toàn bộ file | ➕ Thêm mới | — | Khởi tạo lớp mỏng: phạm vi GĐ1, quyết định nền tảng, 8 Open Questions, 7 Mâu thuẫn | Giai đoạn 1–2 của quy trình vibe-code | 06/09/2026 |
| Mục 7 | ➕ Thêm mới | 7 mâu thuẫn (MT-01…MT-07) | Thêm **MT-08** — `registry.py` chưa thi hành phần kế toán `CTRL` đã chốt ở MT-02; chốt phương án A + cơ chế xác thực CTRL | Phía front-end phát hiện khi dựng tầng gấp trạng thái cho dashboard (phải chép đúng luật của `registry.py`). MT-02 và MT-01…MT-07 **giữ nguyên**, không sửa nội dung đã chốt | 07/09/2026 |
| Mục 7 | ➕ Thêm mới | 8 mâu thuẫn (MT-01…MT-08) | Thêm **MT-09** — trùng mã test khoá `L-Z56` giữa spec (cổng D3.5) và MT-08 (kế toán CTRL) | Phát hiện khi mở backlog D1 (TD-0090), đối chiếu mã test đang có trong spec. Ghi nhận theo quy tắc gốc #11, **không tự chọn bên** — MT-08 giữ nguyên nội dung | 07/09/2026 |
| Mục 6 | ➕ Thêm mới | 10 câu hỏi mở (OQ-01…OQ-10) | Thêm **OQ-11** — cách đánh dấu tiến độ D0→D12, chốt **PA3**: cổng có bằng chứng máy, pha phát triển không đánh dấu, không có `current_stage` | Front-end cần biết "đang ở đâu" để trang Tiền thật nói thật; phân tích cho thấy 12 chặng không cùng một loại nên cơ chế đồng phục sẽ đẻ ra bằng chứng giả | 07/09/2026 |
| Mục 7 | ➕ Thêm mới | 9 mâu thuẫn (MT-01…MT-09) | Thêm **MT-10** — khối `evidence` của `runtime_state.json` không phân biệt "đo được" với "người khai"; chốt gắn nhãn `nguon` cho từng mục | Phát hiện khi rà `runtime_state.json` để thi hành PA3 phía dashboard — luật FE-L03 đòi phân biệt được hai loại này | 07/09/2026 |
| Mục 7 (MT-09) | ♻️ Sửa đổi | *chờ* — hai hướng (a) `L-Z59` / (b) đổi spec, chưa chọn | **ĐÃ CHỐT — phương án (c)**: không cấp phát mã `L-Z` mới, đặt tên `test_td0087_ctrl_khong_tinh_vao_n.py` theo đúng quy tắc bất thành văn đã có (test tự nghĩ mang mã `TD-xxxx`, xác nhận bằng cách rà 21/21 test `L-Z` hiện có đều truy được về spec) | Cả hai hướng ban đầu đều chiếm hoặc đe doạ chiếm namespace `L-Z` của spec; `L-Z59` là số kế tiếp spec sẽ dùng ở v9 nên sẽ va chạm lần hai | 07/09/2026 |
| Mục 7 | ➕ Thêm mới | 10 mâu thuẫn (MT-01…MT-10) | Thêm **MT-11** — trần chọn ý tưởng ghi hai số (1/quý vs 5/quý); chốt **5/quý dùng chung Tool A + D** và nới `L-Z17` thành **cảnh báo, không chặn chạy** (sửa có ý thức H16, giữ `L-Z16` chặn cứng) | Phát hiện khi front-end hỏi về trang “hàng chờ ý tưởng” — tra spec thì thấy hai dòng đá nhau, và đòn bẩy chặn đang phạt sai chỗ | 07/09/2026 |
| Mục 7 | ➕ Thêm mới | 11 mâu thuẫn (MT-01…MT-11) | Thêm **MT-12** — tờ đơn ý tưởng thiếu phần toán học; chốt **sửa đặc tả §9c.7.3** theo mô hình **hai cửa** (nộp rẻ / chọn chặt) + nối `so_bien_the` vào sổ trial qua `hypothesis_slot = IQ-xxxx` | Chủ dự án yêu cầu phân tích sâu cách LLM + người cùng đề xuất ý tưởng; lộ ra khoảng trống từ “ý tưởng” tới “phép thử chạy được” đang lấp bằng trí nhớ | 07/09/2026 |
| Mục 6 | ➕ Thêm mới | 12 câu hỏi mở (OQ-01…OQ-12) | Thêm **OQ-13** — kênh đề xuất đổi tham số khi đã chạy: §12c.3/§12d có luật đủ nhưng `L-Z26` chưa có code và chưa có sổ ghi; phải xong trước D11 | Phát hiện khi tra spec cho câu hỏi “khi hệ thống chạy được một thời gian thì đề xuất thế nào” — `grep` toàn repo không ra `L-Z26` | 07/09/2026 |
| Mục 6 (OQ-07) | ♻️ Sửa đổi | *chờ* — tiêu chí chọn ý tưởng của quý chưa viết | **✅ Đã giải (TD-0120)** — `DR-Q3-2026-tieu-chi-chon-y-tuong.md`: hạn ngạch chọn quý 3/2026 = **0** + ba điều kiện mở lại viết trước + 4 tiêu chí thứ tự từ điển; máy kiểm `selection_reason` phải trích mã TC | Chủ dự án yêu cầu phân tích tổng thể trước khi mở hàng chờ; ba ràng buộc cứng (chưa có lệnh live, `mult_edge` cần 50 lệnh, lockbox chỉ có một và chưa chạm) cho thấy quý này chọn ai cũng là tiêu suất A để thay thứ chưa biết có hỏng không | 07/09/2026 |
| Mục 6 (OQ-13) | ♻️ Sửa đổi | *Chưa chốt — phác thảo thiết kế đã bàn 07/09/2026, chờ duyệt khi tới lượt* | ✅ **Đã giải** (TD-0125) — phác thảo được duyệt và thi hành nguyên vẹn; chi tiết máy kiểm ở MT-13 | Chủ dự án duyệt phác thảo; `L-Z26` từ 0 dòng code → 30 test khoá | 07/09/2026 |
| Mục 7 | ➕ Thêm mới | 12 mâu thuẫn (MT-01…MT-12) | Thêm **MT-13** — sổ đề xuất đổi tham số: 8 ca fail-closed, `luan_diem` là mảng có cấu trúc, `bao_cao_hash` máy tự băm, một đề xuất = một tham số | Luật §12c.3/§12d đủ nhưng không có gì thi hành — cùng hình dạng MT-02 | 07/09/2026 |
| Mục 7 (MT-11) | ♻️ Sửa đổi | Ghi nhận trần **nhập** 10 ý tưởng/quý *“chưa có test khoá nào thi hành — mới là văn bản; chưa code”* | Đã lấp bằng TD-0124 — `check_td0124_*` + cửa ghi `submit_idea()`, **chặn cứng**, không vào `WARN_ONLY_CODES` | Chính MT-11 nêu lỗ hổng này; công cụ nộp đơn là chỗ tự nhiên để bịt | 07/09/2026 |
| Mục 7 (MT-08) | ♻️ Sửa đổi | 🟡 *đã chốt phương án, chưa thi hành — phải sửa trước dòng CTRL đầu tiên* | ✅ **ĐÃ THI HÀNH** (TD-0130, commit `5d5ae82` + `e6a67f1`): CTRL ra khỏi `n_used()`/`n_reserved()`, `reserve()` miễn kiểm ngân sách cho CTRL, kèm **cửa xác thực fail-closed** hai dạng (tái lập / đo thước). Chặt hơn phương án ở hai chỗ, đã ghi rõ trong ô MT-08 | Việc còn lại của một quyết định đã duyệt từ 07/09 nhưng **0 dòng code** — chọn làm vì nó không giẫm hai phiên song song (chỉ đụng `registry.py`), trong khi TD-0127 (việc 🔓 duy nhất lúc đó) phải sửa đúng hai file cả hai phiên đang gõ dở. Trong lúc làm **bắt được lỗi do chính mình gây ra**: cửa ghi thêm khoá mới mà schema `additionalProperties: false` chưa biết — 735 test xanh vẫn không bắt được, vì test schema chỉ đối chiếu fixture gõ tay. Đã vá + khoá cả lớp lỗi | 07/09/2026 |
| Mục 7 | ➕ Thêm mới | 13 mâu thuẫn (MT-01…MT-13) | Thêm **MT-14** — Bước 2 của DR-015 đo KHÔNG trên testnet, lệch chữ spec dòng 39/2975; đã chốt TRƯỚC khi đo bằng `DR-D35-01`, giá phải trả là phép đo GIÁN TIẾP đã ghi trong `d3_5_han_che` | Ghi nợ đặc tả còn treo từ D3.5 — cổng đã đóng nhưng độ lệch với spec chưa vào sổ | 08/09/2026 |
| Mục 7 | ➕ Thêm mới | 14 mâu thuẫn (MT-01…MT-14) | Thêm **MT-15** 🔴 — §3.3b thi hành `(a) HOẶC (b)` trong khi spec PHẦN 3b đòi `(a VÀ c) HOẶC (b)`; điều kiện (c) volume có **0 dòng code**. Hệ quả: `Z0` trùng khớp `Z0-V1`, một suất trial đo khác biệt bằng không. **Chưa chốt, chờ chủ dự án** | Phát hiện khi làm TD-0182 (nối §3.3b vào tín hiệu vào lệnh) | 08/09/2026 |
| Mục 6 (OQ-06) | ♻️ Sửa đổi | `v_min` hiện `null`, ghi `TUNED_PENDING` | Thêm câu trỏ sang **MT-15**: OQ-06 là *giá trị còn trống*, MT-15 là *công thức sai + (c) chưa có code* — điền một con số KHÔNG đủ để §3.3b chạy đúng | Hai chuyện khác nhau ở cùng một tham số; đọc lẻ OQ-06 sẽ tưởng chỉ thiếu con số | 08/09/2026 |
| Mục 7 (MT-15) | ♻️ Sửa đổi | 🔴 *chờ* — hai việc cần chủ dự án quyết, không tự chọn một bên | ✅ **ĐÃ THI HÀNH** (DR-D4-03 `8046a27` + vá `942efe7`): công thức về đúng `(a VÀ c) HOẶC (b)`, `bat_dieu_kien_c` bắt buộc không mặc định. 1217 passed; +17 ca, −0 ca; phá thật trên file sản xuất ra đúng 1 ca đỏ | Chủ dự án chốt phương án C sau khi xem bảng chi phí 4 phương án | 08/09/2026 |
| Mục 6 (OQ-06) | ♻️ Sửa đổi | 🟡 `v_min` null, `TUNED_PENDING`, chờ calibrate bằng dữ liệu ở B1 | ✅ **Đã giải** — `FROZEN` ở **1.0** kèm `frozen_rationale`, **0 trial**, ĐÓNG BĂNG TẠI CHỖ nên `\|tier_b\|` vẫn 12 và **N vẫn 114** | Chuyển sang `tier_frozen` sẽ hạ N xuống 108 và rào DSR xuống 3,0601 — nới chuẩn đánh giá của chính mình. Thứ tự có chủ đích: ablation TRƯỚC, calibrate SAU | 08/09/2026 |
| Mục 7 (MT-15 §6) | ➕ Thêm mới | — | Ghi nhận **bẫy PASS RỖNG thứ NĂM**, khác bốn cái trước ở chỗ nó **do chính hành động sửa tạo ra**: `check_lz15` chỉ soi tham số `null`, nên ghi giá trị vào là lớp canh thôi canh. Đã siết cùng lượt | Nếu không siết, bản vá MT-15 tự làm câm lớp canh duy nhất theo dõi `v_min` | 08/09/2026 |
| Mục 7 | ➕ Thêm mới | 15 mâu thuẫn (MT-01…MT-15) | Thêm **MT-16** 🔴 — **không có tầng định cỡ lệnh**; `stake_amount` placeholder 10 USDT đang là cỡ THẬT của mọi lệnh; `trade.stake_amount` ở nhánh thêm tranche làm tỉ trọng thành ¼-¼-½ thay vì ⅓ đã đóng băng. Bốn hệ quả, trong đó `Z0-S1` vô nghĩa và mọi ngưỡng tuyệt đối D4 nói về hệ thống 39,98 USDT/lệnh. **Chưa giải, chờ chủ dự án** | Phiên `-f6` phát hiện khi kiểm `mult_zss` bằng cách đọc thẳng đường sinh cỡ lệnh thay vì grep tên; phiên này tái lập độc lập và đo thêm hệ quả (iv) trên artifact D3.5 | 08/09/2026 |
| Mục 7 (MT-16) | ♻️ Sửa đổi | 🔴 *CHƯA GIẢI — chờ chủ dự án*; bốn hệ quả; cảnh báo *"dựng tầng định cỡ ⇒ phải ĐO LẠI Δ_R, mở lại D3.5"* | ✅ **ĐÃ CHỐT `DR-D4-04`, ĐANG THI HÀNH** (TD-0187 ✅, còn 0188/0189). **BẢY** hệ quả — thêm (v) không ai cài `leverage()` ⇒ mọi backtest chạy 1x và `L-Z3` chưa từng bị thử thách; (vi) SL bị nhân đòn bẩy, bug sống từ D1 (`Minimal:256`), đo được 38/38 lệnh nổ stop ở giá cao hơn `sl`; (vii) TD-0082 chưa nhân `mult_*`/chia đòn bẩy + Freqtrade **NUỐT** exception callback. **RÚT LẠI** cảnh báo Δ_R kèm số liệu | Chủ dự án bác cách đóng khung "định cỡ là lựa chọn"; và cảnh báo Δ_R **sai** — Δ_R bất biến với cỡ lệnh tới bit cuối, đo lại trên chính artifact | 08/09/2026 |
| Mục 7 | ➕ Thêm mới | 16 mâu thuẫn (MT-01…MT-16) | Thêm **MT-17** 🔴 — **không có tầng chốt lời (PHẦN 5)**; `minimal_roi` tắt, 0 module TP, mọi lệnh chỉ thoát bằng SL/DG6/DG7/DG8. Đã chốt `DR-D4-04` §4 → TD-0189, chặn bởi `tp1_haircut_pct` đang "im lặng" | Chủ dự án nêu *"Position sizing và tỉ lệ R:R cực kì quan trọng"* — vế "reward" cũng không có code, và spec mặc định D0.9 chạy CÓ TP (câu hỏi mở #3/#11) | 08/09/2026 |
| Mục 7 | ➕ Thêm mới | 17 mâu thuẫn (MT-01…MT-17) | Thêm **MT-18** 🔴 — `L-Z29` canh SỐ ĐẾM của `tier_b`, không canh DANH TÍNH; hoán một khoá ĐÓNG BĂNG lấy một khoá ĐANG TUNE giữ `\|tier_b\|`=12 nên mọi bất biến đắt tiền (`N`, rào DSR, `L-Z15`, TD-0169, TD-0190) đi qua trót lọt. **Đã chốt phương án (a)** — vá từng ca, không đổi kiến trúc `dof_inventory.yaml`/`L-Z29` | Phát hiện khi làm TD-0189, phá thật bằng phép hoán đổi `tp_fallback_dist_r` ↔ `dg7_funding_frac`. TD-0189 đã tự đóng lỗ hổng ở phạm vi hẹp của chính nó | 09/09/2026 |
| Mục 7 | ➕ Thêm mới | 19 mâu thuẫn (MT-01…MT-19) | Thêm **MT-20** 🔴 — §1.3 (zone chết sau 40 nến) và §5.1 (zone đối diện là LUẬT, nạng là NGOẠI LỆ) không thể cùng đúng trên dữ liệu thật; áp hạn dùng đẩy tỉ lệ nạng lên 79–87%, biến phân loại L2 thành kết luận biết trước. **Đã chốt `DR-D4-06`** — §1.3 không áp cho zone đối diện, H-4 tách làm hai số, tuổi zone ghi Decision Log, zone đóng băng lúc vào lệnh | Phát hiện khi dựng vòng quét zone đỉnh cho TP1 (TD-0189); định lượng bởi phiên `-94`. Kèm sự cố hai phiên độc lập viết trùng file DR cho cùng quyết định — xem `CLAUDE.md` N12 mục 6 mới | 09/09/2026 |
| Mục 7 | ➕ Thêm mới | 22 mâu thuẫn (MT-01…MT-22) | Thêm **MT-23** 🔴 — `tier_b` khai 12 tham số tunable nhưng **7 không có đường đọc nào** từ `tool_d_config.yaml` vào mã chạy; giá trị thật đến từ hằng số cứng trong `.py`. Ba trong số đó nằm TRÊN đường chạy sản xuất (`zss_threshold`/`buf_sl_atr`/`dg6a_atr_ratio`). Cùng khuôn **MT-08** (code chưa thi hành điều đã chốt — ở đây là **N4**), không phải mâu thuẫn giữa hai quyết định. **Đo được, không suy luận:** đổi `zss_threshold` 0,5 → 0,99 trong YAML rồi chạy backtest THẬT — TRƯỚC bản vá **17 passed** (vẫn xanh), SAU **1 failed + 16 errors** ⇒ một trial B3 chi cho khoá đó mua về **thông tin bằng không**. Cùng họ MT-15 nhưng im lặng hơn một bậc (sổ trial ghi *cấu hình*, không ghi *tập lệnh*). ✅ **ĐÃ THI HÀNH** phần có hậu quả (TD-0195, `0b5112d`, Docker **1481 passed**); 4 khoá còn lại hoãn có lý do trong `MIEN_TRU`, kèm ca đỏ khi miễn trừ hết hạn và câu **ĐƠN VỊ** phải chốt trước khi nối `dieu_kien_d` | Phát hiện + thi hành 09/09/2026 (TD-0195); `L-Z15`/kiểm kê DOF không phủ vì chúng nói về **tư cách** tham số, MT-23 nói về **đường đi** | 09/09/2026 |
| Mục 7 | ➕ Thêm mới | 26 mâu thuẫn (MT-01…MT-26) | Thêm **MT-27** 🟡 — vòng lặp *"đo expectancy thô → sửa logic → đo lại"* của mọi quy trình chống overfitting **không mua được** trong ngân sách N=114 vì `DR-014 §2` tính mỗi lần đánh giá cấu hình = 1 trial, và `CTRL_OUTPUT_ALLOWED` cố ý không nhận chỉ số hiệu năng; và **MT-28** 🟡 — spec dòng 4260 để trống ô ngưỡng DSR trong khi `gates/thresholds.py:24` đã có `0.10` theo `DR-D0PRE-03` | Phát hiện khi đối chiếu một checklist anti-overfitting ngoài do chủ dự án đưa vào lộ trình D0-PRE→D12 (TD-0208, `docs/doi-chieu-checklist-antioverfitting.md`). Cả hai ghi nhận theo quy tắc 11, **không tự chọn bên** | 10/09/2026 |
| Mục 7 | ♻️ Sửa đổi | `MT-15` ghi *"CHƯA CHỐT"*; `MT-21` ghi *"chặn TD-0184 cho tới khi cả hai được nối"* | Gắn **đính chính tại chỗ** cho cả hai (giữ nguyên chữ cũ): `MT-15` đã chốt bằng `DR-D4-03`, và nỗi lo *"`Z0` ≡ `Z0-V1`"* chết bằng phép đo `TD-0214` (31 vs 22 lệnh); `MT-21` có điều kiện chặn riêng đã thoả (`TD-0192`/`TD-0193` ✅) và *"`Z1` ≡ `Z0`"* cũng chết bằng phép đo (6 vs 22) — nhưng sinh ra mâu thuẫn MỚI (`Z1` là tập con nghiêm ngặt), trỏ sang `MT-31` | Hai kết luận-trùng cũ đều đúng vì arm **chưa được nối**; nối rồi (`TD-0192`/`TD-0193`) thì hết đúng. Phát hiện khi đối chiếu chín arm trên cùng một hệ thống lần đầu (`TD-0213`→`TD-0215`), trong lúc lập `DR-D4-10` (tái định phạm vi D4 theo cỡ mẫu, sinh từ `TD-0208` đối chiếu một checklist anti-overfitting ngoài) | 11/09/2026 |
| Mục 7 | ♻️ Sửa đổi + ➕ Thêm mới | 31 mâu thuẫn; `MT-26` CHƯA GIẢI; `MT-29` kết luận *"D4 Long-only không chạy"* | `MT-26` **câu 1 chốt — phương án (C)**: `Z0-T1` là cấu hình ứng viên, `Z0-T0` chỉ chẩn đoán ⇒ D4 có **đúng một** ứng viên vượt sàn; **câu 2 vẫn mở**. `MT-29` gắn đính chính: câu *"không bao giờ qua sàn"* đúng với 7/9 arm, **không** với `Z0-T1` (325,6) và `Z0-T0` (1.396,0) — nó ngầm trả lời câu 1 của `MT-26` khi `MT-26` còn CHƯA GIẢI. Thêm **`MT-33`** — các arm Phần 2 **không còn lồng nhau** sau `TD-0207` (1 và 20 lệnh mồ côi), cơ chế **tranh chỗ mở** | Phát hiện khi đối chiếu `MT-26`/`MT-29` với `td0212-ba-arm-sau-va.json` theo TỪNG ARM thay vì một con số chung. Cả ba là cùng một ca: *mọi mảnh đều kiểm được và đúng, chỉ **phạm vi** kết luận sai* (`research-log` 12/09). Thi hành ở `DR-D4-10` `24b1d27` | 12/09/2026 |
| Mục 7 | ➕ Thêm mới | 33 mâu thuẫn (MT-01…MT-33) | Thêm **MT-34** 🔴 — pool mà D4 sẽ backtest là **ảnh chụp NGÀY HÔM NAY**: `delisted_at` chưa bao giờ được gán ngoài `tests/unit/test_pool.py` (`grep` cho đúng 5 kết quả), `build_pool.py:226` dùng `compute_pool()` chứ không point-in-time, trong khi `DR-D1-01` §3 đã chốt *"không mở DR chấp nhận survivorship bias"*. Đo được (`TD-0230`, 0 trial): **44/102 mã pool không tồn tại tại `T0`**, 24/102 tại `T1`; kênh mã đã chết **15 trong CALIB, 0 trong WFO**. Kèm đề xuất đính chính con số **219 → 31** của `DR-D1-01` §1. Và **MT-35** 🔴 — khoá `arm` gộp **hai trục**, nên `arm: "Z0"` (44,8 lệnh/năm, dưới sàn 150) ≠ arm được phán quyết `Z0-T1` (325,6) | Rà soát ba việc chủ dự án hỏi (ảnh Docker · D4 cho `Z0-T1` · FreqAI) bắt được hai thứ không nằm trong cả ba; quy tắc 11 — ghi nhận, không tự chọn bên | 12/09/2026 |
| Mục 7 | ♻️ Sửa đổi + ➕ Thêm mới | 35 mâu thuẫn (MT-01…MT-35); `MT-34` chưa có số cho kênh volume; `MT-35` CHƯA GIẢI | `MT-34` **append kết quả `TD-0231`**: tại `T1` — mốc DUY NHẤT chạy vào phán quyết D4 — `pool.yaml` 102 mã vs pool ĐÚNG 116 mã, **CHUNG chỉ 55**, `K = 61` (**52,6%**, gấp hơn NĂM LẦN ngưỡng 10% đã viết TRƯỚC khi chạy) ⇒ WFO đang đo **sai rổ** theo chiều PASS; ba kênh lệch nay tách rời và chỉ một kênh gây thiên lệch. `MT-35` **chốt LUẬT, không chốt arm** — *arm sản xuất = arm PASS Nhánh 1*, `Z0` **không** phải dự phòng; kèm hệ quả phải mở lại tiền đề `DR-D4-10` §2.4 (7,1 năm tính ở tần suất `Z0`, ở `Z0-T1` là ~1 năm). Thêm **`MT-36`** 🔴 — `n = 206` tính trên TOÀN cửa sổ nhưng ba cửa sổ test chỉ phủ **147/231 ngày**; chốt *báo cả hai, phán quyết trên test-only*, đã có máy ở `TD-0232`. Thêm **`MT-37`** 🟡 — `Δ_R = 0,1612` đo trên **đúng hai cặp** mà `d3_5_han_che` không khai; 🟡 vì **chưa kích hoạt**, không phải vì ảnh hưởng nhỏ | Rà soát ba câu treo (MT-34/MT-35/FreqAI) bắt được **câu thứ tư chưa ai đặt** (`MT-36`), và phép đọc artifact niêm phong của `TD-0231` lộ ra `MT-37`. Quy tắc 11 cho hai mục mới; hai mục cũ append, **giữ nguyên chữ cũ** | 13/09/2026 |
| Mục 7 | ➕ Thêm mới | 36 mâu thuẫn (MT-01…MT-31, MT-33…MT-37; MT-32 là chỗ trống của `TD-0217`) | Thêm **MT-38**…**MT-42**, cả năm phát hiện trong một lượt rà soát *"còn gì để qua Dry-run"*: **MT-38** 🔴 cổng D4 **không bao giờ đóng được** (khoá `d4_complete` đòi đúng 18 trial `B2` CONSUMED, `DR-D4-01` cấp 9 Long-only) ⇒ D5 không mở ⇒ chặn toàn bộ đường tới D11; **MT-39** 🔴 SL không sống trên sàn, trái §6.6(1), và cả chuỗi D2a/D2b/D2c cùng `gap_ms` §8.3 chỉ có nghĩa khi SL ở trên sàn — mà D2b/D2c là điều kiện CỨNG để vào D11; **MT-40** đỉnh equity chỉ nằm trong bộ nhớ tiến trình ⇒ thang drawdown/HALT về 0% sau mỗi restart, mất một trong BA tầng Cấp C chặn vòng lặp thua lỗ; **MT-41** kế hoạch cỡ lệnh cũng vậy, và `unfilledtimeout.entry = 180 phút` làm *"restart lúc có lệnh chờ khớp"* thành ca THƯỜNG ⇒ `SizingError` bị `strategy_safe_wrapper` nuốt ⇒ vị thế chạy với `stoploss = −0,99`; **MT-42** E5 để 22/22 chỉ số ở `pending` bằng một HẰNG SỐ và Decision Log có **0 người gọi sản xuất** ⇒ dry-run hai tuần sinh 0 chỉ số đọc được | Chủ dự án yêu cầu rà soát hệ thống và lập danh sách việc pending để triển khai qua giai đoạn Dry-run (13/09/2026). Năm mục đều là mâu thuẫn giữa hai thứ ĐÃ CHỐT hoặc khiếm khuyết của một chốt đã chốt ⇒ ghi theo quy tắc 11, **không tự chọn bên**. Chủ dự án chốt: **ghi `MT` và mở mã việc, CHƯA sửa mã** — `Khối 18` trong `TASKS.md` (`TD-0235`…`TD-0244`, `a97ab8c`). 🔑 Ba mục **MT-39/40/41 KHÔNG làm sai một con số backtest nào đã đo** (backtest chạy một tiến trình, không restart, khớp lệnh mô phỏng cục bộ) — chúng là khoảng hở của **đường chạy DÀI**, tức đúng D11/D12; khai phạm vi ra đây để không ai đọc quá tay. `MT-38` phải đọc kèm `DR-D4-10` (7/9 suất đã thành MÔ TẢ), và mục này ghi cả **cách đọc bị bác** *"0 khác 18 vì Long rớt sàn 150"* — cách đọc đó xuất hiện độc lập HAI lần dù `DR-D4-10:178-179` đã phản bác sẵn trên đĩa | 13/09/2026 |
| Mục 7/`TASKS.md` (TD-0241) | ♻️ Sửa đổi | TD-0241 🔒, vế "tiến trình riêng chạy được" CHẶN BỞI QUYẾT ĐỊNH KIẾN TRÚC | TD-0241 ✅ — ba quyết định kiến trúc (cơ chế phát hiện `LIQUIDATED`, phạm vi hành động khi có cờ đỏ, vị trí file tiến trình) chốt qua trao đổi trực tiếp với chủ dự án; chi tiết + bằng chứng đọc mã nguồn Freqtrade ở `docs/decisions/DR-D11-03-risk-supervisor-tien-trinh.md` | Chủ dự án yêu cầu triển khai TD-0241; quyết định kiến trúc không được tự chọn (quy tắc 2) nên hỏi trước khi code, ghi lại DR theo đúng khuôn các DR-D11-0x trước đó | 14/09/2026 |
| Mục 6 (`OQ-11`) | ♻️ Sửa đổi | Khoá `d4_complete` ← *"đếm được **đúng 18** trial `B2` CONSUMED"* (chốt PA3 07/09/2026, `6342113`) | **Gắn đính chính tại chỗ, KHÔNG xoá con số 18** — trỏ sang `DR-D4-11` (`79db3a8`) đã thay tiêu chí bằng HIỆN VẬT. Giữ nguyên chữ cũ vì nó ghi lại thứ chủ dự án đã NHÌN THẤY khi quyết PA3, cùng kỷ luật đã áp cho `DR-D0PRE-06` | Lý do KHÔNG phải con số sai. `DR-D4-10:211-213` hạ **7/9 suất xuống thống kê MÔ TẢ** ⇒ MỌI cổng đếm trial đều PASS kể cả khi `Z0-T1` — cấu hình DUY NHẤT còn phán quyết được — chưa từng chạy. PASS RỖNG nằm trong chính ĐỊNH NGHĨA cổng, hạ 18→9 không chạm tới | 14/09/2026 |
| Mục 7 (`MT-38`) | ♻️ Sửa đổi | `MT-38` ghi *"CHƯA GIẢI — ghi nhận theo quy tắc 11, không tự chọn bên"*, nêu hai đường (a) hạ khoá 18→9 (b) tách `d4_long/short_complete` | ✅ **ĐÃ GIẢI** — chủ dự án chốt đường THỨ BA: đóng bằng HIỆN VẬT. Cả hai đường cũ bị loại, ghi lý do loại ngay trong `DR-D4-11` §6 để người sau khỏi đề xuất lại | Việc `TD-0236` (`5910f74`): `d4_gate.py` **không chứa số 18 lẫn 9** (ca AST ghim), kế toán là QUAN HỆ *"số `B2` CONSUMED khớp số arm đã chạy"*. Kiểm-có-răng phá thật trong Docker: bỏ kiểm arm phán quyết ⇒ 2 đỏ · ghim cứng `18` ⇒ 6 đỏ · bỏ kiểm kế toán ⇒ 1 đỏ. **0 trial**, sổ giữ 13 dòng. ⚠️ `close_d4_gate()` vẫn CHƯA viết — việc `TD-0186` | 14/09/2026 |
| Mục 7 | ♻️ Sửa đổi | 45 mâu thuẫn (MT-01…MT-43); `MT-34` chưa ghi lựa chọn của chủ dự án; `MT-37`/`MT-41` còn ghi *"chờ chủ dự án"* dù đã giải; `MT-39` mang một câu SAI về `spec:4506` | **Bốn dòng, không thêm mục mới.** `MT-39` **đính chính tại chỗ** claim sai *spec:4506 xếp §6.6 vào nhóm KHÔNG BAO GIỜ CẮT* (§6.6 **không** có trong danh sách đó) — giữ nguyên chữ sai, gạch ngang, gắn lý do. `MT-41` ✅ chốt + thi hành (`TD-0237`, `07326c2`) kèm phân biệt vì sao phục hồi từ `entry_tag` + `stake_amount` **không** phải ca `TD-0114` bị cấm. `MT-37` ✅ giải (`TD-0234`, `237784e`). `MT-34` ghi chủ dự án chốt **A-đầy-đủ** và cảnh báo nó **vẫn chưa có mã việc** | Lệnh *"chuẩn hoá và lưu"* 14/09/2026. 🔑 Đợt này khác các đợt trước ở một chỗ: **một trong bốn thay đổi là sửa lỗi của chính phiên đang ghi** — claim `spec:4506` do phiên `[3f7d14]` viết 13/09, phiên `-3f` bắt được 14/09 **sau khi nó đã lừa được một người đọc thật**. Phân chia với phiên `-3f` (cùng nhận lệnh cùng ngày): họ giữ `MT-38` + `OQ-11`, phiên này giữ bốn dòng trên; **không giao nhau một dòng nào**, và bốn dòng của phiên khác (`MT-38`/`MT-40`/`MT-42`/`MT-43`) được máy kiểm là **không đổi một byte** trước khi ghi | 14/09/2026 |
| Mục 7 | ♻️ Sửa đổi + ➕ Thêm mới | **42** dòng `MT` (`MT-01`…`MT-43`, `MT-32` là chỗ trống của `TD-0217`); `MT-19`/`MT-25`/`MT-26` ghi *“chờ chủ dự án”*; `MT-39`/`MT-40`/`MT-42` ô trạng thái lỗi thời so với đĩa | **Sáu dòng đính chính tại chỗ + ba dòng mới, không xoá chữ nào.** `MT-19` ✅ chốt đường (c) dạng CTRL thứ ba, **kèm lật tiền đề của chính mục**: spec buộc danh sách 3 trường vào D3.5/Bước 1 ở cả ba chỗ ⇒ **mã CHẶT HƠN spec**, và thu hẹp về phạm vi spec **không biểu diễn được** vì sổ không có trường phân biệt pha. `MT-25` ✅ phần đơn vị giải bởi `DR-D4-12` §1, **kèm đính chính `λ` đổi theo từng lệnh**. `MT-26` câu 2 vẫn MỞ. `MT-39`/`MT-40`/`MT-42` sửa ô trạng thái lỗi thời. ➕ **`MT-44`** 🔴 DG1–DG5 bất động trên 6/9 arm kể cả arm sản xuất · ➕ **`MT-45`** 🔴✅ `DR-D4-12` §3 va `MT-34` — chủ dự án chốt A-đầy-đủ vẫn đứng · ➕ **`MT-46`** 🟡 sáu chỗ tầng trạng thái trôi khỏi đĩa, 1/4 lời khai sai, cơ chế kế thừa bằng tham chiếu. Sau: **45** dòng `MT` | Lệnh *“chuẩn hoá và lưu”* 16/09/2026. 🔑 **Đợt này bắt được một lỗi do CHÍNH quy trình ghi làm lộ ra:** N9 + quy tắc 3 buộc đọc lại file cũ, và lần đọc đó đi tới **cuối** dòng `MT-34` — nơi nằm quyết định A-đầy-đủ mà một lệnh cắt đầu dòng đã bỏ sót, dẫn tới việc trình lại một câu đã chốt và commit `DR-D4-12` §3 ngược với nó (`MT-45`). ⚠️ Dòng lịch sử ngay trên ghi *“45 mâu thuẫn (MT-01…MT-43)”* — đếm lại trên đĩa là **42**; không sửa dòng cũ, chỉ không lan con số | 16/09/2026 |
| Mục 7 (`MT-24`, `MT-25`) | ♻️ Sửa đổi | `MT-24` ghi *"CHƯA GIẢI … chờ chủ dự án"*; `MT-25` phần đơn vị ✅ nhưng phần cách đọc `DR-D4-09` §7 điều 1 *"chưa giải"* | **Hai ô trạng thái nối thêm, không xoá chữ nào, không thêm dòng `MT`** (vẫn **45**, đếm lại trên đĩa — con số *"42"* ở dòng lịch sử ngay trên là trạng thái TRƯỚC đợt đó). `MT-24` ✅ theo `DR-D4-13` §1 — EXPLORE theo đúng chữ spec §9c.4b (phân tích không giới hạn, 0 trial; cấm dùng làm căn cứ phán quyết/đổi thước/chứng minh đáng tiêu suất), kèm **đính chính ô bên trái** của chính dòng đó (con đường tới `std_R` mở sẵn; cơ chế allowlist bị loại). `MT-25` ✅ phần còn lại theo `DR-D4-13` §2 — §7 điều 1 đọc trên `R_trien_khai`, cấm quy đổi bằng `λ`. Cả hai dòng ghi rõ **không còn chặn `TD-0184`**; thứ chặn duy nhất còn hiệu lực là `TD-0247` | Lệnh *"chuẩn hoá và lưu"* 16/09/2026, sau `daaaba9` (DR) · `cd7ca8e` (đính chính `DR-D4-08`/`DR-D4-09`) · `b6217c0` (docstring, Docker `tests/lock` 1218 passed). 🔑 Dòng người dùng dán để kích hoạt việc này (*"Chặn TD-0184: MT-24 · MT-25 · MT-38 — cả ba là quyết định của bạn"*) **lệch đĩa ở 2/3 mục**: `MT-38` đã giải từ 14/09 (`DR-D4-11`), `MT-25` đã giải nửa. Và khuyến nghị đầu cho `MT-24` được đưa ra **trước khi mở §9c.4b** — chủ dự án đã chọn theo nó, phải hỏi lại. Cả hai là hình dạng `MT-46` (trạng thái/lời khai đứng sau đĩa) | 16/09/2026 |
