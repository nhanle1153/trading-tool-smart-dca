"""D4 ablation — phần DỰNG (`DR-D4-14`, Khối 30).

* `khoa_do`  — khoá khâu ĐO (`D4_DO_TAM_DUNG`), chỗ DUY NHẤT.
* `ban_ghi`  — lô arm + lệnh đã trích → bản ghi arm (`gates/arm_record.py`).
* `chay_lo`  — vòng đặt chỗ → chạy → niêm phong → ghi → tiêu cho từng arm (E3).

Toàn gói 0 suất trial khi khoá còn bật.
"""
