"""Tầng chống nhiễm phép đo — PHẦN 0d của spec (dòng 517-688).

Bảy ràng buộc, mỗi ràng buộc một module:
    guard.py        0d.1 — guard file tham số ẩn
    (config/loader) 0d.2 — cấm *Parameter của Freqtrade
    (guard/hashing) 0d.3 — cache là nguồn nhiễm bậc hai
    (config/loader) 0d.4 — env chỉ cho vận hành
    provenance.py   0d.5 — khối xuất xứ 7+1 khoá
    tri_state.py    0d.6 — ba trạng thái dữ liệu
    (research-log)  0d.7 — chẩn đoán bot sai / tầng đo sai
"""
