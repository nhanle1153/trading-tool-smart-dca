"""Các cổng đánh giá — fail-closed theo mặc định.

d0_pre.py      — cổng D0-PRE: cấm chạm dữ liệu trước khi hạ tầng đo xong
thresholds.py  — ngưỡng GATE §10.2, chưa điền = +inf (L-Z35)
dsr.py         — dsr_hurdle(N), N đọc từ registry (L-Z34)
"""
