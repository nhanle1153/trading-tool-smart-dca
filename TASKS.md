# TASKS.md — Backlog Tổng & Tracker Công việc — Tool D

> Khởi tạo cuối Giai đoạn 2, ngày 06/09/2026. Mã project: **TD**.
> Backlog phủ **D0-PRE** (Khối 0–8, ✅ đóng cổng 06/09/2026) và **D1** (Khối 9–12, mở 07/09/2026).
> Các giai đoạn D2→D12 sẽ thêm việc khi tới lượt.
> Nguồn tạo "Mục lớn": danh sách chức năng ưu tiên ở `back-end-note.md` §0.1 (GĐ1)
> kết hợp với các khối trong `ARCHITECTURE.md` (GĐ2).

## Quy tắc vận hành file này

1. **Mã việc:** `TD-{4 số}`. Số **không bao giờ tái sử dụng**, kể cả khi việc bị hủy.
2. **Trạng thái:** 🔓 Chưa làm · 🔒 Đang làm · ✅ Done · ❌ Đã hủy.
3. **Trước khi code một việc:** đổi 🔓 → 🔒. ~~Chỉ 1 người/1 tab nên bước khóa được nới lỏng, nhưng vẫn đổi trạng thái để theo dõi.~~
   🔴 **Đính chính 18/09/2026:** câu gạch **sai từ khi dự án có nhiều phiên song song**. Khoá là **bắt buộc**: đổi 🔓 → 🔒 và **commit RIÊNG ngay**, trước dòng mã đầu tiên (`CLAUDE.md` quy tắc 14). Commit khoá mang dòng `Phien: <mã phiên>`; người giữ việc chỉ đọc từ 🔒 + dòng đó, **không** từ tên phiên ghi trong văn bản (`CLAUDE.md` N12 mục 7).
4. **Khi xong:** đổi 🔒 → ✅ — và chỉ khi **lệnh verify ở cột cuối đã chạy và cho đúng kết quả**, không phải khi "code trông có vẻ đúng".
5. **Việc bị hủy:** không xóa dòng — đổi ❌, giữ nguyên mã.
6. **Cập nhật trạng thái:** tự do, ngay lập tức.
7. **Cập nhật nội dung việc:** ghi vào "Lịch sử thay đổi checklist" bên dưới.

> 🔴 **THỨ TỰ KHỐI 3 LÀ BẮT BUỘC TUẦN TỰ** (spec dòng 4420–4424): (1) → (2) → (3), không làm song song.
> 🔴 **KHỐI 1 PHẢI XONG TRƯỚC MỌI KHỐI KHÁC** (dòng 4436–4439): con số nào sinh ra trước khi tầng chống nhiễm sống thì không dùng được cho bất kỳ quyết định nào.

---

## Checklist công việc

### Khối 0 — Hạ tầng repo

| Mã | Tên việc | TT | Phụ thuộc | Verify — chạy gì, thấy gì thì xong |
|---|---|---|---|---|
| TD-0001 | `git init`, `.gitignore`, `.gitattributes` (ép LF), cây thư mục | ✅ | — | `git check-attr text -- src/tool_d/x.py` → `text: set` |
| TD-0002 | Khởi tạo 4 file quy trình: `CLAUDE.md`, `ARCHITECTURE.md`, `back-end-note.md`, `TASKS.md` | ✅ | TD-0001 | 4 file tồn tại; `back-end-note.md` có đủ mục 0–8 |
| TD-0003 | Tạo repo GitHub **private**, push `main` | ✅ | TD-0002 | `git remote -v` có origin (`github.com/nhanle1153/trading-tool-smart-dca`); `git log origin/main` khớp local — đã push thành công |
| TD-0004 | `docker/Dockerfile` (base Freqtrade **pin theo digest** + `git` + pytest + `safe.directory`) và `docker-compose.yml` (3 service; **KHÔNG mount `lockbox/data/`** vào `tests`; `TZ=UTC`) | ✅ | TD-0001 | `docker compose run --rm tests python -c "import freqtrade,pytest,yaml;print('ok')"` |
| TD-0005 | Xác minh `git_sha` lấy được **từ trong container** | ✅ | TD-0004 | `docker compose run --rm tests git rev-parse HEAD` in đúng SHA của host, không phải rỗng |
| TD-0006 | Quyết định số phận `dashboard-ui/` (project Node có `.git` riêng và có `.env`) — submodule / repo riêng / gộp thẳng vào. Hiện đang bị `.gitignore` chặn | ✅ | TD-0001 | Chủ dự án chọn "repo riêng" (lý do: backend D0-PRE có spec kiến trúc khoá cứng + Docker context riêng, không muốn ~1500 gói npm của front-end lẫn vào). Đã tạo `origin` mới (`github.com/nhanle1153/front-end-trading-tool-smart-dca`) và push `main` thành công — `git log origin/main` khớp local. `dashboard-ui/` tiếp tục bị `.gitignore` chặn khỏi repo backend (đúng theo quyết định) |
| TD-0007 | Xác nhận `tool-d-dashboard-thiet-ke.html` (651KB, ảnh chụp giao diện Tool A dùng làm tham chiếu) có nên nằm trong repo này không | ✅ | TD-0001 | Đã chuyển sang `docs/tool-d-dashboard-thiet-ke.html`, lý do ghi trong `ARCHITECTURE.md` mục 3, `.dockerignore` đã hết trùng lặp |

### Khối 1 — PHẦN 0d, tầng chống nhiễm phép đo (LÀM TRƯỚC MỌI THỨ)

| Mã | Tên việc | TT | Phụ thuộc | Verify |
|---|---|---|---|---|
| TD-0010 | `measurement/tri_state.py` — `Measured[T]`, ba trạng thái, `render()` không có nhánh "số cũ kèm cảnh báo" | ✅ | TD-0004 | `pytest -k lz41` xanh |
| TD-0011 | `measurement/hashing.py` + `gitinfo.py` — sha256 file (thiếu → `"MISSING"`), git sha + cờ working tree bẩn | ✅ | TD-0005 | file thiếu → trả `"MISSING"`, không nuốt exception |
| TD-0012 | `measurement/provenance.py` — 7+1 khoá, `validate_provenance()`. Chốt luôn định dạng `cache_key(prov)` để D3 không phải sửa ngược | ✅ | TD-0011 | `pytest -k lz40` xanh, **8 ca** (đủ khoá + thiếu từng khoá) |
| TD-0013 | `config/tool_d_config.yaml` chép nguyên §6.9.5 + `config/loader.py`. Áp MT-03: `_budget_remaining_B3: null # derived` | ✅ | TD-0004 | `tunable_param_names()` trả về **đúng 12** |
| TD-0014 | Test **L-Z37** (cấm `*Parameter`, cấm `<Strategy>.json`) và **L-Z39** (cấm env đọc tham số Tầng B/C) | ✅ | TD-0013 | `pytest -k "lz37 or lz39"` xanh |
| TD-0015 | `measurement/guard.py` — `measurement_guard()` | ✅ | TD-0013 | Tạo `user_data/strategies/Fake.json` **hỏng** → exit **86**, stdout in nội dung, và **file vẫn còn nguyên** |
| TD-0016 | 8 khung entrypoint E1–E8, mỗi file gọi guard ở dòng đầu `main()` | ✅ | TD-0015 | `ls entrypoints/*.py` đúng 8 file, không hơn — đã xác nhận (8/8: run_backtest, run_wfo, run_ablation, touch_lockbox, periodic_report, trial_ledger_audit, build_pool, backfill_data); import sạch + `pytest` 65 passed trong Docker |
| TD-0017 | Test **L-Z36** — AST + danh sách đóng | ✅ | TD-0016 | `pytest -k lz36` xanh (14 ca, gồm 5 ca "răng"); thêm `entrypoints/e9_tmp.py` thật vào thư mục → FAIL đúng như spec, đã xoá; `pytest` toàn bộ 82 passed trong Docker |
| TD-0018 | `assert_cache_none()` trong E1 — **từ chối**, không tự chèn | ✅ | TD-0016 | `python entrypoints/run_backtest.py --timerange X` → exit **87** — đã xác nhận trong Docker (service `freqtrade`); `--cache none` rơi xuống `NotImplementedError` (Khối 2/3, chưa phải lỗi); `pytest -k run_backtest_cache` 3 ca xanh |
| TD-0019 | Bộ test grep: **L-Z32** (tên biến đã xoá), **L-Z33** (cấm 15m), **L-Z46** (cấm `profit_ratio` ở tầng đo), **L-Z48c** (cấm nhãn "R" trần) | ✅ | TD-0016 | `pytest -k "lz32 or lz33 or lz46 or lz48c"` xanh (17 ca, gồm 8 ca "răng") trong Docker; toàn bộ suite 99 passed |
| TD-0020 | 🚪 **CỔNG KHỐI 1** — chạy toàn bộ L-Z36→L-Z41 trong Docker | ✅ | TD-0010…0019 | `docker compose run --rm tests tests/lock/test_lz36_entrypoint_guard.py tests/lock/test_lz37_forbidden_parameter.py tests/unit/test_run_backtest_cache.py tests/lock/test_lz39_env_tunable_param.py tests/lock/test_lz40_provenance.py tests/lock/test_lz41_tri_state.py` → 65 passed, 0 failed (lệnh gốc sai cú pháp — xem Lịch sử thay đổi checklist). `pytest` toàn bộ → 99 passed. Gắn tag `d0pre-0d-live` |

### Khối 2 — Đơn vị đo + cấu hình Freqtrade

| Mã | Tên việc | TT | Phụ thuộc | Verify |
|---|---|---|---|---|
| TD-0025 | `docs/decisions/DR-013-don-vi-do.md` — mọi chỉ số trên `pnl_abs`, bảng phân biệt ba chữ "R" | ✅ | TD-0020 | File có bảng ba chữ "R" (R_eff/r_eff_pct, planned_risk_usdt, R_realized) đúng spec dòng 2777-2787; mục "cấm chữ R trần" có ví dụ đúng/sai; bảng test L-Z46/47/48/48b/48c đối chiếu trạng thái D0-PRE |
| TD-0026 | `config/freqtrade/config.json` theo §0c.3 + ánh xạ lệnh chờ §3.5 | ✅ | TD-0020 | `docker compose run --rm freqtrade -m freqtrade show-config -c config/freqtrade/config.json` → exit 0, không lỗi (xác nhận trong Docker). Ghi chú: `protections` bị freqtrade 2026.8 DEPRECATE khỏi config.json (chuyển sang thuộc tính class của strategy, D1+) — TD-0027/L-Z24 phải kiểm ở đúng chỗ mới, xem comment trong file config |
| TD-0027 | Test **L-Z24** (cờ cấm), **L-Z25** (không dấu vết hyperopt), **L-Z42** (config khớp hằng số spec) | ✅ | TD-0026 | `pytest -k "lz24 or lz25 or lz42"` xanh |
| TD-0028 | Đọc mã nguồn Freqtrade **đang cài** cho giả định D2a/D2b/D6/D7 → `docs/freqtrade-source-read.md`. **Làm TRƯỚC khi viết bất kỳ test nào về chúng** | ✅ | TD-0004 | File có 4 mục, mỗi mục trích đường dẫn + số dòng trong image, kèm image digest |

### Khối 3 — 🔴 THỨ TỰ CỨNG 1→2→3, KHÔNG SONG SONG

| Mã | Tên việc | TT | Phụ thuộc | Verify |
|---|---|---|---|---|
| TD-0030 | **(1)** Verify `zone_width` min có phải **code chết** không (§3.3). Phân tích spec, **không chạm dữ liệu**. Kết luận nhị phân → `DR-D0PRE-01`. Giải MT-04 | ✅ | TD-0020 | DR có kết luận CHẾT/SỐNG + lý do; nếu CHẾT thì xoá hẳn khỏi YAML, không để lại comment |
| TD-0031 | **(2)** Xác nhận lại bảng DOF của DR-010 **từng dòng** → `config/dof_inventory.yaml` | ✅ | TD-0030 | `python -m tool_d.config.dof --check` in DOF gốc và các thành phần khớp bảng |
| TD-0032 | **(3)** Chốt `N_ĐĂNG_KÝ` (114 nếu chết / 120 nếu không) → `DR-D0PRE-02` | ✅ | TD-0031 | 🔴 `pytest -k lz29` **PASS trước khi `git commit`** (spec dòng 4424) |
| TD-0033 | `gates/dsr.py` + test **L-Z34** — N đọc từ registry, không phải hằng số | ✅ | TD-0032 | `pytest -k lz34` xanh; `dsr_hurdle(114)` ≠ `dsr_hurdle(228)`, khớp √(2·ln N) sai số 1e-6 |

### Khối 4 — Điền các ô trống bắt buộc

| Mã | Tên việc | TT | Phụ thuộc | Verify |
|---|---|---|---|---|
| TD-0040 | `gates/thresholds.py` fail-closed + test **L-Z35** | ✅ | TD-0020 | `pytest -k lz35` xanh; đưa kết quả cực tốt giả lập qua gate → vẫn **FAIL** |
| TD-0041 | 🔴 **ĐIỀN ngưỡng DSR-adjusted expectancy §10.2 — blocker B6** (OQ-01). Viết DR **trước** khi biết kết quả lần đánh giá tiếp theo | ✅ | TD-0040, TD-0033 | `DR-D0PRE-03` có SỐ; hằng số không còn `inf`; L-Z35 chuyển sang biến thể "best-known vẫn FAIL" và vẫn xanh |
| TD-0042 | 🔴 **ĐIỀN thang drawdown 5/8/20%** (OQ-03). Sau bước này là **Hạng 0, không sửa được** | ✅ | TD-0013 | `DR-D0PRE-04` commit; giá trị trong YAML khớp DR |
| TD-0043 | 🔴 **ĐIỀN `E_D`, `L_exchange`, `rho_pct`, % lỗ tối đa ngày xấu** theo vốn thật (OQ-02) | ✅ | TD-0013 | Commit riêng; `pytest -k lz29` vẫn xanh |

### Khối 5 — Sổ phép thử + audit

| Mã | Tên việc | TT | Phụ thuộc | Verify |
|---|---|---|---|---|
| TD-0050 | Schema JSON cho `trial_registry.jsonl` (**dạng sổ sự kiện**, theo MT-01) và `idea_queue.jsonl`; tạo hai file rỗng | ✅ | TD-0012 | `jsonschema` validate fixture mẫu cho cả 5 loại sự kiện |
| TD-0051 | `ledger/registry.py` + `budget.py` — reserve / seal / consume / refund, công thức Khả dụng, bản chiếu trạng thái. Áp MT-03 | ✅ | TD-0050 | Chuỗi reserve→seal→consume: `available()` đúng ở từng bước |
| TD-0052 | Test **L-Z52** — hết ngân sách thì từ chối, chưa chạm dữ liệu | ✅ | TD-0051 | `pytest -k lz52` xanh; spy khẳng định **0 lần** đọc `user_data/data` |
| TD-0053 | Test **L-Z53** — giết tiến trình sau khi có kết quả fold đầu → CONSUMED, hoàn trả phải RAISE | ✅ | TD-0051 | `docker compose run --rm tests -k lz53` xanh |
| TD-0054 | Test **L-Z54** — 4 dòng bảng DR-014 + trần trả lại 3 lần | ✅ | TD-0051 | `pytest -k lz54` — 5 ca xanh |
| TD-0055 | `assert_dataset_timerange()` + test **L-Z55** — bộ chạy tự kiểm, không nhận lời khai | ✅ | TD-0051 | `pytest -k lz55` xanh |
| TD-0056 | E6 `trial_ledger_audit.py` **đầy đủ** (H16) + test L-Z10/11/12/15/16/17 | ✅ | TD-0051 | Sổ rỗng → exit 0, in `"đã audit N/M (X đạt, Y chưa đạt, Z chưa đo được)"`; sổ bẩn → exit≠0 |
| TD-0057 | Nối E6 vào **đầu** E1/E2/E3 (chạy trước mỗi lần backtest) | ✅ | TD-0056 | Test AST: `run_audit` được gọi trong `main()` của E1–E3 |

### Khối 6 — Báo cáo định kỳ

| Mã | Tên việc | TT | Phụ thuộc | Verify |
|---|---|---|---|---|
| TD-0060 | E5 `periodic_report.py` **đầy đủ** (§12d.2), mọi chỉ số ở trạng thái `pending` | ✅ | TD-0010, TD-0051 | `python entrypoints/periodic_report.py` in khối xuất xứ ở đầu + bảng toàn "chưa đo được" — xác nhận (commit `03bfeef`): khối xuất xứ Ở ĐẦU, 21 chỉ số §12d.2 BƯỚC 1 toàn "chưa đo được", dòng tổng `audit_line()`. `pytest -k "report_model or periodic_report"` → 12 passed trong Docker; toàn bộ suite → 264 passed |
| TD-0061 | Test **L-Z41** trên đầu ra **thật** của E5 | ✅ | TD-0060 | Grep đầu ra: không có `-1`, `UNKNOWN`, `0.0` giả — xác nhận (commit `a38fca7`, `tests/lock/test_lz41_periodic_report.py`): chạy E5 thật như tiến trình con, `scan_for_sentinels()` sạch + literal check độc lập. `pytest -k lz41` → 27 passed; toàn bộ suite → 272 passed trong Docker |
| TD-0062 | Đóng băng nội dung báo cáo (đổi = tiêu 1 trial, spec dòng 4810) | ✅ | TD-0060 | Test hồi quy giữ hash danh sách chỉ số — xác nhận (commit `6c4d3fc`): `content_fingerprint()` + `FROZEN_CONTENT_HASH` trong `tests/unit/test_report_content_frozen.py`, nhạy với cả thứ tự lẫn nhãn. Toàn bộ suite → 276 passed trong Docker |

### Khối 7 — Lockbox

| Mã | Tên việc | TT | Phụ thuộc | Verify |
|---|---|---|---|---|
| TD-0070 | `lockbox/seal.py` + `access_log.py` + test **L-Z13 / L-Z14** | ✅ | TD-0012 | Sổ truy cập rỗng → PASS (`validate_before_d9([])==[]`); thêm 1 bản ghi trước D9 → FAIL; sửa 1 byte dữ liệu → `verify_seal` FAIL. `docker compose run --rm tests -k "lz13 or lz14"` → 25 passed (xác nhận trong Docker) |
| TD-0071 | E4 `touch_lockbox.py` chế độ `--verify-seal` (H17); chế độ chạm thật vẫn TỪ CHỐI tới sau D9 | ✅ | TD-0070 | Chạy được, **0 bản ghi** được thêm vào sổ truy cập — xác nhận: `--verify-seal` PASS (0 seal ở D0-PRE) và chế độ mặc định TỪ CHỐI, cả hai đều không tạo `lockbox_access.log`. `pytest -k "lz14 or touch_lockbox"` → 34 passed trong Docker |
| TD-0072 | Nối H17 vào đầu pipeline E1/E2/E3 | ✅ | TD-0071 | Test AST: `verify_seal` được gọi trong `main()` |

### Khối 8 — Việc chạm dữ liệu (CHỈ SAU khi Khối 1–7 xanh)

> Ranh giới theo MT-02: "chạm" = đánh giá cấu hình trên CALIB/WFO/LOCKBOX.
> Đo thông tin mô tả là dòng `CTRL`, 0 trial, bắt buộc có assert khoảng thời gian.

| Mã | Tên việc | TT | Phụ thuộc | Verify |
|---|---|---|---|---|
| TD-0079 | Hoàn thành Mục 2–4 của `api-integration-rules.md` (R1–R12) + `provider-map.md` cho Binance. **Bắt buộc trước dòng code gọi mạng đầu tiên** | ✅ | TD-0020 | Bản điền ở gốc repo, Mục 4 đủ 4 bảng (dịch vụ, endpoint, mã lỗi, ngưỡng) |
| TD-0080 | E8 chế độ `--probe-coverage`: verify độ dài lịch sử **Open Interest** Binance thật sự trả về | ✅ | TD-0079 | In số ngày thật; nếu chỉ ~30 ngày → ghi research-log, ảnh hưởng thiết kế chỉ báo |
| TD-0081 | Ước lượng **số lệnh/năm** bằng tính tay. **< 150 → hỏng từ thiết kế, xử lý ngay** | ✅ | TD-0083 | `docs/estimate-trades-per-year.md` có phép tính và kết luận nhị phân |
| TD-0082 | Kiểm **min notional** từng cặp vs notional tranche 1 nhỏ nhất | ✅ | TD-0043, TD-0079 | Bảng đối chiếu; có vi phạm → nâng `E_D` hoặc đặt sàn, ghi DR |
| TD-0083 | Chốt pool ~100 mã, loại BTC/ETH khỏi giao dịch, tách tập EXPLORE (OQ-04) | ✅ | TD-0079, TD-0056 | E7 chạy, ghi **4 trial B0** vào sổ; `pytest -k lz11` vẫn xanh |
| TD-0084 | Chia CALIB / WFO / LOCKBOX, kiểm 4 điều kiện DR-011, niêm phong `lockbox_seal_1.json` (OQ-05) | ✅ | TD-0083, TD-0070 | `touch_lockbox.py --verify-seal` PASS; sổ truy cập vẫn **0 bản ghi** |
| TD-0085 | Thiết lập **backup lockbox ngoài git + test khôi phục thật một lần** (OQ-08) | ✅ | TD-0084 | Khôi phục từ backup vào thư mục tạm → `verify_seal` PASS trên bản khôi phục |
| TD-0086 | 🚪 **GATE D0-PRE** — ghi `runtime_state.json.d0_pre_complete` từ **một lần chạy thật** | ✅ | TD-0020…TD-0085 | `docker compose run --rm tests tests/lock` 0 failed **và** `trial_ledger_audit.py` exit 0 **và** `periodic_report.py` sạch → `git tag d0-pre-complete` |

---

### Khối 9 — D1: cổng D0-PRE có hiệu lực + hạ tầng dữ liệu an toàn (H19)

> D1 = "H20 (§0d) TRƯỚC, rồi H1-D + H4-D + H13 + H19" (spec dòng 4468). **H20 đã xong ở D0-PRE**
> (Khối 1). Khối 9 làm hai việc còn thiếu để chạm dữ liệu thật một cách an toàn.
> 🔴 H19 **chặn lần backfill đầu tiên** (spec dòng 4350) — Tool A suýt xoá nhiều năm dữ liệu vì bẫy
> "ghi đè theo khoảng ngày yêu cầu". Backfill lockbox ở TD-0084 không dính bẫy này vì ghi vào thư
> mục RỖNG, nhưng CALIB/WFO thì có dữ liệu chồng lấn — bắt buộc qua H19.

| Mã | Tên việc | TT | Phụ thuộc | Verify |
|---|---|---|---|---|
| TD-0090 | Nối `is_d0_pre_complete()` vào đầu **E1/E2/E3/E7/E8** — từ chối chạy khi cổng chưa đóng (§N2) | ✅ | TD-0086 | Test AST: 5 entrypoint gọi `is_d0_pre_complete` trong `main()`; giả lập `runtime_state.json` thiếu khoá → exit 90 |
| TD-0091 | 🔴 **H19** — E8 backfill an toàn: (a) sao lưu trước, (b) **GỘP** không ghi đè, (c) verify phần cũ **byte-for-byte**, (d) tải hỏng → `unreadable`, KHÔNG cache rỗng | ✅ | TD-0090 | Test: backfill chồng lên dữ liệu cũ → hash phần cũ KHÔNG đổi; mô phỏng tải hỏng → không sinh file rỗng |
| TD-0092 | 🔴 **H19** — chỉ số **ĐỘ PHỦ DỮ LIỆU** riêng; cấm suy nguyên nhân gốc từ khoảng trống mà chưa kiểm nguồn | ✅ | TD-0091 | E8 in bảng độ phủ theo mã × khung; khoảng trống hiện "chưa kiểm nguồn", không kết luận thay người |
| TD-0093 | Backfill THẬT **CALIB [T0,T1]** + **WFO [T1,T2]** cho 102 mã qua E8 đã an toàn (DR-D0PRE-07) | ✅ | TD-0091, TD-0092 | `touch_lockbox.py --verify-seal` vẫn PASS (lockbox không bị đụng); bảng độ phủ CALIB/WFO ghi vào research-log |
| TD-0094 | 🔴 Phát hiện ở TD-0093: `freqtrade download-data --timerange` **không tôn trọng mốc kết thúc** (tải lố tới gần T3, lấn phạm vi LOCKBOX vào thư mục làm việc). File trên đĩa KHÔNG đáng tin về phạm vi — cơ chế tự kiểm đúng (`assert_dataset_timerange`, L-Z55, DR-014 §2) đã có sẵn nhưng chưa nối được với cấu hình thật. Nối `tier_c.data_split` (T0-T3, DR-D0PRE-07) với L-Z55 qua `dataset_boundaries_from_config()` | ✅ | TD-0093 | `pytest -k timerange` → 10 passed (gồm tái hiện đúng kịch bản TD-0093: khai WFO, dữ liệu lấn qua LOCKBOX → `assert_dataset_timerange` raise). **Phạm vi:** E1/E2/E3 CHƯA có logic tải dữ liệu thật (D1 chỉ dựng khung guard, NotImplementedError) nên CHƯA có chỗ để gọi self-check sau khi tải — việc nối `assert_dataset_timerange()` vào đúng điểm sau bước tải dữ liệu là phần việc của bất kỳ task nào viết logic backtest/WFO/ablation thật sau này, không phải của TD-0094 |

---

### Khối 10 — D1: H1-D pairlist point-in-time (🔴 ĐƯỜNG GĂNG)

> Spec dòng 4338 + 4354: H1-D là hạng mục **tốn thời gian nhất** của v10 gốc, phải **viết lại từ đầu**,
> không dùng `lib/pool-builder`. Mỗi lần đổi pool = backfill lại toàn bộ và **mọi số cũ không so sánh được**.

| Mã | Tên việc | TT | Phụ thuộc | Verify |
|---|---|---|---|---|
| TD-0095 | Khảo sát **nguồn danh sách lịch sử** (symbol đã huỷ niêm yết — API Binance KHÔNG trả). Không có → DR ghi tường minh *"đang chấp nhận survivorship bias, ước lượng hướng lệch: có lợi"*, KHÔNG im lặng | ✅ | TD-0093 | Kết luận + bằng chứng đo trong `docs/research-log.md`; nếu chấp nhận bias → có DR riêng |
| TD-0096 | `pairlist_point_in_time(t)` — pool hợp lệ **tại thời điểm t** theo đúng 4 tiêu chí §0.3, tính point-in-time (tuổi ≥180 ngày, volume ≥15tr USDT — DR-D0PRE-05) | ✅ | TD-0095 | Test: tại t lùi 1 năm, mã niêm yết sau t KHÔNG có mặt; kết quả KHÔNG đổi khi thêm dữ liệu sau t |
| TD-0097 | Verify pool point-in-time **không lệch theo thời gian** + ranh giới tập **EXPLORE không rò** (§9c.4b) | ✅ | TD-0096 | Test: mã EXPLORE (gồm BTC/ETH) không lọt vào pool giao dịch tại BẤT KỲ t nào |

---

### Khối 11 — D1: Zone detection + H4-D (🔴 P0 TUYỆT ĐỐI, trước mọi logic chiến lược khác)

> Spec dòng 2432: *"H4-D — Zone confirmation delay test · P0 · NGÀY 1, TRƯỚC MỌI THỨ KHÁC"*.
> Đây là **blocker B2**. Lookahead trong zone detection làm **mọi kết quả D0.9 bị bơm lên có hệ thống**.
> 🔴 Không sửa code sản xuất để chiều artifact của `lookahead-analysis` (spec dòng 2455).

| Mã | Tên việc | TT | Phụ thuộc | Verify |
|---|---|---|---|---|
| TD-0100 | Swing detection §1.1 (k=3, xác nhận 2 phía) — hàm THUẦN, không đọc dữ liệu sau `t` | ✅ | TD-0090 | `la_diem_swing()` trong `src/tool_d/zone_detection.py` — xác nhận (commit `f14c847`): 8 test trong `tests/unit/test_zone_detection.py` xanh trong Docker (swing đáy/đỉnh đúng/sai, chưa đủ nến sau → chưa xác nhận thay vì "không phải swing", dữ liệu tương lai ngoài cửa sổ không đổi kết quả). `docker compose run --rm tests` toàn bộ suite → 422 passed |
| TD-0101 | `confirm_ratio(i, t)` §7.4 — liên tục 0/0.33/0.67/1.0 + điều kiện **HUỶ** khi giá tạo cực trị vượt qua `i` | ✅ | TD-0100 | `confirm_ratio()` + `zone_da_bi_huy()` trong `src/tool_d/zone_detection.py` — xác nhận (commit `58fd5ca`): 16 test (`test_zone_detection.py`) xanh trong Docker — 4 mốc 0/0.33/0.67/1.0, giữ 1.0 sau `i+k`, huỷ vĩnh viễn không tự phục hồi, dữ liệu sau `t` không đổi kết quả. Toàn bộ suite → 430 passed |
| TD-0102 | 🔴 **H4-D** (§7.2) — `assert confirmed_at_bar − swing_bar == 3`; mọi tranche fill có timestamp ≥ `confirmed_at_bar` | ✅ | TD-0101, TD-0093 | `tests/lock/test_lz1_confirmed_at_bar.py` — xác nhận (commit `8a33239`): 3 test xanh trong Docker, dùng fixture feather-shape TỰ TẠO (không đọc thẳng `user_data/data/` — bị `.gitignore` chặn, không phải bằng chứng Docker portable được), gồm 1 ca CỐ Ý có khoảng trống kiểu CALIB thật (TD-0092) — khẳng định thời gian trôi qua PHẢI ≥ k nến, không bao giờ ít hơn. Toàn bộ suite → 433 passed. **Phạm vi:** chỉ phần bất biến `confirmed_at_bar − swing_bar`; phần "tranche fill có timestamp ≥ confirmed_at_bar" CHƯA kiểm được — D1 chưa có code đặt lệnh, để dành cho task đó |
| TD-0103 | 🔴 **H4-D-b** (§7.5) — cắt dữ liệu tại `t`, tính lại, `confirm_ratio(i,t)` KHÔNG đổi | ✅ | TD-0101 | Mở rộng `tests/lock/test_lz1_confirmed_at_bar.py` (spec dòng 2510: H4-D-b là phần mở rộng của L-Z1, không phải mã riêng) — xác nhận (commit `8e18aad`): 15 test xanh trong Docker — chữ ký `confirm_ratio()` không có tham số mảng (không thể đọc dữ liệu sau t dù muốn), `confirm_ratio`/`zone_da_bi_huy` parametrize nhiều cặp (i,t) mẫu, so mảng đầy đủ với mảng bị cắt đúng tại t → bằng nhau tuyệt đối. Toàn bộ suite → 458 passed |
| TD-0104 | **ZSS** §1.2 — 3 thành phần (`touch_count` theo định nghĩa số LD-35, `volume_ratio`, `compression`) + ngưỡng nhận zone §1.3 | ✅ | TD-0102, TD-0103 | `src/tool_d/zone_strength.py` — xác nhận (commit `f4dcfa3`): 22 test trong `tests/unit/test_zone_strength.py` xanh trong Docker (touch_count cụm/point-in-time/không tính nến hình thành, volume_ratio MA20, compression qua TA-Lib ATR, `zss()` đúng công thức + kẹp trần/sàn, `zone_hop_le()` cả 3 điều kiện §1.3). Toàn bộ suite → 480 passed. **Phạm vi:** "close < zone_low → zone bị phá (DG1)" chỉ thi hành ở mức "không tính touch" — gate DG1 thật (§4) chưa có code đối tượng để nối vào ở D1 |
| TD-0105 | 🔴 **H13** — audit thành phần ZSS: KHÔNG dùng dữ liệu ngoài `confirmed_at_bar` | ✅ | TD-0104 | `tests/lock/test_td0105_zss_confirmed_at_bar_audit.py` — xác nhận (commit `05c1b10`): tính `touch_count`+`volume_ratio`+`compression`+`zss()` tại `confirmed_at_bar` trên chuỗi dài, cắt mảng đúng tại đó, tính lại — cả 4 giá trị giống hệt tuyệt đối. Không có mã `L-Z` cho H13 (spec chỉ đặt tên), dùng tên tự đặt `test_td0105_...`. Toàn bộ suite → 481 passed |
| TD-0106 | Chạy `lookahead-analysis` của Freqtrade trên zone detection; **mỗi** cờ phải quy về FP-1/FP-2/FP-3 **có bằng chứng** hoặc điều tra tới gốc | ✅ | TD-0104 | Xác nhận (commit `64ca450`, chi tiết `docs/research-log.md` 07/09/2026): `ZoneDetectionProbe.py` chạy `freqtrade lookahead-analysis` thật qua Docker trên dữ liệu CALIB thật, 2 mã, `has_bias=No` cả hai — 0 cờ nào được nêu ra nên không có gì để quy về FP-1/2/3 (kết quả sạch, không phải "bỏ qua"). Khớp đánh giá độc lập đọc-code cùng ngày. Giới hạn ghi rõ: mới 2/102 mã, tín hiệu thưa — nên chạy lại rộng hơn khi D2 cần số đáng tin hơn |
| TD-0107 | 🐛 Phát hiện qua review độc lập (không phải lookahead): `touch_count()` (`src/tool_d/zone_strength.py:56-61`) — khi `dang_trong_cum=True`, vòng lặp chỉ kiểm `bat_ra`, KHÔNG kiểm `vo_huong_nguoc` — một cú vỡ mạnh xuyên `zone_low`/`zone_high` giữa lúc đang chờ bật ra bị bỏ qua, sau đó bật ra hợp lệ vẫn được tính là 1 touch. Tái hiện: zone day [100,102], giá `[101, 101, 90, 103]` (chạm→vỡ sâu dưới 90→bật lên 103) → `touch_count()` trả 1, không phải 0 | ✅ | TD-0104 | Xác nhận (commit `e424d63`): thêm nhánh `elif vo_huong_nguoc` khi `dang_trong_cum=True` → huỷ cụm thay vì bỏ qua. Test tái hiện đúng kịch bản báo cáo, từ 1 (sai) → 0 (đúng); 23 test `test_zone_strength.py` xanh, không phá test cũ nào. Toàn bộ suite → 482 passed |

---

### Khối 12 — D1: cổng

| Mã | Tên việc | TT | Phụ thuộc | Verify |
|---|---|---|---|---|
| TD-0110 | 🚪 **GATE D1** — H1-D + H4-D + H4-D-b + H13 + H19 đều PASS → **gỡ blocker B2** | ✅ | TD-0093…TD-0106 | Toàn bộ test khoá D1 xanh trong Docker; ghi `runtime_state.json.d1_complete` từ một lần chạy thật; `git tag d1-complete`. 🔴 **Khối `evidence` phải gắn nhãn nguồn `{nguon: do-duoc\|nguoi-khai, noi_dung: …}` theo MT-10** — `do-duoc` chỉ cho chuỗi do code sinh trong chính lần chạy đó |

---

### Khối 13 — D2: Verify giả định D1–D7 (§9b.2) + H15

> 🔁 **TỪ ĐIỂN ĐỔI TÊN (07/09/2026) — đọc trước khi tra commit cũ.**
> Hai mã từng bị dùng HAI LẦN (vi phạm quy tắc 1 của file này). Cặp khối D2 đã được
> đánh số lại; **cặp Idea Queue giữ nguyên** vì mã của nó đã ăn vào tên file test, tên
> hàm `check_td0119*`/`check_td0120*`, và chuỗi `TD-0119a`/`TD-0119b`/`TD-0120` mà **E6
> in ra trong đầu ra thật** — đổi chúng là đổi luôn bằng chứng đã ghi ở đây và trong
> `DR-Q3-2026-tieu-chi-chon-y-tuong.md`.
>
> | Trong commit/docstring TRƯỚC 07/09/2026 | Nếu nói về… | Mã ĐÚNG bây giờ |
> |---|---|---|
> | `TD-0119` | Context Trend Filter (`trend_context.py`) | **TD-0128** |
> | `TD-0120` | Xác nhận entry §3.3b (`entry_confirmation.py`) | **TD-0129** |
> | `TD-0119` | Tờ đơn ý tưởng hai cửa (MT-12) | `TD-0119` — **không đổi** |
> | `TD-0120` | Niêm phong tiêu chí chọn (OQ-07) | `TD-0120` — **không đổi** |
>
> Lịch sử git **không viết lại được**, nên bảng này là cách duy nhất tra ngược đúng.

> Spec dòng 4469-4470: *"D2 Verify D1-D7 (v7: +D6, +D7; D2 tách a/b/c) + H15 · 🔴 L-Z49 (D7) PASS là
> điều kiện vào D4"*. TD-0028 (D0-PRE) đã đọc source cho **D2a, D2b, D6, D7** (kết luận: D2a ĐÚNG,
> D2b KHÔNG hỗ trợ `closePosition`, D6 ĐÚNG dùng giá mở nến, D7 cơ chế an toàn + cảnh báo `trade.id`)
> — xem `docs/freqtrade-source-read.md`. **D1, D3, D4, D5 CHƯA đọc** (file đó tự ghi rõ "sẽ đọc khi
> tới D2"). D2c/D4 (Testnet/Live-only, spec bảng §9b.2) **KHÔNG thuộc phạm vi D2** — hoãn tới D3.5/
> D9.5+, không chặn cổng D2.
>
> 🔴 TD-0114/TD-0115 là lần ĐẦU TIÊN dự án cần một `IStrategy` thật chạy qua Freqtrade backtesting
> (không còn là hàm thuần `src/tool_d/`) — dùng `zone_detection.py`/`zone_strength.py` đã có ở D1.

| Mã | Tên việc | TT | Phụ thuộc | Verify |
|---|---|---|---|---|
| TD-0111 | **D1** — đọc source `adjust_trade_position()` mô phỏng fill limit-maker trong backtest futures (kể cả ca KHÔNG khớp) → nối vào `docs/freqtrade-source-read.md` | ✅ | TD-0110 | Trích đường dẫn + số dòng thật trong image (LD-38/40); kết luận nhị phân ĐÚNG/SAI mô tả spec |
| TD-0112 | **D3** — đọc source cách Freqtrade tính giá vào trung bình khi nhiều lần entry + xác nhận `custom_stoploss` đọc được đúng giá đó | ✅ | TD-0110 | `docs/freqtrade-source-read.md` có mục D3; test đơn vị dựng backtest nhỏ đa-entry xác nhận giá trung bình đúng công thức |
| TD-0113 | **D5** — đọc source hành vi `timeframe-detail 5m`: thứ tự khớp khi nhiều mức giá (p1,p2,p3,SL,TP) cùng nằm trong một nến 1H | ✅ | TD-0110 | Xác nhận (commit `58547ab`): `docs/freqtrade-source-read.md` mục 7 (đường dẫn + số dòng thật) + `docs/research-log.md` 07/09/2026. Backtest thật 2 lần CÙNG một nến 1H tổng hợp, chỉ đổi `--timeframe-detail`: không có → `stop_loss` (-10,18%); có `5m` → `roi` (+1%) — đảo hoàn toàn kết quả, xác nhận đúng thứ tự dòng 5m thật. **Giới hạn residual ghi rõ:** trong CÙNG một nến 5m đơn lẻ, Freqtrade vẫn dùng policy cố định Stoploss-trước-ROI, không phải chronology thật — 5m thu hẹp cửa sổ mơ hồ, không triệt tiêu hoàn toàn |
| TD-0128 | **Phần 2 — Context Trend Filter**: `trend_dir`/`trend_dir_4h` (EMA20/50 + slope, §2.1), xác nhận đa khung (§2.2, L-Z8), tuổi trend ≥5 nến (§2.3), điều kiện vào lệnh §2.5 | ✅ | TD-0104 | `src/tool_d/trend_context.py` — xác nhận (commit `0ae1a82`): 16 test trong `tests/unit/test_trend_context.py` xanh trong Docker (`trend_dir_tai` UP/DOWN/FLAT + fail-closed khi thiếu dữ liệu/NaN, `xac_nhan_da_khung` đồng thuận, `tuoi_trend_nen` tìm điểm cross hoặc `None`, `du_dieu_kien_vao_lenh` cả 4 điều kiện §2.5). Toàn bộ suite → 532 passed. **Phạm vi:** L-Z8 "tại mọi bản ghi plan" cần plan thật (§8) — chưa có code đặt lệnh để sinh plan; nối khi TD-0114 dựng chiến lược |
| TD-0129 | **§3.3b — Xác nhận entry bằng price-action**: rejection wick (hình học nến) HOẶC RSI(14,1H) divergence dùng lại `touch_count` (TD-0104) — tối đa 3 nến chờ, không nới hạn (L-Z6) | ✅ | TD-0104, TD-0128 | `src/tool_d/entry_confirmation.py` — xác nhận (commit `eb1619c`): 12 test `tests/lock/test_lz6_*` xanh trong Docker (rejection đáy/đỉnh, phân kỳ RSI, quét đúng tối đa 3 nến — KHÔNG mở rộng cửa sổ tìm kiếm). Toàn bộ suite → 544 passed. **Phạm vi:** cần bản ghi tranche fill thật (§3.5) để kiểm "mọi tranche 1 đều có entry_confirmation" đầy đủ — nối khi TD-0114 dựng chiến lược |
| TD-0121 | **DG8 — Time Stop**: `bars_since_tranche1 ≥ 24 nến 4H` → đóng MARKET vô điều kiện (kể cả đang lãi/ZSS cao/trend đúng), tag `TIME_STOP` (§4b) | ✅ | TD-0104 | `tests/lock/test_lz18_*`/`test_lz19_*` — không bản ghi nào có `hold_duration_bars > max_hold_bars`; `hold_duration_bars` ghi ở MỌI lệnh đóng kể cả TP/SL |
| TD-0122 | **DG7 — Funding Stop**: `funding_paid_cumulative = -trade.funding_fees` (đọc thẳng field native Freqtrade, KHÔNG tự xây pipeline tích luỹ — LD-09) ≥ 0.3×R_eff → đóng MARKET, tag `FUNDING_STOP` (§4c.2) | ✅ | TD-0112 | 🔴 **L-Z30 CRITICAL** — không lệnh đã đóng nào có `funding_paid_cumulative > 0.35×R_eff_plan` (biên 0.05 cho độ trễ mốc funding 8h); **L-Z31** — `funding_paid_cumulative` ghi ở MỌI lệnh đã đóng, mọi arm; **L-Z43** — LONG giả lập qua 3 mốc funding dương phải cho `funding_paid_cumulative > 0`, đọc đúng cột `open` (không phải `close`) |
| TD-0123 | **DG6 — Early Invalidation**: điều kiện A (ATR ratio ≥1.8 + `compression` đảo dấu, TD-0104) / B (≥8 nến 1H chưa hồi qua p1) / C (`price_decay_ratio` ≥0.7 + `trend_dir` đảo, TD-0128) / D (short squeeze, chỉ Short) → đóng MARKET (§4.1) | ✅ | TD-0128 | `src/tool_d/dg6_early_invalidation.py` — xác nhận (commit `e6d2f60`): 22 test trong `tests/unit/test_dg6_early_invalidation.py` xanh trong Docker (A/B/C/D độc lập + `dg6_dong_vi_the()` OR tổ hợp; D luôn `False` khi `huong="long"`). Điều kiện A tái dùng `zone_strength.compression()` với mốc tham chiếu là tranche 1 thay vì lúc hình thành zone — không viết lại phép tính ATR. Toàn bộ suite → 581 passed |
| TD-0114 | 🔴 **L-Z49 CRITICAL, D7** — dựng `IStrategy` tối thiểu THẬT (1 pair, 1 zone, 3 tranche) dùng `zone_detection`/`zone_strength` đã có; chạy backtest thật; xác nhận `custom_data` ghi ở tranche 1 đọc lại NGUYÊN VẸN ở callback tranche 2/3 + DG6/7/8 + `custom_exit`. FAIL → chặn D4 | ✅ | TD-0111, TD-0112, TD-0113, TD-0128, TD-0129, TD-0121, TD-0122, TD-0123 | `user_data/strategies/ZoneAbsorptionMinimal.py` — xác nhận (commit `7c7f1f3`/`468fe51`, chi tiết `docs/research-log.md` 07/09/2026): backtest thật 2 mã (CALIB, 2024-2025), 34 trade, 27 nhiều tranche. **🐛 Lần chạy đầu bắt được bug thật: 2/25 trade custom_data lệch** (khởi tạo từ dataframe đã merge `ffill` — không đáng tin khi lệnh post-only khớp trễ sang cửa sổ 4H kế tiếp). **Sửa: nguồn khởi tạo đổi sang `trade.enter_tag`** (Freqtrade chốt cứng lúc tạo lệnh) — chạy lại: **0/27 mismatch**. 5 test round-trip mã hoá tag. Suite → 587 passed. **Phạm vi:** LONG only, chưa nối Phần 2/§3.3b vào tín hiệu vào lệnh, chưa có DG1-5 — ghi rõ trong docstring module |
| TD-0115 | 🔴 **L-Z50, D6** — đo lệch khớp tranche: mọi tranche fill trong backtest thật (TD-0114) có `fill_price` so với giá `timeframe_detail` 5m cho thấy đã CHẠM `p_i`; ghi phân bố lệch, tách tranche 1/2/3, tách Long/Short | ✅ | TD-0114 | Xác nhận (chi tiết `docs/research-log.md` 07/09/2026): tải mới dữ liệu 5m thật (chưa từng có trong pool), backtest thật `--timeframe-detail 5m --cache none` → 91 lượt khớp tranche. Lệch nhỏ (T1 mean −0,32%/max 4,12%; T2 mean −0,40%/max 2,37%; T3 mean −0,12%/max 0,92%), 91/91 LUÔN có lợi/trung tính cho LONG — **ghi nhận D6 CHƯA giảm nhẹ bởi H5** theo đúng yêu cầu, không tự ý coi như đạt. Đối chiếu 5m thật: 62% khớp đúng nến, 29% khớp nến liền trước (độ trễ xử lý 1 nến, không phải lỗi), 10% không chạm nhưng fill luôn RẺ HƠN kế hoạch (cơ chế lệnh giới hạn khi giá gap qua mức trước khi lệnh vào sổ). **Phát hiện giữa chừng đã loại trừ:** vòng đo đầu (không `--cache none`) cho 1 ca tưởng lệch >1 năm/2,3 lần giá — xác nhận là lỗi script phân tích tự viết (gán sai `p_i` theo thứ tự lệnh khi nhiều tranche/trade khớp gần nhau), không phải lỗi `ZoneAbsorptionMinimal.py`; không tái hiện khi chạy `--cache none`. Short: N/A (LONG only) |
| TD-0116 | **H15** — network/auth latency probe `POST /fapi/v1/order/test` với API key THẬT (§6.7 điều kiện bảo mật) — 🟡 P1, không chặn cổng D2 | ✅ | — | Xác nhận (commit `63cc7f7` + `99d9c3f`): đo thật trong Docker trên `fapi.binance.com` production, key THẬT của tài khoản phụ `smartdca_virtual`, n=30/lượt. **ẤM: public 136,4ms · ký `/order/test` 138,3ms → chi phí auth+validate = +1,9ms.** Đo tách LẠNH/ẤM (bản đầu dùng `urllib` mở kết nối mới mỗi mẫu → thổi phồng 3-6×, tự bắt được trước khi lấy số). Testnet bị LOẠI bằng bằng chứng source (`binance.py:52 supports_demo_trading: False`, cố ý). Ngày tạo key 07/09/2026 đã ghi (§6.7). 🔴 **Rủi ro tồn dư chấp nhận có ý thức, phải siết trước D11/D12:** IP chưa whitelist (IP nhà mạng động) + Universal Transfer đang bật. 🔴 **Phát hiện ngoài phạm vi:** latency LẠNH bất ổn (14/30 mẫu >1s, max 11 giây) → cửa sổ không-SL của D2c có thể vượt 15 giây |
| TD-0117 | 🚪 **GATE D2** — D1/D3/D5/D6/D7 (phần verify được ở backtest) PASS, L-Z49/L-Z50/L-Z51 xanh → điều kiện vào D3 | ✅ | TD-0111…TD-0116 | Toàn bộ test khoá D2 xanh trong Docker; ghi `runtime_state.json.d2_complete` từ một lần chạy thật (bằng chứng `do-duoc`, MT-10); `git tag d2-complete`. Ghi rõ D2b/D2c/D4 (Testnet/Live-only) hoãn tới D3.5/D9.5+, không phải "đã qua" \| \| **Xác nhận** (commit `8d3c7a8` máy đóng cổng → `9b3e693` đóng cổng thật → tag `d2-complete`): chạy THẬT trong Docker `E6 --close-d2-gate`, exit 0; ba mục evidence đều `do-duoc` sinh từ chính lần chạy đó (hàm tự gọi pytest — MT-10): `full_suite` **720 passed**, `lz49_lz50` **5 passed**, audit sổ trial **4/12 đạt, 0 chưa đạt**. `d2_git_sha=6613ce9` là HEAD lúc chạy (gồm TD-0126 của phiên song song) — ghi đúng cây đã kiểm, không ghi commit của riêng mình. 🔴 **Chốt riêng của cổng D2 mà cổng D1 không có:** chạy RIÊNG `tests/lock/test_lz49_lz50_backtest_nho.py` và đòi số ca PASS ≥ 1 — suite tổng xanh KHÔNG chứng minh file đó còn tồn tại (xoá đi suite vẫn xanh), đúng bẫy PASS RỖNG của TD-0084. 12 test khoá `tests/unit/test_close_d2_gate.py`; suite 687 → 699 = đúng +12; kiểm có răng bằng cách vô hiệu hoá chốt `so_ca_lz < 1` → đúng 1 ca đỏ. Bất biến: chạy lại → exit **94**, không ghi lại. **D2b/D2c/D4 ghi HOÃN tới D3.5/D9.5+ trong `d2_hoan_lai` (nhãn `nguoi-khai`), KHÔNG phải "đã qua"** — D2c còn vướng latency LẠNH bất ổn đo được ở TD-0116 (max 11 giây). |
| TD-0118 | **Idea Queue** — nới `L-Z17` (trần Ngân sách A 5/quý) thành **cảnh báo, KHÔNG chặn chạy** (quyết định chủ dự án 07/09/2026); giữ nguyên con số 5 và giữ `L-Z16` chặn cứng; ghi **MT-11** vào `back-end-note.md` | ✅ | TD-0086 | `run_audit()` trả exit **0** khi sổ có 6 dòng SELECTED cùng quý (vẫn in dòng cảnh báo), và trả exit **≠ 0** khi có dòng `data_source=TOOL_D_RESULTS` mà `status != REJECTED`; test khoá mới `tests/lock/test_td0118_*`; suite Docker tăng đúng số test mới, không giảm. **Xác nhận** (commit `012a9ef` + `277b9e9`): `WARN_ONLY_CODES = {"L-Z17"}` trong `audit_checks.py`, `run_audit()` loại nó khỏi exit code nhưng vẫn in ⚠️ VƯỢT TRẦN; 5 test khoá `test_td0118_*` xanh trong Docker, gồm test canh riêng để `L-Z16` KHÔNG bị nới lây; toàn bộ suite **492 → 497 passed**; chạy thật E6 trên sổ thật → `đã audit 4/6 (4 đạt, 0 chưa đạt, 2 chưa đo được)`, exit 0. MT-11 + OQ-07 + dòng Lịch sử đã ghi `back-end-note.md` |
| TD-0119 | **Tờ đơn ý tưởng — hai cửa** (chủ dự án chốt 07/09/2026, **MT-12**): cửa NỘP thêm `phep_thu_du_kien`; cửa CHỌN bắt buộc `tin_hieu`/`quy_tac`/`nguong_bac_bo`/`so_bien_the` — thiếu thì **từ chối** (fail-closed, cùng khuôn `selection_reason`). Nối `so_bien_the` vào sổ trial: trial sinh từ ý tưởng ghi `hypothesis_slot = IQ-xxxx`, audit đếm CONSUMED ≤ số đã khai | ✅ | TD-0118 | Schema `idea_queue_entry.schema.json` có đủ 5 trường mới; phép kiểm mới trong `audit_checks.py` FAIL khi (a) dòng SELECTED thiếu bất kỳ trường nào trong 4, (b) số trial CONSUMED mang `hypothesis_slot = IQ-xxxx` vượt `so_bien_the`; test khóa `tests/lock/test_td0119_*`; suite Docker tăng đúng số test mới. **Xác nhận** (commit `f1a0409` + `1983c09`): 5 trường mới trong schema; `TD-0119a` (SELECTED thiếu 1 trong 4 trường — parametrize cả 4) và `TD-0119b` (khai 2 tiêu 3 → sổ bẩn; tiêu đúng 2 → đạt; trial của ý tưởng khác không bị đếm nhầm); 10 test khoá `test_td0119_to_don_hai_cua.py` xanh trong Docker, toàn bộ suite **615 passed, 0 failed**. Hai test cũ FAIL đúng lúc sửa (fixture thiếu trường mới) — chỉ điền thêm dữ liệu mẫu, không nới phép kiểm nào. Quyết định ghi **MT-12**; Kênh 2 ghi **OQ-13** |
| TD-0120 | **OQ-07 — niêm phong tiêu chí chọn ý tưởng Q3/2026 + máy kiểm trích mã.** File `docs/decisions/DR-Q3-2026-tieu-chi-chon-y-tuong.md` (hạn ngạch chọn **0**, ba điều kiện mở lại, 4 tiêu chí thứ tự từ điển `TC-Q3-2026-01…04`), commit **RIÊNG và TRƯỚC** khi mở cửa nộp (spec dòng 4935 cấm mở queue trước khi commit tiêu chí). Phép kiểm mới: `selection_reason` phải **trích ít nhất một mã TC có thật** của đúng quý đó; và quý khai hạn ngạch 0 thì **không được có dòng SELECTED nào** | ✅ | TD-0119 | File tiêu chí commit riêng, trước mọi dòng idea_queue; phép kiểm FAIL khi (a) SELECTED mà `selection_reason` không trích mã TC nào, (b) trích mã không có thật, (c) quý chưa có file tiêu chí, (d) quý khai hạn ngạch 0 mà có dòng SELECTED; test khoá `tests/lock/test_td0120_*`; suite Docker tăng đúng số test mới. **Xác nhận** (commit `d7f649c` tiêu chí → `c085e67` máy kiểm → `OQ-07` đóng): file tiêu chí commit RIÊNG và TRƯỚC mọi dòng đơn; 8 test khoá `test_td0120_*` xanh trong Docker (đủ 4 ca sổ bẩn + ca hợp lệ + đọc được chính file thật bằng các mẫu mà phép kiểm dùng); toàn bộ suite **624 passed, 0 failed**; E6 trên sổ thật → `đã audit 4/9 (4 đạt, 0 chưa đạt, 5 chưa đo được)`, exit 0. `run_audit()` thêm tham số `tieu_chi_dir`; test TD-0118 trỏ sang thư mục giả vì 6 dòng SELECTED của nó đụng đúng hạn ngạch 0 thật — không nới phép kiểm nào |
| TD-0124 | **Công cụ nộp đơn ý tưởng** — writer đầu tiên cho `registry/idea_queue.jsonl` (hiện chỉ có phần ĐỌC; mọi đơn phải gõ tay 20 khoá JSON, `additionalProperties: false`). Cờ mới trên **E6** `--nop-y-tuong <đơn.yaml>` (KHÔNG thêm entrypoint thứ 9 — §0d.2 dòng 664, L-Z36). Module `src/tool_d/ledger/idea_queue.py` **từ chối ghi** 6 ca fail-closed. Kèm **trần NHẬP 10 đơn/quý** (spec dòng 4095) — lấp đúng lỗ hổng MT-11 ghi *"mới là văn bản; chưa code"* | ✅ | TD-0120 | `submit_idea()` từ chối cả 6 ca (sai schema / `status` không thuộc {QUEUED,REJECTED} / TOOL_D_RESULTS mà không REJECTED / quý đã đủ 10 đơn / trùng `idea_id` / thiếu câu bộ lọc §0.1) và **file không dài thêm** ở mọi ca từ chối; phép kiểm mới `check_td0124_tran_nhap_don_moi_quy()` nối vào `run_audit()`, CHẶN CỨNG (không vào `WARN_ONLY_CODES`); `next_idea_id()` sổ rỗng → `IQ-0001`; đơn công cụ sinh chạy qua `check_lz17`+`check_td0120` KHÔNG raise (cạm bẫy micro giây); `entrypoints/` vẫn đúng 8 file; test khoá `tests/lock/test_td0124_*`; suite Docker tăng đúng số test mới, không giảm | **Xác nhận** (commit `3693e52`): 28 test khoá `tests/lock/test_td0124_cong_cu_nop_don.py` xanh trong Docker; suite **629 (bỏ file test mới) → 657 passed, 0 failed** = đúng **+28**, không hơn không kém. E6 chạy thật trên sổ thật: exit 0, dòng tổng `đã audit 4/10 (4 đạt, 0 chưa đạt, 6 chưa đo được)`. Nộp mẫu trống → exit **96**, sổ **không dài thêm dòng nào**. Hộp cát trong container (không ghi rác vào sổ thật, append-only): ghi thành công `IQ-0001`, audit 7/10 đạt, exit 0. `entrypoints/` vẫn đúng 8 file, `WARN_ONLY_CODES` vẫn `frozenset({'L-Z17'})`. **Mốc 629 vs 624 đã truy ra nguồn:** commit `2659a5d` của **phiên song song** (TD-0117, `test_lz49_lz50_backtest_nho.py`, đúng **5 test**) rơi vào giữa đợt này. Mốc **624** ở dòng TD-0120 **vẫn đúng** tại thời điểm nó được ghi — không có lệch số nào.
| TD-0125 | **OQ-13 — sổ đề xuất đổi tham số + `L-Z26`** (🔴 CRITICAL, hiện **0 dòng code**). Sổ thứ ba `registry/param_change_proposals.jsonl` + schema; module `src/tool_d/ledger/param_proposals.py`; cờ E6 `--nop-de-xuat <đề-xuất.yaml>`. Điểm thiết kế then chốt: `luan_diem` là **mảng có cấu trúc** `{chi_so, gia_tri, dai_ky_vong, tham_so_tro_toi}` — câu không trích được số thì KHÔNG biểu diễn được (§12d.2 BƯỚC 3), thay vì ô văn xuôi kèm lời hứa. `bao_cao_hash` do công cụ tự băm từ file báo cáo E5 đã lưu — **KHÔNG sửa `periodic_report.py`** (tránh "đổi nội dung báo cáo = tiêu 1 trial", dòng 4808, và `FROZEN_CONTENT_HASH` của TD-0062) | ✅ | TD-0124 | 8 ca từ chối ghi, mỗi ca một test: sai schema / `cap_tham_so`=C hoặc tham số thuộc `tier_c` / `luan_diem` rỗng / luận điểm thiếu `gia_tri` số / `tham_so_tro_toi` không phải khoá thật (`loader.resolve`) / 🔴 **các luận điểm trỏ >1 tham số** (chống "hyperopt viết bằng tiếng Việt" + L2 đội lốt L1, dòng 4832) / `bao_cao_path` không tồn tại hoặc sha256 ≠ `bao_cao_hash` / `status`=APPLIED mà `trial_id` null. `check_lz26_*` nối vào `run_audit()`, chặn cứng, thêm ca chỉ audit làm được: APPLIED phải trỏ trial CÓ THẬT và CONSUMED trong `trial_registry.jsonl`. Test khoá `tests/lock/test_lz26_*`; suite Docker tăng đúng số test mới | **Xác nhận** (commit `c621762`): 30 test khoá `tests/lock/test_lz26_de_xuat_doi_tham_so.py` xanh trong Docker; suite **657 → 687 passed, 0 failed** = đúng **+30**. E6 chạy thật: exit 0, dòng tổng `đã audit 4/11`. Nộp mẫu trỏ báo cáo chưa có → exit **96**, sổ 0 dòng. Hộp cát: chạy **E5 THẬT** → lưu báo cáo → nộp `PC-0001` gắn vào chính bản đó (L-Z26 ✅ đạt) → **sửa báo cáo sau khi nộp** → L-Z26 🔴 CHƯA ĐẠT, chỉ đúng `sha256 không khớp bao_cao_hash`. `kiem_de_xuat()` dùng CHUNG cho cửa ghi và audit — hai lớp không lệch luật nhau được. 🔴 **`L-Z25` bắt được chuỗi cấm trong thông báo lúc chạy** (`hyperopt`) — diễn đạt lại, **không nới phép kiểm nào**. **Phạm vi KHÔNG phủ, ghi rõ:** §12c.3 đòi Cấp B phải có HAI điểm quyết định LIÊN TIẾP — chưa kiểm được vì chưa có đánh số điểm quyết định (chưa có lệnh live) → TD-0127.
| TD-0126 | **Hai ràng buộc Idea Queue mới chỉ nằm trong `description` của schema, không máy nào canh:** (a) `explore_evidence` BẮT BUỘC có nội dung khi `data_source == EXPLORE` (§9c.7.3); (b) query trùng lặp theo `mechanism` trước khi thêm đơn mới (§9c.7.4 ràng buộc 3 + checklist dòng 4982). Nối vào cửa ghi của TD-0124 | ✅ | TD-0124 | Công cụ từ chối đơn EXPLORE thiếu `explore_evidence`; cảnh báo (không chặn) khi `mechanism` trùng đơn đã có; test khoá tương ứng. Hạn chót: trước D11 | **Xác nhận** (commit `6613ce9`): 21 test khoá `tests/lock/test_td0126_explore_va_trung_mechanism.py` xanh trong Docker; suite **699 → 720 passed, 0 failed** = đúng **+21**. E6 thật: exit 0, `đã audit 4/12`. Cửa ghi từ chối thêm 2 ca (EXPLORE thiếu bằng chứng; nghi trùng `mechanism` chưa khai `overlaps_with`), riêng trùng với đơn **ĐÃ BỊ LOẠI** thì từ chối thẳng (§9c.7.4 ràng buộc 3, tương đương `retest_forbidden`). 🐛 **Test bắt được bug thật trong chính code này:** `đ` (U+0111) KHÔNG tách được bằng NFD — nó là ký tự CƠ SỞ, nét gạch không phải dấu tổ hợp; không thay tay thì `"đóng"` → `"ong"` còn `"dong"` giữ nguyên, hai cách viết cùng một cơ chế hoá thành hai cơ chế khác. Đã sửa + có test canh. **5 test cũ (TD-0118/TD-0124) đỏ đúng lúc sửa** vì fixture nộp nhiều đơn cùng `mechanism` — chỉ đổi DỮ LIỆU MẪU, **không nới phép kiểm, không sửa một dòng `assert` nào**. Ngưỡng nghi trùng cố ý có hậu quả NHẸ (khai thêm một dòng) chứ không mất đơn, và **không** đưa vào `tool_d_config.yaml` — lý do ghi trong code.
| TD-0127 | **`L-Z27` + `L-Z28`** — hai mã cùng khối §12d.4 với `L-Z26`, cũng **0 dòng code**: L-Z27 (B3 ≤ 20 tại mọi thời điểm; B3 chỉ tăng theo `floor(lệnh_mới / 25)`, không tăng bằng tay) và L-Z28 (không thay đổi tham số nào giữa hai điểm quyết định, trừ DR-012 Hạng 1 sửa lỗi). **Ghi nhận phần L-Z26 KHÔNG phủ:** §12c.3 đòi Cấp B phải có HAI điểm quyết định LIÊN TIẾP cùng tín hiệu — chưa kiểm được vì chưa có đánh số điểm quyết định (chưa có lệnh live) | ✅ | TD-0125 | Test khoá `tests/lock/test_lz27_*`, `test_lz28_*`; nối vào `run_audit()`. Hạn chót: trước D11 (dry-run) \| **Xác nhận** (commit `560e795`): 22 test khoá `tests/lock/test_lz27_lz28_ngan_sach_b3_va_diem_quyet_dinh.py` xanh trong Docker; 817 (baseline đo lại ngay lúc so) + 22 = **839 collected, 838 passed**. E6 sổ thật: exit **0**, `4/12 → 5/14`; **L-Z27 ✅ đạt** (`B3 đã dùng=0, trần=20`), **L-Z28 ⏳ chưa đo được**; sổ vẫn 12 dòng. 🔑 **Phần mạnh nhất của L-Z27 KHÔNG phải phép kiểm lúc audit** mà là hình dạng API: `TrialLedger.available()/reserve()` bỏ hẳn tham số `n_tai_sinh`, chỉ nhận `so_lenh_da_dong` rồi tự áp `budget.b3_tai_sinh()` = `min(floor(lệnh/25), 20)` — *tăng B3 bằng tay* (spec dòng 4956) thành **không biểu diễn được**, thay vì bị chặn sau khi đã xảy ra (cùng thủ pháp `luan_diem` của TD-0125). Kèm test AST canh không nơi nào trong `src/` gọi `b3_tai_sinh()` với hằng số. **L-Z28 fail-closed** theo chốt chủ dự án: APPLIED mà không chứng minh được là rơi đúng điểm quyết định → **BÁO ĐỎ**; ba ca phân biệt rõ (chưa tới điểm đầu / GIỮA hai điểm / đúng điểm). 🔎 **Ngoại lệ DR-012 Hạng 1 cố ý KHÔNG thêm ô khai nào:** §12c.4 định nghĩa Hạng 1 là *"code không làm đúng như spec mô tả"* — đó là LỖI, sửa tự do 0 trial, **không phải đổi tham số**, nên không bao giờ vào sổ này (sổ dành cho Hạng 2); thêm ô *"đây là sửa lỗi"* sẽ mở đúng cái cửa L-Z28 sinh ra để đóng. 🔴 **Phạm vi KHÔNG phủ, ghi rõ:** §12c.3 đòi Cấp B có HAI điểm quyết định LIÊN TIẾP — vẫn chưa kiểm được, và giờ đã rõ VÌ SAO: sổ đề xuất **không có trường nào** neo vào điểm quyết định, mà neo được thì cần số lệnh live. Nối nguồn số lệnh (DB Freqtrade) là việc của **D3.5+**. ⚠️ Trong lần chạy full suite có **1 test đỏ không thuộc đợt này**: L-Z46 bắt chuỗi `profit_ratio` trong `src/tool_d/wfo/equity.py` — file **chưa commit của phiên song song**, và chuỗi nằm ở **dòng chú thích giải thích chính lệnh cấm** (đúng hình dạng L-Z25/`hyperopt` của TD-0125). Đã báo phiên đó kèm tiền lệ *diễn đạt lại, không nới phép kiểm*; **không tự sửa file đang dở của người khác** (N12). |
| TD-0130 | **MT-08 — thi hành kế toán dòng `CTRL`** (đã CHỐT 07/09/2026, `back-end-note.md` mục 7, nhưng **0 dòng code**). Ba lỗi trong `src/tool_d/ledger/registry.py`: (1) `TrialProjection` không lưu `budget_line` nên `n_used()`/`n_reserved()` cộng cả dòng CTRL; (2) `reserve()` kiểm ngân sách cho MỌI dòng nên khi N cạn thì **không ghi được điểm kiểm soát**. Kèm **cửa xác thực CTRL fail-closed**: phải khai một trong hai dạng — *tái lập* (§0d.4, `reproduces_trial_id` + hash khớp đúng bản ghi trial đó) hoặc *đo thước* (D3.5 Bước 1, `ctrl_output_whitelist` không chứa chỉ số hiệu năng nào — spec dòng 3605-3607 *"bộ chạy cưỡng chế danh sách này, không phải người"*); không thoả dạng nào → **từ chối ghi**, tính như trial thường. 🔴 KHÔNG dùng mã `L-Z56` (đã có chủ ở DR-015 — MT-09) | ✅ | TD-0086 | 1 trial B1 + 1 trial CTRL đều CONSUMED → `n_used() == 1`; `reserve()` dòng CTRL vẫn thành công khi ngân sách đã cạn, còn dòng B1 vẫn raise `BudgetExhaustedError` (không nới lây); 4 ca từ chối ghi CTRL (không khai dạng nào / hash lệch / trỏ trial không có thật / whitelist chứa `pnl_abs`) và **sổ không dài thêm dòng nào**; bất biến `contribution >= 1` giữ nguyên kể cả với CTRL. Test khoá `tests/lock/test_td0130_*`; suite Docker tăng đúng số test mới. Hạn chót: **trước D3.5** (trước dòng CTRL đầu tiên) \| **Xác nhận** (commit `5d5ae82`): 15 test khoá `tests/lock/test_td0130_ctrl_khong_tinh_vao_n.py` xanh trong Docker; suite **720 → 735 passed, 0 failed** = đúng **+15** (baseline 720 **đo lại ngay trước khi so** bằng `--collect-only --ignore=<file mới>`, KHÔNG lấy con số từ file — hai phiên song song làm mốc test dịch dưới chân). E6 trên sổ thật: exit **0**, `đã audit 4/12`, `L-Z11 N_ĐÃ_DÙNG=4` **không đổi**, sổ vẫn **12 dòng** (sổ thật chưa có dòng CTRL nào → không có thiệt hại quá khứ phải sửa; đây là thời điểm rẻ nhất). Hộp cát trong container trên **bản sao sổ thật**: khai suông → TỪ CHỐI, sổ không dài thêm; `pnl_abs` trong whitelist → TỪ CHỐI; đo thước sạch → ghi được, `n_used` **vẫn 4** và `available` **vẫn 110** sau khi CTRL đã CONSUMED; tái lập hash lệch → TỪ CHỐI, hash khớp → ghi được; ngân sách cạn → B1 raise `BudgetExhaustedError` còn **CTRL vẫn ghi được**. 🔴 **Sửa chặt hơn kế hoạch:** `CTRL_OUTPUT_ALLOWED` là danh sách **CHO PHÉP** (`price_delta`/`tranche_index`/`direction`) chứ không phải blocklist chuỗi cấm — đúng chữ spec dòng 3605-3607 *"bộ chạy cưỡng chế danh sách này"*; blocklist chỉ chặn được tên đã nghĩ ra trước. Thêm ca kế hoạch chưa nêu: **khai CẢ HAI dạng → từ chối** (khai chồng là chừa đường lách sang dạng dễ kiểm hơn). Lời khai CTRL được ghi vào chính sự kiện RESERVE để audit tự đối chiếu lại được. **KHÔNG đụng** `audit_checks.py`/`trial_ledger_audit.py` (hai phiên song song đang giữ) — `check_lz11` gọi `ledger.n_used()` nên tự đúng theo. |

---

## Khối 14 — D3: H3-D Walk-forward orchestrator (mở 07/09/2026, sau tag `d2-complete`)

> 🔴 **H3-D là "VIẾT LẠI TỪ ĐẦU"** (spec dòng 4340) và spec đính kèm **danh sách 9 bug CỤ THỂ
> của Tool A** mà H3-D phải có test chống lại TRƯỚC KHI được coi là xong. Đối chiếu với những
> gì đã có trên đĩa: **5/9 đã có máy canh từ D0-PRE** — (1) file tham số ẩn `guard §0d.1`,
> (2) `L-Z36`, (3) `L-Z38`, (8) `L-Z51`, (9) `L-Z41`. **4/9 còn 0 dòng code** và chính là
> phần việc thật của khối này: (4) cache fold không mang hash cấu hình, (5) cộng dồn
> `profit_ratio` qua fold, (6) ghép fold bằng CỘNG thay vì NHÂN, (7) bản ghi trùng khi chạy
> lại cùng timerange. Spec nói thẳng: *"Zero-dependency là về CODE, không phải về BÀI HỌC"*.
>
> 🔢 **Vì sao đánh số từ TD-0140, bỏ trống 0131–0139:** phiên song song đang ở TD-0130 và sẽ
> đi tiếp lên. Chừa một dải trống là cách rẻ nhất để không lặp lại va chạm mã việc
> TD-0119/TD-0120 (xem TỪ ĐIỂN ĐỔI TÊN ở trên).

| Mã việc | Nội dung | TT | Phụ thuộc | Tiêu chí XONG |
|---|---|---|---|---|
| TD-0140 | 🔴 **DR-D3-01 — chốt SƠ ĐỒ FOLD. CẦN CHỦ DỰ ÁN QUYẾT.** Spec **không định nghĩa** ở bất kỳ dòng nào: anchored hay rolling, độ dài cửa sổ train/test, bước trượt, số fold. Đây là quyết định kỹ thuật bắt buộc phải chốt (CLAUDE.md quy tắc 2), không được tự chọn im lặng. 🔴 **Ràng buộc cứng phải đưa vào lúc quyết:** cửa sổ WFO `[T1..T2]` chỉ dài **7,6 tháng** (12/06/2025 → 29/01/2026) — chia càng nhiều fold thì mỗi fold càng ít lệnh, và DSR/Sharpe trên vài lệnh là số vô nghĩa chứ không phải số bi quan | ✅ | TD-0117 ✅ | File `docs/decisions/DR-D3-01-so-do-fold.md` commit **RIÊNG và TRƯỚC** mọi dòng code fold — cùng khuôn OQ-07/TD-0120 (tiêu chí phải niêm phong trước khi nhìn thấy kết quả, nếu không thì sơ đồ fold sẽ bị vặn theo số đã thấy). Ghi rõ số lệnh kỳ vọng mỗi fold và ngưỡng dưới mà nếu không đạt thì fold đó bị đánh `unreadable`, KHÔNG phải 0 | **Xác nhận** (commit `cb1e3ce`, RIÊNG và TRƯỚC mọi dòng code fold): `docs/decisions/DR-D3-01-so-do-fold.md` — chốt **PA-1 neo gốc, train đầu 12 tuần + 3 × test 7 tuần**, cửa sổ test không chồng lấn, không fold nào chạm LOCKBOX. **Phát hiện đổi cách hiểu D3:** WFO của Tool D KHÔNG khớp lại tham số (N3 cấm hyperopt, N4 buộc tham số từ yaml) — train dùng để **XẾP HẠNG 9 arm ablation**, test kiểm thứ hạng có giữ được không (spec dòng 4326); nên đơn vị phân tích là **thứ hạng giữa các arm**, không phải giá trị tuyệt đối. Số liệu: cửa sổ 33,0 tuần; mật độ **17,0 lệnh/mã/năm** đo thật ở TD-0114 nhưng ghi rõ là **cận TRÊN** (2 meme coin + chưa nối bộ lọc trend/DG1-5) → **88–219 lệnh cả cửa sổ**, tức **19–47 lệnh/fold** — mỏng dưới MỌI phương án, ghi thẳng thay vì giấu. Khai TRƯỚC: `SAN_LENH_MOI_FOLD = 30` (dưới sàn → `unreadable`, KHÔNG ghi số — N6), và nếu rơi cận dưới thì D3 kết luận **orchestrator ĐÚNG, thống kê CHƯA ĐỌC ĐƯỢC**, hoãn phán quyết sang D9. Cấm chia lại fold sau khi thấy số; cấm nới cửa sổ sang LOCKBOX.
| TD-0141 | `src/tool_d/wfo/folds.py` — sinh danh sách fold từ DR-D3-01 + `tool_d_config.yaml` (N4: không hardcode, không đọc env) | ✅ | TD-0140 | Mọi fold nằm TRỌN trong `[T1,T2]` — **nối `assert_dataset_timerange()`/L-Z55 đã có sẵn**, không viết lại phép kiểm; cửa sổ test giữa các fold không chồng lấn nhau; **không fold nào chạm LOCKBOX**. Fail-closed: cấu hình thiếu khoá → raise, KHÔNG tự đoán mặc định | **Xác nhận** (commit `225e6ef`): `src/tool_d/wfo/folds.py` + `tests/unit/test_wfo_folds.py`; tham số sơ đồ vào `tier_c.wfo_folds` (N4 — DR giữ lý do, YAML giữ con số). **KHÔNG viết lại phép kiểm timerange:** `kiem_folds()` gọi thẳng `assert_dataset_timerange()` (L-Z55, TD-0094) thay vì tự so ngày. ⚠️ **ĐÍNH CHÍNH (rà soát độc lập 07/09/2026 → TD-0148): câu này KHÔNG được đọc thành "L-Z55 đã được nối".** Lời gọi đó chỉ kiểm **KẾ HOẠCH**, và với danh sách fold do `sinh_folds()` sinh ra thì **cả hai vế cùng suy từ `wfo.start`** nên nó gần như luôn đúng theo cấu tạo — đúng cái bẫy `DatasetBoundary` tự cảnh báo. Phần việc thật ở TD-0141 do phép kiểm **sức chứa** (`can_ngay > co_ngay`) làm, không phải do L-Z55. Lời gọi vẫn có răng với fold **dựng tay/bị sửa hỏng** (đường mà test khoá đi). Phép kiểm L-Z55 ĐÚNG NGHĨA — đọc phạm vi ngày THẬT từ dữ liệu đã tải — **chưa được gọi ở đâu trong đường chạy WFO**: đó là TD-0148, chặn cổng D3. Fail-closed: thiếu khoá → raise; không phải số nguyên dương → raise (**chặn cả `bool`**, vì `isinstance(True, int)` là True nên `so_fold: true` sẽ lặng lẽ chạy như 1); **sơ đồ không vừa cửa sổ → RAISE, KHÔNG tự giảm số fold cũng không cắt ngắn fold cuối** (phải sửa DR qua L-Z26). `kiem_folds()` tách hàm công khai để test dựng TAY fold hỏng (chồng lấn / vượt ranh giới / train lấn sang test) — chỉ test qua `sinh_folds()` thì mọi nhánh raise không bao giờ chạy, đúng bẫy PASS RỖNG. 24 test xanh trong Docker; suite **738 → 762 = đúng +24** (baseline đo lại ngay trước khi so, không lấy từ file).
| TD-0142 | 🔴 **L-Z47 + ghép fold bằng NHÂN** — bug Tool A số (5) và (6). `wfo/equity.py`: `starting_balance + Σ pnl_abs == final_balance` cho TỪNG fold; ghép fold thành đường vốn liên tục bằng **NHÂN hệ số**, không cộng (mỗi fold backtest độc lập, tự reset vốn — DR-013) | ✅ | TD-0141 | Sai số < **0,01 USDT** mỗi fold; `L-Z46` (cấm `profit_ratio`) phủ luôn tầng WFO mới. **Test phải có răng:** cố tình ghép bằng CỘNG → đúng ca đó đỏ, không phải cả bộ | **Xác nhận** (commit `8c82f5d`): `src/tool_d/wfo/equity.py` + `tests/lock/test_lz47_can_doi_va_ghep_fold.py`. L-Z47 giữ **cả danh sách lãi/lỗ từng lệnh** chứ không chỉ tổng — truyền sẵn tổng vào thì hai vế của phép đối chiếu cùng một nguồn và phép kiểm mất nghĩa. **Test có răng:** đổi phép NHÂN thành CỘNG → đúng **4 ca đỏ, 18 ca còn lại vẫn xanh** (đỏ tràn nghĩa là test đang canh thứ khác); số dựng sao cho CỘNG vs NHÂN cách xa nhau (2.500 vs 3.000), vì trên lãi/lỗ nhỏ hai phép gần trùng nên bộ test dùng số hiền sẽ xanh cả khi code cộng nhầm. Fail-closed: `chi_so` không phải dãy 1..k → RAISE, **không tự sắp xếp** (phép nhân giao hoán nên tích cuối vẫn trông hợp lý, chỉ các điểm GIỮA là bịa); `final_balance <= 0` → TỪ CHỐI ghép tiếp. 🔴 **L-Z46 bắt tên trường bị cấm trong chính DOCSTRING của file này** (nó grep cả docstring, khác L-Z25 có bỏ chú thích) — xử bằng **diễn đạt lại, KHÔNG nới phép kiểm**; phiên song song -91 báo trước khi commit. Hai lỗi tự bắt, đều của TEST không phải module: kiểm sát mốc dung sai float (`(1100.0+0.01)-1100.0 = 0,00999...` nên `>=` không kích hoạt) và `import tests.lock...` không chạy vì `tests/` không phải package. 22 test mới xanh trong Docker.
| TD-0143 | 🔴 **Cache fold phải mang hash** — bug Tool A số (4), §0d.3. Đây đúng là chỗ Tool A *"sửa code rồi chạy lại vẫn ra số y hệt"* | ✅ | TD-0141 | Cache resume WFO ghi kèm `params_hash` + `code_sha` + `data_hash`; đọc lại mà hash lệch → **CẢNH BÁO TO và KHÔNG dùng** (không âm thầm dùng tiếp, cũng không âm thầm bỏ) | **Xác nhận:** `src/tool_d/wfo/cache.py` + 33 test khoá `tests/lock/test_td0143_cache_fold_mang_hash.py` xanh trong Docker. Đếm `--collect-only` ngay trước khi so (mốc dịch liên tục vì 4 phiên song song): **784 → 817 = đúng +33**. Vân tay đủ ba thành phần §0d.3 (`params_hash` từ `cfg.sha256`, `code_sha` từ `get_git_info()`, `data_hash` gộp tất định từ `hash_many()`) — dùng LẠI hạ tầng có sẵn nên cache và provenance không thể kể hai câu chuyện khác nhau về cùng một lần chạy. 🔑 **BA trạng thái, không phải hai:** `HIT`/`MISS`/`STALE`; gộp STALE vào MISS thì kết quả cuối vẫn ĐÚNG nên không test nào đỏ, nhưng mất tín hiệu *“có gì đó đổi ngoài ý muốn”* — có test riêng đòi hai trạng thái đó phân biệt được. STALE không bao giờ trả payload; cảnh báo in cả giá trị cũ lẫn mới. Lệch vân tay = BÌNH THƯỜNG (STALE), file hỏng/thiếu khoá = LỖI (`CacheError`) — không gộp. `xoa_cache()` thi hành bước *“xoá cache trước khi đo lại sau khi đổi mặc định”*. 🔴 **Ghi thẳng sự thật về commit:** code KHÔNG có commit riêng — nó nằm trong **`4ec0fd3`** (nhãn *“TASKS.md: TD-0127 hoàn tất”*) vì `git add` của phiên song song cuốn mất hai file đã stage của phiên này trước khi kịp commit. **Không mất dữ liệu** (`git diff HEAD` rỗng), **không viết lại lịch sử** (4 phiên đang commit đồng thời — rebase nguy hiểm hơn cái sai nó sửa). Đây là lần thứ **4** tai nạn N12 xảy ra; chi tiết `docs/research-log.md`.
| TD-0144 | 🔴 **L-Z45 — `dedup_key` tất định** — bug Tool A số (7). Khoá theo `exchange_order_id` của **TỪNG tranche**, KHÔNG theo `trade_id` (§8.3 — mỗi tranche là một sự kiện vào lệnh riêng) | ✅ | TD-0141 | Chạy hai lần liên tiếp cùng timerange ghi thêm **ĐÚNG 0 dòng**, kể cả khi có tiến trình song song ghi cùng file. Ghi = **no-op** nếu key đã tồn tại; KHÔNG đọc-sửa-ghi-đè (append-only tuyệt đối, LD-20) | **Xác nhận** (commit `82c4572`): `src/tool_d/ledger/decision_log.py` + `tests/lock/test_lz45_dedup_key_append_only.py`. **Đặt ở `ledger/` chứ KHÔNG phải `wfo/`, có chủ đích:** việc mở nó là TD-0144 (khối D3) nhưng Decision Log là sổ của §8.3 — chiến lược chạy thật cũng ghi vào đây; để trong `wfo/` sẽ ngụ ý "chỉ dùng cho walk-forward" và gần như chắc chắn dẫn tới một bản thứ hai cho đường live rồi hai bản trôi lệch. Khoá theo `order_id` TỪNG tranche, không theo `trade_id`; trường dựng khoá rỗng → RAISE (khoá dựng từ trường rỗng làm các sự kiện khác hẳn nhau nuốt lẫn nhau, hỏng nặng hơn ghi trùng); dòng JSON hỏng → RAISE chứ không bỏ qua. 🔴 **BÀI HỌC PASS RỖNG BẮT ĐƯỢC NGAY TRONG ĐỢT NÀY:** bản đầu của ca đồng thời dựng 4 tiến trình THẬT và xanh, nhưng **gỡ hẳn `flock` khỏi module thì vẫn 22/22 xanh** — thời điểm khởi động lệch hàng chục ms trong khi cửa sổ tranh chấp chỉ vài micro giây, nên tiến trình sau luôn thấy khoá đã có kể cả khi không có khoá nào. Sửa bằng hai ca bổ sung nhau: một ca **tất định** đo bằng đồng hồ (tiến trình ngoài giữ `flock` 1 giây, cửa ghi phải bị chặn suốt thời gian đó) + ca chạy đua thêm `mp.Barrier`. Kiểm lại: gỡ `flock` → **đúng 2 ca đó đỏ** (ca chạy đua sinh đúng 4 dòng trùng), 21 ca còn lại vẫn xanh. 23 test xanh trong Docker; suite **839 → 862 = đúng +23**.
| TD-0145 | Nối toàn bộ vào **E2 `entrypoints/run_wfo.py`**, gỡ `NotImplementedError` | ✅ | TD-0142, TD-0143, TD-0144 | Khung guard/audit/seal đã có sẵn ở đầu `main()` (TD-0016/0057/0072) — **không đụng vào**, chỉ thêm phần sau nó. `entrypoints/` vẫn đúng **8 file** (L-Z36) | **Xác nhận** (commit `f9cfa82`): `src/tool_d/wfo/orchestrator.py` + 19 test khoá; đếm `--collect-only` ngay trước khi so: **884 → 903 = đúng +19**; full suite **903 passed, 0 failed**. Chạy THẬT E2 qua service `lockbox`: **exit 97**, sơ đồ fold in ra khớp DR-D3-01 từng con số (train 84d = 12 tuần, test 49d = 7 tuần, 3 fold, kết thúc đúng T2 29/01/2026). Khối guard/audit/seal KHÔNG bị đụng; `entrypoints/` vẫn đúng 8 file. Bộ chạy backtest **tiêm vào** nên logic điều phối kiểm được TẤT ĐỊNH không cần dữ liệu thị trường. L-Z47 chạy TRƯỚC khi ghi cache (ghi trước thì lần sau HIT thẳng vào số sai) và chạy cả cho kết quả LẤY TỪ cache. STALE không bị gộp vào MISS. Fold dưới sàn → `unreadable`, và hệ số GỘP cũng `unreadable` nếu bất kỳ fold nào thiếu. 🐛 **Tự bắt được một ca PASS RỖNG trong test của chính mình** (assert trên thư mục cache chưa được tạo) — đã sửa và chứng minh test có RĂNG bằng cách tạm đảo thứ tự kiểm/ghi → đỏ đúng chỗ. 🔴 **PHẠM VI KHÔNG PHỦ:** chưa có bộ chạy backtest thật ở đâu trong repo (E1 cũng còn `NotImplementedError`, việc Khối 2/3). Mảnh đó khi viết phải gọi `TrialLedger.reserve()` TRƯỚC khi chạm dữ liệu (L-Z52).
| TD-0146 | Bản ghi fold mang đủ xuất xứ | ✅ | TD-0145 | Mỗi bản ghi fold có đủ **7+1 khoá provenance** (L-Z40) và mọi chỉ số chưa đo ở trạng thái `pending` (L-Z41). Hai máy canh đã tồn tại — việc ở đây là **NỐI vào bản ghi fold**, không viết lại phép kiểm | **Xác nhận** (commit `b2110e2`): `src/tool_d/wfo/fold_record.py` + `registry/schemas/fold_record.schema.json` + 28 test. **Không viết lại phép kiểm nào** — gọi thẳng `validate_provenance()` và dùng kiểu `Measured`. Thi hành sàn DR-D3-01 §5.2: dưới sàn thì **ÉP** mọi chỉ số thành `unreadable` (kể cả chỉ số đã tính ra số) — ép chứ không cảnh báo, vì số đã nằm trong bảng thì sẽ được đọc bất kể chú thích. Giữ `unreadable` **tách khỏi** `pending` (chưa chạy vs đã chạy nhưng mẫu mỏng). 🔴 **Bịt khe schema phiên -91 cảnh báo:** bộ test cũ chỉ đối chiếu FIXTURE GÕ TAY với schema nên khoá mới trong code không bị bắt (735 test xanh vẫn lọt, ca `trial_event.schema.json`); test ở đây đối chiếu **ĐẦU RA THẬT** của `build_fold_record()` — kiểm có răng: thêm khoá vào code mà quên schema → **đúng 4 ca đỏ ngay**. Cầu nối `build_fold_record_tu_ket_qua()` là đường DUY NHẤT từ `KetQuaFold` (TD-0145) sang bản ghi đĩa, tránh hai bộ tuần tự hoá trôi lệch. **Chốt:** xuất xứ KHÔNG vào payload cache (vân tay TD-0143 đã trả lời câu đó; hai nguồn sự thật thì khi lệch không có luật nào nói tin cái nào) — thay vào đó ghi `tu_cache` vào bản ghi. Suite **881 → 909 = đúng +28**.
| TD-0147 | 🚪 **GATE D3** — điều kiện vào D3.5 | ✅ | TD-0140…TD-0146, **TD-0148** | `runtime_state.json.d3_complete` + tag `d3-complete`, dùng lại khuôn `close_d2_gate()`: chạy **RIÊNG** test khoá cốt lõi của D3 (L-Z45 + L-Z47) và đòi số ca PASS ≥ 1 — suite tổng xanh không chứng minh được chúng còn tồn tại  🔴 **THÊM ĐIỀU KIỆN (rà soát độc lập 07/09/2026):** cổng KHÔNG được đóng khi `L-Z55` cho WFO còn ở trạng thái chưa thi hành — xem **TD-0148**. Cổng đóng là lúc mọi người thôi nhìn phần này, nên đóng kèm một lời tuyên bố *"L-Z55 đã nối"* không đúng sự thật là đúng thứ §0d sinh ra để chặn. | **Xác nhận** (commit `9436677` máy đóng cổng → `497c95b`/`8294355` chốt cây sạch → `0539d3c` đóng cổng thật → tag `d3-complete`): chạy THẬT trong Docker qua service **`freqtrade`**, exit 0; ba mục evidence đều `do-duoc` (MT-10): `full_suite` **973 passed**, `test_khoa_d3` (L-Z47 22 · L-Z45 23 · TD-0148 14, **mỗi file chạy RIÊNG**), audit **5/14 đạt, 0 chưa đạt**. Chạy lại → exit **94**. 🔴 **Ba bài học từ lần chạy thật (chi tiết `docs/research-log.md` 08/09/2026):** (1) ba cổng D1/D2/D3 đều ghi `git_sha` mà **không cổng nào kiểm cây sạch** — lần chạy đầu có 8 ca đỏ thoáng qua vì suite chạy 6,5 phút đúng lúc phiên khác sửa dở `registry.py`; nếu sửa đổi đó tình cờ không làm đỏ test nào thì cổng ĐÃ ĐÓNG với sha trỏ tới commit không chứa thứ vừa kiểm. Đã vá cho D3 (`d3_cay_sach: true`, kiểm TRƯỚC cả suite); **D1/D2 không truy lại được — hạn chế đã ghi sổ, không sửa vì là bằng chứng đã niêm phong**. (2) Chốt đầu viết **quá chặt** (dùng thẳng `is_clean`, tính cả rác chưa theo dõi) → cổng không bao giờ đóng được; sửa thành phân loại: file **đã theo dõi** bị sửa luôn tính, file **chưa theo dõi** chỉ tính khi nằm trong vùng đo (một `.py` chưa commit trong `tests/` VẪN được pytest thu). (3) **Suite không độc lập với service** — chạy nhầm `lockbox` làm 3 test đỏ đúng, vì đó là service duy nhất KHÔNG che `lockbox/data/`. **Bẫy PASS RỖNG thứ ba của khối** cũng bắt trong đợt này: fixture autouse che chính hàm cần kiểm → giữ `PHAN_LOAI_THAT` từ lúc import. **Kiểm có răng:** gộp ba file thành một lượt → đúng 1 ca đỏ, 12 ca xanh. 🔴 **Phạm vi:** `d3_han_che` ghi rõ cổng chứng nhận **BỘ ĐIỀU PHỐI** đúng, **KHÔNG** chứng nhận đã có kết quả walk-forward — chưa có bộ chạy backtest thật, mọi phép kiểm mới nuôi bằng bộ chạy GIẢ; D3.5 viết bộ chạy thật **bắt buộc** đọc ngày từ dataframe + test khoá riêng.
| TD-0148 | 🔴 **`L-Z55` cho WFO: bộ chạy phải KHAI PHẠM VI DỮ LIỆU THẬT ĐÃ ĐỌC** (phát hiện khi rà soát độc lập khối D3, 07/09/2026; chủ dự án chốt phương án **A + C**). `kiem_folds()` có gọi `assert_dataset_timerange()` nhưng **cả hai vế đều suy từ `wfo.start`** — đúng cái bẫy `DatasetBoundary` tự cảnh báo (*"cả hai đến từ cùng chỗ thì hàm này không bảo vệ được gì"*), nên nó gần như luôn đúng theo cấu tạo và phần việc thật đã do phép kiểm sức chứa làm. Phép kiểm ở tầng **dữ liệu thật** hiện **không được gọi ở đâu** trong đường chạy WFO. Gốc rễ: `FoldEquity` (kiểu trả về của bộ chạy tiêm vào) **không mang thông tin ngày nào**, nên orchestrator không thể kiểm kể cả khi muốn. Mối đe doạ có thật và đã xảy ra một lần: TD-0093 — `download-data --timerange` không cắt file, nến vùng LOCKBOX (tới 2026-09-05) **đang nằm sẵn** trong thư mục WFO sẽ đọc | ✅ | TD-0145 | `FoldEquity` mang thêm `observed_start`/`observed_end` **BẮT BUỘC** (không mặc định — có mặc định là sớm muộn có chỗ quên truyền); `chay_wfo()` kiểm **HAI TẦNG** ngay sau khi bộ chạy trả về và **TRƯỚC** `kiem_can_doi_fold()`: (a) nằm trong biên WFO, (b) nằm trong đúng cửa sổ của CHÍNH fold đó — (b) chặt hơn (a) và bắt thêm **rò rỉ giữa các fold** mà L-Z55 nguyên bản không phủ. ⚠️ **Bẫy lệch quy ước phải xử tường minh:** cửa sổ fold là **nửa mở** `[start, end)` còn ngày đọc từ dataframe là **bao gồm hai đầu** — trộn hai quy ước là lệch đúng một ngày, mà một ngày ở đây là một ngày dữ liệu tương lai. Test khoá `tests/lock/test_td0148_*`. 🔑 **PHẢI GHI RÕ, KHÔNG ĐƯỢC NGẦM HIỂU:** tới khi có bộ chạy backtest THẬT (D3.5), phép kiểm này chỉ được nuôi bằng bộ chạy giả trong test — **cơ chế** đã tại chỗ và bị khoá, **bảo đảm trên dữ liệu thật** thì D3.5 mới có. Và nó **vẫn tin lời khai của bộ chạy**: bộ chạy trả ngày *dự kiến* thay vì ngày *thật đọc được* sẽ vô hiệu hoá nó → khi viết bộ chạy thật, bắt buộc đọc từ dataframe và có test khoá riêng cho điều đó. Không viết hai câu này ra là lặp lại đúng sai lầm đang sửa. | **Xác nhận** (commit `8c05106`): `FoldEquity` thêm `observed_start`/`observed_end` **BẮT BUỘC** (có test canh: thiếu → `TypeError` ngay); `kiem_pham_vi_du_lieu()` kiểm **hai tầng** — (a) gọi thẳng `assert_dataset_timerange()` không chép lại luật, (b) cửa sổ của CHÍNH fold, bắt **rò rỉ giữa các fold** mà (a) không thấy (fold 1 đọc sang cửa sổ test fold 2 vẫn nằm gọn trong `[T1,T2]` nên vẫn qua (a)). Cận dưới lấy `train_start` chứ không phải `test_start` — neo gốc thì đọc dữ liệu cũ hơn là **đúng thiết kế**, chiều nguy hiểm chỉ có một. **Bẫy lệch quy ước xử tường minh:** cửa sổ fold NỬA MỞ vs `observed_*` BAO GỒM hai đầu → cận trên là `test_end − 1 ngày`, có ca riêng cho "đọc đúng ngày `test_end`". Nối vào `chay_wfo()` **TRƯỚC** `kiem_can_doi_fold()` và trước `ghi_cache()`, chạy cho **cả nhánh cache HIT**; mục cache cũ thiếu trường → **TỪ CHỐI dùng lại**, buộc chạy lại. `observed_*` vào payload cache (là DỮ LIỆU của kết quả) nhưng xuất xứ vẫn KHÔNG vào cache (TD-0146). 🔑 **Hai giới hạn đã ghi rõ trong code, không ngầm hiểu:** (1) tới D3.5 phép kiểm chỉ được nuôi bằng bộ chạy GIẢ — **cơ chế** tại chỗ, **bảo đảm trên dữ liệu thật** thì D3.5 mới có; (2) nó **vẫn tin lời khai của bộ chạy** — bộ chạy trả ngày *dự kiến* thay vì ngày *thật đọc từ dataframe* sẽ vô hiệu hoá nó, nên bộ chạy thật bắt buộc đọc từ dataframe + có test khoá riêng. **Kiểm có răng:** vô hiệu hoá tầng (b) → **đúng 5 ca đỏ, 9 ca tầng (a) vẫn xanh**. 14 test mới; collect **931 → 945 = đúng +14** (đo bằng `--collect-only` hai lần sát nhau vì suite đang dịch do phiên song song commit xen giữa).
| TD-0149 | 🔴 **Schema sổ trial phải soi ĐẦU RA THẬT, không phải fixture gõ tay.** `tests/unit/test_registry_schemas.py` hiện CHỈ validate `tests/fixtures/trial_events_valid.jsonl` — không một dòng nào lấy đầu ra thật của `TrialLedger.reserve()`/`seal()`/`consume()`/`refund()`/`mark_contaminated()` rồi đối chiếu `trial_event.schema.json`. Đây đúng lỗ đã cắn ở TD-0130 (thêm 2 khoá vào sự kiện RESERVE, quên schema, **735 test xanh KHÔNG bắt được**, sẽ nổ đúng lần ghi dòng CTRL thật đầu tiên). TD-0130 vá TRƯỜNG HỢP đó; lỗ THÌ VẪN CÒN — hai bên vẫn chưa nhìn nhau. Mẫu có sẵn ở `TestSchemaSoiDauRaThat` của `tests/unit/test_fold_record.py` (TD-0146) | ✅ | TD-0130 | Test mới lấy đầu ra THẬT của cả 5 loại sự kiện (ghi bằng `TrialLedger` vào `tmp_path`, đọc lại từ đĩa) và validate với chính `trial_event.schema.json` mà `audit_checks` dùng. **Kiểm CÓ RĂNG:** thêm một khoá vào sự kiện trong code mà cố tình không sửa schema → phải ĐỎ ngay, không chờ tới lần ghi thật. Không sửa `registry.py`, không sửa schema, không nới phép kiểm nào | **Xác nhận** (commit `60ebef8`): `tests/lock/test_td0149_schema_soi_dau_ra_that.py`, 21 test; đếm `--collect-only` ngay trước khi so: **924 → 945 = đúng +21**; full suite **945 passed, 0 failed**. Ghi thật đủ 5 loại sự kiện bằng chính `TrialLedger` rồi **ĐỌC LẠI TỪ ĐĨA** (byte trên đĩa mới là thứ `audit_checks` đọc; soi dict trong bộ nhớ bỏ lọt lỗi serialize). Lớp `TestCoRang` tự chứng minh có răng: thêm khoá lạ / bỏ khoá bắt buộc / bỏ một khoá provenance / đổi `event` ngoài 5 loại → đều ĐỎ. Có ca riêng canh hai khoá đã gây sự cố TD-0130 (`reproduces_trial_id`, `ctrl_output_whitelist`) và ca canh `hypothesis_slot = IQ-xxxx` (MT-12) khỏi bị siết nhầm. Sổ THẬT trên đĩa: **12/12 dòng hợp lệ**. 🔴 **PHÁT HIỆN KÈM, ghi nhận KHÔNG sửa:** `TrialLedger` không validate đầu vào theo schema của chính nó — `seal_path` (schema đòi mẫu `runs/<...>/metrics.seal`) và `verdict` (chỉ `KEPT|REJECTED|INCONCLUSIVE`) sai đều GHI ĐƯỢC. Hai trường khác nhau cùng một hiện tượng → tính chất chung của cửa ghi. Đã ghim bằng lớp `TestKhoangHoConLai`; sửa `registry.py` là đổi hành vi cửa ghi, cần người chủ quản quyết → **đề nghị mở TD riêng**.
| TD-0150 | 🔴 **Cửa ghi sổ trial phải TỰ ĐỐI CHIẾU SCHEMA trước khi append** (phiên `trading-tool-smart-dca-91` phát hiện khi làm TD-0149; đã tái hiện độc lập; chủ dự án duyệt siết 08/09/2026). `TrialLedger` **không** validate đầu vào theo schema của chính nó: `seal_path` schema đòi mẫu `^runs/.+/metrics\.seal$` mà `seal()` nhận mọi chuỗi; `verdict` schema chỉ cho `KEPT|REJECTED|INCONCLUSIVE` mà `consume()` nhận mọi chuỗi. **Hai trường khác nhau, cùng một hiện tượng** → tính chất chung của cửa ghi, không phải hai chỗ sót lẻ. Cùng họ với lỗ TD-0149 vừa vá nhưng **ngược chiều**: lỗ kia là *schema không biết khoá mới*, lỗ này là *cửa ghi không biết ràng buộc của schema* | ✅ | TD-0149 | Validate **NGAY TRƯỚC** khi append (fail-closed, cùng khuôn `submit_idea()`/`submit_proposal()` — sổ append-only không có đường lùi, ghi rồi mới báo là vô nghĩa). Ca kiểm: `seal_path` sai mẫu → raise và **sổ không dài thêm dòng nào**; `verdict` ngoài enum → raise; dòng hợp lệ vẫn ghi bình thường; sổ THẬT 12/12 dòng vẫn hợp lệ sau khi siết. **Sửa** (không xoá) hai test `TestKhoangHoConLai` của TD-0149 thành khẳng định ngược — chúng ghim hiện trạng cũ nên PHẢI đỏ, và người sửa buộc phải đọc lý do. ⚠️ **Đánh đổi đã nêu:** tạo phụ thuộc mới `registry.py` → `jsonschema` + file schema trên đĩa; schema thiếu/hỏng thì cửa ghi raise. Với sổ trial fail-closed như vậy là ĐÚNG (không ghi được còn hơn ghi một dòng không kiểm được), nhưng ghi ra để không ngầm hiểu \| **Xác nhận** (commit `7f84ae2`): 8 test khoá `test_td0150_*` xanh; suite **958 → 966 passed, 0 failed** = đúng **+8** (baseline đo lại ngay lúc so). Vá ở **`_append()`** — điểm nghẽn duy nhất — kèm test AST canh không hàm nào tự `write` vòng qua; vá từng hàm là mời lỗi thứ ba xuất hiện ở hàm thứ bảy. Bốn ca từ chối: `seal_path` sai mẫu / `verdict` ngoài enum / `dataset` ngoài enum ngay từ `reserve` / `provenance` thiếu khoá — **mỗi ca sổ không dài thêm dòng nào**. Hai test ghim hiện trạng của TD-0149 **sửa thành khẳng định ngược, KHÔNG xoá** (`TestKhoangHoConLai` → `TestKhoangHoDaVA`). **Hai file test của chính tôi (TD-0130, TD-0127) đỏ đúng lúc siết** vì dùng `seal_path="s.json"` sai mẫu — chỉ sửa DỮ LIỆU MẪU, không nới một dòng phép kiểm nào. 📌 **Suýt báo nhầm một bug:** `outcome={'pnl_abs': 1.0}` làm ca đường-đúng đỏ, đọc schema mới thấy `outcome` đòi đúng 4 khoá — dữ liệu test sai, không phải code sai. **Đánh đổi đã nói ra:** `registry.py` nay phụ thuộc `jsonschema` + file schema trên đĩa, schema hỏng thì cửa ghi raise (fail-closed, có test riêng canh). |

---

## Khối 15 — D3.5: 🚪 Cổng sai lệch thước đo (DR-015), CHẶN D4 (mở 08/09/2026)

> 🔴 **Vì sao cổng này tồn tại và vì sao nó đứng TRƯỚC D4.** Z0 và các nhánh DCA **không được đo
> bằng cùng một bộ máy**: Z0 đi đường vào lệnh chuẩn (một lần khớp), tranche 2+ đi qua
> `adjust_trade_position()` — đánh giá THEO NẾN tại giá MỞ nến (giả định D6). Chênh lệch quan sát
> được = chênh CHIẾN LƯỢC thật **+** chênh THƯỚC ĐO, và backtest **không tách được**. Sai số cộng
> dồn theo số tranche: nhánh k tranche chịu k lần, Z0 chịu một — **không tự triệt tiêu**.
>
> v7 để trạng thái tệ nhất có thể (đo D6 ở D10, tức SAU ablation, rồi "đọc lại"). v8 tách phép
> thăm dò ra thành cổng chặn TRƯỚC D4, vì *đọc kết quả Z0-vs-DCA rồi mới đo thước là kết luận đã
> hình thành trên thước chưa kiểm* (spec dòng 278).
>
> 🔑 **Ba việc đã có sẵn, không phải làm lại:** Bước 1 gần như đo xong ở **TD-0115** (91 lượt khớp
> tranche trên CALIB, dữ liệu 5m thật — spec đòi ≥30); `L-Z55` đã có máy canh timerange;
> `binance_public.py` đã là cửa mạng duy nhất (R1) và biết ký request (TD-0116).
>
> 🔢 **Đánh số từ TD-0160, bỏ trống 0151–0159** — cùng lý do Khối 14 bỏ trống 0131–0139: các phiên
> song song đang ở TD-0150 và sẽ đi tiếp lên.

| Mã việc | Nội dung | TT | Phụ thuộc | Tiêu chí XONG |
|---|---|---|---|---|
| TD-0160 | 🔴 **DR-D35-01 — chốt MÔI TRƯỜNG ĐO Bước 2 + cách quy đổi sang đơn vị R.** Lộ trình cũ ghi *"cần testnet, mà Freqtrade tắt testnet"* — **đóng khung SAI**: Bước 2 không cần Freqtrade (spec §2: *"không cần chiến lược hoàn chỉnh, không cần bot chạy thật"*), và testnet có thật, gọi được. Câu hỏi thật là **đo ở đâu thì con số mang thông tin về thị trường ta sẽ giao dịch** | ✅ | TD-0147 ✅ | File `docs/decisions/DR-D35-01-moi-truong-do-buoc-2.md` commit **RIÊNG và TRƯỚC** mọi dòng code đo — DR-015 §4 tự cấm đặt ngưỡng sau khi thấy số, chọn môi trường sau khi thấy kết quả là uốn kết luận bằng cách khác. **Xác nhận** (commit `4c55b78`): chốt **suy tỷ lệ không-khớp từ `aggTrades` production THẬT, không đặt lệnh nào**; đầu ra là **KHOẢNG** `[p_nf_thap, p_nf_cao]` chứ không phải một số. **LOẠI testnet dù là chữ của spec** — sổ lệnh riêng, mỏng, phần lớn bot thử ⇒ con số về MỘT THỊ TRƯỜNG KHÁC, đúng lỗi `N6` chặn, nguy gấp đôi vì Δ_R chảy thẳng vào phân xử Z0-vs-DCA. Ghi rõ 3 thứ phương án KHÔNG thấy được (post-only bị từ chối / khớp một phần / vị trí hàng đợi) — cả ba đều được vùng bất định bao phủ về phía an toàn. 3 điều kiện mở lại viết trước. 🔴 Sai khác với spec → **mục Mâu thuẫn `back-end-note.md`**, chờ lệnh "chuẩn hóa và lưu" |
| TD-0161 | **Bước 1 — quy đổi phân phối lệch sang đơn vị R và tính Δ_R** (KHÔNG đo lại: dùng đúng 91 lượt khớp của TD-0115). `lệch_R = (fill − p_i) / planned_risk_usdt` với mẫu số **ĐÓNG BĂNG tại tranche 1** (DR-013 §2 — không dùng giá vào trung bình, không `initial_stop_loss_abs`) | ✅ | TD-0160 | `lệch-mỗi-lệnh = Σ|lệch_R|` của tranche **≥ 2** (tranche 1 không tính — Z0 cũng có nó); `Δ_R = P90` nếu `n ≥ 30`, `= max` nếu `n < 30` (fail-closed); tách **Long/Short**, Short ghi **`unreadable` KHÔNG phải 0** (chiến lược hiện LONG-only). Ghi **dòng `CTRL`, 0 trial** + assert timerange `L-Z55`. Chạy trên **CALIB**, không WFO \| **Xác nhận** (commit `a8f881e`): `src/tool_d/dr015/buoc1_lech_tranche.py`, 23 test mới; đếm `--collect-only` ngay trước khi so: **1005 → 1028 = đúng +23**; full suite **1029 passed, 0 failed**. Dùng đúng 91 lượt khớp của TD-0115 (KHÔNG đo lại). `planned_risk_usdt` không đọc được từ dữ liệu (tranche chưa khớp không có `amount` thật) nên phải SUY — dựa trên mẫu hình position-sizing đã xác minh trên chính dữ liệu (cấp luỹ thừa: tranche2=tranche1, tranche3=tranche1+tranche2; KHÔNG phải chia đều 1/3 — hằng số `TRONG_SO_TRANCHE` chỉ dùng cho giá trung bình, không liên quan khối lượng), kiểm khớp 8/8 trade đủ 3 tranche thật, sai số <0,1%. `Δ_R = P90` (n=35≥30). Tranche 1 loại khỏi Σ\|lệch\|. Short → `unreadable` (N6). `L-Z55` đúng nghĩa: ngày quan sát lấy từ TIMESTAMP KHỚP LỆNH THẬT, không phải ngày cấu hình. Chạy THẬT trên sổ trial thật: `trial_id=D-0005`, LONG `n=35 Δ_R≈0,161`, SHORT unreadable; E6 audit `N_ĐÃ_DÙNG=4` không đổi (CTRL không tính vào N). 🔴 `L-Z48c` bắt 3 chỗ "R" trần trong docstring — diễn đạt lại thành `R_realized`, không nới phép kiểm. Một test cũ (`test_registry_schemas.py`) tự ghi chú "đỏ vì có trial khác là tín hiệu TỐT" — đã cập nhật đúng như vậy (4 B0 + 1 CTRL). |
| TD-0162 | **Bước 2 — đo tỷ lệ NO_FILL từ dữ liệu khớp lệnh THẬT** (`aggTrades` production, không đặt lệnh). Ba trạng thái: chắc chắn khớp / bất định (low == `p`, hoặc khớp một phần) / chắc chắn không khớp | ✅ | TD-0160 | Trả về **KHOẢNG** `[p_nf_thap, p_nf_cao]`, **cấm tự thu về một điểm**; mọi lệnh gọi mạng đi qua `binance_public.py` (R1 single egress), không module thứ hai. Không đủ sự kiện → ghi **`UNKNOWN`** theo đường fail-closed của DR-015 §2, KHÔNG bịa số | **Xác nhận** (commit `e8bee3e`): `src/tool_d/fill_probe.py` + cửa tải dump trong `binance_public.py` (R1) + 33 test. **Kết quả: p_nf = [0%, 0%]** trên 91 lượt khớp CALIB (LONG); Short **`unreadable`**, không phải 0. Chốt tập đo = **tập backtest ĐÃ CẤP khớp** (chủ dự án 08/09/2026) — đúng tập §3 cần vì phép hiệu chỉnh trừ đi tranche *"backtest đã cho"*. 🔴 **Con số đầu tiên SAI, tự bắt được:** cửa sổ neo LÙI từ `order_filled_timestamp` cho **15/91 (16,5%)** không khớp — nhưng **15/15 ca đó chạm được `p` SAU mốc**; mốc backtest lệch **cả hai chiều** so với lần chạm thật. Sửa API thành `truoc_ms`/`sau_ms` riêng, **bắt buộc, không mặc định**. **Đo ĐỘ NHẠY thay vì chọn một cửa sổ:** p_nf = 16,5% ở cửa sổ 0 → **0% ở cửa sổ ≥ 15 PHÚT**, phẳng tới 3h — đường cong **dựng đứng** nên kết luận không dựa vào giả định rộng tay (trễ tối đa **+4,4 phút**, dưới một nến 5m; 76/91 xuyên TRƯỚC mốc). **Vùng bất định rỗng KHÔNG phải hiện vật:** ca nông nhất vẫn xuyên **0,140%** dưới `p` (~250 bước giá), 0/91 ca dưới 0,05%; đã kiểm `tickSize` thật để khẳng định làm tròn không lật ca nào. Module **không có** hàm trả tỷ lệ vô hướng (có test liệt kê tên bị cấm). REST `aggTrades` chỉ tra được **2 ngày gần nhất** (`-4166`) nên phải dùng dump lịch sử. Full suite **1029 passed, 0 failed**. 🔴 **Hạn chế:** bản xuất backtest KHÔNG mang thời điểm ĐẶT lệnh — ghi được mốc đó thì độ nhạy trên biến mất hẳn; đáng làm trước D4.
| TD-0163 | **Bước 3 — đối chứng âm (BẮT BUỘC)**: chạy CẢ Bước 1 và Bước 2 cho nhánh **Z0** | ✅ | TD-0161, TD-0162 | Không có mốc tham chiếu thì con số lệch của DCA **không biết là lớn hay nhỏ**. Hai nhánh lệch tương đương ⇒ sai số phần lớn triệt tiêu khi so sánh; chỉ DCA lệch ⇒ xác nhận đúng vấn đề. Kết luận phải ghi rõ rơi vào vế nào | **Xác nhận** (commit `2483ed5`): `src/tool_d/dr015/buoc3_doi_chung_z0.py` + 13 test. 🔴 **KẾT LUẬN: HAI NHÁNH TƯƠNG ĐƯƠNG** — Δ_R(Z0) = **0,1552** · Δ_R(DCA) = **0,1612** · tỉ lệ **1,04**. Ý nghĩa đụng thẳng tính công bằng của §4: quy tắc phân xử trừ/cộng Δ_R vào **mọi arm DCA** và **không đụng Z0**, phép bất đối xứng đó chỉ chính đáng nếu sai số thước **đặc thù tranche 2+**. Dữ liệu nói KHÔNG: tranche 1 — thứ Z0 cũng có — lệch gần bằng hệt ⇒ phần lớn sai số **chung cho cả hai nhánh và tự triệt tiêu khi so sánh**. §4 vẫn dùng Δ_R nhưng **phải ghi kèm: phần lớn nó KHÔNG phải bất lợi riêng của DCA**. Đối chứng dựng bằng phân hoạch 91 lượt khớp (tranche 1 = điểm vào của Z0 theo §D0.9), **cùng `planned_risk_usdt`** — hợp lệ vì `lệch_R` bất biến theo quy mô ở bậc nhất. **KHÔNG viết lại phép tính:** gọi thẳng hàm nội bộ của TD-0161, có test ghim Δ_R(DCA) khớp tới **1e-12**; kiểm có răng bằng cách đổi mẫu số → đúng ca đó đỏ, 12 ca kia xanh. **Thêm vế thứ ba `chi_z0_lech` mà spec không liệt kê** — nếu Z0 lệch nhiều hơn thì giả thuyết nền DR-015 bị bác; không có chỗ ghi kết cục đó là giấu một phát hiện ngược. 🔴 **Ghi rõ chỗ P90 che mất:** hai nhánh bằng nhau ở P90 nhưng **hình dạng phân phối khác hẳn** (Z0 trung vị ~0 + vài ca lệch lớn; DCA lệch nhỏ nhưng đều). Bước 2 đối chứng: Z0 **0/35**, DCA **0/56** — không bất đối xứng ở tỷ lệ khớp. Full suite **1042 passed, 0 failed**.
| TD-0164 | **§4 — hiệu chỉnh cực đoan HAI CHIỀU + `L-Z58`**. Chiều 1: trừ Δ_R khỏi expectancy mọi arm DCA. Chiều 2: cộng Δ_R. Cùng nhánh thắng ở CẢ HAI ⇒ kết luận VỮNG; người thắng ĐỔI ⇒ backtest **không đủ tư cách phân xử** ⇒ áp §5.1 (mặc định **Z0**) | ✅ | TD-0163 | 🔴 **Cố ý KHÔNG có ngưỡng phần trăm cho "lệch bao nhiêu là nhiều"** — ngưỡng tuỳ tiện là chỗ uốn kết luận sau khi thấy số. `L-Z58`: bộ giả lập chênh **NHỎ hơn** Δ_R → kết luận "KHÔNG PHÂN XỬ ĐƯỢC" + chọn Z0; chênh **LỚN hơn** → kết luận không đổi giữa hai chiều và biên hiệu chỉnh ghi kèm kết quả | **Xác nhận** (commit `39900a0`): `src/tool_d/dr015/buoc4_hieu_chinh_hai_chieu.py` + 28 test `test_lz58_*`. **KHÔNG có hằng số ngưỡng lệch nào** — §4 tự cấm (*"ngưỡng tuỳ tiện là chỗ uốn kết luận sau khi thấy số"*); chỉ hỏi **người thắng có đổi giữa hai chiều không**. Hằng số **20%** là của §10.2 Nhánh 2, dùng lại nguyên vẹn, **0 DOF mới**. 🔴 **`ket_luan_buoc3` là tham số BẮT BUỘC:** §4 áp Δ_R **chỉ lên arm DCA**, phép phạt bất đối xứng đó chỉ chính đáng nếu sai số đặc thù tranche 2+ — mà TD-0163 đo được **tỉ lệ 1,04 ⇒ tương đương**, tức phần lớn Δ_R là sai số CHUNG và tự triệt tiêu. Làm nó thành tham số bắt buộc là cách duy nhất khiến không ai đọc §4 mà thiếu câu đó; ba kết luận Bước 3 sinh ba diễn giải khác nhau, có **test đối chứng** để cảnh báo không mất nghĩa vì xuất hiện ở mọi nhánh. **Fail-closed:** Δ_R/p_nf chưa đo → RAISE, **không mặc định 0** (N6); `p_nf > 0` mà thiếu `expectancy_r_khong_tranche` → RAISE (bỏ qua một thành phần **bất lợi** vì thiếu dữ liệu sẽ làm kết quả lạc quan hơn thực tế); **Z0 expectancy ≤ 0 → RAISE** ("vượt ≥ 20%" là so sánh tương đối, vô nghĩa và đổi dấu tuỳ ý với mẫu số không dương). **L-Z58 đủ hai vế spec**, kèm test đòi **con số Δ_R xuất hiện trong diễn giải** chứ không chỉ nằm trong đầu người chạy. **Khoá chống hồi quy lỗi v5** (tinh thần L-Z57): 7 mức expectancy, mọi kết cục chọn Z0 đều phải nói "TIẾP TỤC", không kết cục nào chứa "DỪNG DỰ ÁN". **Kiểm có răng:** đổi thành chỉ xét MỘT chiều → đúng 3 ca đỏ, 25 ca xanh. `L-Z48c` bắt 2 chữ "R" trần trong docstring — **diễn đạt lại, không nới phép kiểm** (phiên -d8 đã cảnh báo trước). Full suite **1070 passed, 0 failed**.
| TD-0165 | 🔴 **`L-Z56` CRITICAL — bộ chạy ablation TỪ CHỐI khởi động** khi chưa có kết quả Δ_R của cổng D3.5 (cả ba bước, cả hai chiều) đã commit | ✅ | TD-0164 | Chạy bất kỳ arm ablation nào mà thiếu Δ_R đã commit → **exit ≠ 0, không chạm dữ liệu**. Đây là thứ biến "phải đo thước trước" từ kỷ luật con người thành máy từ chối | **Xác nhận** (commit `6760344`): `src/tool_d/dr015/cong_d35.py` + nối vào **E3** + 14 test `test_lz56_*`. 🔴 **Lỗ hổng phát hiện khi làm:** TD-0161 tính Δ_R trong bộ nhớ rồi ghi dòng CTRL, **KHÔNG lưu con số ra file** — nên không có gì để "đã commit", và Δ_R có thể đổi lặng lẽ SAU khi thấy kết quả ablation (đúng thứ §1 chặn). Sinh `docs/du-lieu-do/dr015-buoc1-delta-r.json` bằng cách **gọi `tinh_buoc1()`** của TD-0161, không tính lại bằng công thức riêng. **"Đã commit" kiểm hai hỏng hóc khác nhau:** git không theo dõi; hoặc có theo dõi nhưng **đã sửa sau khi commit** (con số đang chạy khác con số niêm phong). **Thêm một chốt spec không đòi:** tính lại Δ_R từ dữ liệu thô đã commit và **đối chiếu artifact** — một file commit từ tháng trước vẫn "đã commit" hoàn hảo trong khi công thức đã đổi; lệch → TỪ CHỐI, buộc commit lại **có ý thức**. **"Cả hai hướng" — diễn giải ghi rõ, không đọc theo chữ:** đòi Δ_R cho mọi hướng ablation **thực sự chạy**, đọc từ `tier_a.enable_long/enable_short`; đọc theo chữ thì với chiến lược LONG-only cổng **không bao giờ mở được**, mà chốt không bao giờ thoả được sẽ bị gỡ (bài học cổng D3). Chốt đứng **TRƯỚC** `verify_all_seals()` (rẻ trước, đắt sau); exit code riêng **98**. Test dùng **repo git THẬT** trong `tmp_path`, không mock git. **Kiểm có răng, hai phép phá độc lập:** gỡ lời gọi khỏi E3 → 2 ca AST đỏ; nới "đã commit" thành "có file" → ca CHƯA COMMIT đỏ; 11 ca còn lại xanh cả hai lần. Full suite **1085 passed, 0 failed**.
| TD-0166 | 🚪 **GATE D3.5** — điều kiện vào D4 | ✅ | TD-0161…TD-0165 | `runtime_state.json.d3_5_complete` + tag `d3-5-complete`, dùng lại khuôn `close_d3_gate()`: đòi `d3_complete`, **kiểm cây làm việc sạch**, chạy RIÊNG từng file test khoá cốt lõi (`L-Z56`, `L-Z58`) đòi số ca PASS ≥ 1. Ghi `d3_5_han_che` nói rõ Bước 2 đo **gián tiếp** từ dữ liệu khớp lệnh, không phải từ lệnh thật — và ba thứ nó không thấy được | **Xác nhận** (commit `9596beb` máy đóng cổng → `9d43ecb` đóng cổng thật → tag `d3-5-complete`): chạy THẬT trong Docker qua service **`freqtrade`**, exit 0; bốn mục evidence đều `do-duoc` (MT-10): `full_suite` **1102 passed**, `test_khoa_d3_5` (Bước 1 **23** · Bước 2 **33** · Bước 3 **14** · L-Z58 **28** · L-Z56 **14**, **mỗi file chạy RIÊNG**), `cong_d35_kiem_cong`, audit **5/14 đạt, 0 chưa đạt**. `d3_5_git_sha` **khớp đúng HEAD**, `d3_5_cay_sach: true`; chạy lại → exit **94**. 🔴 **Khác biệt lớn nhất so với ba cổng trước:** cổng gọi thẳng `kiem_cong_d35()` — **chính hàm E3 dùng để TỪ CHỐI chạy ablation** — nên điều kiện đóng cổng và điều kiện chạy ablation là **MỘT**, không phải hai danh sách song song sẽ trôi lệch. **Xác nhận nhất quán:** sau khi đóng, E3 đi qua được `L-Z56` và dừng ở `NotImplementedError` của logic ablation (việc của D4). **Δ_R niêm phong ghi vào file trạng thái:** LONG = 0,16120552895757337, SHORT = `unreadable` (**không phải 0** — N6) → ablation sau này truy được nó chạy với con số nào. **Kiểm có răng, hai phép phá:** bỏ lời gọi `kiem_cong_d35()` → 2 ca đỏ; gộp năm file thành một lượt → 1 ca đỏ; 14 ca còn lại xanh. 🔴 **`d3_5_han_che` ghi BỐN điều** vì cổng này dễ bị đọc quá tay nhất (nó có `p_nf = 0` trông rất sạch): Bước 2 đo **gián tiếp**; chỉ **LONG**; Bước 3 kết luận **tương đương** nên §4 rộng hơn bất lợi thực của DCA; `p_nf = 0` đo trên tập **ĐÃ CẤP** khớp. 📌 **Trùng lặp ghi nhận, không sửa:** vòng lặp của `close_d3_gate()` nay trùng helper `_chay_rieng_tung_file()` — không refactor code của cổng ĐÃ ĐÓNG.
| TD-0167 | 🔴 **`phan_xu()` kết luận SAI khi cả hai nhánh cùng `Δ_R = 0`** (rà soát độc lập 08/09/2026, phiên `…-91`). `buoc3_doi_chung_z0.py` chỉ kiểm `z0.value == 0` rồi trả thẳng `chi_dca_lech` kèm câu *"Z0 không lệch chút nào còn DCA có lệch"* — **không kiểm `dca.value`**. Cả hai cùng 0 thì câu đó sai sự thật, và kết luận đáng lẽ phải là `tuong_duong` (không nhánh nào lệch). Đã xác minh bằng cách gọi thẳng `phan_xu()`, KHÔNG phải suy từ đọc code | ✅ | TD-0163 ✅ | Vá đúng một điều kiện; test hiện có `test_z0_bang_0_khong_chia_cho_0` chỉ thử ca `DCA≠0` nên không lộ ra — thêm ca `Z0=0 VÀ DCA=0` phải cho `tuong_duong`. **Kiểm có răng:** gỡ điều kiện vừa thêm → ca mới phải ĐỎ. 🟡 **Không chặn tiến độ:** dữ liệu thật có `Z0 ≈ 0,155 ≠ 0` nên kết luận `tuong_duong` của TD-0163 KHÔNG bị ảnh hưởng — đây là lỗ hổng đang ngủ, vá lúc rẻ. Không đụng `buoc4_*` của TD-0164 | **Xác nhận** (commit `7fdb25c`): tách ca *cả hai cùng 0* ra trước, kết luận `tuong_duong`; tỉ lệ 0/0 vẫn `unreadable` nhưng KẾT LUẬN thì xác định. **Kiểm có răng:** vô hiệu hoá đúng điều kiện vừa thêm → **ĐÚNG 1 ca đỏ** (ca mới), 13 ca còn lại vẫn xanh. Full suite **1071 passed, 0 failed** (bỏ `test_lz56_*` — file CHƯA COMMIT của phiên đang làm TD-0165; đã kiểm `cong_d35.py` KHÔNG import gì từ file tôi sửa nên không liên quan, không phải đoán). 🔴 **Ghi nhận tai nạn N12 lần nữa khi mở việc này:** dòng TD-0167 bị commit `02b539b` (nhãn *TD-0164 hoàn tất*) của phiên khác nuốt. Không mất dữ liệu, không viết lại lịch sử. Lần này **pathspec KHÔNG cứu được** vì cả hai phiên cùng sửa `TASKS.md` — đúng giới hạn N12 mục 5 đã ghi sẵn (*pathspec chặn nuốt file LẠ, không chặn chụp nhầm DÒNG lạ trong file mình đang commit*).
| TD-0168 | 🔴 **`chi_tiet` của Bước 2 — 91 bản ghi từng ca — KHÔNG được lưu ra đâu cả** (rà soát độc lập 08/09/2026, phiên `…-d8`). `do_ty_le_khong_khop()` trả `(khoảng, chi_tiet)` và docstring nói thẳng dấu vết đó tồn tại vì *"một tỉ lệ tổng không cho biết ca nào rơi vào đâu, và khi số trông lạ thì thứ cần đầu tiên là danh sách ca"* — nhưng đường chạy thật để nó trong `$TEMP` rồi bỏ. `docs/du-lieu-do/dr015-buoc2-ty-le-khong-khop.json` chỉ có số tổng + 5 phân vị. **Lời văn hứa một thứ mà artifact không có** | ✅ | TD-0162 ✅ | Ghi **FILE RIÊNG** `docs/du-lieu-do/dr015-buoc2-chi-tiet.json`, **KHÔNG sửa** ba artifact `FILE_KET_QUA_D35` mà `L-Z56` niêm phong và cổng D3.5 đã đóng lên (`d3_5_git_sha = 9596beb`) — sửa artifact sau khi cổng đóng làm "thứ cổng đã thấy" khác "thứ trên đĩa"; và `chi_tiet` là **dấu vết chẩn đoán**, không phải kết quả, tách ra đúng vai. **KHÔNG thêm file mới vào `FILE_KET_QUA_D35`** (danh sách đóng đúng ba). Kèm **kịch bản sinh có commit** — hiện artifact Bước 2 do script tạm sinh ra, không tái lập được từ repo. Giữ `bien_pct` (biên xâm nhập %) vì đó là cột chứng minh vùng bất định rỗng không phải hiện vật \| **Xác nhận** (commit `b9d4183`): `src/tool_d/dr015/buoc2_chi_tiet.py` + `docs/du-lieu-do/dr015-buoc2-chi-tiet.json` (91 dòng) + 24 test. 🔴 **Phát hiện lớn hơn việc được giao, và nó đổi cách đọc TD-0162:** vòng đo chốt của TD-0162 **KHÔNG đi qua `do_ty_le_khong_khop()`** — nó là kịch bản viết thẳng, quét mỗi dump một lượt rồi phân loại tại chỗ (gọi hàm kia ngây thơ sẽ mở lại 45 dump / 599 MB tới 91 lần). Cùng LUẬT phân loại, khác ĐƯỜNG CHẠY ⇒ **33 phép kiểm của TD-0162 canh một hàm mà đường sản xuất chưa từng gọi**. Đây là câu hỏi chẩn đoán của dự án (*"ca sai có đi qua đường sản xuất thật không?"*) ở **chiều ngược lại**: thứ được canh không nằm trên đường chạy. Module này gọi THẲNG hàm đó, nên từ nay hàm được test là hàm sinh số. Vì là lần chạy MỚI chứ không phải dấu vết thừa kế, nó **TỪ CHỐI GHI** khi lệch artifact ở bất kỳ đại lượng nào không phụ thuộc phương pháp. **Kết quả: đường chạy độc lập TÁI LẬP ĐÚNG 8 con số niêm phong** — 3 số đếm (91/0/0), `p_nf` hai đầu (0/0), biên xâm nhập sâu nhất **−9,2828** / nông nhất **−0,1403** / trung vị **−2,1233** / **0** ca nông hơn 0,05%, độ trễ min **−179,9989** / max **+4,4055** / trung vị **−4,8849** / **76** ca xuyên TRƯỚC mốc ⇒ **xác nhận độc lập cho `p_nf = [0,0]`**, không phải chép lại. **P10/P25/P75/P90 cố ý đứng NGOÀI phép đối chiếu** (artifact không ghi cách nội suy ⇒ chênh ở đó là chênh QUY ƯỚC, mà phép kiểm báo đỏ vì lý do sai sớm muộn bị gỡ) — và quyết định đó hoá ra **đúng**: `bien.P10/P90` khớp tới 4 chữ số nhưng `tre.P25` (−47,149 vs −48,354) và `tre.P75` (−1,528 vs −1,618) **lệch thật**; ghi cả bốn ô vào artifact kèm lý do để người sau không tưởng là lệch dữ liệu. Thêm `ts_xuyen_dau_ms`+`tre_phut` vì khối `tre_so_voi_moc_backtest_phut` của artifact niêm phong **cũng không có dấu vết nào phía sau** — cùng lỗ hổng, cùng một lượt quét. `NapGomTheoNgay` giải phóng dump ngay khi hết sự kiện cần ⇒ bộ nhớ chặn theo **cửa sổ** chứ không theo tổng sự kiện, mỗi dump vẫn đọc **đúng một lượt**; lớp này bám vào THỨ TỰ gọi nên ràng buộc đó **kiểm tường minh mỗi lời gọi** — sai thứ tự làm một dòng dấu vết mang cửa sổ ca này và thời điểm xuyên ca khác, trong khi **mọi con số tổng vẫn đúng nguyên**. **Kiểm có răng trên ĐƯỜNG THẬT 91 ca** (không phải mẫu dựng tay): sửa bản sao artifact thành 76/15 → TỪ CHỐI, file **không** bị ghi. `kiem_cong_d35()` chạy thật sau khi thêm file: **vẫn xanh**, Δ_R(LONG) không đổi. Đếm `--collect-only` ngay trước khi so: **1102 → 1126 = đúng +24**; full suite **1126 passed, 0 failed**. 📌 Một ca đỏ lúc soạn là **lỗi bộ dựng của tôi**, không phải lỗi code: 35 sự kiện dùng chung một mã nên cửa sổ ±3h bao trùm cùng tập giao dịch. |

---

## Khối 16 — D4: 🔴 ABLATION D0.9, chín cấu hình × hai hướng (mở 08/09/2026)

> ✅ **Hai điều kiện chặn D4 đã gỡ:** D3.5 đóng (tag `d3-5-complete`) và **TD-0041 ✅** —
> `DSR_ADJ_EXPECTANCY_MIN = 0.10` đã điền trong `gates/thresholds.py`, không còn `inf`
> (xác minh trên đĩa, không tin chữ ghi).
>
> 🔴 **NHƯNG D4 KHÔNG PHẢI "chạy 18 backtest".** Đối chiếu chín arm với code thật trên đĩa:
>
> | Arm | Cần gì | Có chưa |
> |---|---|---|
> | Z0 | §3.3b xác nhận entry **nối vào tín hiệu vào lệnh** | module có (TD-0129), **chưa nối** |
> | Z1 | SL = 2,2×ATR cố định (công tắc thay SL neo zone) | **chưa có** |
> | Z2 | 3 tranche, không DG5, không `mult_zss` | tranche có, **công tắc chưa có** |
> | Z3 | **DG1–DG5** đầy đủ + `mult_zss` | 🔴 **DG1–DG5 KHÔNG có một dòng code nào** |
> | Z3b | Z3 + DG6 | DG6 có (TD-0123), phần còn lại như Z3 |
> | Z0-T0 / Z0-T1 | Phần 2 nối vào tín hiệu + **tắt/bật theo tầng** | module có (TD-0128), **chưa nối** |
> | Z0-V1 | tắt điều kiện (c) volume của §3.3b | **công tắc chưa có** |
> | Z0-S1 | notional CỐ ĐỊNH thay rủi ro cố định | **chưa có** |
>
> Tức phần lớn D4 là **DỰNG ARM**, không phải chạy arm. `ZoneAbsorptionMinimal` tự ghi trong
> docstring: *LONG only, chưa nối Phần 2/§3.3b vào tín hiệu vào lệnh, chưa có DG1-5*.
>
> 🔴 **HAI THỨ MỘT CHIỀU, phải chốt TRƯỚC khi chạy arm đầu tiên:**
> 1. **Tiêu ngân sách B2 = 18 trial** (9 cấu hình × 2 hướng) trên tổng `N_ĐĂNG_KÝ = 114`. Đặt
>    chỗ trial là hành động ghi sổ append-only (DR-014) — tiêu rồi không lấy lại được ngoài
>    cơ chế hoàn trả có trần 3 lần/giả thuyết.
> 2. **Hướng SHORT.** Chiến lược hiện LONG-only, và `L-Z56` sẽ **TỪ CHỐI** chạy ablation nếu
>    `enable_short: true` mà chưa có Δ_R(SHORT) — mà Δ_R(SHORT) lại cần lệnh Short để đo.
>    Spec §10.1 cho phép gỡ vòng lặp đó: *"Nếu giới hạn thời gian, ưu tiên Long trước… Short
>    chạy sau khi có DG7 và ngưỡng riêng đã calibrate."*
>
> 🔢 **Đánh số từ TD-0180, chừa trống 0169–0179** cho các phiên song song (đang ở TD-0168).
> 🔴 **Viết lại phạm vi 08/09/2026 (DR-D4-04):** khối này khi mở đối chiếu *chín arm* với code nhưng KHÔNG đối chiếu *chiến lược* với danh mục phần của spec — nên thiếu hẳn **hai tầng**: định cỡ §6.8f (MT-16) và chốt lời PHẦN 5 (MT-17). Không có chúng thì *"1 R"* không phải hằng số và §10.2 phán quyết bằng một đại lượng không có đơn vị. **Thứ tự bắt buộc: TD-0187 (định cỡ) → 0188 → 0189 → nối 0182 → 0184.** Chiến lược D4 chạy là `ZoneAbsorption.py` (MỚI); `ZoneAbsorptionMinimal` giữ nguyên làm fixture `L-Z49`.

| Mã việc | Nội dung | TT | Phụ thuộc | Tiêu chí XONG |
|---|---|---|---|---|
| TD-0180 | 🔴 **DR-D4-01 — chốt PHẠM VI và KẾ TOÁN của D4. CẦN CHỦ DỰ ÁN QUYẾT.** Hai câu: (a) chạy **Long trước** (9 trial) rồi Short sau, hay chờ dựng Short xong để chạy đủ 18 trial một lượt; (b) xác nhận tiêu **B2** trên tổng N=114, và thứ tự chạy arm. Kèm: nếu Long-trước thì ghi rõ Short là **hoãn có điều kiện**, không phải "đã đo" | ✅ | TD-0166 ✅, TD-0041 ✅ | ✅ **Chốt 08/09/2026 — `df011da`, commit RIÊNG và TRƯỚC mọi dòng mã arm.** (a) **Long trước, 9 trial**; (b) xác nhận tiêu B2, 9/110 suất còn lại. Short **hoãn có điều kiện** với **ba điều kiện mở lại viết TRƯỚC** (khuôn OQ-07) — chỉ điều kiện 2 (Δ_R(SHORT) qua `L-Z56`) có máy canh. **Đặt chỗ CẢ 9 trước khi chạy arm đầu**: dừng-giữa-chừng bị loại có ý thức vì nó làm `N` — mẫu số rào DSR §10.2 — phụ thuộc kết quả. 🔴 `Z0-T2` **KHÔNG phải arm thứ 10**, nó CHÍNH LÀ Z0 (§10.1b) — đếm nhầm là tiêu thừa 1 suất. Kèm chốt hai việc: giữ nguyên artifact D3.5 (hai quy ước phân vị cùng sống, `d1f73f2`) và xoá cache 599 MB |
| TD-0181 | 🔴 **DG1–DG5 — hiện KHÔNG có một dòng code nào.** DG1 zone còn nguyên (chưa có nến 4H đóng dưới `sl`); DG2 `trend_dir` không đổi hướng kể từ tranche 1; DG3 margin còn nguyên; DG4 trần chờ ≤ 8 nến 1H kể từ tranche 1; DG5 ZSS tính lại không giảm > 30% so với lúc tranche 1 | ✅ | TD-0180 ✅ | Năm cổng là năm hàm thuần, mỗi hàm test riêng; DG4/DG5 mang **đơn vị trong TÊN BIẾN** (`dg4_bars_1h`) theo câu hỏi mở #12 — không lặp lại lỗi "2×k" đã bị bác ở DR-010. DG5 **dùng lại** `zone_strength.zss()`, không viết lại phép tính \| **Xác nhận** (commit `e801a9e`): `src/tool_d/dg1_dg5_tranche_gates.py` + 45 test. 🔴 **PHÁT HIỆN CẦN CHỦ DỰ ÁN QUYẾT — ngưỡng 30% của DG5 là một BẬC TỰ DO KHÔNG AI ĐẾM:** §4 định nghĩa DG5 là *"ZSS không giảm > 30%"*, nhưng `tool_d_config.yaml` **không có khoá nào** cho nó (`tier_b` đủ 12 mục, không mục nào là DG5) và **bảng kiểm kê DOF không có dòng nào** cho DG5 (có đủ DG4, DG6-A/B/C-D, DG7, `max_hold_bars`). Đúng hình dạng thiếu sót mà chính bảng đó đã tự sửa một lần — dòng DG7 ghi *"(0 — bỏ sót)"*. **Không tự điền** vì thêm khoá vào `tier_b` là **+1 DOF**, mà `N_ĐĂNG_KÝ = 114` là mẫu số rào DSR §10.2 đã cam kết ⇒ quyết định kiến trúc (quy tắc 2). Nên `nguong_giam_toi_da` là **tham số BẮT BUỘC, KHÔNG mặc định** — cùng cách `do_ty_le_khong_khop()` bắt buộc `truoc_ms`/`sau_ms`: không mặc định thì không ai vô tình chạy bằng một con số chưa ai duyệt, và không có hằng số lậu nằm ngoài kiểm kê. 🔴 **KHÔNG dùng chung hằng số với DG6** (test AST ghim): `dg4_bars_1h` là 🟡 tunable #7 còn `dg6b_bars_1h` là 🔒 đóng băng `dof: -1` ghi chú *"= dg4_bars_1h"* — cùng bằng 8 nhưng **khác hạng**; gộp làm một là biến tham số đóng băng thành tune được, đúng chiều nới lỏng kiểm kê DOF tồn tại để chặn. **Ba diễn giải ghi ra để cãi lại được:** DG2 so **đồng nhất** nên `UP → FLAT` cũng fail (FLAT = *"chưa có bằng chứng trend"*, và §2.5 đã tự loại FLAT ở bước vào lệnh — cho phép bơm thêm tiền vào bối cảnh mà hệ thống không cho mở lệnh mới là tự mâu thuẫn); DG1 dùng giá **đóng cửa** chứ không phải `low` (râu xuyên rồi thu lại chính là hành vi hấp thụ đang đo); và **ghi nhận DG1 gần như không bao giờ fail** khi SL là lệnh thật — giá chạm `sl` là vị thế đã đóng, không còn tranche để xét, khớp spec dòng 998 (*"DG1 và SL là cùng một điều kiện nhìn từ hai góc"*); cổng vẫn cài đủ theo chữ §4, ghi ra để không ai đọc `DG1: true` rồi tưởng nó mang nhiều thông tin hơn thực tế. **Fail-closed:** đầu vào hỏng **RAISE** chứ không trả `False` (`False` = *"đã xét và cổng đóng"*, khác hẳn *"không xét được"*) — margin âm, số nến âm / trần = 0, `zss_tai_tranche1 ≤ 0` (với `zss1 = 0` thì `zss ≥ 0` luôn đúng ⇒ **cổng chết đội lốt cổng đang chạy**), và ngưỡng ngoài `(0,1)` (truyền `30` thay vì `0.30` làm DG5 **bị tắt mà không ai biết**). **KHÔNG có công tắc bật/tắt cổng** — Z2 tắt DG5 là TD-0183, có test ghim; `danh_gia_tat_ca()` cũng **không gộp** năm `bool` thành một, ai cần "được bơm không" thì tự `all(...)` và khi đó nhìn thấy mình đang bỏ thông tin cổng nào đã chặn. **Kiểm có răng, BA phép phá độc lập, mỗi lần ĐÚNG 1 ca đỏ / 44 xanh:** thêm mặc định `0.30`; nới DG2 thành chỉ-đảo-chiều-thật; bỏ chốt `zss1 ≤ 0`. Đếm `--collect-only` ngay trước khi so: **1141 → 1186 = đúng +45**; full suite **1186 passed, 0 failed**. 📌 Một test tôi viết đầu bắt NHẦM — nó tìm chuỗi `SO_NEN_TOI_THIEU_B` và bắt luôn chỗ **docstring nhắc tên đó để giải thích vì sao không dùng**, đúng hình dạng lỗi `L-Z25` từng gặp với `hyperopt` trong một câu văn; sửa sang hỏi bằng **AST**: *"có DÙNG không"*, không phải *"có nhắc tới không"*. |
| TD-0169 | 🔴 **DR-D4-02 — ngưỡng 30% của DG5 là BẬC TỰ DO KHÔNG AI ĐẾM. CHỦ DỰ ÁN ĐÃ QUYẾT: ĐÓNG BĂNG.** §4 định nghĩa DG5 là *"ZSS không giảm > 30%"*, nhưng `tool_d_config.yaml` không có khoá nào cho nó (`tier_b` đủ 12 mục) và `config/dof_inventory.yaml` **không có dòng nào cho DG5** (§4 chỉ có DG4). Phát hiện khi làm TD-0181; e8 xác nhận độc lập | ✅ | TD-0181 ✅ | Vào **`tier_frozen`** + một dòng trong `khong_dem_vao_dof_goc`, **KHÔNG** vào `tier_b`. Giữ `N_ĐĂNG_KÝ = 114`, rào DSR 3,0777 không đổi, **0 trial**. Phương án `tier_b` bị loại có ý thức: N → 120 (**+6 trial**), rào DSR +0,54%, và phải sửa `dof_goc` 28 → 29 tức nói con số spec **trích nguyên văn** là thiếu. Tiền lệ đã có sẵn trong chính bảng: `adx_threshold` ghi *"chưa từng có trong 26"*, `dg6b_bars_1h` đóng băng `dof: -1`. **L-Z29 phải VẪN xanh** (`dof_goc` 28, `tier_b` 12, N 114 — cả ba không đổi). DR commit **RIÊNG và TRƯỚC** mọi dòng cấu hình | **Xác nhận** (commit `9cac982` DR → `28de9cb` cấu hình): `tier_frozen.dg5_zss_decay_max: {value: 0.30, dof: 0}` + một dòng `khong_dem_vao_dof_goc` + 14 test khoá. **`dof: 0` chứ KHÔNG phải `-1`** — `dof` âm nghĩa là *đóng băng thứ ĐÃ được đếm nên trừ đi bấy nhiêu* (`dg6b_bars_1h: -1`); DG5 chưa từng được đếm nên trừ đi là **trừ khống**, đúng LỖI 1 của v5 mà bảng đó sinh ra để chặn. **Bất biến giữ nguyên:** `dof_goc` 28 · `|tier_b|` 12 · **N = 114** · rào DSR **3,0777** · **0 trial**. **Kiểm có răng:** dời khoá sang `tier_b` → **8 ca đỏ**, gồm cả ba con số đắt nhất (N=120, `|tier_b|`=13, L-Z29 lệch); hoàn nguyên bằng phép ĐẢO chính xác, `git diff --stat` xác nhận đúng 7 dòng thêm / 0 xoá nên không nuốt sửa đổi của phiên song song. 🔑 **Phân biệt e8 rút ra, đáng giữ:** cùng động tác đóng băng nhưng hậu quả TRÁI NGƯỢC tuỳ *thứ này đã nằm trong 12 chưa* — DG5 chưa (N giữ 114), còn `v_min` rồi nên đóng băng kiểu đó sẽ hạ N 114→108 và **NỚI** rào DSR (DR-D4-03). 📌 **Commit TRƯỚC khi có lượt suite sạch, cố ý và có ghi nợ trong commit message:** phiên `-d5` đang bị chặn ở chính file YAML này; giữ file làm con tin để chờ một lượt suite mà Docker khi đó KHÔNG cho chạy là chặn người khác bằng điều kiện mình không kiểm soát được. Nợ đã trả: full suite **1244 passed, 0 failed**. 
| TD-0182 | **Nối Phần 2 (trend filter) + §3.3b (xác nhận entry) vào TÍN HIỆU VÀO LỆNH của `ZoneAbsorption.py`** (chiến lược SẢN XUẤT — DR-D4-04; **KHÔNG** sửa `ZoneAbsorptionMinimal`, nó là fixture `L-Z49`), kèm công tắc theo tầng cho arm Z0-T0 (bỏ hết) / Z0-T1 (chỉ giữ 4H) / Z0-T2 (đầy đủ = Z0) | ✅ | TD-0180 ✅, **TD-0187** | ✅ Đã xong phần độc lập: `arms.py` (`d87203a`) + MT-15 (`942efe7`, `(a VÀ c) HOẶC (b)`, `v_min` FROZEN 1.0). Phần NỐI chờ TD-0187 vì tín hiệu vào lệnh chỉ có nghĩa khi cỡ lệnh đúng. Công tắc đọc từ `tool_d_config.yaml` (N4), KHÔNG từ env (L-Z39). 🔴 **DG8 BẬT trong CẢ Z0-T0 và Z0-T1** cùng `max_hold_bars` như mọi arm — DG8 là thuộc tính khung thử nghiệm, KHÔNG phải biến so sánh (§4b.4) **Xác nhận** (commit `7c06a8d`): nối qua `du_dieu_kien_trend_theo_tang()`, `TANG_THEO_ARM` 4 → 10 khoá. 🐛 **Lỗi chỉ lộ trên dữ liệu THẬT:** `zone_valid_4h` mang `NaN` ở vùng warmup, pandas từ chối mảng có `NaN` làm mặt nạ ⇒ backtest CRASH; bộ sinh tổng hợp không có vùng đó. 🔴 **Fixture phải sửa vì hệ thống đổi, không phải để test xanh:** fixture cũ khớp đủ 3 tranche do TRÙNG HỢP — hệ thống cũ không lọc trend nên lệnh mở lúc 4H đang DOWN ⇒ `t4=DOWN` ⇒ DG2 so `DOWN==DOWN` cho qua; hệ thống mới bắt vào lệnh khi 4H UP nên cú lùi làm DG2 chặn. Tức fixture cũ nghiệm thu cỗ máy DCA bằng **một lệnh mà hệ thống mới sẽ không bao giờ mở**. Sửa: pha giảm 60 ngày (chuỗi 1D có cross EMA thật) + đáy chữ V 60 → 20 (sweep: 60→FLAT,DOWN,DOWN,DOWN · 30→UP,UP,UP,FLAT · 20→UP,UP,UP,UP — chọn giá trị ĐẦU TIÊN đủ) + mốc thời gian neo ở ĐUÔI. 📌 **Bẫy PASS RỖNG tự tạo rồi tự bắt:** ca `test_enter_tag_...` chép hằng số `"2025-01-01"` lần thứ BA trong file; kéo dài lịch sử làm chỉ số trỏ vào **nến 513 giá 44,30** — cách nến tín hiệu **60 ngày** — mà **vẫn XANH** vì nến sai tình cờ cũng `UP`. Thay bằng `_moc_bat_dau()` suy từ đuôi. 🔑 **DG2 đo trên dữ liệu THẬT (48 mã alt EXPLORE, 0 trial):** chặt 21,0% vs lỏng 22,2% đủ ba tranche ⇒ cách đọc chặt chỉ tốn **1,2 điểm phần trăm**, GIỮ NGUYÊN, không mở DR. Full suite **1400 passed**. |
| TD-0183 | **Công tắc arm còn lại:** Z1 (SL = 2,2×ATR cố định), Z2 (không DG5, không `mult_zss`), Z0-V1 (tắt điều kiện (c) volume §3.3b), Z0-S1 (notional cố định thay rủi ro cố định) | ✅ | TD-0181 ✅, TD-0182 🔒 (phần `arms.py` đã commit) | Mỗi công tắc một test chứng minh nó **đổi hành vi thật**, không phải cờ chết. Z0-S1 là arm **robustness**: nó không phụ thuộc `R_eff` tính đúng — nếu `Z0-S1 > Z0` thì đó là tín hiệu `R_eff` đang SAI (phân loại L2), không phải "vốn cố định tốt hơn" 🔴 **PHẠM VI RÚT GỌN — chủ dự án duyệt 08/09/2026: 2,5/4 công tắc.** **LÀM:** Z1 (`trade_plan.py`) · Z0-S1 (`notional.py`) · nửa Z2 vế *không DG5* (nối `danh_gia_tat_ca()` của TD-0181). **KHÔNG LÀM, có lý do, không phải bỏ sót:** (1) **Z0-V1** — MT-15 chặn nguyên vẹn, `(c)` chưa có dòng code nào nên `Z0-V1` hiện TRÙNG `Z0`; (2) vế **không `mult_zss`** của Z2 — `mult_zss` **không có ở tầng tính cỡ lệnh** (grep: chỉ có trong `ZoneDetectionProbe.py` sinh một CỘT để xem; `tranche1_notional()` nhận thẳng `rho_pct`, không nhân hệ số nào), nên *tắt một thứ chưa bật* là **CỜ CHẾT** — đúng thứ chính dòng này cấm. Hệ quả phải ghi khi đọc kết quả: spec dòng 4164 nói cặp Z2/Z3 trả lời *"DG5 + mult_zss có giá trị đo được"*, nhưng thực tế **chỉ đo được DG5** — arm đo ÍT hơn thứ nó khai. | **Xác nhận** (commit `39669e0`): `src/tool_d/arm_switches.py` + 27 test. Module RIÊNG, không gộp `arms.py` — `arms.py` chọn *bao nhiêu tầng lọc trend*, module này chọn *cách tính SL / cách tính cỡ lệnh / cổng nào được xét*. **BỌC chứ không sửa chữ ký** `tinh_ke_hoach()`/`tranche1_notional()`: hai hàm đó có người gọi thật (`ZoneAbsorptionMinimal.py`, `build_pool.py`, nhiều test), thêm tham số bắt buộc là bắt cả hệ thống khai một lựa chọn ablation ở chỗ không liên quan — đường chạy thường **không đổi một dòng**. **Z1:** SL = `p1 − 2,2×ATR`; 🔴 `r_eff_plan` **TÍNH LẠI** theo SL mới — bỏ sót là lỗi im lặng đắt nhất ở đây vì `r_eff` chảy thẳng vào cỡ lệnh (§6.8e), một Z1 mang `r_eff` của zone sẽ vào lệnh với cỡ của arm KHÁC mà bảng kết quả vẫn bình thường. Dùng `atr_4h` cho cả hai chế độ là **diễn giải** (spec không nói Z1 lấy ATR khung nào) — cùng khung để chỉ CÔNG THỨC khác nhau, khác khung thì kết quả trộn hai nguyên nhân. **Z0-S1:** không chia `R_eff`, có test đổi `r_eff` gấp ba và đòi kết quả KHÔNG đổi (test chỉ hỏi *có nhánh riêng không* sẽ không bắt được ca nhánh riêng vẫn lỡ chia). 🔴 **`notional_co_dinh_usdt` BẮT BUỘC, không mặc định** — cùng chốt với ngưỡng DG5: spec KHÔNG cho con số này ở đâu (§10.1b chỉ nói *notional CỐ ĐỊNH*; *300 USDT* dòng 1285 nằm trong lập luận minh hoạ), `tool_d_config.yaml` không có khoá — **cần chủ dự án quyết, đã báo**. **Z2:** bỏ ĐÚNG DG5, giữ DG1–DG4; arm ENTRY ĐƠN → `cong_ap_dung()` trả tuple RỖNG và `duoc_them_tranche()` RAISE (trả `True` cho arm không có tranche 2/3 là câu trả lời sai cho câu hỏi lẽ ra không được đặt). `ARM_HOP_LE` có test khớp `ARM_B2_COUNT = 9`; `Z0-T2` KHÔNG nằm trong đó. **Hai vế KHÔNG làm có test ghim** để không lặng lẽ quay lại dạng cờ chết. 🐛 **Test bắt lỗi thật trong chính code này:** chốt đầu chỉ bắt `sl ≥ p_avg`, bỏ lọt ca ATR lớn làm `sl` **ÂM** (vẫn < `p_avg` nên lọt) và `r_eff` ra **234%** — thêm chốt `sl ≤ 0`. **Kiểm có răng, BA phép phá, mỗi lần bắt đúng công tắc bị vô hiệu:** bỏ `CHE_DO_SL_THEO_ARM["Z1"]` → 3 đỏ (đều lớp Z1); cho `NOTIONAL_CO_DINH` chia lại `r_eff` → 2 đỏ (gồm ca *không phụ thuộc R_eff*); cho Z2 giữ DG5 → 3 đỏ. Đếm `--collect-only` ngay trước khi so: **1217 → 1244 = đúng +27**; full suite **1244 passed, 0 failed** (4:26). 📌 Một test tôi viết đầu dùng phép cắt chuỗi rối rắm dễ thành RỖNG — thay bằng **AST**, cùng bài học `L-Z25`/TD-0181: hỏi *có DÙNG không*, không phải *có NHẮC TỚI không*. 
| TD-0187 | 🔴 **TẦNG ĐỊNH CỠ LỆNH §6.8f BƯỚC 1 — hiện 0 dòng code (MT-16, DR-D4-04).** Dựng `src/tool_d/sizing.py` (thuần) + `user_data/strategies/ZoneAbsorption.py` (file MỚI, sản xuất) với `custom_stake_amount()`: `rho_eff = rho × 6 mult` · `N_full = rho_eff × E_D / R_eff` · stake tranche i = `N_full × w_tranche[i]` (⅓⅓⅓, `tier_frozen`). Nối DG1–DG5 (TD-0181) vào `adjust_trade_position` | ✅ | TD-0181 ✅, TD-0169 ✅ | ✅ **Xong 08/09/2026** — `c0ba7a0` (`sizing.py`, 35 test đơn vị) + `bfe01ed` (`ZoneAbsorption.py` MỚI, 9 test khoá đo trên **FILL backtest THẬT**). Full suite **1288 passed, 0 failed**. Đo được: tỉ trọng cost **1:1:1** (MT-16 là 1:1:2) · `leverage = 3` (trước: 1x) · `stake × L == cost` · `cost₁ = rho×E_D×mult_zss/R_eff × w₁` ±1% · cùng zone & giờ mở với Minimal. Phá thật (trả `trade.stake_amount` về) → **đúng 3 ca đỏ**. 🔴 **Ba lỗi chỉ lộ khi đòn bẩy THẬT** (MT-16 vi/vii): SL bị nhân đòn bẩy (`Minimal:256` mang y hệt, vô hình ở 1x) · Freqtrade **NUỐT** exception callback (`rc = 0`, lệnh biến mất im lặng) → test khoá nay CẤM dòng đó · `cost.min` sàn = 50 USDT (BTC) mà TD-0082 chưa tính `mult_*`/đòn bẩy. 🔴 Vá thêm **lookahead ở callback**: `dp.get_pair_dataframe()` trong callback trả cả nến TƯƠNG LAI — `_df_4h()` cắt `date+4h ≤ now`; `H4-D` không phủ mặt cắt này (test khoá riêng: phiên `-f6`) \| 🔴 **KHÔNG trả `trade.stake_amount`** ở tranche 2/3 — đó là TỔNG hiện tại, chính là bug sinh ra tỉ trọng ¼¼½ (MT-16 cơ chế). `N_full` đọc từ kế hoạch chốt lúc tranche 1 (`custom_data`, đường L-Z49), không tính lại. **Mọi `mult_*` ≤ 1.0** — test khoá không đường nào sinh `> 1.0`. Sáu hệ số ghi TƯỜNG MINH vào Decision Log; `mult_edge = 1.0` trong backtest (spec dòng 1774), `mult_dd = 0.0` là HALT không phải size 0. **Test khoá PHẢI đo từ backtest THẬT:** tỉ trọng tranche từ `cost` = ⅓⅓⅓ ± 1%, và stake **biến thiên theo `1/R_eff`** giữa các lệnh — không mock, không dựng tay (bài học TD-0168) |
| TD-0188 | 🔴 **KẾT NẠP DANH MỤC §6.8f BƯỚC 2 — hiện 0 dòng code.** Chỉ mở lệnh nếu SAU KHI mở vẫn thoả CẢ HAI: (a) Σ rủi ro mọi vị thế (kế hoạch đầy đủ, tương quan = 1) ≤ `daily_loss_budget_pct × E_D`; (b) Σ margin (kế hoạch đầy đủ) ≤ `0.85 × E_D` | ✅ | TD-0187 ✅ | `max_open_trades = 100` trong `config.json` chỉ là trần an toàn phía Freqtrade — số vị thế thật là KẾT QUẢ của phép kiểm này, không phải tham số (§6.8f "hệ quả"). Nối qua `confirm_trade_entry()`. Test khoá: dựng danh mục sát trần rồi thêm một lệnh → từ chối; kiểm cả hai vế riêng | ✅ **Xong 08/09/2026** — `395516e` (`admission.py` MỚI, 17 test đơn vị + nối vào `ZoneAbsorption`). Bảng ví dụ §6.8f tái lập ĐÚNG: R_eff 0,9/1,5/2,0/3,0% → **6/10/13/20 mã**. 🔴 **Phá thật bắt được PASS RỖNG chưa từng gặp:** bỏ đọc danh mục → **0 ca đỏ** (backtest 1 mã nên danh mục luôn rỗng) — thứ được canh nằm ĐÚNG trên đường chạy nhưng đường chạy KHÔNG BAO GIỜ đi qua nhánh cần canh. Vá bằng lượt backtest **hai mã**, đòi tổng margin lần kết nạp sau LỚN HƠN lần trước (bằng chứng số học) → phá lại ra đúng 2 ca đỏ. Bắt kèm bug thật: `Trade` đang mở mà lệnh CHƯA khớp làm `AdmissionError` bị Freqtrade NUỐT — đọc `custom_data` trước, kế hoạch chờ sau.
| TD-0189 | 🔴 **CHỐT LỜI PHẦN 5 (MT-17, DR-D4-04 §4).** TP1 = zone đối diện gần nhất (cùng `zone_detection`, `loai="dinh"`) trừ hao `tp1_haircut_pct`, chốt 50%; TP2 = trail `ATR(14,1H) × tp2_trail_atr` sau TP1; fallback `p_avg + 1.5 × R_eff` khi không có zone trong `4.0 × R_eff`, tag `tp_source: "fallback_r_multiple"` | ✅ | TD-0187 ✅ | 🔴 `4.0`/`1.5` nhân **`R_eff` — KHOẢNG GIÁ đóng băng lúc entry**, không phải R tiền tệ; code/log viết `4.0 × R_eff`, không bao giờ "4R" (v8 dòng 1667). Ghi **chỉ số H-4** (tỉ lệ dùng fallback) từ D4; > 40% ⇒ L2, KHÔNG tune nạng. **DG8 áp dụng KHÔNG ĐIỀU KIỆN kể cả khi TP2 đang trail** (câu hỏi mở #11, mặc định v3.2). ~~⚠️ Chặn bởi một con số: `tp1_haircut_pct` không có trạng thái trong `param_status.yaml`.~~ ✅ Đã gỡ bởi TD-0190 (`FROZEN`, `chua_calibrate: true`) — xem test `test_tp1_haircut_pct_co_TRANG_THAI`. **Xác nhận chặng 2b, 09/09/2026 (`1b90456`):** TP1 nối qua `adjust_trade_position` (`stake_amount` ÂM ⇒ `ExitType.PARTIAL_EXIT`, đọc mã nguồn Freqtrade trước khi viết — N7/rule 6), chốt đúng 50% theo AMOUNT; TP2 nối qua `custom_exit` (trail trên đỉnh giá TỪ MỐC TP1 khớp, không phải từ lúc mở lệnh). `KeHoachChotLoi` thêm `zone_gia_goc`; `custom_data["chot_loi"]` mang đủ `tp_zone_age_bars` (DR-D4-06 ràng buộc 1) cho TD-0184 đọc sau. `_cap_nhat_chot_loi()` tính lại TP1 ở MỌI lần entry khớp theo DR-D4-06 §3 ràng buộc 5. 🔴 **Chốt mới, hỏi chủ dự án ngay khi đo bắt được ca thật:** ĐÃ CHỐT TP1 THÌ KHÔNG DCA THÊM (dù giá tụt về đúng p2/p3) — tranche 2/3 bảo vệ một thesis chưa xác nhận, TP1 xác nhận ngược lại; không phải chữ tường minh của spec, ghi là DIỄN GIẢI. Kèm 2 vá bắt buộc trong `test_td0187_dinh_co_lenh_backtest_that.py` (đã báo phiên khác trước khi sửa): nến "chạm p1 → tranche 1" hạ wick (với `--timeframe-detail 5m`, `adjust_trade_position` thấy giá ở độ phân giải 5 phút cho vị thế ĐÃ MỞ, wick cũ chạm luôn ngưỡng TP1-nạng ngay sau tranche 1 — cùng hình lỗi TD-0182/TD-0194); `test_stake_la_ky_quy_bang_cost_chia_L` đổi fallback sang `max_stake_amount` (đỉnh trước khi có exit, không phải `stake_amount` hiện tại). Test mới `test_td0189_chot_loi_noi_vao_chien_luoc.py` (8 ca, kiểm-có-răng bằng AST cho chốt không-DCA — đã thử phá-thật-chạy-lại-backtest trước, TP2 tự đóng hết vị thế trước khi giá chạm p2/p3 nên không phân biệt được hai nhánh, ghi lại lý do đổi hướng trong docstring). Docker: **1467 passed, 0 failed**. ⚠️ **Chưa làm trong chặng này:** `chi_so_h4()` (H-4 tổng hợp D4) là việc của bộ chạy E3 (TD-0184), đọc từ Decision Log — không tính trong `ZoneAbsorption.py` |
| TD-0170 | 🔴 **LOOKAHEAD Ở CALLBACK — mặt cắt `H4-D` KHÔNG phủ** (phiên `-d5` phát hiện khi rà tĩnh `ZoneAbsorption`, 08/09/2026). Trong backtest `dp.get_pair_dataframe()` trả **TOÀN BỘ** dữ liệu đã nạp, kể cả nến TƯƠNG LAI; chỉ `get_analyzed_dataframe()` mới bị cắt. Bản nháp đầu lấy `iloc[-1]` khung 4H trong callback ⇒ **DG1 (`close_4h_ke_tu`), DG2 (`trend_4h_hien_tai`), DG5 (`zss_hien_tai`) đều "biết" giá sắp tới**. Cùng họ lỗi B2 mà `H4-D` canh ở zone detection, nhưng `H4-D` chỉ canh `populate_*`, **không canh callback**. `-d5` đã vá bằng `_df_4h(pair, current_time)` (cắt `date + 4h ≤ now`) — nhưng **chưa có test khoá nào** giữ nó | ✅ | TD-0181 ✅, TD-0187 ✅ | Test phải đi qua **ĐƯỜNG BACKTEST THẬT** (`fixture kq_san_xuat` của TD-0187 sinh lệnh đủ 3 tranche nên cả ba cổng thực sự được gọi), **không dựng tay** — đúng bài học TD-0168. Ba lớp: (a) gọi chính `_df_4h` tại **mốc quyết định THẬT** lấy từ kết quả backtest, đòi không nến nào `date + 4h > t` **và** số nến TĂNG theo t (chỉ 'có cắt' thì một cài đặt trả rỗng cũng qua); (b) chốt **AST**: `get_pair_dataframe` xuất hiện ĐÚNG MỘT lần trong file và nằm trong `_df_4h`, cả ba helper đều đi qua nó — bịt ca 'callback mới thêm sau đọc thẳng df'; (c) kiểm có răng bằng cách **bỏ phép cắt** → ca (a) phải đỏ. 🐛 **Kèm vá một lỗi trong `dg1_dg5_tranche_gates` do chính TD-0170 phát hiện:** `dg5_zss_khong_suy_yeu()` nhận `NaN` thì **trả `False`, KHÔNG raise** (`NaN >= x` là `False` theo IEEE-754) — tức *không đo được* bị gộp thành *cổng đóng*, đúng hai thứ `TrancheGateError` sinh ra để tách và N6 cấm gộp. `ZoneAbsorption.py:505` đang ghi *"dg5 fail-closed với NaN (TrancheGateError)"* — **chú thích nói sai về code**, cùng hạng `_comment_stake_amount` của MT-16 \| **Xác nhận** (commit `758d3c6`): `tests/lock/test_td0170_lookahead_cong_dg.py` (14 ca) + vá `NaN` trong `dg1_dg5_tranche_gates`. 🔴 **Loại lỗi này MÙ với toàn bộ suite hiện có:** bỏ phép cắt **không làm đỏ một khẳng định nào** — nó chỉ khiến mọi con số **TỐT LÊN**; nên mọi mốc xanh kể từ khi `ZoneAbsorption` ra đời đều **không loại trừ được** lookahead. Vì thế ca đối chứng âm phải **THƯỜNG TRỰC** trong suite, không phải một phép phá tay. **Ba lớp:** (a) hành vi trên **mốc quyết định THẬT** — tái dùng `_chay()` của TD-0187 qua `importlib` (không dựng bộ chạy thứ hai), mốc lấy từ `order_filled_timestamp` của lệnh **đủ ba tranche** (tranche 2/3 mới là lúc ba cổng được hỏi — bài học TD-0188); ba bất biến: không nến nào `date+4h > t`, **số nến TĂNG** theo mốc (bản trả rỗng hoặc trả cùng lát cắt cũng thoả bất biến đầu), và lát cắt phải **bỏ đi** một phần dữ liệu thật. (b) **AST** — không hàm nào ngoài `populate_indicators`/`_df_4h` đọc thẳng `get_pair_dataframe`; ba helper đều gọi `_df_4h`; `_df_4h` nhận `current_time` chứ không `datetime.now()` (trong backtest `now()` là thời gian THẬT, sẽ không cắt gì). 📌 Bản đầu đòi *đúng MỘT lần trong file* và **đỏ ngay** — siết quá tay vì `populate_indicators` đọc thẳng là HỢP LỆ (`H4-D` có phủ); sửa thành danh sách CHO PHÉP. (c) **đối chứng âm thường trực** + một ca **tự-bắt**: nếu dữ liệu 4H kết thúc trước mọi mốc quyết định thì bản không-cắt cũng thoả và cả bộ test thành vô nghĩa **mà vẫn xanh** — cần vì đuôi 30 nến của bộ sinh được `-d5` thêm để lệnh có chỗ đóng theo DG8, **không phải** để phép cắt có nghĩa. 🐛 **Vá lỗi trong module của tôi:** `dg5_zss_khong_suy_yeu()` nhận `NaN` **trả `False` chứ không raise** (`NaN >= x` là `False` — IEEE-754), tức gộp *không đo được* vào *cổng đóng*; đường chạy thật đưa `NaN` vào thật (`_zss_hien_tai()`). Và `ZoneAbsorption.py:505` đã ghi sẵn *"dg5 fail-closed với NaN"* — **chú thích nói sai về code**, cùng hạng `_comment_stake_amount` (MT-16). **Kiểm có răng, CÔ LẬP:** chép chiến lược ra `tempfile.mkdtemp()`, bỏ phép cắt **trên bản chép**, nạp bằng `importlib` → **cả ba bất biến đều bắt được** (`[100, 100, 100]` — số nến đứng im). Baseline đo lại ngay trước khi so: **1311 → 1325 = đúng +14**; full suite **1325 passed, 0 failed**. 🔴 **Bài học về CÁCH LÀM, đắt hơn phần code:** lần phá đầu tôi ghi thẳng vào `ZoneAbsorption.py` — file thuộc sân phiên khác — không báo trước; họ ghi đè giữa chừng, phép phá biến mất, lượt chạy báo *12 passed*: **một phép đo HỎNG trông y hệt một phép đo ĐẠT**. Không mất dữ liệu (`cmp` từng byte), nhưng nếu trúng lúc họ đang đo thì họ nhận một **mốc xanh SAI** trên chiến lược đang lookahead. Ba vế: `git checkout` không cứu file chưa commit · `cp` sao lưu cũng không cứu nếu người khác ghi đè trước · và **mọi cơ chế hoàn nguyên đều diễn ra SAU khi cửa sổ đã mở**. ⇒ quy ước hai phiên có vế thứ ba: **không ghi vào file thuộc sân phiên khác, kể cả tạm** — ràng buộc theo *ai sở hữu file*, không theo *ai đang chạy gì*. |
| TD-0190 | **Rà TOÀN BỘ dấu `[CẦN CALIBRATE]` trong spec (25 chỗ) đối chiếu `param_status.yaml`** — `v_min` và `tp1_haircut_pct` chắc chắn không phải hai cái duy nhất | ✅ | — | Bản siết L-Z15 (DR-D4-03 §6) chỉ soi mục **đã khai**; mục chưa khai vẫn im lặng. Đầu ra: danh sách đầy đủ vào `param_status.yaml`, mỗi mục TUNED / FROZEN / TUNED_PENDING (chỉ khi còn null). Không tự điền giá trị — chỉ khai trạng thái cho cái đã có, liệt kê cái chưa có | **Xác nhận** (commit `93561b3`): `L-Z15` duyệt **mọi khoá `tier_b`** thay vì danh sách tự khai, + 12 mục `param_status.yaml`, + 15 test khoá. 🔴 **Kiểm kê phát hiện:** DR-D4-03 §6 đặt `param_status.yaml` làm *nguồn sự thật cho DANH SÁCH*, nên phép kiểm chỉ soi mục **có người tự khai** — danh sách khi đó có **ĐÚNG MỘT** mục, trong khi **11/12** tham số mang giá trị mà không trial, không `frozen_rationale` (sổ thật: `param_under_test` **chưa từng** là một khoá `tier_b`). 🔑 **Hình dạng lỗi THỨ BA của dự án:** không phải *lớp canh cùn* (19/19 phép phá đều bị bắt), cũng không phải *chĩa nhầm hướng* (ca sai chỉ đi qua mẫu dựng tay) — mà là **NGƯỜI BỊ CANH TỰ CHỌN PHẠM VI BỊ CANH**; một danh sách tự khai thì bỏ trống luôn hợp lệ, phép kiểm xanh vĩnh viễn mà không ai nói dối câu nào. **`FROZEN` là trạng thái DUY NHẤT nói thật được**, không phải lựa chọn: `TUNED` đòi trial (không có), `TUNED_PENDING` bị DR-D4-03 cấm khi đã có giá trị. **Nhóm B — phát hiện phụ:** `dg6d_retrace_frac` · `mult_corr_thresholds` · `dg7_funding_frac` **KHÔNG mang dấu `[CẦN CALIBRATE]` nhưng vẫn tunable**, tức lọt CẢ HAI lưới — chính là lý do danh sách phải lấy từ `tier_b` chứ không từ dấu trong spec. 🔴 **Bổ sung của chủ dự án ngoài đề xuất ban đầu — hoá ra là lớp canh DUY NHẤT:** trường máy đọc `chua_calibrate` + `L-Z15` **in con số ngay cả khi ĐẠT** (*"12/12 đã khai; 12 ở CHỖ GIỮ chưa calibrate, 0 đã TUNED bằng trial"*). Thêm vì lý do **tài liệu** — để cái xanh không bị đọc thành *"đã QUYẾT đúng"* thay vì *"đã KHAI"* — nhưng phép phá cô lập cho thấy: bản thu hẹp phạm vi trở lại **vẫn trả `r.ok = True`** trên file THẬT, và **chỉ khẳng định `"12/12"` bắt được nó**. Một tính năng thêm vì đọc-hiểu hoá ra là chốt duy nhất có răng. ⇒ Đề nghị **TD-0186 ghi con số đó vào `d4_han_che`**. Hai ca ghim hiện trạng **LẬT NGƯỢC, KHÔNG xoá** (tiền lệ TD-0150): TD-0056 `r.ok` (xanh vì phạm vi hẹp) → TD-0190 `r.is_fail` (đỏ đúng sự thật) → nay `r.ok` (xanh vì **đã khai đủ**). 🐛 Bắt được lỗi của chính tôi lúc lật: docstring còn nói *"phép kiểm ĐỎ đúng sự thật"* trong khi code đã assert `ok` — **chú thích nói ngược với code**, đúng thứ vừa bắt hai lần trong ngày ở file phiên khác. `tp1_haircut_pct` nay có trạng thái, gỡ chặn cho TD-0189. **Bằng chứng:** 36 ca xanh; full suite **1337 passed, 0 failed** đo trong **phòng sạch dựng bằng `git clone --local` từ HEAD** — cần vì cây chung đang có 125 dòng chưa commit của một phiên **đã mất** làm backtest sinh **0 lệnh** (17 ca đỏ, một gốc, không liên quan TD-0190). Clone chứa commit này và KHÔNG chứa phần dở dang. 
| TD-0171 | 🔴 **MỞ LẠI TD-0082 — sàn min-notional đo trên MỘT CÔNG THỨC KHÔNG CÒN LÀ CÔNG THỨC CỦA HỆ THỐNG.** `docs/min-notional-check.md` kết luận *"102/102 qua, KHÔNG có vi phạm"* bằng `tranche1_notional(rho_pct = rho)` — nhưng TD-0187 dựng tầng định cỡ thật thì cỡ lệnh là `rho_eff = rho × Π mult_*` với **mọi `mult_* ≤ 1.0`** (§6.2), tức notional THẬT ≤ notional đã kiểm. Bảng cũ là cận **dưới** của rủi ro rớt sàn, đúng chiều tâng kết quả lên. Kèm: `build_symbol_filters()` chỉ đọc `MIN_NOTIONAL.notional`, bỏ hẳn `LOT_SIZE.minQty` | ✅ | TD-0187 ✅ | Bảng đối chiếu mới sinh từ metadata sàn THẬT (0 trial, chỉ đọc — DR-014 mục 2), phủ dải `Π mult_*` thay vì một điểm; có vi phạm → nâng `E_D` / đặt sàn / thu pool, **ghi DR** (tiêu chí gốc TD-0082). 🔴 Phải trả lời bằng ĐO, không bằng suy luận: vế *"chưa chia đòn bẩy"* trong ghi chú MT-16 đúng hay sai | **Xác nhận** (`de98329` → `c642ec5`, 6 commit): `mult_product` BẮT BUỘC + sàn thật của Freqtrade + `sl_hieu_dung` (6 đường chạy) + `san_tool_d`/`kiem_san_tool_d` (DR-D4-05 C) + `E_D` 500→750 (A). Full suite **1406 passed, 0 failed**. **Đo trên metadata sàn THẬT** (0 trial): 4/102 mã rớt ở ca TỐT NHẤT (BCH/ETC/LINK/LTC). 🔴 **Phát hiện lớn hơn con số — backtest và live KHÔNG cùng một sàn**, lệch HAI CHIỀU ngược nhau (vào lệnh: live 7,50 vs backtest 5,53; phần dư: backtest 5,83 vs live 2,50); ở `Π mult_* = 0,326` zone 3% thì backtest cho **94/102** mã vào lệnh còn live cho **0/102** — lỗ hổng parity (quy tắc 9), nên C lấy `max` mọi đường chạy chứ không lấy sàn đường hiện tại. 🔴 Và lệnh dưới sàn **biến mất im lặng** (`backtesting.py:1173` là `if` KHÔNG có `else`) — phiên `-f4` chứng kiến 72 lệnh mất trên một lượt backtest báo *thành công*. **Kiểm có răng ba lần:** bỏ hai hệ số dự trữ → 8 đỏ; phá `max` thành sàn một đường → 3 đỏ; trả `E_D` về 500 → đúng 1 đỏ (ca ghim quyết định), ca ghim quan hệ vẫn xanh. 🔑 **Bài học đắt nhất, và nó tự lặp lại ba lần trong chính đợt này:** một con số đo đúng MỘT LẦN rồi được đọc như thể LUÔN đúng — bảng `102/102` của TD-0082, giả định `stoploss = -0.99` áp cho mọi đường chạy (backtest hardcode `-0.05`), và ba khẳng định `1.875`/`425.0`/`40.0` đóng băng trong test. Cách chữa không phải cập nhật số mà là **tách ghim QUAN HỆ khỏi ghim QUYẾT ĐỊNH**: quan hệ đúng ở mọi `E_D`, còn con số bị ghim đúng MỘT chỗ và dòng `assert` ở đó **nêu đích danh DR**. 📌 `E_D` = **750 chứ không phải 720** như đề xuất đầu: ở 720 tranche 1 ra ĐÚNG 30,00 trong khi sàn cũng ĐÚNG 30,00 — chốt thoả bằng đẳng thức chính xác là chốt không có lề, và nó lật im lặng. ⚠️ **Chưa nối vào chiến lược** (`ZoneAbsorption`): phần đó là việc riêng, phiên `-2f` dùng `kiem_san_tool_d()` ở TD-0189.
| TD-0191 | 🔴 **Thi hành DR-D4-07 — thang notional arm `Z0-S1`.** Bỏ `notional_co_dinh_usdt` (con số cứng, đang `null` ⇒ arm raise ⇒ **0 lệnh** trên 48 mã EXPLORE/22 tháng ⇒ một suất trong 114 mua số 0). Thay bằng `notional_ref_r_eff: 0.03` + notional **dẫn xuất** `= rho_pct/100 × E_D / ref` | ✅ | DR-D4-07 ✅ (`9599d24`) | **Chặn `TD-0184`** — không đặt chỗ 9 suất khi một arm không chạy được. 🔑 Tham chiếu là trung bình **ĐIỀU HOÀ** của `R_eff` (N tỉ lệ `1/R_eff`), không phải trung bình cộng, KHÔNG phải *"giữa dải"*: đo 83 lệnh Z0 cho median **3,028%** / HM **2,868%** / min **1,303%** ⇒ dải giả định *0,9–3,0%* là số viết lúc THIẾT KẾ, **zone 0,9% không xuất hiện lần nào**. Chốt **3,00** chứ không 2,868 vì lệch 5% là dưới sai số mẫu `n=83` trên tập không phải pool — ghi 2,868 là **độ chính xác giả**. 🔴 **Công thức, KHÔNG con số cứng:** `E_D` vừa đổi 500→750; một số cứng 300 sẽ tự đổi ý nghĩa arm **50%** mà không ai quyết gì (cùng họ `E_D=500` ghim trong docstring, `"2025-01-01"` ghim trong test). **DOF: `N` giữ 114**, rào 3,0777 không đổi, 0 trial — khoá thuộc `tier_c.arm_ablation`, chưa từng được đếm (tiền lệ DR-D4-02); vẫn BẮT BUỘC khai một dòng `dof_inventory.yaml`. **Test khoá phải có răng:** notional **PHẢI đổi khi `E_D` đổi** (test chỉ ghim giá trị hiện tại sẽ không bắt được việc quay lại số cứng) và **KHÔNG đổi khi `R_eff` đổi** (test TD-0183, giữ nguyên). **Chốt chống PASS RỖNG:** chạy lại `Z0-S1` trên EXPLORE đòi **≥ 1 lệnh** — trước bản vá là 0. ⚠️ `arm_switches.py` + `ZoneAbsorption.py` đang thuộc sân phiên `-2f`, phải hỏi trước khi đụng **Xác nhận** (`a7c5248`): `notional_ref_r_eff: 0.03`; hai nhánh của `notional_tranche1_theo_arm()` nay gọi **CHUNG** `tranche1_notional()`, khác nhau đúng MỘT thứ — `R_eff` nào được đưa vào ⇒ tính chất *"không phụ thuộc `R_eff` của lệnh"* thành **cấu trúc**, không phải lời hứa cần test canh. 🔴 **Chốt chống PASS RỖNG ĐẠT: Z0-S1 từ 0 → 83 lệnh** trên 48 mã EXPLORE, 0 exception bị nuốt. 🔑 **Phá thật cho kết quả đáng hơn bản vá:** quay về hằng số cứng 300 USDT → 4 ca đỏ, **nhưng ca *"không phụ thuộc R_eff"* của TD-0183 VẪN XANH** — một hằng số dĩ nhiên thoả bất biến ấy. Chỉ ca mới *"notional PHẢI đổi khi `E_D` đổi"* mới bắt được ⇒ **PASS RỖNG do CHỌN SAI BẤT BIẾN**, không phải lớp canh cùn: bất biến đúng, chỉ là nó không phân biệt được thứ cần phân biệt. 🐛 **Lỗi tự gây tự bắt:** config nay CÓ giá trị nên chiến lược truyền tham chiếu cho MỌI arm ⇒ chốt fail-closed bắt đúng ⇒ 48 exception, 12 test đỏ; chốt làm đúng việc, chỗ sai là chỗ gọi. Bản cũ không lộ vì khoá là `null` — và **chính cái "lọt" đó là lý do Z0-S1 không chạy được**. Khi phát hiện, **trả cây về HEAD** (xác nhận 42 passed) rồi mới xin bàn giao, không để phiên khác dính. **Không xoá ca đỏ nào:** 6 ca TD-0183 + 3 ca `test_sizing` đổi tên tham số, giữ nguyên cả khẳng định lẫn CON SỐ (`ref` tái lập đúng notional cũ, hằng số **TÍNH** chứ không gõ tay). Một ca được SIẾT, kèm ghi thẳng giới hạn (gõ `0.625` thay `0.00625` VẪN LỌT). **Kế toán: `dof_goc` 28 · `|tier_b|` 12 · N 114 · rào 3,0777 — KHÔNG ĐỔI, 0 trial**, có test ghim. Khoá chết `notional_co_dinh_usdt` xoá hẳn (điều kiện xoá đã thoả). Full suite **1437 passed, 0 failed**. |
| TD-0192 | 🔴 **Nối SL cố định của arm `Z1` vào chiến lược — hiện `Z1` chạy GIỐNG HỆT `Z0` (0 dòng gọi `sl_neo_atr`/`ke_hoach_theo_arm` trong `ZoneAbsorption.py`).** `_tinh_zone_4h` gọi thẳng `tinh_ke_hoach()` (luôn ZONE mode), không đi qua `arm_switches.ke_hoach_theo_arm()` — công thức SL = `p1 − 2,2×ATR` (arm_switches.py, có test khoá) chưa từng được đọc trên đường sản xuất | ✅ | MT-21 ✅ | **Xác nhận** (`2447ef2`): `ke_hoach_theo_arm(arm=self._arm, ...)` thay `tinh_ke_hoach()` tại chỗ scan zone (`ZoneAbsorption.py`). Test tầng CHIẾN LƯỢC gọi thẳng `_tinh_zone_4h()` — không dựng lại phép tính SL: SL của `Z1` KHÁC `Z0` trên CÙNG zone, đúng công thức `p1 − 2,2×ATR(4H)`; `r_eff_plan` tính LẠI theo `sl` mới (đúng cảnh báo "lỗi im lặng đắt nhất" trong docstring `ke_hoach_theo_arm()`); arm khác (`Z0`/`Z3`/`Z0-T1`) KHÔNG đổi (đối chứng vá không lan); AST cấm `tinh_ke_hoach` bị import lại, đòi `ke_hoach_theo_arm` PHẢI được gọi. **Kiểm có răng:** phá cô lập (quay về gọi thẳng `tinh_ke_hoach()`) → ĐÚNG 4/7 ca đỏ, đúng các ca canh chỗ đó. Full suite phòng sạch **1444 passed** (1437+7, đúng cộng). |
| TD-0193 | 🔴 **Nối §3.3b (xác nhận entry price-action) vào tín hiệu vào lệnh — hiện KHÔNG có một dòng nào gọi `entry_confirmation.tim_xac_nhan_entry()` trong `ZoneAbsorption.py`.** `populate_entry_trend` chỉ có `zone_ok & trend_ok`; điều kiện `(a VÀ c) HOẶC (b)` (rejection wick / RSI divergence / volume) — MẶC ĐỊNH cho MỌI arm theo spec dòng 4146 (*"thiếu xác nhận entry sẽ làm win rate MỌI cấu hình thấp giả tạo, không riêng gì Z0"*) — hoàn toàn vắng mặt | ✅ | MT-21 | 🔴 **Rủi ro lookahead thật, cùng vùng `TD-0170` từng dính:** xác nhận quét tối đa 3 nến TỚI PHÍA TRƯỚC kể từ nến chạm zone (`i_cham`) — cần theo dõi trạng thái "đang chờ xác nhận" theo pair qua nhiều nến, khác hẳn phép lọc hàng-theo-hàng (`zone_ok & trend_ok`) hiện có; nếu cài sai sẽ để lộ giá TƯƠNG LAI cho quyết định vào lệnh của nến trước. Test khoá phải chứng minh: (1) `Z0-V1` (`bat_dieu_kien_c=False`) và `Z0` (`bat_dieu_kien_c=True`) cho ra SỐ LỆNH KHÁC NHAU trên cùng dữ liệu — bài học TD-0188 áp cho task này y hệt TD-0192; (2) đối chứng âm lookahead theo khuôn `TD-0170` (`_df_4h`-style: không nến nào sau mốc quyết định được đọc). **KHÔNG tự sửa mà chưa thiết kế** — đổi hành vi vào lệnh của TOÀN hệ thống (quy tắc 2), không phải chi tiết nội bộ. ⏳ **Tiến độ (09/09/2026):** `MT-22` chốt Phương án A + phản thực (chủ dự án, xem `back-end-note.md`). Phần 1/n xong: `quet_xac_nhan_zone()` (`c80e3bd`) — hàm thuần, 9 test khoá, kiểm có răng. Còn lại: nối vào `ZoneAbsorption.py` (đặt logic ở `populate_indicators`, không phải callback — cần cột `atr_4h` qua `merge_informative_pair` để an toàn lookahead; gọi lại `tinh_ke_hoach()` tại nến xác nhận C; `custom_entry_price()` không cần sửa, đã đọc `p1` từ tag). 🔒 **Phiên `be` nhận tiếp 09/09/2026** — chủ dự án chốt hướng P1 (vector hoá trong `populate_indicators`) + 6 diễn giải, ghi ở `DR-D4-08` (commit trước code). ✅ **Xong 09/09/2026** — `b50a175` (DR) → `99114fb` (tầng thuần: `wick_frac` + `bat_dieu_kien_c` BẮT BUỘC, `Z0-V1` biểu diễn được qua vòng quét) → `19dfabc` (nối `_xac_nhan_3_3b` vào `populate_indicators`, `enter_long` tại nến C, tag ec/wb/lc, phản thực B hai cột chỉ ghi, gỡ `v_min`/`wick_close_upper_frac` khỏi `MIEN_TRU`) → `2a76b74` (đo). Fixture `test_td0187` lỗi thời lần thứ BA: nến "chạm p1" tách thành 4 nến 1H tường minh gộp ĐÚNG nến 4H cũ + đối chứng thường trực thứ hai. 15 ca khoá mới: (1) Z0 vs Z0-V1 cho TẬP tín hiệu khác nhau ✅; (2) đối chứng âm lookahead cắt tại C−1/C + phá thật ✅. Full suite Docker **1552 passed, 0 failed** (1524+11+15+2). 🔴 **Đo sau khi nối (`td0193-lenh-nam-explore.json`, 88 mã EXPLORE, 0 trial): 4.918 zone → A 2.103 (42,8%) → sau trend 96 → lệnh thật 62 ⇒ quy đổi pool 63,6 lệnh/năm < sàn 150 Nhánh 1** ⇒ theo `DR-D4-08` §6 **DỪNG TD-0184, chờ chủ dự án**. Bộ lọc trend cắt 95%, không phải §3.3b. 📌 Ngoài phạm vi, chỉ ghi: TP1 nổ 21/62, **100% nạng, 0 zone đỉnh** (H-4 = 100% trên EXPLORE) |
| TD-0194 | 🔴 **Bộ sinh `test_td0187` cho 0 ZONE ĐỈNH ⇒ nhánh "TP1 lấy từ zone đối diện" (§5.1) KHÔNG kiểm được trên backtest thật.** Đo 09/09: đỉnh — ứng viên swing **1**, qua `touch≥1` **0**, zone hợp lệ **0**; đáy — 3 / 2 / **1**. Mọi lệnh dùng nạng ⇒ H-4 = 100% ⇒ mọi khẳng định về nhánh zone đều **xanh-vô-nghĩa**. Chèn **HAI** zone đỉnh + ca đối chứng thường trực | ✅ | TD-0182 ✅, TD-0189 ✅ | **HAI đỉnh, KHÔNG phải một** — một đỉnh duy nhất trong tầm thì bản nối hỏng kiểu *"lấy đại zone nào cũng được, bỏ qua phép lọc tầm"* **vẫn xanh**. 🔴 **Ràng buộc thật là GIÁ, KHÔNG phải TUỔI** — cả hai phiên `-f4`/`-2f` ban đầu cùng khai nhầm `tuoi_nen ≤ 40`, nhưng **cả ba** chỗ gọi `zone_hop_le()` truyền `tuoi_nen=K_XAC_NHAN` (hằng **3**) nên vế tuổi **luôn thoả**; `DR-D4-06` cũng đã bỏ hạn tuổi cho zone TP. Ràng buộc thật: `_zone_dinh_tren()` lọc `zone > p_avg` ⇒ đỉnh phải **> 95,0**, mà giá chỉ vượt 95 ở đoạn cuối pha tăng. **Đúng cửa sổ, sai lý do** — ai nới ngưỡng tuổi rồi tưởng cửa sổ rộng ra sẽ chèn đỉnh ở chỗ giá thấp và nó **im lặng không thành ứng viên**. Mốc: `R_eff` là **KHOẢNG GIÁ** (v8 dòng 1667, cấm viết "4R") = `p_avg × r_eff_plan` ≈ **0,842**; trần tìm zone `p_avg + 4×R_eff` ≈ **98,37** ⇒ đỉnh **trong tầm** ~97, **ngoài tầm** ~99,5. ⚠️ Cả hai rơi vào ~20-30 nến cuối pha tăng = vùng ATR/EMA nhạy nhất, **không tránh được, chỉ đo được**: sau khi chèn PHẢI đo lại phễu zone + `p1/p2/p3` + số tranche khớp + `t4`, vì DG2 vừa được tinh chỉnh ở TD-0182. **Ca đối chứng THƯỜNG TRỰC** *"bộ sinh phải cho ≥1 zone đỉnh VÀ ≥1 zone đáy"* gắn ngay trong `_sinh_du_lieu` để chạy MỌI lần fixture được dùng — hai lần trong ngày fixture im lặng sai với hệ thống mới mà **không làm đỏ ca nào**. **Xác nhận 10/09/2026 (phiên `fd`)** — đo lại xong, KHÔNG cần sửa code: chạy RIÊNG `tests/lock/test_td0187_dinh_co_lenh_backtest_that.py` qua Docker (N7) — **17 passed, 0 failed**, gồm đúng bốn điều task này tự đặt ra, cả bốn đều tính ĐỘNG từ lệnh THẬT chứ không so với số ghim sẵn nên chính việc chúng còn xanh sau TD-0182/0189/0193 LÀ bằng chứng không lệch: (1) phễu zone + p1/p2/p3 — `test_co_dung_HAI_zone_dinh_o_HAI_PHIA_tran_TP` tính `p_avg`/`R_eff`/trần TP1 từ `enter_tag` của một lệnh 3-tranche thật, xác nhận đúng 2 zone đỉnh (`_dem_zone(...,"dinh")==2`) nằm hai phía trần `p_avg + 4,0×R_eff`; (2) số tranche khớp — `TestKhongPassRong::test_co_lenh_va_co_lenh_du_ba_tranche`; (3) `t4` — `TestTagMangDuKhoaMoi::test_enter_tag_co_zs_t4_sw_va_t4_la_UP` đối chiếu động với `trend_dir_tai()`. `test_doi_chung_thuong_truc_co_RANG` (ca đối chứng tự canh chính mình) cũng xanh. ⚠️ KHÔNG chạy full suite lượt này: thấy `src/tool_d/api_client/binance_public.py` + `tests/unit/test_api_client_binance_public.py` + `tests/conftest.py` mới đang sửa dở — 🔴 **đính chính**: lúc viết dòng này quy nhầm cho `-46`; theo `-46` báo lại thì hai file đầu là việc của `-76` (TD-0197), còn `conftest.py` + `docs/du-lieu-do/do_td0193_lenh_nam_explore.py` khả năng là `-28` (đang đo tín hiệu Short) — CHƯA tự kiểm chứng, chỉ ghi lại lời báo của `-46`. Việc KHÔNG chạy full suite khi thấy file lạ đang dở vẫn đúng, chỉ riêng chỗ quy cho ai là sai — chạy chung dễ lây nhiễu kết quả đúng bài học TD-0130 của chính dự án; chỉ chạy đúng một file liên quan, không đụng module nào khác. |
| TD-0195 | 🔴 **7/12 tham số `tier_b` KHÔNG có đường đọc nào từ `config/tool_d_config.yaml` vào đường chạy — giá trị thật đến từ HẰNG SỐ CỨNG trong `.py`, vi phạm N4.** Đo tĩnh bằng AST (`docs/du-lieu-do/do_duong_doc_tham_so_tier_b.py` → `td0195-duong-doc-tham-so-tier-b.json`, 0 trial): `zss_threshold`←`zone_strength.py:20 NGUONG_ZSS`, `buf_sl_atr`←`trade_plan.py:20 BUF_SL_HE_SO`, `dg6a_atr_ratio`←`dg6_early_invalidation.py:22`, `dg6d_retrace_frac`←`:26`, `funding_rate_pct`←`:25`, `wick_close_upper_frac`← số ma `0.5` (`entry_confirmation.py:134,136`), `v_min`← chưa nối (MT-21). 🔴 Ba cái đầu NẰM TRÊN đường chạy sản xuất `ZoneAbsorption.py` (`zone_hop_le` :314/:384 · `tinh_ke_hoach` qua `ke_hoach_theo_arm` :322 · `dieu_kien_a` :808) ⇒ **đổi YAML không có tác dụng gì và không có lớp canh nào báo**, vì hai con số đang BẰNG NHAU. Hệ quả ngân sách cùng họ MT-15: một trial B3 tiêu thật để đo một thay đổi KHÔNG XẢY RA. 🔑 `L-Z15` KHÔNG phủ — nó soi `param_status.yaml` (*đã khai TUNED/FROZEN chưa*), không hỏi *giá trị có chảy tới phép tính không*. 📌 Phát hiện phụ: `funding_rate_pct` YAML **-0.05** vs hằng **-0.0005** — cùng số vật lý, khác ĐƠN VỊ (pct vs tỉ lệ); nối mà quên ÷100 là sai 100 lần (lớp lỗi `L-Z48c`) | ✅ | TD-0190 ✅ | Mỗi tham số `tier_b` đang có người dùng phải đi qua **đúng một** `resolve(cfg, "tier_b.<khoá>")`; hàm thuần nhận ngưỡng qua đối số **BẮT BUỘC, không mặc định** (khuôn `time_stop`/`dg4` — mặc định là chỗ để một arm quên khai mà vẫn lặng lẽ chạy). Lớp canh AST mới: khoá `tier_b` nào không có literal đường dẫn nào ⇒ ĐỎ, nêu đích danh khoá. Kiểm CÓ RĂNG: trả một hằng số về cứng ⇒ đúng số ca đỏ dự kiến. Hành vi backtest **bất biến** (giá trị khớp) — 0 trial, N giữ 114, không đụng `param_status.yaml`. Bằng chứng chạy trong Docker (N7) — **ĐÃ XONG 09/09/2026** (`0b5112d`): Docker **1481 passed, 0 failed** (2:18). Ba khoá trên đường chạy nay đọc YAML; ba hằng số `NGUONG_ZSS`/`BUF_SL_HE_SO`/`NGUONG_ATR_RATIO_A` **XOÁ HẲN** (không giữ làm mặc định — mặc định là chỗ để một đường gọi quên khai mà vẫn lặng lẽ chạy). 🔑 **Kiểm có răng, hai lớp:** (a) trả `NGUONG_ZSS` về hằng cứng ⇒ **đúng 1 ca đỏ**; (b) **phép đối chứng đo được GIÁ của lỗ hổng** — đổi `zss_threshold` 0.5 → 0.99 trong YAML rồi chạy backtest THẬT: commit TRƯỚC bản vá (`0152158`) **17 passed** (ngưỡng gần như không thể đạt mà mọi thứ vẫn xanh), commit SAU (`0b5112d`) **1 failed + 16 errors**. Tức trước bản vá, một trial B3 chi cho khoá đó mua về thông tin BẰNG KHÔNG. ⚠️ Phép phá cũng phơi ra **giới hạn của lớp canh**: ca *"có đường đọc"* VẪN XANH khi hàm nhận bỏ qua giá trị — AST chứng minh được chiều PHỦ ĐỊNH, chiều khẳng định do phép đối chứng gánh. ⏳ **CÒN NỢ 4 khoá** (`v_min`, `wick_close_upper_frac`, `funding_rate_pct`, `dg6d_retrace_frac`) — tầng dùng chúng CHƯA được nối (MT-21 / `dieu_kien_d` chưa ai gọi), vá bây giờ là vá một đường không ai đi. Đã ghi `MIEN_TRU` kèm lý do + điều kiện gỡ, và có ca đỏ khi miễn trừ hết hạn. 🔴 `funding_rate_pct` phải chốt ĐƠN VỊ trước khi nối (YAML -0.05 pct vs hằng -0.0005 tỉ lệ). ⏳ Chờ lệnh **"chuẩn hóa và lưu"** để ghi **MT-23** vào `back-end-note.md` mục 7 (N9) |
| TD-0196 | 🔴 **Risk Supervisor §6.6 (OQ-09) — hiện 0 dòng code.** Tiến trình giám sát RIÊNG, đọc trực tiếp margin/vị thế từ sàn, KHÔNG import code bot; bốn ràng buộc LD-22/23/26: SL sống trên sàn, không import bot (hằng số khai lại có test L-Z44), trần API + circuit breaker phân tầng lùi giờ, một endpoint lỗi không hỏng cả snapshot | ✅ | TD-0079 ✅ | Dựng tầng THUẦN trước (theo khuôn `sizing.py`/`admission.py`): phân loại lỗi theo Mục 4.3 `api-integration-rules.md`, state machine breaker bất biến, đối chiếu hằng số khai lại. KHÔNG chạy thật (không cần entrypoint mới — L-Z36 giữ đúng 8 file) \| **Xác nhận** (`src/tool_d/risk_supervisor.py` + `tests/unit/test_risk_supervisor.py`, 43 test): OQ-09 hai ngưỡng đã chốt (chủ dự án, 09/09/2026) — **5 lỗi liên tiếp**, backoff **1s→2s→4s→…→60s** — ghi vào `api-integration-rules.md` v1.1 (đọc qua ENV `TOOLD_BREAKER_THRESHOLD`, KHÔNG qua `tool_d_config.yaml` — tham số VẬN HÀNH, không phải tín hiệu, không tính DOF). `HANG_SO_KHAI_LAI` (E_D/thang dd) đối chiếu khớp bản thật qua `kiem_khai_lai_khop_ban_goc()`. `trang_thai_tai_khoan()` tri-state (`OK`/`LIQUIDATED`/`unreadable`) — `None` KHÔNG BAO GIỜ đọc thành `OK` (N6, fail-closed cho một cờ gọi là "CỜ ĐỎ"). `doc_snapshot_an_toan()` cô lập lỗi TỪNG endpoint nhưng KHÔNG nuốt lỗi lập trình thật (`TypeError` vẫn văng ra — kiểm có răng riêng, tránh lặp bẫy MT-16 vii). `DUNG_HAN` (418, cùng cấp LIQUIDATED) là cờ VĨNH VIỄN — không hàm nào trong module tự gỡ được (test riêng). `LOI_LOGIC`/`KHONG_NHAN_DIEN` KHÔNG cộng vào bộ đếm breaker (test lặp 100 lần không mở). Docker: **1524 passed, 0 failed**. ⚠️ **KHÔNG viết trong task này** (ghi TODO cuối module, không phải bỏ sót): (1) xác nhận chính xác trường Binance đánh dấu lệnh thanh lý — chưa verify; (2) tiến trình chạy thật ở đâu (quyết định khi tới gần D10/D11); (3) dòng `provider-map.md` cho endpoint đọc margin/vị thế nếu tách khỏi market-data đã điền ở TD-0079 |
| TD-0197 | 🔴 **Rà soát 09/09/2026 (đối chiếu key learning "tắt liên tục" của Tool A) — `binance_public.py` là module Single Egress duy nhất gọi mạng nhưng 0 dòng rate-limit/circuit-breaker.** Trần tự áp `tier_c.api_calls_per_min: 30` chỉ là số trong `provider-map.md`, không có gì ép nó trong code; circuit breaker đã dựng THUẦN ở `risk_supervisor.py` (TD-0196) nhưng chưa nối. Bảng nghiệm thu R1-R12 (`api-integration-rules.md` Mục 5) tự ghi "CHƯA chạy" dù đã có gọi mạng thật nhiều lần (TD-0080/82/83/84/116/162) | ✅ | TD-0196 ✅ | **Xong** (`2f0db93`) — 5 hàm GET/JSON qua điểm nghẽn chung `_goi_json_cong_khai()`: R2 giãn nhịp theo `tier_c.api_calls_per_min` (đọc qua `resolve()`, N4), R3 circuit breaker tái dùng `risk_supervisor.py` (TD-0196, không viết lại). `tai_dump_agg_trades()` (host phụ `data.binance.vision`) dùng chung breaker, KHÔNG nhận giãn nhịp (không có trần công bố cho host đó). `latency_samples_ms()` (H15) CỐ Ý đứng ngoài rate-limit/breaker — phá chính phép đo RTT. `tests/conftest.py` mới: fixture autouse reset trạng thái mạng module-level trước MỌI test toàn suite (tránh breaker/sleep rò giữa các test). 🔑 **Nghiệm thu R1-R12 chạy trong context sạch** (subagent riêng, chỉ đọc code + Mục 4, đúng rule 18) — bắt được **2 FAIL 🔴** đã vá ngay: R5 (`latency_samples_ms` thiếu trần `n` + hạn chót tổng — thêm `MAX_MAU_LATENCY=200` + `han_chot_s`, raise thay vì trả kết quả một phần, N6), R10 (thiếu `.env.example` dù `.env.binance` giá trị thật đã tồn tại — đã tạo, `.gitignore` có sẵn `!.env.example`). Chi tiết bằng chứng từng dòng R1-R12: `api-integration-rules.md` Mục 5 (v1.2). Docker: **1582 passed, 0 failed**. 🏷️ **3 tech-debt 🟡 (không chặn gate, ghi nợ):** R6 (chưa có ENV kill switch tự kiểm trong `binance_public.py`/`risk_supervisor.py`), R8 (chưa có logging — không gộp lỗi lặp, không summary định kỳ), R12 (chưa có bộ đếm call + cảnh báo 80% trần dù đã khai trong tài liệu) — cả ba nhận khi có nhu cầu thật (D9.5+/nhiều lệnh gọi hơn), không tự bịa hạn xử lý. ⚠️ R7/R9 (môi trường/idempotency cho endpoint ĐẶT LỆNH) giữ N/A/PASS **có điều kiện** — PHẢI nghiệm thu lại trong context sạch riêng khi D3.5 viết code đặt lệnh thật, không coi hôm nay là đã qua |
| TD-0184 | **Bộ chạy ablation trong E3** — đặt chỗ trial qua DR-014 TRƯỚC khi chạy, chạy từng arm trên **`ZoneAbsorption`** (DR-D4-04 §2.1), ghi bản ghi kết quả có đủ xuất xứ | 🔓 | TD-0182, TD-0183 ✅, TD-0187, TD-0188, TD-0189, **TD-0192, TD-0193** · 🔴 **TD-0247** (rổ A-đầy-đủ — `MT-34` / `MT-45`) · 🔴 **TD-0261** (H14 → N theo DR-007, đầu vào cổng D4 `spec:4267-4268`; chủ dự án chốt đo TRƯỚC suất B2 đầu tiên, 17/09/2026) | 🔴 **CHẶN THÊM 09/09/2026 (MT-21, chỉ đạo chủ dự án):** `TD-0192`/`TD-0193` phải ✅ trước — nếu không, `Z1`/`Z0-V1` chạy giống hệt `Z0` (đo được 83/83 lệnh trùng khít trên EXPLORE), tiêu 2/9 suất trial (18% ngân sách B2 Long) mua thông tin bằng 0. `L-Z52` (khởi chạy khi không có đặt chỗ hợp lệ → TỪ CHỐI, không chạm dữ liệu) và `L-Z53` (giết tiến trình sau khi có kết quả đầu → CONSUMED, không hoàn trả được) phải xanh trên đường chạy THẬT, không phải mock. `L-Z56` đã canh sẵn ở đầu E3 (TD-0165). 🔴 **CHẶN THÊM 09/09/2026 (`DR-D4-08` §6, phiên `be`):** TD-0192/TD-0193 đã ✅, nhưng đo sau khi nối §3.3b cho **63,6 lệnh/năm quy đổi pool < sàn 150 của Nhánh 1** (`td0193-lenh-nam-explore.json`) — 9 suất trial sẽ mua một kết cục INCONCLUSIVE vì thiếu mẫu. **Chờ chủ dự án quyết** (không tự nới §3.3b/trend; bộ lọc trend là chốt cắt 95%) 🔴 **CHẶN THÊM 12/09/2026 (`MT-34`, `TD-0230`) — LÝ DO THỨ BA, và nó về NỀN DỮ LIỆU chứ không về cỡ mẫu:** pool mà D4 sẽ backtest (`config/pool.yaml`, 102 mã) được chốt bằng `compute_pool()` — tiêu chí đo tại **09/2026** — rồi dùng cho **[T0,T2] = 04/2024 → 01/2026**. `delisted_at` **chưa bao giờ** được gán ngoài `tests/unit/test_pool.py` (`grep` cho đúng 5 kết quả), nên `pairlist_point_in_time()` **không có người gọi nào có thể đúng**. Mà `DR-D1-01` §3 đã chốt *"**Không mở DR chấp nhận survivorship bias**"*. Chiều lệch là **chiều PASS** (mã đã chết bị loại khỏi rổ ⇒ kết quả đẹp lên), và spec `:4338` nói *"mỗi lần đổi pool = **MỌI số cũ không so sánh được**"* ⇒ tiêu `B2` trước khi giải xong câu này là mua một kết luận có thể bị chính spec vô hiệu hoá. `TD-0230` đo độ lệch (0 trial). 🔴 **CHẶN THÊM 16/09/2026 — LÝ DO THỨ TƯ, và là lý do DUY NHẤT trong bốn lý do còn hiệu lực thật sự: `TD-0247` (dựng lại rổ pool point-in-time có xuất xứ TRƯỚC D4).** `MT-34` ghi chủ dự án **chốt đường A-ĐẦY-ĐỦ từ 13/09/2026**, và **xác nhận lại 16/09/2026** (`MT-45`). ⇒ **KHÔNG được tiêu một suất `B2` nào** cho tới khi `TD-0247` ✅. Đọc kèm ba lý do cũ ở trên: lý do *MT-21* đã gỡ (TD-0192/0193 ✅); lý do *63,6 lệnh/năm < sàn 150* đã được `DR-D4-10` §2.1 + `MT-26`(C) + `MT-29` xử (`Z0-T1` = 325,6/năm); lý do *MT-34* nay **có mã việc** là chính `TD-0247`. ⚠️ **Hệ quả cho phạm vi TD-0184, viết trước:** đổi rổ ⇒ spec `:4338` làm **mọi số quy đổi sang pool** mất hiệu lực (`325,6` · `n = 206` · `44,8`), nên bảng `n` ở `DR-D4-12` §4.1 phải **đo lại trên rổ mới** trước khi TD-0184 dựa vào nó. Còn **cách chọn 4/9 arm** của `DR-D4-12` §4 thì **đứng**, vì căn cứ cắt là ma trận thiết kế, không phải cỡ `n`. 🔑 Vì sao ghi dòng này: `DR-D4-12` §3 và `MT-45` đều đã nói TD-0184 bị chặn, nhưng **ô này không nói** — đúng hình dạng lệch trạng thái của `MT-46` (văn bản đứng sau hiện thực), và người chỉ đọc `TASKS.md` đã có thể nhận TD-0184 ⏸ **TẠM DỪNG 17/09/2026** theo `DR-IQ-01` §1 (cổng sống còn TD-0291 luật 4) — không làm tiếp cho tới khi thoả điều kiện nối lại bất đối xứng của DR đó. |
| TD-0198 | 🔴 **`sizing.mult_regime()` raise SAI cho arm CỐ Ý tắt bộ lọc ADX — hai arm đo một cấu hình KHÁC thứ chúng khai.** Hàm raise `SizingError` khi `ADX(1D) < 20` với lý do *"§2.5 đáng lẽ đã chặn từ trước"*; đúng cho `DAY_DU`, SAI THEO ĐỊNH NGHĨA cho `Z0-T0` (§10.1b: *"KHÔNG bộ lọc trend nào"*) và `Z0-T1` (*"bỏ ADX(1D)≥20"*). Freqtrade NUỐT exception callback (MT-16 vii) ⇒ lệnh biến mất im lặng: đo được **588 lệnh `Z0-T0` + 123 `Z0-T1`** mất trên 88 mã EXPLORE (`70542cb`, 0 trial), `Z0`/`Z3` mất 0. 🔴 Các lệnh mất KHÔNG ngẫu nhiên — đúng tập `ADX(1D) < 20`, tức **chính tập lệnh làm nên khác biệt của hai arm**. Nặng hơn MT-15 một bậc: MT-15 là hai arm TRÙNG nhau (mua *thông tin bằng 0*), đây là arm chạy cấu hình KHÁC thứ nó khai (mua *thông tin sai*) mà bảng kết quả vẫn bình thường | ✅ | MT-21 ✅, TD-0193 ✅ | Việc 1/4 của gói chủ dự án chốt 09/09/2026 (làm tuần tự). `arms.co_loc_adx_1d(tang)` → chỉ `DAY_DU`; `mult_regime(..., da_loc_adx=)` **BẮT BUỘC, không mặc định** (khuôn `bat_dieu_kien_c`/MT-15): `True` + ADX thấp ⇒ raise như cũ (lớp canh MT-21 nguyên vẹn), `False` ⇒ trả `weak`. 🔑 **`weak` là DIỄN GIẢI, ghi ra để cãi lại được:** §6.2 có ĐÚNG HAI bậc chia tại `adx_split`; `adx_threshold` là ngưỡng VÀO LỆNH của §2.5, không phải ranh giới của hệ số này ⇒ không thêm số nào vào kiểm kê DOF (đặt bậc thứ ba = bịa tham số, đúng lỗi DR-D4-02 chặn). Test khoá: DAY_DU vẫn raise · KHONG/CHI_4H trả weak · vùng trên ngưỡng HAI arm giống hệt nhau (bản vá không lan sang 7 arm kia) · NaN vẫn raise · AST đòi cờ SUY TỪ ARM chứ không phải hằng số · phá thật trên bản chép ⇒ raise quay lại. **DOF: 0 tham số mới, N = 114, rào DSR 3,0777 — không đổi, 0 trial** \| **Xác nhận** (`6351017` khoá → `17ec1bb` code): 23 ca khoá mới. 🔑 **Bằng chứng ĐƯỜNG CHẠY THẬT, không chỉ test** — đo lại 88 mã EXPLORE trước/sau bản vá: `Z0-T0` exception bị nuốt **588 → 4**, lệnh **949 → 1.244** (+31%); `Z0-T1` **123 → 3**, lệnh **245 → 325** (+33%); 🔴 `Z0` **0 → 0**, lệnh **62 → 62** — bản vá chạm ĐÚNG hai arm cần chạm, 7 arm `DAY_DU` không đổi một lệnh. Lệnh/năm quy đổi pool: `Z0-T0` 973,8 → **1.276,5**, `Z0-T1` 251,4 → **333,5**. ⚠️ Còn **7 exception** (4+3) chưa truy nguyên nhân — không phải `mult_regime` (Z0 = 0), ghi nợ để không bỏ qua im lặng |
| TD-0199 | **Thi hành `DR-D4-09` §8 chặng a — tầng THUẦN đọc kết quả theo cỡ mẫu.** `phan_loai_ket_cuc()` trả PASS/**INCONCLUSIVE**/FAIL (§2.2: INCONCLUSIVE khi `thuế nhiễu > ngưỡng của chính tiêu chí đó`) và `so_paired()` (§2.1: so trên tập GIAO, mẫu số là `n_giao`, khai `ρ`). Hiện §10.2 chỉ có HAI kết cục nên *"không đo được"* và *"đo được, không đạt"* bị gộp — hai thứ dẫn tới hành động trái ngược | ✅ | DR-D4-09 ✅ (`353c867`) | Việc 2/4 (phần thi hành) của gói chủ dự án chốt 09/09/2026. Tầng THUẦN, không đọc file, không biết Freqtrade — cùng khuôn `dsr.py`/`sizing.py`. Dùng LẠI `dsr.dsr_hurdle()`/`dsr_adjusted_expectancy()`, KHÔNG chép công thức (MT-03). 🔴 Viết TRƯỚC khi tồn tại một con số expectancy nào (spec dòng 3956-3958) — đây là mã hoá một ĐỊNH NGHĨA, không phải lớp canh cho đường chưa chạy. Người gọi: TD-0184 (bản ghi kết quả) + TD-0185 (`L-Z57`). **0 trial, N = 114 không đổi** \| **Xác nhận** (`c6e0094` + `699f36f`): `src/tool_d/gates/ket_cuc.py` + 23 ca khoá; full suite **1605 passed, 0 failed**. 🔑 **Hai điểm thiết kế, ghi ra để cãi lại được:** (1) **PASS xét TRƯỚC** — `DSR_adj` đã trừ thuế rồi nên vượt được là vượt SAU KHI đã trả giá cho N phép thử; đảo thứ tự biến cơ chế bảo vệ thành cái chặn chính kết quả nó vừa xác nhận (có ca ghim `mean_r=2.0, n=40` ⇒ vẫn PASS dù thuế 0,608). (2) Mốc INCONCLUSIVE là `thuế > ngưỡng CỦA CHÍNH TIÊU CHÍ`, **suy từ ngưỡng** chứ không phải hằng số mới ⇒ **0 tham số vào kiểm kê DOF**; có ca AST cấm module mang hằng số float riêng. **Ca đắt nhất:** CÙNG `mean_r` không đạt — `n=40` ⇒ INCONCLUSIVE, `n=5000` ⇒ FAIL; hai ca cho cùng kết cục nghĩa là phân biệt của DR-D4-09 đã biến mất. 📌 `L-Z48c` bắt 2 nhãn `R` trần trong docstring mới ⇒ **diễn đạt lại thành `R_realized`, KHÔNG nới phép kiểm** (tiền lệ L-Z25/TD-0125) |
| TD-0200 | 🔴 **Tải dữ liệu 5m cho 102 mã pool trên WFO [T1,T2] — điều kiện để `--timeframe-detail 5m` (H5 "Bắt buộc P0") có hiệu lực khi chạy ablation.** Pool hiện chỉ có 5m cho **2/102 mã** (`1000BONK`, `1000PEPE`, và cả hai phủ [2024-06-01, 2025-05-31] — **kết thúc TRƯỚC T1**, tức 0/102 mã dùng được cho WFO). Không có 5m thì E3 chạy ở độ phân giải 1H: spec D5 (dòng 2923) nói backtest khi đó **"thiên vị có lợi một cách hệ thống"** và D6 (dòng 2924) gọi đây là **"rủi ro số một của toàn bộ kết quả Tool D"** | ✅ | TD-0093 ✅ | **Chủ dự án chốt "phương án A" 09/09/2026** — tải TRƯỚC, không chạy ablation trên dữ liệu 1H rồi ghi hạn chế. 🔴 **Chiều thiên vị KHÔNG trung tính:** Tool D có 5 mức giá/nến (p1,p2,p3,SL,TP) so với 2 của Tool A, và mọi arm DCA có 3 lần khớp so với 1 của Z0 (spec dòng 1215) ⇒ thiếu 5m ưu ái arm DCA **đúng chiều** Nhánh 2 §10.2 đang phán quyết. **0 trial** (DR-014 §2 chỉ tính *đánh giá cấu hình*; đây là tải dữ liệu, cùng tiền lệ TD-0093/TD-0182). 🔴 **Bẫy TD-0093 phải phòng, không phải phòng xa:** `download-data --timerange` **không tôn trọng mốc kết thúc** — đã tái diễn ở TD-0182 ⇒ bắt buộc cắt lại ≤ T2 rồi verify seal lockbox. Quy trình: `E8 --snapshot-before` → `freqtrade download-data -t 5m` → cắt ≤ T2 → `E8 --verify-after` → `touch_lockbox.py --verify-seal`. 🔴 **Kiểm độ phủ là BẮT BUỘC, không phải tuỳ chọn:** `backtesting.py:1739` có `and pair in self.detail_data` — mã thiếu 5m **không làm backtest đỏ**, nó lặng lẽ chạy ở 1H trong khi mã khác chạy 5m; `_load_bt_data_detail` truyền `startup_candles=0` nên 5m chỉ cần phủ đúng [T1,T2], không cần warmup \| **Xác nhận** (`docs/research-log.md` 09/09/2026, **0 trial**): 102/102 mã tải xong trong 6:08; **101 mã có 5m dùng được** (TRIA rỗng — niêm yết 06/02/2026, sau T2, khớp 5 file `TRIA*` khác); **5.887.071 nến**, phạm vi `2025-06-12 00:00` → `2026-01-28 23:55`. Độ phủ đo bằng **chính hàm `_load_bt_data_detail()` gọi** (`history.load_data(..., startup_candles=0)`), không đếm file trên đĩa: 101 mã nạp được 1h, 101 mã nạp được 5m, **0 mã có 1h mà thiếu 5m**. `verify-seal` lockbox **PASS** (service `lockbox`); `E8 --verify-after` **PASS 507 file**. 🔴 **Bẫy TD-0093 còn sống và KHÔNG ĐỀU GIỮA CÁC KHUNG:** tải thử ADA cho 5m **đúng phạm vi tuyệt đối** trong khi cùng lượt `mark` lấn tới `2026-02-16`, `funding_rate` tới `2026-09-10`; rồi trên 102 mã vẫn có **4 mã** (`NOM`/`PROM`/`STRK`/`VIRTUAL`) lấn 24 nến 5m qua T2 ⇒ *"khung này đúng"* và *"mã này đúng"* đều không suy rộng được. 🔴 **`download-data -t 5m` ở futures LUÔN kéo thêm `1h-mark` + `1h-funding_rate`** ⇒ tải thẳng vào thư mục pool sẽ ghi đè 204 file đã cắt từ TD-0093; đã tải vào thư mục nháp rồi chỉ chép `*-5m-futures.feather`. 🔴 **H19 bắt được hư hại THẬT do chính lần nhập này gây ra — lớp gác đúng, người sai:** `shutil.copy2` GHI ĐÈ hai file 5m cũ của TD-0115 (`1000BONK`/`1000PEPE`, mất 105.120 nến/file); đã GỘP lại từ bản sao lưu (105.120 + 66.528 = 171.648, không trùng một nến). 🔴 **Phát hiện ngoài phạm vi, đã trình chủ dự án:** `E8 --snapshot-before` in `✅ H19 (a) đã sao lưu` mà **không có bản sao lưu nào** — mặc định `--backup-root ../tool-d-data-backup` trỏ ra ngoài `/workspace` (chỉ mount `..:/workspace`) nên bốc hơi khi `--rm`; bằng chứng lạnh: `C:\tool-d-data-backup` KHÔNG tồn tại dù TD-0093 đã chạy hồi 07/09. Đường thứ hai: từ Git Bash, MSYS đổi `/backup` thành `C:/Program Files/Git/backup`, chuỗi không mở đầu bằng `/` nên Linux hiểu là TƯƠNG ĐỐI ⇒ 93 MB ghi vào **gốc repo** dưới thư mục `C` + U+F03A (**lần thứ BA** hình dạng này) — đã đối chiếu tên/kích thước/8 mẫu sha256 rồi mới xoá |
| TD-0185 | 🚪 **GATE D0.9 §10.2 — HAI NHÁNH + `L-Z57`** | 🔓 | TD-0184 | Nhánh 1 (tiêu chí PASS **độc lập** cho Z0 — v5 thiếu hẳn cái này); Nhánh 2 (chọn cấu hình, "vượt ≥ 20%" chỉ tính là CÓ khi **sống sót qua CẢ HAI chiều** hiệu chỉnh Δ_R — gọi `buoc4_hieu_chinh_hai_chieu` của TD-0164, không viết lại). 🔴 `L-Z57` CRITICAL: bộ kết quả giả lập "Z0 vượt trội mọi arm DCA" đưa qua GATE → kết cục **"chọn Z0, dự án TIẾP TỤC"**, KHÔNG có đường nào dẫn tới DỪNG DỰ ÁN ⏸ **TẠM DỪNG 17/09/2026** theo `DR-IQ-01` §1 (cổng sống còn TD-0291 luật 4) — không làm tiếp cho tới khi thoả điều kiện nối lại bất đối xứng của DR đó. |
| TD-0186 | 🚪 **GATE D4** — điều kiện vào D5 | 🔓 | TD-0180…TD-0185 | `runtime_state.json.d4_complete` + tag `d4-complete`, dùng lại khuôn `close_d3_5_gate()`: đòi `d3_5_complete`, kiểm cây sạch, chạy RIÊNG từng file test cốt lõi. Ghi `d4_han_che` nói rõ hướng nào đã chạy và hướng nào **hoãn** — không để "9 trial" bị đọc thành "đã phủ cả hai hướng". 🔴 **Thêm theo DR-D4-04 §7:** evidence `do-duoc` phải chứa (i) tên chiến lược đã chạy = `ZoneAbsorption` (không phải Minimal), (ii) tỉ trọng tranche đo từ fill THẬT = ⅓⅓⅓, (iii) stake biến thiên theo `1/R_eff`, (iv) H-4 tỉ lệ fallback TP. Thiếu một ⇒ cổng từ chối — D4 chạy trên hệ thống 40 USDT/¼¼½/không-TP là D4 của một hệ thống khác ⏸ **TẠM DỪNG 17/09/2026** theo `DR-IQ-01` §1 (cổng sống còn TD-0291 luật 4) — không làm tiếp cho tới khi thoả điều kiện nối lại bất đối xứng của DR đó. |
| TD-0201 | 🔴 **`decision_log.py` không phân biệt được nguồn bản ghi (backtest/dry-run/live) — phát hiện khi soát dashboard front-end (`-dc`).** `TRUONG_KHOA` chỉ định nghĩa 4 loại sự kiện dùng để DỰNG KHOÁ, không có trường nào mang nguồn. §8.3 của spec cũng KHÔNG định nghĩa trường này ở bất kỳ loại bản ghi nào — khoảng trống thật của spec, không phải sót code. Vô hại hôm nay vì **0 nơi trong production gọi module này** (đã grep `src/`, `user_data/strategies/`, `entrypoints/` — chỉ `fold_record.py` NHẮC TỚI trong docstring, không gọi), nhưng sẽ âm thầm sai ngay khi có bản ghi thật đầu tiên trộn giữa các nguồn — đúng lỗi Tool A đã dính (`dashboard-ui/src/data/tool-a/trang-go-live.json` tiêu chí #2) | ✅ | — | Trường `nguon` ∈ {`backtest`,`dry_run`,`live`} **BẮT BUỘC, không mặc định** — cùng khuôn `bat_dieu_kien_c`/`nguong_zss` (fail-closed, không phải mặc định im lặng). `dedup_key()` RAISE nếu thiếu hoặc giá trị ngoài tập cho phép. Không đổi cấu trúc `TRUONG_KHOA` (đó là bảng DỰNG KHOÁ, `nguon` không tham gia khoá — cùng sự kiện thật không đổi khoá chỉ vì đổi nguồn ghi). Test khoá mới; không đụng 4 caller `TRUONG_KHOA` hiện có vì chưa ai gọi. Bằng chứng chạy trong Docker (N7) \| **Xác nhận** (`51c0385`): `NGUON_HOP_LE = ("backtest","dry_run","live")`, kiểm ở `dedup_key()` — điểm nghẽn duy nhất mọi bản ghi đi qua trước khi chạm file (khuôn TD-0150). Cập nhật 8 fixture/inline record của `test_lz45_*` hiện có (chưa ai gọi module trong production nên không phá gì khác). Test mới `test_td0201_*`: 15 ca — cả bốn loại sự kiện (không riêng `VAO_RA_LENH`) đều đòi `nguon`; đổi nguồn KHÔNG đổi khoá; AST cấm `setdefault`/mặc định lén. Docker full suite **1628 passed, 0 failed** (2:22). 🔑 **Kiểm có răng, hai phép phá độc lập trong lab `git clone --local`:** (1) mặc định lặng lẽ về `"backtest"` khi thiếu ⇒ **đúng 15 ca đỏ**, đúng nhóm `TestFailClosed`/`TestApDungChoCaBonLoai`; (2) `NGUON_HOP_LE` đổi thành tập nhận mọi giá trị ⇒ **16 ca đỏ** (thêm ca kiểm cấu trúc hằng số). Cả hai phép phá không đụng nhóm test khác |
| TD-0202 | **`config/freqtrade/config.json` không khai `db_url`** — phát hiện khi soát dashboard front-end (`-dc`). Freqtrade mặc định `sqlite:///tradesv3.dryrun.sqlite` (đọc từ chính hằng số cài trong image Docker, N7) — đường TƯƠNG ĐỐI, rơi vào gốc `/workspace` vì `working_dir` của compose; không mất, nhưng KHÔNG có tài liệu nào ghi đường dẫn này, và chưa tách riêng dry-run/live theo đúng ánh xạ N11 ("DB staging riêng, tách hẳn DB live") | ✅ | — | Ghim tường minh `db_url: "sqlite:////workspace/user_data/tradesv3_dryrun.sqlite"` (4 gạch chéo = đường tuyệt đối theo cú pháp SQLAlchemy) kèm `_comment_db_url` theo đúng khuôn mọi khoá khác trong file — giải thích TẠI SAO đường này, không chỉ giá trị. `*.sqlite` đã có trong `.gitignore`, không cần sửa. Đường LIVE (D12) để trống, quyết khi tới D10-D12 (N11) — không tự bịa trước. Không có test khoá riêng (file cấu hình tĩnh); xác nhận bằng `L-Z24`/`L-Z27` (nếu có) vẫn xanh sau khi thêm khoá \| **Xác nhận** (`51c0385`): JSON hợp lệ (`json.load` không lỗi). Docker full suite **1628 passed, 0 failed** — bao gồm mọi test khoá đọc `config.json` (L-Z24 và các test khác nếu có) không đổi hành vi |
| TD-0203 | 🔴 **`backup_data_dir()` KHÔNG kiểm bản sao lưu có đáp xuống đích không — H19 (a) là một ✅ RỖNG** (phát hiện ở TD-0200). Hàm chỉ có `rmtree(dest)` → `copytree(...)` → `return dest`; người gọi in `✅ H19 (a) đã sao lưu` chỉ vì không có exception. Bằng chứng lạnh: `C:\tool-d-data-backup` **KHÔNG tồn tại** dù TD-0093 đã chạy `--snapshot-before` trên 510 file hồi 07/09/2026 | ✅ | TD-0200 ✅ | Hạng **DR-012 Hạng 1** (code không khớp chữ spec H19 (a) *"SAO LƯU TRƯỚC"*) ⇒ **0 trial, không cần DR mới**. 🔴 **Phép kiểm phải hỏi *"file có đáp xuống đích không"*, KHÔNG được hỏi *"tôi đang chạy ở đâu"*** — ràng buộc do phiên `-46` nêu và nó đúng: `backup_data_dir()` là Python thuần (`shutil`+`pathlib`), chạy được trên host, và khi chạy trên host thì `../tool-d-data-backup` **trỏ đúng chỗ**; mọi heuristic kiểu so `st_dev` để đoán "có phải volume không" sẽ báo động giả trên host ⇒ đúng loại *"chốt không bao giờ thoả được"* mà cổng D3 đã trả giá. Phép so đúng ở đây là **byte-đối-byte với NGUỒN** (khác `verify_old_candles_preserved()` phải so ở mức NẾN vì gộp nến mới làm bytes đổi hợp lệ — bản sao lưu thì không gộp gì). **Hai lỗi phụ đọc ra cùng lúc, sửa luôn:** (i) `rmtree` chạy TRƯỚC `copytree` ⇒ copy hỏng giữa chừng là **mất cả hai bản**, đúng lúc cần nhất — đổi thành copy ra `dest.tmp`, đối chiếu xong mới đổi tên đè; (ii) `dest = dest_root / source_dir.name` không được kiểm, `--backup-root /` sẽ `rmtree("/futures")`. **Kiểm có răng bắt buộc:** cho `copytree` ghi vào thư mục tạm rồi xoá ⇒ phải ra ĐÚNG 1 ca đỏ; không có ca này thì bản vá chỉ là một `assert` chưa ai thử \| **Xác nhận** (`src/tool_d/data/backfill_guard.py`, `entrypoints/backfill_data.py`, 7 test mới ở `tests/unit/test_backfill_guard.py::TestSaoLuuXacThuc`): `backup_data_dir()` nay chép ra `dest.tmp-backup`, đối chiếu **tập file + kích thước + sha256 từng file** với nguồn, chỉ `rmtree(dest)` rồi đổi tên đè SAU KHI đối chiếu qua — bản CŨ không còn bị xoá trước khi bản mới được xác nhận. Thêm chặn `dest_root` là gốc hệ thống file hoặc trùng/chứa `source_dir`. Đổi mặc định `--backup-root` từ `../tool-d-data-backup` sang `runs/backfill_backup` (cùng quy ước `--snapshot-out`, luôn nằm trong `/workspace`) — giải quyết đúng ca THẬT đã xảy ra (mặc định trỏ ra ngoài mount); heuristic kiểu `st_dev` bị loại có chủ đích vì báo động giả trên host (ràng buộc của phiên `-46`). **Kiểm có răng chạy thật:** tạm quay `backup_data_dir()` về 3 dòng cũ (không verify) ⇒ đúng **3/7 ca đỏ** (đúng ba ca kiểm hành vi verify/dọn dẹp; 4 ca sanity-check khác vẫn xanh vì không phụ thuộc bước đó) — khôi phục rồi xác nhận khớp byte-đối-byte với bản đã viết. Full suite Docker **1648 passed, 0 failed** (1628 + 20, cộng đúng với TD-0204) |
| TD-0204 | 🔴 **Đường dẫn Windows lọt vào tiến trình Linux, hỏng IM LẶNG — đã xảy ra BA lần ở ba chỗ khác nhau.** Git Bash (MSYS) đổi `/backup` thành `C:/Program Files/Git/backup` TRƯỚC khi tới container; chuỗi đó **không mở đầu bằng `/`** nên Linux hiểu là đường dẫn TƯƠNG ĐỐI so với `/workspace` ⇒ ghi thẳng vào gốc repo dưới thư mục tên `C` + **U+F03A**. Ba lần: `backup_lockbox(dest_dir=...)` 07/09 (suýt bị xoá nhầm vì tưởng là rác — hoá ra chứa bản backup lockbox thật), `--backup-root` ở TD-0200 (93 MB), và lần gốc | ✅ | TD-0200 ✅ | 🔑 **Phạm vi ĐÃ ĐO, không suy rộng:** grep 8 file `entrypoints/` cho ra **đúng 4 cờ nhận đường dẫn, tất cả trong `backfill_data.py`** (`--data-dir`, `--backup-root`, `--snapshot-out`, `--verify-after`); `--with-params-file` có ở 8/8 file nhưng là cờ **bool** — tên nó lừa mắt khi grep. 🔴 **Quy ước văn xuôi KHÔNG đủ** — đó chính là thứ `MT-15`/`L-Z15` liên tục bắt được, và cả ba lần trước đều đã "biết rồi". Máy canh đặt trong `measurement_guard()` (mọi entrypoint đã gọi ở dòng đầu theo N5, nên phủ cả 4 cờ hiện có **và mọi cờ tương lai**): chạy trên POSIX mà thấy `argv` mang giá trị khớp `^[A-Za-z]:[\\/]` ⇒ **TỪ CHỐI CHẠY** kèm thông báo đọc được. Điều kiện `sys.platform` là bắt buộc — trên host Windows thì `C:\...` hợp lệ, thiếu điều kiện này là tự tạo báo động giả. Cộng một ca khoá cấm ký tự **U+F03A** xuất hiện trong bất kỳ đường dẫn nào của repo (bắt được cả ba sự cố đã xảy ra, kể cả khi máy canh argv bị đi vòng). Ghi **một dòng** trỏ tới máy canh trong `CLAUDE.md`, **không** chép luật ra văn xuôi (MT-03: một nguồn sự thật) — cần lệnh `"chuẩn hóa và lưu"` \| **Xác nhận** (`src/tool_d/measurement/guard.py` + `tests/lock/test_td0204_duong_dan_windows_lot_vao_posix.py`, 13 test mới): `find_windows_path_leak()` nối vào `measurement_guard()` TRƯỚC cả bước quét file tham số ẩn, xét cả hai cách viết cờ (`--co gia-tri` và `--co=gia-tri`), chỉ kích hoạt khi `os.name != "nt"`. Tái sử dụng đúng đường thoát `outcome=BLOCKED` sẵn có ⇒ **không sửa một dòng nào trong 8 entrypoint**. Lớp canh thứ hai: quét toàn bộ cây làm việc cấm ký tự U+F03A (loại trừ `.git`/`node_modules`/`__pycache__` — có đo, `node_modules` chiếm 95,6% số mục nếu quét, không phải linh cảm hiệu năng). **Kiểm có răng chạy thật:** vô hiệu hoá điều kiện `os.name` ⇒ đúng **5/13 ca đỏ** (5 ca kiểm hành vi phát hiện; 8 ca khác — gồm cả ca "không báo động giả trên host" — vẫn xanh vì đúng ngay cả khi hàm luôn trả `None`) — khôi phục rồi xác nhận khớp byte-đối-byte. `CLAUDE.md` **CHƯA ghi** — chờ lệnh `"chuẩn hóa và lưu"` (N9). Full suite Docker **1648 passed, 0 failed** |
| TD-0205 | 🔴 **`n = 40` — con số trung tâm của mọi quyết định về D4 — là một NGOẠI SUY, chưa ai đo.** `td0193-lenh-nam-explore.json` đo 63,6 lệnh/năm trên **[T0,T2] = 1,81 năm** rồi nhân 0,632 năm để suy `n` trên **WFO [T1,T2]** — cửa sổ mà ablation D4 thật sự chạy (spec dòng 3287). Phép này giả định tốc độ lệnh trong WFO bằng trung bình toàn cửa sổ, trong khi cơ hội Long treo vào tỉ lệ 1D = UP và tỉ lệ đó trên [T0,T2] chỉ **19,0%** (UP 400 · DOWN 1.339 · FLAT 364 trên 2.103 nến C) — một cửa sổ 7 tháng lệch xa được **cả hai chiều**. Đo `n` RIÊNG trong [T1,T2] có thể **tự nó đóng câu hỏi "có nên tiêu 9 suất trial không"**: sàn Sharpe mỗi lệnh `h/√n` tăng khi `n` giảm, nên `n` thật thấp hơn 40 thì mọi tranh luận về `std_R` thành thừa (MT-25) | ✅ | TD-0193 ✅ | Thêm `--tu`/`--den`/`--ket-qua` vào `docs/du-lieu-do/do_td0193_lenh_nam_explore.py`, **mặc định giữ nguyên T0/T2 và đường ghi cũ** ⇒ đường chạy cũ không đổi một byte, artifact `td0193-*.json` **không bị ghi đè** (kỷ luật xuất xứ MT-24). Chạy `--tu 2025-06-12 --den 2026-01-29`, ghi **file MỚI** `td0205-lenh-nam-wfo-explore.json`. **0 trial** — EXPLORE, chỉ đếm, ngoài phạm vi DR-014 §2 (cùng tiền lệ TD-0182/TD-0193). 🔴 Phải báo cáo kèm **tỉ lệ 1D=UP/DOWN/FLAT riêng trong [T1,T2]** — không có nó thì không phân biệt được *"WFO ít lệnh"* với *"WFO là cửa sổ nghịch chiều Long"*, hai thứ dẫn tới hành động trái ngược. Bằng chứng chạy trong Docker (N7) \| \| **Xác nhận** (`848ee6f`): `td0205-lenh-nam-wfo-explore.json` — Z0 **44,8 lệnh/năm** trên WFO (×0,70 so với ngoại suy 63,6) ⇒ **n = 28**, sàn Sharpe mỗi lệnh `h/√n` **0,487 → 0,578**; chỉ **22 lệnh thật** trên 88 mã trong 7,6 tháng. Cơ chế đo được: `1D = UP` chỉ **15,85%** trong WFO vs **19,02%** trên [T0,T2] (`z = −2,22`, phân biệt được ở 95%) — WFO là cửa sổ **nghịch chiều Long**. Kiểm ngược độ phủ: WFO **90%** mã-năm vs [T0,T2] **63%** ⇒ sụt giảm KHÔNG phải do thiếu dữ liệu. Bất định đã khai: CI95 Poisson (k=22) cho `n` ∈ [16, 40], sàn `S` ∈ [0,486 · 0,758] — **đầu lạc quan nhất đúng bằng ước lượng cũ**, nên kết luận vững trên toàn khoảng. Ba arm dịch chuyển khác hướng đúng theo mức phụ thuộc tầng 1D (Z0-T0 ×1,19 · Z0-T1 ×0,98 · Z0 ×0,70) — bằng chứng cơ chế. Đường chạy cũ không đổi một byte (verify trong Docker). **0 trial**, sổ `trial_registry.jsonl` vẫn 13 dòng |
| TD-0206 | 🔴 **H-4 = 100% ở MỌI arm, MỌI cửa sổ — 723/723 lần TP1 rơi nạng, 0 lần chốt theo zone đối diện.** Ngưỡng Nhánh 1 §10.2 là ≤ 40%; spec dòng 4285-4286: *"> 40% → tiền đề TP sai (L2) → KHÔNG vào live"*. 🔑 **Nghiêm trọng hơn vấn đề cỡ mẫu:** nếu TP1 chưa bao giờ chốt theo zone thì mọi arm đang đo một hệ thống **không dùng tầng chốt lời như thiết kế** — `mean_R` của mọi arm là của một cỗ máy khác, và D4 vô hiệu **bất kể cỡ mẫu**. Bằng chứng cắt hai chiều: `td0189-diem-thoi-gian-zone-dinh-explore.json` cho khoảng cách tới zone đỉnh gần nhất trung vị **16,36%** trong khi trần nhận TP1 là `4,0 × R_eff` ≈ **12,1%** (R_eff trung vị 3,028%) ⇒ quá nửa rơi nạng vì khoảng cách là **cấu trúc thị trường**; nhưng *quá nửa* ≠ **100%** ⇒ khoảng hở đó là tín hiệu chẩn đoán | ✅ | TD-0205 ✅ | **0 trial**, EXPLORE, chỉ đếm — tiền lệ TD-0193/TD-0205. Đo trên **ĐƯỜNG SẢN XUẤT THẬT** (thêm dòng log ở `_zone_dinh_tren` + `_cap_nhat_chot_loi`, parse như khuôn `KET_NAP`/`SAN_TOOL_D`), **KHÔNG tái lập logic** — đúng câu hỏi chẩn đoán của dự án *"ca sai có đi qua đường sản xuất thật không?"* (bài học TD-0168). Phải tách được BA số: (a) tổng zone đỉnh đã xác nhận trước bộ lọc `> p_avg`; (b) còn lại sau `> p_avg` (`_zone_dinh_tren:1150`); (c) khoảng cách tới ứng viên gần nhất quy theo `R_eff` của chính lệnh đó, so với trần `tp_fallback_dist_r`. 🔑 **Phán quyết:** (a)=0 ở đa số ⇒ **LỖI** `_quet_zone_dinh`; (a)>0 mà rớt ở (b) ⇒ lỗi bộ lọc `> p_avg`; rớt ở (c) ⇒ **CẤU TRÚC THỊ TRƯỜNG**, H-4 = 100% là kết quả THẬT và Nhánh 1 fail thật theo §11b.1. ⚠️ Nếu là lỗi, sửa thuộc **DR-012 Hạng 1** (0 trial, không cần DR) nhưng **đổi mọi con số D4 đã đo** ⇒ phải ghi như `DR-D4-08` §8 \| \| **Xác nhận** (`c991ba2`+`5d07698`): **LỖI MÃ, không phải cấu trúc thị trường.** Đo ở tầng CƠ CHẾ trên đường sản xuất thật (`td0206-chan-doan-h4.json`, 88 mã, 22 lệnh, 0 trial): `so_lan_goi_zone_dinh_tren = 22` (CÓ được gọi 22/22) · `co_cot_zone_dinh_gia = {'False': 22}` (cột VẮNG MẶT 22/22) · `cot_thay_duoc_mau = date,open,high,low,close,volume`. `_df_4h()` gọi `dp.get_pair_dataframe()` trả **OHLCV thô đúng 6 cột**, trong khi `zone_dinh_gia` tính ở dòng 382 rồi merge vào 1H với hậu tố `_4h` (dòng 301) ⇒ tên thật `zone_dinh_gia_4h` trên dataframe **đã phân tích** ⇒ `_zone_dinh_tren` rơi `return []` **100%**. Bán kính: đúng **hai** hàm (`_zone_dinh_tren`, `_tuoi_zone_dinh_nen`), cả hai là đường TP-theo-zone; ba hàm còn lại tự tính lại từ OHLCV thô nên sống. Full suite **1648 passed, 0 failed** — **một đường chốt lời sản xuất chết hoàn toàn mà suite vẫn xanh**. ✅ `MT-20`/`DR-D4-06` **KHÔNG lung lay** (kiểm độc lập: hai script đo 73-87% chỉ import module thuần + `read_feather`). 🔑 **Hình dạng lỗi THỨ NĂM: kiểm BÊN SINH không kiểm được ĐƯỜNG ĐI** — docstring `td0189-*.py` kết luận *"`_quet_zone_dinh` HOẠT ĐỘNG ĐÚNG"*, và câu đó ĐÚNG; bên đọc mới là chỗ hỏng. ⏳ **Bản vá tách TD riêng** — `merge_informative_pair(ffill=True)` cho giá trị LẶP, đổi tên cột mà không xử ffill là đổi im lặng ý nghĩa TP1 (lớp `L-Z48c`); và vá xong đổi mọi con số D4 đã đo |
| TD-0207 | 🔴 **Vá lỗi TD-0206 phát hiện: `_zone_dinh_tren` và `_tuoi_zone_dinh_nen` đọc cột `zone_dinh_gia` từ `_df_4h()` — nơi cột đó KHÔNG BAO GIỜ có mặt.** `_df_4h` gọi `dp.get_pair_dataframe()` trả OHLCV thô đúng 6 cột; cột thật tên `zone_dinh_gia_4h` và nằm trên dataframe đã merge (`ZoneAbsorption.py:301`). Đo được 22/22 lần vắng mặt ⇒ TP1 **luôn** rơi nạng, H-4 = 100% là defect. `_tuoi_zone_dinh_nen` chết theo ⇒ `tp_zone_age_bars` luôn `None` ⇒ **ràng buộc 1 của `DR-D4-06` chưa bao giờ thoả được** | ✅ | TD-0206 ✅ | **DR-012 Hạng 1** (mã không khớp spec) ⇒ **0 trial, không cần DR mới**. 🔑 **Chọn cách vá — TÍNH LẠI bằng chính `_quet_zone_dinh` trên khung 4H đã cắt, KHÔNG đọc cột đã merge.** Ba lý do: (i) cột đã merge mang `ffill=True` nên `notna()` cho **giá trị LẶP** — vô hại với `_zone_dinh_tren` (dùng `unique()`) nhưng **chết người** với `_tuoi_zone_dinh_nen`, vốn lấy `khop.iloc[-1]` ⇒ sau ffill sẽ ra tuổi ≈ 0 ở MỌI lệnh, sai im lặng đúng lớp `L-Z48c`; (ii) `merge_informative_pair` dịch mốc ngày nên tuổi tính từ cột merge lệch so với nến xác nhận thật; (iii) tính lại trên khung ĐÃ CẮT là lookahead-safe **theo cấu trúc** — zone chưa đủ `K_XAC_NHAN` nến thì tự động chưa xác nhận. Dùng LẠI `_quet_zone_dinh`, **không** viết lại phép tính nào (khác hẳn MT-03: đây là gọi cùng một hàm, không phải bản sao thứ hai). ⚠️ **Vá xong ĐỔI MỌI CON SỐ D4 đã đo** (`td0193`, `td0205`, `dg2-explore-quet-arm`, `td0206`) ⇒ ghi như `DR-D4-08` §8. Test khoá cho **CẢ HAI** hàm (hiện `_tuoi_zone_dinh_nen` chết theo nên chưa ai kiểm nó) + **phá thật**. Bằng chứng trong Docker (N7) \| \| **Xác nhận** (`61479b7` mã + test khoá, `e6c1bfc` fixture kéo theo): full suite **1655 passed, 0 failed**. Đo lại trên 88 mã EXPLORE (`td0207-h4-sau-va.json`, 0 trial): `tp1_theo_nguon` lật từ **6 nạng / 0 zone** sang **7 zone / 1 nạng** ⇒ **H-4 từ 100% → 18,2%**, dưới ngưỡng 40% của Nhánh 1. `exit_reason` đổi đúng chiều (`TP2_TRAIL` 6→8, `trailing_stop_loss` 10→8); **số lệnh KHÔNG đổi** (22→22, 44,8/năm) ⇒ `n = 28` và sàn Sharpe **0,578 giữ nguyên**. 🔑 **Kiểm có răng bằng HAI phép phá độc lập**: trả `_zone_dinh_tren` về cách đọc cột cũ ⇒ đúng **2 ca đỏ**; đổi `_tuoi_zone_dinh_nen` lấy mốc CUỐI (hình dạng lỗi ffill) ⇒ đúng **1 ca đỏ**. Không lần nào lan ra cả bộ. 📌 Cách vá: **tính lại bằng chính `_quet_zone_dinh`** trên khung 4H đã cắt (`_zone_dinh_da_xac_nhan`), KHÔNG đọc cột đã merge — vì `ffill=True` cho giá trị LẶP, vô hại với `_zone_dinh_tren` nhưng làm `_tuoi_zone_dinh_nen` ra tuổi ≈ 0 ở MỌI lệnh (lớp `L-Z48c`), mà `DR-D4-06` §2.3 lại dựa vào chính con số đó. Sinh **MT-30** (phép lọc `> p_avg` dùng `p_avg` cũ) — ghi, không tự sửa |
| TD-0210 | 🔴 **Fixture `test_td0187` LỖI THỜI LẦN THỨ TƯ sau bản vá TD-0207 — 5 test khoá `test_td0189` đỏ.** Vá làm TP1 chuyển từ nạng sang zone đối diện, và zone nằm XA HƠN nạng (thiết kế cho phép tới `3,2R` vs nạng `1,5R` — chính là "vách 2,1 lần" `CLAUDE.md` đã ghi). Đường giá đuôi dựng tay không còn với tới TP1 ⇒ lệnh thoát bằng `TIME_STOP` ⇒ 5 ca đòi hình dạng `[buy×3, TP1, TP2]` đỏ | ✅ | TD-0207 ✅ | **Đo trước, không đoán** (log `TP_CHON`/`TP_ZONE_UNGVIEN` của TD-0206 trên chính lượt chạy fixture): `xac_nhan=2 tren_p_avg=2` · `nguon=zone_doi_dien` · `gan_nhat_r` 2,2617→2,4946→2,7377 · `open_rate=94,97212` ⇒ TP1 ≈ **96,7165**. `TIME_STOP` cắt ở nến **898** (874 + `max_hold_bars_4h`=24) nên đỉnh DÙNG ĐƯỢC chỉ **96,48**, không phải 96,59 của cả đuôi ⇒ hụt **0,237**. 🔑 **Không sửa một dòng `assert` nào** — sửa **bộ sinh**, đúng tiền lệ TD-0182/TD-0194/TD-0193 (*"fixture phải sửa vì hệ thống đổi, không phải để test xanh"*). Sweep đo được: `s=0,02` vẫn TIME_STOP · `s=0,025` đạt (lề 0,034) · `s=0,03` đạt (lề 0,124) — **chọn 0,03**: 0,025 chỉ hơn TP1 khoảng 4% của một `R_eff`, mà TP1 tính động từ ATR và `p_avg`, đúng loại *"chốt thoả bằng đẳng thức chính xác"* mà `DR-D4-05` đã bác khi chọn `E_D` 750 thay 720. Bốn bất biến đã kiểm giữ nguyên: `_dem_zone(dinh)=2` · `_i_cham_p1=874` · `low` tăng đơn điệu · không đụng chỉ số nến 848/861 của TD-0194. **Thêm đối chứng THƯỜNG TRỰC thứ BA** (*"đỉnh giá sau nến vào lệnh phải > TP1 đã chọn"*) — thứ duy nhất bắt được lần thứ NĂM. Phá thật hai chiều \| \| **Xác nhận** (`e6c1bfc`): full suite **1655 passed, 0 failed** (1648 + 7 của TD-0207). 🔑 **`git diff` trên `test_td0189_chot_loi_noi_vao_chien_luoc.py` RỖNG — không một dòng `assert` nào bị sửa**, đúng cam kết; chỉ bộ sinh đổi. Đo trên đường chạy thật TRƯỚC khi chạm fixture: `TP_ZONE_UNGVIEN xac_nhan=2 tren_p_avg=2` · `TP_CHON nguon=zone_doi_dien gan_nhat_r` 2,2617→2,4946→2,7377 · `open_rate=94,97212`. **Phá thật**: hạ độ dốc về 0,01 ⇒ đối chứng thường trực thứ BA nổ **trong 1,41 giây ngay tại bộ sinh** (trước khi backtest chạy), nêu đích danh zone 97,1237 và TP1 96,6990. 🔴 **Hai lỗi của chính máy canh này, tự bắt và ghi lại**: (i) bản đầu dùng `p1 − sl` làm `R_eff` — sai, `R_eff` neo vào `p_avg`, lệch **43%** (1,136 vs 0,797), đúng lớp `L-Z48c`; (ii) bản đầu đòi đỉnh giá vượt TP1 **xa nhất về lý thuyết** (zone đúng trần `4,0×R_eff`) trong khi fixture không có zone ở đó ⇒ *"chốt không bao giờ thoả được"* (bài học cổng D3) — sửa thành neo vào zone CÓ THẬT. 📌 Ghi thêm một lỗi thao tác: `git checkout --` trên file có thay đổi **chưa commit** đã xoá sạch bản dựng đầu, phải làm lại — bài học: commit TRƯỚC khi phá thật, như TD-0207 đã làm đúng |
| TD-0211 | 🚪 **Tái định phạm vi D4 theo cỡ mẫu — `DR-D4-10`.** Chủ dự án chốt **phương án A** (10/09/2026) sau phân tích của TD-0208. 🔴 **Căn cứ là một phép tính 0-trial, không phải một ý kiến:** `DR-D4-09 §2.2` định nghĩa INCONCLUSIVE = *thuế nhiễu > ngưỡng của chính tiêu chí đó*; nhóm C (7/9 arm) có `n ≈ 28` (TD-0205 **đo**, không ngoại suy) ⇒ thuế nhiễu `= 0,578 × std_R` > ngưỡng 0,10 R với **mọi** `std_R ≥ 0,18` ⇒ **INCONCLUSIVE chứng minh được TRƯỚC khi chạy, không cần chạm dữ liệu**. Nhóm C cần `mean_R` **0,44–0,82 R** để PASS — ngoài tầm mọi hệ thống thật. Và trần trên của MỌI đòn bẩy cộng lại (pool 204 mã ×2 · CALIB+WFO ×2,86) chỉ cho `n ≈ 162`, chưa bằng một nửa 370 cần thiết ⇒ **"ngoài tầm với", không phải "thiếu mẫu"** | ✅ | TD-0208 ✅ · TD-0205 ✅ | **0 trial** — DR không đánh giá cấu hình nào. Commit **RIÊNG và TRƯỚC** mọi dòng mã thi hành; đã báo phiên `c3` trước khi mở file (N12 mục 6). 🔴 **KHÔNG đổi ngưỡng nào** (`0,10 R` · `≥ 20%` · `150 lệnh/năm` · `N = 114` · rào `3,0777` giữ nguyên), **KHÔNG đụng `DR-D4-01`** (Long-only, 9 trial, đặt chỗ cả lô), **KHÔNG mở lại** phương án *"chạy từng arm rồi dừng giữa chừng"* mà chủ dự án đã từ chối ở `DR-D4-01 §4b`. Chỉ chốt **D4 được hỏi câu gì**. Ghi đính chính tại chỗ cho `DR-D4-09 §1.1` (dùng `n ≈ 40` ngoại suy từ `td0193`; TD-0205 đo ra 28 ⇒ mọi bảng §1.2/§1.3/§4 lạc quan ~19%) — **không sửa file đó** (quy tắc 5). 🔴 Viết **TRƯỚC khi tồn tại một con số expectancy nào** của Tool D — đây là lúc DUY NHẤT sửa phạm vi gate mà không ai nghi *"sửa cho dễ qua"* \| **Xác nhận** (`ff87475`): `docs/decisions/DR-D4-10-tai-dinh-pham-vi-d4-theo-co-mau.md`, 263 dòng, commit **RIÊNG và TRƯỚC** mọi dòng mã. **0 trial** — `trial_registry.jsonl` vẫn 13 dòng. Chốt: D4 phán quyết **hai câu**, không phải chín — `Z0-T0`/`Z0-T1` cho câu Phần 2; bảy arm nhóm C khai INCONCLUSIVE **ngay tại DR**, chín suất `B2` giữ nguyên theo `DR-D4-01` nhưng bảy suất đổi mục đích sang **thống kê mô tả**, và **CẤM** đọc `DSR_adj` của nhóm C như phán quyết. §2.2 ghim dự báo ngược: nhóm C ra kết cục ≠ INCONCLUSIVE ⇒ **nghi bộ đo trước, không mừng**. 🔴 **BA điều phiên `c3` sửa/củng cố, đã kiểm lại trên đĩa trước khi nhận:** (i) **bỏ `×2` cho Short** — spec dòng 4187 + 4973-4974 đòi chạy hai lần độc lập, *"không dùng chung kết quả"*, và `DR-D4-01 §4` tính 1 trial/1 cấu hình/**1 hướng** ⇒ mở Short không cộng `n`, nó tạo bộ arm THỨ HAI ⇒ trần **324 → 162**, kết luận **mạnh hơn** sau khi sửa (con số 324 ở bản nháp đầu của tôi là SAI, đã đính chính ngay trong dòng này); (ii) chặn ca nghi ngờ `MT-25` bằng cận `λ < 0,138` — thang `R_realized` phải co hơn **7 lần** mới lật được kết luận; (iii) **H-4 không còn hỏng** (TD-0207: 100% → 18,2%) ⇒ Nhánh 1 còn **đúng một** tiêu chí hỏng là 44,8 < 150, và cách đọc nó treo vào `MT-29`. **Không đổi ngưỡng nào** (`0,10 R` · `≥20%` · `150` · `N=114` · `3,0777`), không đụng `DR-D4-01`/`DR-D4-09`. ⏳ **Chờ chủ dự án xác nhận §2.4** — mặc định `Z0` single-entry khi câu DCA không đo được; chưa xác nhận thì câu DCA ở trạng thái mở, **không được ngầm hiểu theo hướng nào** |
| TD-0212 | **Đo lại BA arm (`Z0-T0`, `Z0-T1`, `Z0`) SAU bản vá TD-0207 — bảng cỡ mẫu đang trộn hai hệ thống.** `td0207-h4-sau-va.json` chỉ đo lại `Z0`; số của `Z0-T0` (746 lệnh) và `Z0-T1` (160) trong `td0205-lenh-nam-wfo-explore.json` vẫn là số **TRƯỚC vá**, tức của một hệ thống KHÔNG có tầng chốt lời theo zone. `DR-D4-09` §1.1 và `DR-D4-10` (phiên `-c0`) đều đang dùng hai con số đó. Phát hiện do phiên `-c0` đánh dấu khi rà `td0207-h4-sau-va.json` | ✅ | TD-0207 ✅ | **0 trial**, EXPLORE, chỉ đếm — tiền lệ TD-0193/TD-0205/TD-0206. Chạy lại chính `do_td0193_lenh_nam_explore.py` (đã có `--tu/--den/--ket-qua/--chan-doan-tp` từ TD-0205/TD-0206), cửa sổ WFO `[T1,T2]`, ghi **file MỚI** `td0212-ba-arm-sau-va.json` — không ghi đè artifact nào. 🔑 **Hai câu hỏi phép đo này trả lời, và chúng độc lập nhau:** (1) *số lệnh* có đổi không — nếu đổi thì mọi `n` và sàn Sharpe của `DR-D4-09`/`DR-D4-10` phải tính lại; lưu ý cơ chế `-c0` đã nêu đúng: Freqtrade mặc định một lệnh mở mỗi cặp nên **exit sớm hơn CÓ THỂ giải phóng chỗ cho entry sau**, tức entry/exit không độc lập ở tầng số đếm — phải ĐO, không suy (trên `Z0` đo được 22→22, nhưng `Z0-T0` có 746 lệnh nên áp lực chiếm chỗ khác hẳn); (2) *H-4 theo từng arm* — `Z0` cho 18,2%, chưa ai biết hai arm kia. ⚠️ Kết quả sẽ được gửi cho `-c0` để `DR-D4-10` không phải giữ hai hệ thống trong một bảng \| \| **Xác nhận** (`td0212-ba-arm-sau-va.json`, 88 mã, WFO, 0 trial): **chỉ `Z0-T0` đổi số lệnh.** `Z0-T0` **746 → 686** (−60, −8%) ⇒ `n` **960 → 883**, sàn `S` 0,099 → **0,104**. `Z0-T1` **160 → 160** và `Z0` **22 → 22** ⇒ `n` = 206 và 28, sàn `S` **không đổi**. H-4 tụt dưới ngưỡng 40% ở **cả ba** arm: `Z0-T0` 100% → **22,0%** · `Z0-T1` 100% → **6,5%** · `Z0` 100% → **12,5%**. 🔑 **Kết quả BÁC một lập luận tôi từng đưa** (*"đếm lệnh không phụ thuộc tầng chốt lời"*) và xác nhận cảnh báo cơ chế của phiên `-c0`: Freqtrade mặc định một lệnh mở mỗi cặp, nên exit sớm hơn **giải phóng chỗ cho entry sau** — entry/exit KHÔNG độc lập ở tầng số đếm. Trên `Z0` (22 lệnh) không thấy vì gần như không có cạnh tranh chỗ; trên `Z0-T0` (~700 lệnh) thì hiện. ⇒ *"`Z0` 22→22"* là một **QUAN SÁT trên một arm ít cạnh tranh**, KHÔNG phải một nguyên tắc áp được sang arm khác — đúng lớp lỗi *"sai ở NHÃN dán lên phép đo"*. 📌 Điều kiện 3 của `DR-D4-10` §7 (phiên `-c0`) **KHÔNG kích hoạt**: `Z0-T1` giữ nguyên 160, không rơi dưới 100 |
| TD-0213 | **Đo ba arm DCA (`Z3`, `Z3b`, `Z2`) SAU bản vá TD-0207** — phiên `-c0` đặt hàng cho `DR-D4-10` §2.3, vì `dg2-explore-quet-arm.json` (48 mã, [T0,T2], trước cả TD-0193 lẫn TD-0207) là con số DCA duy nhất đang có | ✅ | TD-0207 ✅, TD-0212 ✅ | ⚠️ **Thứ tự BỊ ĐẢO, khai thẳng:** phép đo **chạy TRƯỚC** khi khoá 🔒, lệch quy tắc 14. Rủi ro va chạm bằng 0 (phiên `-c0` là người đặt hàng), nhưng quy tắc 14 tồn tại cho ca **không** ai đặt hàng, nên ghi ra thay vì bù dòng cho trông như đã khoá trước. **Xác nhận** (`td0213-arm-dca-sau-va.json`, 88 mã, WFO, 0 trial): cả ba arm đều **22 lệnh · 44,8/năm · `n` = 28**, **giống hệt `Z0`** ⇒ giả định *"nhóm C chung `n` vì chung tầng entry"* của `DR-D4-10` **được xác nhận sau vá**. 🔑 Không mâu thuẫn TD-0212 (`Z0-T0` −8%): cùng một cơ chế tranh chỗ ở hai mật độ — 686 lệnh thì hiện, 22 lệnh trên 88 mã thì không có gì để tranh. `phan_bo_tranche` cả ba = `{1:12, 2:8, 3:2}` ⇒ **đủ 3 tranche = 9,1%** (số cũ 18,07%, giảm nửa, đúng chiều dự báo) — nhưng theo lập luận vòng tròn của `-c0`, con số này **không** trả lời được *"DCA có đáng không"* (nó phản ánh chính luật *"đã chốt TP1 thì không DCA thêm"*), nên chỉ dùng làm **thống kê mô tả**. H-4 = **12,5%** cả ba. 🔴 **Phát hiện chính, ngoài câu hỏi ban đầu: `Z3b` lệch `Z3` ĐÚNG MỘT LỆNH** (`DG6_EARLY_INVALIDATION` 1/22) — mà `Z3b` vs `Z3` chính là chiều so sánh dành cho DG6. Khác hạng với "thiếu mẫu": **không cần giả định `std_R` nào**, một khác biệt một-lệnh không phân xử được *"vượt ≥ 20%"* dù phương sai bằng bao nhiêu. Đã vào `DR-D4-10` §2.5. `Z2` **trùng khít** `Z3` ⇒ DG5 không ràng buộc lần nào, xác nhận lại sau vá |
| TD-0214 | 🔴 **Ba arm CUỐI chưa ai đo sau vá: `Z1`, `Z0-V1`, `Z0-S1`.** Phiên `-c0` bắt được một dẫn chiếu lỗi thời của tôi: tôi nhắc *"`Z1 ≡ Z0` (MT-21)"* như kết luận còn hiệu lực, nhưng `MT-21` là phát hiện **TRƯỚC** `TD-0192` (✅ đã nối `Z1`) và `TD-0193` (✅ đã nối §3.3b) — kiểm trên đĩa: cả hai đều ✅, và `Z1`/`Z0-V1`/`Z0-S1` **không có mặt trong `td0205`, `td0212`, `td0213`**. 🔑 Đây đúng cái bẫy `MT-21` sinh ra để chống: một arm bị khai là trùng vì **chưa được nối**, rồi kết luận đó sống sót qua chính bản vá đã nối nó | ✅ | TD-0207 ✅, TD-0212 ✅, TD-0213 ✅ | **0 trial**, EXPLORE 88 mã, WFO `[T1,T2]`, sau vá — cùng script và cùng cờ với TD-0212/TD-0213, ghi file MỚI. Khoá 🔒 **TRƯỚC** khi chạy lần này (TD-0213 bị đảo thứ tự, không lặp lại). Đo xong thì **cả 9 arm** của `ARM_HOP_LE` có số trên cùng một hệ thống, lần đầu tiên. 🔴 Câu phải trả lời: ba arm này có **phân biệt được** với `Z0` không — `Z2≡Z3` và `Z3b≈Z3` (lệch 1 lệnh) đã biết; nếu ba arm còn lại cũng gần-trùng thì **bảy suất mô tả của nhóm C mua rất ít thông tin**, và đó là dữ kiện chủ dự án cần trước khi quyết `DR-D4-10` §2.4 và trước khi cân nhắc kế toán 9 suất của `DR-D4-01`. ⚠️ **Không tự mở lại kế toán đó** — chỉ cung cấp số \| \| **Xác nhận** (`td0214-ba-arm-cuoi-sau-va.json`, 88 mã, WFO, 0 trial): **lần đầu cả 9 arm có số trên CÙNG một hệ thống.** `Z1` **6 lệnh** (`n`=8, sàn `S`=1,108 — đuối nhất trong chín) · `Z0-S1` **24** (`n`=31) · `Z0-V1` **31** (`n`=40). 🔴 **BÁC khẳng định tôi đưa cho `-c0` ở TD-0213**: nhóm C **KHÔNG** cùng `n`=28 — thật sự là **8 → 40, chênh 5,2 lần**; tôi kết luận từ BA arm rồi phát biểu cho cả BẢY (cùng lớp lỗi *"kết luận rộng hơn phạm vi đã đo"*). Bảng `n` của `DR-D4-10` phải tách theo arm; đã báo `-c0`. 🔑 **Hai kết luận-trùng cũ HẾT HIỆU LỰC bằng phép đo:** `MT-21` (*"`Z1 ≡ Z0`"*) — `Z1` nay 6 vs `Z0` 22 ⇒ `TD-0192` có tác dụng thật, cắt 22→6; `MT-15` (*"`Z0` trùng khít `Z0-V1`"*) — 31 vs 22 ⇒ `TD-0193` đã nối §3.3b. **Cả hai chết theo hướng làm bài toán KHÓ hơn**: không phải arm thừa mà là arm thật với cỡ mẫu riêng, phần lớn nhỏ hơn `Z0`. Nguồn chênh **không phải tranh chỗ** (giả thuyết đầu của tôi) mà là **tầng entry khác nhau**: `Z1` đổi SL ⇒ `r_eff_plan` đổi ⇒ cỡ lệnh đổi ⇒ cổng kết nạp §6.8f chặn khác; `Z0-V1` tắt điều kiện (c) volume ⇒ nhiều tín hiệu hơn. 📌 **MT-26 nay định lượng được trên dữ liệu đủ**: xếp hạng arm theo `DSR_adj` thô cộng không cho `Z0-T0` **+1,256 R = 12,6 lần** ngưỡng 0,10 R so với `Z1`, thuần do cỡ mẫu |
| TD-0215 | 🔴 **`Z1` mất 73% số lệnh so với `Z0` (22 → 6) — nếu 6 lệnh đó là TẬP CON của `Z0` thì phép so `Z1` vs `Z0` KHÔNG còn so hai kiểu SL.** Nó sẽ đang so *"SL neo zone trên 22 lệnh"* với *"SL theo `2,2×ATR` trên một tập con 27% đã bị lọc bởi một cơ chế KHÁC"* (cổng kết nạp §6.8f: `Z1` đổi SL ⇒ `r_eff_plan` đổi ⇒ cỡ lệnh đổi ⇒ kết nạp chặn khác). Mà `Z1` chính là arm §10.1 dùng để trả lời *"SL neo zone có tốt hơn SL theo ATR không"*. Câu hỏi do phiên `-c0` nêu khi đọc TD-0214 | ✅ | TD-0214 ✅ | **0 trial**, EXPLORE 88 mã, WFO, cùng script/cờ. 🔑 **Phải chạy `Z0` và `Z1` trong CÙNG một lượt** — `so_tap_lenh` chỉ so các arm trong cùng lần gọi, và `_tap_lenh` bị `pop` trước khi ghi artifact nên không ghép được hai file đã có (`td0212` có `Z0`, `td0214` có `Z1`). 🔴 Ba kết cục, ba cách đọc khác nhau: (a) `n_giao = 6` và `Z1` ⊂ `Z0` ⇒ **phép so bị nhiễu loạn**, `Z1` vs `Z0` không trả lời được câu SL — hạng hỏng mới, khác `§2.5` của `DR-D4-10` (*"biến không đổi"*) vì đây là *"biến đổi KÈM một thứ khác đổi theo"*; (b) `n_giao < 6` ⇒ `Z1` có lệnh mà `Z0` không có ⇒ hai tập giao nhau một phần, phải so KHÔNG paired; (c) `n_giao = 6` nhưng không ⊂ ⇒ cần xét riêng. Kết quả gửi `-c0` để quyết có ghi vào `DR-D4-10` không \| \| **Xác nhận** (`td0215-z1-vs-z0-tap-lenh.json`, 88 mã, WFO, 0 trial, `Z0`+`Z1` CÙNG một lượt): **kết cục (a)** — `n_A=22 · n_B=6 · n_giao=6 · chi_co_o_A=16 · chi_co_o_B=0`. `Z1` **không có lệnh nào ngoài `Z0`** ⇒ tập con nghiêm ngặt; **72,7% lệnh của `Z0` biến mất trong `Z1`**, và biến mất ở **cổng kết nạp §6.8f**, không phải vì SL tốt hay xấu. 🔑 **Hai vấn đề RIÊNG BIỆT, đừng gộp:** (i) **nội tại** — trên 6 lệnh giao, `Z1` vs `Z0` khác đúng MỘT biến (kiểu SL), nên phép so paired ở đó **hợp lệ về cấu trúc**; (ii) **ngoại suy** — 6 lệnh đó là tập được CHỌN bởi chính cơ chế tương quan với biến đang xét (cỡ lệnh, vốn là hàm của SL), nên kết quả **không suy rộng ra 22 lệnh**. Cộng thêm `n_giao=6` cho thuế nhiễu paired **0,497 → 1,571 R** tuỳ `ρ` = **5,0 → 15,7 lần** ngưỡng 0,10 R. Ngay trên 6 lệnh chung hành vi đã khác hẳn: `Z1` có `TIME_STOP` **2/6 (33%)** trong khi `Z0` **0/22**. 🔴 **Đây là hạng hỏng THỨ BA, khác hai cái đã ghi**: `MT-21`/`MT-15` là *"biến không đổi gì"* (arm trùng); `DR-D4-10` §2.5 là *"biến so sánh gần như không tồn tại"* (`Z3b` lệch `Z3` một lệnh); cái này là ***"biến đổi KÈM một thứ khác đổi theo"*** — arm khác thật, nhưng khác vì hai lý do trộn vào nhau, trong khi §10.1 thiết kế mỗi arm để đổi **đúng một** biến. ⏳ Đề xuất ghi thành **MT mới** — chờ lệnh "chuẩn hóa và lưu" |
| TD-0208 | **Đối chiếu checklist anti-overfitting ngoài (5 giai đoạn: rule-based → hyperopt → dry-run → FreqAI → live vốn nhỏ) vào lộ trình D0-PRE→D12** — chủ dự án đưa 10/09/2026. 🔴 **2/5 giai đoạn của checklist là thứ Tool D CẤM TUYỆT ĐỐI** (GĐ2 Hyperopt `tool-d-smart-dca.md:448`, GĐ4 FreqAI `:462`) ⇒ áp dụng theo nghĩa đen là mời `L-Z25` vô hiệu hoá toàn bộ `trial_registry`. Ba giai đoạn còn lại Tool D **chặt hơn** checklist. Giá trị thật nằm ở ba mục dự án **không có lớp canh nào**: GĐ1.1 (số điều kiện entry), GĐ1.3+1.4 (≥100 lệnh + expectancy dương ở backtest thô TRƯỚC khi tối ưu), GĐ3.3+3.4 (cảnh báo tự động + edge case) | ✅ | — | **0 trial** — chỉ đọc tài liệu + đối chiếu file trên đĩa, không chạm CALIB/WFO/LOCKBOX (N2). Địa vị đã chốt với chủ dự án: **tài liệu tham chiếu ngoài, audit MỘT LẦN**, KHÔNG phải nguồn sự thật (N1), không có nghĩa vụ cập nhật ⇒ tránh MT-03. Ghi ra `docs/doi-chieu-checklist-antioverfitting.md`. 🔴 **Mọi con số phải trích từ file trên đĩa kèm `file:dòng`**, không chép từ mục "TRẠNG THÁI HIỆN TẠI" của `CLAUDE.md` (N12 mục 3: dòng mô tả việc là lời khai, không phải bằng chứng). 🔴 **KHÔNG thêm ngưỡng "chênh IS/OOS > 30-40%" của checklist** — đó là tiêu chí thứ hai cho câu hỏi §10.2 đã trả lời bằng DSR + PBO (đúng thứ MT-03 cấm). **KHÔNG mở việc trùng:** cỡ mẫu → TD-0184; 7/12 tham số không có đường đọc → MT-23/TD-0195; "2-4 điều kiện entry" → chính là ablation D4. §3 của tài liệu liệt kê phương án cho bế tắc cỡ mẫu **kèm đánh đổi, KHÔNG tự chọn** (quy tắc 2 + 11) \| **Xác nhận** (`dc02170` TASKS + `2adec39` tài liệu): `docs/doi-chieu-checklist-antioverfitting.md`, 200 dòng, **0 trial** — `trial_registry.jsonl` vẫn **13 dòng**, không đặt chỗ nào. Trích dẫn đã verify lại trên đĩa: spec `:448` `:462` `:2954` `:4260` `:4274` `:3605`, `gates/dsr.py:24` (N=114), `gates/thresholds.py:24` (0.10), `config/tool_d_config.yaml:122-132`, `param_status.yaml` (12/12 FROZEN + `chua_calibrate`), `runs/` (0 thư mục trial, 0 file `.seal`), `dg2-...json` (Z0-T0: 4.119 `sizing_error`, rớt 42 mã). 🔑 **Con số trung tâm lấy từ TD-0205, KHÔNG dùng 63,6 của TD-0193** — 63,6 là của [T0,T2]; cửa sổ ablation thật là WFO, nơi `Z0` chỉ có **22 lệnh ⇒ 44,8/năm ⇒ n ≈ 28** (tự tính lại từ mốc trong config: 44,8 × 0,632 = 28,3, khớp TD-0205). 🔴 **Bắt được một phát biểu vượt quá phép đo của chính bản nháp đầu, sửa TRƯỚC khi commit:** `td0195` ghi `khop_yaml: false` cho `funding_rate_pct` — trông như lệch 100 lần, nhưng phép so đó **mù đơn vị** (khoá tên `_pct`, chú thích hằng số viết `-0.05%` ⇒ cùng một giá trị); đổi thành ghi chú **đơn vị chưa chốt**, không kết luận là bug. Số dòng `:25` trong JSON cũng đã lỗi thời (thực tế `:38`). ⚠️ **Hạn dùng ghi ở §0:** mọi con số lệnh đo TRƯỚC bản vá TD-0207 (`61479b7`). 🔴 **VA CHẠM MÃ VIỆC:** phiên khác cũng lấy TD-0208 ở `b6fc2fc` (fixture `test_td0187`). Đã nhắn cả hai phiên Smart DCA; đề xuất bên kia đổi sang **TD-0210** theo CẢ HAI tiền lệ (commit trên đĩa — TD-0192; và định danh máy đọc được — TD-0119/0120: số này đã nằm trong nội dung file đã commit). **Không tự sửa dòng của họ.** ⏳ **Chờ lệnh "chuẩn hóa và lưu"** để ghi hai mục MT đề xuất ở §4 vào `back-end-note.md` |
| TD-0209 | 🔓 **Heartbeat/cảnh báo khi tiến trình Freqtrade ở trạng thái `stopped`/idle mà không ai biết** — mục (2) của dòng `D10–D12` ở bảng "Việc đã biết là sẽ có, chưa mở", đã nêu từ 09/09/2026 nhưng **chưa từng có mã việc**; TD-0208 đối chiếu checklist ngoài bắt lại đúng chỗ này (GĐ3.3 *"có cơ chế alert tự động khi có dấu hiệu bất thường"*). Đo trên đĩa 10/09/2026: chuỗi `telegram` chỉ xuất hiện ở `config/freqtrade/config.json` (mặc định tắt) và trong spec/template — **không có module cảnh báo nào trong `src/`**. 🔑 Khác Risk Supervisor §6.6: §6.6 giám sát margin/thanh lý, **không** giám sát "tiến trình có đang chạy hay không" | ✅ | — (xem đính chính) | **Hạn: trước D11 (dry-run)** — dry-run không có cảnh báo thì không quan sát được chính thứ nó sinh ra để quan sát; cùng hạn với OQ-13. Ranh giới: **chỉ** heartbeat/cảnh báo tiến trình — rate-limit + circuit breaker đã xong ở TD-0197 ✅. ✅ **Đính chính 13/09/2026:** mục (3) của dòng `D10–D12` (validate fail-closed khi thiếu `BINANCE_API_KEY`/`BINANCE_API_SECRET`) đã tách số riêng và ĐÓNG — xem **TD-0242** (`bbf16f1`). ⏳ **Chuẩn bị gate check 13/09/2026 (chưa code, vẫn 🔓):** chủ dự án chốt qua trao đổi trực tiếp ba điểm — kênh **Telegram bot**, phát hiện bằng **file heartbeat** (không chỉ kiểm PID), watchdog là **tiến trình tách riêng** (không phải thread trong Freqtrade). Telegram là dịch vụ ngoài MỚI ⇒ theo quy tắc 17 đã điền `provider-map.md` + `api-integration-rules.md` Mục 4.1-4.4b (dịch vụ #3, endpoint, mã lỗi, 9 tham số ngưỡng) **TRƯỚC khi cho phép "bắt đầu code"**. 9 tham số ở Mục 4.4b (ngưỡng 300s, chu kỳ watchdog 60s, retry 3 lần...) là **ĐỀ XUẤT, CHỜ XÁC NHẬN** — chưa chốt bằng DR, cùng khuôn OQ-09/TD-0196. 🔴 **Đính chính riêng (phiên `-ef`, 13/09/2026):** cột phụ thuộc từng ghi `OQ-09 (ngưỡng circuit breaker §6.6)` là **ghi nhầm lúc tạo việc** — chính ghi chú bên trái đã tự nhận TD-0209 KHÔNG cùng phạm vi §6.6, nên OQ-09 (nay ✅ đã giải trong `back-end-note.md`, không liên quan việc này) chưa từng thật sự chặn TD-0209. Ngưỡng 300s/kênh Telegram ở trên là quyết định ĐÚNG chỗ cần, không phụ thuộc OQ-09. ✅ **Xác nhận 14/09/2026 (`faaafc0` code, `e166e65` gate-check doc):** tầng thuần xong — `src/tool_d/ops/{heartbeat,telegram_client,heartbeat_watchdog}.py`, 56 test, Docker (N7) **56 passed**, kiểm-có-răng (phá `quyet_dinh_loai_tin` → đúng 3 ca đỏ). Đổi 1 tham số lúc code so với đề xuất: bỏ backoff nội bộ 3 lần cho Telegram, dùng chu kỳ poll 60s làm cơ chế thử lại (ghi tại chỗ ở `api-integration-rules.md` 4.4b). ⚠️ **CHƯA nối vào Freqtrade/launcher thật** (D10-D12 chưa mở) — hai TODO cuối `heartbeat_watchdog.py`: (1) cơ chế khởi chạy watchdog trên máy thật, (2) xác nhận Freqtrade có gọi vòng lặp worker ở CẢ RUNNING lẫn STOPPED hay chỉ RUNNING (chưa đọc mã nguồn cho câu này, rule 6). Việc coi là ĐÓNG ở phạm vi "hàm thuần" — phần nối tiến trình thật để lại cho khi D10-D12 mở, cùng khuôn TD-0196/TD-0242 |
| TD-0226 | **Nộp DCA vào Idea Queue theo `DR-D4-10 §2.4` điều 2 + §8 chặng (f).** Chủ dự án chốt 12/09/2026: hệ thống mặc định **`Z0` single-entry**; DCA ba tranche **KHÔNG bị bác bỏ** mà chuyển thành giả thuyết chờ, vì nó **chưa từng được đo** chứ không phải đã thất bại | ✅ | DR-D4-10 ✅ (`fc53a54`) | **0 trial** — nộp đơn là cửa NHẬP, không phải đánh giá cấu hình. Dùng `E6 --nop-y-tuong` (TD-0124), **không** thêm entrypoint thứ 9. `mechanism` = *"hạ giá vào trung bình ba tranche neo zone"*. 🔴 Ô lý do phải mang **nguyên văn** *"chưa từng được đo, không phải đã thất bại"* — `DR-D4-09 §2.2` sinh ra đúng để chặn việc đọc INCONCLUSIVE thành FAIL. 🔴 Điều kiện mở lại ghi thẳng vào đơn: **`n ≥ 319`** trên một cấu hình cố định; ở 44,8 lệnh/năm là **7,1 năm** ⇒ đây là điều kiện về **nâng tần suất**, không phải về chờ đợi. ⚠️ `data_source = EXPLORE` và `explore_evidence` bắt buộc (TD-0126 chặn fail-closed): mọi số dẫn đều là EXPLORE, 0 trial — `td0212`/`td0213`/`td0214`/`td0215`. Kiểm trần NHẬP 10 đơn/quý (TD-0124) trước khi nộp. ✅ **Xác nhận `1a00c66` (14/09/2026):** `E6 --nop-y-tuong` → **`IQ-0001`**, exit 0, sổ `registry/idea_queue.jsonl` **0 → 1 dòng** (21 khoá; 7 khoá cửa CHỌN đều `None`). Audit ngay sau khi ghi: **9/14 đạt, 0 chưa đạt**; `N_ĐÃ_DÙNG` = 4 **không đổi** (0 trial). Trần NHẬP quý 3/2026: **1/10** — đã đếm trên sổ TRƯỚC khi nộp, không giả định. `entrypoints/` vẫn **đúng 8 file** (không thêm entrypoint thứ 9, `L-Z36`). Hai chuỗi nguyên văn bắt buộc đã kiểm **bằng máy** trước khi nộp, không bằng mắt: *"chưa từng được đo, không phải đã thất bại"* và điều kiện mở lại *"n ≥ 319"* — kèm cảnh báo kế thừa rằng ở 44,8 lệnh/năm thì `n` = 319 là **7,1 năm**, tức đây là điều kiện về **nâng tần suất**, không phải về chờ đợi. `data_source = EXPLORE` + `explore_evidence` đủ (TD-0126 fail-closed): bốn nguồn dẫn `td0213`/`td0214`/`td0215`/`td0228` đều EXPLORE 0 trial, và **không** số nào đến từ kết quả tiêu trial (sổ có 4 × `B0` + 1 × `CTRL`, **0 dòng `B2`**, cả 4 đều `outcome.expectancy: null`). 🔑 Luận cứ mạnh nhất của đơn là `td0228` đo cùng ngày: `Z0` vs `Z3` **trùng tập tuyệt đối** (`n_giao` 22/22) mà `phan_bo_tranche` khác hẳn ⇒ phép so paired `DR-D4-09` §2.1 **hợp lệ về cấu trúc**, thứ thiếu duy nhất là **CỠ MẪU** (22 vs ~7.400 cần thiết) — tức đơn này **bác bỏ được**, chỉ chưa đo được. Đơn đã điền đặt ở `runs/` (đã gitignore) rồi xoá: dòng sổ là nguồn sự thật duy nhất, giữ thêm một bản là hai nguồn cho một việc (N1/MT-03) |
| TD-0227 | 🔴 **Đổi `tier_c.arm_ablation.arm` từ `"Z3"` sang `"Z0"` — thi hành `DR-D4-10 §2.4` điều 1, `§8` chặng (h).** `config/tool_d_config.yaml:139`. Chú thích tại chỗ ghi giá trị này *"chỉ để chiến lược NẠP ĐƯỢC ngoài ablation; bộ chạy E3 ghi đè theo từng arm"* ⇒ **không** ảnh hưởng kết quả ablation, **nhưng nó CHÍNH LÀ arm chạy ở dry-run và live** — tức đúng chỗ §2.4 vừa chốt | 🔓 | DR-D4-10 ✅ · **TD-0186 (cổng D4)** | 🔴 **Đây là ĐỔI HÀNH VI HỆ THỐNG, không phải sửa cấu hình vặt** — cấu hình sẽ chạy trên tiền thật ở D12. Bắt buộc: test khoá ghim `arm == "Z0"` kèm `assert` **nêu đích danh `DR-D4-10 §2.4`** (khuôn TD-0171: ghim QUYẾT ĐỊNH ở đúng một chỗ, và chỗ đó phải nhắc tới quyết định — đổi ý thì buộc phải sửa một dòng có nhắc DR, không phải một hằng số vô danh) + **kiểm-có-răng** (trả về `"Z3"` ⇒ phải ra đúng ca đỏ đã dự kiến). ⚠️ `arm` là khoá `tier_c` (KHOÁ, `dof` không đếm) — rà kế toán DOF xác nhận đổi giá trị **không** đụng `|tier_b|` = 12 và `N` = 114 trước khi sửa. 🔴 **KHÔNG làm trước khi cổng D4 đóng** (TD-0186): đổi arm sản xuất lúc ablation chưa chạy xong là tự đổi thứ đang được đo. Bằng chứng trong Docker (N7) 🔴 **CHẶN THÊM 12/09/2026 (`MT-35`, phiên `[3f7d14]`) — CHỜ PHÁN QUYẾT TRỤC TREND, chủ dự án chốt treo việc này.** Khoá `arm` là **MỘT chuỗi nhưng gộp HAI trục**. `arms.py:TANG_THEO_ARM` ánh xạ `"Z0" → DAY_DU`, và đo trên `td0212-ba-arm-sau-va.json`: `Z0` (`DAY_DU`) = 22 lệnh = **44,8 lệnh/năm quy đổi pool — DƯỚI sàn 150**, trong khi `Z0-T1` (`CHI_4H`) = 160 lệnh = **325,6/năm** và là **cấu hình ứng viên DUY NHẤT** D4 phán quyết được (`MT-26` (C), `DR-D4-10` §2.1 `:174`). Cả ba arm đều single-entry (`phan_bo_tranche` 100% tranche 1) nên **trục DCA không phân biệt chúng**. `DR-D4-10` §2.4 chốt trục **DCA** (`Z0` thay `Z3`); **không chốt nào nói trục TREND của arm sản xuất**. ⇒ Đặt `arm: "Z0"` là cho production chạy **tiền thật** trên cấu hình mà `MT-29` đã chốt là **không thể qua sàn**, còn thứ D4 phán quyết thì là `Z0-T1`. Theo **quy tắc 11**: ghi nhận, KHÔNG tự chọn bên — `Z0 ⊆ Z0-T1` tuyệt đối (`chi_co_o_B = 0`) nên đây là *thiếu một chốt*, không phải *hai chốt va nhau*. |
| TD-0228 | 🔴 **`Z0` và `Z3` TRÙNG CỠ (22 = 22) nhưng CHƯA có phép đo nào chứng minh TRÙNG TẬP — và `DR-D4-10 §2.4` đang dựa vào giả định đó.** Kiểm trên đĩa: không file nào trong `docs/du-lieu-do/` đo cặp `Z0 vs Z3`; `td0205`/`td0212` chỉ so nội bộ `Z0-T0`/`Z0-T1`/`Z0`, `td0213` chỉ so `Z3`/`Z3b`/`Z2`, `td0214` chỉ so `Z1`/`Z0-V1`/`Z0-S1`, `td0215` chỉ `Z0 vs Z1`. 🔑 **Có lý do cụ thể để nghi, không phải phòng xa:** `TD-0214` vừa cho thấy `Z0-V1` (31) và `Z0-S1` (24) lệch `Z0` (22) dù cùng họ; và `TD-0212` cho thấy `Z0 ⊄ Z0-T0` sau vá (1 lệnh mồ côi) — giả định lồng nhau **vỡ được mà không ai nghi**, vì tranh chỗ mở làm việc nới bộ lọc **vừa thêm vừa đánh bật**. `Z3` bơm tranche 2/3 nên giữ chỗ **lâu hơn** `Z0` ⇒ đúng cơ chế đó | ✅ | DR-D4-10 ✅ (`24b1d27`) · TD-0213 ✅ · TD-0214 ✅ | **0 trial** — EXPLORE, chỉ đếm, cùng tiền lệ `TD-0213`/`TD-0215` (chạy `Z0`+`Z3` **CÙNG MỘT LƯỢT** thì mới so tập được; không ghép chéo số liệu giữa hai file khác lượt chạy). Ghi file MỚI, không đụng artifact nào. Cần `so_tap_lenh` cho `Z0 vs Z3`, `Z0 vs Z3b`, `Z0 vs Z2`. 🔴 **Cách đọc viết TRƯỚC:** `chi_co_o_B = 0` và `n_giao = 22` ⇒ trùng tập, luận điểm *"chọn `Z0` không mất lệnh nào"* của `DR-D4-10 §2.4` **đứng vững**; lệch bất kỳ ⇒ luận điểm đó **phải hạ xuống** thành *"mất ít lệnh"* kèm con số, và `§2.5` phải thêm `Z3` vào hạng (3). ⚠️ **Dù kết quả thế nào cũng KHÔNG lật `§2.4`** — ba căn cứ chính (fail-closed · lockbox một lượt · không phán quyết được ở mọi giai đoạn) không phụ thuộc phép đo này; chỉ một luận điểm PHỤ phụ thuộc. Viết ra trước để kết quả không bị đọc quá tay. ✅ **Xác nhận `0edac03` + `e3fc929` (14/09/2026):** EXPLORE 88 mã, WFO [T1,T2], 0 trial, bốn arm chạy CÙNG MỘT LƯỢT → **6/6 cặp TRÙNG KHỚP HOÀN TOÀN**; `Z0 vs Z3` = `Z0 vs Z3b` = `Z0 vs Z2` cho `n_A` 22 · `n_B` 22 · `n_giao` 22 · `chi_co_o_A` 0 · `chi_co_o_B` 0. ⇒ Điều kiện của cách đọc viết trước ĐÃ ĐẠT: luận điểm *"chọn `Z0` không mất lệnh nào"* của `DR-D4-10` §2.4 **ĐỨNG VỮNG**, và §2.5 **không** phải thêm `Z3` vào hạng (3). Cơ chế khớp luận điểm: `phan_bo_tranche` `Z0` = {1: 22} vs `Z3`/`Z3b`/`Z2` = {1: 12, 2: 8, 3: 2} — cùng tập lệnh, khác chuyện xảy ra SAU khi vào lệnh. Ba cặp `Z3`/`Z3b`/`Z2` tái lập ĐÚNG số của `td0213` (đối chứng độc lập). Không viết dòng mã nào: `do_td0193_lenh_nam_explore.py` đã có đủ `--arms` + `_so_tap_lenh` + `--tu --den` + `--ket-qua`; file MỚI `docs/du-lieu-do/td0228-z0-vs-dca-tap-lenh.json`, không đụng artifact nào. 🟡 **Ghi nhận kèm, KHÔNG tự sửa DR:** `DR-D4-10` §2.5 viết *"`Z3b` vs `Z3` đúng 1/22 lệnh"* bằng ngôn ngữ khác-biệt-TẬP-LỆNH, nhưng artifact của chính `TD-0213` ghi `trung_khop_hoan_toan: true` — thứ khác nhau là `exit_reason` (`DG6_EARLY_INVALIDATION` 1 lần). Con số 1 không sai, NHÃN dán lên nó sai; chờ "chuẩn hóa và lưu" để vào mục 7 |
| TD-0229 | 🔴 **Khoá xuất xứ thứ 8 `runtime_image_digest` (`MT-07`) là một PASS RỖNG — có kiểu dữ liệu, có schema, KHÔNG có bộ sinh giá trị, không test nào ghim, không gate nào so, và `cache_key()` cố ý loại nó ra.** Tự kiểm trên đĩa: **cả hai** nơi gọi `build_provenance()` (`src/tool_d/dr015/buoc1_lech_tranche.py:261`, `entrypoints/periodic_report.py:55`) **bỏ trống** ⇒ giá trị luôn `None`; bản ghi thật duy nhất mang nó là `registry/trial_registry.jsonl` dòng 13 với `null`; `cache_key()` (`provenance.py:125-141`) gộp `params_effective`+`git_sha`+`data_hashes`, **không có digest**; `src/tool_d/gates/` grep `image|docker` → 0. ⇒ **Đổi `docker/Dockerfile:21` hôm nay là thao tác IM LẶNG HOÀN TOÀN**: full suite xanh, 5 cổng vẫn đóng, cache vẫn trúng, không bản ghi nào ghi nhận môi trường đã đổi. Đúng hình dạng `MT-08` (chính sách chốt mà `registry.py` 0 dòng thi hành, phải mở `TD-0130` vá sau) | ✅ | — | **0 trial** — không chạm CALIB/WFO/LOCKBOX, không đánh giá cấu hình nào. 🔑 **Vì sao làm TRƯỚC khi quyết đổi ảnh:** quy tắc 9 buộc ảnh đo = ảnh chạy thật; nếu D4 tiêu suất trial rồi mới đổi ảnh thì các suất đó đo **một môi trường không phải môi trường sản xuất**, mà **trial không tái tạo được**. Nối dây bây giờ tốn vài giờ; đổi ảnh sau khi tiêu trial tốn **trial**. Và cần bất kể FreqAI có xảy ra hay không — Freqtrade sẽ lên phiên bản. 🔴 **Thiết kế — KHÁC bản duyệt ban đầu theo hướng AN TOÀN HƠN:** spec §0d.5 (dòng 597-616) liệt kê **đúng 7 khoá** và định nghĩa `L-Z40` theo đúng tập đó, nên **KHÔNG** đổi `REQUIRED_PROVENANCE_KEYS` 7→8 (làm thế thì hằng số đó thôi mang nghĩa *"tập spec đòi"* — đúng lớp trôi ngữ nghĩa dự án hay dính). Thay bằng: giữ nguyên tập 7 của spec; **thêm** một tập riêng của Tool D (7 + digest, ghi rõ căn cứ `MT-07`); `validate_provenance()` đòi digest theo tập mới; `cache_key()` gộp digest — **phần có răng thật**, vì khi đó đổi ảnh **tự động làm hỏng cache** ⇒ không thể vô tình so kết quả giữa hai môi trường, biến sáu ràng buộc giấy của `DR-FAI-01 §4b` thành một máy canh. ⇒ Dòng `assert len(REQUIRED_PROVENANCE_KEYS) == 7` của `L-Z40` **giữ nguyên và vẫn đúng**; chỉ **THÊM** ca, không sửa ca nào — nhẹ hơn hẳn điều kiện tiên quyết `DR-FAI-01 §7.1` vì không có assertion nào bị đổi. **Bộ sinh digest fail-closed** khi đọc thất bại (N6 cấm `""` / `"UNKNOWN"`). **Kiểm-có-răng bắt buộc:** đổi digest giả ⇒ `cache_key` phải đổi; bỏ digest khỏi tập Tool D ⇒ test phải đỏ; bộ sinh thất bại ⇒ phải từ chối chạy. ⚠️ **Ghi hạn chế, không giả vờ vá quá khứ:** mọi con số `td0193`→`td0215` và bằng chứng 5 cổng đã đóng đều **không mang digest** và **không truy ngược được**; nối dây chỉ bảo vệ từ nay. Bằng chứng full suite trong Docker (N7). **Xác nhận** (`5b34e1b`): full suite Docker **1673 passed, 0 failed** (2:46). **0 trial**, sổ `trial_registry.jsonl` vẫn 13 dòng. Bốn thay đổi: (1) `doc_runtime_image_digest()` — bộ sinh **fail-closed**, hai vế đều cần: dấu hiệu `/opt/freqtrade_source_commit.txt` (đang chạy trong ảnh CỦA PROJECT) **và** dòng `FROM ...@sha256:` của `docker/Dockerfile` (digest GHIM) — vế 1 một mình không nói ảnh nào, vế 2 một mình chỉ nói *ghim* gì chứ không nói đang *chạy* gì; đọc thất bại ⇒ `ImageDigestError`, không trả `""`/`"UNKNOWN"` (N6); (2) hai nơi gọi truyền digest, không bắt lỗi — cùng lý do `build_provenance` không bắt `GitInfoError`; (3) **`cache_key()` gộp digest — phần có răng thật**: đổi ảnh ⇒ khoá cache đổi ⇒ KHÔNG THỂ vô tình dùng lại kết quả cũ cho môi trường mới, biến sáu ràng buộc GIẤY của `DR-FAI-01 §4b` thành một máy canh; (4) `validate_fold_record()` gọi bản Tool D. 🔑 **Thiết kế khác bản duyệt ban đầu theo hướng AN TOÀN HƠN, và nó là phần đáng giữ:** giữ `REQUIRED_PROVENANCE_KEYS` = **7 khoá spec, không sửa một ký tự**, thêm `KHOA_XUAT_XU_TOOL_D` = 7 + digest cùng `validate_provenance_tool_d()` riêng ⇒ *"spec đòi"* và *"Tool D đòi thêm"* kiểm được riêng. **Dòng `assert len(REQUIRED_PROVENANCE_KEYS) == 7` của `L-Z40` giữ nguyên và vẫn đúng; KHÔNG sửa một ca nào của `L-Z40`** — chỉ thêm file test mới, nhẹ hơn hẳn điều kiện tiên quyết `DR-FAI-01 §7.1` vì không assertion nào bị đổi. **Kiểm-có-răng, ba phép phá thật trong Docker, mỗi phép ra ĐÚNG ca đã dự kiến:** bỏ digest khỏi `cache_key` ⇒ **2 đỏ** (`TestCacheKeyCoRang`), 16 xanh · cho `null` qua ⇒ **1 đỏ** (`test_null_BI_TU_CHOI`), 17 xanh · bộ sinh trả `"UNKNOWN"` ⇒ **1 đỏ** (`test_ngoai_docker_thi_tu_choi`), 17 xanh. Khôi phục xong `diff -q` ⇒ **giống byte-đối-byte**. ⚠️ **Hạn chế đã ghi, không giả vờ vá quá khứ:** mọi con số `td0193`→`td0215` và bằng chứng 5 cổng đã đóng đều **không mang digest** và **không truy ngược được** — nối dây chỉ bảo vệ từ nay. ⏳ **Mở đường cho việc 2:** câu *"có đổi ảnh sang `_freqai` không"* nay an toàn để hỏi |
| TD-0230 | 🔴 **Pool mà D4 sẽ backtest là ẢNH CHỤP NGÀY HÔM NAY — đo độ lệch sống sót, 0 trial (`MT-34`).** Tự kiểm 12/09/2026: `grep -rn "delisted_at" --include=*.py` cho **đúng 5 kết quả** — `src/tool_d/pool.py:32` (khai báo), `:115` (dùng), **3 chỗ còn lại đều trong `tests/unit/test_pool.py`** ⇒ **không mã sản xuất nào gán nó**. Bộ sinh `SymbolStat` DUY NHẤT (`pool.py:41 build_symbol_stats`) lọc `status == "TRADING"` + `quote_volume_24h` từ ticker **24h hiện tại**. `config/pool.yaml` (102 mã, `committed_at_git_sha: 4ae2c7d…`) chốt bằng `compute_pool()` (`entrypoints/build_pool.py:226`), và docstring `:3-7` của chính file đó tự khai *"H1-D ĐẦY ĐỦ (pairlist point-in-time chống survivorship bias…) là việc RIÊNG của D1 — **CHƯA viết ở đây**"*. Mà `DR-D1-01` §3 (`:51`) đã chốt **"Không mở DR chấp nhận survivorship bias"**. Khối 10 (`:146-156`) có **đúng ba dòng** TD-0095/0096/0097, **cả ba ✅** — và cả ba chỉ là **hàm thuần + test dựng tay**. 🔑 **Hình dạng `MT-08` lần thứ hai:** quyết định đã chốt mà phần thi hành chưa bao giờ tồn tại, trong khi dòng việc mang nhãn ✅ (`N12` mục 3). 🔴 **Chiều lệch là chiều PASS** — loại lệch không cổng nào tự bắt; và spec `:4338` nói *"mỗi lần đổi pool = backfill lại toàn bộ, **MỌI số cũ không so sánh được**"* ⇒ để sau khi tiêu `B2` thì đắt hơn nhiều | ✅ | DR-D1-01 ✅ · TD-0095 ✅ | **0 trial, KHÔNG đọc một nến pool nào** — liệt kê kho tĩnh S3 `data.binance.vision` (đúng đường `DR-D1-01` §2 đã đi) ⇒ metadata của **SÀN**, nên **không vướng `MT-19`** (MT-19 chặn phép đo mô tả trên *dữ liệu pool*). **Ba con số, mỗi con số một câu:** (1) bao nhiêu trong 102 mã pool **chưa tồn tại** tại `T0` (09/04/2024) và `T1` (12/06/2025) — file ĐẦU của `<SYMBOL>/1d/`; (2) bao nhiêu mã perpetual/USDT **đang sống tại `T1`** mà `pool.yaml` **không có**; (3) trong nhóm (2) bao nhiêu **huỷ niêm yết trước `T2`** (29/01/2026) — file CUỐI. 🔴 **Cách đọc viết TRƯỚC khi chạy** (khuôn `DR-D4-08` §6): (1)+(2) ≲ 5% ⇒ lệch không đáng kể, D4 chạy được **kèm hạn chế đã ghi**, và đó là **dữ kiện** không phải hy vọng; (2) hoặc (3) **lớn** ⇒ mọi số D4 trên pool này mang lệch **chiều PASS**, tiêu `B2` trước khi sửa pool là mua một kết luận mà spec `:4338` sẽ vô hiệu hoá. 🔴 **Ranh giới TỰ KHAI, bắt buộc vào artifact:** (2)/(3) là **CẬN TRÊN** — chưa áp tiêu chí volume ≥ 15tr USDT tại `t` (volume lịch sử cần nến thật, mà với mã pool thì **chặn cứng ở `MT-19`**) ⇒ trả lời *"rổ có thể thiếu TỐI ĐA bao nhiêu mã"*, **KHÔNG** trả lời *"pool đúng tại `T1` gồm những mã nào"*. Lẫn hai câu là đọc quá tay. 🔴 **Dù kết quả thế nào cũng KHÔNG tự đổi `config/pool.yaml`** — `build_pool.py` **từ chối ghi đè** có chủ đích (spec `:350-352`: không được chọn lại pool sau khi đã thấy kết quả); sửa pool là quyết định riêng của chủ dự án, có giá riêng. 🔴 **Không hard-code danh sách 219 mã** — `DR-D1-01` §4 cấm thẳng: *"mỗi lần chạy thật phải đo lại, không dùng bản ghi cũ"*. Hàm liệt kê S3 đặt ở `src/tool_d/api_client/binance_public.py` (**R1 single egress**; module đã có rate-limit + circuit breaker của TD-0197 để dùng lại, và đã có tiền lệ cùng host — `AGG_TRADES_HOST:315`); kịch bản ở `docs/du-lieu-do/do_td0230_lech_song_sot_pool.py` — **không** vào `entrypoints/` (`L-Z36` khoá cứng đúng 8 file). Dùng nhánh **monthly** (`futures/um/monthly/klines/<SYMBOL>/1d/`) thay vì `daily/`: ~12 khoá/năm thay vì ~365 nên file đầu/cuối nằm gọn một trang. **Kiểm-có-răng:** prefix > 1000 khoá ⇒ **phải phân trang** qua `NextMarker`, không dừng ở trang đầu (đúng bẫy `DR-D1-01` §1 nêu); lỗi mạng/HTTP ⇒ **raise**, KHÔNG trả danh sách rỗng (N6 — rỗng ở đây đọc thành *"không mã nào bị thiếu"*, tức im lặng theo **chiều PASS**); prefix không tồn tại ⇒ phân biệt được *"rỗng thật"* với *"lỗi"*. Bằng chứng trong Docker (N7), service `freqtrade` (entrypoint `python`) — **không** service `tests` (entrypoint `pytest` nuốt script thành tham số, đúng bẫy TD-0219 đã dính). Trước/sau: `trial_registry.jsonl` vẫn **13 dòng**, `config/pool.yaml` **không đổi một byte** ✅ **ĐÃ ĐO 12/09/2026 — `docs/du-lieu-do/td0230-lech-song-sot-pool.json`, `1d4ff06`, 0 trial.** Docker (service `freqtrade`), 864 mã, 394s. **(1) kênh *"mã chưa sinh vẫn nằm trong rổ"*: **44/102 mã pool (43,1%) KHÔNG tồn tại tại `T0`**, **24/102 (23,5%) không tồn tại tại `T1`** (+2 mã cùng tháng `T0`, không phân giải được). **(3) kênh *"mã đã chết bị loại khỏi rổ"*: 31 mã đã ngừng sinh nến — **15 trong CALIB `[T0,T1]`, 0 trong WFO `[T1,T2]`** ⇒ kênh này **không chạm** phán quyết `Z0-T1` (đo trên WFO); kênh (1) thì **không** được đỡ vậy. ⚠️ **(2) = 385 KHÔNG phải số về lệch sống sót** — bị chi phối bởi mã đang niêm yết nhưng trượt ngưỡng volume 15tr; đã khai trước trong `ranh_gioi`, **không dẫn ra ngoài**. ⚠️ **Con số 15 chưa phân biệt ĐỔI TÊN với SẬP**: gồm `MATICUSDT`→POL, `RNDRUSDT`→RENDER, `EOSUSDT`→A/Vaulta — đổi ticker thì tài sản vẫn sống, nên đó KHÔNG phải lệch sống sót. 🔴 **Tự bắt một lỗi của chính mình ở lượt chạy đầu:** con số (3) sai **ĐƠN VỊ** — tính kênh "mã đã chết" **chỉ trong nhóm (2)**, mà nhóm (2) đòi mã *còn sống tại `T1`* nên nó bỏ qua đúng những mã đã chết rồi; ra 0, và 0 không khớp 219 của `DR-D1-01` nên đi kiểm. Đã sửa phạm vi và chạy lại. 📌 **Đối chứng `DR-D1-01` §1 — con số 219 ĐẾM QUÁ TAY:** `kho − perp = 210` nhưng **179 trong số đó VẪN sinh nến hôm nay** (`TRADIFI_PERPETUAL`, `status=TRADING` — `AAPLUSDT`/`AMZNUSDT`/`ANTHROPICUSDT`; toàn sàn **191** mã loại này); số ngừng thật là **31**. Cơ chế: **phép trừ giữa hai tập lọc theo hai tiêu chí khác nhau** (`contractType` phía sàn vs **TÊN** phía kho) — hiệu không mang nghĩa nào, nhưng luôn ra một số trông hợp lý. ✅ Kết luận của `DR-D1-01` **không lung lay**: xác nhận độc lập chính ca §2 đã kiểm tay, `BTTUSDT` = **2021-04 → 2022-01**, khớp *"06/04/2021 đến 26/01/2022"*. Đề xuất đính chính tại chỗ ghi ở `MT-34`, **không tự sửa** (quy tắc 5). **Kiểm-có-răng: 4 phép phá thật trong Docker**, khôi phục `diff -q` giống byte-đối-byte mỗi lần (bỏ phân trang → 3 đỏ, **dự 2** — trần trang chỉ có nghĩa khi phân trang tồn tại; cắt im lặng ở trần → 1 đỏ; lỗi mạng trả rỗng → 1 đỏ; `iter("Prefix")` hút thẻ gốc → 4 đỏ). Test 32 → **43, cộng đúng**. **Full suite Docker 1684 passed, 0 failed** (1673 → 1684, cộng đúng). Kho trả **1018 thư mục qua 2 TRANG** ⇒ phân trang không phải lo xa. `config/pool.yaml` **không đổi một byte**; `trial_registry.jsonl` vẫn **13 dòng**. ✅ Ghi `MT-34`+`MT-35` ở `7337735`. |
| TD-0231 | 🔴 **Dựng lại POOL ĐÚNG TẠI `T1` (và `T0`) — kênh lệch DUY NHẤT còn chưa đo của `MT-34`.** `TD-0230` đã đo hai kênh và tách được ba: (1) *mã đã chết bị loại khỏi rổ* = **0 trong WFO** (15 trong CALIB); (2) *mã chưa sinh vẫn trong rổ* = **cỡ mẫu, KHÔNG phải thiên lệch** (độ phủ WFO **624/714 = 87,4%**; CALIB 64,5%); (3) 🔴 **tiêu chí volume ≥ 15tr đo tại 09/2026 — CHƯA ĐO**. Kênh (3) là kênh thật: một mã đủ volume giữa 2025 mà nay dưới ngưỡng là mã **đã suy giảm**, loại nó làm `expectancy` **đẹp lên** — lệch sống sót đi qua cửa *tiêu chí lọc* thay vì cửa *huỷ niêm yết*, và **kênh (1) bằng 0 KHÔNG đỡ được kênh (3)**. ⚠️ **Đính chính bản phân tích đầu (phiên `[3f7d14]`, cùng ngày):** lệch CALIB **KHÔNG chạm phán quyết D4** — mọi bằng chứng D4 (`td0205`/`td0212`/`td0213`/`td0214`/`td0215`) mang `timerange: "20250612-20260129"`, `DR-D4-09:23` ghi *"**không phải toàn bộ [T0,T2]**"*, và dữ liệu 5m chỉ tồn tại cho `[T1,T2]` (`TD-0200`) ⇒ con số 44/102-tại-`T0` là hạn chế của *tương lai nếu ai chạy CALIB*, không của `Z0-T1`. Bản đầu đặt nó thành tiêu đề — **sai phạm vi**, đúng lớp lỗi đã ghi hai lần trong ngày | ✅ | TD-0230 ✅ · DR-D1-01 ✅ | **0 trial** — ứng viên **KHÔNG thuộc pool** nên không vướng `MT-19`; `DR-014 §2` chỉ tính *đánh giá cấu hình*. 🔑 **Dùng lại hàm ĐÃ CÓ, không viết lại:** gọi `pairlist_point_in_time()` (`src/tool_d/pool.py:91`) và `pairlist_over_time()` (`:120`) — **đây chính là người gọi mà docstring `pool.py:112` đòi và chưa từng tồn tại** (*"`quote_volume_24h` PHẢI là volume TẠI thời điểm `t`, người gọi tự cung cấp từ dữ liệu lịch sử"*). **KHÔNG sửa `pool.py`.** Ứng viên: 864 mã đã có khoảng tồn tại trong `td0230-lech-song-sot-pool.json` — **không liệt kê lại**. Volume tại `t`: nến **1d monthly** của tháng chứa `T1`, lấy `quote_volume` của **đúng ngày `T1`** — 🔴 **trung thành tiêu chí gốc** (ticker 24h = một ngày); **KHÔNG** đổi sang trung vị 30 ngày, đổi là phát minh một tiêu chí mới rồi so với pool dựng bằng tiêu chí cũ. Tuổi ≥ 180 ngày suy từ `thang_dau` đã có. Hàm tải một zip nến thêm vào `src/tool_d/api_client/binance_public.py` (**R1 single egress**, khuôn `tai_dump_agg_trades:325`, dùng lại `liet_ke_kho_luu_tru()` của TD-0230); kịch bản ở `docs/du-lieu-do/do_td0231_pool_point_in_time.py` — **không** vào `entrypoints/` (`L-Z36` khoá đúng 8 file). **Hai con số ra:** `K` = mã **thuộc** pool đúng tại `T1` mà `pool.yaml` **không có**; `M` = mã trong `pool.yaml` **không thuộc** pool đúng tại `T1`. 🔴 **Cách đọc viết TRƯỚC khi chạy** (khuôn `DR-D4-08 §6`): `K/|pool_T1|` ≲ 10% ⇒ rổ lệch nhỏ, đường B (chạy D4 + khai `d4_han_che`) **có căn cứ**; `K` lớn ⇒ WFO đang đo **sai rổ** theo chiều PASS, phải xử trước khi tiêu `B2`. 🔴 **Dù `K` bằng bao nhiêu cũng KHÔNG tự đổi `config/pool.yaml`** — `build_pool.py` từ chối ghi đè có chủ đích (spec `:350-352`), và spec `:4338` nói đổi pool ⇒ backfill lại toàn bộ + **mọi số cũ mất hiệu lực**. **Kiểm-có-răng:** cấp `quote_volume_24h` của **hôm nay** thay vì tại `t` ⇒ `K` **phải** sụt về ~0 (nếu không thì đường đọc volume chưa thật sự point-in-time); thiếu file nến tháng `T1` của một mã ⇒ mã đó vào ô `khong_do_duoc`, **KHÔNG** bị coi là *"trượt tiêu chí"* (N6 — gộp hai trạng thái là đúng lỗi `pool.py:78` cảnh báo); `pairlist_over_time()` trên `[T0,T1,T2]` ⇒ ràng buộc *"mã EXPLORE không bao giờ vào pool sau này"* (§9c.4b) phải thi hành được trên **đường chạy THẬT**, không chỉ mẫu dựng tay. Bằng chứng trong Docker (N7), service `freqtrade`. Trước/sau: `trial_registry.jsonl` vẫn **13 dòng**, `config/pool.yaml` **không đổi một byte** ✅ **ĐÃ ĐO 13/09/2026 — `docs/du-lieu-do/td0231-pool-point-in-time.json`, `4f2beed`, 0 trial.** Docker (service `freqtrade`), 3 mốc, 864 ứng viên, ~14 phút. 🔴 **Tại `T1` — mốc DUY NHẤT chạy vào phán quyết D4: `pool.yaml` = 102 mã, pool ĐÚNG = 116 mã, CHUNG chỉ 55.** `K = 61` (**52,6%** pool đúng vắng khỏi `pool.yaml`), `M = 47`. Tại `T0`: `K = 122` (74,4%); tại `T2`: `K = 38` (40,4%). 🔴 **Cách đọc đã viết TRƯỚC khi chạy** (trong chính kịch bản, commit `ab290eb` **trước** lượt chạy — kiểm bằng thứ tự commit): *"`K/|pool_T1|` ≲ 10% ⇒ rổ lệch nhỏ, đường B có căn cứ; `K` lớn ⇒ WFO đang đo **SAI RỔ** theo chiều PASS, phải xử trước khi tiêu suất `B2`"*. **52,6% là gấp hơn NĂM LẦN ngưỡng** ⇒ theo đúng cách đọc đã viết trước, đây là kết cục **"K lớn"**. **Phần KHÔNG nhiễu, tách ra để không bị bác bằng *"nhiễu ngưỡng volume"*:** trong `M = 47` có **24 mã KHÔNG TỒN TẠI tại `T1`** (`TD-0230`) ⇒ **không thể** thuộc pool đúng, bất kể volume; 23 mã còn lại tồn tại nhưng trượt tiêu chí — phần này **có** thể nhiễu. ⚠️ **BA HẠN CHẾ TỰ KHAI:** (1) nhiễu ngưỡng volume **một ngày** chưa định lượng được, và **đó là lỗi của artifact này** — tôi **không lưu volume từng mã**, nên không ai kiểm được bao nhiêu mã nằm sát ngưỡng 15tr; kết luận không phụ thuộc nó (52,6% vs 10%) nhưng **tính tái lập thì có**; (2) mốc onboard: **428/475** mã ở `T1` dùng mốc **THÁNG** (xấp xỉ), 47 mã dùng mốc **NGÀY** chính xác — artifact ghi riêng hai con số, không gộp; (3) 🔴 **phép chạy `pairlist_over_time()` trong artifact này KHÔNG chứng minh gì cả** — tôi nạp cho mỗi mốc ĐÚNG tập pool đúng của mốc đó với volume giả `2×` sàn, nên dĩ nhiên `explore = 0` ở cả ba mốc; ràng buộc §9c.4b **KHÔNG được thi hành**. Đây là lỗi thiết kế bước verify của chính tôi, khai ra thay vì để nó trông như một phép kiểm đã chạy. **Full suite Docker 1724 passed, 0 failed** (1684 → 1724, cộng đúng 40). `config/pool.yaml` **không đổi một byte**; `trial_registry.jsonl` vẫn **13 dòng**. 🔴 **KHÔNG đề xuất sửa pool** — spec `:350-352` + `:4338`. |
| TD-0232 | 🔴 **Schema bản ghi kết quả theo ARM — biến chốt `MT-36` thành MÁY, không để nó thành `MT-08` lần thứ ba.** `MT-36` (chủ dự án chốt 13/09/2026): Nhánh 1 phán quyết trên `n` **test-only**, con số toàn-cửa-sổ báo kèm nhưng dán nhãn **MÔ TẢ**. Bằng chứng vì sao phải có máy: `n = 206` của `DR-D4-10 §2.1` **không phải một số lệnh quan sát được** — nó là 160 lệnh trên **88 mã EXPLORE** quy đổi 88→102 rồi annualize trên **toàn** `[T1,T2]` (tái lập đúng: `325,6 × 231/365,25 = 205,9`); ba cửa sổ **test** của `folds.py:174-185` chỉ phủ **147/231 ngày** (đoạn **84 ngày đầu là train-only ở CẢ BA fold**); `dsr.py:52` nhận `n_trades: int` **không có ngữ nghĩa cửa sổ nào** (ràng buộc duy nhất `>= 2`); và `chay_wfo`/`sinh_folds` **không có người gọi nào** từ `run_ablation.py` (E3 dừng ở `NotImplementedError` `:76-78`). ⇒ **`TD-0184` sẽ quyết câu này bằng cách vô tình** nếu không có chỗ nào bắt nó khai. Kèm hai mâu thuẫn đã ghi: `DR-D3-01:29-31` phân vai *"train XẾP HẠNG arm, test kiểm thứ hạng giữ được"* (khác hẳn *"toàn cửa sổ phán quyết"*), và `DR-D3-01 §5.2` nói sàn 30 áp *"trong cửa sổ test"* trong khi `orchestrator.py:203-216` dùng `len(equity.pnl_abs)` **không lọc cửa sổ** | ✅ | MT-36 (chốt 13/09/2026) · TD-0199 ✅ (`gates/ket_cuc.py`) | **0 trial** — chỉ dựng schema + validator + test, không chạy arm nào. `registry/schemas/arm_result.schema.json` với `additionalProperties: false`, **bắt buộc**: `arm` (∈ `ARM_HOP_LE`) · **`n_phan_quyet`** (test-only — số Nhánh 1 dùng) · **`n_mo_ta`** (toàn `[T1,T2]`, MÔ TẢ) · `pham_vi_phan_quyet` ∈ {`phan_quyet`,`mo_ta`} (`DR-D4-10 §8` chặng a) · ba số của `DR-D4-09 §2.3` (`n`, `thue_nhieu`, `dsr_adj` — *"thiếu một ⇒ bản ghi không hợp lệ"*) · khối xuất xứ **8 khoá** (`KHOA_XUAT_XU_TOOL_D`, TD-0229). 🔑 **Vì sao SCHEMA chứ không phải một phép kiểm:** `TD-0127` đã dạy — *một phép kiểm có thể bị bỏ qua, một trường không tồn tại thì không ai truyền vào được*. Thiếu `n_phan_quyet` ⇒ bản ghi **KHÔNG BIỂU DIỄN ĐƯỢC**, không phải *bị báo đỏ sau*. Khuôn đã có: `registry/schemas/fold_record.schema.json` + `wfo/fold_record.py:168 validate_fold_record()` — dùng lại, không phát minh hình dạng mới. **Kiểm-có-răng:** bản ghi thiếu `n_phan_quyet` ⇒ validate **phải** đỏ; bản ghi khai `pham_vi_phan_quyet: "phan_quyet"` cho một arm **nhóm C** ⇒ **phải** đỏ (thi hành dự báo `DR-D4-10 §2.2`: *"nếu nhóm C ra bất cứ kết cục nào KHÁC INCONCLUSIVE ⇒ nghi ngờ BỘ ĐO trước, không mừng"*); `n_phan_quyet > n_mo_ta` ⇒ **phải** đỏ (test không thể nhiều lệnh hơn toàn cửa sổ). ⚠️ **KHÔNG** sửa `dsr.py`/`orchestrator.py`/`folds.py` trong việc này — đổi ngữ nghĩa `n_trades` hay sơ đồ fold là việc khác, và sơ đồ fold khít **0 ngày dư** (`folds.py:162-171`: `(12+3×7)×7 = 231` vs 231) nên đụng vào là fail-closed ngay. Bằng chứng trong Docker (N7) ✅ **XONG 13/09/2026 — `80efced`, 0 trial.** `registry/schemas/arm_result.schema.json` + `src/tool_d/gates/arm_record.py` + `tests/lock/test_td0232_ban_ghi_arm.py` (**28 ca**, Docker 28 passed). **Cả `n_phan_quyet` (test-only) và `n_mo_ta` (toàn cửa sổ) là `required`** ⇒ bản ghi chỉ mang một con số `n` là **KHÔNG DỰNG NỔI**. `build_arm_record()` **KHÔNG nhận** `pham_vi_phan_quyet` làm tham số — nó suy từ `arm` qua `pham_vi_theo_arm()`, nên khai sai cờ là **không biểu diễn được** (khuôn `TD-0127`: một tham số không tồn tại thì không ai truyền vào được). Ba bất biến JSON Schema không so được nằm ở validator: `n_phan_quyet <= n_mo_ta`; cờ phạm vi khớp bảng `DR-D4-10` §2.1 (`ARM_MUA_PHAN_QUYET` = tập CHO PHÉP đúng một phần tử `{Z0-T1}`); arm `mo_ta` **không được** mang `ket_cuc = PASS`. Khoá xuất xứ thứ 8 **bắt buộc và KHÔNG nhận `null`** — khác `fold_record.schema.json` có chủ đích. **Kiểm-có-răng: 3 phép phá thật trong Docker**, khôi phục byte-đối-byte mỗi lần — bỏ `n_phan_quyet` khỏi `required` → 1 đỏ; thêm `Z0-T0` vào `ARM_MUA_PHAN_QUYET` → 2 đỏ; bỏ bất biến tập con → 1 đỏ. Mỗi phép ra **đúng** ca đã dự. ⚠️ **KHÔNG** sửa `dsr.py`/`orchestrator.py`/`folds.py` — đổi ngữ nghĩa `n_trades` hay sơ đồ fold là việc khác, và sơ đồ fold khít **0 ngày dư** (`folds.py:162-171`) nên đụng vào là fail-closed ngay. |
| TD-0233 | 🔴 **27 dòng `TASKS.md` VỠ CỘT — nội dung bị markdown bỏ khi render, và kỷ luật con người đã chứng minh không đủ trong 24 giờ.** Bảng markdown **bỏ mọi ô vượt quá số cột của header**, nên một dòng 6 ô trong bảng 5 cột thì ô thứ 6 **vô hình ở bản render** (GitHub) dù nội dung vẫn nguyên trong file. Soát **theo TỪNG BẢNG** (`TASKS.md` có **20 bảng**; bảng changelog ở cuối **6 cột là ĐÚNG** ⇒ **không có một con số chung nào dùng được**), đếm dấu `|` **cấu trúc** = bỏ phần trong backtick rồi bỏ `\|` đã escape: **27 dòng** lệch so với header của chính bảng đó — `TD-0117`, `TD-0127`, `TD-0130`, `TD-0150`, `TD-0161` (**7 cột**), `TD-0168`, `TD-0170`, `TD-0181`, `TD-0187`, `TD-0192` (**4 cột**), `TD-0196`, `TD-0198`…`TD-0207`, `TD-0210`…`TD-0215`, `TD-0208`. 🔑 **Bằng chứng vì sao cần MÁY chứ không phải kỷ luật:** dòng `TD-0229` vỡ **12/09**, do phiên `[3f7d14]`, **trong cùng ngày phiên đó chạy ĐÚNG phép đếm này cho `back-end-note.md`** (34/34 dòng đúng) rồi commit — *phép kiểm đúng, áp sai chỗ*. Đã vá riêng ở `fc89a85` (2029 ký tự bằng chứng đang bị bỏ). ⚠️ Và nó là **bản THẬT của thứ `MT-18` từng báo động** — `MT-18` là báo động **GIẢ** (4/12 dấu `|` đã escape đúng) | ✅ | — | **0 trial**, không chạm dữ liệu. **Thứ tự BẮT BUỘC: phép kiểm TRƯỚC, rồi vá.** Chạy trước khi vá ⇒ phải **ĐỎ** và liệt kê **đúng 27 dòng** đã biết (*"đỏ nói thật"*); vá xong ⇒ xanh. Ngược lại thì không ai biết phép kiểm có răng. Test mới `tests/lock/test_td0233_cot_bang_md.py` đọc `TASKS.md` · `back-end-note.md` · `ARCHITECTURE.md`, quét theo từng bảng (mỗi dòng `|---|` mở một bảng, header là dòng ngay trước), và đòi **mọi dòng khớp header của CHÍNH bảng đó**. 🔴 **Kiểm-có-răng bắt buộc, hai vế:** thêm một dấu `|` vào một dòng của bảng **5 cột** ⇒ phải đỏ; thêm vào một dòng của bảng **6 cột** (changelog) ⇒ **CŨNG** phải đỏ — vế thứ hai chứng minh nó so theo **từng bảng**, không theo hằng số 5 (nếu chỉ có vế đầu thì một phép kiểm ghim hằng 5 cũng xanh, và nó sẽ báo đỏ giả cho cả bảng changelog). Vá: đổi dấu `|` thừa thành dấu chấm hoặc escape `\|` — **giữ nguyên từng byte nội dung**, kiểm bằng phép so TIỀN TỐ như `fc89a85` đã làm. ⚠️ **N12:** sửa 27 dòng một file trạng thái dùng chung ⇒ nhắn phiên song song trước, và đọc hết `git diff` (phiên `[f5177d]` đã tự kiểm Khối 17: **0 dòng lệch**, nên không dòng nào của họ). Bằng chứng trong Docker (N7). ✅ **Xác nhận 13/09/2026 — hai commit CÓ THỨ TỰ, cố ý để lịch sử git tự chứng minh phép kiểm có răng:** `9603998` (phép kiểm — tại commit đó repo **ĐỎ**, 2 ca) rồi `0823541` (bản vá — xanh, 28 dòng, 28 insertions / 28 deletions). Phép kiểm bắt **27 dòng `TASKS.md` đúng như đã đo** **và thêm 1 dòng `back-end-note.md`** (`OQ-13`, dòng 82, header 4 cột / dòng 5 cột) mà lần soát trước KHÔNG nhìn tới — vì nó chỉ đếm các dòng `MT-`, tức phạm vi phép soát hẹp hơn phạm vi lời khẳng định của nó. BỐN nguyên nhân gốc, tìm bằng cách đọc chữ quanh từng dấu cấu trúc chứ không đoán: (A) dùng dấu gạch dọc làm **dấu phân cách** trong ô Xác nhận — phổ biến nhất, đúng lỗi của `TD-0229`; (B) hai dấu liền nhau sinh **ô rỗng** (dòng 227); (C) ký hiệu toán không bọc backtick (dòng 291, `TD-0161`, 2 dấu thừa); (D) **thiếu** dấu đóng ô cuối (dòng 350, `TD-0192`, 4 cột). **37 dấu thừa được escape, 0 byte nội dung bị xoá** — kiểm tự động bằng phép so TIỀN TỐ trên cả 28 dòng, **28/28 khớp**. Kiểm-có-răng HAI vế, mỗi vế khôi phục byte-đối-byte: thêm một ô vào bảng **5 cột** ⇒ 1 đỏ; thêm một ô vào bảng **changelog 6 cột** ⇒ **cũng** 1 đỏ. Vế thứ hai là vế quyết định — một bản cài ghim hằng số 5 sẽ xanh ở vế đầu nhưng báo đỏ GIẢ cho cả bảng changelog. Docker: **11 passed**; full suite **1755 passed, 0 failed** |
| TD-0234 | 🔴 **Hai sửa vào bản ghi arm: thi hành `MT-37` (khai phạm vi `Δ_R`) và SỬA `MT-36` (đoạn nào phán quyết Nhánh 1).** **(a) `MT-37`** — chủ dự án chốt 13/09/2026: **KHÔNG đặt sàn số mã** cho `Δ_R`, mà **bắt KHAI phạm vi**. Căn cứ bác chính câu hỏi *"sàn bao nhiêu"*: `buoc4_hieu_chinh_hai_chieu.py:6-8` khai thẳng *"§4 cố ý KHÔNG đặt ngưỡng phần trăm — **ngưỡng tuỳ tiện là chỗ uốn kết luận sau khi thấy số**. Nên module này **không có** hằng số nào kiểu `LECH_TOI_DA`"*, và một sàn đặt **sau khi** đã biết `Δ_R` đo trên 2 mã chính là con số **chọn khi đã thấy số**; cộng với `MT-36` vừa chốt khuôn ngược lại (bắt khai **cả hai** con số rồi để khoảng cách **nhìn thấy được**) và khuôn đó **rẻ hơn**. **(b) SỬA `MT-36`** — chủ dự án duyệt 13/09/2026 sau khi đọc `DR-D3-01` §2 nguyên văn: §2 tiêu đề *"🔴 **WFO của Tool D KHÔNG khớp lại tham số**"*; `:30-31` *"train dùng để **XẾP HẠNG các arm ablation**… **Không có tham số liên tục nào được khớp**"*; `:33-35` *"**đơn vị phân tích của fold là *thứ hạng giữa các arm*, không phải *giá trị tuyệt đối của một arm***"*; §5.3 *"phán quyết thống kê **hoãn sang D9**"*. ⇒ **Fold đo THỨ HẠNG; Nhánh 1 hỏi GIÁ TRỊ TUYỆT ĐỐI của một arm** — hai đại lượng khác nhau, và dưới `DR-D4-10` chỉ còn **một** ứng viên nên **không có thứ hạng nào để đo**. ✅ Và `run_ablation.py` **không gọi** `chay_wfo`/`sinh_folds` là **THEO THIẾT KẾ**, không phải thiếu sót — phiên `[3f7d14]` đọc sự vắng mặt đó thành một lỗ hổng và mở `MT-36` trên cơ sở ấy; **nó là một RANH GIỚI**. Lỗi phạm vi thứ ba trong hai ngày, lần này theo chiều *thấy lớp canh ở chỗ vốn có ranh giới* | ✅ | TD-0232 ✅ · MT-36 (sửa 13/09) · MT-37 | **0 trial**, không chạy arm nào. Sửa `registry/schemas/arm_result.schema.json` + `src/tool_d/gates/arm_record.py` + `tests/lock/test_td0232_ban_ghi_arm.py` — **cả ba do đợt 13/09 tạo ⇒ KHÔNG test khoá nào của người khác phải sửa**, và ba điều kiện tiên quyết `§7.1` **không phải viện tới**. **(a)** thêm khối **BẮT BUỘC** `delta_r_pham_vi {so_ma, so_fill, dataset, chien_luoc}` + `so_ma_da_chay`; nguồn đọc từ artifact niêm phong `dr015-luot-khop-tranche.json` → `_nguon.cap` (**2 cặp**) và `_xac_minh.so_dong` (**91**). Không ngưỡng, không phán xét — **hai con số cạnh nhau**. **(b)** `n_phan_quyet` đổi nghĩa thành *"số lệnh trên cửa sổ mà phán quyết được tính"* = **toàn WFO** cho D4; `n_mo_ta` giữ làm số đối chứng; thêm `lenh_theo_thang` làm **MÔ TẢ**. 🔴 **Giữ** bất biến `n_phan_quyet <= n_mo_ta`; **giữ** cả 28 ca hiện có, **không xoá ca nào** — chỉ sửa docstring/khẳng định về *nghĩa* của trường (xoá một test đỏ cho sạch bảng là cách một quyết định biến mất mà không ai ghi — tiền lệ `TD-0150`). ⚠️ **Cái mất của (b), phải khai vào `d4_han_che`:** Nhánh 1 nay **không còn phép kiểm ổn định theo thời gian**; một PASS có thể đến từ một tháng tốt. Bù bằng `lenh_theo_thang` như **thống kê mô tả**, 🔴 **KHÔNG** biến nó thành ngưỡng. **Kiểm-có-răng:** thiếu `delta_r_pham_vi` ⇒ `validate_arm_record()` đỏ **và** `build_arm_record()` raise; đối chiếu **ĐẦU RA THẬT** với schema, không fixture (tiền lệ `trial_event.schema.json`: **735 test xanh vẫn không bắt được**). ⚠️ **KHÔNG** sửa `dsr.py`/`orchestrator.py`/`folds.py`/`buoc4_hieu_chinh_hai_chieu.py`, **KHÔNG** đụng `L-Z58`, **KHÔNG** hạ sàn 30 lệnh/fold — việc này làm sàn đó **không áp cho Nhánh 1**, chứ không sửa nó. ✅ **Xác nhận 13/09/2026 (`237784e`):** Docker **48 passed** trong `test_td0232_ban_ghi_arm.py` (28 → 48, **+20 ca, 0 ca bị xoá** — hai ca chỉ đổi tên theo trường); full suite **1755 passed, 0 failed** = 1724 + 11 của `TD-0233` + 20, phép cộng khớp từng ca. 🔑 **Vế `MT-36` ra khác bản kế hoạch, và lý do đáng ghi:** kế hoạch viết *"đổi NGHĨA `n_phan_quyet` thành toàn cửa sổ, GIỮ bất biến `n_phan_quyet <= n_mo_ta`"* — ba câu đó **không thể cùng đúng** trên số thật (toàn cửa sổ 206, chỉ-test 131 ⇒ 206 > 131 làm bất biến vỡ ở MỌI bản ghi). Chủ dự án chốt phương án **đổi TÊN, không đổi nghĩa**: `n_toan_cua_so` / `n_chi_test`, bất biến `n_chi_test <= n_toan_cua_so`, cộng `nguon_phan_quyet` khai tường minh cửa sổ nào đem so ngưỡng. Giữ tên cũ mà đảo nghĩa sẽ làm MỘT tên trường mang HAI nghĩa ở hai thời điểm — đúng lỗi *hai nguồn sự thật* của `N1`/`MT-03`. `nguon_phan_quyet` **không** là tham số của `build_arm_record()` (nó là hằng `NGUON_PHAN_QUYET_D4`), nên *lặng lẽ phán quyết trên đoạn test* là **không biểu diễn được** — thủ pháp `TD-0127`. Cái MẤT đã khai: mất phép kiểm ổn định theo THỜI GIAN ⇒ bù bằng `lenh_theo_thang` làm **MÔ TẢ bắt buộc có mặt**, không thành ngưỡng. **Kiểm-có-răng BA vế, cả ba khôi phục byte-đối-byte:** (1) đổi `NGUON_PHAN_QUYET_D4` sang `chi_test` ⇒ đúng 2 đỏ; (2) bỏ `lenh_theo_thang` + `delta_r_pham_vi` khỏi `required` của schema ⇒ đúng 2 đỏ; (3) **THÊM** một sàn giả `so_ma >= 10` ⇒ **40 đỏ, gồm đúng ca ghim "KHÔNG có sàn số mã tối thiểu"** — vế quyết định, vì nó chứng minh một ngưỡng thêm vào sau này không thể lọt im lặng |
| TD-0246 | 🔴 **TẦNG TRẠNG THÁI ĐÃ TRÔI KHỎI ĐĨA Ở SÁU CHỖ, TẤT CẢ CÙNG MỘT CHIỀU — và hai chỗ nặng nhất là THAM SỐ, tức thứ sinh ra mọi con số D4 sắp mua.** (1) `param_status.yaml:192-200` đóng băng `dg7_funding_frac` với lý do *"DG7 CHỈ áp cho SHORT nên chưa có đường chạy nào chạm tới"* — nhưng `funding_stop.py:7-8` tự khai *"ÁP DỤNG KHÔNG ĐIỀU KIỆN"*, `:12-14` *"đúng cho CẢ Long lẫn Short"*, `ZoneAbsorption.py:1156-1162` gọi vô điều kiện, và đo trên `td0212` (LONG, EXPLORE 88 mã, WFO) nó **kết thúc 19,4% lệnh của `Z0-T1`** (31/160), 27,3% của `Z0`, 14,9% của `Z0-T0`. (2) spec §10.2 đặt dải TIME_STOP **5–25%** hàm ý `max_hold_bars_4h` có ràng buộc — đo được **0%** ở cả `Z0` (0/22) và `Z0-T1` (0/160), và `TIME_STOP_RATIO_BAND` (`thresholds.py:37`) xuất hiện **đúng một lần toàn repo: tại dòng định nghĩa**, không có trong `evaluate_branch1()` cùng file dù bảy hằng số còn lại đều có. (3) ô `MT-39` còn ghi *"Chưa code"* mà `config/freqtrade/config.json:49` đã `stoploss_on_exchange: true`. (4) ô `MT-40` còn ghi *"chờ chủ dự án"* mà TD-0238 ✅ và `equity_peak.py` đã nối. (5) ô `MT-42` còn ghi *"Decision Log có 0 NGƯỜI GỌI SẢN XUẤT"* mà nay là **2** (`ZoneAbsorption.py:872`, `:1149`). (6) ô TD-0184 còn ghi chặn *"63,6 lệnh/năm < sàn 150"* mà `DR-D4-10` §2.1 + `MT-26`(C) + `MT-29` đã xử. 🔑 **Nền rộng hơn sáu ca:** `param_status.yaml` cho **12/12** tham số `tier_b` đều `FROZEN` + `chua_calibrate`; ta có bằng chứng đo về đúng **2**, và **cả 2 lời khai đều SAI, lệch hai phía NGƯỢC nhau** — mười cái còn lại chưa ai đo. `L-Z15` vẫn ✅ vì nó kiểm **sự có mặt của lời khai**, không kiểm **nội dung** (cùng hình *lớp canh chĩa nhầm hướng*, TD-0168) | ✅ | TD-0212 ✅ · TD-0228 ✅ · DR-D4-12 ✅ (`63769f8`) | **0 trial, 0 dòng mã sản xuất mới** — EXPLORE, chỉ ĐẾM. Dựng bảng **12 dòng**: mỗi tham số `tier_b` ràng buộc bao nhiêu **% LỆNH** của arm ứng viên `Z0-T1`. ⚠️ Đọc theo **LỆNH**, KHÔNG theo *lần xét cổng* — bài học TD-0182: tỉ lệ theo lần-xét **thổi phồng cổng DAI** (hết cửa sổ rồi thì mọi lần hỏi sau đều chặn) và **làm nhẹ cổng THOÁNG QUA**. Ưu tiên dẫn từ artifact **ĐÃ CÓ** (`td0212`, `td0228`, `dg2-explore-quet-arm`) trước khi chạy thêm; tham số nào không suy được từ artifact có sẵn thì ghi **`pending`**, CẤM điền `0.0` hay suy từ tên tham số (N6). Ghi file MỚI trong `docs/du-lieu-do/`, **không đụng artifact nào**. 🔴 **KHÔNG tự sửa `param_status.yaml` / ô `MT` / `TASKS.md` của việc khác** — quy tắc 5 + 11: ghi nhận, chờ lệnh **"chuẩn hóa và lưu"** (N9) để vào mục 7 `back-end-note.md`. 🔴 **KHÔNG calibrate tham số nào** — calibrate tiêu trial; việc này tồn tại để biết THỨ TỰ trước khi tiêu. Vì sao nó đứng TRƯỚC TD-0184 (chủ dự án duyệt 14/09/2026): mọi con số D4 sắp mua đều là con số **của bộ máy này**, nên nếu `0,3` của `dg7_funding_frac` đang kết thúc 1/5 số lệnh mà không ai biết, `mean_R` đo được là `mean_R` của **một hệ chưa được mô tả đúng**. ✅ **Xác nhận `b3827ff` + `2acd1d0` (14/09/2026), 0 trial, KHÔNG chạy backtest nào** — chỉ đọc artifact đã có (`td0212`, `td0228`) + đường đọc mã kiểm tay kèm `file:line`. Sổ trial giữ nguyên 13 dòng. 🔴 **PHÁT HIỆN LỚN NHẤT, và nó KHÔNG phải về tham số: DG1–DG5 không bao giờ được xét trên 6/9 arm** — `arm_switches.py:73-75` `ARM_DON_TRANCHE` = {Z0, Z1, Z0-T0, Z0-T1, Z0-V1, Z0-S1}, `cong_ap_dung()` trả `()` cho cả sáu (`:255-256`), và `ZoneAbsorption.py:1006-1007` `return None` đứng TRƯỚC lời gọi `danh_gia_tat_ca` ở `:1031`. Trong sáu arm đó có **CẢ arm ứng viên sản xuất `Z0-T1` và CẢ mốc so `Z0`** ⇒ năm cổng, tức phần *"Smart"* của Smart DCA, là **BẤT ĐỘNG trên cấu hình sẽ lên tiền thật**; trong 4 arm D4 sắp chạy chỉ `Z3` còn xét. 🔑 Đây **KHÔNG phải lỗi** — mã tự khai đúng ý định (`arm_switches.py:71-72`) — nó là **HỆ QUẢ của chốt `DR-D4-10` §2.4 mà chưa ai viết ra**: chọn single-entry đồng thời **xoá năm cổng khỏi phạm vi đo**. 📊 **Ba hạng, và câu hỏi ban đầu SAI ĐƠN VỊ với một nửa:** `CONG` **2** (tính % lệnh thật) · `BAT_KHA_TREN_ARM_NAY` **4** (`0.0` kèm `ly_do_bang_0`) · `DAU_VAO_LIEN_TUC` **6** (`pending` — dịch KẾT QUẢ chứ không CHẶN lệnh, nên đo được cần phân tích độ nhạy và việc đó **TỐN TRIAL**). Hai hạng cuối đều cho 0 sự kiện chặn nhưng nghĩa trái ngược; gộp lại là đúng thứ N6 cấm. Đo được trên `Z0-T1` (160 lệnh): `dg7_funding_frac` **19,4%** (31/160) · `max_hold_bars_4h` **0,0%**. 🔴 **ĐÍNH CHÍNH của chính việc này — ô bên trên viết *"cả 2 lời khai đều SAI, lệch hai phía NGƯỢC nhau"*: câu đó SAI, giữ nguyên làm lịch sử.** Tỉ lệ đúng là **1/4 lời khai đã kiểm**: chỉ `dg7_funding_frac` sai; `max_hold_bars_4h` khai **TRUNG THỰC** (`param_status.yaml:161-164` — *"chính spec gọi nó là ỨNG VIÊN CHỐT kèm dấu [CẦN CALIBRATE], tức tự khai đây là số tạm"*), và con số 0% đo được **chính là thứ điều kiện mở lại của nó đòi**; thứ lệch là **HÀM Ý của spec §10.2**, không phải lời khai. `dg6d_retrace_frac` ✅ ĐÚNG (short-only `dg6_early_invalidation.py:98-99` + 0 đường đọc sản xuất) và `funding_rate_pct` ✅ trung thực (khai một mâu thuẫn nội tại của spec). ⇒ **Cùng lớp lỗi việc này đi tìm, mắc ngay trong lúc đi tìm nó** — sai ở NHÃN dán lên phép đo, không sai ở phép đo; và nó lọt vào commit `16db105` trước khi bị bắt, bắt được vì đi đọc `frozen_rationale` để viết bảng chứ không vì một lớp canh nào. 🔑 **Cơ chế trôi** (phiên `-4e` nêu, đã kiểm lại): lý lẽ của `dg7` kế thừa **BẰNG THAM CHIẾU** — *"cùng lý do với `dg6d_retrace_frac`"* — mà `dg6d` thì khai ĐÚNG. Người viết không bịa: họ trỏ tới một lý lẽ THẬT, chỉ trỏ nhầm chỗ ⇒ sinh một phép quét rẻ hơn: tìm mọi `frozen_rationale` biện minh bằng THAM CHIẾU thay vì bằng một sự kiện kiểm được. ⚠️ **Một khẳng định của tôi hoá ra KHÔNG mới:** *"`L-Z15` kiểm sự có mặt, không kiểm nội dung"* đã được `ledger/audit_checks.py:256-262` **tự khai** trước; phần mới là một THỰC THỂ của khoảng cách đó (`dg7`) và CƠ CHẾ sinh ra nó. **Không sửa** `param_status.yaml` / ô `MT` nào (quy tắc 5 + 11) — chờ "chuẩn hóa và lưu" |
| TD-0247 | 👤 **NGƯỜI GIỮ TỪ 17/09/2026: phiên `-01`.** Chủ dự án giao lại vì khoá 🔒 từ 16/09 không còn phiên sống nào giữ (commit cuối `4fe5068`/`3e3c96e`; phiên `-33` và `-30` đối chiếu). Phần code còn lại vẫn cần lệnh "bắt đầu code" trong phiên giữ. 🔴 **DỰNG LẠI RỔ POOL POINT-IN-TIME CÓ XUẤT XỨ, TRƯỚC D4 — đường A-ĐẦY-ĐỦ của `MT-34`.** Chủ dự án chốt **13/09/2026**, *“sau khi thấy đề xuất A-tối-thiểu … và chọn khác — ghi nhận để không ai đọc ngược”*; **xác nhận lại 16/09/2026** (`MT-45`) sau khi một phiên đọc thiếu `MT-34` và ghi ngược quyết định vào `DR-D4-12` §3 (đã đính chính, `8a13593`). **Vấn đề nó giải:** `config/pool.yaml` (102 mã) chốt bằng tiêu chí đo tại **09/2026** rồi dùng cho `[T1,T2]`; `TD-0231` đo rổ đúng tại `T1` = **116** mã, chung chỉ **55**, **`K = 61` (52,6%)** vắng khỏi `pool.yaml`, `M = 47` — gấp hơn năm lần ngưỡng ≲10% viết trước ⇒ **WFO đang đo SAI RỔ theo chiều PASS**. Và `delisted_at` **chưa bao giờ** được gán ngoài `tests/unit/test_pool.py` ⇒ `pairlist_point_in_time()` không có người gọi nào có thể đúng. 🔴 **Mã việc này tồn tại muộn:** `MT-34` tự khai *“VẪN CHƯA CÓ MÃ VIỆC NÀO … đây là thứ đáng lo nhất còn lại … mỗi ngày không mở là một ngày mất thẳng”* | ✅ | `MT-34` ✅ (chốt A-đầy-đủ) · `MT-45` ✅ · `DR-D1-01` §3 · `TD-0231` ✅ · **DR MỚI — phải commit TRƯỚC mọi dòng mã** | **Việc NHIỀU TUẦN** — spec `:4338` xếp H1-D là *“đường găng, tốn nhiều tuần nhất ở Tool A”*. **Ba điều kiện đã biết trước (`MT-34`), ghi lại để lúc làm khỏi dựng lại:** **(a)** `build_pool.py:210-216` **từ chối ghi đè** và tự khai đường hợp lệ duy nhất là *“xoá thủ công + ghi DR mới”* (exit 94) ⇒ **DR phải commit TRƯỚC**, kiểm bằng `git merge-base --is-ancestor`; **(b)** spec `:4338` — *“mỗi lần đổi pool = backfill lại toàn bộ dữ liệu phụ trợ, và MỌI số cũ không so sánh được”*: phạm vi chính xác là các số **quy đổi sang pool** (`325,6` · `n = 206` · `44,8` · phép so sàn 150), còn phép đếm **EXPLORE thô vẫn đúng**; **(c)** rổ backtest hôm nay **KHÔNG CÓ XUẤT XỨ** — `runs/pool_pairs.json` (102 mã `download-data` đã dùng) **không được git theo dõi** và **không có bộ sinh nào** trong repo ⇒ rổ mới **phải sinh bằng mã có xuất xứ**, nếu không đổi pool xong vẫn không ai biết backtest chạy trên rổ nào. 📊 **Giá đo được 16/09/2026 (đọc `td0231` + đếm file dữ liệu, 0 trial):** trong `K = 61` mã, **52 mã không có dữ liệu ở bất kỳ đâu** (`user_data/data/binance/` 0/61, `explore/` 9/61) ⇒ phải tải ~52 mã × (1h/4h/1d/mark/funding) trên `[T1,T2]`. 🔴 **Ba bẫy đã biết, mỗi cái phải có kiểm-có-răng:** **(i)** `download-data --timerange` **KHÔNG tôn trọng mốc kết thúc** (bug đo ở TD-0093 — 10/10 file lấn quá `T2`, hai file chạy sâu 7 tháng vào LOCKBOX) ⇒ bắt buộc cắt lại + kiểm nằm trọn `[T1,T2]`; **(ii)** dữ liệu mới **không được** đổ vào `binance/` (làm lệch nghĩa pool 102 cũ) **cũng không** vào `explore/` (§9c.4b: EXPLORE không sang pool) ⇒ cần **thư mục thứ ba** với ràng buộc ở **tầng thư mục** như cách dự án tách EXPLORE; và 9 mã của `K` đang nằm trong EXPLORE là một câu ranh giới §9c.4b **phải quyết trong DR, không tự quyết**; **(iii)** bộ dựng nào lấy ứng viên từ kho **bằng lọc TÊN** sẽ hút **191 mã `TRADIFI_PERPETUAL`** (cổ phiếu token hoá — `AAPLUSDT`, `ANTHROPICUSDT` …, đo được `status=TRADING`) vào pool crypto; `pool.py:41` lọc `contractType == "PERPETUAL"` **đúng** nhưng hở ở **phía kho**. ⚠️ **Mã sàn phi ASCII** (`币安人生USDT` nằm TRONG pool 102): mọi đường tải/URL phải chịu được (bài học 13/09 — `http.client` raise trước khi gửi). **Tiêu chí XONG:** rổ đúng tại `T1` sinh bằng mã có xuất xứ + commit; dữ liệu `[T1,T2]` đủ cho rổ đó trong thư mục tách riêng, đã kiểm timerange; **đo lại** phễu + `n` + lệnh/năm của `Z0-T1`/`Z0`/`Z0-T0`/`Z3` trên rổ mới (EXPLORE-style, **0 trial**) và ghi file MỚI, **không đụng artifact cũ**; bằng chứng trong Docker (N7). **0 trial** cho toàn bộ việc này — dựng rổ và tải dữ liệu **không** phải đánh giá cấu hình (`DR-014` §2). 🔴 **Không tự đổi `config/pool.yaml`** trước khi DR commit 🔑 **Dùng lại máy đã có, không viết kiểm tra mới (bổ sung 16/09/2026, phiên -30 nêu):** mọi lượt tải đi đúng quy trình **E8** `entrypoints/backfill_data.py` — `--snapshot-before` → `download-data` → `--verify-after` — và **`assert_dataset_timerange()`** (`src/tool_d/ledger/timerange.py:40`, canh bởi `tests/lock/test_lz55_ctrl_explore_timerange_self_check.py`) phải xanh cho **MỌI** mã mới. Tiêu chí thứ tự DR: `git merge-base --is-ancestor <commit DR> <commit mã đầu tiên>` phải trả 0 — kiểm bằng máy, không bằng mắt (khuôn `DR-D3-01`/`TD-0243`) ✅ **DR đã commit 16/09/2026 (`12c5228`, `docs/decisions/DR-D1-02-rong-lai-pool-point-in-time.md`)** — dùng số `DR-D1-02` (nối lineage `DR-D1-01`), không phải `DR-D4-xx` (dãy đó phiên `-22` đang giữ cho `MT-24`/`MT-25`, đã nhắn xác nhận không trùng qua N12 mục 6). Ranh giới §9c.4b cho câu (ii) đã chốt qua `AskUserQuestion` với chủ dự án: **9 mã (ARKMUSDT, BRETTUSDT, CETUSUSDT, MEUSDT, PNUTUSDT, RENDERUSDT, SEIUSDT, THETAUSDT, XTZUSDT) — đủ tiêu chí tại `T1` nhưng đã bị `compute_pool()` xếp vào khối `explore:` của `config/pool.yaml` bằng dữ liệu 09/2026 — LOẠI VĨNH VIỄN, không đưa vào rổ `T1` mới, không ngoại lệ.** Thư mục dữ liệu mới: `user_data/data/pool_t1/` (chi tiết triển khai, xem DR §3). ✅ **Lệnh "bắt đầu code" nhận 16/09/2026 — bước đầu XONG (`4fe5068`, sau `12c5228`, thứ tự xác nhận bằng `git merge-base --is-ancestor`):** `src/tool_d/pool_t1.py` (2 hàm THUẦN: `loai_tru_explore_hien_tai()` thi hành quyết định 9 mã ở trên — đọc `explore:` từ `config/pool.yaml` mỗi lần gọi, KHÔNG hardcode tên; `loai_tru_tradifi_perpetual()` xử bẫy (iii) — loại mã có `contractType != "PERPETUAL"` trong `exchangeInfo` hôm nay, giữ nguyên mã vắng mặt hoàn toàn (đã huỷ niêm yết thật, đúng ý `DR-D1-01`)). Không sửa `pool.py`. 9 test đơn vị + kiểm-có-răng phá thật trong Docker (vô hiệu hoá từng hàm ⇒ đúng 2 ca đỏ mỗi lần, khôi phục). Docker full suite: **1999 passed, 1 failed** (không liên quan — `idea_queue` không còn rỗng). 🔴 **CÒN LẠI, CHƯA LÀM:** script gọi mạng thật (exchangeInfo + kho `data.binance.vision`) để dựng danh sách ứng viên T1 cuối cùng có xuất xứ, rồi backfill ~52 mã qua E8 vào `user_data/data/pool_t1/`, rồi đo lại phễu/n/lệnh-năm trên rổ mới — đây là phần lớn của "việc nhiều tuần", chưa bắt đầu. 🔄 **CẬP NHẬT 17/09/2026 (phiên `-01`, chữ trên giữ nguyên làm lịch sử):** **(1)** Quyết định *"9 mã … khối `explore:`"* ở trên **ĐẾM SAI**: khối có **430** mã, giao rổ `T1` = **54** mã. Áp đúng chữ thì rổ còn 62 mã, tái tạo lệch sống sót. Chủ dự án chốt lại ở **`DR-D1-03`** (`a6b4f8f`): loại vĩnh viễn **mọi mã có dữ liệu EXPLORE đã dùng**; xét trượt tiêu chí **tại `T1`**, không chuỗi từ `T0`. **(2)** ✅ **Bộ sinh rổ `T1` có xuất xứ XONG** (`385cbbb`, sau DR): E7 `build_pool.py --ro-t1 [--ghi]` → `config/pool_t1.yaml`, 0 trial, đối chiếu khít `TD-0231`. Docker full suite **2242 passed, 0 failed**; kiểm có răng 3 chốt ⇒ đúng 3 đỏ. **(3)** 🔄 **Chủ dự án chốt BỎ bước *"đo lại phễu + `n` + lệnh/năm của `Z0-T1`/`Z0`/`Z0-T0`/`Z3`"* khỏi Tiêu chí XONG** (Zone Absorption LONG tạm dừng theo `DR-IQ-01`). Phép đo đó chuyển sang `TD-0184` khi nối lại. **TD-0247 XONG khi:** rổ `T1` sinh + commit; dữ liệu `[T1,T2]` tải bù đủ cho rổ đó trong `user_data/data/pool_t1/`, đã kiểm timerange, qua E8. **Còn lại:** chạy `--ro-t1 --ghi` thật trong Docker → tải bù. ✅ **XONG 17/09/2026:** rổ `config/pool_t1.yaml` 107 mã (`a62540b`) · dữ liệu `user_data/data/pool_t1/futures/` 642 file (55 chép · 45 `download-data` · 7 nhập kho, MKR/FLM cắt tại mốc ngừng giao dịch — `DR-D1-03` §4–§5, artifact `td0247-moc-ngung-giao-dich.json`) · `E8 --ro-t1-kiem` PASS · Docker full suite **2283 passed, 0 failed** tại `96e957f` (HEAD không đổi suốt lượt) · research-log `a3a77ac` |

---


## Khối 17 — FreqAI: mở lại §0c.2 (mở 12/09/2026 theo quyết định chủ dự án)

> ⛔ **ĐÃ ĐÓNG 18/09/2026 — chủ dự án quyết** (`DR-FAI-01` mục ĐÃ ĐÓNG, `e7638b9`). Cả 10 việc ❌.
> Lệnh cấm FreqAI (spec §0c.2, `CLAUDE.md` N3, `L-Z24`) **giữ nguyên hiệu lực**. ML chỉ quay lại
> như phương pháp của một ý tưởng qua Idea Queue, với DR MỚI — không dựng lại khối này.
> Đoạn dẫn bên dưới giữ nguyên chữ làm lịch sử.

> 🔴 **Vì sao khối này tồn tại.** `tool-d-smart-dca.md:462-473` (§0c.2) xếp FreqAI vào
> **MODULE CẤM TUYỆT ĐỐI**, và quy tắc **N3** của `CLAUDE.md` ghi *"cấm vĩnh viễn, không có
> ngoại lệ, không có 'chỉ thử một lần'"*. Chủ dự án **chốt ngược lại ngày 12/09/2026**: Tool D sẽ
> dùng FreqAI. Khối này là đường thi hành quyết định đó. Ghi lại cho người đọc sau: đây **không
> phải** một khoảng hở của spec bị phát hiện — đây là một quyết định đã chốt bị **mở lại có chủ
> đích**, nên mọi bước dưới đây phải để lại dấu vết đủ để ai đó sáu tháng sau dựng lại được
> *vì sao*.
>
> 🔴 **TD-0216 (DR) PHẢI XONG VÀ COMMIT TRƯỚC MỌI DÒNG MÃ CỦA KHỐI** — cùng kỷ luật đã áp cho
> `DR-D4-01`/`DR-D4-08`/`DR-D4-10`: giấy tờ quyết định commit **RIÊNG và TRƯỚC**, vì viết sau khi
> đã nhìn thấy kết quả thì không ai phân biệt được *"quyết định"* với *"giải thích cho cái đã
> xảy ra"*. TD-0217…TD-0225 đều **phụ thuộc cứng** vào nó.
>
> ⚠️ **Ba lệnh cấm KHÔNG nằm trong phạm vi khối này, không được nới lây:** `hyperopt` (`L-Z25`),
> `IntParameter`/`DecimalParameter`/… (`L-Z37`), Edge Positioning. Spec dòng 448-460 gọi hyperopt
> là *"mối nguy lớn nhất, **lớn hơn FreqAI nhiều**"* — quyết định mở FreqAI **không** kéo theo
> hai cái kia. Ai sửa `L-Z25`/`L-Z37` trong khối này là đi ra ngoài phạm vi đã chốt.
>
> ⚠️ **Cỡ mẫu KHÔNG được FreqAI giải.** `n ≈ 28` lệnh trên cửa sổ WFO là số **đo được**
> (TD-0205), và `DR-D4-10` đã chốt bảy trong chín arm là INCONCLUSIVE vì nó. FreqAI học trên
> **nến** chứ không trên **lệnh**, nên nó đổi chỗ bài toán chứ không xoá bài toán — mọi phán
> quyết cuối cùng vẫn đếm bằng lệnh. Đừng đọc khối này như một đường vòng thoát bế tắc D4.
>
> 📌 **Tiền đề sản xuất đã ĐỔI ngày 12/09/2026, đọc trước khi viết `DR-FAI-01` §phạm vi:**
> `DR-D4-10 §2.4` chốt hệ thống mặc định là **`Z0` single-entry** — **không còn tranche 2/3**,
> `adjust_trade_position` **VẪN ở trên đường chạy sản xuất** — nó là đường **TP1 chốt 50%**
> (`ZoneAbsorption.py:886-887`, xét TRƯỚC chốt arm ở `:891`); chỉ **nhánh bơm tranche 2/3** tắt
> (TD-0226/TD-0227). 🔴 **Đính chính 12/09/2026:** bản đầu của dòng này viết *"`adjust_trade_position`
> rời khỏi đường chạy sản xuất"* — **SAI**, phiên `[3f7d14]` đọc mã và bác, tôi đã kiểm lại trên đĩa.
> Ai bỏ qua / bọc lại / thay thế callback đó vì tin nó đã chết thì **TP1 chết theo, và chết IM LẶNG**
> (Freqtrade hạ exception của callback thành WARNING rồi đi tiếp với `rc = 0` — MT-16 (vii):
> *"một chốt fail-closed bị nuốt là một chốt KHÔNG TỒN TẠI"*). Nên FreqAI sẽ gắn lên
> một hệ thống **một-lần-vào-lệnh**, không phải lên cỗ máy DCA mà tên dự án nói tới. ⚠️ DCA
> **chưa bị bác bỏ**, chỉ *"chưa từng được đo"* (điều kiện mở lại `n ≥ 319` đã viết trước) —
> nếu `DR-FAI-01` định cho FreqAI điều khiển việc bơm tranche thì nó đang chạm một câu hỏi
> **đang đóng**, phải mở lại qua DR chứ không ngầm hiểu.

| Mã | Tên việc | TT | Phụ thuộc | Verify — chạy gì, thấy gì thì xong |
|---|---|---|---|---|
| TD-0216 | 🚪 **`DR-FAI-01` — quyết định mở lại §0c.2 cho FreqAI.** Tài liệu quyết định, **0 trial**, commit **RIÊNG và TRƯỚC** mọi dòng mã. Phải trả lời **đích danh ba lý do cấm** mà spec dòng 462-473 nêu, mỗi lý do một câu trả lời **kiểm được bằng máy** hoặc một lời khai *"chấp nhận rủi ro"* ghi rõ: **(a)** không đếm được N ⇒ DSR vô nghĩa; **(b)** backtest FreqAI tốn thời gian tương đương chạy dry ⇒ **không chạy được GATE D0.9** trên nó; **(c)** mô hình tự thích ứng **không bao giờ "sai" một cách quan sát được** — nó chỉ thích ứng ⇒ ngược triết lý bác-bỏ-được của cả dự án. Chốt thêm: **phạm vi** (FreqAI đứng ở đâu — lọc tín hiệu vào lệnh? định cỡ? thay hẳn zone absorption?), quan hệ với **D4 đang dở**, và **điều kiện ĐÓNG LẠI viết TRƯỚC** (khuôn OQ-07, giống `DR-D4-01 §7`) — tức *"thấy gì thì tắt FreqAI đi"*, viết lúc chưa có con số nào để không ai nghi là viết cho vừa kết quả. 🔴 **Thêm 12/09/2026 (phiên `[67bb21]` nêu, tôi nhận):** DR phải có một mục khai thẳng **LÝ DO MỞ**, và khai **đích danh thứ FreqAI KHÔNG giải** — `n = 28` lệnh trên WFO, cần `n ≥ 319` để một arm thôi INCONCLUSIVE (`DR-D4-10`); đặt một tầng học máy lên **28 mẫu** làm bài toán overfitting **nặng hơn**, không nhẹ đi. Nếu lý do mở là *"để cải thiện kết quả"* thì tiền đề đó **sai và phải ghi là sai**; nếu mở vì lý do khác thì ghi rõ lý do đó — kẻo sáu tháng nữa ai đó đọc cả Khối 17 thành *"FreqAI là cách gỡ bế tắc D4"* | ❌ | — | File `docs/decisions/DR-FAI-01-mo-lai-0c2-freqai.md` tồn tại; `git show --stat` của commit đó chỉ có **đúng file này**; `registry/trial_registry.jsonl` **không tăng dòng nào**. ⚠️ **N12 mục 6:** nhắn phiên song song mã `DR-FAI-01` **trước khi mở file** — hai phiên từng viết hai `DR-D4-06` cho cùng một quyết định ❌ **HUỶ 18/09/2026** (`DR-FAI-01` mục ĐÃ ĐÓNG, `e7638b9`): chủ dự án đóng Khối 17; lệnh cấm FreqAI (spec §0c.2, `CLAUDE.md` N3, `L-Z24`) giữ nguyên hiệu lực. |
| TD-0217 | **Thi hành `DR-FAI-01` trên tầng GIẤY TỜ — ba file, không nhân đôi nội dung.** (i) `CLAUDE.md` **N3**: dòng *"FreqAI, Edge Positioning, `trailing_stop` dựng sẵn"* (`CLAUDE.md:88`) phải **tách** — FreqAI ra khỏi bảng cấm, **giữ nguyên** Edge + `trailing_stop`; (ii) `back-end-note.md` mục 7: **`MT-32`** ghi nhận xung đột *spec cấm ↔ chủ dự án cho phép* theo quy tắc 11, phân loại 🔴 (mọi con số đo bằng FreqAI trước khi MT-32 được giải là con số của một hệ thống mà spec đang cấm); (iii) `ARCHITECTURE.md` nếu FreqAI thêm tầng mới. 🔴 **KHÔNG sửa `tool-d-smart-dca.md`** — quy tắc 5 cấm sửa spec; DR là chỗ ghi đính chính, spec giữ nguyên chữ cũ làm lịch sử | ❌ | TD-0216 | `grep -n "FreqAI" CLAUDE.md` → dòng N3 không còn liệt FreqAI là cấm, **và** số lần xuất hiện `Edge Positioning` không giảm. `MT-32` có trong `back-end-note.md`. `git log --oneline -- tool-d-smart-dca.md` **không có commit mới**. ⏳ Ghi `back-end-note.md`/`ARCHITECTURE.md` chỉ được làm sau lệnh **"chuẩn hóa và lưu"** (N9) ❌ **HUỶ 18/09/2026** (`DR-FAI-01` mục ĐÃ ĐÓNG, `e7638b9`): chủ dự án đóng Khối 17; lệnh cấm FreqAI (spec §0c.2, `CLAUDE.md` N3, `L-Z24`) giữ nguyên hiệu lực. |
| TD-0218 | **Sửa test khoá `L-Z24` — đổi NHÃN, không gỡ răng.** `tests/lock/test_lz24_freqtrade_config_flags.py` hiện chấp nhận `freqai` *vắng mặt HOẶC `enabled=false`*, **từ chối `true`**. Phương án đề xuất (chờ DR chốt): `enabled=true` chỉ hợp lệ khi trong chính `config/freqtrade/config.json` có khoá `_comment_*` trích **đúng mã `DR-FAI-01`** — cùng thủ pháp test này đã dùng cho `protections` (*"lý do ghi rõ"* phải là chuỗi **máy đọc được**, không phải lời hứa miệng). 🔴 **Không xoá file test** — xoá một test đỏ cho sạch bảng là cách một quyết định biến mất mà không ai ghi (tiền lệ TD-0150). 🔴 **Không đụng `L-Z25`/`L-Z37`** | ❌ | TD-0216, TD-0220 | Docker: file test chạy **RIÊNG**, PASS ≥ 1 ca (*"exit 0 mà 0 ca chạy"* không phải bằng chứng — tiền lệ cổng D2). **Kiểm có răng bắt buộc:** đặt `freqai.enabled=true` **mà bỏ** khoá `_comment_*` trỏ DR → phải **đỏ đúng 1 ca**; phá thử bốn cờ còn lại (`edge.enabled`, `trailing_stop`, `position_adjustment_enable`, `use_exit_signal`) → vẫn đỏ như cũ. 🔴 **Điều kiện TIÊN QUYẾT, kiểm bằng THỨ TỰ COMMIT chứ không bằng phán xét** (phiên `[3f7d14]` đề nghị, nhận): `git log --oneline -- docs/decisions/DR-FAI-01-*.md tests/lock/test_lz24_freqtrade_config_flags.py` phải cho thấy **commit DR đứng TRƯỚC** commit sửa test. Lý do: chỗ khác nhau giữa *"lần này hợp lệ"* và *"sửa test khoá khi nó cản"* **không nằm ở mức độ chính đáng** mà ở một dữ kiện đọc được trên đĩa — **điều khoản spec mà test thi hành đã bị đổi trước đó**. Test không sai; thứ nó canh đã đổi. ❌ **KHÔNG** phải căn cứ hợp lệ: *"test đang cản tiến độ"*, hay *"chủ dự án đã đồng ý"* nói chung — đồng ý **đổi spec** thì hợp lệ, đồng ý **sửa test** mà spec không đổi thì không, vì khi đó test và spec lệch nhau và **spec thắng** (N1). Phạm vi cho phép ghi theo **MÃ TEST**, không theo phạm trù: *đúng `L-Z24`, đúng khoá `freqai.enabled`* — *"các test khoá liên quan FreqAI"* là một phạm trù, mà phạm trù thì tự nở ra (bài học TD-0130: danh sách CHO PHÉP chặn được cả tên chưa ai nghĩ tới, blocklist thì không) ❌ **HUỶ 18/09/2026** (`DR-FAI-01` mục ĐÃ ĐÓNG, `e7638b9`): chủ dự án đóng Khối 17; lệnh cấm FreqAI (spec §0c.2, `CLAUDE.md` N3, `L-Z24`) giữ nguyên hiệu lực. |
| TD-0219 | 🔴 **Ảnh Docker hiện tại nhiều khả năng KHÔNG có thư viện FreqAI — phải ĐO, CHƯA ĐO ĐƯỢC.** `docker/Dockerfile` ghim **digest** `freqtradeorg/freqtrade@sha256:7031bca4…`, là biến thể **thường**, không phải biến thể `_freqai` (Freqtrade đóng gói scikit-learn/lightgbm/catboost/torch ở ảnh riêng). Đo thử 12/09/2026 **thất bại vì Docker Desktop không chạy** ⇒ trạng thái là **CHƯA ĐO**, không phải *"thiếu"* (N6). Nếu thiếu ⇒ đổi digest, và **hệ quả phải khai TRƯỚC**: (i) `runtime_image_digest` — khoá xuất xứ thứ 8 — **đổi giá trị**, nên bản ghi kết quả trước/sau **không so trực tiếp được** (đúng hình dạng `DR-D4-08 §8`); (ii) quy tắc 9 (parity) đòi local–staging–prod **cùng một ảnh**, không được *"chỉ máy local có torch"* | ❌ | TD-0216 | ✅ **ĐÃ ĐO 12/09/2026 — kết quả: THIẾU** (`docs/du-lieu-do/td0219-anh-docker-thu-vien-ml.json`, **0 trial**). Service `freqtrade` (entrypoint `python`, **không** phải `tests` — service đó entrypoint là `pytest` nên nuốt script thành tham số): `sklearn 1.9.0` ✅ · `joblib 1.5.3` ✅ · **`datasieve`/`lightgbm`/`catboost`/`torch`/`xgboost` THIẾU**. 🔑 Thư mục `freqtrade/freqai/` **CÓ tồn tại** — nhưng `import freqtrade.freqai.freqai_interface` → **`ModuleNotFoundError: datasieve`**. Dừng ở *"thư mục có tồn tại: True"* thì kết luận **ngược hẳn và sai**; thư mục là **hình dạng**, import được mới là **cơ chế**. ⇒ **Điều kiện dừng của `DR-FAI-01 §4(b)`, viết TRƯỚC khi chạy, đã NỔ** — trình chủ dự án, không tự đổi digest. Việc còn lại của TD-0219 (đổi digest + hệ quả `runtime_image_digest`) **chưa làm**. Sau khi đổi digest: `docker compose run --rm tests cat /opt/freqtrade_version.txt` **và** full suite xanh trên ảnh MỚI — ⚠️ nhắn phiên song song trước khi chạy full suite ✅ **PHẦN CÒN LẠI ĐÓNG BẰNG QUYẾT ĐỊNH 12/09/2026 — chủ dự án chốt: GIỮ NGUYÊN digest, KHÔNG đổi sang `_freqai`.** Căn cứ **quản trị**, không phải hạ tầng: spec `:472-473` *"Muốn thử ML → đó là **GIẢ THUYẾT RIÊNG, ngân sách riêng, lockbox riêng. Không phải nâng cấp cho Tool D**"*; cộng `DR-FAI-01` §4b điều 2 (ảnh `_freqai` có thể khác **cả pandas/numpy/ta-lib** ⇒ mọi số `td0193→td0215` mất nền so sánh). Giá của việc giữ: **0**. ⇒ Phép so hai ảnh của `DR-FAI-01` §4b điều 3 **không cần chạy**. 📌 **Đường thứ ba do phiên `[f5177d]` nêu, đã đo, ghi để không mất:** ảnh ta chạy là ảnh **DẪN XUẤT** (`FROM …@sha256:7031bca4…` rồi `pip install --user`), nên thêm `datasieve`+`lightgbm` vào **chính bước pip đó** giữ nguyên base digest — đo bằng `pip install --dry-run` trong container: **đúng hai gói được cài, không gói nào bị nâng cấp** ⇒ tầng chỉ báo không đổi. Đường này giải bài toán **kỹ thuật** nhưng **KHÔNG** giải bài toán **quản trị** (đặt hai gói ML vào ảnh dùng chung local–staging–prod là ngược chiều *"ngân sách riêng, lockbox riêng"*) — nên **không lật quyết định trên**. ⚠️ Nếu sau này đi đường đó thì **BẮT BUỘC commit `docker/Dockerfile` TRƯỚC khi dựng lại ảnh**: `runtime_image_digest` (TD-0229) ghi digest ảnh **GỐC** nên **không** thấy lớp dẫn xuất; thứ bắt được là **`git_sha`**, và nó chỉ bắt khi Dockerfile đã commit. Sửa tay rồi build mà chưa commit ⇒ **không gì bắt được**. ❌ **HUỶ 18/09/2026** (`DR-FAI-01` mục ĐÃ ĐÓNG, `e7638b9`): chủ dự án đóng Khối 17; lệnh cấm FreqAI (spec §0c.2, `CLAUDE.md` N3, `L-Z24`) giữ nguyên hiệu lực. |
| TD-0220 | 🔴 **Khối `freqai` trong `config/freqtrade/config.json` va thẳng vào N4 — đây là xung đột, không phải việc gõ thêm JSON.** N4: *"mọi tham số đọc từ `config/tool_d_config.yaml`, **không nơi nào khác**, không dùng cơ chế tham số của Freqtrade"*. FreqAI đọc `feature_parameters`, `data_split_parameters`, `model_training_parameters`, `train_period_days`, `backtest_period_days`… **từ config.json của Freqtrade** — đúng cơ chế N4 cấm. DR phải chốt: mirror từ YAML sang config.json lúc dựng (kèm test đối chiếu hai bên), hay khai một ngoại lệ có phạm vi. ⚠️ `config/freqtrade/config.json:114` giải thích vì sao khoá `freqai` **cố ý vắng mặt**: freqtrade 2026.8 đòi **đủ bộ khoá con** một khi khoá này xuất hiện — thêm `{"enabled": false}` làm `show-config` lỗi. 🔴 **Mỗi khoá thêm vào là một BẬC TỰ DO mới** ⇒ phải vào `config/dof_inventory.yaml` + `config/param_status.yaml` (nay 12/12 `tier_b` đều `FROZEN`) | ❌ | TD-0216, TD-0222 | `docker compose run --rm freqtrade freqtrade show-config --config config/freqtrade/config.json` → **exit 0** (đúng chỗ bản *"khai báo tắt"* từng lỗi). `dof_report()` chạy được, tổng DOF khớp kiểm kê mới. Kiểm có răng: bỏ một khoá bắt buộc → `show-config` đỏ ❌ **HUỶ 18/09/2026** (`DR-FAI-01` mục ĐÃ ĐÓNG, `e7638b9`): chủ dự án đóng Khối 17; lệnh cấm FreqAI (spec §0c.2, `CLAUDE.md` N3, `L-Z24`) giữ nguyên hiệu lực. |
| TD-0221 | 🔴 **Kế toán trial khi FreqAI TỰ HUẤN LUYỆN LẠI — `DR-014 §2` không có câu trả lời sẵn.** DR-014 §2: *mọi lần **đánh giá cấu hình** trên CALIB/WFO/LOCKBOX = **1 trial***. Một lần backtest FreqAI retrain hàng chục lần trên cùng cửa sổ ⇒ ba cách đọc đều đọc xuôi: 1 trial cho cả lần chạy · 1 trial cho **mỗi lần retrain** · FreqAI đứng ngoài sổ. Ba cách cho ba con số N khác nhau **hàng chục lần**. Phải chốt trong `DR-FAI-01` **và có máy thi hành** ở `src/tool_d/ledger/registry.py` — không để nguyên chữ (tiền lệ **MT-08**: chính sách chốt 07/09 mà `registry.py` 0 dòng thi hành, phải mở TD-0130 sau đó) | ❌ | TD-0216 | Test khoá mới: một lần chạy FreqAI giả lập **N lần retrain** → `n_used()` tăng **đúng con số DR chốt**, không phải "một con số nào đó". Kiểm có răng: đổi số lần retrain → con số trial đổi theo đúng luật đã khai (hoặc **không đổi**, nếu DR chốt vậy — miễn máy nói đúng thứ DR nói) ❌ **HUỶ 18/09/2026** (`DR-FAI-01` mục ĐÃ ĐÓNG, `e7638b9`): chủ dự án đóng Khối 17; lệnh cấm FreqAI (spec §0c.2, `CLAUDE.md` N3, `L-Z24`) giữ nguyên hiệu lực. |
| TD-0222 | 🔴 **`N` và rào DSR: FreqAI KHÔNG KHAI ĐƯỢC số phép thử — đây là lý do (a) của spec, và là lý do KỸ THUẬT, không phải lời cảnh báo tinh thần.** `src/tool_d/gates/dsr.py:24` ghim `N_DANG_KY = 114` (DR-D0PRE-02) ⇒ rào `√(2·ln N) = 3,0777`. Một mô hình học được có số bậc tự do **thực tế** là số tham số của mô hình (hàng nghìn với LightGBM/torch), không phải số khoá trong config. Ba đường ra, DR chọn **đúng một** và ghi giá phải trả: (i) khai N mới theo một luật đếm viết trước; (ii) tuyên bố **DSR không áp cho nhánh FreqAI** — và khi đó phải nói **GATE D0.9 §10.2 lấy gì phán quyết thay**; (iii) nhánh FreqAI có **lockbox riêng, ngân sách riêng** — đúng chữ spec dòng 471-473 (*"Muốn thử ML → đó là **giả thuyết riêng, ngân sách riêng, lockbox riêng**. Không phải nâng cấp cho Tool D"*). 🔴 **Không để trống rồi chạy trước tính sau** — N6: ngưỡng chưa điền = `+inf`, để gate **không thể vô tình PASS** | ❌ | TD-0216 | `DR-FAI-01` có mục trả lời (i)/(ii)/(iii), **chọn đúng một**. Nếu đổi `N_DANG_KY`: `dsr_hurdle()` trả đúng rào mới **và** test đang ghim `3,0777` được sửa thành khẳng định mới **nêu đích danh mã DR** (tiền lệ TD-0171: ghim QUYẾT ĐỊNH tách khỏi ghim QUAN HỆ) ❌ **HUỶ 18/09/2026** (`DR-FAI-01` mục ĐÃ ĐÓNG, `e7638b9`): chủ dự án đóng Khối 17; lệnh cấm FreqAI (spec §0c.2, `CLAUDE.md` N3, `L-Z24`) giữ nguyên hiệu lực. |
| TD-0223 | 🔴 **Rò rỉ dữ liệu — ba đường, và hai trong đó là THIẾT KẾ của FreqAI chứ không phải lỗi.** (i) **Nhãn nhìn tương lai**: `&-target` tính bằng `shift(-n)` theo đúng thiết kế — hợp lệ lúc train, **rò** nếu lọt vào đường tín hiệu; (ii) **Cửa sổ train trượt**: `train_period_days` lùi về TRƯỚC mốc bắt đầu cửa sổ đang chạy ⇒ backtest trên WFO `[T1,T2]` có thể **đọc dữ liệu CALIB**, tệ hơn là **chạm LOCKBOX** — mốc T0…T3 đã **niêm phong** (`DR-D0PRE-07`) và lockbox chỉ được chạm **đúng một lần trong đời dự án**; (iii) `startup_candle_count` tăng mạnh ⇒ `assert_dataset_timerange()`/`L-Z55` phải kiểm lại xem có còn bắt được không. 🔑 Dự án **đã tự bắt** hai ca rò thật (`dp.get_pair_dataframe()` trả cả nến tương lai; `lookahead-analysis` ở TD-0106) — nên câu hỏi **không phải** *"có cơ chế chưa"*, mà *"cơ chế cũ có còn nhìn thấy đường rò MỚI không"* | ❌ | TD-0216, TD-0219, TD-0220 | Chạy thật trong Docker: `freqtrade lookahead-analysis` **và** `recursive-analysis` trên chiến lược FreqAI → ghi kết quả vào `docs/du-lieu-do/`. Kiểm cứng ranh giới phân vùng: chạy WFO `[T1,T2]` rồi **đọc log/dataframe thật** xác nhận mốc sớm nhất FreqAI chạm tới nằm trong phạm vi DR cho phép — **không tin lời khai của bộ chạy** (tiền lệ TD-0148, nhãn `nguoi-khai`) ❌ **HUỶ 18/09/2026** (`DR-FAI-01` mục ĐÃ ĐÓNG, `e7638b9`): chủ dự án đóng Khối 17; lệnh cấm FreqAI (spec §0c.2, `CLAUDE.md` N3, `L-Z24`) giữ nguyên hiệu lực. |
| TD-0224 | 🔴 **Quan hệ với D4 đang dở — không được tự cộng suất trial.** `DR-D4-01` chốt **9 suất `B2`, Long-only**; `DR-D4-10` chốt D4 phán quyết **hai câu**, bảy arm nhóm C khai INCONCLUSIVE ngay tại DR. FreqAI là **arm thứ 10**, một **nhánh song song**, hay thứ chạy **sau khi D4 đóng**? Ba cách đọc, ba đường tiêu tiền khác nhau. 🔴 Và `L-Z57` (TD-0185) đòi GATE D0.9 phải có đường dẫn tới **DỪNG DỰ ÁN**; nếu FreqAI thành một arm thì kịch bản *"mọi arm rule-based thua, FreqAI thắng"* phải ra kết cục gì — **viết TRƯỚC** | ❌ | TD-0216, TD-0222 | `DR-FAI-01` có mục trả lời. Nếu FreqAI tiêu trial: `reserve()` ghi sổ **TRƯỚC** khi chạy (`L-Z52`) và `trial_registry.jsonl` tăng **đúng** số suất đã khai — không hơn ❌ **HUỶ 18/09/2026** (`DR-FAI-01` mục ĐÃ ĐÓNG, `e7638b9`): chủ dự án đóng Khối 17; lệnh cấm FreqAI (spec §0c.2, `CLAUDE.md` N3, `L-Z24`) giữ nguyên hiệu lực. |
| TD-0225 | **Bộ chạy + hiện vật mô hình: không mở entrypoint thứ 9, và model phải có xuất xứ.** `L-Z36` khoá cứng `entrypoints/` ở **đúng 8 file** (§0d.2 dòng 664: *"Không có E9 script thử nghiệm nhanh"*) — huấn luyện FreqAI phải đi qua E1/E2/E3 sẵn có, hoặc một **cờ** trên entrypoint đã có (tiền lệ TD-0124/TD-0125: hai công cụ nhập liệu làm thành cờ trên E6, `entrypoints/` vẫn đúng 8 file). Kèm: `user_data/models/**` vào `.gitignore` (mỗi lần train sinh hiện vật lớn), và mô hình đã train phải mang khối **xuất xứ** (`git_sha` + `runtime_image_digest` + hash cấu hình) — thiếu thì một con số sinh ra từ mô hình nào **không truy lại được**, đúng thứ PHẦN 0d tồn tại để chặn | ❌ | TD-0216, TD-0219 | `ls entrypoints/*.py` → **đúng 8 file**, `L-Z36` xanh. `git status` sau một lần train → **không file model nào hiện ra**. Bản ghi kết quả có mô hình mang đủ khối xuất xứ (`L-Z40` xanh) ❌ **HUỶ 18/09/2026** (`DR-FAI-01` mục ĐÃ ĐÓNG, `e7638b9`): chủ dự án đóng Khối 17; lệnh cấm FreqAI (spec §0c.2, `CLAUDE.md` N3, `L-Z24`) giữ nguyên hiệu lực. |

---

## Khối 18 — Đường tới D11 (Dry-run) (mở 13/09/2026 theo yêu cầu rà soát của chủ dự án)

> 🔴 **Vì sao khối này tồn tại.** Chủ dự án yêu cầu rà soát hệ thống và lập danh sách việc pending để
> đi tới **D11 (Dry-run)**. Rà soát ra hai câu mà không mục nào trong 17 khối trước nói tới.
>
> **Câu thứ nhất — chạy dry-run hôm nay thì nó sinh ra 0 chỉ số đọc được.** `report_model.py:26` để
> cả **22/22** chỉ số ở `pending` bằng một **hằng số chuỗi**, không có bộ tính; và
> `src/tool_d/ledger/decision_log.py` (cửa ghi §8.3) có **0 người gọi sản xuất** — grep toàn repo chỉ
> ra test. ⇒ H-1…H-4 mà §11b.2 đòi *"ghi hàng tuần khi live"*, tỉ lệ khớp post-only, `gap_ms`,
> `hold_duration` **đều không có nguồn nào**. Hai tuần dry-run sẽ trả về đúng bảng 22 dòng `pending`
> như hôm nay.
>
> **Câu thứ hai — cổng D4 không bao giờ đóng được** (`MT-38`). Chưa nổ vì `close_d4_gate()` chưa tồn
> tại, nên đây là lúc rẻ nhất để sửa, đúng lập luận `MT-08`.
>
> 📌 **Khoảng cách, đọc cho đúng:** spec đặt D11 ở **Tuần 18–20**; hệ thống đang ở **Tuần 6–8**. Giữa
> hai mốc có **năm pha (D5–D9) không có một dòng đặc tả nào** trong repo (`tool-d-smart-dca.md:4492`
> chỉ ghi `D5–D9  (giữ nguyên)`, kế thừa từ bản tài liệu đã bị xoá). Chủ dự án chốt 13/09/2026:
> **dựng testnet trước, HOÃN đặc tả D5–D9** — vì cổng vào D11 (`spec:2954`) đòi D2b/D2c verify bằng
> testnet hoặc một lệnh live tối thiểu, **không có đường thứ ba**, và môi trường testnet mà `spec:4480`
> giả định *"dựng ở D3.5, dùng lại nguyên vẹn cho D10"* **chưa từng được dựng** (`DR-D35-01` đã loại
> testnet — `runtime_state.json:92-94`).
>
> 🔴 **Luật mạnh hơn mọi cổng, và nó là chữ của `back-end-note.md:128`:** *"không mang mâu thuẫn 🟡
> vượt qua go-live"* ⇒ `MT-19` · `MT-26` · `MT-27` · `MT-28` · `MT-30` · `MT-33` · `MT-37` đều phải
> đóng trước Giai đoạn 5. Và `back-end-note.md:80` khai rõ D10/D11/D12 là **pha phát triển**, *"xong"
> là phán xét của người* ⇒ **không có cổng máy nào chặn việc vào dry-run sớm**; kỷ luật nằm trọn ở
> luật 🟡 trên.
>
> ⚠️ **Chủ dự án chốt: ghi `MT-38`…`MT-42` và mở mã việc, CHƯA sửa mã.** Năm mục `MT` là đầu ra của
> `TD-0235` và chỉ được ghi sau lệnh **"chuẩn hóa và lưu"** (N9). Đã nhắn cả hai phiên `-70` dãy mã
> `MT-38`…`MT-42` · `TD-0235`…`TD-0244` · `DR-D11-01` **trước** khi mở (N12 mục 6); cả hai xác nhận
> không dùng trùng.

| Mã việc | Nội dung | TT | Phụ thuộc | Tiêu chí XONG |
|---|---|---|---|---|
| TD-0235 | **Ghi `MT-38`…`MT-42` vào `back-end-note.md` mục 7 theo quy tắc 11.** `MT-38` 🔴 cổng D4 không bao giờ đóng được — khoá `d4_complete` đòi *"đếm được **đúng 18** trial `B2` CONSUMED"* (`back-end-note.md:80`, OQ-11 PA3, chốt 07/09) nhưng `DR-D4-01:15` chốt **9 trial Long-only** (08/09) và `:113` nói *"nửa còn lại **không tự động** thuộc về Short"*. `MT-39` 🔴 (nổ ở D12) SL không sống trên sàn — `config.json:49` đặt `stoploss_on_exchange: false` và giao SL cho `custom_stoploss` nội bộ, trái `spec:1955-1957` *"SL SỐNG TRÊN SÀN … cơ chế đó **chết cùng tiến trình**"*; nặng hơn, cả chuỗi D2a/D2b/D2c và `gap_ms` §8.3 **chỉ có nghĩa khi SL nằm trên sàn**, mà D2b/D2c là điều kiện CỨNG để vào D11. `MT-40` 🔴 trước D11 — `_dinh_equity` chỉ nằm trong RAM (`ZoneAbsorption.py:282`) ⇒ thang drawdown/HALT về **0%** sau mỗi lần khởi động lại, tức mất **một trong ba tầng** §12b.2 dựng ra để chặn vòng lặp thua lỗ. `MT-41` 🔴 trước D11 — `self._cho` cũng chỉ trong RAM, mà `unfilledtimeout.entry = 180 phút` làm *"restart lúc có lệnh chờ khớp"* thành ca **THƯỜNG**; `order_filled` raise nhưng `strategy_safe_wrapper` **nuốt** exception của callback (`MT-16` vii, dự án đã đo) ⇒ lệnh mở **không có kế hoạch**, `custom_stoploss` cũng raise ⇒ Freqtrade rơi về lưới cuối **`stoploss = −0,99`**. `MT-42` 🔴 trước D11 — E5 trả **22/22 `pending`** bằng hằng số, Decision Log **0 người gọi sản xuất** | ✅ | — | Năm mục `MT-38`…`MT-42` có trong `back-end-note.md` mục 7, mỗi mục phân loại 🔴/🟡 và **không tự chọn bên** (quy tắc 11). `MT-38` phải dẫn **cả** `DR-D4-10` (nó tái định 7/9 suất thành **mô tả**, nên người sau đọc `MT-38` rồi đi sửa 18 thành 9 là chưa đủ) **và** ghi **cách đọc BỊ BÁC** *"mâu thuẫn là 0 khác 18 vì Long rớt sàn 150"* — cách đọc đó đã xuất hiện **độc lập hai lần** (`DR-D4-10:178-179` phải viết đoạn phản bác tại chỗ vì lần đầu; phiên `-70` là lần hai dù đoạn phản bác đã nằm sẵn trên đĩa), cùng lý do `DR-FAI-01` §4b ghi hai phép kiểm **thất bại**. Chẩn đoán phải khai là **hai quyết định va nhau**, không phải một cái thay cái kia: grep `d4_complete` và `OQ-11` và `runtime_state` trong `DR-D4-01` cho **0 kết quả**, và thứ tự commit `6342113` (07/09) → `df011da` (08/09) khớp. 🔴 **Chỉ ghi sau lệnh "chuẩn hóa và lưu"** (N9), **0 trial**, KHÔNG sửa một dòng mã sản xuất nào. ✅ **Xác nhận 13/09/2026 (`a4c7f02`, lệnh "chuẩn hóa và lưu"):** mục 7 đi từ **36 → 41** mâu thuẫn. **7 insertions / 0 deletions** — thuần append, kiểm bằng máy TRƯỚC khi ghi: so từng dòng cũ phải còn nguyên và đúng thứ tự · 5 mã mới chưa từng tồn tại · mỗi dòng mới đúng **7 cột** của header mục 7. `MT-32` giữ nguyên là **chỗ trống** của phiên `-70` (mã phiên `869c86a0`, ghi chú 18/09/2026 theo N12 mục 7) (đầu ra `TD-0217`) — chỉ được NHẮC trong dòng lịch sử để giải thích vì sao dãy có lỗ, **không lấp**. Phép kiểm cột `TD-0233`: **11 passed**. Ba câu N9 đã trả lời trong chat TRƯỚC khi ghi; `ARCHITECTURE.md` và `tu-dien-du-lieu.md` **không đổi** (chưa có tầng/sổ/bảng nào mới). ⚠️ **Lệch quy trình, khai ra:** quy tắc 14 đòi khoá 🔓→🔒 và commit TRƯỚC khi làm, tôi **bỏ bước đó** — dòng này vừa được mở và lệnh N9 tới trong cùng một lượt. Rủi ro mà quy tắc 14 canh (hai phiên cùng làm một việc) **không xảy ra** vì đã nhắn cả hai phiên `-70` dãy mã trước khi mở và cả hai xác nhận không chạm, nhưng đó là may, không phải do quy trình |
| TD-0236 | 🔴 **`MT-38` — sửa tiêu chí khoá `d4_complete` cho khớp `DR-D4-01`.** Đây là *hai quyết định đã chốt va nhau*, nên quy tắc 11 cấm tự chọn bên: phải trình chủ dự án. Hai đường đã nhìn thấy — (a) khoá đếm **9** kèm một trường `d4_huong: LONG` để *"đã đóng cho Long"* khác *"đã đóng cho cả hai hướng"*; (b) tách hẳn `d4_long_complete` và `d4_short_complete`. ⚠️ Câu thật có thể còn lớn hơn con số: `DR-D4-10` đã tái định **7/9 suất thành thống kê MÔ TẢ**, nên *"cổng D4 đóng bằng cái gì"* đáng được đặt lại, không chỉ sửa 18 thành 9 | ✅ | TD-0235 · TD-0186 | 🔴 **PHẢI xong TRƯỚC khi `close_d4_gate()` được viết một dòng nào** — sau đó thì mọi bản vá đều là gỡ một chốt đang chặn, đúng thứ cổng D3 đã trả giá (*"một chốt không bao giờ thoả được thì tệ hơn không có chốt"*, vì lúc gỡ thì gỡ luôn cả phần đúng của nó). Test khoá nêu **đích danh** quyết định được chọn; kiểm-có-răng: trả tiêu chí về 18 ⇒ đúng 1 ca đỏ, và ca đó phải là ca ghim quyết định ✅ **Xong 14/09/2026.** `DR-D4-11` (`79db3a8`) commit **RIÊNG và TRƯỚC** mọi dòng mã, đính chính `d363abb`; kiểm thứ tự bằng `git merge-base --is-ancestor` chứ không bằng mắt. Mã: `src/tool_d/gates/d4_gate.py` + `tests/lock/test_td0236_tieu_chi_dong_cong_d4.py` (26 ca, `5910f74`). Docker full suite **1906 passed** (baseline 1880 đo lại bằng `--collect-only` NGAY trước khi so, + 26 ca mới, đúng cộng); sổ `trial_registry.jsonl` giữ **13 dòng**, **0 trial**. 🔑 **Chủ dự án chốt đường THỨ BA, không phải (a) cũng không phải (b):** đóng bằng **HIỆN VẬT**. Lý do mạnh hơn lý do ghi ở cột bên trái — câu thật không phải *"18 hay 9"* mà là **một cổng đếm trial sẽ PASS kể cả khi toàn bộ số suất đó đều là loại MÔ TẢ và `Z0-T1` chưa từng chạy** (`DR-D4-10:211-213` đã hạ 7/9 suất xuống mô tả) ⇒ PASS RỖNG nằm trong chính ĐỊNH NGHĨA cổng, sửa con số không chạm tới. Tiêu chí dùng lại máy đã có (`validate_arm_record()` đã cấm `mo_ta`+`PASS` và đã bắt khai `delta_r_pham_vi`) thay vì khai lại luật — hai danh sách song song sẽ trôi lệch. Kế toán là **QUAN HỆ** *"số `B2` CONSUMED khớp số arm đã chạy"*, module **không chứa số 18 lẫn số 9**, có ca AST ghim. 📌 **Kiểm-có-răng phá THẬT trong Docker** (khôi phục `diff -q` giống hệt): bỏ `_kiem_arm_phan_quyet` ⇒ **2 đỏ** · ghim cứng `18` ⇒ **6 đỏ** · bỏ `_kiem_ke_toan` ⇒ **1 đỏ**. ⚠️ **Dự đoán trong cột bên trái SAI**: nó viết *"trả tiêu chí về 18 ⇒ đúng 1 ca đỏ"*, đo thật ra **6** — ca ghim quyết định CÓ đỏ, nhưng ghim cứng `18` phá luôn HÀNH VI chứ không chỉ vi phạm luật. Mạnh hơn dự đoán, không yếu hơn, nhưng đã đính chính tại chỗ trong DR (`d363abb`) chứ không im. 📌 Một ca tôi tưởng do lớp mới canh hoá ra do lớp CŨ: `test_Z0_T1_mang_co_mo_ta` **không** đỏ khi bỏ hàm kiểm mới — `validate_arm_record()` bắt độc lập. Không phá thật thì đã ghi vào DR một câu sai. 🔴 **CHƯA xong, KHÔNG thuộc việc này:** `close_d4_gate()` là `TD-0186`, và hàm đó phải **GỌI** `kiem_tieu_chi_dong_d4()` chứ không khai lại tiêu chí (bài học cổng D3.5). `back-end-note.md:80` đã gắn đính chính (`74ad7ba`, lệnh "chuẩn hóa và lưu") nhưng vẫn giữ nguyên con số 18 làm lịch sử |
| TD-0237 | 🔴 **`MT-41` — kế hoạch cỡ lệnh phải sống sót qua restart.** `custom_stake_amount()` lưu kế hoạch vào `self._cho` (bộ nhớ tiến trình), `order_filled()` mới ghi nó vào `trade.custom_data`. Giữa hai mốc đó là một lệnh post-only chờ tới **180 phút**, nên một lần restart trong cửa sổ ấy làm `order_filled` raise `SizingError` — mà `strategy_safe_wrapper` **nuốt** exception của callback ⇒ vị thế mở **không có `co_lenh`/`ke_hoach`**, rồi `custom_stoploss`/`adjust_trade_position`/`custom_exit` đều raise theo ⇒ Freqtrade dùng `stoploss = −0,99` của config. **Một chốt fail-closed bị nuốt là một chốt KHÔNG TỒN TẠI** | ✅ | TD-0235 | Ca test dựng đúng kịch bản: có kế hoạch trong `_cho` ⇒ **xoá sạch bộ nhớ tiến trình** ⇒ `order_filled` vẫn lắp được kế hoạch. **0 trial.** ⚠️ Sửa xong thì mọi số backtest trước đó **không so trực tiếp được** (`DR-D4-08` §8) — nên phải cân với việc D4 chưa chạy. 🔴 KHÔNG nới `SizingError` thành cảnh báo: *"thiếu là lỗi lắp ráp, không phải chưa có"* ✅ **Xong (`07326c2`), phương án B chốt qua `AskUserQuestion` với chủ dự án trong phiên.** `order_filled()` không raise ngay khi `self._cho` rỗng — suy ngược lại `co_lenh`/`ke_hoach` từ hai nguồn Freqtrade đã bền vững (đúng khuôn `gap_ms.py` né cùng lớp lỗi MT-40/MT-41): `entry_tag` (giải mã lại, đúng bước đầu `custom_stake_amount()` đã làm) + `trade.stake_amount` THẬT ngay sau khi tranche 1 khớp. `w_tranche`/`L_exchange` là hằng số cấu hình (N4) nên `n_full = stake1_thật × L_exchange / w[0]` suy ngược CHÍNH XÁC — `planned_risk_usdt`/`planned_margin_usdt` (thứ tranche 2/3 và kết nạp danh mục thật sự đọc) đều chính xác theo. `mult` (6 hệ số) và `rho_eff_pct` KHÔNG suy ngược được (nhiều tổ hợp mult cho cùng notional) — đánh dấu NaN (cùng quy ước `_zss_hien_tai`, N6: không bịa số), không ảnh hưởng risk management hạ lưu. Hàm thuần `phuc_hoi_ke_hoach_sau_restart()` mới ở `sizing.py`; vẫn `raise SizingError` khi `enter_tag` không giải mã được hoặc `stake ≤ 0` — không nới thành cảnh báo. Kiểm-có-răng: mutation trả raise ngay ⇒ đúng **1 ca đỏ** (ca ghim kịch bản restart), 3 ca còn lại (không-restart, hai ca raise-khi-hỏng-dữ-liệu) vẫn xanh. Docker full suite **1917 passed, 0 failed** (2:57), baseline **1917** đo bằng `--collect-only` ngay trước khi chạy. |
| TD-0238 | 🔴 **`MT-40` — đỉnh equity phải bền vững qua restart.** `_dinh_equity` khởi tạo `None` ở `__init__` ⇒ mỗi lần bot chạy lại, đỉnh reset về số dư hiện tại ⇒ `_dd_pct()` trả **0%** ⇒ `mult_dd` không bao giờ tới ngưỡng `soft` hay `halt`. §12b.2 khai `mult_dd` là **một trong BA tầng** chặn vòng lặp thua lỗ, và ba tầng đó thuộc **Cấp C** *"không bao giờ được đổi"* — một tầng tự vô hiệu sau mỗi lần restart là tầng không tồn tại | ✅ | TD-0235 | Đỉnh equity đọc lại từ nguồn bền vững (DB Freqtrade hoặc sổ riêng có xuất xứ), **không** từ bộ nhớ tiến trình. Kiểm-có-răng: dựng chuỗi số dư có đỉnh rồi sụt quá `halt`, **khởi động lại** giữa chừng ⇒ vẫn HALT. **0 trial** ✅ **Xong (`487d483`), hai câu hỏi mở chốt qua `AskUserQuestion` với chủ dự án trong phiên:** (1) cơ chế = **checkpoint SỐNG** — ghi nguyên tử mỗi khi có đỉnh mới, đọc lại ở `bot_start()`; bị bác: tính lại từ DB Freqtrade (bỏ sót phí funding trên vị thế đang mở giữa hai lần đóng lệnh, có thể đánh giá THẤP đỉnh thật, nới lỏng HALT sai hướng an toàn); (2) reset = **KHÔNG BAO GIỜ** (cực đại lịch sử, không thêm tham số DOF mới). Module mới `src/tool_d/equity_peak.py` (`DinhEquityBenVung`/`dinh_equity_moi`/`luu_dinh_equity`/`doc_dinh_equity`, cùng khuôn ghi/đọc nguyên tử `risk_supervisor.py` TD-0241 nhưng file RIÊNG — tiến trình strategy khác tiến trình risk supervisor §6.6, không nhét chung trạng thái). `ZoneAbsorption.bot_start()` nạp lại đỉnh (chỉ live/dry_run qua `runmode` gate — backtest/hyperopt không rò trạng thái); `_dd_pct()` ghi đĩa mỗi khi có đỉnh mới. 🔴 Đã đọc mã nguồn Freqtrade thật (`strategy/interface.py::ft_bot_start()`) trước khi viết: exception trong `bot_start()` **không** bị `strategy_safe_wrapper` nuốt im lặng (khác MT-16 vii) — re-raise thành `StrategyError`, đúng hướng fail-closed. Kiểm-có-răng thật: tắt dòng ghi `luu_dinh_equity()` ⇒ đúng **3 ca đỏ** (kịch bản restart, ghi-mỗi-lần-có-đỉnh-mới, lệch `stake_currency`), 0 ca ngoài file test mới. Docker full suite **1940 passed, 0 failed** (baseline **1917** đo lại bằng `--collect-only` NGAY trước khi so + 18 unit + 5 lock, đúng cộng). **0 trial** — không chạm CALIB/WFO/LOCKBOX |
| TD-0239 | 🔴 **Nối Decision Log vào đường sản xuất (§8.3).** `src/tool_d/ledger/decision_log.py` có cửa ghi append-only, `dedup_key` theo `order_id` TỪNG tranche, `flock`, và test `L-Z45`/`TD-0201` — nhưng **0 người gọi sản xuất**. Đây là *"thứ được canh không nằm trên đường chạy"* (`TD-0168`) ở dạng nặng nhất: sổ tồn tại, luật đầy đủ, không ai ghi vào | ✅ | TD-0235 | Mỗi tranche khớp sinh **đúng một** bản ghi (dedup theo `order_id`), mang `nguon` thuộc backtest/dry_run/live và khối xuất xứ. Kiểm bằng một lượt backtest THẬT rồi đếm bản ghi so với số fill; chạy lại cùng timerange ⇒ **thêm 0 dòng** (đúng bug Tool A số 7 mà `L-Z45` sinh ra để chặn). **0 trial** ✅ **Xong (`05bff6a`):** `src/tool_d/vao_ra_lenh.py` (mới, hàm thuần `sinh_ban_ghi_vao_lenh`) nối vào cuối `order_filled()` sau khi `custom_data`/`_cap_nhat_chot_loi` đã ghi xong (Freqtrade nuốt exception callback — đặt fail-closed sau cùng để không mất logic thiết yếu nếu nó raise). **Phạm vi CHỈ chiều VÀO** (mỗi tranche entry khớp) — bản ghi cho lệnh RA (TP1/TP2/SL/DG6/DG8) là mã việc riêng, chưa mở. Kiểm-có-răng phá THẬT trong Docker, khôi phục byte-đối-byte: bỏ dòng gọi ⇒ 0 bản ghi vs 3 fill thật · `order_id` không tất định ⇒ chạy lại cùng timerange thành 6 dòng thay vì 3 (đúng bug Tool A số 7). 🔴 **Sự cố phát hiện giữa chừng, đã vá cùng commit:** một lượt full suite (không phải test riêng của việc này) làm lộ `DEFAULT_DECISION_LOG_PATH`/`DEFAULT_CONFIG_PATH` là đường dẫn TƯƠNG ĐỐI — hai test đã có từ trước (`test_td0187`, `test_td0189`, chạy backtest thật với `cwd=REPO_ROOT`) vô tình ghi thẳng vào `registry/decision_log.jsonl` CỦA REPO mỗi lần chạy, kể từ khi `_ghi_vao_lenh` được nối (phiên `-54` phát hiện qua đọc `git status`, không qua test). Đã xoá file rác (chưa commit, không vi phạm LD-20) và vá gốc: cô lập `_chay()`/`_chay_rieng()` bằng `cwd=tmp` + copy `config/` (tự động bảo vệ 3 file phụ thuộc khác qua importlib). **Đối chứng số liệu bắt buộc trước khi tin cách vá** (yêu cầu của `-54`): gọi trực tiếp bản GỐC và bản ĐÃ SỬA, so toàn bộ `trades` — **giống hệt nhau**, xác nhận cô lập không đụng phép đo. Docker full suite: **1969 passed, 2 failed** (cả hai không liên quan — `idea_queue` không còn rỗng, `tu-dien-du-lieu.md` đang được việc khác sửa); `registry/decision_log.jsonl` không còn tái xuất hiện sau full suite |
| TD-0240 | 🔴 **`MT-42` — bộ tính 22 chỉ số của E5.** `report_model.py` trả `Measured.pending(...)` với lý do là **hằng số chuỗi** `_VAN_HANH` = *"chưa có lệnh đóng thật — D0-PRE chưa có strategy code"* — câu đó nay đã lỗi thời (chiến lược có rồi), và quan trọng hơn là **không có đường nào** để một chỉ số chuyển sang `ok`. ⇒ dry-run hai tuần vẫn in ra 22 dòng `pending` | ✅ | TD-0239 | 22 chỉ số tính được từ Decision Log và DB. 🔴 **CHỈ đổi bộ TÍNH, KHÔNG đổi `code`/`label`** — `content_fingerprint()` băm đúng `code` cộng `label`, nên đổi nhãn = **đổi nội dung báo cáo = tiêu 1 trial** (`spec:4808`, TD-0062). Ca test: một chỉ số chưa có dữ liệu vẫn phải là `pending` với lý do **đọc được**, không phải `0.0` (N6). **0 trial** nếu hash không đổi — kiểm bằng test hồi quy hash đã có ✅ **Xong (`c68ddf0`), phạm vi HẸP theo chủ dự án chốt (AskUserQuestion):** chỉ làm 8/22 chỉ số CÓ ĐỦ NGUỒN ngay — `BUDGET_B3_REMAINING`/`N_CURRENT`/`DSR_CURRENT` (`trial_registry.jsonl`), `SL_GAP_MS_DIST` (Decision Log `DOI_SL`), `H3`, `TRANCHE_FILL_1/2/3`, `WINRATE_LONG/SHORT` (DB Freqtrade qua **ORM `Trade.get_trades()`**, chốt kỹ thuật thứ hai của chủ dự án — không tự viết SQL, tránh diễn giải lại ý nghĩa cột). 14 chỉ số còn lại **giữ `pending` với LÝ DO MỚI CỤ THỂ** (không phải câu `_VAN_HANH` cũ đã lỗi thời) — chi tiết từng lý do trong docstring `report_model.py`; không mở rộng sang viết bản ghi RA lệnh/PLAN (mã việc riêng). Module mới `src/tool_d/reporting/freqtrade_db.py` — `doc_danh_sach_lenh(db_url)` **bắt buộc nhận `db_url` làm tham số**, không mặc định ngầm. N6: thêm `*_error` cho từng nguồn để phân biệt **UNREADABLE** (lỗi đọc) với **PENDING** (chưa có dữ liệu). 🔴 **Phát hiện của `TD-0246` (phiên `-54`), sửa TRƯỚC khi commit:** DG1-DG5 không bao giờ được xét trên arm ∈ `ARM_DON_TRANCHE` — `TRANCHE_FILL_2/3` trên các arm đó **luôn = 0 một cách CẤU TRÚC**, không phải "đã đo và bằng 0"; `build_metrics()` nhận thêm `arm`, trả `pending` (không phải `ok(0.0)`) khi arm thuộc tập đó hoặc chưa biết arm. Kiểm-có-răng phá thật trong Docker: bỏ check `ARM_DON_TRANCHE` ⇒ 2 ca đỏ đúng (OK sai thay vì PENDING) · bỏ check `trades_error` ⇒ 1 ca đỏ đúng (PENDING thay vì UNREADABLE). 🔴 **Sự cố phát hiện giữa chừng (lần 2 trong ngày, cùng họ `TD-0239`):** `db_url` là đường **TUYỆT ĐỐI** (khác `decision_log.jsonl` — tương đối), không cô lập được bằng `cwd`; lần full suite đầu đã tự tạo `user_data/tradesv3_dryrun.sqlite` THẬT trong repo (bị `.gitignore` che nên không có nguy cơ commit nhầm, nhưng vẫn là rác). Đã xoá + vá gốc: thêm 3 cờ CLI tuỳ chọn (`--freqtrade-config-path`/`--decision-log-path`/`--registry-path`, mặc định `None` = dùng sản xuất thật — đúng vai trò entrypoint) để mọi test truyền `tmp_path`. Docker full suite: **1989 passed, 2 failed** (không liên quan — `idea_queue` không còn rỗng, `tu-dien-du-lieu.md` đang được việc khác sửa); xác nhận riêng: **0 file `.sqlite`** còn sót sau full suite |
| TD-0241 | 🔴 **Risk Supervisor tiến trình RIÊNG (§6.6) + test `L-Z44`.** Hôm nay `src/tool_d/risk_supervisor.py` là tầng breaker và phân loại mã lỗi của `binance_public.py`, **không** phải cái §6.6 đòi: một tiến trình **không import code bot**, tự gọi API đọc ký quỹ và vị thế, và coi `LIQUIDATED` là **cờ đỏ dừng toàn hệ thống**. `L-Z44` (đối chiếu hai bản khai của `E_D`, thang 5/8/20, trần margin 0,85) **chưa tồn tại** — `ls tests/lock/` không có file nào mang mã đó | ✅ | TD-0235 | Tiến trình riêng chạy được, và grep các dòng import trong nó **không** trỏ vào chiến lược. `L-Z44` báo đỏ khi hai bản khai lệch nhau — đây là **ngoại lệ hợp lệ DUY NHẤT** của *"một nguồn sự thật"* (LD-09), nên phép đối chiếu chính là thứ giữ nó hợp lệ. **0 trial** ⏳ **VẾ `L-Z44` XONG (`ac3f06f`), VẾ "tiến trình riêng" CHƯA — giữ 🔒, KHÔNG đánh ✅.** `tests/lock/test_lz44_khai_lai_hang_so_supervisor.py` (11 ca) + 1 dòng vào bảng `doi_chieu`. Docker full suite **1784 passed, 0 failed** (2:49), baseline **1773** đo lại bằng `--collect-only` NGAY trước khi so, không lấy từ file này. Diff **215 thêm / 0 xoá** — không khẳng định cũ nào bị gỡ. 🔑 **Phát hiện chính, và nó KHÔNG phải thứ dòng này mô tả:** `L-Z44` không hề "chưa tồn tại" — `kiem_khai_lai_khop_ban_goc()` có từ `TD-0196` cùng 2 ca ở `tests/unit/`. Lỗ hổng thật nằm chỗ khác và lớn hơn: `HANG_SO_KHAI_LAI` khai **NĂM** trường, bảng `doi_chieu` chỉ liệt **BỐN** — `tran_margin_ty_le` (0,85) khai lại từ 09/09 mà **chưa bao giờ được đối chiếu**, dù §6.6(2) gọi đích danh *"trần margin 0.85"*. Không phép kiểm nào đỏ, vì mọi phép kiểm đều hỏi *"bốn cái kia có khớp không"* — **không cái nào hỏi "có phải chỉ có bốn cái không"**. Ngoại lệ LD-09 của Supervisor hợp lệ **CHỈ NHỜ** phép đối chiếu, nên một hằng số ngoài tầm canh là ngoại lệ mất đúng thứ biện minh cho nó; đây là *lớp canh chĩa nhầm hướng* ở dạng lặng nhất — cái thiếu không để lại triệu chứng. Bản gốc lấy qua `tier_frozen.mult_deploy_thr.value` (đường mà `TRAN_MARGIN_TREN_E_D` đã bị ghim, `test_admission.py:148`), **KHÔNG** thêm khoá YAML mới: §6.8f là GỐC, `mult_deploy_thr` là bên MƯỢN (`admission.py:80-88`), khoá thứ ba = nguồn sự thật thứ ba (MT-03) + đụng kế toán DOF. **Kiểm-có-răng, phá THẬT trên file sản xuất trong Docker, khôi phục `diff -q` giống hệt:** bỏ dòng đối chiếu ⇒ **1 đỏ** · lệch `e_d` 750→751 ⇒ **2 đỏ** · thêm `import tool_d.config.loader` ⇒ **2 đỏ** (cả blocklist LẪN allowlist) · thêm hằng số thứ sáu không đối chiếu ⇒ **1 đỏ**. 📌 Lần phá thứ tư ĐẦU **không cho kết quả nào** — chèn trường có mặc định TRƯỚC trường không có làm dataclass hỏng, lỗi ngay lúc import: **người phá sai, không phải lớp canh giả** (đúng bẫy phiên rà lớp canh 08/09 ghi). Đã phá lại cho đúng. 🔴 **CÒN LẠI, CHẶN BỞI QUYẾT ĐỊNH KIẾN TRÚC — chờ chủ dự án:** vế *"tiến trình riêng chạy được"*. `risk_supervisor.py:12-15` đã tự khai cách nó chạy thật là câu **CHƯA CHỐT** — `L-Z36` khoá cứng `entrypoints/` ở **đúng 8 file** (§0d.2 dòng 664), nên đặt tiến trình này ở đâu (script ngoài `entrypoints/`? một chế độ của entrypoint có sẵn?) là quyết định kiến trúc, **quy tắc 2 cấm tự chọn**. Đánh ✅ lúc này tạo đúng thứ N12 mục 3 dạy đừng tin: một dòng ✅ mà bằng chứng trên đĩa chỉ phủ một nửa. ✅ **VẾ CÒN LẠI ĐÃ XONG (`DR-D11-03`, phiên `-a5`, 14/09/2026) — nay đánh ✅ trọn vẹn.** Quyết định kiến trúc mà đoạn trên nói "chờ chủ dự án" đã được chốt qua `AskUserQuestion` với chủ dự án ngay trong phiên: (1) phát hiện `LIQUIDATED` bằng REST polling `GET /fapi/v1/forceOrders?autoCloseType=LIQUIDATION` (không WebSocket); (2) làm LUÔN việc gọi Freqtrade REST API để dừng bot thật (`POST /api/v1/stop`), không chỉ ghi cờ chờ nối sau — **mở rộng phạm vi Tiêu chí XONG gốc**; (3) tiến trình đặt tại `src/tool_d/ops/risk_supervisor_daemon.py` (module độc lập, cùng khuôn `heartbeat_watchdog.py` của TD-0209, phiên `-91` đang dựng song song — không phải thư mục `ops/` cấp gốc mới, không phải entrypoint thứ 9). Bằng chứng đọc mã nguồn Freqtrade thật (`docker-freqtrade:latest` tag `2026.7`, đúng khuôn TD-0028, xem `DR-D11-03` §2) trước khi viết code gọi API: `/api/v1/stop` idempotent (`rpc.py:978`, gọi lặp khi đã `STOPPED` trả `"already stopped"`, không lỗi); xác thực chấp nhận HTTP Basic trực tiếp, không bắt buộc `/token/login`; schema `api_server` đòi `username`/`password`/`jwt_secret_key` bắt buộc một khi khoá đó xuất hiện trong BẤT KỲ config nào truyền vào Freqtrade — vì thế **KHÔNG sửa `config/freqtrade/config.json` dùng chung** (sẽ làm vỡ mọi backtest hiện tại của 5-6 phiên song song nếu thiếu 3 biến ENV), tách hẳn thành `config/freqtrade/config.risk_supervisor.json` (file phụ, chỉ dùng qua `-c` thứ hai từ D11). `autoCloseType=LIQUIDATION` xác nhận qua mã nguồn `ccxt` bundled trong image (`binance.py:13351`), không đoán qua trang docs render-JS. Thêm: `binance_public.py` (`_goi_json_ky`, `get_account_info`, `get_position_risk`, `get_force_orders`, `trang_thai_breaker_hien_tai` — đi ĐÚNG cùng breaker/rate-limit với endpoint public, khác `latency_samples_ms`); `risk_supervisor.py` (`phat_hien_thanh_ly`, `TrangThaiBenVung`/`luu_trang_thai`/`doc_trang_thai` — bền vững hoá cờ đỏ qua restart, bài học `MT-40`/`MT-41`); module mới `api_client/freqtrade_control.py` (`dung_bot`, R5 bounded retry 5 lần, 401 không retry); module mới `ops/risk_supervisor_daemon.py` (`chay_mot_vong_giam_sat` thuần + `main()` khung CLI, `--max-iterations` cho R5/test). Mở rộng `test_lz44_khai_lai_hang_so_supervisor.py` (đề xuất của phiên `-3f`, không viết lock test thứ hai): ca "không import code bot" nay lặp trên BA file; tách riêng ca "risk_supervisor.py không tự import tool_d.config" (kỷ luật module thuần) khỏi ca dùng chung (§6.6(2) đúng nghĩa hẹp — daemon ĐƯỢC PHÉP import config để tự đối chiếu L-Z44 sống lúc khởi động). Docker full suite **1880 passed, 0 failed** (3:12). Smoke test thủ công (`docker compose run --rm freqtrade`, không có credential thật): thiếu `BINANCE_API_KEY`/`SECRET` → `CRITICAL` + exit 99; thêm Binance giả nhưng thiếu `FREQTRADE_API_USERNAME`/`PASSWORD` → `CRITICAL` + exit 100; đủ cả hai (giả) → qua được cổng L-Z44 + cổng cờ đỏ cũ, vào vòng lặp thật, Binance từ chối auth giả (HTTP 401), cô lập đúng từng endpoint, không crash, thoát 0 sau 1 vòng. `grep -n "^import\|^from"` cả ba file xác nhận thủ công: không dòng nào trỏ `ZoneAbsorption`/`freqtrade.strategy`/`user_data.strategies`. **0 trial** — không chạm CALIB/WFO/LOCKBOX, không đánh giá cấu hình nào. |
| TD-0242 | **Validate fail-closed khi thiếu `BINANCE_API_KEY` hoặc `BINANCE_API_SECRET`.** Đây là hạng mục **(3)** của dòng `D10–D12` trong mục *"Việc đã biết là sẽ có, chưa mở"*, và nó là hạng mục duy nhất ở đó **chưa từng có mã việc** — `TD-0209` tự khai không gộp nó vào. Thiếu key lúc một entrypoint cần đặt lệnh thật phải **báo rõ**, không bị chặn âm thầm không ai hay | ✅ | — | Entrypoint cần key mà thiếu key ⇒ exit code riêng cộng thông báo đọc được, **không** phải một lỗi mạng trông giống sự cố sàn. Kiểm-có-răng: xoá biến môi trường ⇒ đúng 1 ca đỏ. 🔴 **KHÔNG commit key thật**; `config/freqtrade/config.json` giữ `api_key` và `secret` rỗng. **0 trial** ✅ **Xong (`bbf16f1`):** `validate_credentials_for_live()` trong `binance_public.py` — rỗng tính như thiếu, raise `BinanceCredentialsMissingError` (thông báo tự khai "lỗi CẤU HÌNH cục bộ, KHÔNG phải sự cố mạng/sàn"), exit code riêng `EXIT_MISSING_API_CREDENTIALS=99`. Kiểm-có-răng thật: vô hiệu hoá nhánh raise ⇒ đúng 6 ca đỏ, tất cả và chỉ trong `TestValidateCredentialsForLive`, 0 ca ngoài lớp đó. Docker full suite: **1762 passed** (1755 + 7, đúng cộng — baseline đo lại bằng `--collect-only` ngay trước khi so, xác nhận chéo bởi phiên `-80`). ⚠️ **Phạm vi, đọc đúng:** đây là hàm THUẦN, **CHƯA nối vào entrypoint nào** — không có entrypoint E1-E8 nào đặt lệnh thật hôm nay (D10-D12 chưa mở), cùng khuôn "hàm thuần trước, nối vào tiến trình thật là việc SAU" đã dùng ở `risk_supervisor.py` (TD-0196). Khi launcher live được viết, `main()` của nó gọi hàm này Ở ĐẦU rồi bắt `BinanceCredentialsMissingError` → `sys.exit(EXIT_MISSING_API_CREDENTIALS)`. |
| TD-0243 | 🚪 **`DR-D11-01` — môi trường testnet và ngưỡng chấp nhận D10, viết TRƯỚC khi chạy.** `spec:2954`: *"**KHÔNG được vào D11** (Dry-run) nếu D2 chưa verify bằng (a) testnet hoặc (b) một lệnh live tối thiểu"* — không có đường thứ ba, và dry-run **không** thay được testnet (`spec:2902-2913`: dry-run khớp lệnh mô phỏng **cục bộ**, không gọi API đặt lệnh). `spec:4480` giả định testnet *"dựng ở D3.5, dùng lại nguyên vẹn cho D10"* nhưng `DR-D35-01` đã **loại** testnet ⇒ D10 và D11 đang thừa hưởng một thứ **không tồn tại**. D10 phải đo **BA** thứ (`spec:2956-2972`): lệch khớp tranche D6 · D2b/D2c với **≥ 30** lần đổi khối lượng SL · tỉ lệ khớp post-only | ✅ | — | 🔴 **Ngưỡng chấp nhận điền bằng DR TRƯỚC khi chạy D10** (`L-Z35`, `spec:2962-2963` — *"KHÔNG sau khi thấy số"*). DR commit **RIÊNG và TRƯỚC** mọi dòng mã, kiểm bằng `git merge-base --is-ancestor`, không bằng mắt. **0 trial.** ⚠️ Hạng mục **dài ngày nhất** còn lại trước D11 và **không phụ thuộc D4** ⇒ chạy song song được. ✅ **Xong 13/09/2026** (`docs/decisions/DR-D11-01-testnet-va-nguong-D10.md`, commit `39dbc5a`→`083d6ac`→`823c305`): testnet-qua-Freqtrade bị loại bằng đọc mã nguồn (`TD-0116`), đi thẳng phương án (b) lệnh live tối thiểu; siết IP whitelist + tắt Universal Transfer NGAY trước khi đặt lệnh (sớm hơn "trước D11" ban đầu); ba ngưỡng đều neo vào số ĐÃ NIÊM PHONG, không bịa số mới — D6 ≤ Δ_R=0,161206R (D3.5), D2c p99(gap_ms) ≤ 15.000ms qua mẫu **cưỡng bức** (định nghĩa phép đo ở `DR-D11-02`/MT-39, ngân sách cưỡng bức 20 vị thế/14 ngày+gia hạn 1 lần chốt tại đây), post-only neo `p_nf_cao`=0,0% (D3.5 Bước 2). Rà soát chéo với phiên song song `-fa` bắt được và sửa một mâu thuẫn thật giữa hai DR (§3.2 của `DR-D11-02` ban đầu giả định testnet, đã tự sửa ở `bcc4bf1` sau khi đối chiếu) — xử theo quy tắc 11, không tự chọn bên. **0 dòng mã** — cơ chế kỹ thuật cưỡng bức tranche 2/3 để ngỏ cho `TD-0244` |
| TD-0244 | 🔴 **Bộ sinh `gap_ms` (§8.3) — nguồn DUY NHẤT cho D2c.** Grep `gap_ms` toàn repo cho **đúng một** kết quả: `report_model.py:71`, và đó là một **NHÃN** của chỉ số `SL_GAP_MS_DIST`, không phải một bộ sinh. §8.3 đòi *"mỗi lần đổi KHỐI LƯỢNG SL = một bản ghi có `gap_ms`"*, và D10 đòi phân bố `gap_ms` qua **≥ 30** lần đổi. ⇒ chỉ số này không thể rời trạng thái `pending` bằng bất cứ cách nào hiện có | ✅ | TD-0243 · MT-39 | Mỗi lần khối lượng SL đổi sinh một bản ghi Decision Log mang `gap_ms` đo được. ⚠️ Phụ thuộc cách giải `MT-39`: nếu SL **không** nằm trên sàn thì `gap_ms` đo cái gì là một câu chưa có đáp án — đó chính là lý do `MT-39` phải được ghi trước. **0 trial** 🔧 **LÀM MỘT PHẦN 13/09/2026 (`7c1233e`+`3bac677`), CÒN CHẶN — giữ 🔒, chưa ✅.** Phần cơ chế xong: `src/tool_d/gap_ms.py` (hàm thuần `sinh_ban_ghi_doi_sl`, đọc lịch sử `trade.orders`, không giữ trạng thái tiến trình — né MT-40/MT-41) nối vào `ZoneAbsorption.custom_stoploss()` qua `_ghi_gap_ms()`, ghi bản ghi `DOI_SL` vào `registry/decision_log.jsonl` (hằng số `DEFAULT_DECISION_LOG_PATH` mới ở `decision_log.py`, để dành cho `TD-0239` dùng chung — LD-09). `config/freqtrade/config.json` bật `stoploss_on_exchange: true` (MT-39 phương án (a)) + đồng bộ `stoploss = -0.99` giữa config và chiến lược (trước đây `-0.30` trong class không bao giờ có hiệu lực — `DR-D11-02` §5). Test khoá mới `test_td0244_stoploss_on_exchange.py` ghim cả ba. Docker: 8+3 test mới xanh, 47 test tích hợp chiến lược (td0187/89/92/93) xanh, không có test nào của TD-0244 đỏ. 🔴 **CHẶN MỚI, phát hiện bởi phiên `[564e4e]` sau khi code xong phần trên:** arm sản xuất đã chốt là `Z0` (`DR-D4-10` §2.4, 12/09) — `Z0 ∈ arm_switches.ARM_DON_TRANCHE` (single-entry, không bao giờ thêm tranche) ⇒ khối lượng SL **không bao giờ đổi** trên cấu hình sẽ chạy thật ⇒ `_ghi_gap_ms()` sẽ luôn trả `[]` khi `TD-0227` chuyển `arm` sang `"Z0"` (hiện `tool_d_config.yaml:139` vẫn `"Z3"`, chưa nổ vì `TD-0227` phụ thuộc cổng D4 chưa đóng). Mâu thuẫn giữa `DR-D4-10` §2.4 (12/09) và `DR-D11-01`/`DR-D11-02` (13/09, giả định có sự kiện để cưỡng bức đo) — ghi theo quy tắc 11, chưa tự chọn bên, xem mục 7 `back-end-note.md` (chờ "chuẩn hóa và lưu"). Ba đường đã nêu, chờ chủ dự án: (a) D2c thành N/A cho hệ thống single-entry; (b) cưỡng bức trên một CẤU HÌNH KHÁC có DCA chỉ để đo cơ chế; (c) mở lại `DR-D4-10` §2.4, giữ `Z3`. ✅ **ĐÃ CHỐT 13/09/2026 — chủ dự án chọn (a): D2c SẼ là N/A khi arm sản xuất chuyển sang single-entry.** Không cưỡng bức trên cấu hình khác, không mở lại `DR-D4-10` §2.4. ⚠️ **ĐÍNH CHÍNH CÙNG NGÀY (rà soát chéo `-3f`/`-80`): "N/A" là CÓ ĐIỀU KIỆN, chưa đúng ngay hôm nay.** `tool_d_config.yaml:139` hiện vẫn `arm: "Z3"` (có DCA, KHÔNG thuộc `ARM_DON_TRANCHE`) — D2c hôm nay **vẫn là điều kiện SỐNG**, chỉ N/A SAU KHI `TD-0227` chuyển arm sang `Z0`. Đã bác một claim sai (TP1 có sinh sự kiện) bằng đọc mã nguồn: Binance Futures `stoploss_blocks_assets=False` khiến `cancel_stoploss_on_exchange(allow_nonblocking=True)` tự thoát sớm khi TP1 chốt — TP1 không đổi kết luận. Test tripwire mới (`test_td0244_stoploss_on_exchange.py`) chốt cứng giá trị arm quan sát được hôm nay làm mốc so sánh nên đỏ khi arm đổi sang BẤT KỲ giá trị nào khác — dù đi vào hay đi RA khỏi `ARM_DON_TRANCHE` (chiều đi ra mới nguy hiểm nhất: DCA quay lại production mà không ai xét lại N/A) — buộc đọc lại `MT-43` trước khi coi ca đỏ là bình thường. Cơ chế đo (`gap_ms.py` + hook) **giữ nguyên, không phải code chết** — đang có dữ liệu thật NGAY HÔM NAY nếu chạy live/dry-run (arm hiện tại có DCA). Còn lại hai việc phái sinh, **KHÔNG thuộc TD-0244**: `TD-0240` (bộ tính 22 chỉ số E5) khi tới lượt phải gán lý do `pending`/N/A cho `SL_GAP_MS_DIST` theo ĐÚNG điều kiện arm, không phải hằng số tĩnh; `DR-D11-01` (ngưỡng D10) cần ghi rõ D2c phụ thuộc arm tại thời điểm D10 chạy, không giả định N/A sẵn — đã báo hai phiên đang giữ nội dung đó. **TD-0244 đóng.** |
| TD-0245 | 🔴 **Quy tắc 7 đang chặn `TD-0238` và `TD-0240`, vì `tu-dien-du-lieu.md` **chưa bao giờ tồn tại**.** Quy tắc 7 (`CLAUDE.md:25`) cấm đọc/ghi bất kỳ trường database nào trước khi tra từ điển; gốc repo chỉ có khuôn chỉ-đọc `template/tu-dien-du-lieu.md`. Hoãn là có ý thức và đã chốt **ba lần** (`ARCHITECTURE.md:249-251`, `:253-257`, `CLAUDE.md:867/923/968`) với điều kiện kích hoạt *"khi Freqtrade bắt đầu ghi SQLite lệnh"*. Nay `TD-0238` (đường vốn từ `trades`) và `TD-0240` (11/22 chỉ số đọc `trades`/`orders`) đều đụng DB, và cả hai là đường vào D11. Grep toàn repo: **0 dòng** trong `src/` · `entrypoints/` · `user_data/strategies/` đọc `close_profit_abs` ⇒ hai việc đó là **lần đầu tiên** dự án đọc trường PnL. 🔴 **Gà-và-trứng phải giải trước:** Quy tắc 8 đòi *"xuất schema thật từ database (sau khi migration đã chạy)"*, nhưng **không có file `.sqlite` nào trên đĩa** (`config/freqtrade/config.json:18` mới chỉ *ghim đường dẫn*) — DB chỉ sinh ra khi chạy dry-run, tức chính thứ Quy tắc 7 đang chặn. ⚠️ **Con số "6 bảng / 109 cột" là LỜI KHAI, chưa phải bằng chứng** — grep `109` · `6 bảng` · `sqlite_master` cho **0 kết quả** liên quan; theo `CLAUDE.md:27`, viết từ điển dựa trên nó = viết từ trí nhớ | ✅ | — | Chủ dự án chốt 14/09/2026 bốn điểm: **(1) nguồn schema** — gọi chính hàm khởi tạo DB của Freqtrade trong Docker lên đường dẫn tạm ⇒ Freqtrade tự chạy migration ⇒ đọc lại bằng `PRAGMA table_info` **từ file SQLite THẬT** (đúng chữ Quy tắc 8, tái lập được, **0 trial**, không chạm dữ liệu thị trường); **(2) phạm vi** — **CHỈ** 6 bảng SQLite của Freqtrade, không đưa `custom_data`, không trỏ sang 5 JSON Schema, không đụng Decision Log; **(3) cột Ý nghĩa** — ba trạng thái, **cấm bịa**: chỉ cột có bằng chứng `file:line` mới `✅ đã tra`, còn lại `⏳ chưa tra cứu` (N6 — không suy đoán từ tên trường, không để trống mập mờ); **(4) lớp canh** — test khoá **cưỡng chế Quy tắc 7 bằng máy**. **XONG khi:** artifact `docs/du-lieu-do/td0245-schema-sqlite-freqtrade.json` sinh từ một lần chạy **thật trong Docker** (khối xuất xứ qua `doc_runtime_image_digest()` — fail-closed, từ chối chạy ngoài Docker, đúng N7) · `tu-dien-du-lieu.md` **sinh tự động** từ artifact, **đúng 9 cột** bảng chính và **6 cột** bảng lịch sử (`TD-0233`: markdown **bỏ im lặng** ô vượt số cột header) · hai test khoá xanh. 🔑 **Kiểm-có-răng, phá THẬT trên file sản xuất** (bài học `TD-0168` — không chỉ mẫu dựng tay): sửa một ô từ điển ⇒ test A đỏ; hạ một cột đang `✅` về `⏳` ⇒ test B đỏ đúng chỗ mã sản xuất đọc cột đó. ⚠️ **N9**: `tu-dien-du-lieu.md` là file đặc tả — trả lời ba câu trong chat và chờ lệnh **"chuẩn hóa và lưu"** trước khi ghi. ⚠️ Số đo lệch "6/109" ⇒ **báo cáo con số đo được**, không sửa cho khớp. **0 trial** ✅ **Xong 14/09/2026** (`d876648` khoá → `1678125` đo → `210175f` từ điển+test). 🔑 **Lời khai được XÁC NHẬN bằng phép đo, không phải tin sẵn:** đúng **6 bảng / 109 cột** — `trades` 51 · `orders` 26 · `wallet_history` 11 · `KeyValueStore` 7 · `pairlocks` 7 · `trade_custom_data` 7. Cách đo (giải gà-và-trứng của Quy tắc 8 khi **không có file `.sqlite` nào trên đĩa**): gọi chính `freqtrade.persistence.init_db()` — hàm **bản thân bot gọi lúc khởi động** — lên đường dẫn tạm trong `/tmp` của container, rồi đọc lại bằng `PRAGMA table_info` từ **file SQLite có thật**; `models.py:48` chạy `create_all()` **rồi** `check_migrate()` ⇒ đúng chữ *"sau khi migration đã chạy"*, và là cùng đường mã dry-run sẽ đi. **Không sinh file `.sqlite` nào trong repo** (có test canh). Xuất xứ qua `doc_runtime_image_digest()` — fail-closed, từ chối chạy ngoài Docker (N7): Freqtrade 2026.8 · `9f10e35` · `sha256:7031bca4`. 🔴 **Từ điển KHÔNG GÕ TAY MỘT DÒNG** — kiểu dữ liệu/PK/NOT NULL/khoá ngoại suy THẬNG từ `PRAGMA`; **39/109 cột** đã tra nghĩa kèm `file:line`, **70 cột còn lại ghi thẳng `⏳ chưa tra cứu`** — điền cho đủ bằng suy đoán là đúng thứ Quy tắc 7 cấm, và một dòng bịa **trông y hệt** một dòng đã tra. 🔴 **HAI CÁI BẪY ĐÃ ĐỌC MÃ ĐỂ CHỐT, cả hai đều cắn thẳng Tool D:** (i) `trades.close_profit_abs` khi trade **CÒN MỞ** sau một lần thoát từng phần **chỉ là lãi của lần thoát CUỐI**, `realized_profit` mới là cộng dồn (`trade_model.py:1318-1321` vs `:1342-1344`) — TP1 chốt 50% qua `adjust_trade_position` ⇒ `PARTIAL_EXIT` ⇒ **trúng đúng ca này**; hôm nay TP1 chỉ nổ MỘT lần nên hai trường bằng nhau, tức một phép kiểm viết hôm nay sẽ **XANH** rồi sai im lặng ngày có lần thoát thứ hai; (ii) `funding_fees` **DƯƠNG = được NHẬN** và **đã nằm trong** `close_profit_abs` rồi (`:1130-1131`, `:1128-1135`) — trừ lần nữa là tính hai lần và sai dấu. Cộng lại là câu trả lời cho DR-013 (`calculate_profit` docstring `:1160` *"All calculations include fees"*). 🔴 **Quy tắc 7 nay có MÁY** (`kiem_quy_tac_7.py`): năm phép kiểm — `KHONG-CO-COT` (tên cột không tồn tại trong schema đo được) · `DOC-COT-CHUA-TRA` · `KHAI-DOC-CHUA-TRA` · `MIEN-TRU-CHET` · `MIEN-TRU-THIEU-LY-DO`. Phép quét **thiếu hụt CÓ KIỂM SOÁT** (chỉ tên cột đặc trưng, có gạch dưới): bài học 08/09 *"đừng đo tính chất NGỮ NGHĨA bằng dấu hiệu CÚ PHÁP"* — `self.timeframe` của chiến lược **không** phải cột `trades.timeframe`; nó bỏ sót tên chung nhưng **không bao giờ buộc tội oan** (tiền lệ `TD-0147`: *một chốt không bao giờ thoả được thì tệ hơn không có chốt*). **Kiểm-có-răng, phá THẬT trên file sản xuất** (`TD-0168`: không chỉ mẫu dựng tay): sửa 1 ô từ điển ⇒ **đúng 1 đỏ**; hạ `funding_fees` về `⏳` ⇒ **đúng 3 đỏ** (gồm ca *"cột đang đọc phải đã tra"*); hoàn nguyên ⇒ **22 passed**. 🔑 **Lớp canh bắt đúng người viết ra nó:** lần đầu nối, tôi truyền bản ĐÃ LỌC vào `kiem_quy_tac_7()` nên không mục miễn trừ nào được đánh dấu *đã dùng* ⇒ mục nào cũng bị `MIEN-TRU-CHET`; đã thêm ca hồi quy ghim đúng cái sai đó. Full suite Docker **1962 passed, 0 failed** (1940 mốc sau `TD-0238` + 22 ca mới, **cộng đúng**). Lệnh N9 *"chuẩn hóa và lưu"* 14/09/2026, ba câu đã trả lời trong chat. **0 trial** — không đọc một nến nào, không chạm CALIB/WFO/LOCKBOX. ⚠️ **Ghi nợ, ngoài phạm vi đợt này:** ruột `trade_custom_data.cd_value` (5 khoá riêng của Tool D, **chưa có schema nào**) · `decision_log.jsonl` là sổ JSONL **duy nhất không có JSON Schema**, trái chính `ARCHITECTURE.md:260` (chưa ai ghi thành `MT`) · `ARCHITECTURE.md:265-269` khai **3** schema, trên đĩa có **5** · `dashboard-ui/scripts/lib/doc-lenh-that.py:72-85` gõ tay **23+7 cột** ⇒ nguồn sự thật **thứ hai** (hình dạng `MT-03`), nên cho nó đọc từ điển · `ARCHITECTURE.md` mục 7 + ERD chưa cập nhật (Quy tắc 8 đòi, nhưng N9 đòi một lệnh *"chuẩn hóa và lưu"* riêng) · 🟡 **mâu thuẫn quy trình chưa chọn bên** (quy tắc 11): `QUY-TRINH-VIBE-CODE.md:423` nói từ điển cập nhật **tự động ngay, không cần chờ duyệt**, trái N9 vốn liệt đúng file này vào diện phải chờ lệnh — đã đi đường **chặt hơn** (hỏi), ghi lại để chủ dự án quyết |
| TD-0259 | 🟡 **Sửa test LỖI THỜI `test_idea_queue_van_rong_chua_mo` — suite đỏ 1 ca từ 14/09/2026.** `tests/unit/test_registry_schemas.py` khẳng định `registry/idea_queue.jsonl` **phải rỗng** vì *"OQ-07 chưa chốt"*; tiền đề hết đúng từ hai mốc đã commit: OQ-07 ✅ 07/09 (`DR-Q3-2026`, cửa NỘP mở) và `1a00c66` (TD-0226) nộp hợp lệ `IQ-0001`. ⇒ mọi *"N passed / 1 failed"* từ 14/09 đều mang ca này — một ca đỏ THẬT về sau sẽ lẫn vào nền đỏ quen mắt. Chủ dự án duyệt 16/09/2026: thay bằng *"mọi dòng sổ thật khớp schema"*, đối xứng ca `trial_registry` cùng class. 🔑 Giữ chốt `len > 0` — thiếu nó sổ rỗng cho PASS RỖNG; và nó không lỗi thời được vì sổ append-only không co (ghim QUAN HỆ, không ghim `== 1`). Không đổi schema, không đổi sổ, không đụng `tests/lock/` | ✅ | — | ✅ **Xong 16/09/2026** (`293849d`): full suite Docker **2000 passed, 0 failed** (lượt trước 1999 + 1 failed đúng ca này); file đơn 16 passed, số ca giữ nguyên; kiểm-có-răng trên bản clone đỏ đúng hai chỗ. Tiêu chí gốc — Docker: file đơn xanh + **full suite 0 failed**; kiểm-có-răng trên bản clone: sổ rỗng ⇒ đỏ ở `len`, xoá một khoá `required` ⇒ đỏ ở schema; `registry/idea_queue.jsonl` không đổi byte nào |

---

## Khối 19 — D5: Calibration, ngân sách B1 (mở 16/09/2026 theo yêu cầu chủ dự án)

> 🔴 **Vì sao khối này tồn tại, và nó MỞ LẠI một quyết định.** Ngày 13/09/2026 chủ dự án chốt *"dựng
> testnet trước, HOÃN đặc tả D5–D9"* (đoạn mở Khối 18). Ngày 16/09/2026 chủ dự án yêu cầu triển khai D5
> và duyệt kế hoạch — quyết định MỚI hơn, hợp lệ, nhưng `DR-D5-01` (TD-0250) phải **khai đích danh** rằng
> nó mở lại quyết định hoãn đó, để repo không có hai câu cùng đứng và ngược nhau (hình `MT-38`).
>
> 🔴 **Spec KHÔNG có đặc tả D5.** `tool-d-smart-dca.md:4492` chỉ ghi `D5–D9 (giữ nguyên)`. Căn cứ duy nhất:
> B1 = 3 giá trị × 12 tham số × 2 hướng = 72 (`:3235`), *"chọn từ ≤3 ứng viên trên tập CALIB"* (`:4527`),
> mỗi lần hiệu chỉnh là một trial ghi sổ TRƯỚC (§9.5).
>
> ✅ **Chủ dự án đã chốt 16/09/2026** (phiên `-24`, qua câu hỏi khi lập kế hoạch):
> 1. **KHÔNG tiêu suất B1 nào trước `d4_complete`** — định nghĩa cổng theo **`DR-D4-11`** (hiện vật), không
>    theo `OQ-11` (đếm 18 trial, đã bị thay). Khối này làm phần **0 trial** trước.
> 2. **Arm = `Z0-T1`, chỉ LONG** (DR-D4-12 §2.3; Short hoãn theo DR-D4-01). **7 tham số** calibrate:
>    `zss_threshold` · `wick_close_upper_frac` · `v_min` (nhóm LỌC LỆNH) · `buf_sl_atr` · `tp1_haircut_pct` ·
>    `dg7_funding_frac` · `max_hold_bars_4h` (nhóm KẾT CỤC). **Năm tham số giữ FROZEN:** bốn **cấu trúc không
>    thể tác động** — `dg4_bars_1h` · `dg6a_atr_ratio` · `dg6d_retrace_frac` · `funding_rate_pct` (căn cứ
>    **đường đọc mã**, không phải số đếm EXPLORE, `DR-D4-13` §1.2 mục 3) — và `mult_corr_thresholds` (chỉ
>    đổi cỡ lệnh, mà thước `R` bất biến với cỡ lệnh ⇒ chỉ đo được tác dụng phụ; `DR-D5-01` §2.2).
> 3. **Cách tiêu suất:** 1 lần chạy MỐC CHUNG (giá trị hiện tại) + 2 giá trị thử/tham số + 1 lần XÁC NHẬN
>    cấu hình ghép ⇒ **trần 16 suất** (15 nếu bỏ `max_hold_bars_4h = 32`, `DR-D5-01` §4). Ghép không thắng
>    mốc rõ ràng ⇒ giữ nguyên toàn bộ.
> 4. **Luật chọn:** ứng viên không vượt thuế nhiễu ⇒ **giữ giá trị hiện tại**. Ngưỡng *"thắng rõ"* = **> 0 sau
>    khi trừ nhiễu** (`h = 3,0777`, không thêm con số mới). Nhóm KẾT CỤC so cặp trên tập giao; nhóm LỌC LỆNH
>    **đo riêng nhóm lệnh bị thêm/bớt** (so cặp cho `d ≡ 0` với bộ lọc — hình `MT-21`). Chi tiết `DR-D5-01` §5.
> 5. **`v_min` ứng viên `0` / `1.0` / `1.5`** — `0` = tắt điều kiện (c) trên đúng `Z0-T1`, thay điều kiện
>    *"`Z0` thắng `Z0-V1`"* của `DR-D4-03` §5 (không thể thoả: `Z0-V1` bị cắt ở `DR-D4-12` §4, và arm sản xuất
>    đã đổi). Không phân biệt được ⇒ **giữ `1.0`**. ⇒ `DR-D5-01` phải khai là **mở lại `DR-D4-03` §5**.
> 6. **`wick_close_upper_frac` ứng viên `0.5` / `0.6` / `0.67`** — chỉ phía chặt: dưới 0,5 vế đóng cửa không
>    còn là "nửa trên", tức đổi định nghĩa §3.3b chứ không hiệu chỉnh.
> 7. **FreqAI:** D5 đi trước, độc lập; `DR-D5-01` ghi điều kiện xem lại nếu FreqAI đổi tín hiệu vào lệnh.
> 8. **Giá trị thử còn lại** (duyệt nguyên bảng): `zss_threshold` 0,4/0,6 · `buf_sl_atr` 0,3/0,5 ·
>    `tp1_haircut_pct` 10/30 · `dg7_funding_frac` 0,2/0,4 · `max_hold_bars_4h` 20/32. Danh sách băm trong
>    `DR-D5-01` §3.3 (`sha256 8f22faf3…`). ✅ `DR-D5-01` đã commit `492b559`.
> 9. **Mẫu nhỏ** (chủ dự án chốt 16/09, `DR-D5-01` §6.2 đính chính `2b76b16`): KHÔNG dời `T0`, KHÔNG mượn
>    WFO/EXPLORE/LOCKBOX. Hai đường tăng mẫu hợp lệ **bắt buộc trước suất 1**: TD-0252 (5m trọn CALIB) +
>    TD-0247 (rổ gồm mã đã huỷ niêm yết). Sau đó `n ≥ 30` ⇒ **chạy mặc định**, chủ dự án giữ quyền dừng.
> 10. **Máy kế toán B1 chặn TẠI CỬA `reserve()`** (chủ dự án chốt 16/09): suất sai không bao giờ vào sổ; đổi
>    helper `_kw()` của 12 file test đang đặt chỗ B1 tuỳ ý sang B3, không đổi khẳng định.
>
> ⚠️ **Hai bất biến không được vô tình phá:** FROZEN→TUNED **tại chỗ**, không chuyển khoá nào ra khỏi `tier_b`
> (`|tier_b|` 12→11 ⇒ `N` 114→108, rào DSR 3,0777→3,0601 — nới chuẩn của chính mình); và `L-Z29` chỉ canh
> **số đếm**, không canh **danh tính** (`MT-18`) — `DR-D5-01` phải trình câu có kích hoạt `MT-18` (b) ở D5 không.
>
> ⚠️ Quy tắc gốc 1: TD-0250 là giấy tờ; **TD-0251…TD-0258 chỉ bắt đầu khi chủ dự án gõ "bắt đầu code"**.
> Dãy mã `DR-D5-01` + `TD-0250…TD-0258` đã nhắn bốn phiên `-30`/`-ea`/`-df`/`-22` trước khi ghi (N12 mục 6), cả
> bốn xác nhận không dùng trùng.

| Mã việc | Nội dung | TT | Phụ thuộc | Tiêu chí XONG |
|---|---|---|---|---|
| TD-0250 | 🚪 **`DR-D5-01` — phạm vi, ứng viên, luật chọn của D5.** Tài liệu quyết định, **0 trial**, commit **RIÊNG và TRƯỚC** mọi dòng mã của khối. Ghi các chốt ở đoạn mở khối + ứng viên cho **mọi tham số calibrate** (lý do không lấy từ dữ liệu) + thứ tự thử cố định + điều kiện DỪNG + khai mở lại quyết định HOÃN 13/09 và `DR-D4-03` §5 + câu `MT-18` (b) | ✅ | — | File `docs/decisions/DR-D5-01-*.md` commit riêng; danh sách ứng viên có **băm** trong DR (TD-0253 đối chiếu). Luật INCONCLUSIVE viết **sau** TD-0251 (biết `n`) và **trước** khi tồn tại bất kỳ `mean_R` nào trên CALIB. Ngưỡng `DR-D4-12` §2.4 không đổi (`N = 114`, rào 3,0777, `\|tier_b\| = 12`) ✅ **Xong 16/09/2026** — `492b559` (DR) + `4188381` (§8 `MT-18` b), cả hai TRƯỚC mọi dòng mã của khối. 🔴 **Lệch tiêu chí trên, khai thẳng:** luật INCONCLUSIVE **không** chờ TD-0251 — chủ dự án chốt ngưỡng *"> 0 sau trừ nhiễu"* (`h = 3,0777`), là CÔNG THỨC không phụ thuộc `n` nên viết được ngay; con số `n` của TD-0251 chuyển thành **cổng đi/không đi** trước suất 1 (`DR-D5-01` §6.2) thay vì đầu vào để viết luật. **0 trial.** |
| TD-0251 | **Đếm cỡ mẫu `Z0-T1` trên CALIB [T0,T1] — CHỈ ĐẾM, không một chỉ số hiệu năng nào** (DR-014 §2) | 🔓 | TD-0250 (phần phạm vi), TD-0247, TD-0252 | Dòng `CTRL` theo đường đã chốt ở `MT-19`; `n_used()` không đổi; artifact `docs/du-lieu-do/td0251-*.json` chỉ chứa số lệnh (tổng, theo nhánh xác nhận `ac`/`b`) + xuất xứ. Kết quả là đầu vào cho luật INCONCLUSIVE của TD-0250 |
| TD-0252 | **Dữ liệu 5m cho CALIB [T0,T1].** Đo 16/09/2026: file 5m sớm nhất **2024-06-01**, `T0` = **2024-04-09** ⇒ thiếu ~2 tháng cho mọi mã, nhiều mã bắt đầu muộn hơn ⇒ `--timeframe-detail 5m` (H5, P0) không phủ CALIB | ✅ | TD-0200 ✅, TD-0247 (rổ tại `T0`) | Tải qua E8 với snapshot H19 (kiểm file đáp xuống đích — TD-0203); `assert_dataset_timerange()`/L-Z55 xác nhận nằm trọn khoảng (bẫy TD-0093: `download-data --timerange` không dừng đúng mốc cuối); bảng độ phủ theo mã ghi artifact ✅ **XONG 18/09/2026** — `DR-D1-05` §3b (`1092246`, commit TRƯỚC mọi dòng mã) chốt sáu điều; lõi `d1a1a7a`; E8 `4ca9a84`; cổng đối chiếu `7d93ae1`; vá + artifact `f9222d6`. **143/143 mã × 6 loại = 858 file**, 5m phủ `[T0,T1]` (**16.555.667 nến**), **0 giờ 1H nào thiếu nến 5m** (tiêu chí §3b.4). 18 mã ngừng giao dịch trong CALIB cắt đúng mốc đọc từ artifact TD-0301 (**không đo lại**). H19 PASS 715 file cũ còn nguyên; `--cat-den-t1` **0 file lấn** (đường kho không có bẫy TD-0093); `--ro-kiem --moc t0` PASS kèm L-Z55. Full suite Docker **2382 passed, 0 failed**. **0 trial.** 🔴 Nguồn là **kho `data.binance.vision` cho cả 143 mã**, không `download-data` — đường chép chết vì **65/66** file 5m sẵn có bắt đầu đúng tại `T1` (chép rồi cắt còn ĐÚNG MỘT nến, mà file một nến không rỗng nên `cat_den_moc()` không raise). 🔴 **Bẫy PASS RỖNG do chính đợt này tạo ra rồi tự bắt:** `--ro-do-phu` bản đầu nạp 0/143 mã (`load_data` tự nối `futures/` vào `datadir`) và in *"✅ không giờ nào thiếu, trên toàn bộ 0 mã"* + **exit 0** — chỉ lộ vì ĐỌC output, không nhìn mã thoát. Đã fail-closed trước mọi khẳng định + 2 ca khoá (một ca kiểm **chữ in ra**, vì lỗi không nằm ở mã thoát) |
| TD-0253 | 🔴 **Máy kế toán B1 — hiện CHỈ B3 có trần (`L-Z27`), B1 không có dòng nào.** Từ chối ghi (fail-closed) **tại cửa** `registry.reserve()`, trước khi ghi sổ (chốt 10): vượt trần B1 của `DR-D5-01` · quá số suất mỗi `(tham số, hướng)` · giá trị thử KHÔNG thuộc danh sách ứng viên đã băm · tham số ngoài 7 mục đã chốt | ✅ | TD-0250 | Phép kiểm mới cùng khuôn `check_lz27` + test khoá mới trong `tests/lock/`. Kiểm-có-răng phá THẬT: bỏ trần ⇒ đỏ; ứng viên ngoài danh sách ⇒ đỏ; tham số bất khả (`dg4_bars_1h`) hoặc `mult_corr_thresholds` ⇒ đỏ. Docker full suite xanh, số ca cộng đúng ✅ **Xong 16/09/2026** — `54da01f`. `calibration/ung_vien.py` đọc khối ứng viên + kiểm băm ngay trong `DR-D5-01`; `registry.reserve()` nhánh B1 gọi cửa TRƯỚC kiểm ngân sách và TRƯỚC `_append()`, lỗi ⇒ `B1Error`. 22 ca khoá, mỗi ca từ chối khẳng định CẢ raise LẪN sổ không thêm dòng. 9 file test đổi helper B1 → B3 (`lz27` đối chứng âm → **B2**, phiên `-30` rà; `lz54` trần hoàn trả khoá theo giả thuyết, không theo dòng); 5 chỗ ghi JSON thẳng vào sổ giữ nguyên. Kiểm-có-răng trên file sản xuất: tắt cửa ⇒ **14 ca đỏ**. Full suite **2063 passed, 0 failed**; E6 sổ thật exit 0, sổ vẫn 13 dòng. **0 trial.** |
| TD-0254 | **Tầng chọn giá trị — hàm THUẦN** `src/tool_d/calibration/chon_gia_tri.py` | ✅ | TD-0250, TD-0199 ✅ | **Dùng lại** `phan_loai_ket_cuc()` + `so_paired()`, không tính lại. Test đơn vị: thắng rõ ⇒ chọn · không phân biệt được ⇒ **giữ mốc** · thiếu mẫu ⇒ `pending` (N6, cấm `0.0`) · `v_min`: `0` thắng rõ ⇒ trả kết cục *"bỏ điều kiện (c)"* tường minh, không lặng lẽ ghi `0.0` vào YAML · xác nhận ghép thua/hoà mốc ⇒ giữ toàn bộ ✅ **Xong 16/09/2026** — `f070e6c`. `src/tool_d/calibration/chon_gia_tri.py` + 31 ca. Gọi `thue_nhieu()`/`dsr_hurdle()`, KHÔNG dùng `so_paired()` (nó đòi `ty_le_vuot ∈ (0,1)`, không biểu diễn được ngưỡng 0). 🔴 **DIỄN GIẢI, cãi lại được:** §5.1 *"áp thêm §5.2 cho phần chênh"* đọc thành áp cho TỪNG phía — ứng viên thua ở phần chênh khi lệnh nó THÊM lỗ rõ HOẶC lệnh nó BỎ lời rõ (tập không lồng nhau khi `buf_sl_atr` đổi cỡ lệnh). Kiểm-có-răng: bỏ trừ nhiễu ở `xet_mot_phia` ⇒ **chỉ 1 ca đỏ** — phủ phía *lỗ rõ* của phép một phía còn mỏng, ghi ra thay vì im. 🐛 Lượt full suite đầu **`L-Z48c` bắt chữ "R" trần trong docstring của chính module này** — đổi sang `R_realized`, không nới phép kiểm. Full suite Docker **2063 passed, 0 failed** (mốc 2000 + 63). **0 trial.** |
| TD-0255 | **Bộ chạy calibration trên E1 `run_backtest.py`** (hiện `NotImplementedError`, `:65`) — **không** thêm entrypoint thứ 9 (`L-Z36`) | 🔓 | **TD-0184** (lõi đặt chỗ→niêm phong→chạy→tiêu thụ), TD-0253 | Giá trị thử truyền bằng **file cấu hình phủ** sinh từ `config/tool_d_config.yaml` (N4), băm vào `params_effective`; **không** qua env (`L-Z39`), không qua cơ chế tham số Freqtrade (`L-Z37`). `measurement_guard()` dòng đầu sau parse (N5). Test: đổi giá trị trong file phủ ⇒ tập lệnh **thật sự đổi** (bài học `MT-23`: sổ ghi CẤU HÌNH, không ghi TẬP LỆNH) |
| TD-0256 | 🔴 **Tầng trạng thái tham số có bằng chứng — hai phần.** (i) **Siết `L-Z15`: `status: TUNED` phải trỏ tới `trial_id` CONSUMED thật ở B1** — hôm nay nó chỉ kiểm *có lời khai* (TD-0246). (ii) **`MT-18` phương án (b)** — chủ dự án chốt 16/09/2026 (`DR-D5-01` §8): `dof_inventory.yaml` mang **TÊN khoá** `tier_b` cho mọi dòng `dof_v6 = 1`, `L-Z29` so **TẬP HỢP tên** với `tool_d_config.yaml`, không chỉ số đếm | ✅ | TD-0253 | Test khoá (i): TUNED không `trial_id` ⇒ đỏ · `trial_id` không tồn tại / chưa CONSUMED / khác dòng B1 ⇒ đỏ · `\|tier_b\|` vẫn 12 sau chuyển trạng thái. (ii): tráo `dg7_funding_frac` lấy một khoá `tier_frozen` mà giữ `\|tier_b\| = 12` ⇒ **đỏ** (hôm nay xanh — ca `MT-18`). `N = 114`, rào 3,0777, `dof_goc = 28` **không đổi**. Đổi hình dạng `dof_inventory.yaml` ⇒ chạy RIÊNG từng file đang đọc nó (`test_lz29_*`, `test_td0169_*`, `test_td0189_*`, `test_td0191_*`, `test_td0195_*`). Ô `MT-18` ở `back-end-note.md` chờ **"chuẩn hóa và lưu"** ✅ **Xong 16/09/2026** — `8ae3bc3`. (i) `L-Z15`: TUNED đòi `trial_ids` → B1 CONSUMED đúng khoá + đúng giá trị đang chạy (hoặc `D5_XAC_NHAN` chứa khoá). (ii) `dof_inventory.yaml` +`khoa_tier_b` ở đúng 12 mục; `DofReport.tier_b_ok` đòi số đếm VÀ tập tên. `N = 114`, `dof_goc = 28` không đổi. 10 ca khoá. Kiểm-có-răng: bỏ so tập tên ⇒ ca tráo `dg7_funding_frac`↔`trend_age_days` (giữ 12) đỏ; bỏ kiểm `trial_ids` ⇒ 4 ca đỏ. Full suite **2063 passed, 0 failed**. ⏳ Ô `MT-18` ở `back-end-note.md` cập nhật trong lượt "chuẩn hóa và lưu" cùng đợt. **0 trial.** |
| TD-0257 | 🚪 **GATE D5** — `close_d5_gate()` trong E6 (`--close-d5-gate`), khuôn `close_d3_5_gate()` | 🔓 | TD-0250…TD-0256, TD-0258 | Đòi `d4_complete`; kiểm cây sạch TRƯỚC suite; chạy RIÊNG từng file test lõi, PASS ≥ 1; ghi `runtime_state.json.d5_complete` + `d5_han_che` nói rõ: **chỉ LONG** · bốn tham số không calibrate và vì sao · tham số nào INCONCLUSIVE giữ mốc. Chạy lại ⇒ exit từ chối ghi đè ⏸ **TẠM DỪNG 17/09/2026** theo `DR-IQ-01` §1 (cổng sống còn TD-0291 luật 4) — không làm tiếp cho tới khi thoả điều kiện nối lại bất đối xứng của DR đó. |
| TD-0258 | **Chạy B1 — tối đa 16 suất** theo thứ tự `DR-D5-01`: mốc chung → 14 thử → xác nhận ghép | 🔓 | **`d4_complete`** (`DR-D4-11`), TD-0247, TD-0251, TD-0252, TD-0254, TD-0255 | Mỗi trial đặt chỗ TRƯỚC khi chạm CALIB (L-Z52); kết quả qua TD-0254; cập nhật `tool_d_config.yaml` + `param_status.yaml` (FROZEN→TUNED kèm `trial_id`, tại chỗ) **chỉ sau lệnh "chuẩn hóa và lưu"** (N9). E6 sổ thật exit 0; sổ B1 ≤ 16 suất CONSUMED, khớp đúng số suất `DR-D5-01` §4 cho phép ⏸ **TẠM DỪNG 17/09/2026** theo `DR-IQ-01` §1 (cổng sống còn TD-0291 luật 4) — không làm tiếp cho tới khi thoả điều kiện nối lại bất đối xứng của DR đó. |

---

## Khối 20 — D6: Niêm phong ứng viên & xác minh đúng hệ thống cuối (mở 17/09/2026, đặc tả NHÁP)

> ⏸ **TẠM DỪNG 17/09/2026 — cả ba Khối 20, 21, 22 và TD-0261**, theo `DR-IQ-01` §1 (cổng sống còn
> TD-0291 luật 4). Chủ dự án chốt ở phiên `-33`, **xác nhận lại ở phiên `-01`** trước khi ghi dòng này.
> Trạng thái từng dòng giữ nguyên, không xoá việc nào; `DR-D6D8-01` giữ hiệu lực làm đặc tả khi nối lại.
> Nối lại theo điều kiện của `DR-IQ-01`. **TD-0247 KHÔNG dừng** (rổ pool dùng chung mọi chiến lược).

> 🔴 **Vì sao ba khối 20–22 tồn tại, và chúng MỞ LẠI một quyết định.** Ngày 13/09/2026 chủ dự án chốt
> *"dựng testnet trước, HOÃN đặc tả D5–D9"* (đoạn mở Khối 18). `DR-D5-01` đã mở lại phần D5. Ngày
> 17/09/2026 chủ dự án yêu cầu phân tích và đề xuất D6–D8 rồi duyệt kế hoạch. Nguồn sự thật:
> **`docs/decisions/DR-D6D8-01-dac-ta-pha-d6-d8.md`**. Bản đó **còn là NHÁP**: các dòng dưới đây trỏ
> § sang DR, không chép nội dung.
>
> 🔴 **Spec KHÔNG có đặc tả D6–D8.** `tool-d-smart-dca.md:4492` chỉ ghi `D5–D9 (giữ nguyên)`; bản tài
> liệu gốc chưa bao giờ nằm trong git. Neo còn lại: `spec:279` *"B7 chặn D7"* · `:2932` *"D9 (Walk-forward
> + GATE)"* · `:4526-4530` (② → ③) · §11 (**H2, H5, H9/H10, H14 chưa gán pha nào**).
>
> ⚠️ **Bẫy ký hiệu:** D6/D7/D8 ở ba khối này là **PHA**. Spec §9b.2 dùng cùng ký hiệu cho **GIẢ ĐỊNH**
> (giả định D6 = giá mở nến, giả định D7 = `custom_data`). Không liên quan.
>
> **Hai chốt của chủ dự án 17/09/2026:** (1) **Long-only tới live**; Short thành chu trình riêng với
> lockbox MỚI. Giá phải trả: lần chạm lockbox `[T2,T3]` tiêu luôn cho Short (`DR-011` `spec:3335-3337`).
> (2) **D7 dùng kịch bản tổng hợp + dòng CTRL** theo đường (c) của `MT-19` (dạng *đo mô tả*), **không**
> nới `CTRL_OUTPUT_ALLOWED`.
>
> 🔑 **Nguyên tắc chung (`DR-D6D8-01` §2):** **0 trial** · không chạm WFO `[T1,T2]` (thuộc D9, phiên
> `-33`) · không chạm LOCKBOX · không thêm biến thể cấu hình (ablation DG8, Short, FreqAI đều ngoài) ·
> lỗi code sửa theo DR-012 Hạng 1, **giá trị tham số không đổi** · mỗi pha một cổng máy trên E6.
> Đã nhắn `-33`/`-30` dãy mã trước khi mở (N12 mục 6); D9 dùng Khối 23 / `TD-0280…`.

| Mã việc | Nội dung | TT | Phụ thuộc | Tiêu chí XONG |
|---|---|---|---|---|
| TD-0260 | 🚪 **`DR-D6D8-01` — chốt bản đặc tả D6–D8.** Bản nháp đã viết 17/09/2026; còn 5 ô `__CHUA_DIEN__` (§8: biên độ kịch bản stress · danh sách tên CTRL *đo mô tả* · `MT-40` đỉnh equity reset khi nào · D9 đợi D6–D8 hay chạy song song · ngưỡng DR-007 `>`/`≥` 50%) | ✅ | — | Không còn `__CHUA_DIEN__`; bỏ nhãn NHÁP; commit **RIÊNG và TRƯỚC** mọi dòng mã Khối 20–22 (kiểm bằng `git merge-base --is-ancestor`, không bằng mắt). Nhắn phiên `-33` để đính chính điều kiện vào D9 trong `DR-D9-01` §6.1 |
| TD-0261 | ⏸ **TẠM DỪNG 17/09/2026** theo `DR-IQ-01` §1 (chủ dự án xác nhận ở phiên `-01`: dừng cùng Khối 20–22). **H14 + DR-007 — đo overlap pool, chốt N của D9 TRƯỚC D9** (`DR-D6D8-01` D6.1). Hiện **chưa từng đo**. 🔄 **Đính chính 17/09/2026:** N này là đầu vào của **cổng D4** (`spec:4267-4268`), không chỉ D9 ⇒ làm **ngay sau TD-0247, TRƯỚC suất B2 đầu tiên** (`TD-0184` phụ thuộc việc này); **không** đòi `d5_complete`. Phiên `-01` giữ (mã phiên `0ea66758`; ghi chú 18/09/2026 theo N12 mục 7 — tên phiên đổi sau khởi động lại, nối lại thì hỏi theo mã việc) | 🔓 | TD-0260 ✅ (ngưỡng `MT-50` ≥ 50%), TD-0247 | Overlap rổ Tool D (sau TD-0247) với pool Tool A, kèm xuất xứ, trong `docs/du-lieu-do/`; áp DR-007 **máy móc**; N (cho cổng D4 và D9) ghi research-log; **không** đổi tiêu chí pool; 0 trial. ➕ **(17/09/2026, chủ dự án chốt) Chụp số trial Tool A TẠI LÚC ĐO overlap**, TRƯỚC suất B2 đầu tiên. N chốt một lần nên cổng D4 tái lập được. Lệch chữ spec `:4268` (*"tại thời điểm chạy GATE"*) ⇒ ghi bằng DR riêng commit TRƯỚC phép đo, kèm mục MT (phiên `-30` nêu). ➕ Nếu overlap **≥ 50%**, liệt kê đích danh các DR mang rào `3,0777` là **phải đọc lại con số** (đo 17/09: 11 DR đã commit `DR-D4-02/03/07/08/09/10/11/12/13` · `DR-D5-01` · `DR-D9-01` + nháp `DR-FAI-01`). Không được coi *"quyết định không đổi nên DR vẫn đúng"* |
| TD-0262 | **Kiểm kê DOF sau D4/D5 — CHỈ GHI** (D6.2): DG5 chết khi bỏ DCA, DG1–DG5 không được xét trên `Z0-T1` (`MT-44`) | 🔓 | `d5_complete` | Mục research-log liệt kê từng bậc tự do đã chết kèm bằng chứng code; **N của D9 không đổi** (`spec:4314-4316`) |
| TD-0263 | **`MT-30` có chạm `Z0-T1` không — ĐO, không suy** (D6.3) | 🔓 | `d5_complete` | EXPLORE, 0 trial: đếm lệnh `Z0-T1` mà danh sách zone đối diện ở lúc đóng băng ≠ lúc xét TP1. Bằng 0 ⇒ ghi `MT-30` N/A cho arm sản xuất (vẫn là nợ của DCA); > 0 ⇒ sửa Hạng 1 + test đỏ khi bỏ bản sửa |
| TD-0264 | **H5 — `timeframe-detail 5m` trên cấu hình cuối** (D6.4) | 🔓 | TD-0252, `d5_complete` | EXPLORE, 0 trial: so tập lệnh có/không detail; mỗi lệnh lệch được soi nguyên nhân; kết luận *swing 4H có lệch do intrabar không* kèm artifact |
| TD-0265 | **H2 — cổng danh mục có ràng buộc thật không** (D6.5): §6.8f · `mult_corr` · `mult_deploy` | 🔓 | TD-0247, `d5_complete` | EXPLORE, 0 trial: số lần mỗi cổng ràng buộc, phân bố số vị thế đồng thời, tỉ lệ lệnh bị từ chối vì danh mục; **kiểm có răng**: phá cổng ⇒ số lệnh đổi |
| TD-0266 | **Niêm phong cấu hình ứng viên `d6_ung_vien`** (D6.6) | 🔓 | `d5_complete`, TD-0263, TD-0265 | `config_hash` + `params_frozen_hash` + git sha của `Z0-T1` LONG giá trị D5 trong `runtime_state.json`; từ chối ghi đè (khuôn `lockbox/seal.py`); test khoá |
| TD-0267 | 🚪 **GATE D6** — `close_d6_gate()` trong E6 (`--close-d6-gate`), khuôn `close_d3_5_gate()` | 🔓 | TD-0260…TD-0266 | Đòi `d5_complete`; kiểm cây sạch TRƯỚC suite; chạy RIÊNG từng file test lõi, PASS ≥ 1; ghi `d6_complete` + tag `d6-complete`; chạy lại ⇒ từ chối |

## Khối 21 — D7: Hardening tồn vong — "B7 chặn D7" (mở 17/09/2026, đặc tả NHÁP)

> Mục tiêu (`DR-D6D8-01` §4): chứng minh mọi cầu dao Cấp C (§12c.3) **thật sự nổ** ở `L_exchange = 3`
> trên `d6_ung_vien`. D7 **không** hỏi *"lời bao nhiêu"*. Kịch bản làm vỡ tiêu chí là **phát hiện**, xử
> theo §11b.1; **không** tinh chỉnh tham số trong D7. Mọi phép kiểm phải chạy qua **đường sản xuất**,
> không qua mẫu dựng tay.

| Mã việc | Nội dung | TT | Phụ thuộc | Tiêu chí XONG |
|---|---|---|---|---|
| TD-0268 | **Danh sách tên đầu ra CTRL *đo mô tả* cho D7** (§4.3) — mỗi tên kèm một câu vì-sao-không-phải-chỉ-số-hiệu-năng; chủ dự án quyết lý do thoát lệnh / tỉ lệ lỗ-ngân sách có được tính là "mô tả" không | 🔓 | TD-0260, code dạng CTRL thứ ba của `MT-19` (hiện 0 dòng; TD-0251 cần trước) | Danh sách đóng trong DR; allowlist trong code liệt kê đích danh; test: ghi tên ngoài danh sách ⇒ cửa ghi từ chối; `n_used()` không đổi |
| TD-0269 | **H9/H10 — stress thanh lý bằng kịch bản tổng hợp** (§4.1) theo dải `R_eff` **đo lại** trên `d6_ung_vien` (không dùng dải thiết kế 0,9–3,0%) | 🔓 | `d6_complete`, TD-0260 (biên độ kịch bản) | Mọi kịch bản báo `liq_buffer_ratio` và `max_single_trade_loss / risk_budget` so với 8 và 1.15; artifact có xuất xứ §0d.5; 0 trial, không chạm dữ liệu |
| TD-0270 | **Thang drawdown 5/8/20% chạy lại trên equity tổng hợp** (§4.2) qua `_dd_pct()` + `equity_peak.py` + Risk Supervisor: HALT · khởi động lại · nửa size · trần 3 HALT/100 lệnh · ABORT | 🔓 | `d6_complete`, TD-0260 (`MT-40`) | Mỗi bậc có test qua đường sản xuất, đỏ khi vô hiệu hoá; `MT-40` có kết luận (reset đỉnh khi nào, xếp Cấp C) |
| TD-0271 | **DG6/DG7/DG8 trên CALIB — dòng CTRL *đo mô tả*** (§4.3) + ghi `BAT_KHA` DG1–DG5 cho `Z0-T1` (`MT-44`) + sửa lời khai `dg7_funding_frac` (`MT-46` (1), không sửa giá trị) | 🔓 | TD-0268, `d6_complete` | Dòng CTRL trong sổ, đầu ra đúng danh sách TD-0268; tỉ lệ kích hoạt từng cổng; `n_used()` không đổi; `param_status.yaml` hết lời khai "chỉ áp cho SHORT" |
| TD-0272 | **Sự cố vận hành — kiểm có răng** (§4.4): restart giữa vị thế (TD-0237/0238) · 418/429 (TD-0241) · thiếu key (TD-0242) · LIQUIDATED dừng toàn hệ thống | 🔓 | `d6_complete` | Mỗi ca một test **phá-thật-chạy-lại** đỏ; ca nào chưa có thì viết |
| TD-0273 | 🚪 **GATE D7** — `close_d7_gate()` trong E6 (`--close-d7-gate`) | 🔓 | TD-0267, TD-0268…TD-0272 | Đòi `d6_complete`; khuôn TD-0267; ghi `d7_complete` + `d7_han_che` (chỉ LONG · kịch bản tổng hợp không phải lịch sử · cầu dao nào chỉ kiểm trên mẫu) + tag `d7-complete` |

## Khối 22 — D8: Tiền đăng ký lockbox (D9.5) & dọn nợ trước go-live (mở 17/09/2026, đặc tả NHÁP)

> Chỉ giấy tờ, **không chạm dữ liệu** (`DR-D6D8-01` §5). **Soạn được ngay, song song D5–D7**, nhưng
> ngưỡng lockbox phải commit **trước khi thấy số WFO**. 🔴 **Ngoài phạm vi, thuộc `DR-D9-01` (phiên
> `-33`):** fold WFO, PBO/CSCV, tập cấu hình CSCV, sàn lệnh/năm và thuế nhiễu của D9. Không lặp lại ở
> đây: một ý, một file (N12 mục 6).

| Mã việc | Nội dung | TT | Phụ thuộc | Tiêu chí XONG |
|---|---|---|---|---|
| TD-0274 | 🚪 **Quy tắc lockbox — điền ô `......` của DR-011** (`spec:3328-3343`) **trừ ô "cấu hình"** (điền sau D9 theo luật). Kèm câu *"Short không được đánh giá; lần chạm này tiêu lockbox cho Short"* | 🔓 | TD-0260 | DR riêng commit **trước suất WFO đầu tiên của D9** (kiểm bằng thứ tự commit); ngưỡng LONG bằng SỐ; không ô trống |
| TD-0275 | **Điều kiện chất lượng lockbox (c) — ≥ 30 lệnh dự kiến** trên rổ pool mới | 🔓 | TD-0247, TD-0251 | Ước lượng = tốc độ lệnh đếm trên CALIB (TD-0251, CTRL chỉ đếm) × độ dài `[T2,T3]`; **không** đếm trên lockbox; dưới 30 ⇒ trình chủ dự án trước D9 |
| TD-0276 | **Hệ quả Long-only cho Short** — bổ sung **một đoạn** vào `DR-D4-01` §2b: Short cần lockbox MỚI | 🔓 | TD-0260 | Đoạn bổ sung có ngày, giữ nguyên chữ cũ; **không** tạo DR Short thứ hai |
| TD-0277 | **Đóng nợ trước go-live**: `MT-26` câu 2 · `MT-27` · `MT-28` · `MT-33` (🔴) · `MT-46`; rà `OQ-10`/`OQ-12`/`OQ-13` | 🔓 | — | Mỗi MT có quyết định chủ dự án, ghi qua "chuẩn hóa và lưu" (N9); hoặc quyết định *"mang theo có ý thức"* có lý do |
| TD-0278 | **Ghi `MT-49` + `MT-50`** (`DR-D6D8-01` §9): `DR-D0PRE-07:85-87` *"chạm thật là D9"* vs spec **D9.5**; ngưỡng DR-007 `spec:2821` *"> 50%"* vs `:2854`/`:4267` *"≥ 50%"* | ✅ | — | Hai mục trong `back-end-note.md` mục 7 sau lệnh "chuẩn hóa và lưu"; `MT-50` phải chốt **trước** TD-0261 |
| TD-0279 | 🚪 **GATE D8** — `close_d8_gate()` trong E6 (`--close-d8-gate`) | 🔓 | TD-0274…TD-0278 | Không cần `d7_complete` (giấy tờ song song); kiểm: file DR lockbox tồn tại + không còn `__CHUA_DIEN__` + các MT ở TD-0277/0278 có trạng thái; ghi `d8_complete` + tag `d8-complete` |

---

## Khối 23 — D9: Walk-forward đủ phân hoạch + PBO/CSCV nâng lên P0 (mở 17/09/2026 theo yêu cầu chủ dự án)

> 🔴 **Spec KHÔNG có đặc tả D9.** Căn cứ duy nhất: `spec:4287-4288` (PBO ≤ 0,5 qua CSCV), `:4326` *"nâng lên P0
> ở GATE TOOL D (D9), nơi WFO đã sinh đủ cặp IS/OOS để CSCV có power"*, `:4349` (H18), `:4526-4531` (② → ③).
> Mở lại phần D9 của quyết định *"HOÃN đặc tả D5–D9"* 13/09 (`DR-D5-01` §0, `MT-48`). Nguồn sự thật:
> **`DR-D9-01`** (TD-0280). D6–D8 thuộc `DR-D6D8-01` (phiên `-01`, mã phiên `0ea66758` — ghi chú 18/09/2026, N12 mục 7); fold/PBO/CSCV **chỉ** ở `DR-D9-01`.
>
> ✅ **Chủ dự án đã chốt 17/09/2026** (phiên `-33`, qua câu hỏi khi lập kế hoạch):
> 1. **Làm phần 0 trial ngay**; lượt chạy WFO thật chờ cổng vào (`d5_complete`, cộng cổng D6–D8 khi
>    `DR-D6D8-01` chốt — đính chính `DR-D9-01` §6.1 tại chỗ, không ghi trước khoá chưa tồn tại).
> 2. **Tập CSCV = các biến thể D5** đã CONSUMED trên CALIB (mốc · giá trị thử · xác nhận ghép; ≤ 16), mỗi cái
>    chạy lại **một lần** trên WFO. Không dùng arm D4 (`Z0` ≡ `Z3`; lựa chọn cuối trước D9 là của D5).
> 3. **Ngân sách = phần B1 còn dư**, chuyển giao **tường minh** ≤ 16 suất (tiền lệ `DR-D4-01:113` *"không tự
>    động"*). `N` đăng ký không tăng; rào DSR của D9 **không ghim 114** (DR-007 union có thể đổi `N`).
>    Cửa `ung_vien.py:149` (TD-0253) **không nới** — nhánh WFO **thêm vào, chặt hơn**.
> 4. **CSCV tách khỏi fold, S = 8** khối 693 giờ trên `[T1,T2)`, C(8,4) = 70 tổ hợp. 3 fold `DR-D3-01` giữ
>    nguyên cho phán quyết Nhánh 1 (test-only, `MT-36`). `DR-D3-01` **không** mở lại.
> 5. **Một backtest toàn cửa sổ mỗi cấu hình, rồi cắt lát** theo `open_date` cho cả 3 đoạn test lẫn 8 khối —
>    một cấu hình, một tập lệnh.
>
> ⏳ **Chờ chủ dự án trước khi commit `DR-D9-01`:** (i) **nhãn sổ** cho suất WFO — (a) `B1` + `dataset`
> (nghiêng) hay (b) `B1W`, bảng rủi ro ở `DR-D9-01` §3.1; (ii) đơn vị xếp hạng CSCV = `R_trien_khai` (khớp
> `DR-D4-12` §1) thay vì `R_realized` như kế hoạch ghi; (iii) cổng vào tạm chỉ `d5_complete`.
>
> ⚠️ Quy tắc gốc 1: TD-0280 là giấy tờ; **TD-0281…TD-0288 chỉ bắt đầu khi chủ dự án gõ "bắt đầu code"**.
> Dãy mã `DR-D9-01` + `TD-0280…TD-0288` + `MT-51…MT-53` đã nhắn `-30`/`-01` trước khi ghi (N12 mục 6); va
> chạm dãy `TD-0260…` với `-01` giải bằng cách `-33` dời mã.

| Mã việc | Nội dung | TT | Phụ thuộc | Tiêu chí XONG |
|---|---|---|---|---|
| TD-0280 | 🚪 **`DR-D9-01` — WFO đủ phân hoạch + PBO/CSCV P0.** Tài liệu quyết định, **0 trial**, commit **RIÊNG và TRƯỚC** mọi dòng mã của khối. Chốt 5 điểm ở đoạn mở khối + khối băm cấu hình CSCV + sàn mẫu 70/70 + tiêu chí cổng D9 + hạn chế khai trước + khai mở lại quyết định HOÃN 13/09 và `OQ-11` | ✅ | — | File `docs/decisions/DR-D9-01-*.md` commit riêng, **sau** khi chủ dự án trả lời ba câu ⏳; khối băm §4.5 kiểm lại bằng `bam_chuan_hoa`; `MT-51…MT-53` soạn sẵn cho lệnh "chuẩn hóa và lưu" ✅ **Xong 17/09/2026** — `95f3fe1`. Ba câu ⏳ chủ dự án chốt trước commit: nhãn sổ **(a)** `B1` + `dataset` · xếp hạng **`R_trien_khai`** · cổng vào tạm **`d5_complete`**. Băm khối §4.5 kiểm lại khớp (`7f8c5ff0…`). **0 trial.** |
| TD-0281 | **Khoá `tier_c.cscv` trong `config/tool_d_config.yaml`** (N4), FROZEN, ngoài `tier_b` | ✅ | TD-0280 | Đọc qua `loader.py`; S lẻ/≤ 0 ⇒ lỗi; khớp khối băm `DR-D9-01` §4.5; `N = 114`, `\|tier_b\| = 12` không đổi; chạy RIÊNG `test_lz29_*` ✅ **Xong 17/09/2026** — `557153b`. `tier_c.cscv.so_khoi: 8` + `src/tool_d/gates/cscv_cau_hinh.py` (đối chiếu khối băm `DR-D9-01` §4.5: `so_khoi`, 693 giờ × 8 = cửa sổ WFO, nguồn sàn = `wfo_folds.san_lenh_moi_fold`, `PBO_MAX`). **Không thêm dòng `dof_inventory.yaml`** — tiền lệ `wfo_folds` (khoá phương pháp đo, không có dòng DOF). 20 ca khoá. Kiểm-có-răng: tắt so YAML/DR ⇒ **1 đỏ**; YAML thật `so_khoi: 16` ⇒ **2 đỏ**. Chạy RIÊNG `test_lz29_*`/`test_lz39_*`/`test_lz42_*`/`test_wfo_folds` xanh. Full suite **2083 = 2063 + 20**; `N = 114`, rào 3,0777. 🐛 Test tự sai lần đầu: `len(tier_b) == 12` quên khoá ghi chú `_budget_remaining_B3` — sửa giả định test, không sửa máy. **0 trial.** |
| TD-0282 | **`src/tool_d/gates/cscv.py` — `tinh_pbo()` hàm THUẦN** | ✅ | TD-0280, TD-0281 | Test đáp án tính tay: PBO = 1 (IS-best luôn đội sổ) · PBO = 0 (thứ hạng bền) · ca S = 4 tính từng λ · hoà đỉnh IS ⇒ trung bình λ · dưới sàn ⇒ `unreadable` (không `0.0`) · lệnh ngoài `[T1,T2)` ⇒ raise · gộp cấu hình trùng. Kiểm-có-răng: đảo dấu λ ⇒ đỏ; bỏ sàn ⇒ đỏ. Qua `L-Z46`/`L-Z48c` ✅ **Xong 17/09/2026** — `676c5ca`. `src/tool_d/gates/cscv.py::tinh_pbo(lenh_theo_cau_hinh, ch)`: gán khối theo `open_date`, gộp cấu hình trùng khít, C(S,S/2) tổ hợp, hạng phân số, hoà đỉnh IS ⇒ trung bình λ, sàn mỗi nửa; PBO là SỐ chỉ khi mọi tổ hợp đọc được. 18 ca, đáp án TÍNH TAY (PBO = 1 · PBO = 0 · ca S = 4 ba cấu hình λ = ln(1/3)/ln(5/3) · hoà đỉnh ⇒ λ = 0). Kiểm-có-răng: đảo dấu λ ⇒ **5 đỏ**; bỏ sàn ⇒ **2 đỏ**. 🐛 Viết file giữa lượt suite làm `test_td0245` đỏ (bộ sinh từ điển thấy chỗ đọc `open_date` mới) — sinh lại theo **N13** (`1eff406` → `30b824f`, chỉ ô "Dùng bởi"). Full suite `10c3716`: **2171 passed, 0 failed**. **0 trial.** |
| TD-0283 | **`src/tool_d/wfo/lenh.py` — bản ghi từng lệnh + cắt lát** theo fold và theo khối; bộ chuyển `chay_mot_fold` cắt lát trên một lượt chạy *(🔴 đính chính 18/09/2026, MT-61: chữ này viết TRƯỚC `DR-D9-01` §5.1 — D9 cắt lát bằng `wfo/lenh.py`, KHÔNG qua `chay_mot_fold`/`chay_wfo`)* | ✅ | TD-0280 | **Dùng lại** đường trích lệnh/`R_trien_khai` đang có (TD-0232), không viết đường thứ hai; `chay_wfo` giữ nguyên hành vi sàn fold (`MT-36`); schema bản ghi (nếu lưu) đối chiếu **đầu ra thật** (TD-0149/0150) ✅ **Xong 17/09/2026** — `e0d583f` + `10c3716`. `src/tool_d/wfo/lenh.py`: `LenhWFO` (hai thước `r_trien_khai`/`r_realized`), `rui_ro_da_trien_khai_usdt()` (công thức `DR-D4-12` §1.4, thuần), `cat_lat_theo_fold`/`cat_lat_theo_khoi` theo `open_date`, nửa mở. 🔴 **Lệch tiêu chí, khai thẳng:** (1) *"dùng lại đường trích lệnh/R"* — kiểm ngày 17/09: **không tồn tại** trong `src/` (chỉ số tổng hợp ở `arm_record.py`); đường đọc fill thật thuộc lõi bộ chạy (TD-0184/TD-0286), ở đây chỉ công thức. (2) **Bộ chuyển `chay_mot_fold` KHÔNG làm** — va TD-0148 tầng (b); chủ dự án chốt D9 **không đi qua `chay_wfo`** (`DR-D9-01` §5.1, `83c59b5`). 22 ca. Kiểm-có-răng: biên fold đóng ⇒ **1 đỏ**; bỏ chia `entry` ⇒ **2 đỏ**. Full suite `10c3716`: **2171 passed, 0 failed**. **0 trial.** |
| TD-0284 | 🔴 **Cửa B1 `dataset = WFO` tại `reserve()`** — chuyển giao ≤ 16 suất B1 dư | ✅ | TD-0280 (**cả nhãn sổ §3.1**), TD-0253 ✅ | Nhánh CALIB + mọi ca `test_td0253_*` giữ nguyên. Từ chối: thiếu cổng vào · cấu hình không khớp suất CALIB CONSUMED · trùng · vượt trần = số suất CALIB CONSUMED. Kiểm-có-răng hai chiều theo phương án được chọn (`DR-D9-01` §3.1); nếu (b): sửa cả ba phép so nhãn **trước** khi mở enum ✅ **Xong 17/09/2026** — `79e9a44`. Nhánh `dataset = WFO` trong `kiem_dat_cho_b1` (thêm kw bắt buộc `d5_complete`); `TrialProjection.dataset` (đọc `e["dataset"]`, không `.get` — sổ thật 5/5 RESERVE có trường, phiên `-30` kiểm); trần 16 D5 chỉ đếm CALIB; `L-Z15` chỉ nhận bằng chứng B1/**CALIB**/CONSUMED. 17 ca khoá; 149 ca ledger/B1 (gồm mọi `test_td0253_*`/`test_td0256_*`) xanh, không đổi khẳng định. Kiểm-có-răng: trần đếm gộp ⇒ 1 đỏ · L-Z15 nhận WFO ⇒ 1 đỏ · bỏ trần WFO ⇒ 1 đỏ · bỏ cổng `d5_complete` ⇒ 2 đỏ. Full suite `79e9a44`: **2170 passed, 1 failed** — ca đỏ DUY NHẤT theo tên `test_td0245::test_khop_tung_ky_tu_voi_ban_sinh_lai` (từ điển chờ sinh lại, của TD-0282/0283, không phải việc này). E6 sổ thật exit 0, sổ 13 dòng. **0 trial.** ➕ **Bổ sung 17/09/2026** — `946f6e6` (DR trước: `ff4772c`): `DR-D6D8-01` chốt ĐỢI D6–D8 ⇒ cửa B1/WFO đòi ĐỦ `KHOA_CONG_VAO_D9` = `d5`+`d6`+`d7`+`d8_complete` (kiểm `d6` tường minh). Kiểm-có-răng: trả về chỉ `d5` ⇒ **13 đỏ**. Full suite `946f6e6`: 2186 passed + 1 đỏ **nhiễu môi trường** (`test_td0143` — `git status` quá hạn 10 s khi máy tải nặng; chạy riêng 34/34 xanh hai lần). |
| TD-0285 | **PBO: ghi ở D4, chặn ở D9** (`MT-51`) — `evaluate_branch1(..., pbo_chan)` bắt buộc khai + `src/tool_d/gates/d9_gate.py` | ✅ | TD-0280, TD-0282 | `test_lz35_*`: ca cũ giữ **nguyên** khẳng định 7 tiêu chí dưới `pbo_chan=True`; thêm ca `False` ⇒ 6. `danh_gia_cong_d9()` đọc `N` từ nguồn kế toán, không hằng 114. Kiểm-có-răng: bỏ `pbo_chan` ⇒ đỏ ✅ **Xong 17/09/2026** — `9c42b3c`. `evaluate_branch1(metrics, *, pbo_chan)` bắt buộc khai, không mặc định; `test_lz35`: 6 lời gọi cũ thêm `pbo_chan=True`, **giữ nguyên** khẳng định 7 tiêu chí, +7 ca (`False` ⇒ 6 = 7 trừ đúng `pbo`). `src/tool_d/gates/d9_gate.py::danh_gia_cong_d9`: FAIL (đo được mà không đạt) trước INCONCLUSIVE (chưa đo / thuế nhiễu > ngưỡng) trước PASS — DIỄN GIẢI từ DR-011 + `DR-D4-09`, không thêm con số; `n_trials` bắt buộc; `skewness_diff_vs_z1` `pending` ⇒ không PASS (`MT-53`). 24 ca khoá. Kiểm-có-răng: bỏ `pbo_chan` ⇒ 2 đỏ; đảo thứ tự kết cục ⇒ 1 đỏ. Full suite `79e9a44`: 2170 passed + 1 đỏ đã biết (`test_td0245`, không thuộc việc này). **0 trial.** |
| TD-0286 | **Bộ chạy D9 trên E2 `run_wfo.py`** — không thêm entrypoint (`L-Z36`) | 🔓 | TD-0283, TD-0284, TD-0285, **TD-0184**, **TD-0255** | Guard dòng đầu (N5); thiếu cổng vào ⇒ exit từ chối **trước** khi đọc dữ liệu, sổ 0 dòng mới; `reserve()` trước dữ liệu (`L-Z52`); đổi file phủ ⇒ tập lệnh thật đổi (`MT-23`); không mock đường sản xuất (`L-Z51`) ⏸ **TẠM DỪNG 17/09/2026** theo `DR-IQ-01` §1 (cổng sống còn TD-0291 luật 4) — không làm tiếp cho tới khi thoả điều kiện nối lại bất đối xứng của DR đó. |
| TD-0287 | 🚪 **GATE D9** — `close_d9_gate()` trong E6 (`--close-d9-gate`), khuôn `close_d3_5_gate()` | 🔓 | TD-0282…TD-0285 (dựng) · TD-0288 (đóng) | Đòi cổng vào; cây sạch TRƯỚC suite; chạy RIÊNG từng file lõi, PASS ≥ 1; gọi **cùng** `danh_gia_cong_d9()` với E2; ghi `d9_complete` + `d9_ket_cuc` + `d9_han_che` (`DR-D9-01` §9); chạy lại ⇒ exit 94. Thử khi chưa có cổng vào ⇒ từ chối, `runtime_state.json` không đổi ⏸ **TẠM DỪNG 17/09/2026** theo `DR-IQ-01` §1 (cổng sống còn TD-0291 luật 4) — không làm tiếp cho tới khi thoả điều kiện nối lại bất đối xứng của DR đó. |
| TD-0288 | **Chạy D9 thật** | 🔓 | cổng vào `DR-D9-01` §6.1, TD-0286, TD-0287, TD-0274 (ngưỡng lockbox commit **trước** suất WFO đầu tiên) | Số suất WFO CONSUMED = số cấu hình CSCV; E6 sổ thật exit 0; kết quả vào YAML/`param_status.yaml` chỉ sau "chuẩn hóa và lưu" (N9) ⏸ **TẠM DỪNG 17/09/2026** theo `DR-IQ-01` §1 (cổng sống còn TD-0291 luật 4) — không làm tiếp cho tới khi thoả điều kiện nối lại bất đối xứng của DR đó. |

---

## Khối 24 — Cổng sống còn + gỡ khoá cấu trúc (mở 17/09/2026 theo đánh giá tổng hợp chủ dự án duyệt)

> 🔴 **Vì sao khối này tồn tại.** Đánh giá tổng hợp 17/09/2026 (phiên `-33`, chủ dự án duyệt, mục tiêu *"chắc chắn
> thống kê trên hết"*): sau 11 ngày **chưa có một con số lời/lỗ nào** của chiến lược (`MT-27`), trong khi đường ống
> sẽ tiêu ~41 suất đo, và cổng Nhánh 1 **không thể PASS trọn** vì `MT-53`. Khối này làm ba thứ **trước** khi tiêu
> suất: (1) cổng sống còn 0 trial trên EXPLORE với luật dừng viết trước; (2) gỡ khoá `MT-53`; (3) hai việc hạ tầng
> nhỏ đã lộ ra khi làm Khối 23.
>
> ✅ **Chủ dự án đã chốt 17/09/2026:**
> 1. Mục tiêu 1–2 tháng: **chắc chắn thống kê** — giữ D4→D12 và rào DSR; không live trước D9.5.
> 2. Phép đo sớm 0 trial trên EXPLORE **được làm**, kèm luật dừng viết trước; **lỗ rõ sau phí ⇒ DỪNG Zone
>    Absorption LONG**, chuyển ý tưởng, giữ hạ tầng.
> 3. `MT-53`: phương án **(b′)** — tiêu chí skewness **không áp dụng có căn cứ** cho cấu hình entry đơn, **tự khôi
>    phục** cho cấu hình DCA (`DR-D9-02`).
> 4. **Đóng băng tạm** Khối 17 FreqAI (không huỷ quyết định 12/09); D6–D8 **không** đóng băng (đường găng D9);
>    TD-0261 kéo lên trước suất B2 đầu tiên (`0fdea20`, phiên `-01`).
> 5. Test khoá CRLF và đo độ trễ `git status` dưới tải — **được làm**.
>
> ⚠️ Quy tắc gốc 1: TD-0289 (phần mã) · TD-0291…TD-0293 chỉ bắt đầu khi chủ dự án gõ **"bắt đầu code"**.
> Mã `DR-D9-02` · `DR-SONG-CON-01` · `TD-0289…TD-0293` · Khối 24 đã nhắn `-30`/`-01`, cả hai xác nhận không trùng.

| Mã việc | Nội dung | TT | Phụ thuộc | Tiêu chí XONG |
|---|---|---|---|---|
| TD-0289 | 🚪 **`DR-D9-02` + thi hành — `MT-53` phương án (b′).** Tiêu chí *"skewness không âm hơn skewness(Z1) quá 0,5"* không áp dụng có căn cứ khi cấu hình ∈ `arm_switches.ARM_DON_TRANCHE`; áp dụng như cũ cho arm DCA (mốc `Z1` phải n ≥ `san_lenh_moi_fold`) | 🔓 | — | DR commit RIÊNG và TRƯỚC mã, khi **chưa có** số skewness nào. Mã: `evaluate_branch1(..., arm)` + `danh_gia_cong_d9(..., arm)` bắt buộc khai; tiêu chí "không áp dụng" **liệt kê tường minh**, không xoá khoá; `test_lz35` ca cũ giữ nguyên khẳng định dưới arm DCA. Kiểm-có-răng hai chiều. Full suite xanh. `MT-53` ✅ qua "chuẩn hóa và lưu" ⏸ **Trả 🔓 17/09/2026** — tạm dừng theo `DR-IQ-01` §1 (đường ống Zone Absorption LONG dừng tiêu suất). `DR-D9-02` (`6e42463`) giữ nguyên hiệu lực; chưa viết dòng mã nào. |
| TD-0290 | 🚪 **`DR-SONG-CON-01` — cổng sống còn EXPLORE, luật dừng viết trước** | ✅ | — | DR commit RIÊNG và TRƯỚC script/artifact TD-0291: arm `Z0-T1` LONG tham số hiện hành · EXPLORE 88 mã `[T0,T2)` · đơn vị `R_trien_khai` · `M` theo công thức `td0184` · bốn luật (không kết luận / DỪNG / ĐI / không tiêu suất) · hạn chế (không 5m, EXPLORE ≠ pool) · luật chạy lại. **0 trial** ✅ **Xong 17/09/2026** — `dd76a61`, commit TRƯỚC mọi script/artifact TD-0291. Luật bốn nhánh áp máy móc trên số gộp; `M` dùng `N = 114` và ghi `M_union` pending nếu TD-0261 chưa đo. **0 trial.** |
| TD-0291 | **Phép đo cổng sống còn** — `docs/du-lieu-do/do_td0291_song_con_explore.py` + `td0291-song-con-explore.json` | ✅ | TD-0290, TD-0283 ✅ (công thức rủi ro đã triển khai) | Dùng lại khung `do_td0193_*` và `wfo/lenh`; artifact ghi `n`/`mean`/`std`/khoảng tin cậy/`M` (N = 114, `M_union` pending nếu TD-0261 chưa đo) theo CALIB · WFO · gộp, kết luận áp **máy móc** theo `DR-SONG-CON-01` §4. `merge-base` DR trước artifact. Sổ trial không thêm dòng. Kết luận trình chủ dự án ✅ **Xong 17/09/2026** — artifact `a634702` (DR §8 `ed25bbd` đứng trước, kiểm `merge-base`). **Kết luận luật 4: KHÔNG TIÊU SUẤT lúc này — trình chủ dự án.** `Z0-T1`, EXPLORE CALIB `[T0,T1)`, 65 mã / 49,28 mã-năm: **n = 162 · mean `R_trien_khai` −0,0015 · KTC95 [−0,177; +0,175] · M = 0,403** (N = 114, `M_union` pending). Điểm quyết định: **cận trên +0,175 < M** — nhưng KTC chứa 0 ⇒ *"không có bằng chứng về lợi thế"*, KHÔNG phải *"có bằng chứng không có lợi thế"*. `Z0-T0` chẩn đoán: n = 631 · mean −0,060 · KTC95 [−0,159; +0,038]. 🔴 **Ba lượt, khai đủ:** lượt 1 (`0c474ed`) cả hai arm `unreadable` (exception bị nuốt ⇒ lộ TD-0294), lưu `020a37b`, 0 con số; lượt 2 crash (biến `t0` bị đè, `91b9852`), 0 con số; lượt 3 hợp lệ. Trước §8, một lượt THỬ MÁY 3 mã đã in số cả CALIB lẫn WFO (n = 12) — khai DR-009 trong artifact. Nhãn `vai_tro` trong artifact còn ghi "§4" (đúng là §8), không sửa tay. Kiểm-có-răng luật: DỪNG theo cận dưới ⇒ bản đầu 22/22 XANH (lớp canh cùn) ⇒ thêm ca KTC cắt qua 0 ⇒ 1 đỏ. Full suite `a634702`: **2219 passed, 0 failed**. **0 trial**, sổ 13 dòng. |
| TD-0292 | **Test khoá: file đã theo dõi trong cây đo có `\r\n` trên đĩa ⇒ đỏ** | ✅ | — | ✅ `99e1f72` — Docker full suite **2283 passed, 0 failed** (3 lượt, 17/09/2026); phá thật `config/dof_inventory.yaml` CRLF ⇒ đúng 1 đỏ. Trước khi cài: 31 file tracked CRLF/lẫn trên đĩa đã về LF (blob trùng HEAD); đo được `update-index --refresh` KHÔNG gỡ M ảo, `git add` gỡ được · Phạm vi: `src/ tests/ entrypoints/ config/ registry/schemas/` + `docs/decisions/DR-*.md` có khối băm. 🔴 **Lý do ghi đúng:** (a) `git status` báo `M` ảo sau khi chuẩn hoá ⇒ mọi `close_dN_gate` từ chối vì cây đo bẩn (đã xảy ra 17/09); (b) `sha256_of()` đọc nhị phân **nhạy** đuôi dòng nếu sau này file văn bản vào `hash_many`. **KHÔNG** vì `config_hash` — `loader.py` đọc `read_text()` nên hash sổ bất biến với đuôi dòng (đã đo LF/CRLF cùng `adc61c58…`). Kiểm-có-răng: một file CRLF tạm trong `config/` ⇒ đỏ |
| TD-0293 | **`test_td0143` quá hạn 10 s khi tải nặng — ĐO TRƯỚC, SỬA SAU** | ✅ | — | ✅ `464dc89` — thời hạn **giữ 10 s, không đổi mã**: `status` max 1,32 / 1,50 / 1,53 s (nhàn / 1 suite / 2 suite chồng, n = 40/79/72), công thức viết trước ra đúng 10. 🔴 Ca đỏ gốc **không tái hiện** được bằng tải suite — nguyên nhân chưa biết · Đo độ trễ `git --no-optional-locks status --porcelain` trong container: nhàn · 1 suite chạy · 2 suite chồng, ≥ 30 mẫu mỗi mức, artifact `docs/du-lieu-do/`; thời hạn mới **suy từ số đo** (ghi công thức), không đoán. Thư mục chưa theo dõi đã kiểm nhỏ (9–73 file) ⇒ không phải nguyên nhân |
| TD-0294 | 🔴 **Vá: nến xác nhận §3.3b đóng ≤ `sl_zone` vẫn được xác nhận** — lộ ra khi đo TD-0291 (cả hai arm `unreadable`: `SizingError('R_eff phải > 0')` bị Freqtrade nuốt). Đo phễu EXPLORE (0 trial): **20/2.102** xác nhận có `p1 ≤ sl` (loại A), `Z0-T1` 7 tín hiệu; loại B (`p1 > sl ≥ p_avg`) = 0. `r_eff_plan` dùng `p_avg` ba tranche nên **che một phần** loại A ⇒ ~4/7 lệnh **mở dưới SL**, số còn lại mất im lặng; xác nhận ma còn **chiếm chỗ** `trung_nen` của zone lành (chẩn đoán phiên `-30`). Gốc: huỷ zone đo ở 4H đóng, xác nhận quét 1H | ✅ | — | Chủ dự án chốt 17/09/2026: **chặn tại bước xác nhận** — `close(C) ≤ sl_zone` (SL kiểu zone, độc lập arm, `ZoneAbsorption.py:492`) ⇒ zone KHÔNG có xác nhận, kiểm TRƯỚC `trung_nen` (không chiếm chỗ), không quét lại C khác (MT-22 A). Đếm vào log `XAC_NHAN_3_3B`. Test 3 vế + slot: Z0 thủng · Z1 cùng zone thủng · ca `p_avg` che · zone lành cùng C được xác nhận. Kiểm-có-răng: gỡ điều kiện ⇒ đỏ. Tập lệnh ĐỔI ⇒ số đếm EXPLORE cũ không so trực tiếp (`DR-D4-08` §8). `MT-55` (arm entry đơn định cỡ theo `p_avg` ba tranche) ghi nợ qua "chuẩn hóa và lưu", KHÔNG sửa ở đây ✅ **Xong 17/09/2026** — `0afbc70`. `close(C) ≤ sl_zone` ⇒ không phải xác nhận (trước khi gán `xac_nhan[c]` ⇒ không chiếm chỗ; trước kiểm `trung_nen` ⇒ đếm đúng); đếm `thung_sl` vào log. Tách `trade_plan.sl_kieu_zone()` — `tinh_ke_hoach` và `_xac_nhan_3_3b` cùng gọi (gợi ý (a) phiên `-30`). 🔴 **Chẩn đoán ban đầu của phiên -33 SAI** (giả thuyết "p_avg dưới SL", loại B) — đo phễu EXPLORE: loại A 20/2102, loại B **0**; chẩn đoán đúng của `-30` (lệch độ phân giải 4H/1H). 9 ca khoá (cô lập bộ quét). Kiểm-có-răng: gỡ điều kiện ⇒ **6 đỏ**; đảo thứ tự ⇒ **1 đỏ** (thứ tự chỉ quyết ĐẾM). Full suite `a634702`: **2219 passed, 0 failed**. Tập lệnh ĐỔI ⇒ số đếm EXPLORE cũ không so trực tiếp. ⏳ `MT-55` (arm entry đơn định cỡ theo `p_avg` ba tranche) + mâu thuẫn `p1 < p2` cho arm DCA (phiên `-30`) chờ "chuẩn hóa và lưu". **0 trial.** |
| TD-0295 | 🚪 **`DR-IQ-01` — tạm dừng tiêu suất Zone Absorption LONG + đường thoát (d) cho cửa chọn Idea Queue** + file tiêu chí quý **`DR-Q4-2026-tieu-chi-chon-y-tuong.md`**. Căn cứ: TD-0291 luật 4 (`a634702`); D4 không thể FAIL (`std_R` đo 1,14 > 0,466, `DR-D4-12` §2) ⇒ không có phán quyết L2/L3 ⇒ `DR-Q3-2026` §2 khoá cửa chọn vĩnh viễn (`MT-57`) | ✅ | TD-0291 ✅ | Hai file commit RIÊNG, TRƯỚC mọi đơn chọn: tạm dừng (phạm vi dừng/tiếp, không tuyên bố L3, điều kiện nối lại) · điều kiện (d) + khai điểm yếu *"viết sau khi thấy kết quả"* · `DR-Q4-2026`: `HAN_NGACH_CHON: 1`, tiêu chí `TC-Q4-2026-01…04` chép NGUYÊN nội dung Q3 (máy TD-0120 đòi mã đúng quý); `DR-Q3-2026` append-only ⇒ cửa chọn mở sớm nhất **01/10/2026**. E6 audit sổ thật exit 0. **0 trial** ✅ **Xong 17/09/2026** — `0db8587` (`DR-IQ-01`) + `34a5f43` (`DR-Q4-2026`, `HAN_NGACH_CHON: 1`, không chứa số kết quả Tool D). Vá 5 phản biện phiên `-30` TRƯỚC commit: danh sách loại trừ cơ chế Z-1…Z-5 (gồm IQ-0001) · nối lại ZA bất đối xứng · CHỌN do phiên IDEA sạch (DR-009) · hiệu lực Q4. E6 sổ thật exit 0; 72 ca Idea Queue xanh. **0 trial.** |
| TD-0296 | **Nộp giả thuyết sửa cấu trúc chốt lời/cắt lỗ vào Idea Queue** (cửa NỘP, `E6 --nop-y-tuong`) | ❌ | TD-0295 | `data_source = EXPLORE`, căn cứ là hình dạng lệnh quan sát được (không phải giá trị tham số); qua kiểm trùng `mechanism` (TD-0126) — KHÔNG được là Zone Absorption đổi tên; trần nhập 10/quý. **KHÔNG chọn** — chọn theo `TC-Q4-2026` từ 01/10. **0 trial** ❌ **HUỶ 17/09/2026** (`DR-IQ-01` §3.2): giả thuyết sinh từ kết quả Tool D mà phiên `-33` đã đọc ⇒ `TOOL_D_RESULTS` loại thẳng (spec `:3062`, DR-009), và thuộc họ Z-4. Ý tưởng mới phải do phiên IDEA sạch nộp — chủ dự án sắp xếp. |
| TD-0297 | **Ghi `MT-55` · `MT-57` · `MT-58` vào `back-end-note.md` mục 7** | ✅ | TD-0295 | ✅ `e97e325` ("chuẩn hóa và lưu" 17/09/2026; kèm nối `MT-53` ✅ (b′); `MT-54`/`MT-56` của `-01` để trống) · Qua lệnh "chuẩn hóa và lưu" (N9, trả lời ba câu trước): `MT-55` arm entry đơn định cỡ theo `p_avg` ba tranche (ghi nợ, không sửa — đường ống dừng) · `MT-57` ngõ cụt quản trị (giải bằng `DR-IQ-01`) · `MT-58` tranche 2 có thể cao hơn tranche 1 ở arm DCA (`p1 = min(zh, close(C)) < p2 = mid`, phiên `-30`) |


## Khối 25 — Rổ pool theo từng giai đoạn (mở 17/09/2026, thi hành `DR-D1-05`)

> **Vì sao khối này tồn tại:** chủ dự án yêu cầu thực hiện `DR-D1-02` §6 (*"đưa rổ `T1` thành rổ sản xuất"*). Khảo sát
> 17/09/2026 cho thấy thay thẳng `config/pool.yaml` là sai hình. **Không** mã nào đọc `pool.yaml` khi chạy, và mỗi tập
> dữ liệu cần rổ đúng tại mốc của nó: CALIB→`T0` (`DR-D5-01` §1), WFO→`T1`, LOCKBOX→`T2`, live→hôm nay.
> **Chủ dự án chốt `DR-D1-05` (`8cec5cf`):**
> - rổ theo giai đoạn; `pool.yaml` **không đổi** (rổ hôm nay + sổ 4 suất B0);
> - lockbox xử khi nối lại D8 (`MT-60`);
> - dựng rổ `T0`/`T2` bằng đúng tiêu chí B0 = **0 suất** (H1-D, `spec:438`), **không** được đọc thành *"chọn lại pool
>   miễn phí"*.
>
> Mã đã nhắn phiên `-a2` trước khi ghi (N12 mục 6). Đính chính kèm: khối `explore:` có **426** mã, không phải 430.

| Mã việc | Nội dung | TT | Phụ thuộc | Tiêu chí XONG |
|---|---|---|---|---|
| TD-0298 | 🚪 **`DR-D1-05` — rổ theo giai đoạn, 0 suất, đính chính 426** | ✅ | — | Commit `8cec5cf` TRƯỚC mọi dòng mã khối này; đính chính tại chỗ `DR-D1-02` §2 + `DR-D1-03` §0 |
| TD-0299 | **Hàm chọn rổ theo tập dữ liệu** `src/tool_d/pool_giai_doan.py` + test khoá | ✅ | TD-0298 | CALIB→`pool_t0`, WFO→`pool_t1`; `LOCKBOX` ⇒ từ chối nêu `MT-60`; tập lạ / file rổ thiếu ⇒ từ chối; **không bao giờ** trả `config/pool.yaml`; kiểm có răng ✅ XONG 17/09/2026: `4f15353` — 11 ca test khoá, kiểm có răng 2 chốt ⇒ 2 đỏ. |
| TD-0300 | **Tổng quát hoá bộ sinh E7 theo mốc + sinh `config/pool_t0.yaml`** | ✅ | TD-0298 | `E7 --ro-t0 [--ghi]` (giữ nguyên `--ro-t1`); đối chiếu **khít** `td0231["pool_dung_tai_t0"]` (164 mã); loại mã có dữ liệu EXPLORE, xét tại `T0`; 0 trial; chạy thật trong Docker + commit file rổ; test cũ `--ro-t1` vẫn xanh ✅ XONG 17/09/2026: bộ sinh `4f15353`+`5954e14`; `config/pool_t0.yaml` 143 mã (`e538518`), khít TD-0231 (164), loại 21 mã EXPLORE, 0 trial. |
| TD-0301 | **Dữ liệu `[T0,T1]` cho rổ `T0`** trong `user_data/data/pool_t0/futures/` | ✅ | TD-0300 | 5 loại/mã (`1h`/`4h`/`1d` futures, `1h` mark, `1h` funding), **không 5m** (`TD-0252`); chép từ `binance/` rồi `pool_t1/`, tải `download-data` mã còn giao dịch, nhập kho mã đã huỷ; cắt `≤ T1` và tại mốc ngừng giao dịch; H19 quanh mỗi lượt tải; `E8 --ro-t0-kiem` PASS; full suite Docker 0 failed ✅ XONG 17/09/2026: 715 file (143 × 5) — 66 chép · 43 `download-data` · 34 nhập kho (18 cắt tại mốc ngừng, `87858ce`); H19 PASS; `E8 --ro-kiem --moc t0` PASS; full suite **2307 passed, 0 failed** tại `87858ce`. |
| TD-0302 | ⏸ **Rổ `T2` + dữ liệu/niêm phong lockbox đoạn 2** | 🔓 | `MT-59`, `MT-60`, nối lại D8 (`DR-IQ-01`) | ⏸ **TẠM DỪNG** — không làm cho tới khi nối lại D8; phải xong TRƯỚC lần chạm lockbox (`TD-0274`) |
| TD-0303 | **Sửa docstring `src/tool_d/pool_t1.py` 430 → 426** | ✅ | TD-0298 | Không đổi hành vi; đi cùng commit mã đầu tiên của khối ✅ XONG 17/09/2026: `4f15353`. |

---

## Khối 26 — Đường thoát `DR-IQ-01`: suất (d) dùng được + máy lockbox đúng rổ (mở 18/09/2026)

> **Vì sao khối này tồn tại:** `DR-IQ-01` (17/09/2026) tạm dừng tiêu suất Zone Absorption LONG và
> **tự viết sẵn đường thoát** — suất **(d)**, đúng 1 suất Ngân sách A, cửa mở **01/10/2026**, đóng
> **31/12/2026**, **không dồn**. Rà 18/09/2026 thấy hai chỗ làm đường thoát đó không đi được:
> - 🔴 **Suất (d) chưa có ứng viên nào.** `registry/idea_queue.jsonl` có đúng 1 đơn (`IQ-0001`), và
>   nó thuộc **Z-2** của `DR-IQ-01A` ⇒ bị loại khỏi chính suất này. `DR-IQ-01:127-128` đã cảnh báo:
>   không có phiên IDEA sạch nộp đơn mới trong quý 4 thì **suất (d) hết hạn không ứng viên**.
> - 🔴 **Ứng viên mới cần lockbox mới đúng rổ**, mà `MT-59`/`MT-60` chưa giải và `verify_seal()`
>   **không nhìn thấy** lỗi rổ nên sẽ im lặng.
>
> **Bàn giao:** `MT-59`, `MT-60`, `TD-0302` do phiên `-01` bàn giao cho phiên `-a2` (18/09/2026);
> 🔴 *Ghi chú 18/09/2026 (N12 mục 7):* `-01` = mã phiên `0ea66758`; `-a2` **lúc bàn giao** = mã phiên
> `55661c40`, **sau khởi động lại mang tên `-2b`** — tên `-a2` nay thuộc về một phiên KHÁC (mã `c95baba3`).
> Người giữ Khối 26 đọc từ 🔒 + dòng `Phien:` của commit khoá, không từ tên ở dòng trên.
> mã `TD-0304`…`TD-0310` + Khối 26 + `DR-LOCKBOX-01` đã nhắn trước và được xác nhận còn trống (N12 mục 6).
>
> 🔴 **Phần B (`TD-0305`…`TD-0310`) CHƯA ĐƯỢC BẮT ĐẦU** — cần chủ dự án (a) gỡ ⏸ cho việc lockbox
> (`TD-0302` đang ⏸ theo `DR-IQ-01`, nối lại cùng D8) và (b) trả lời 8 câu trong `DR-LOCKBOX-01`.
> Câu nặng nhất: *"đoạn niêm phong 2"* ở đây là **MỞ RỘNG RỔ**, trong khi `MAX_SEGMENTS = 3` và
> `L-Z13` sinh ra cho nghĩa **GIA HẠN INCONCLUSIVE** (`DR-011:3356-3362`, dữ liệu MỚI, tối đa 2 lần).
> Dùng lẫn là tiêu suất gia hạn cho một việc khác nghĩa. Phiên `-01` độc lập nêu đúng lo ngại này.
>
> 🔑 **Đo được, phiên `-93` bàn giao 18/09/2026 (đọc mã + artifact đã commit, 0 trial) — sửa một
> phần lo ngại trên:** ở **tầng máy** việc này **KHÔNG** tiêu suất gia hạn. `access_log.py:98` áp
> trần lên **số BẢN GHI**, `:104-122` chỉ đòi `seal_path`/`seal_file_hash` đôi một khác nhau, và
> **toàn hàm không đọc khoá `segment` lần nào** (đối chứng `test_lz13:104` — 3 bản ghi seal bất kỳ
> vẫn PASS, chỉ đỏ ở bản ghi thứ 4). Lo ngại vẫn đứng ở **tầng NGHĨA**, và lộ thêm một vết: tên
> `MAX_SEGMENTS` nói *"đoạn"* nhưng thi hành *"bản ghi"*, mà `seal.py:36-38` lại ghi *"tối đa
> segment 3"* ⇒ đi đường này thì gia hạn lần hai mang `segment = 4`, **vượt tài liệu mà không hàm
> nào bắt** vì `segment` chưa bao giờ được validate.
>
> 📊 **Số liệu cho `DR-LOCKBOX-01`, khỏi đo lại:** seal 1 = 510 file = 102 mã × 5 loại, tập mã
> **trùng khít `config/pool.yaml`**, lệch 0 ⇒ đúng rổ đo 09/2026 (CUỐI giai đoạn lockbox).
> `pool_dung_tai_t2` = **94 mã**; **38 mã (40,4%)** thuộc rổ `T2` mà `pool.yaml` không có;
> **46/102 (45,1%)** trong `pool.yaml` không thuộc rổ `T2` ⇒ lệch **gần một nửa**, không phải rìa.
> Rổ `T1`: **55/107** mã có dữ liệu lockbox. Ghép `td0247`+`td0301`: 20 mã có mốc ngừng thật, cả 20
> ngừng trước `T2`, **0/20** lọt vào `pool_dung_tai_t2` — ⚠️ 20 mã là tập **tình cờ có sẵn**, nên
> đây là **tín hiệu, KHÔNG phải kết luận** *"MT-59 không chạm rổ T2"*. Cơ chế: mã chết vẫn có hàng
> nến ngày mốc nhưng `volume = 0`, `pool.py:85` đòi ≥ 15tr ⇒ rơi `explore`. **Cái sai thật** là
> `ung_vien_song` phồng (629 tại `T2`) và `delisted_at = None` sai (`pool_t1.py:205`); 🔑 chốt sai
> đang bị chốt đúng **CHE** — hết che ngay khi ai đó đổi `quote_volume_24h` sang trung bình N ngày
> hoặc gọi `pairlist_over_time()`.
>
> 🔴 **Hai câu hỏi MỚI cho `DR-LOCKBOX-01`, ngoài 8 câu ban đầu:** **(9)** rổ `T2` dùng **5 hay 6
> loại file**? Seal 1 có **5**, KHÔNG có `5m`; nhưng rổ `T1` (WFO) **có** `5m`, `DR-D5-01` ghi
> `--timeframe-detail 5m` là **bắt buộc**, `DR-D9-01:244` dùng ở D9, còn `DR-D0PRE-07` **không nhắc
> `5m` lần nào**. **(10)** 46 mã đã niêm phong nhưng ngoài rổ `T2` — đề xuất **giữ file trên đĩa,
> append-only, cấm dùng** (`verify_seal` là danh sách ĐÓNG, `seal.py:100-101`, nên thêm file cho mã
> mới không làm seal 1 đỏ, miễn không xoá/sửa byte nào của 510 file cũ).
>
> ⚠️ **Nợ riêng, phiên `-93` tìm được, chưa mở mã:** `pool_t1_du_lieu.py:303` — với `moc_cuoi="t3"`
> phép so tháng cho `False` ⇒ **833/864 mã** rơi nhánh `elif`, file thiếu 5 tuần cuối cửa sổ lockbox
> **vẫn PASS**. PASS RỖNG, chỉ nổ khi làm rổ `T2`. Và `build_pool.py:389-390` hardcode `_tai_t1` ⇒
> `config/pool_t0.yaml:182-183` ghi `ung_vien_song_tai_t1: 285` cho rổ `T0` — **lỗi thật đã commit**;
> sửa bộ sinh + đính chính trong DR, **không sửa file đã commit**.

| Mã việc | Nội dung | TT | Phụ thuộc | Tiêu chí XONG |
|---|---|---|---|---|
| TD-0304 | **Làm suất (d) dùng được** — kiểm cửa nộp còn răng + vá chú thích `docs/mau-don-y-tuong.yaml` | ✅ | — | (a) chạy thật trong Docker: đơn thiếu `who_pays` ⇒ TỪ CHỐI ghi, sổ thật **bất biến từng byte**; (b) kiểm-có-răng hai vế trong sổ hộp cát: cùng đơn, khác đúng `who_pays` ⇒ hai kết cục ngược nhau; (c) mẫu đơn trỏ `DR-IQ-01A` + danh sách file phiên sạch được/cấm đọc + bỏ số quý hardcode; (d) mẫu đơn vẫn parse, 14 khoá không đổi ✅ **XONG 18/09/2026** (`d418443`): (a) exit **96**, `sha256 a0e1d000…` **giống hệt trước và sau**; (b) hai vế ngược nhau đúng như đòi — cửa nộp từ chối **đúng chỗ**, không phải từ chối mọi thứ; (c)(d) parse OK, 14 khoá không đổi; test liên quan **63 passed, 0 failed**. 🔑 Không tạo file hướng dẫn mới — nối vào mẫu đơn đã có (một ý, một nguồn, `MT-03`). ⚠️ Phiên làm việc này **đã NHIỄM** theo DR-009 nên chỉ đặt con trỏ tới tài liệu, **không** định hướng nội dung ý tưởng; phiên sạch nên đọc thẳng `DR-IQ-01A` thay vì tin phần tóm tắt |
| TD-0305 | 🚪 **`DR-LOCKBOX-01`** — quyết định về rổ lockbox (10 câu) | ✅ | Chủ dự án gỡ ⏸ + trả lời | Commit **RIÊNG và TRƯỚC** mọi dòng mã của phần B ✅ **XONG 18/09/2026** (`bd646ea`, kèm đính chính tại chỗ `DR-D1-05` §5b). Chủ dự án chọn qua phiên `-93`/`-2c`, xác nhận lại ở phiên `-2b`. Gỡ ⏸ cho `TD-0306/0307/0309`; **giữ ⏸** `TD-0308/0310` tới D8 |
| TD-0306 | **Đo lại khoảng tồn tại → artifact MỚI** (giải `MT-59`) | ✅ | TD-0305 | Dùng lại `pool_t1_du_lieu.moc_ngung_giao_dich()`, **không** viết khuôn đo mới; artifact mới cùng schema `khoang_ton_tai` + khối `doi_chieu_td0230`; **0 trial**, **KHÔNG ghi dòng `CTRL`** — đo trên kho công khai `data.binance.vision` (metadata SÀN), không đọc một nến pool nào, đúng tiền lệ `TD-0230`/`0231`/`0247`/`0301`; 🔴 **không sửa** `td0230`/`td0247`. 🔑 **Chốt nguồn ĐỘC LẬP với kho** (phiên `-93`): mã bị kết luận *"chết trước `T2`"* mà **vẫn đang `TRADING` trong `exchangeInfo` hôm nay** ⇒ mâu thuẫn ⇒ **DỪNG** — mã còn niêm yết thì không thể đã huỷ trước `T2`, không cần giả định gì về kho, và `build_pool.py:326` đã tải `exchange_info` sẵn nên gần như miễn phí. 📤 **Xuất kèm danh sách mã ∈ rổ `T2` có trạng thái `chet_trong_t2_t3`, tách *chết-thật* / *rebrand-merge*** — đây là **đầu vào bắt buộc** cho ca test cắt dữ liệu của `TD-0308`, và không tra ra được trước khi `TD-0306` chạy (chúng nằm trong nhóm 833 mã ghi `thang_cuoi = 2026-08`, đúng nhóm `khoang_ton_tai` không phân biệt được). 📊 Kỳ vọng nền **~3,1–3,4 mã** (λ gộp 0,495 %/tháng, 220 ngày); **0 mã ⇒ P ≈ 4% ⇒ ĐÁNG NGHI, bắt buộc chỉ ra một mã cụ thể còn giao dịch tới `T3` để đối chiếu**, không nhận thẳng. ⚠️ Ba giới hạn phải chép kèm con số: (1) λ gộp dựa trên `p = 0,074` — nghĩa *"chưa đủ bằng chứng để nói hai nhịp khác nhau"*, **không** phải *"hai nhịp giống nhau"*; (2) tổng `n = 14`, đừng trích `0,495` như hằng số; (3) `[T2,T3]` là đoạn BTC **−53%**, sập sâu là lúc alt bị huỷ nhiều nhất ⇒ **3,1–3,4 là CẬN DƯỚI** ✅ **XONG 18/09/2026** (`b466603` mã+artifact · `f9df20b` chốt cây sạch · `443c801` sinh lại artifact từ cây sạch): đo 94 mã, 102 file tháng, **0 trial**. **`MT-59` làm lệch `pool_dung` tại `T2`: 0 mã** — mã chết có volume 0 nên tự trượt ngưỡng 15tr; đường sản xuất VẪN phải sửa ở `TD-0307` (đúng nhờ may). **3 mã chết trong `[T2,T3]`**: `FUNUSDT` 2026-04-15 · `TONUSDT` 2026-06-23 · `IPUSDT` 2026-06-28 — đúng kỳ vọng nền ~3,1–3,4; đầu vào cắt dữ liệu của `TD-0308`. 0 không đo được, 0 mâu thuẫn với `exchangeInfo` (528 TRADING). Kiểm-có-răng 4 phép phá, dự đoán trước, cả 4 khớp. ⚠️ Lần chạy đầu ghi `git_sha` trỏ commit KHÔNG chứa mã sinh số (đúng lỗi cổng D1/D2/D3) — đã thêm chốt từ chối ghi khi cây bẩn (exit 6, kiểm có răng) và sinh lại; hai lần chạy trùng khít. ⚠️ Chưa tách được rebrand ĐƠN LẺ — chỉ phát hiện merge đồng loạt (ngừng cùng giờ; 3 mã trên không cùng giờ); không ảnh hưởng `TD-0308` |
| TD-0307 | **Dựng rổ `T2`** (`config/pool_t2.yaml`) | ✅ | TD-0306 | Nới `MOC_RO_HOP_LE` + tham số hoá `NGUON_TD0230` theo mốc (t0/t1 giữ `td0230` để tái lập bằng chứng cũ); 🔴 **KHÔNG đòi khít** `td0231["pool_dung_tai_t2"]` — chính con số đó tính BẰNG `khoang_ton_tai` đang sai, đòi khít là ép kết quả đã sửa khớp lại số mang lệch ⇒ `TD-0306` thành vô nghĩa mà suite vẫn xanh. Thay bằng **phép so BẤT ĐỐI XỨNG hai chiều, một chiều phải RỖNG**: (a) `chỉ-ở-mới` (không có trong 94, xuất hiện ở rổ mới) **phải RỖNG** — sửa `MT-59` chỉ có thể **thu hẹp** (đổi `delisted_at` từ `None` sai sang ngày thật, thu nhỏ `ung_vien_song`), không đường nào **thêm** mã ⇒ khác rỗng là có thứ khác đã đổi ngoài đời sống mã ⇒ **DỪNG vô điều kiện**; (b) `chỉ-ở-td0231` **⊆ (mã-chết-trước-`T2` ∪ mã-EXPLORE-bị-loại)**, không thoả ⇒ **DỪNG**. ⚠️ **So đúng TẦNG kẻo chốt rỗng thành PASS RỖNG:** `td0231["pool_dung_tai_t2"]` = 94 là **TRƯỚC** khi loại mã EXPLORE, còn `config/pool_t2.yaml` là **SAU** (tiền lệ: rổ T0 164→143, rổ T1 116→107) ⇒ phải so ở tầng `KetQuaMoc.pool_dung`, rồi loại EXPLORE ở bước sau. 🔴 **Và chốt (a) CÓ THỂ XANH GIẢ:** nếu `TD-0306` đo đời sống **cũng từ kho `/1d/`** thì với mã thiếu tháng ở BIÊN, `td0231` và `TD-0306` **mù giống hệt nhau** ⇒ mã đó vắng ở CẢ HAI ⇒ `chỉ-ở-mới` vẫn rỗng, rổ vẫn thiếu mã, không đỏ ở đâu. 🔑 Phân biệt phải ghi vào DR, vì `L-Z55` rất dễ bị đọc thành *"hễ cùng nguồn là vô dụng"*: `so_khoa` vs biên là **cùng nguồn KHÁC phép** (phân biệt được — nên phép đo 864/864 có giá trị); `td0231` vs `TD-0306` nếu cùng đọc kho 1d là **cùng nguồn CÙNG phép** (không phân biệt được gì). Vì vậy chốt `exchangeInfo` của `TD-0306` là vế bù **độc lập với kho**, không phải trang trí. Lệch 0 mã cũng là **kết quả phải ghi**, không im lặng bỏ qua; mở `pool_giai_doan.RO_THEO_TAP["LOCKBOX"]`, bỏ nhánh từ chối `MT-60` (🔑 `tests/lock/test_td0299_*.py:27,43` **đảo chiều khẳng định, KHÔNG xoá** — tiền lệ `TD-0150`); 0 trial ✅ **XONG 18/09/2026** (`04aba7f` mã+test · `fd556fc` sinh `config/pool_t2.yaml`, commit ĐẦU TIÊN của file — mốc cho chốt 3 `DR-LOCKBOX-01` §3; lúc commit sổ trial có **0** `CONSUME` ngoài B0/CTRL). **Rổ T2 = 86 mã**: 94 đủ tiêu chí, **khớp chính xác** `td0231` (0 lệch cần giải thích), loại 8 EXPLORE, 0 TRADIFI; FUN/TON/IP **có** trong rổ (Q7). `dung_ro_tai_moc()` dùng mốc ngừng đo thật + fail-closed khi mã lọt rổ mà đời sống chỉ là cận trên (Q3 bước 2); dữ liệu `TD-0230` đi nhánh cũ ⇒ `t0`/`t1` tái lập từng byte. Kiểm-có-răng 5 phép phá, dự đoán trước, cả 5 khớp. 🔴 **LỆCH TIÊU CHÍ CÓ Ý THỨC:** **KHÔNG** mở `RO_THEO_TAP["LOCKBOX"]`, **không** bỏ nhánh từ chối. Tiêu chí đó viết TRƯỚC Khối 27; nay lõi bộ chạy không có cửa chặn LOCKBOX riêng (`test_td0312:203`), `ro_cho_tap()` là cửa chặn DUY NHẤT — mở là gỡ chốt giữa bộ chạy và dữ liệu chỉ chạm một lần. `DR-LOCKBOX-01` cũng không đòi (máy canh lần chạm là của `TD-0310`, trong E4). Chỉ sửa lời thông báo; test mới khoá rằng LOCKBOX VẪN bị chặn khi `pool_t2.yaml` đã có. Đã báo phiên `-a2`. ⚠️ Khối `lockbox:` ở `tier_c` (vii) **chưa thêm** — chuyển sang `TD-0309`, nơi đầu tiên đọc nó (thêm bây giờ là một khoá không ai đọc, tiền lệ TD-0182) |
| TD-0308 | **Dữ liệu `[T2,T3]` cho mã còn thiếu** | 🔓 ⏸ | TD-0307 | 🔴 **6 loại file, có `5m`** (`DR-LOCKBOX-01` Q6 — lockbox chạy đúng cấu hình D5/D9; `5m` của `[T2,T3]` là dữ liệu **chưa ai tải**, không chép lại được); mã sống tại `T2` nhưng huỷ giữa `[T2,T3]` ⇒ **giữ + cắt tại mốc ngừng** (Q7). Chạy trong service **`lockbox`** (service DUY NHẤT thấy thư mục thật — chạy nhầm service ra *"0 file"* chứ không báo lỗi); 🔴 dữ liệu **ở lại `lockbox/data/futures/`**, **KHÔNG** chép sang `user_data/data/pool_t2/` theo khuôn `TD-0301` — chép là đưa dữ liệu lockbox ra ngoài vùng cách ly tầng OS (`ARCHITECTURE.md` 3.1), đúng hình sự cố thư mục `C`+U+F03A; 🔴 `--ro-kiem` ở đây ĐỌC NẾN THẬT ⇒ va `MT-19`, và phải khai tường minh là **không** ghi `lockbox_access.log` (ghi là tiêu mất lần chạm duy nhất); 7 mã đã huỷ không thể có dữ liệu ⇒ **luật khai báo tường minh**, không im lặng bỏ qua |
| TD-0309 | **Vá `verify_seal()` để XÉT rổ** (giải `MT-60`) | ✅ | TD-0307, TD-0316 | Đọc được trường `thay_the` của bản cấp lại: seal bị thay thế **không bao giờ** là seal hiệu lực của đoạn (`DR-LOCKBOX-01` §3). `Seal` thêm `pool = {moc, file, sha256, trading[]}` + `schema: 2`; `kiem_pool_seal()` trả **ba trạng thái** `KHAI_DUNG`/`KHAI_SAI`/`KHONG_KHAI` (N6); 🔴 tương thích ngược **không im lặng** — `KHONG_KHAI` là lỗi trừ khi có trong danh sách miễn đọc từ `tool_d_config.yaml`, và `verify_all_seals()` in **dòng riêng**, không trộn vào PASS ✅ **XONG 18/09/2026** (`3ad5c0d`): `src/tool_d/lockbox/ro_seal.py` — `kiem_pool_seal()` ba trạng thái `KHAI_DUNG`/`KHAI_SAI`/`KHONG_KHAI`; `verify_lockbox()` = băm (`verify_all_seals` NGUYÊN VẸN) + xét rổ; miễn khai đọc từ `tier_c.lockbox.seal_mien_khai_pool` (khối (vii) của DR, thêm ở đây vì đây là nơi đầu tiên đọc nó), miễn vẫn hiện dòng riêng. `seal_hieu_luc_doan_1()` đọc `thay_the`. Ca bắt MT-60 dùng DỮ LIỆU THẬT (510 tên file seal 1 + `pool_t2.yaml`) ⇒ `KHAI_SAI`. E4 thật: `lockbox` PASS + dòng ⚠️ miễn seal 1 · `freqtrade` 89 không đổi. Kiểm-có-răng 6 phép phá, cả 6 khớp. **Full suite 2443 passed, 0 failed**. ⚠️ **Lệch tiêu chí có ý thức:** KHÔNG gộp xét rổ vào `verify_all_seals` — test khoá `L-Z14` (`test_lz14:180`) sẽ đỏ vì một mối lo khác hẳn thứ nó canh; tách để nó giữ nguyên nghĩa. 🔴 **Cho `TD-0310`:** lần chạm PHẢI gọi `verify_lockbox()` + `seal_hieu_luc_doan_1()`, KHÔNG chỉ `verify_all_seals()` — không thì phép xét rổ thành *"thứ được canh không nằm trên đường chạy"* |
| TD-0310 | **Đường mã ghi bản CẤP LẠI của đoạn 1** (`lockbox_seal_1_cap_lai.json`, `DR-LOCKBOX-01` §3) | 🔓 ⏸ | TD-0305, TD-0308, TD-0309 | 🔴 **KHÔNG** ghi `lockbox_seal_2.json` — tên đó dành cho lần gia hạn INCONCLUSIVE thứ nhất (`seal.py:80`); chiếm nó là cấp lại vẫn ăn vào trần 3 đoạn, ngược `DR-LOCKBOX-01` Q5. Fail-closed theo 4 chốt của DR §3: (1) seal 1 **chưa từng bị chạm** (sổ truy cập không bản ghi nào trỏ nó); (2) **chưa có bản cấp lại nào** — tối đa một lần; (3) rổ = nội dung **commit ĐẦU TIÊN** của `config/pool_t2.yaml`, và commit đó có **TRƯỚC** sự kiện `CONSUMED` đầu tiên ngoài `B0`/`CTRL` — sửa rổ sau mốc đó ⇒ từ chối, cần DR mới; (4) trỏ DR cho phép. Bản cấp lại mang `thay_the: "lockbox_seal_1.json"` (seal 1 không sửa được); `verify_all_seals()` phải PASS; `write_seal` từ chối ghi đè; `kiem_pool_seal` phải `KHAI_DUNG`; 🔴 `lockbox_seal_1.json` **bất biến từng byte**. 🔑 `--touch` nhận `--tap LOCKBOX` rồi tự chọn seal hiệu lực của đoạn 1, **không nhận đường dẫn seal** — người vận hành không có cách gõ ra seal 1 (tiền lệ `TD-0127`). ⚠️ **Viết lại 18/09/2026 theo `DR-LOCKBOX-01`:** bản trước thiết kế `--seal-segment N` với chốt `N == len(discover_seals())+1` cho *"đoạn niêm phong thứ 2"* — đúng nghĩa DR đã bác |
| TD-0315 | **Bỏ tham chiếu quý ghi cứng khỏi đầu ra lúc chạy của cửa nộp** (`src/tool_d/ledger/idea_queue.py`) | ✅ | TD-0304 | Thông báo lỗi Ca 2 của `submit_idea()` (chuỗi IN RA lúc chạy, không phải chú thích) ghi cứng *"quý 3/2026 khai `HAN_NGACH_CHON: 0`"* trong khi `DR-Q4-2026` khai **1** ⇒ phiên IDEA sạch điền nhầm `status: SELECTED` sẽ tin cửa chọn đang đóng. Thay bằng câu trỏ sang DR tiêu chí chọn của quý hiện hành, **không in số quý nào**; sửa cả docstring module; 🔴 **không đụng logic** (Ca 2 vẫn chặn `SELECTED`). Test khoá `test_td0315_*` chốt theo **VẮNG MẶT MẪU** `quý [1-4]/\d{4}` + `HAN_NGACH_CHON: \d+`, kèm ca *"vẫn trỏ tới nguồn sự thật"* để bỏ số không thành bỏ luôn đường tra. Kiểm-có-răng trong Docker: chèn lại câu cũ ⇒ đúng **2** ca đỏ (mẫu quý + mẫu hạn ngạch). ⚠️ Mã code TRƯỚC khi khoá — `TD-0314` bị cấp mất cho việc khác trong lúc lập kế hoạch, nên đổi số ✅ **XONG 18/09/2026** (`dd3ff7c`): test mới **5 passed**, nhóm cửa nộp **68 passed** (63 + 5, đúng cộng); kiểm-có-răng ba phép phá, dự đoán TRƯỚC, cả ba khớp đúng số và đúng ca (câu cũ ⇒ 2 đỏ · xoá con trỏ ⇒ 1 đỏ · chép số vào docstring ⇒ 1 đỏ); ca *"Ca 2 vẫn chặn `SELECTED`"* **không** phá riêng vì đã có test `TD-0124` phủ; sổ thật bất biến (`a0e1d000…`) |
| TD-0316 | 🚪 **Tách H17 theo service** — `DR-LOCKBOX-02` (chủ dự án chọn hướng (a), 18/09/2026) | ✅ | TD-0305 | Đo 18/09/2026: E1/E2/E3 gọi `verify_all_seals()` lúc khởi động (`run_backtest.py:164`, `run_wfo.py:125`, `run_ablation.py:68`) mà ở service `freqtrade` dữ liệu lockbox bị che có chủ đích ⇒ 510/510 file `MISSING` ⇒ **exit 89 mọi lần** — E1/E2/E3 không có service hợp lệ nào để chạy trước D9. Ba thứ đã chốt va nhau: cách ly tầng OS (ARCHITECTURE 3.1) · test khoá xác nhận FAIL ở đó là ĐÚNG (`test_touch_lockbox.py:56`) · H17 băm dữ liệu mỗi lần khởi động. Hướng (a): ở service che lockbox, H17 = **file seal không bị sửa** + **cách ly còn hiệu lực** (tấm chắn MỚI lúc chạy — hiện chỉ có trong test); băm dữ liệu (`L-Z14`) chuyển về service `lockbox`. ⚠️ Sổ truy cập (`L-Z13`) **0 chỗ ghi, 0 chỗ đọc** sản xuất, file sổ không tồn tại — dùng nó là dựng mới một đường chưa từng chạy, thuộc lần chạm (`TD-0274`), không phải tái dùng. DR viết TRƯỚC dòng mã ✅ **XONG 18/09/2026** (`82642b9` DR · `f1b26fa` mã+test): `src/tool_d/lockbox/h17.py` — cách ly còn hiệu lực (thử ĐỌC THẬT file seal liệt kê + quét thư mục; đọc được ⇒ **exit 108**) + seal không bị sửa (git sạch, commit ĐÚNG MỘT LẦN) + sổ truy cập (rỗng nghĩa tới `TD-0274`). E1/E2/E3 thôi gọi `verify_all_seals`. **Chạy `main()` THẬT qua Docker, dự đoán trước, cả 4 khớp:** `freqtrade`/E1 → 107 (qua H17) · E2 → 97 · E3 → `NotImplementedError` SAU `kiem_h17` · `lockbox`/E1 → **108** cách ly vỡ. `test_run_backtest_cache` (E1 thật, tiến trình con) đảo chiều 89 → 107 — nay là ca đường-chạy-thật qua cổng H17. 5 test đảo chiều/đổi tên cổng, không xoá (1 chuỗi trong file của phiên `c95baba3`, đã xin phép). Kiểm-có-răng 5 phép phá, cả 5 khớp. **Full suite 2430 passed, 0 failed** |

---

## Khối 27 — Lõi bộ chạy backtest thật + nối `ro_cho_tap()` vào đường sản xuất (mở 18/09/2026, thi hành `DR-BC-01`)

> **Vì sao mở khối này, và vì sao nó KHÔNG phải là nối lại đường ống đang ⏸.**
> `ro_cho_tap()` (TD-0299 ✅, `src/tool_d/pool_giai_doan.py:44`) là hàm chọn rổ theo giai đoạn, có 11
> test khoá, và **chưa có một lời gọi sản xuất nào** — `docs/research-log.md:3095` khai thẳng lý do:
> E1/E2/E3 chưa có bộ chạy backtest thật. Hệ quả: 143 mã rổ `T0` + 107 mã rổ `T1` và 1.357 file dữ
> liệu của Khối 25 **không chảy vào phép đo nào**, và người viết bộ chạy sau này rất dễ tự đọc thẳng
> một file rổ — đúng thứ `DR-D1-05` §1 sinh ra để cấm.
>
> 🔴 **Phạm vi, chủ dự án chốt 18/09/2026:** chỉ dựng **lõi hạ tầng đo** + nối **E1**. Căn cứ là chính
> bảng `DR-IQ-01` §1, dòng ▶ *"Hạ tầng đo … không phụ thuộc chiến lược"* — lõi này phục vụ bất kỳ
> chiến lược nào thắng suất (d), không riêng Zone Absorption. **KHÔNG** viết bộ chạy ablation E3
> (TD-0184 ⏸), **KHÔNG** nối `chay_mot_fold` vào E2 (TD-0286 ⏸ tường minh), **KHÔNG** chạy `--chay`
> trên `pool_t0`/`pool_t1` (đó là *đánh giá cấu hình trên CALIB/WFO* = chạm = tiêu 1 suất trong 114,
> và `seal()` làm nó không hoàn lại được — L-Z53). Toàn khối **0 suất trial**.
>
> 🔑 **Hai quyết định kèm theo, đều đã chốt trong `DR-BC-01`, không phải mặc định lặng lẽ:**
> (1) đặt chỗ trial tính theo **một CẤU HÌNH**, không phải một suất mỗi fold — trái chữ đang có ở
> `entrypoints/run_wfo.py:21-24` và `src/tool_d/wfo/orchestrator.py:24-28`, nên hai docstring đó phải
> sửa; (2) **băm dữ liệu ≠ chạm dữ liệu**, để dòng `RESERVE` mang đủ 7 khoá xuất xứ mà vẫn giữ L-Z52.
>
> **N12 mục 6:** mã `TD-0311`…`TD-0313` + tên `DR-BC-01` đã nhắn phiên `-93` và được xác nhận còn
> trống (18/09/2026). Nhân lần nhắn đó chặn được một va chạm: `-93` định mở `DR-D1-06` + TD-0304/0305
> cho việc mà Khối 26 đã phủ — họ đã rút và gỡ file chưa commit.

| Mã việc | Nội dung | TT | Phụ thuộc | Tiêu chí XONG |
|---|---|---|---|---|
| TD-0311 | 🚪 **`DR-BC-01`** — phạm vi lõi bộ chạy + kế toán suất + `băm ≠ chạm` + `budget_line` không mặc định | ✅ `a15e5c1` | — | Commit **RIÊNG và TRƯỚC** mọi dòng mã (tiền lệ `DR-D4-08`). Phải khai tường minh: cái gì vẫn ⏸; nó sửa chữ nào ở `run_wfo.py`/`orchestrator.py`; giá phải trả của việc nới định nghĩa "chạm" chỉ đúng cho *đọc byte để băm*, không mở cho việc đọc nào khác |
| TD-0312 | ✅ `1bb1051` **Lõi `src/tool_d/bo_chay/`** — `yeu_cau.py` · `moi_truong.py` · `doc_ket_qua.py` · `chay.py` | ✅ | TD-0311 | `L-Z36` giữ đúng 8 file `entrypoints/`. `ro_cho_tap()` là cửa DUY NHẤT lấy rổ — `YeuCauChay` **không có** trường đường dẫn rổ nào ⇒ "bộ chạy tự đọc file rổ" **không biểu diễn được**. `observed_start`/`observed_end` đọc THẬT từ `backtest_start_ts`/`backtest_end_ts` trong zip, **không** từ `timerange` (ngày dự kiến — bẫy TD-0148) và **không** từ `.meta.json` (đơn vị GIÂY vs mili-giây trong zip). Fail-closed khi xin `--timeframe-detail 5m` mà thư mục không có 5m (`pool_t0`: 0 file, `pool_t1`: 107 — đo thật). Cấu hình phủ bằng **thay dòng văn bản**, không `yaml.dump` (dump xoá chú thích và làm `cfg.sha256` không truy được về file đã commit) |
| TD-0313 | ✅ `d2d3549` **Nối E1 `run_backtest.py`** (hiện `NotImplementedError`, `:65`) + sửa 2 docstring nói ngược `DR-BC-01` §2 | ✅ | TD-0312 | 6 cổng cũ (`measurement_guard` → `require_d0_pre_complete` → `assert_cache_none` → `run_audit` → `verify_all_seals`) **không đụng một chữ**; `measurement_guard()` vẫn là lệnh đầu sau parse (N5, không decorator). `--tap` là MỘT chuỗi nuôi ba chỗ (`ro_cho_tap` · `dataset_boundaries_from_config` · `reserve(dataset=)`) ⇒ ba thứ không lệch nhau mà không ai thấy. Mặc định **không chạy**: thiếu `--chay` ⇒ in kế hoạch rồi thoát mã riêng, sổ trial **bất biến từng byte**. Thứ tự AST có test ghim: `reserve` < `chay_mot_luot` < `seal` < `consume` |
| TD-0314 | **Nối `cho_thieu_khung_chi_tiet()` vào lõi, XOÁ chốt `is_file()` yếu** — chốt 5m hiện tại chỉ kiểm file CÓ TỒN TẠI; `DR-D1-05` §3b.4 đòi *"không một giờ 1H nào thiếu nến 5m tương ứng, ở mọi mã"*. File 5m thủng giữa chừng vẫn lọt, và `backtesting.py:1739` (`and pair in self.detail_data`) **không báo gì** — mã thiếu 5m lặng lẽ tụt về 1H trong khi mã khác chạy 5m | ✅ | TD-0312 ✅, TD-0252 (`cho_thieu_khung_chi_tiet()`, `d1a1a7a`) | 🔴 **MỘT câu phải chủ dự án chốt trước khi code:** hàm của TD-0252 nhận **dataframe đã nạp**, còn lõi giao hẳn việc nạp cho tiến trình con ⇒ hoặc **nạp hai lần** (đắt, nhưng phép kiểm ĐỘC LẬP với thứ nó kiểm, và đi đúng `history.load_data(..., startup_candles=0)` mà `Backtesting._load_bt_data_detail()` đi), hoặc **để tiến trình con tự khai** (rẻ, nhưng là lời khai của chính thứ đang bị kiểm — `DR-014` §3 / `MT-10`). Phê giữ **nạp hai lần**, chưa tự chốt. Đối chứng chéo: `E8 --ro-do-phu` (`4ca9a84`) — hai đường độc lập ra cùng số là xác nhận, lệch thì có thứ để truy. **Giữ ĐÚNG MỘT chốt** — xóa `is_file()` khi nối xong, không giữ cả hai |
| TD-0317 | **`E8 --ro-do-phu` nạp KHÔNG lấp** — `entrypoints/backfill_data.py` (`_nap`, TD-0252) gọi `history.load_data` với mặc định `fill_up_missing=True`, nên Freqtrade lấp chỗ thủng giữa chuỗi bằng nến giả và phép đo độ phủ MÙ với đúng loại lỗi nó sinh ra để bắt (đo 18/09/2026: thủng 3 giờ ⇒ lấp 576 nến / rỗng, không lấp 540 nến / bắt đúng 3 giờ). Chủ dự án duyệt sửa 18/09/2026 | ✅ | TD-0314 ✅ (`6dedac7`) | `_nap` truyền `fill_up_missing=False` cho CẢ HAI khung, cùng tham số với `_kiem_do_phu_chi_tiet()` của lõi; test khoá bắt thủng giữa chuỗi qua đường E8 thật + kiểm có răng (đổi về lấp ⇒ đỏ); đo lại rổ `T0` thật ghi kết quả vào cột này; chú thích `nguon` của artifact nói rõ nạp không lấp; full suite Docker xanh · ✅ `d012326`: rổ `T0` thật **143/143 mã, 0 giờ thiếu** (16.555.667 nến 5m, trùng bản cũ); full suite **2452 passed, 0 failed** |

---

## Việc đã biết là sẽ có, chưa mở

| Giai đoạn | Nội dung | Chặn bởi |
|---|---|---|
| ~~D1~~ ✅ **ĐÃ MỞ 07/09/2026** | H20 ✅ (xong ở D0-PRE) · H1-D · H4-D · H13 · H19 → **Khối 9–12** bên trên (TD-0090…TD-0110) | — |
| ~~D2~~ ✅ **ĐÃ MỞ 07/09/2026** | Verify giả định D1–D7 (§9b.2), H15 → **Khối 13** bên trên (TD-0111…TD-0117). L-Z49 (D7) là điều kiện vào D4 | — |
| ~~D3~~ ✅ **ĐÃ ĐÓNG 08/09/2026** | H3-D walk-forward orchestrator → **Khối 14** bên trên (TD-0140…TD-0148), tag `d3-complete`. 🔴 Cổng chứng nhận **bộ điều phối** đúng, **không** chứng nhận đã có kết quả WFO — chưa có bộ chạy backtest thật | — |
| ~~D3.5~~ ✅ **ĐÃ ĐÓNG 08/09/2026** (tag `d3-5-complete`) | 🚪 Cổng sai lệch thước đo (DR-015) — **chặn D4** → **Khối 15** bên trên (TD-0160…TD-0166). 🔴 Chữ "cần testnet" ở đây là đóng khung SAI, xem DR-D35-01 | — |
| ~~D4~~ ✅ **ĐÃ MỞ 08/09/2026** | 🔴 Ablation D0.9, 9 cấu hình × 2 hướng — **blocker B4** → **Khối 16** bên trên (TD-0180…TD-0186). Cả hai điều kiện chặn đã gỡ. 🔴 Phần lớn D4 là **DỰNG arm**, không phải chạy arm — DG1–DG5 chưa có dòng code nào | — |
| ~~D6–D8~~ **ĐÃ MỞ 17/09/2026 (đặc tả NHÁP)** | Niêm phong ứng viên + H2/H5/H14 (D6) · hardening tồn vong H9/H10 + thang drawdown (D7) · tiền đăng ký lockbox + nợ trước go-live (D8) → **Khối 20–22** bên trên (TD-0260…TD-0279), nguồn `DR-D6D8-01` | TD-0260 (chốt DR) |
| ~~D9~~ **ĐÃ MỞ 17/09/2026 (phần 0 trial)** | WFO đủ phân hoạch + PBO/CSCV P0 (H18) → **Khối 23** bên trên (TD-0280…TD-0288), nguồn `DR-D9-01` | TD-0280 (chốt DR) · cổng vào `DR-D9-01` §6.1 |
| D9.5 | Lockbox chạm **đúng một lần** | D9 |
| D10–D12 | Testnet quy mô đầy đủ → dry-run → live vốn nhỏ. 🔴 **Rà soát 09/09/2026 (đối chiếu key learning "tắt liên tục" của Tool A) bổ sung việc phải xong TRƯỚC khi có tiến trình chạy dài đầu tiên/lệnh thật:** (1) rate-limit + circuit breaker cho `binance_public.py` → **TD-0197** (Khối 16, đang code); (2) heartbeat/cảnh báo khi tiến trình Freqtrade ở trạng thái `stopped`/idle mà không ai biết (khác Risk Supervisor §6.6 — giám sát margin/thanh lý, không giám sát "tiến trình có đang chạy") → **TD-0209**; (3) validate fail-closed khi thiếu `BINANCE_API_KEY`/`BINANCE_API_SECRET` lúc entrypoint cần đặt lệnh thật — cảnh báo rõ thay vì bị chặn âm thầm không ai hay — ✅ **TD-0242** (`bbf16f1`, 13/09/2026; hàm thuần, chưa nối entrypoint vì D10-D12 chưa mở). ⚠️ **Ràng buộc cho launcher tương lai** (phiên `-3f`/`-80` nêu 13/09/2026, sau khi `DR-D11-02` đổi D10 sang lệnh LIVE TỐI THIỂU): `validate_credentials_for_live()` phải gọi ở `main()` của entrypoint, KHÔNG BAO GIỜ trong một callback chiến lược (`strategy_safe_wrapper` nuốt exception — `MT-16` vii); và việc nối nó phải **kèm một phép kiểm AST** kiểu `L-Z36`/`guard_ast_check.py` xác nhận vị trí gọi, không chỉ dựa lời khai docstring — nợ đã khai, không phải nợ ẩn. 🔑 **Thiết kế đã phác sẵn** (docstring `validate_credentials_for_live()`, `binance_public.py`, phiên `-3f` 13/09/2026 chỉ đúng tiền lệ `ZoneAbsorption.py:224` — cùng hình dạng "lời khai, không phải bằng chứng"): ALLOW-list (xác nhận nằm trong `main`) chứ không phải deny-list tên callback (bài học MT-08), kiểm-có-răng bằng nguồn giả qua `ast.parse` — KHÔNG cần entrypoint/strategy thật, viết được ĐỘC LẬP với việc launcher tồn tại hay chưa | D9.5 |

---

## Lịch sử thay đổi checklist

| Mã việc | Loại | Nội dung cũ | Nội dung mới | Lý do | Ngày |
|---|---|---|---|---|---|
| TD-0001…TD-0086 | ➕ Thêm mới | — | Khởi tạo backlog D0-PRE, 9 khối, 41 việc | Cuối Giai đoạn 2 của quy trình vibe-code | 06/09/2026 |
| TD-0006, TD-0007 | ➕ Thêm mới | — | Hai việc xử lý `dashboard-ui/` và file HTML thiết kế | Hai thứ này xuất hiện trong thư mục project trong lúc khởi tạo repo, do một tiến trình khác ghi vào — không nằm trong phạm vi đã chốt ở Giai đoạn 1 | 06/09/2026 |
| TD-0001, TD-0002 | ♻️ Sửa đổi | 🔓 | ✅ | Đã hoàn thành trong cùng buổi khởi tạo | 06/09/2026 |
| TD-0007 | ♻️ Sửa đổi | 🔓 | ✅ | Chủ dự án chọn phương án "chuyển vào docs/" trong 3 phương án đề xuất | 06/09/2026 |
| TD-0003 | ♻️ Sửa đổi | 🔓 | ✅ | Chủ dự án cung cấp link repo GitHub, đã thêm remote `origin` và push thành công | 06/09/2026 |
| TD-0018 | ♻️ Sửa đổi | 🔓 | ✅ | Nối `assert_cache_none()` (đã có sẵn ở `gates/cache_policy.py`) vào `main()` của E1 ngay sau `measurement_guard()`; thêm `tests/unit/test_run_backtest_cache.py` (3 ca, gọi E1 qua subprocess vì `entrypoints/` không nằm trên `pythonpath`) | 06/09/2026 |
| TD-0006 | ♻️ Sửa đổi | 🔓 | ✅ | Chủ dự án chọn phương án "repo riêng" trong 3 phương án đề xuất (submodule / repo riêng / gộp thẳng) — lúc này `dashboard-ui/` đã có nội dung thật (8 trang dashboard Tool A/D dựng trên horizon-ui-chakra), không còn là thư mục rỗng. Đã gỡ remote `origin` cũ (trỏ về repo gốc `horizon-ui/horizon-ui-chakra`, nguy cơ push nhầm), chủ dự án cấp link `github.com/nhanle1153/front-end-trading-tool-smart-dca`, đã thêm remote và push `main` thành công | 06/09/2026 |
| TD-0020 | ♻️ Sửa đổi | Cột verify: `docker compose run --rm tests tests/lock -k "lz3[6-9] or lz4[01]"` | Cột verify: liệt kê trực tiếp đường dẫn 6 file test (xem dòng TD-0020) | Lệnh gốc có hai lỗi kỹ thuật, phát hiện lúc chạy thật: (1) `pytest -k` không hỗ trợ character-class regex `[6-9]`/`[01]` — chỉ khớp chuỗi con văn bản, nên biểu thức gốc chọn được **0/79** test kể cả các L-Z đang tồn tại; (2) giới hạn đường dẫn `tests/lock` bỏ sót L-Z38 (TD-0018) — test của nó nằm ở `tests/unit/test_run_backtest_cache.py` (hợp lý vì nó gọi E1 qua subprocess, không phải kiểm tĩnh) và tên hàm/class không chứa chuỗi "lz38" nên không `-k` nào chọn được. Sửa bằng cách liệt kê thẳng 6 đường dẫn file, không phụ thuộc keyword. Không đổi vị trí file của TD-0018 — đó là quyết định hợp lý của người viết, chỉ sửa cách gọi ở cổng | 06/09/2026 |
| TD-0094 | ♻️ Sửa đổi | Nội dung: "E1/E2/E7 (backtest/WFO/ablation)" | Nội dung: "E1/E2/E3 (backtest/WFO/ablation)" | Sai số entrypoint lúc viết task — `E7` thật ra là `entrypoints/build_pool.py` (không liên quan), ablation là `entrypoints/run_ablation.py` = `E3` (`ENTRYPOINT = "E3"`, xác nhận trực tiếp trong file). Phát hiện lúc đọc lại trước khi khoá task để code | 07/09/2026 |
| TD-0094 | ♻️ Sửa đổi | "tự cưỡng chế cận trên timerange" ở E1/E2/E3, verify đòi "entrypoint từ chối chạy" | Thu hẹp phạm vi: nối T0-T3 với `assert_dataset_timerange()` (L-Z55) đã có sẵn, KHÔNG viết cơ chế song song mới; verify đổi thành `pytest -k timerange` + ghi rõ E1/E2/E3 chưa có bước tải dữ liệu thật để nối self-check vào | Lúc bắt tay viết mới phát hiện E1/E2/E3 (Khối 1, TD-0016) chỉ là khung guard, rơi thẳng xuống `NotImplementedError` — không có dataframe nào đã tải để mà kiểm `observed_start/end`. Viết một hàm mới chỉ kiểm chuỗi `--timerange` yêu cầu (chưa kiểm dữ liệu THẬT đã tải) sẽ là cơ chế yếu hơn `assert_dataset_timerange()` đã có sẵn từ trước (L-Z55) — đúng loại lỗi "hai nguồn sự thật" MT-03/MT-08 đã cảnh báo. Không đóng giả một wiring chưa có chỗ để nối | 07/09/2026 |
| TD-0119 → **TD-0128** · TD-0120 → **TD-0129** | ♻️ Sửa đổi | Hai mã bị dùng HAI LẦN cho hai việc khác hẳn (khối D2 vs khối Idea Queue) — vi phạm quy tắc 1 *“số không bao giờ tái sử dụng”* | Cặp **khối D2** đổi số; cặp **Idea Queue giữ nguyên**. Kèm từ điển đổi tên ở đầu Khối 13 | Chọn đổi cặp D2 vì cặp Idea Queue đã ăn vào **tên file test, tên hàm, và mã E6 in ra trong đầu ra thật** — đổi nó là đổi luôn bằng chứng đã ghi. Cặp D2 chỉ nằm trong docstring + ô phụ thuộc, **không có định danh máy đọc nào** | 07/09/2026 |
| TD-0216…TD-0225 | ➕ Thêm mới | — | **Khối 17 — FreqAI: mở lại §0c.2** (10 việc) | Chủ dự án chốt **12/09/2026**: Tool D sẽ dùng FreqAI — ngược với §0c.2 (`tool-d-smart-dca.md:462-473`, "MODULE CẤM TUYỆT ĐỐI") và quy tắc **N3** của `CLAUDE.md` đang cấm vĩnh viễn. Ghi thành backlog thay vì sửa spec (quy tắc 5); xung đột *spec cấm ↔ chủ dự án cho phép* ghi thành **MT-32** theo quy tắc 11, không tự chọn bên. Phạm vi **không** kéo theo `hyperopt`/`L-Z25`/`L-Z37`/Edge | 12/09/2026 |
| TD-0216 | ♻️ Sửa đổi | Nội dung: DR khai phạm vi + ba lý do cấm + điều kiện đóng lại | Thêm: DR phải khai **lý do mở** và **đích danh thứ FreqAI không giải** (`n = 28` vs `n ≥ 319`) | Phiên `[67bb21]` nêu 12/09/2026: nếu không khai, người đọc sau sẽ hiểu Khối 17 thành *"FreqAI là cách gỡ bế tắc D4"* — trong khi một tầng học máy trên 28 mẫu làm overfitting **nặng hơn**. Tiền đề sai đọc xuôi thì nguy hơn tiền đề sai đọc ngược | 12/09/2026 |
| TD-0216…TD-0225 | ♻️ Sửa đổi | Ghi chú đầu Khối 17: *"`adjust_trade_position` rời khỏi đường chạy sản xuất"* | *"`adjust_trade_position` VẪN ở trên đường chạy sản xuất — nó là đường TP1 chốt 50%; chỉ nhánh bơm tranche 2/3 tắt"* | 🔴 **Đính chính** — câu cũ SAI. `ZoneAbsorption.py:886-887` gọi `_xet_tp1()` **TRƯỚC** chốt arm ở `:891`, nên `arm = Z0` chỉ tắt nhánh thêm-tranche. Phiên `[3f7d14]` đọc mã và bác; đã kiểm lại trên đĩa trước khi sửa. Nguy vì nó nằm trong phần *"đọc trước khi viết `DR-FAI-01` §phạm vi"* — tin câu cũ mà bọc/bỏ callback ⇒ TP1 chết im lặng (MT-16 (vii)) | 12/09/2026 |
| TD-0218 | ♻️ Sửa đổi | Cột verify: chạy riêng + PASS ≥ 1 + kiểm có răng | Thêm **điều kiện tiên quyết kiểm bằng thứ tự commit** (DR phải commit TRƯỚC khi chạm test), câu phủ định tường minh về căn cứ KHÔNG hợp lệ, và phạm vi ghi theo **mã test** chứ không theo phạm trù | Phiên `[3f7d14]` đề nghị 12/09/2026. Chiều nguy của ca này **không phải** chốt-quá-chặt như cổng D3, mà là **chốt bị nới đúng một lần vì một lý do thật, rồi lần sau được dẫn lại mà không mang theo lý do**. Nên điều kiện phải nói *"phải có gì TRƯỚC đã"*, không nói *"được nới bao nhiêu"* | 12/09/2026 |
| TD-0219 | ♻️ Sửa đổi | Cột verify: lệnh đo + *"ghi kết quả THẬT vào cột này"* | Kết quả đo thật 12/09/2026: **THIẾU** (`datasieve` vắng ⇒ FreqAI **không import được**, dù thư mục `freqai/` có tồn tại) | Phép đo 0 trial, đã chạy được sau khi Docker Desktop bật. Điều kiện dừng `DR-FAI-01 §4(b)` viết TRƯỚC khi chạy đã **nổ** ⇒ dừng, trình chủ dự án. Ghi vào cột verify vì chính cột đó yêu cầu *"ghi kết quả THẬT, kể cả khi nó là `ModuleNotFoundError`"* | 12/09/2026 |
| TD-0230…TD-0234 | ➕ Thêm mới | — | Năm việc của `MT-34`/`MT-36`/`MT-37`: đo độ lệch sống sót của pool · dựng lại pool ĐÚNG tại `T1` · schema bản ghi arm · phép kiểm cột bảng `.md` · khai phạm vi `Δ_R` | Bảng changelog dừng ở 12/09 nên toàn bộ việc 12–13/09 không có dòng nào — ghi bù. Con số nặng nhất của đợt: tại `T1`, `pool.yaml` 102 mã vs pool ĐÚNG 116, **chung chỉ 55**, `K = 61` (**52,6%**) so với ngưỡng ≲10% viết TRƯỚC khi chạy ⇒ WFO đang đo **sai rổ theo chiều PASS** | 13/09/2026 |
| TD-0235…TD-0244 | ➕ Thêm mới | — | **Khối 18 — Đường tới D11 (Dry-run)** (10 việc) | Chủ dự án yêu cầu rà soát hệ thống và lập danh sách việc pending tới D11. Rà soát ra hai câu chưa ai ghi: **(1)** chạy dry-run hôm nay sinh **0 chỉ số đọc được** — E5 để 22/22 ở `pending` bằng một HẰNG SỐ và Decision Log có **0 người gọi sản xuất**; **(2)** **cổng D4 không bao giờ đóng được** (`MT-38`) vì khoá `d4_complete` đòi 18 trial `B2` CONSUMED mà `DR-D4-01` chỉ cấp 9. Năm mâu thuẫn mới `MT-38`…`MT-42` ghi theo quy tắc 11, **không tự chọn bên**; chủ dự án chốt ghi `MT` và mở mã việc, **CHƯA sửa mã** | 13/09/2026 |
| TD-0250…TD-0258 | ➕ Thêm mới | — | **Khối 19 — D5: Calibration, ngân sách B1** (9 việc) | Chủ dự án yêu cầu triển khai D5 và duyệt kế hoạch 16/09/2026; mở lại quyết định *"HOÃN đặc tả D5–D9"* ngày 13/09. Bảy chốt ghi ở đoạn mở khối (0 trial trước `d4_complete`, `Z0-T1` LONG, 8 tham số, 18 suất, giữ mốc khi hoà, ứng viên `v_min`/`wick_close_upper_frac`, FreqAI độc lập) | 16/09/2026 |
| TD-0250…TD-0258 | ♻️ Sửa đổi | Đoạn mở Khối 19: **8** tham số (gồm `mult_corr_thresholds`), **18** suất; TD-0253 *"8 mục"*; TD-0258 *"18 suất"* | **7** tham số, `mult_corr_thresholds` sang FROZEN; **trần 16** suất; thêm chốt thước đo/ngưỡng/giá trị thử (chốt 4 mở rộng, chốt 8 mới) | Chủ dự án chốt thêm ba câu khi soạn `DR-D5-01` (16/09/2026): loại `mult_corr_thresholds` (thước `R` không nhìn thấy cỡ lệnh), đo nhóm lọc lệnh bằng nhóm lệnh bị thêm/bớt, ngưỡng > 0 sau trừ nhiễu; duyệt bảng giá trị thử | 16/09/2026 |
| TD-0256 | ♻️ Sửa đổi | Chỉ siết `L-Z15` (TUNED ⇒ `trial_id`) | Thêm phần (ii): `MT-18` phương án (b) — `dof_inventory.yaml` mang tên khoá, `L-Z29` so tập hợp tên | Chủ dự án chốt làm đủ (b) khi `DR-D5-01` §8 trình câu (16/09/2026); gộp vào TD-0256 thay vì mở mã mới để giữ trong dải `TD-0250…TD-0258` đã báo các phiên | 16/09/2026 |
| TD-0259 | ➕ Thêm mới | — | Việc sửa test lỗi thời `test_idea_queue_van_rong_chua_mo` (cuối Khối 18) | Phiên `-df` chạy full suite 16/09/2026 ra 1 failed; truy ra test ghim trạng thái *"sổ ý tưởng rỗng"* đã hết đúng từ TD-0226 (`1a00c66`). Chủ dự án duyệt hướng sửa. Mã 0259 thay vì khe 0248/0249 để không giẫm mã có thể đang được dành | 16/09/2026 |
| TD-0253 · TD-0258 · đoạn mở Khối 19 | ♻️ Sửa đổi | Chốt 1–8; TD-0253 *"từ chối ở `registry.reserve()`"*; TD-0258 không phụ thuộc TD-0247 | Thêm chốt 9 (mẫu nhỏ: hai đường hợp lệ rồi chạy mặc định) + chốt 10 (chặn tại cửa, đổi helper 12 file test); TD-0258 phụ thuộc thêm TD-0247 | Chủ dự án hỏi *"mẫu nhỏ thì tăng mẫu được không"* và chốt chỗ chặn khi lập kế hoạch đợt code (16/09/2026) | 16/09/2026 |
| TD-0260…TD-0279 | ➕ Thêm mới | — | **Khối 20 (D6) · 21 (D7) · 22 (D8)** — 20 việc, đặc tả NHÁP `DR-D6D8-01` | Chủ dự án yêu cầu phân tích và đề xuất D6–D8 (spec `:4492` không có dòng nào), duyệt kế hoạch 17/09/2026, chốt Long-only tới live + D7 dùng kịch bản tổng hợp + CTRL. Mở lại phần D6–D8 của quyết định *"HOÃN đặc tả D5–D9"* 13/09. Va chạm mã với phiên `-33` (D9) giải trước khi ghi: `-33` dời sang Khối 23 / `TD-0280…` | 17/09/2026 |
| TD-0280…TD-0288 | ➕ Thêm mới | — | **Khối 23 (D9)** — 9 việc, nguồn `DR-D9-01` | Chủ dự án yêu cầu triển khai D9 (*"WFO đủ fold + PBO/CSCV nâng lên P0"*, spec `:4326`/`:4349` không có đặc tả), duyệt kế hoạch 17/09/2026 với 5 chốt ở đoạn mở khối. Mở lại phần D9 của quyết định *"HOÃN đặc tả D5–D9"* 13/09. Va chạm dãy `TD-0260…` với phiên `-01` (D6–D8) giải trước khi ghi: `-33` dời sang `TD-0280…`/`MT-51…` | 17/09/2026 |
| TD-0184 · TD-0261 | ♻️ Sửa đổi | TD-0261 *"chốt N của D9"*, xếp ở D6 (sau `d5_complete`); TD-0184 không phụ thuộc TD-0261 | TD-0261 là đầu vào **cổng D4**, đo ngay sau TD-0247 và **trước suất B2 đầu tiên**; TD-0184 phụ thuộc thêm TD-0261 | Phiên `-33` nêu; đối chiếu `spec:4267-4268` (§10.2 Nhánh 1, N gộp Tool A *"tại thời điểm chạy GATE"*) xác nhận `DR-D6D8-01` D6.1 đóng khung thiếu. Chủ dự án chốt 17/09/2026 kèm phân vai: `-01` giữ D6–D8 + TD-0261 | 17/09/2026 |
| TD-0289…TD-0293 | ➕ Thêm mới | — | **Khối 24 — Cổng sống còn + gỡ khoá cấu trúc** (5 việc; `DR-D9-02`, `DR-SONG-CON-01`) | Đánh giá tổng hợp 17/09/2026 (phiên `-33`) chủ dự án duyệt: chưa có con số lời/lỗ nào (`MT-27`) trước khi tiêu ~41 suất; `MT-53` khoá Nhánh 1. Kèm hai việc hạ tầng lộ ra ở Khối 23 (CRLF ⇒ `git status` `M` ảo; `test_td0143` quá hạn khi tải nặng). Mã đã nhắn `-30`/`-01` trước khi ghi | 17/09/2026 |
| Khối 20–22 · TD-0261 · TD-0247 | ♻️ Sửa đổi | Khối 20–22 + TD-0261 đang mở; Tiêu chí XONG TD-0247 gồm *"đo lại phễu/n/lệnh-năm Z0-T1/Z0/Z0-T0/Z3"*; ghi *"9 mã explore"* | ⏸ Khối 20–22 + TD-0261 tạm dừng, trạng thái giữ nguyên. TD-0247 bỏ bước đo Zone Absorption, XONG ở rổ + dữ liệu; nối thêm cập nhật `DR-D1-03` + bộ sinh `385cbbb` | Chủ dự án chốt ở phiên `-33` sau cổng sống còn TD-0291 (`DR-IQ-01`), **xác nhận lại ở phiên `-01`** trước khi ghi. `DR-D1-03` đính chính con số 9 → 54 | 17/09/2026 |
| TD-0298…TD-0303 | ➕ Thêm mới | — | **Khối 25 — Rổ pool theo từng giai đoạn** (6 việc; TD-0298 ✅ ngay vì DR đã commit) | Chủ dự án yêu cầu thực hiện `DR-D1-02` §6; khảo sát cho thấy thay `config/pool.yaml` là sai hình; chốt `DR-D1-05` (rổ theo giai đoạn, lockbox ⏸ D8, 0 suất) 17/09/2026 | 17/09/2026 |
| Luật 3 · TD-0235 · TD-0261 · dẫn Khối 23 · dẫn Khối 26 | ♻️ Sửa đổi | Luật 3: *"Chỉ 1 người/1 tab nên bước khóa được nới lỏng"*; bốn chỗ ghi người giữ việc bằng tên phiên (`-01`, `-70`, `-a2`) | Luật 3: câu cũ **gạch ngang, giữ nguyên chữ**, thêm đính chính — khoá bắt buộc + commit riêng + dòng `Phien:`. Bốn chỗ kia: **nối thêm** mã phiên (`0ea66758`, `869c86a0`, `55661c40`), không đổi chữ cũ | Chủ dự án chốt `CLAUDE.md` N12 mục 7 (`c9f72ee`): tên phiên **đổi sau khởi động lại** — đo 18/09 tên `-a2` chỉ hai mã phiên khác nhau, tin nhắn tới `-a2` rơi nhầm phiên. Chỉ sửa chỗ đang làm địa chỉ cho việc còn mở; ~300 chỗ lịch sử giữ nguyên | 18/09/2026 |
| TD-0216…TD-0225 | ♻️ Sửa đổi | 🔒 TD-0216 (khoá mồ côi từ 12/09) · 🔓 TD-0217…TD-0225 | ❌ cả 10 việc, lý do nối vào cột cuối từng dòng; đoạn dẫn Khối 17 thêm ghi chú ĐÃ ĐÓNG; `MT-32` (chỗ giữ cho đầu ra TD-0217) để trống vĩnh viễn | Chủ dự án đóng Khối 17 ngày 18/09/2026 (`DR-FAI-01` `e7638b9`): đường FreqAI-trong-Tool-D đã tự đóng bởi quyết định hạ tầng 12/09; đường giả thuyết riêng cần một ý tưởng có `who_pays`; năm ô quyết định trống 6 ngày; hướng chính nay là suất (d) (`DR-HUONG-01`) | 18/09/2026 |
| TD-0317 | ➕ Thêm mới | — | Sửa `E8 --ro-do-phu` nạp không lấp | Phát hiện khi làm TD-0314 (`docs/research-log.md` 18/09/2026): cơ chế đo độ phủ của E8 mù với thủng giữa chuỗi; chủ dự án duyệt sửa | 18/09/2026 |
